From 6febd5fbf80c4af9538059b91ef969b11d8477af Mon Sep 17 00:00:00 2001
From: Chongyun Lee <licy183@termux.dev>
Date: Tue, 10 Mar 2026 02:01:04 +0800
Subject: [PATCH 7/8] content

---
 .../accessibility_action_browsertest.cc       |  6 +-
 .../browser_accessibility_android.cc          |  1 +
 .../browser_accessibility_manager_android.cc  |  1 +
 .../browser_accessibility_state_impl.cc       |  3 +-
 ...rowser_accessibility_state_impl_android.cc |  4 +-
 ...ross_platform_accessibility_browsertest.cc |  3 +-
 .../dump_accessibility_browsertest_base.cc    |  7 +-
 .../aggregatable_report_assembler.cc          |  3 +-
 .../aggregatable_report_scheduler.cc          |  2 +-
 .../aggregation_service_test_utils.cc         |  3 +-
 .../attribution_resolver_impl.cc              |  7 +-
 .../attribution_src_browsertest.cc            | 19 ++---
 .../attribution_storage_sql.cc                |  1 +
 .../attributions_browsertest.cc               | 21 ++---
 .../sql_query_plan_test_util.cc               |  5 +-
 .../browser/back_forward_cache_browsertest.cc |  6 +-
 ...back_forward_cache_features_browsertest.cc | 10 +--
 .../background_fetch_registration_notifier.cc |  3 +-
 .../background_sync_manager.cc                |  5 +-
 .../browser/bluetooth/advertisement_client.cc |  3 +-
 .../bluetooth_adapter_factory_wrapper.cc      |  3 +-
 .../bluetooth/bluetooth_allowed_devices.cc    |  7 +-
 .../bluetooth_device_chooser_controller.cc    |  3 +-
 .../frame_connected_bluetooth_devices.cc      |  3 +-
 .../bluetooth/web_bluetooth_service_impl.cc   |  9 ++-
 .../browsing_data_filter_builder_impl.cc      |  6 +-
 .../browsing_data_remover_impl.cc             |  3 +-
 .../browsing_data_remover_impl_browsertest.cc |  3 +-
 .../browsing_data/clear_site_data_handler.cc  |  3 +-
 content/browser/browsing_instance.cc          |  3 +-
 .../btm/btm_bounce_detector_browsertest.cc    |  7 +-
 .../browser/buckets/bucket_manager_host.cc    |  3 +-
 .../browser/cache_storage/cache_storage.cc    | 16 ++--
 .../cache_storage_dispatcher_host.cc          |  3 +-
 .../cache_storage/cache_storage_index.cc      |  3 +-
 .../child_process_security_policy_impl.cc     | 13 ++--
 content/browser/client_hints/client_hints.cc  |  1 +
 .../generated_code_cache_browsertest.cc       |  2 +-
 .../content_security_policy_browsertest.cc    |  3 +-
 .../device_posture_registry_watcher_win.cc    |  1 +
 .../devtools/devtools_agent_host_impl.cc      |  9 ++-
 .../browser/devtools/devtools_http_handler.cc |  2 +-
 content/browser/devtools/devtools_session.cc  |  5 +-
 .../devtools_url_loader_interceptor.cc        |  4 +-
 .../browser/devtools/frame_auto_attacher.cc   |  6 +-
 .../devtools/protocol/input_handler.cc        |  9 ++-
 .../devtools/protocol/network_handler.cc      |  4 +-
 .../devtools/protocol/security_handler.cc     |  3 +-
 .../devtools/protocol/target_handler.cc       | 17 ++--
 .../devtools/protocol/tethering_handler.cc    |  3 +-
 .../service_worker_devtools_manager.cc        |  3 +-
 ...shared_storage_worklet_devtools_manager.cc |  3 +-
 .../shared_worker_devtools_manager.cc         |  3 +-
 .../devtools/worker_devtools_manager.cc       |  3 +-
 content/browser/do_not_track_browsertest.cc   | 17 ++--
 .../dom_storage_context_wrapper.cc            |  5 +-
 .../browser/download/download_browsertest.cc  | 39 +++++-----
 .../browser/download/download_manager_impl.cc | 11 +--
 content/browser/download/save_file_manager.cc |  3 +-
 content/browser/download/save_package.cc      |  9 ++-
 .../fenced_frame/fenced_frame_browsertest.cc  | 57 +++++++-------
 .../fenced_frame/fenced_frame_url_mapping.cc  |  5 +-
 ...le_path_watcher_fsevents_change_tracker.cc |  5 +-
 .../file_path_watcher_inotify.cc              | 13 ++--
 ...ile_system_access_directory_handle_impl.cc |  1 +
 ...tem_access_file_writer_impl_browsertest.cc |  1 +
 .../file_system_access_manager_impl.cc        |  3 +-
 .../file_system_chooser_browsertest.cc        | 14 ++--
 content/browser/find_request_manager.cc       |  7 +-
 .../first_party_set_parser.cc                 | 11 +--
 ...irst_party_sets_handler_database_helper.cc |  1 +
 .../web_contents_sensor_provider_proxy.cc     |  3 +-
 content/browser/gpu/gpu_internals_ui.cc       |  1 +
 content/browser/hid/hid_service.cc            | 14 ++--
 content/browser/host_zoom_map_impl.cc         |  5 +-
 .../in_memory_federated_permission_context.cc |  1 +
 .../indexed_db/indexed_db_context_impl.cc     |  5 +-
 .../indexed_db/instance/bucket_context.cc     |  3 +-
 .../browser/indexed_db/instance/connection.cc |  3 +-
 .../browser/indexed_db/instance/database.cc   |  4 +-
 .../instance/leveldb/active_blob_registry.cc  |  3 +-
 .../instance/leveldb/backing_store.cc         |  3 +-
 .../interest_group/auction_nonce_manager.cc   |  3 +-
 .../interest_group/interest_group_auction.cc  |  3 +-
 .../interest_group_browsertest.cc             | 29 +++----
 ...erest_group_private_aggregation_manager.cc |  8 +-
 .../loader/navigation_url_loader_impl.cc      |  5 +-
 .../object_navigation_fallback_body_loader.cc |  1 +
 content/browser/locks/lock_manager.h          |  1 +
 .../media/active_media_session_controller.cc  |  4 +-
 content/browser/media/audible_metrics.cc      |  3 +-
 content/browser/media/audio_stream_monitor.cc |  3 +-
 ...b_capture_target_id_web_contents_helper.cc |  4 +-
 content/browser/media/cdm_storage_manager.cc  |  2 +-
 content/browser/media/media_internals.cc      |  6 +-
 content/browser/media/media_internals.h       |  4 +-
 content/browser/media/midi_host.cc            |  7 +-
 .../session/media_session_browsertest.cc      |  3 +-
 .../media/session/media_session_impl.cc       |  4 +-
 .../browser_memory_consumer_registry.cc       |  3 +-
 content/browser/mojo_binder_policy_applier.cc |  5 +-
 content/browser/navigation_browsertest.cc     |  9 ++-
 .../network/sandboxed_nqe_browsertest.cc      |  4 +-
 .../network/shared_dictionary_browsertest.cc  |  2 +-
 .../trust_token_parameters_browsertest.cc     | 13 ++--
 .../payment_app_content_unittest_base.cc      |  3 +-
 .../payments/payment_app_context_impl.cc      |  3 +-
 .../browser/payments/payment_app_database.cc  |  3 +-
 .../permissions/permission_overrides.cc       |  6 +-
 ..._picture_in_picture_content_browsertest.cc |  4 +-
 content/browser/plugin_list.cc                |  3 +-
 .../posix_file_descriptor_info_impl.cc        |  3 +-
 .../prefetch/prefetch_canary_checker.cc       |  3 +-
 .../prefetch/prefetch_document_manager.cc     |  3 +-
 .../preloading/prefetch/prefetch_service.cc   |  2 +-
 .../browser/preloading/preloading_decider.cc  |  2 +-
 .../prerender/prerender_browsertest.cc        |  7 +-
 .../preloading/prerender/prerender_host.cc    |  3 +-
 .../prerender/prerender_host_registry.cc      |  7 +-
 .../prerender_navigation_throttle.cc          |  4 +-
 .../browser/preloading/prerenderer_impl.cc    |  4 +-
 ...ivate_aggregation_pending_contributions.cc |  5 +-
 .../process_internals_handler_impl.cc         |  2 +-
 .../agent_scheduling_group_host.cc            |  3 +-
 .../renderer_host/back_forward_cache_impl.cc  |  3 +-
 .../renderer_host/compositor_impl_android.cc  |  1 +
 content/browser/renderer_host/cookie_utils.cc |  2 +-
 .../renderer_host/delegated_frame_host.cc     |  1 +
 content/browser/renderer_host/frame_tree.cc   |  7 +-
 .../touch_selection_controller_client_aura.cc | 25 +++---
 ...selection_controller_client_child_frame.cc | 25 +++---
 .../media/media_devices_manager.cc            |  5 +-
 .../media/media_stream_manager.cc             | 14 ++--
 .../media/video_capture_controller.cc         |  9 ++-
 .../renderer_host/media/video_capture_host.cc |  9 ++-
 .../media/video_capture_manager.cc            |  7 +-
 .../renderer_host/mixed_content_checker.cc    |  7 +-
 .../navigation_controller_impl_browsertest.cc |  6 +-
 .../renderer_host/navigation_request.cc       |  6 +-
 .../renderer_host/plugin_registry_impl.cc     |  3 +-
 .../renderer_host/render_frame_host_impl.cc   | 13 ++--
 .../render_frame_host_impl_browsertest.cc     |  6 +-
 .../render_frame_host_manager.cc              | 12 +--
 .../renderer_host/render_frame_proxy_host.cc  |  1 +
 .../renderer_host/render_process_host_impl.cc |  5 +-
 .../render_widget_host_browsertest.cc         |  6 +-
 .../renderer_host/render_widget_host_impl.cc  |  7 +-
 .../render_widget_host_view_base.cc           |  1 +
 .../render_widget_host_view_browsertest.cc    |  3 +-
 ...render_process_host_manager_browsertest.cc |  3 +-
 .../spare_render_process_host_manager_impl.cc |  3 +-
 content/browser/renderer_host/stored_page.cc  |  1 +
 .../embedded_worker_instance.cc               |  4 +-
 .../service_worker_browsertest.cc             | 78 ++++++++++---------
 .../service_worker/service_worker_client.cc   |  3 +-
 .../service_worker_container_host.cc          |  5 +-
 .../service_worker_process_manager.cc         |  3 +-
 .../service_worker/service_worker_registry.cc |  9 ++-
 .../service_worker_script_cache_map.cc        |  5 +-
 .../service_worker/service_worker_version.cc  | 29 +++----
 .../shared_storage_browsertest.cc             |  9 ++-
 content/browser/site_info.cc                  |  6 +-
 content/browser/site_instance_impl.cc         |  6 +-
 .../browser/site_per_process_browsertest.cc   |  7 +-
 .../site_per_process_hit_test_browsertest.cc  | 17 ++--
 content/browser/snapshot_browsertest.cc       |  3 +-
 content/browser/storage_partition_impl.cc     |  1 +
 content/browser/storage_partition_impl_map.cc |  3 +-
 .../background_tracing_manager_impl.cc        |  4 +-
 .../browser/web_contents/web_contents_impl.cc |  7 +-
 .../web_contents_impl_browsertest.cc          | 17 ++--
 .../webauth/authenticator_common_impl.cc      |  4 +-
 .../webauth/authenticator_environment.cc      |  7 +-
 content/browser/webid/accounts_fetcher.cc     | 15 ++--
 content/browser/webid/url_computations.cc     |  8 +-
 .../websockets/websocket_connector_impl.cc    |  5 +-
 .../webui/shared_resources_data_source.cc     |  3 +-
 content/browser/webui/url_data_manager.cc     |  4 +-
 .../browser/webui/url_data_manager_backend.cc |  4 +-
 .../xr/service/browser_xr_runtime_impl.cc     | 15 ++--
 content/browser/xr/service/vr_service_impl.cc | 13 ++--
 .../xr/service/xr_permission_results.cc       |  3 +-
 content/child/font_warmup_win.cc              |  3 +-
 .../child_memory_consumer_registry.cc         |  3 +-
 content/common/color_parser.cc                |  3 +-
 content/common/font_cache_dispatcher_win.cc   |  4 +-
 content/common/input/actions_parser.cc        |  3 +-
 .../memory_consumer_traits_mojom_traits.h     | 25 +++---
 content/common/origin_util.cc                 |  3 +-
 .../webid/identity_url_loader_throttle.cc     |  3 +-
 .../public/browser/internal_webui_config.cc   |  1 +
 .../public/browser/web_contents_delegate.cc   |  5 +-
 content/public/browser/webui_config_map.cc    |  1 +
 content/public/common/content_client.cc       |  3 +-
 .../public/test/back_forward_cache_util.cc    | 12 +--
 content/public/test/browser_test_utils.cc     |  3 +-
 content/public/test/download_test_observer.cc | 13 ++--
 ...e_file_system_access_permission_context.cc |  6 +-
 .../test/fake_service_worker_context.cc       |  3 +-
 .../public/test/mock_render_process_host.cc   |  5 +-
 content/public/test/prerender_test_util.cc    | 10 +--
 .../test/test_download_http_response.cc       |  7 +-
 content/public/test/test_launcher.cc          |  3 +-
 .../annotations/ax_image_annotator.cc         | 11 +--
 .../annotations/ax_image_stopwords.cc         |  3 +-
 content/renderer/agent_scheduling_group.cc    |  4 +-
 .../renderer/dom_serializer_browsertest.cc    |  9 ++-
 .../media/media_permission_dispatcher.cc      |  5 +-
 content/renderer/navigation_client.cc         | 17 ++--
 content/renderer/render_frame_impl.cc         |  1 +
 .../renderer/render_frame_impl_browsertest.cc |  2 +-
 content/renderer/render_thread_impl.cc        |  3 +-
 .../renderer_navigation_metrics_manager.cc    |  1 +
 .../renderer/worker/worker_thread_registry.cc |  3 +-
 .../auction_worklet/auction_v8_helper.cc      |  3 +-
 .../auction_worklet/bidder_worklet.cc         |  1 +
 .../auction_worklet/debug_command_queue.cc    |  3 +-
 .../shell/browser/shell_devtools_bindings.cc  |  5 +-
 .../shell_devtools_manager_delegate.cc        |  3 +-
 .../browser/shell_platform_delegate_aura.cc   |  1 +
 .../browser/shell_platform_delegate_views.cc  | 17 ++--
 content/test/content_browser_test_test.cc     |  5 +-
 .../test/mock_background_sync_controller.cc   |  5 +-
 content/test/mock_clipboard_host.cc           |  4 +-
 .../mock_platform_notification_service.cc     |  3 +-
 content/test/mock_ssl_host_state_delegate.cc  | 11 +--
 ...b_contents_observer_consistency_checker.cc |  7 +-
 .../browser/fake_bluetooth_delegate.cc        |  7 +-
 .../web_test/browser/web_test_control_host.cc |  7 +-
 .../browser/web_test_permission_manager.cc    |  3 +-
 .../web_test_shell_platform_delegate_views.cc |  7 +-
 content/web_test/renderer/test_runner.cc      |  3 +-
 232 files changed, 819 insertions(+), 667 deletions(-)

diff --git a/content/browser/accessibility/accessibility_action_browsertest.cc b/content/browser/accessibility/accessibility_action_browsertest.cc
index 9e3de22060..a882659a8e 100644
--- a/content/browser/accessibility/accessibility_action_browsertest.cc
+++ b/content/browser/accessibility/accessibility_action_browsertest.cc
@@ -2,12 +2,12 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <optional>
 #include <string>
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/strings/escape.h"
@@ -960,8 +960,8 @@ IN_PROC_BROWSER_TEST_F(AccessibilityActionBrowserTest,
       target->GetIntListAttribute(ax::mojom::IntListAttribute::kControlsIds);
   EXPECT_EQ(2u, control_list.size());
 
-  EXPECT_TRUE(std::ranges::contains(control_list, radio1->GetId()));
-  EXPECT_TRUE(std::ranges::contains(control_list, radio2->GetId()));
+  EXPECT_TRUE(base::Contains(control_list, radio1->GetId()));
+  EXPECT_TRUE(base::Contains(control_list, radio2->GetId()));
 }
 
 IN_PROC_BROWSER_TEST_F(AccessibilityActionBrowserTest, FocusLostOnDeletedNode) {
diff --git a/content/browser/accessibility/browser_accessibility_android.cc b/content/browser/accessibility/browser_accessibility_android.cc
index f3141b1fc3..0ceb624d18 100644
--- a/content/browser/accessibility/browser_accessibility_android.cc
+++ b/content/browser/accessibility/browser_accessibility_android.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
diff --git a/content/browser/accessibility/browser_accessibility_manager_android.cc b/content/browser/accessibility/browser_accessibility_manager_android.cc
index c22f594398..f12b28891f 100644
--- a/content/browser/accessibility/browser_accessibility_manager_android.cc
+++ b/content/browser/accessibility/browser_accessibility_manager_android.cc
@@ -7,6 +7,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/i18n/char_iterator.h"
 #include "base/strings/utf_string_conversions.h"
 #include "content/browser/accessibility/browser_accessibility_android.h"
diff --git a/content/browser/accessibility/browser_accessibility_state_impl.cc b/content/browser/accessibility/browser_accessibility_state_impl.cc
index c35bba83bd..e9b0c3fc81 100644
--- a/content/browser/accessibility/browser_accessibility_state_impl.cc
+++ b/content/browser/accessibility/browser_accessibility_state_impl.cc
@@ -12,6 +12,7 @@
 #include "base/check.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
@@ -816,7 +817,7 @@ void BrowserAccessibilityStateImpl::OnWebContentsHidden(
   }
 
   // Add `web_contents` to the list of the last five hidden WCs.
-  CHECK(!std::ranges::contains(last_hidden_, web_contents));
+  CHECK(!base::Contains(last_hidden_, web_contents));
   last_hidden_.push_back(web_contents);
 
   // Create the disabler for this WebContents. The provided callback will be run
diff --git a/content/browser/accessibility/browser_accessibility_state_impl_android.cc b/content/browser/accessibility/browser_accessibility_state_impl_android.cc
index 4055a625ce..e4dd73fec9 100644
--- a/content/browser/accessibility/browser_accessibility_state_impl_android.cc
+++ b/content/browser/accessibility/browser_accessibility_state_impl_android.cc
@@ -4,10 +4,10 @@
 
 #include "content/browser/accessibility/browser_accessibility_state_impl_android.h"
 
-#include <algorithm>
 #include <memory>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/debug/crash_logging.h"
 #include "base/metrics/histogram_functions.h"
@@ -297,7 +297,7 @@ void BrowserAccessibilityStateImplAndroid::
 
     if (RecordAssistiveTechHistogram(service_hash)) {
       has_assistive_tech = true;
-    } else if (std::ranges::contains(kPasswordPackageHashes, service_hash)) {
+    } else if (base::Contains(kPasswordPackageHashes, service_hash)) {
       has_password_manager = true;
     } else {
       has_unknown = true;
diff --git a/content/browser/accessibility/cross_platform_accessibility_browsertest.cc b/content/browser/accessibility/cross_platform_accessibility_browsertest.cc
index aa38f9ce7d..b5c961cad7 100644
--- a/content/browser/accessibility/cross_platform_accessibility_browsertest.cc
+++ b/content/browser/accessibility/cross_platform_accessibility_browsertest.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/strings/escape.h"
 #include "base/strings/utf_string_conversions.h"
@@ -77,7 +78,7 @@ class CrossPlatformAccessibilityBrowserTest : public ContentBrowserTest {
   void RecursiveAssertUniqueIds(const ui::AXNode* node,
                                 absl::flat_hash_set<int>* ids) const {
     ASSERT_TRUE(ids);
-    ASSERT_FALSE(ids->contains(node->id()));
+    ASSERT_FALSE(base::Contains(*ids, node->id()));
     ids->insert(node->id());
     for (const ui::AXNode* child : node->children()) {
       RecursiveAssertUniqueIds(child, ids);
diff --git a/content/browser/accessibility/dump_accessibility_browsertest_base.cc b/content/browser/accessibility/dump_accessibility_browsertest_base.cc
index c5ecd0f0a8..d0b84bf1c9 100644
--- a/content/browser/accessibility/dump_accessibility_browsertest_base.cc
+++ b/content/browser/accessibility/dump_accessibility_browsertest_base.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/path_service.h"
 #include "base/scoped_observation.h"
@@ -58,7 +59,7 @@ namespace {
 bool SkipUrlMatch(const std::vector<std::string>& skip_urls,
                   const std::string& url) {
   for (const auto& skip_url : skip_urls) {
-    if (url.contains(skip_url)) {
+    if (base::Contains(url, skip_url)) {
       return true;
     }
   }
@@ -429,7 +430,7 @@ void DumpAccessibilityTestBase::WaitForExpectedText() {
     bool all_wait_for_strings_found = true;
     std::string tree_dump = DumpTreeAsString();
     for (const auto& str : scenario_.wait_for) {
-      if (!tree_dump.contains(str)) {
+      if (!base::Contains(tree_dump, str)) {
         VLOG(1) << "Still waiting on this text to be found: " << str;
         all_wait_for_strings_found = false;
         break;
@@ -556,7 +557,7 @@ void DumpAccessibilityTestBase::RunTestForPlatform(
     while (wait_for_string) {
       // Loop until specified string is found.
       std::string tree_dump = DumpUnfilteredAccessibilityTreeAsString();
-      if (tree_dump.contains(str)) {
+      if (base::Contains(tree_dump, str)) {
         wait_for_string = false;
         // Append an additional dump if the specified string was found.
         std::vector<std::string> additional_dump = Dump();
diff --git a/content/browser/aggregation_service/aggregatable_report_assembler.cc b/content/browser/aggregation_service/aggregatable_report_assembler.cc
index 38962e63c9..0a734182cf 100644
--- a/content/browser/aggregation_service/aggregatable_report_assembler.cc
+++ b/content/browser/aggregation_service/aggregatable_report_assembler.cc
@@ -11,6 +11,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
 #include "base/memory/scoped_refptr.h"
@@ -122,7 +123,7 @@ void AggregatableReportAssembler::AssembleReport(
   }
 
   int64_t id = unique_id_counter_++;
-  CHECK(!pending_requests_.contains(id));
+  CHECK(!base::Contains(pending_requests_, id));
 
   const PendingRequest& pending_request =
       pending_requests_
diff --git a/content/browser/aggregation_service/aggregatable_report_scheduler.cc b/content/browser/aggregation_service/aggregatable_report_scheduler.cc
index 75a5642f55..d09d0ed9a6 100644
--- a/content/browser/aggregation_service/aggregatable_report_scheduler.cc
+++ b/content/browser/aggregation_service/aggregatable_report_scheduler.cc
@@ -179,7 +179,7 @@ void AggregatableReportScheduler::TimerDelegate::OnRequestsReturnedFromStorage(
   std::erase_if(
       requests_and_ids,
       [this](const AggregationServiceStorage::RequestAndId& request_and_id) {
-        return in_progress_requests_.contains(request_and_id.id);
+        return base::Contains(in_progress_requests_, request_and_id.id);
       });
   for (const AggregationServiceStorage::RequestAndId& request_and_id :
        requests_and_ids) {
diff --git a/content/browser/aggregation_service/aggregation_service_test_utils.cc b/content/browser/aggregation_service/aggregation_service_test_utils.cc
index b51eccf1bf..53a6b6d82f 100644
--- a/content/browser/aggregation_service/aggregation_service_test_utils.cc
+++ b/content/browser/aggregation_service/aggregation_service_test_utils.cc
@@ -18,6 +18,7 @@
 
 #include "base/base64.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -408,7 +409,7 @@ std::vector<uint8_t> DecryptPayloadWithHpke(
       base::as_byte_span(authenticated_info_str);
 
   // No null terminators should have been copied when concatenating the strings.
-  CHECK(!authenticated_info_str.contains('\0'));
+  CHECK(!base::Contains(authenticated_info_str, '\0'));
 
   bssl::ScopedEVP_HPKE_CTX recipient_context;
   if (!EVP_HPKE_CTX_setup_recipient(
diff --git a/content/browser/attribution_reporting/attribution_resolver_impl.cc b/content/browser/attribution_reporting/attribution_resolver_impl.cc
index 448caf7506..bfd4236035 100644
--- a/content/browser/attribution_reporting/attribution_resolver_impl.cc
+++ b/content/browser/attribution_reporting/attribution_resolver_impl.cc
@@ -15,6 +15,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -85,7 +86,7 @@ DestinationLimitResult GetDestinationLimitResult(
   DestinationLimitResult result =
       sources_to_deactivate.empty()
           ? DestinationLimitResult::kAllowed
-          : (std::ranges::contains(
+          : (base::Contains(
                  sources_to_deactivate,
                  StoredSource::Id(RateLimitTable::kUnsetRecordId))
                  ? DestinationLimitResult::kNotAllowed
@@ -834,7 +835,7 @@ AttributionResolverImpl::MaybeCreateEventLevelReport(
   }
 
   if (event_trigger->dedup_key.has_value() &&
-      std::ranges::contains(source.dedup_keys(), *event_trigger->dedup_key)) {
+      base::Contains(source.dedup_keys(), *event_trigger->dedup_key)) {
     return CreateReportResult::Deduplicated();
   }
 
@@ -904,7 +905,7 @@ AttributionResolverImpl::MaybeCreateAggregatableAttributionReport(
   }
 
   if (dedup_key.has_value() &&
-      std::ranges::contains(source.aggregatable_dedup_keys(), *dedup_key)) {
+      base::Contains(source.aggregatable_dedup_keys(), *dedup_key)) {
     return CreateReportResult::Deduplicated();
   }
 
diff --git a/content/browser/attribution_reporting/attribution_src_browsertest.cc b/content/browser/attribution_reporting/attribution_src_browsertest.cc
index d6b185bdf3..accd561f6c 100644
--- a/content/browser/attribution_reporting/attribution_src_browsertest.cc
+++ b/content/browser/attribution_reporting/attribution_src_browsertest.cc
@@ -7,6 +7,7 @@
 #include <vector>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/location.h"
 #include "base/run_loop.h"
 #include "base/strings/strcat.h"
@@ -601,10 +602,10 @@ IN_PROC_BROWSER_TEST_P(AttributionSrcBrowserTest,
                                                register_url1)));
 
   response1->WaitForRequest();
-  ASSERT_FALSE(response1->http_request()->headers.contains(
-      "Attribution-Reporting-Eligible"));
-  ASSERT_FALSE(response1->http_request()->headers.contains(
-      "Attribution-Reporting-Support"));
+  ASSERT_FALSE(base::Contains(response1->http_request()->headers,
+                              "Attribution-Reporting-Eligible"));
+  ASSERT_FALSE(base::Contains(response1->http_request()->headers,
+                              "Attribution-Reporting-Support"));
 
   GURL register_url2 = http_server->GetURL("d.test", "/register_source2");
   ASSERT_TRUE(ExecJs(web_contents(), JsReplace(R"(
@@ -612,10 +613,10 @@ IN_PROC_BROWSER_TEST_P(AttributionSrcBrowserTest,
                                                register_url2)));
 
   response2->WaitForRequest();
-  ASSERT_FALSE(response2->http_request()->headers.contains(
-      "Attribution-Reporting-Eligible"));
-  ASSERT_FALSE(response2->http_request()->headers.contains(
-      "Attribution-Reporting-Support"));
+  ASSERT_FALSE(base::Contains(response2->http_request()->headers,
+                              "Attribution-Reporting-Eligible"));
+  ASSERT_FALSE(base::Contains(response2->http_request()->headers,
+                              "Attribution-Reporting-Support"));
 }
 
 IN_PROC_BROWSER_TEST_P(AttributionSrcBrowserTest,
@@ -641,7 +642,7 @@ IN_PROC_BROWSER_TEST_P(AttributionSrcBrowserTest,
   register_response->WaitForRequest();
   const net::test_server::HttpRequest* request =
       register_response->http_request();
-  EXPECT_FALSE(request->headers.contains("Referer"));
+  EXPECT_FALSE(base::Contains(request->headers, "Referer"));
 }
 
 IN_PROC_BROWSER_TEST_P(AttributionSrcBrowserTest,
diff --git a/content/browser/attribution_reporting/attribution_storage_sql.cc b/content/browser/attribution_reporting/attribution_storage_sql.cc
index 6bb9cb3997..58b594bbe0 100644
--- a/content/browser/attribution_reporting/attribution_storage_sql.cc
+++ b/content/browser/attribution_reporting/attribution_storage_sql.cc
@@ -23,6 +23,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
diff --git a/content/browser/attribution_reporting/attributions_browsertest.cc b/content/browser/attribution_reporting/attributions_browsertest.cc
index 9bd399546e..55b01ce80e 100644
--- a/content/browser/attribution_reporting/attributions_browsertest.cc
+++ b/content/browser/attribution_reporting/attributions_browsertest.cc
@@ -13,6 +13,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/location.h"
 #include "base/memory/scoped_refptr.h"
@@ -226,7 +227,7 @@ struct ExpectedReportWaiter {
     // The embedded test server resolves all urls to 127.0.0.1, so get the real
     // request host from the request headers.
     const net::test_server::HttpRequest& request = *response->http_request();
-    DCHECK(request.headers.contains("Host"));
+    DCHECK(base::Contains(request.headers, "Host"));
     const GURL& request_url = request.GetURL();
     GURL header_url = GURL("https://" + request.headers.at("Host"));
     std::string host = header_url.GetHost();
@@ -256,7 +257,7 @@ struct ExpectedReportWaiter {
     // for report url was not set properly.
     EXPECT_EQ(expected_url, request_url.ReplaceComponents(replace_host));
 
-    EXPECT_TRUE(request.headers.contains("User-Agent"));
+    EXPECT_TRUE(base::Contains(request.headers, "User-Agent"));
     EXPECT_EQ(request.headers.at("Content-Type"), "application/json");
   }
 };
@@ -664,10 +665,10 @@ IN_PROC_BROWSER_TEST_P(AttributionsBrowserTest,
 
   // Verify the navigation request does not contain the eligibility header.
   register_response1->WaitForRequest();
-  EXPECT_FALSE(register_response1->http_request()->headers.contains(
-      "Attribution-Reporting-Eligible"));
-  EXPECT_FALSE(register_response1->http_request()->headers.contains(
-      "Attribution-Reporting-Support"));
+  EXPECT_FALSE(base::Contains(register_response1->http_request()->headers,
+                              "Attribution-Reporting-Eligible"));
+  EXPECT_FALSE(base::Contains(register_response1->http_request()->headers,
+                              "Attribution-Reporting-Support"));
 
   auto http_response = std::make_unique<net::test_server::BasicHttpResponse>();
   http_response->set_code(net::HTTP_OK);
@@ -1936,10 +1937,10 @@ void TestServiceWorker(const char* registration_js,
                     "a.test", "/attribution_reporting/register_source"))));
 
   register_response->WaitForRequest();
-  EXPECT_TRUE(register_response->http_request()->headers.contains(
-      "Attribution-Reporting-Eligible"));
-  EXPECT_TRUE(register_response->http_request()->headers.contains(
-      "Attribution-Reporting-Support"));
+  EXPECT_TRUE(base::Contains(register_response->http_request()->headers,
+                             "Attribution-Reporting-Eligible"));
+  EXPECT_TRUE(base::Contains(register_response->http_request()->headers,
+                             "Attribution-Reporting-Support"));
 }
 
 IN_PROC_BROWSER_TEST_P(
diff --git a/content/browser/attribution_reporting/sql_query_plan_test_util.cc b/content/browser/attribution_reporting/sql_query_plan_test_util.cc
index bbbac65fbe..dff9352168 100644
--- a/content/browser/attribution_reporting/sql_query_plan_test_util.cc
+++ b/content/browser/attribution_reporting/sql_query_plan_test_util.cc
@@ -14,6 +14,7 @@
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/path_service.h"
 #include "base/process/launch.h"
@@ -99,7 +100,7 @@ bool SqlIndexMatcher::MatchAndExplain(const SqlQueryPlan& plan,
       plan_piece.substr(start_pos, end_pos - start_pos);
 
   return std::ranges::all_of(columns_, [index_text](const std::string& col) {
-    return index_text.contains(col);
+    return base::Contains(index_text, col);
   });
 }
 
@@ -154,7 +155,7 @@ void SqlIndexMatcher::DescribeTo(std::ostream* out, bool negated) const {
 // TODO(apaseltiner): This check is not robust, as some "scans" are optimized
 // using an index.
 bool HasFullTableScan(const SqlQueryPlan& plan) {
-  return plan.plan.contains("SCAN");
+  return base::Contains(plan.plan, "SCAN");
 }
 
 }  // namespace
diff --git a/content/browser/back_forward_cache_browsertest.cc b/content/browser/back_forward_cache_browsertest.cc
index 1d4fcfc7f4..40921051fd 100644
--- a/content/browser/back_forward_cache_browsertest.cc
+++ b/content/browser/back_forward_cache_browsertest.cc
@@ -4,13 +4,13 @@
 
 #include "content/browser/back_forward_cache_browsertest.h"
 
-#include <algorithm>
 #include <climits>
 #include <optional>
 #include <string_view>
 #include <unordered_map>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
 #include "base/memory/raw_ptr.h"
@@ -317,8 +317,8 @@ std::string BackForwardCacheBrowserTest::DepictFrameTree(FrameTreeNode* node) {
 bool BackForwardCacheBrowserTest::HistogramContainsIntValue(
     base::HistogramBase::Sample32 sample,
     std::vector<base::Bucket> histogram_values) {
-  return std::ranges::contains(histogram_values, static_cast<int>(sample),
-                               &base::Bucket::min);
+  return base::Contains(histogram_values, static_cast<int>(sample),
+                        &base::Bucket::min);
 }
 
 void BackForwardCacheBrowserTest::EvictByJavaScript(RenderFrameHostImpl* rfh) {
diff --git a/content/browser/back_forward_cache_features_browsertest.cc b/content/browser/back_forward_cache_features_browsertest.cc
index 199c6a8f53..0dec8cd163 100644
--- a/content/browser/back_forward_cache_features_browsertest.cc
+++ b/content/browser/back_forward_cache_features_browsertest.cc
@@ -2,8 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/stringprintf.h"
 #include "base/threading/platform_thread.h"
@@ -1781,16 +1780,15 @@ IN_PROC_BROWSER_TEST_F(BackForwardCacheBrowserTest,
   std::vector<base::Bucket> blocklist_values = histogram_tester().GetAllSamples(
       "BackForwardCache.HistoryNavigationOutcome."
       "BlocklistedFeature");
-  EXPECT_TRUE(
-      std::ranges::contains(blocklist_values, sample, &base::Bucket::min));
+  EXPECT_TRUE(base::Contains(blocklist_values, sample, &base::Bucket::min));
 
   std::vector<base::Bucket> all_sites_blocklist_values =
       histogram_tester().GetAllSamples(
           "BackForwardCache.AllSites.HistoryNavigationOutcome."
           "BlocklistedFeature");
 
-  EXPECT_TRUE(std::ranges::contains(all_sites_blocklist_values, sample,
-                                    &base::Bucket::min));
+  EXPECT_TRUE(
+      base::Contains(all_sites_blocklist_values, sample, &base::Bucket::min));
 }
 
 // Pages with acquired keyboard lock should not enter BackForwardCache.
diff --git a/content/browser/background_fetch/background_fetch_registration_notifier.cc b/content/browser/background_fetch/background_fetch_registration_notifier.cc
index a0f5b77a6d..83a51b02ce 100644
--- a/content/browser/background_fetch/background_fetch_registration_notifier.cc
+++ b/content/browser/background_fetch/background_fetch_registration_notifier.cc
@@ -7,6 +7,7 @@
 #include <map>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "content/common/background_fetch/background_fetch_types.h"
 #include "content/public/common/content_switches.h"
@@ -64,7 +65,7 @@ void BackgroundFetchRegistrationNotifier::AddObservedUrl(
     const std::string& unique_id,
     const GURL& url) {
   // Ensure we have an observer for this unique_id.
-  if (!observers_.contains(unique_id)) {
+  if (!base::Contains(observers_, unique_id)) {
     return;
   }
 
diff --git a/content/browser/background_sync/background_sync_manager.cc b/content/browser/background_sync/background_sync_manager.cc
index 944066323a..919d2aa9f1 100644
--- a/content/browser/background_sync/background_sync_manager.cc
+++ b/content/browser/background_sync/background_sync_manager.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -1530,7 +1531,7 @@ bool BackgroundSyncManager::AllConditionsExceptConnectivitySatisfied(
   if (registration.is_suspended())
     return false;
 
-  if (emulated_offline_sw_.contains(service_worker_id))
+  if (base::Contains(emulated_offline_sw_, service_worker_id))
     return false;
 
   return true;
@@ -2372,7 +2373,7 @@ blink::ServiceWorkerStatusCode BackgroundSyncManager::CanEmulateSyncEvent(
   if (!network_observer_->NetworkSufficient())
     return blink::ServiceWorkerStatusCode::kErrorEventWaitUntilRejected;
   int64_t registration_id = active_version->registration_id();
-  if (emulated_offline_sw_.contains(registration_id))
+  if (base::Contains(emulated_offline_sw_, registration_id))
     return blink::ServiceWorkerStatusCode::kErrorEventWaitUntilRejected;
   return blink::ServiceWorkerStatusCode::kOk;
 }
diff --git a/content/browser/bluetooth/advertisement_client.cc b/content/browser/bluetooth/advertisement_client.cc
index 4672827537..dc71fa6f89 100644
--- a/content/browser/bluetooth/advertisement_client.cc
+++ b/content/browser/bluetooth/advertisement_client.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "content/browser/bluetooth/bluetooth_blocklist.h"
 #include "content/browser/bluetooth/bluetooth_metrics.h"
@@ -151,7 +152,7 @@ void WebBluetoothServiceImpl::ScanningClient::SendEvent(
       if (std::ranges::none_of(
               filter->services.value(),
               [&filtered_event](const BluetoothUUID& filter_uuid) {
-                return std::ranges::contains(filtered_event->uuids,
+                return base::Contains(filtered_event->uuids,
                                              filter_uuid);
               })) {
         continue;
diff --git a/content/browser/bluetooth/bluetooth_adapter_factory_wrapper.cc b/content/browser/bluetooth/bluetooth_adapter_factory_wrapper.cc
index 4f7aadfccf..c631f4daeb 100644
--- a/content/browser/bluetooth/bluetooth_adapter_factory_wrapper.cc
+++ b/content/browser/bluetooth/bluetooth_adapter_factory_wrapper.cc
@@ -8,6 +8,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/no_destructor.h"
@@ -154,7 +155,7 @@ bool BluetoothAdapterFactoryWrapper::HasAdapter(
     WebBluetoothServiceImpl* service) {
   DCHECK_CALLED_ON_VALID_THREAD(thread_checker_);
 
-  return adapter_observers_.contains(service);
+  return base::Contains(adapter_observers_, service);
 }
 
 void BluetoothAdapterFactoryWrapper::MaybeAddAdapterObserver(
diff --git a/content/browser/bluetooth/bluetooth_allowed_devices.cc b/content/browser/bluetooth/bluetooth_allowed_devices.cc
index 632e4ce7aa..1265b40ec4 100644
--- a/content/browser/bluetooth/bluetooth_allowed_devices.cc
+++ b/content/browser/bluetooth/bluetooth_allowed_devices.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/strings/string_util.h"
 #include "content/browser/bluetooth/bluetooth_blocklist.h"
@@ -108,7 +109,7 @@ bool BluetoothAllowedDevices::IsAllowedToAccessService(
   auto id_iter = device_id_to_services_map_.find(device_id);
   return id_iter == device_id_to_services_map_.end()
              ? false
-             : id_iter->second.contains(service_uuid);
+             : base::Contains(id_iter->second, service_uuid);
 }
 
 bool BluetoothAllowedDevices::IsAllowedToGATTConnect(
@@ -125,12 +126,12 @@ bool BluetoothAllowedDevices::IsAllowedToAccessManufacturerData(
   auto id_iter = device_id_to_manufacturers_map_.find(device_id);
   return id_iter == device_id_to_manufacturers_map_.end()
              ? false
-             : id_iter->second.contains(manufacturer_code);
+             : base::Contains(id_iter->second, manufacturer_code);
 }
 
 blink::WebBluetoothDeviceId BluetoothAllowedDevices::GenerateUniqueDeviceId() {
   blink::WebBluetoothDeviceId device_id = blink::WebBluetoothDeviceId::Create();
-  while (device_id_set_.contains(device_id)) {
+  while (base::Contains(device_id_set_, device_id)) {
     LOG(WARNING) << "Generated repeated id.";
     device_id = blink::WebBluetoothDeviceId::Create();
   }
diff --git a/content/browser/bluetooth/bluetooth_device_chooser_controller.cc b/content/browser/bluetooth/bluetooth_device_chooser_controller.cc
index 35ff6873ad..4c097c8398 100644
--- a/content/browser/bluetooth/bluetooth_device_chooser_controller.cc
+++ b/content/browser/bluetooth/bluetooth_device_chooser_controller.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <unordered_set>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -143,7 +144,7 @@ bool MatchesFilter(const std::string* device_name,
 
   if (filter->services) {
     for (const auto& service : filter->services.value()) {
-      if (!device_uuids.contains(service)) {
+      if (!base::Contains(device_uuids, service)) {
         return false;
       }
     }
diff --git a/content/browser/bluetooth/frame_connected_bluetooth_devices.cc b/content/browser/bluetooth/frame_connected_bluetooth_devices.cc
index ccfcca2381..1ecd92942e 100644
--- a/content/browser/bluetooth/frame_connected_bluetooth_devices.cc
+++ b/content/browser/bluetooth/frame_connected_bluetooth_devices.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "content/browser/web_contents/web_contents_impl.h"
 #include "content/public/browser/web_contents.h"
@@ -109,7 +110,7 @@ void FrameConnectedBluetoothDevices::CloseConnectionsToDevicesNotInList(
   std::set<blink::WebBluetoothDeviceId> ids_to_delete;
 
   for (const auto& device_pair : device_id_to_connection_map_) {
-    if (!permitted_ids.contains(device_pair.first))
+    if (!base::Contains(permitted_ids, device_pair.first))
       ids_to_delete.insert(device_pair.first);
   }
 
diff --git a/content/browser/bluetooth/web_bluetooth_service_impl.cc b/content/browser/bluetooth/web_bluetooth_service_impl.cc
index 7a3592cbbb..78ed69c40e 100644
--- a/content/browser/bluetooth/web_bluetooth_service_impl.cc
+++ b/content/browser/bluetooth/web_bluetooth_service_impl.cc
@@ -15,6 +15,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/queue.h"
 #include "base/containers/to_vector.h"
 #include "base/functional/bind.h"
@@ -563,7 +564,7 @@ void WebBluetoothServiceImpl::OnPermissionRevoked(const url::Origin& origin) {
 
   std::erase_if(watch_advertisements_clients_,
                 [&](const std::unique_ptr<WatchAdvertisementsClient>& client) {
-                  return !permitted_ids.contains(client->device_id());
+                  return !base::Contains(permitted_ids, client->device_id());
                 });
 
   MaybeStopDiscovery();
@@ -721,8 +722,8 @@ void WebBluetoothServiceImpl::GattCharacteristicValueChanged(
     BluetoothRemoteGattCharacteristic* characteristic,
     const std::vector<uint8_t>& value) {
   // Don't notify of characteristics that we haven't returned.
-  if (!characteristic_id_to_service_id_.contains(
-          characteristic->GetIdentifier())) {
+  if (!base::Contains(characteristic_id_to_service_id_,
+                      characteristic->GetIdentifier())) {
     return;
   }
 
@@ -2307,7 +2308,7 @@ void WebBluetoothServiceImpl::RunPendingPrimaryServicesRequests(
   }
 
   // Sending get-service responses unexpectedly queued another request.
-  DCHECK(!pending_primary_services_requests_.contains(device_address));
+  DCHECK(!base::Contains(pending_primary_services_requests_, device_address));
 }
 
 RenderProcessHost* WebBluetoothServiceImpl::GetRenderProcessHost() {
diff --git a/content/browser/browsing_data/browsing_data_filter_builder_impl.cc b/content/browser/browsing_data/browsing_data_filter_builder_impl.cc
index 9f4a766715..392e6c90e3 100644
--- a/content/browser/browsing_data/browsing_data_filter_builder_impl.cc
+++ b/content/browser/browsing_data/browsing_data_filter_builder_impl.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/notreached.h"
@@ -87,7 +88,8 @@ bool MatchesStorageKey(const std::set<url::Origin>& origins,
       registerable_domain = storage_key.origin().host();
     }
 
-    return is_delete_list == registerable_domains.contains(registerable_domain);
+    return is_delete_list ==
+           base::Contains(registerable_domains, registerable_domain);
   };
 
   switch (match_mode) {
@@ -130,7 +132,7 @@ bool MatchesPluginSiteForRegisterableDomainsAndIPs(
     domain_or_ip = site;
 
   return ((mode == BrowsingDataFilterBuilder::Mode::kDelete) ==
-          (domains_and_ips.contains(domain_or_ip)));
+          (base::Contains(domains_and_ips, domain_or_ip)));
 }
 
 template <typename T>
diff --git a/content/browser/browsing_data/browsing_data_remover_impl.cc b/content/browser/browsing_data/browsing_data_remover_impl.cc
index 973a753fcc..e8bd3ed665 100644
--- a/content/browser/browsing_data/browsing_data_remover_impl.cc
+++ b/content/browser/browsing_data/browsing_data_remover_impl.cc
@@ -11,6 +11,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -95,7 +96,7 @@ bool DoesStorageKeyMatchMask(
     storage::SpecialStoragePolicy* policy) {
   const std::vector<std::string>& schemes = url::GetWebStorageSchemes();
   bool is_web_scheme =
-      std::ranges::contains(schemes, storage_key.origin().scheme());
+      base::Contains(schemes, storage_key.origin().scheme());
 
   // If a websafe origin is unprotected, it matches iff UNPROTECTED_WEB.
   if ((origin_type_mask & BrowsingDataRemover::ORIGIN_TYPE_UNPROTECTED_WEB) &&
diff --git a/content/browser/browsing_data/browsing_data_remover_impl_browsertest.cc b/content/browser/browsing_data/browsing_data_remover_impl_browsertest.cc
index f302a94ed5..c21bb7074d 100644
--- a/content/browser/browsing_data/browsing_data_remover_impl_browsertest.cc
+++ b/content/browser/browsing_data/browsing_data_remover_impl_browsertest.cc
@@ -4,6 +4,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -92,7 +93,7 @@ std::unique_ptr<net::test_server::HttpResponse> HandleHttpAuthRequest(
   }
 
   auto http_response = std::make_unique<net::test_server::BasicHttpResponse>();
-  if (request.headers.contains("Authorization")) {
+  if (base::Contains(request.headers, "Authorization")) {
     http_response->set_code(net::HTTP_OK);
     http_response->set_content("Success!");
   } else {
diff --git a/content/browser/browsing_data/clear_site_data_handler.cc b/content/browser/browsing_data/clear_site_data_handler.cc
index 1b97df5423..2d620c7076 100644
--- a/content/browser/browsing_data/clear_site_data_handler.cc
+++ b/content/browser/browsing_data/clear_site_data_handler.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/string_split.h"
@@ -273,7 +274,7 @@ bool ClearSiteDataHandler::ParseHeader(
       net::ClearSiteDataHeaderContents(header);
   std::string output_types;
 
-  if (std::ranges::contains(input_types, net::kDatatypeWildcard)) {
+  if (base::Contains(input_types, net::kDatatypeWildcard)) {
     input_types.push_back(net::kDatatypeCookies);
     input_types.push_back(net::kDatatypeStorage);
     input_types.push_back(net::kDatatypeCache);
diff --git a/content/browser/browsing_instance.cc b/content/browser/browsing_instance.cc
index 18ed48be99..0dd5137ec4 100644
--- a/content/browser/browsing_instance.cc
+++ b/content/browser/browsing_instance.cc
@@ -6,6 +6,7 @@
 
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "content/browser/child_process_security_policy_impl.h"
 #include "content/browser/origin_agent_cluster_isolation_state.h"
 #include "content/browser/site_info.h"
@@ -49,7 +50,7 @@ BrowsingInstance::BrowsingInstance(
 }
 
 bool BrowsingInstance::HasSiteInstance(const SiteInfo& site_info) {
-  return site_instance_map_.contains(site_info);
+  return base::Contains(site_instance_map_, site_info);
 }
 
 scoped_refptr<SiteInstanceImpl> BrowsingInstance::GetSiteInstanceForURL(
diff --git a/content/browser/btm/btm_bounce_detector_browsertest.cc b/content/browser/btm/btm_bounce_detector_browsertest.cc
index 4d0885e915..3fd0c7e05d 100644
--- a/content/browser/btm/btm_bounce_detector_browsertest.cc
+++ b/content/browser/btm/btm_bounce_detector_browsertest.cc
@@ -15,6 +15,7 @@
 #include <vector>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
@@ -3127,9 +3128,9 @@ class BtmPrivacySandboxDataPreservationTest : public ContentBrowserTest {
               if (request.relative_url != "/issue") {
                 return nullptr;
               }
-              if (!request.headers.contains("Sec-Private-State-Token") ||
-                  !request.headers.contains(
-                      "Sec-Private-State-Token-Crypto-Version")) {
+              if (!base::Contains(request.headers, "Sec-Private-State-Token") ||
+                  !base::Contains(request.headers,
+                                  "Sec-Private-State-Token-Crypto-Version")) {
                 return MakeTrustTokenFailureResponse();
               }
 
diff --git a/content/browser/buckets/bucket_manager_host.cc b/content/browser/buckets/bucket_manager_host.cc
index ebfd202496..56de648997 100644
--- a/content/browser/buckets/bucket_manager_host.cc
+++ b/content/browser/buckets/bucket_manager_host.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/task/sequenced_task_runner.h"
@@ -162,7 +163,7 @@ void BucketManagerHost::DeleteBucket(const std::string& name,
 }
 
 void BucketManagerHost::RemoveBucketHost(storage::BucketId id) {
-  DCHECK(bucket_map_.contains(id));
+  DCHECK(base::Contains(bucket_map_, id));
   bucket_map_.erase(id);
 }
 
diff --git a/content/browser/cache_storage/cache_storage.cc b/content/browser/cache_storage/cache_storage.cc
index b87bb0fa62..106d84c3c8 100644
--- a/content/browser/cache_storage/cache_storage.cc
+++ b/content/browser/cache_storage/cache_storage.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/files/file_util.h"
 #include "base/files/memory_mapped_file.h"
@@ -249,13 +250,13 @@ class CacheStorage::MemoryLoader : public CacheStorage::CacheLoader {
 
   void NotifyCacheCreated(const std::u16string& cache_name,
                           CacheStorageCacheHandle cache_handle) override {
-    DCHECK(!cache_handles_.contains(cache_name));
+    DCHECK(!base::Contains(cache_handles_, cache_name));
     cache_handles_.insert(std::make_pair(cache_name, std::move(cache_handle)));
   }
 
   void NotifyCacheDoomed(CacheStorageCacheHandle cache_handle) override {
     auto* impl = CacheStorageCache::From(cache_handle);
-    DCHECK(cache_handles_.contains(impl->cache_name()));
+    DCHECK(base::Contains(cache_handles_, impl->cache_name()));
     cache_handles_.erase(impl->cache_name());
   }
 
@@ -294,7 +295,7 @@ class CacheStorage::SimpleCacheLoader : public CacheStorage::CacheLoader {
       int64_t cache_size,
       int64_t cache_padding) override {
     DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-    DCHECK(cache_name_to_cache_dir_.contains(cache_name));
+    DCHECK(base::Contains(cache_name_to_cache_dir_, cache_name));
 
     std::string cache_dir = cache_name_to_cache_dir_[cache_name];
     base::FilePath cache_path = directory_path_.AppendASCII(cache_dir);
@@ -399,7 +400,7 @@ class CacheStorage::SimpleCacheLoader : public CacheStorage::CacheLoader {
     protobuf_index.set_bucket_is_default(bucket_locator_.is_default);
 
     for (const auto& cache_metadata : index.ordered_cache_metadata()) {
-      DCHECK(cache_name_to_cache_dir_.contains(cache_metadata.name));
+      DCHECK(base::Contains(cache_name_to_cache_dir_, cache_metadata.name));
 
       proto::CacheStorageIndex::Cache* index_cache = protobuf_index.add_cache();
       index_cache->set_name(base::UTF16ToUTF8(cache_metadata.name));
@@ -547,7 +548,7 @@ class CacheStorage::SimpleCacheLoader : public CacheStorage::CacheLoader {
     {
       base::FilePath cache_path;
       while (!(cache_path = file_enum.Next()).empty()) {
-        if (!cache_dirs->contains(cache_path.BaseName().AsUTF8Unsafe()))
+        if (!base::Contains(*cache_dirs, cache_path.BaseName().AsUTF8Unsafe()))
           dirs_to_delete.push_back(cache_path);
       }
     }
@@ -982,7 +983,8 @@ bool CacheStorage::InitiateScheduledIndexWriteForTest(
 
 void CacheStorage::CacheSizeUpdated(const CacheStorageCache* cache) {
   // Should not be called for doomed caches.
-  DCHECK(!doomed_caches_.contains(const_cast<CacheStorageCache*>(cache)));
+  DCHECK(
+      !base::Contains(doomed_caches_, const_cast<CacheStorageCache*>(cache)));
   DCHECK_NE(cache->cache_padding(), kSizeUnknown);
   bool size_changed =
       cache_index_->SetCacheSize(cache->cache_name(), cache->cache_size());
@@ -1165,7 +1167,7 @@ void CacheStorage::HasCacheImpl(const std::u16string& cache_name,
                                 BoolAndErrorCallback callback) {
   TRACE_EVENT("CacheStorage", "CacheStorage::HasCacheImpl",
               perfetto::Flow::Global(trace_id), "cache_name", cache_name);
-  bool has_cache = cache_map_.contains(cache_name);
+  bool has_cache = base::Contains(cache_map_, cache_name);
   std::move(callback).Run(has_cache, CacheStorageError::kSuccess);
 }
 
diff --git a/content/browser/cache_storage/cache_storage_dispatcher_host.cc b/content/browser/cache_storage/cache_storage_dispatcher_host.cc
index 42fcbb22f1..3e75bc3182 100644
--- a/content/browser/cache_storage/cache_storage_dispatcher_host.cc
+++ b/content/browser/cache_storage/cache_storage_dispatcher_host.cc
@@ -8,6 +8,7 @@
 #include <string>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
@@ -1150,7 +1151,7 @@ void CacheStorageDispatcherHost::UpdateOrCreateDefaultBucket(
 bool CacheStorageDispatcherHost::WasNotifiedOfBucketDataDeletion(
     const storage::BucketLocator& bucket_locator) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  return deleted_buckets_.contains(bucket_locator);
+  return base::Contains(deleted_buckets_, bucket_locator);
 }
 
 void CacheStorageDispatcherHost::NotifyBucketDataDeleted(
diff --git a/content/browser/cache_storage/cache_storage_index.cc b/content/browser/cache_storage/cache_storage_index.cc
index ecc6060478..619d3b8070 100644
--- a/content/browser/cache_storage/cache_storage_index.cc
+++ b/content/browser/cache_storage/cache_storage_index.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 
 namespace content {
 
@@ -31,7 +32,7 @@ CacheStorageIndex& CacheStorageIndex::operator=(CacheStorageIndex&& rhs) {
 
 void CacheStorageIndex::Insert(const CacheMetadata& cache_metadata) {
   DCHECK(!has_doomed_cache_);
-  DCHECK(!cache_metadata_map_.contains(cache_metadata.name));
+  DCHECK(!base::Contains(cache_metadata_map_, cache_metadata.name));
   ordered_cache_metadata_.push_back(cache_metadata);
   cache_metadata_map_[cache_metadata.name] = --ordered_cache_metadata_.end();
   storage_size_ = CacheStorage::kSizeUnknown;
diff --git a/content/browser/child_process_security_policy_impl.cc b/content/browser/child_process_security_policy_impl.cc
index 441a835365..9c3fe588e7 100644
--- a/content/browser/child_process_security_policy_impl.cc
+++ b/content/browser/child_process_security_policy_impl.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
@@ -284,8 +285,8 @@ bool AllowProcessLockMismatchForNTP(const ProcessLock& expected_lock,
   // does not require its process to be locked.  This should only be the case
   // for sites used to load most visited tiles.
   const auto& webui_schemes = URLDataManagerBackend::GetWebUISchemes();
-  if (!std::ranges::contains(webui_schemes,
-                             expected_lock.GetProcessLockURL().GetScheme())) {
+  if (!base::Contains(webui_schemes,
+                      expected_lock.GetProcessLockURL().GetScheme())) {
     return false;
   }
   if (GetContentClient()->browser()->DoesWebUIUrlRequireProcessLock(
@@ -1522,7 +1523,7 @@ bool ChildProcessSecurityPolicyImpl::CanRequestURL(int child_id,
   // scheme.
   const auto& webui_schemes = URLDataManagerBackend::GetWebUISchemes();
   if (!RenderProcessHost::run_renderer_in_process() &&
-      std::ranges::contains(webui_schemes, url.GetScheme())) {
+      base::Contains(webui_schemes, url.GetScheme())) {
     bool should_be_locked =
         GetContentClient()->browser()->DoesWebUIUrlRequireProcessLock(url);
     if (should_be_locked) {
@@ -2810,7 +2811,7 @@ bool ChildProcessSecurityPolicyImpl::IsIsolatedOrigin(
 
 bool ChildProcessSecurityPolicyImpl::IsGloballyIsolatedOriginForTesting(
     const url::Origin& origin) {
-  return std::ranges::contains(GetIsolatedOrigins(), origin);
+  return base::Contains(GetIsolatedOrigins(), origin);
 }
 
 std::vector<url::Origin> ChildProcessSecurityPolicyImpl::GetIsolatedOrigins(
@@ -3285,8 +3286,8 @@ void ChildProcessSecurityPolicyImpl::
   // We only support adding new entries, not modifying existing ones. If at
   // some point in the future we allow isolation status to change during the
   // lifetime of a BrowsingInstance, then this will need to be updated.
-  if (!std::ranges::contains(it->second, origin,
-                             &OriginAgentClusterOptInEntry::origin)) {
+  if (!base::Contains(it->second, origin,
+                      &OriginAgentClusterOptInEntry::origin)) {
     it->second.emplace_back(oac_isolation_state, origin);
   }
 }
diff --git a/content/browser/client_hints/client_hints.cc b/content/browser/client_hints/client_hints.cc
index 8246d97469..67bb645548 100644
--- a/content/browser/client_hints/client_hints.cc
+++ b/content/browser/client_hints/client_hints.cc
@@ -10,6 +10,7 @@
 
 #include "base/check_is_test.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
diff --git a/content/browser/code_cache/generated_code_cache_browsertest.cc b/content/browser/code_cache/generated_code_cache_browsertest.cc
index 68a3095a49..fb3fdfca48 100644
--- a/content/browser/code_cache/generated_code_cache_browsertest.cc
+++ b/content/browser/code_cache/generated_code_cache_browsertest.cc
@@ -146,7 +146,7 @@ class CodeCacheBrowserTest
     // GeneratedCodeCache (>1024 characters).
     if (absolute_url.GetPath() == "/cacheable.js") {
       if (trigger_validation_requests_ &&
-          request.headers.contains("If-Modified-Since")) {
+          base::Contains(request.headers, "If-Modified-Since")) {
         auto http_response =
             std::make_unique<net::test_server::BasicHttpResponse>();
         http_response->set_code(net::HTTP_NOT_MODIFIED);
diff --git a/content/browser/content_security_policy_browsertest.cc b/content/browser/content_security_policy_browsertest.cc
index 76d835f723..9a45654185 100644
--- a/content/browser/content_security_policy_browsertest.cc
+++ b/content/browser/content_security_policy_browsertest.cc
@@ -2,7 +2,6 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <string_view>
 #include <tuple>
 
@@ -462,7 +461,7 @@ constexpr std::string_view kHandledPaths[] = {
 std::unique_ptr<net::test_server::HttpResponse>
 ServeCSPSandboxedWithAllowSameSiteNoneCookies(
     const net::test_server::HttpRequest& request) {
-  if (!std::ranges::contains(kHandledPaths, request.relative_url)) {
+  if (!base::Contains(kHandledPaths, request.relative_url)) {
     return nullptr;
   }
 
diff --git a/content/browser/device_posture/device_posture_registry_watcher_win.cc b/content/browser/device_posture/device_posture_registry_watcher_win.cc
index ccf63efcb0..c47053f19d 100644
--- a/content/browser/device_posture/device_posture_registry_watcher_win.cc
+++ b/content/browser/device_posture/device_posture_registry_watcher_win.cc
@@ -5,6 +5,7 @@
 
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/json/json_reader.h"
 #include "base/strings/string_number_conversions.h"
diff --git a/content/browser/devtools/devtools_agent_host_impl.cc b/content/browser/devtools/devtools_agent_host_impl.cc
index 9ad1eff1d9..34a866ac84 100644
--- a/content/browser/devtools/devtools_agent_host_impl.cc
+++ b/content/browser/devtools/devtools_agent_host_impl.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ref_counted_memory.h"
 #include "base/no_destructor.h"
@@ -198,7 +199,7 @@ DevToolsAgentHost::List DevToolsAgentHost::GetOrCreateAll() {
 #if DCHECK_IS_ON()
   for (auto it : result) {
     DevToolsAgentHostImpl* host = static_cast<DevToolsAgentHostImpl*>(it.get());
-    DCHECK(GetDevtoolsInstances().contains(host->id_));
+    DCHECK(base::Contains(GetDevtoolsInstances(), host->id_));
   }
 #endif
 
@@ -317,7 +318,7 @@ bool DevToolsAgentHostImpl::AttachInternal(
   }
   renderer_channel_.AttachSession(session);
   sessions_.push_back(session);
-  DCHECK(!session_by_client_.contains(session->GetClient()));
+  DCHECK(!base::Contains(session_by_client_, session->GetClient()));
   session_by_client_.emplace(session->GetClient(), std::move(session_owned));
   if (sessions_.size() == 1)
     NotifyAttached();
@@ -548,7 +549,7 @@ std::string DevToolsAgentHostImpl::GetSubtype() {
 }
 
 void DevToolsAgentHostImpl::NotifyCreated() {
-  DCHECK(!GetDevtoolsInstances().contains(id_));
+  DCHECK(!base::Contains(GetDevtoolsInstances(), id_));
   GetDevtoolsInstances()[id_] = this;
   for (auto& observer : GetDevtoolsObservers())
     observer.DevToolsAgentHostCreated(this);
@@ -575,7 +576,7 @@ void DevToolsAgentHostImpl::NotifyCrashed(base::TerminationStatus status) {
 }
 
 void DevToolsAgentHostImpl::NotifyDestroyed() {
-  DCHECK(GetDevtoolsInstances().contains(id_));
+  DCHECK(base::Contains(GetDevtoolsInstances(), id_));
   for (auto& observer : GetDevtoolsObservers())
     observer.DevToolsAgentHostDestroyed(this);
   GetDevtoolsInstances().erase(id_);
diff --git a/content/browser/devtools/devtools_http_handler.cc b/content/browser/devtools/devtools_http_handler.cc
index 79bf965d5d..904e6e9805 100644
--- a/content/browser/devtools/devtools_http_handler.cc
+++ b/content/browser/devtools/devtools_http_handler.cc
@@ -634,7 +634,7 @@ void DevToolsHttpHandler::OnJsonRequest(
   std::vector<std::string_view> query_components = base::SplitStringPiece(
       query, "&", base::KEEP_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
 
-  bool for_tab = std::ranges::contains(query_components, "for_tab");
+  bool for_tab = base::Contains(query_components, "for_tab");
 
   if (command == "list") {
     DevToolsManager* manager = DevToolsManager::GetInstance();
diff --git a/content/browser/devtools/devtools_session.cc b/content/browser/devtools/devtools_session.cc
index ab01d49452..d982e32714 100644
--- a/content/browser/devtools/devtools_session.cc
+++ b/content/browser/devtools/devtools_session.cc
@@ -7,6 +7,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/functional/bind.h"
 #include "base/trace_event/trace_event.h"
@@ -392,7 +393,7 @@ void DevToolsSession::FallThrough(int call_id,
   // In browser-only mode, we should've handled everything in dispatcher.
   DCHECK(!browser_only_);
 
-  if (waiting_for_response_.contains(call_id)) {
+  if (base::Contains(waiting_for_response_, call_id)) {
     DispatchProtocolMessageToClient(
         crdtp::CreateErrorResponse(call_id,
                                    crdtp::DispatchResponse::InvalidRequest(
@@ -628,7 +629,7 @@ void DevToolsSession::DetachChildSession(const std::string& session_id) {
 }
 
 bool DevToolsSession::HasChildSession(const std::string& session_id) {
-  return child_sessions_.contains(session_id);
+  return base::Contains(child_sessions_, session_id);
 }
 
 void DevToolsSession::AddObserver(ChildObserver* obs) {
diff --git a/content/browser/devtools/devtools_url_loader_interceptor.cc b/content/browser/devtools/devtools_url_loader_interceptor.cc
index 88fe791f8c..aff0f49ae8 100644
--- a/content/browser/devtools/devtools_url_loader_interceptor.cc
+++ b/content/browser/devtools/devtools_url_loader_interceptor.cc
@@ -10,6 +10,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/raw_ref.h"
@@ -152,7 +153,8 @@ DevToolsURLLoaderInterceptor::Pattern::Pattern(
 bool DevToolsURLLoaderInterceptor::Pattern::Matches(
     const std::string& url,
     blink::mojom::ResourceType resource_type) const {
-  if (!resource_types.empty() && !resource_types.contains(resource_type)) {
+  if (!resource_types.empty() &&
+      !base::Contains(resource_types, resource_type)) {
     return false;
   }
   return base::MatchPattern(url, url_pattern);
diff --git a/content/browser/devtools/frame_auto_attacher.cc b/content/browser/devtools/frame_auto_attacher.cc
index 3b1c56def0..e9d2d0e8f0 100644
--- a/content/browser/devtools/frame_auto_attacher.cc
+++ b/content/browser/devtools/frame_auto_attacher.cc
@@ -4,6 +4,7 @@
 
 #include "content/browser/devtools/frame_auto_attacher.h"
 
+#include "base/containers/contains.h"
 #include "base/time/time.h"
 #include "content/browser/devtools/auction_worklet_devtools_agent_host.h"
 #include "content/browser/devtools/devtools_renderer_channel.h"
@@ -30,7 +31,8 @@ void GetMatchingHostsByScopeMap(
   for (const GURL& url : urls)
     host_name_set.insert(url.DeprecatedGetOriginAsURL());
   for (const auto& host : agent_hosts) {
-    if (!host_name_set.contains(host->scope().DeprecatedGetOriginAsURL())) {
+    if (!base::Contains(host_name_set,
+                        host->scope().DeprecatedGetOriginAsURL())) {
       continue;
     }
     const auto& it = scope_agents_map->find(host->scope());
@@ -216,7 +218,7 @@ void FrameAutoAttacher::WorkerCreated(ServiceWorkerDevToolsAgentHost* host,
       render_frame_host_->GetProcess()->GetBrowserContext();
   auto hosts = GetMatchingServiceWorkers(browser_context,
                                          GetFrameUrls(render_frame_host_));
-  if (!hosts.contains(host->GetId())) {
+  if (!base::Contains(hosts, host->GetId())) {
     return;
   }
 
diff --git a/content/browser/devtools/protocol/input_handler.cc b/content/browser/devtools/protocol/input_handler.cc
index 0e9590badb..3f1332823f 100644
--- a/content/browser/devtools/protocol/input_handler.cc
+++ b/content/browser/devtools/protocol/input_handler.cc
@@ -13,6 +13,7 @@
 
 #include "base/check.h"
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -252,7 +253,7 @@ bool GenerateTouchPoints(
                                   ? blink::WebTouchPoint::State::kStateCancelled
                                   : blink::WebTouchPoint::State::kStateReleased;
     event->SetType(type);
-  } else if (!points.contains(changing.id)) {
+  } else if (!base::Contains(points, changing.id)) {
     event->touches[0].state = blink::WebTouchPoint::State::kStatePressed;
     event->SetType(blink::WebInputEvent::Type::kTouchStart);
   } else {
@@ -535,7 +536,7 @@ CreateWebTouchEvents(
   std::vector<blink::WebTouchEvent> events;
   bool ok = true;
   for (auto& id_point : points) {
-    if (touched_points.contains(id_point.first) &&
+    if (base::Contains(touched_points, id_point.first) &&
         type == blink::WebInputEvent::Type::kTouchMove &&
         touched_points[id_point.first].PositionInWidget() ==
             id_point.second.PositionInWidget()) {
@@ -1807,7 +1808,7 @@ void InputHandler::DispatchSyntheticPointerActionTouch(
 
     SyntheticPointerActionParams::PointerActionType action_type =
         SyntheticPointerActionParams::PointerActionType::MOVE;
-    if (!pointer_ids_.contains(id)) {
+    if (!base::Contains(pointer_ids_, id)) {
       pointer_ids_.insert(id);
       action_type = SyntheticPointerActionParams::PointerActionType::PRESS;
     }
@@ -1830,7 +1831,7 @@ void InputHandler::DispatchSyntheticPointerActionTouch(
           SyntheticPointerActionParams::PointerActionType::MOVE &&
       current_pointer_ids.size() < pointer_ids_.size()) {
     for (auto it = pointer_ids_.begin(); it != pointer_ids_.end();) {
-      if (current_pointer_ids.contains(*it)) {
+      if (base::Contains(current_pointer_ids, *it)) {
         it++;
         continue;
       }
diff --git a/content/browser/devtools/protocol/network_handler.cc b/content/browser/devtools/protocol/network_handler.cc
index 685deb662d..347ed2150a 100644
--- a/content/browser/devtools/protocol/network_handler.cc
+++ b/content/browser/devtools/protocol/network_handler.cc
@@ -7,7 +7,6 @@
 #include <stddef.h>
 #include <stdint.h>
 
-#include <algorithm>
 #include <memory>
 #include <string_view>
 #include <utility>
@@ -16,6 +15,7 @@
 #include "base/barrier_closure.h"
 #include "base/base64.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/queue.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
@@ -1630,7 +1630,7 @@ NetworkHandler::BuildProtocolReport(const net::ReportingReport& report) {
     return nullptr;
   }
   std::vector<GURL> reporting_filter_urls = ComputeReportingURLs(host_);
-  if (std::ranges::contains(reporting_filter_urls, report.url)) {
+  if (base::Contains(reporting_filter_urls, report.url)) {
     return protocol::Network::ReportingApiReport::Create()
         .SetId(report.id.ToString())
         .SetInitiatorUrl(report.url.spec())
diff --git a/content/browser/devtools/protocol/security_handler.cc b/content/browser/devtools/protocol/security_handler.cc
index 4508e10634..872ac32adc 100644
--- a/content/browser/devtools/protocol/security_handler.cc
+++ b/content/browser/devtools/protocol/security_handler.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "content/browser/devtools/devtools_agent_host_impl.h"
 #include "content/browser/renderer_host/back_forward_cache_disable.h"
@@ -129,7 +130,7 @@ Response SecurityHandler::Disable() {
 
 Response SecurityHandler::HandleCertificateError(int event_id,
                                                  const String& action) {
-  if (!cert_error_callbacks_.contains(event_id)) {
+  if (!base::Contains(cert_error_callbacks_, event_id)) {
     return Response::ServerError(
         String("Unknown event id: " + base::NumberToString(event_id)));
   }
diff --git a/content/browser/devtools/protocol/target_handler.cc b/content/browser/devtools/protocol/target_handler.cc
index 18f066a5c7..9d08c36348 100644
--- a/content/browser/devtools/protocol/target_handler.cc
+++ b/content/browser/devtools/protocol/target_handler.cc
@@ -10,6 +10,7 @@
 #include <string_view>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -902,7 +903,7 @@ bool TargetHandler::AutoAttach(TargetAutoAttacher* source,
   if (!auto_attach_target_filter_->Match(*host)) {
     return false;
   }
-  if (auto_attached_sessions_.contains(host)) {
+  if (base::Contains(auto_attached_sessions_, host)) {
     return false;
   }
   if (!auto_attach_service_workers_ &&
@@ -937,12 +938,12 @@ void TargetHandler::SetAttachedTargetsOfType(
     if (host->GetType() == type &&
         entry.second->auto_attacher_id_ ==
             reinterpret_cast<uintptr_t>(source) &&
-        !new_hosts.contains(host)) {
+        !base::Contains(new_hosts, host)) {
       AutoDetach(source, host.get());
     }
   }
   for (auto& host : new_hosts) {
-    if (!old_sessions.contains(host.get())) {
+    if (!base::Contains(old_sessions, host.get())) {
       AutoAttach(source, host.get(), false);
     }
   }
@@ -950,7 +951,7 @@ void TargetHandler::SetAttachedTargetsOfType(
 
 void TargetHandler::TargetInfoChanged(DevToolsAgentHost* host) {
   // Only send target info for targets we reported in any way.
-  if (!reported_hosts_.contains(host) &&
+  if (!base::Contains(reported_hosts_, host) &&
       auto_attached_sessions_.find(host) == auto_attached_sessions_.end()) {
     return;
   }
@@ -1239,7 +1240,7 @@ Response TargetHandler::CloseTarget(const std::string& target_id,
   if (access_mode_ == AccessMode::kAutoAttachOnly) {
     // Only allow to close the targets that we are attached to.
     if (target_id != owner_target_id_ &&
-        !auto_attached_sessions_.contains(agent_host.get())) {
+        !base::Contains(auto_attached_sessions_, agent_host.get())) {
       return Response::ServerError(kNotAllowedError);
     }
   }
@@ -1411,7 +1412,7 @@ void TargetHandler::DevToolsAgentHostCreated(DevToolsAgentHost* host) {
   }
   // If we start discovering late, all existing agent hosts will be reported,
   // but we could have already attached to some.
-  if (!reported_hosts_.contains(host)) {
+  if (!base::Contains(reported_hosts_, host)) {
     frontend_->TargetCreated(BuildTargetInfo(host));
     reported_hosts_.insert(host);
   }
@@ -1422,7 +1423,7 @@ void TargetHandler::DevToolsAgentHostNavigated(DevToolsAgentHost* host) {
 }
 
 void TargetHandler::DevToolsAgentHostDestroyed(DevToolsAgentHost* host) {
-  if (!reported_hosts_.contains(host)) {
+  if (!base::Contains(reported_hosts_, host)) {
     return;
   }
   frontend_->TargetDestroyed(host->GetId());
@@ -1439,7 +1440,7 @@ void TargetHandler::DevToolsAgentHostDetached(DevToolsAgentHost* host) {
 
 void TargetHandler::DevToolsAgentHostCrashed(DevToolsAgentHost* host,
                                              base::TerminationStatus status) {
-  if (!reported_hosts_.contains(host)) {
+  if (!base::Contains(reported_hosts_, host)) {
     return;
   }
   frontend_->TargetCrashed(host->GetId(), TerminationStatusToString(status),
diff --git a/content/browser/devtools/protocol/tethering_handler.cc b/content/browser/devtools/protocol/tethering_handler.cc
index 0a846fd5c8..74d47787a2 100644
--- a/content/browser/devtools/protocol/tethering_handler.cc
+++ b/content/browser/devtools/protocol/tethering_handler.cc
@@ -7,6 +7,7 @@
 #include <map>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/task/single_thread_task_runner.h"
 #include "content/public/browser/browser_task_traits.h"
@@ -289,7 +290,7 @@ TetheringHandler::TetheringImpl::~TetheringImpl() = default;
 
 void TetheringHandler::TetheringImpl::Bind(
     uint16_t port, std::unique_ptr<BindCallback> callback) {
-  if (bound_sockets_.contains(port)) {
+  if (base::Contains(bound_sockets_, port)) {
     GetUIThreadTaskRunner({})->PostTask(
         FROM_HERE,
         base::BindOnce(&BindCallback::sendFailure, std::move(callback),
diff --git a/content/browser/devtools/service_worker_devtools_manager.cc b/content/browser/devtools/service_worker_devtools_manager.cc
index 2532df420a..f771973298 100644
--- a/content/browser/devtools/service_worker_devtools_manager.cc
+++ b/content/browser/devtools/service_worker_devtools_manager.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/observer_list.h"
 #include "content/browser/devtools/devtools_instrumentation.h"
@@ -154,7 +155,7 @@ void ServiceWorkerDevToolsManager::WorkerStarting(
     bool* pause_on_start) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   const WorkerId worker_id(worker_process_id, worker_route_id);
-  DCHECK(!live_hosts_.contains(worker_id));
+  DCHECK(!base::Contains(live_hosts_, worker_id));
 
   scoped_refptr<ServiceWorkerDevToolsAgentHost> agent_host =
       TakeStoppedHost(context_wrapper.get(), version_id);
diff --git a/content/browser/devtools/shared_storage_worklet_devtools_manager.cc b/content/browser/devtools/shared_storage_worklet_devtools_manager.cc
index 9f104741b9..f0cd49219d 100644
--- a/content/browser/devtools/shared_storage_worklet_devtools_manager.cc
+++ b/content/browser/devtools/shared_storage_worklet_devtools_manager.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "content/browser/devtools/shared_storage_worklet_devtools_agent_host.h"
 #include "content/browser/renderer_host/render_frame_host_impl.h"
 #include "content/browser/shared_storage/shared_storage_worklet_host.h"
@@ -32,7 +33,7 @@ void SharedStorageWorkletDevToolsManager::WorkletCreated(
     const base::UnguessableToken& devtools_worklet_token,
     bool& wait_for_debugger) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
-  CHECK(!hosts_.contains(&worklet_host));
+  CHECK(!base::Contains(hosts_, &worklet_host));
   CHECK(!wait_for_debugger);
 
   hosts_[&worklet_host] = MakeRefCounted<SharedStorageWorkletDevToolsAgentHost>(
diff --git a/content/browser/devtools/shared_worker_devtools_manager.cc b/content/browser/devtools/shared_worker_devtools_manager.cc
index 973073a346..cdfb66e140 100644
--- a/content/browser/devtools/shared_worker_devtools_manager.cc
+++ b/content/browser/devtools/shared_worker_devtools_manager.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "content/browser/devtools/shared_worker_devtools_agent_host.h"
 #include "content/browser/worker_host/shared_worker_host.h"
 #include "content/public/browser/browser_thread.h"
@@ -29,7 +30,7 @@ void SharedWorkerDevToolsManager::WorkerCreated(
     bool* pause_on_start,
     base::UnguessableToken* devtools_worker_token) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
-  DCHECK(!live_hosts_.contains(worker_host));
+  DCHECK(!base::Contains(live_hosts_, worker_host));
 
   auto it = std::ranges::find_if(
       terminated_hosts_,
diff --git a/content/browser/devtools/worker_devtools_manager.cc b/content/browser/devtools/worker_devtools_manager.cc
index c95b46717f..e3707c6321 100644
--- a/content/browser/devtools/worker_devtools_manager.cc
+++ b/content/browser/devtools/worker_devtools_manager.cc
@@ -4,6 +4,7 @@
 
 #include "content/browser/devtools/worker_devtools_manager.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "content/browser/devtools/dedicated_worker_devtools_agent_host.h"
 #include "content/browser/devtools/devtools_instrumentation.h"
@@ -49,7 +50,7 @@ void WorkerDevToolsManager::WorkerCreated(
     const GlobalRenderFrameHostId& ancestor_render_frame_host_id,
     scoped_refptr<DevToolsThrottleHandle> throttle_handle) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
-  DCHECK(!hosts_.contains(host));
+  DCHECK(!base::Contains(hosts_, host));
 
   hosts_[host] = base::MakeRefCounted<DedicatedWorkerDevToolsAgentHost>(
       process_id,
diff --git a/content/browser/do_not_track_browsertest.cc b/content/browser/do_not_track_browsertest.cc
index 266eee4064..66e6364e08 100644
--- a/content/browser/do_not_track_browsertest.cc
+++ b/content/browser/do_not_track_browsertest.cc
@@ -3,6 +3,7 @@
 // found in the LICENSE file.
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/strings/utf_string_conversions.h"
@@ -171,7 +172,7 @@ IN_PROC_BROWSER_TEST_F(DoNotTrackTest, Worker) {
       shell(), GetURL("/workers/create_worker.html?worker_url=/capture")));
   loop.Run();
 
-  EXPECT_TRUE(header_map.contains("DNT"));
+  EXPECT_TRUE(base::Contains(header_map, "DNT"));
   EXPECT_EQ("1", header_map["DNT"]);
 
   // Wait until the worker script is loaded to stop the test from crashing
@@ -203,7 +204,7 @@ IN_PROC_BROWSER_TEST_F(DoNotTrackTest, MAYBE_SharedWorker) {
       GetURL("/workers/create_shared_worker.html?worker_url=/capture")));
   loop.Run();
 
-  EXPECT_TRUE(header_map.contains("DNT"));
+  EXPECT_TRUE(base::Contains(header_map, "DNT"));
   EXPECT_EQ("1", header_map["DNT"]);
 
   // Wait until the worker script is loaded to stop the test from crashing
@@ -229,7 +230,7 @@ IN_PROC_BROWSER_TEST_F(DoNotTrackTest, ServiceWorker_Register) {
   EXPECT_EQ("DONE", EvalJs(shell(), "register('/capture');"));
   loop.Run();
 
-  EXPECT_TRUE(header_map.contains("DNT"));
+  EXPECT_TRUE(base::Contains(header_map, "DNT"));
   EXPECT_EQ("1", header_map["DNT"]);
 
   // Service worker doesn't have to wait for onmessage event because
@@ -255,7 +256,7 @@ IN_PROC_BROWSER_TEST_F(DoNotTrackTest, ModuleServiceWorker_Register) {
   EXPECT_EQ("DONE", EvalJs(shell(), "register('/capture', '', 'module');"));
   loop.Run();
 
-  EXPECT_TRUE(header_map.contains("DNT"));
+  EXPECT_TRUE(base::Contains(header_map, "DNT"));
   EXPECT_EQ("1", header_map["DNT"]);
 
   // Module Service worker doesn't have to wait for onmessage event because
@@ -289,7 +290,7 @@ IN_PROC_BROWSER_TEST_F(DoNotTrackTest,
             EvalJs(shell(), "register('/captureWorker','', 'module');"));
   loop.Run();
 
-  EXPECT_TRUE(header_map.contains("DNT"));
+  EXPECT_TRUE(base::Contains(header_map, "DNT"));
   EXPECT_EQ("1", header_map["DNT"]);
 
   // Module Service worker doesn't have to wait for onmessage event because
@@ -319,7 +320,7 @@ IN_PROC_BROWSER_TEST_F(DoNotTrackTest, ServiceWorker_Update) {
   EXPECT_EQ("DONE", EvalJs(shell(), "update();"));
   loop.Run();
 
-  EXPECT_TRUE(header_map.contains("DNT"));
+  EXPECT_TRUE(base::Contains(header_map, "DNT"));
   EXPECT_EQ("1", header_map["DNT"]);
 
   // Service worker doesn't have to wait for onmessage event because
@@ -349,7 +350,7 @@ IN_PROC_BROWSER_TEST_F(DoNotTrackTest, ModuleServiceWorker_Update) {
   EXPECT_EQ("DONE", EvalJs(shell(), "update();"));
   loop.Run();
 
-  EXPECT_TRUE(header_map.contains("DNT"));
+  EXPECT_TRUE(base::Contains(header_map, "DNT"));
   EXPECT_EQ("1", header_map["DNT"]);
 
   // Module service worker doesn't have to wait for onmessage event because
@@ -382,7 +383,7 @@ IN_PROC_BROWSER_TEST_F(DoNotTrackTest, StaticImportModuleServiceWorker_Update) {
   EXPECT_EQ("DONE", EvalJs(shell(), "update();"));
   loop.Run();
 
-  EXPECT_TRUE(header_map.contains("DNT"));
+  EXPECT_TRUE(base::Contains(header_map, "DNT"));
   EXPECT_EQ("1", header_map["DNT"]);
 
   // Module service worker doesn't have to wait for onmessage event because
diff --git a/content/browser/dom_storage/dom_storage_context_wrapper.cc b/content/browser/dom_storage/dom_storage_context_wrapper.cc
index f60d962470..58b6f78773 100644
--- a/content/browser/dom_storage/dom_storage_context_wrapper.cc
+++ b/content/browser/dom_storage/dom_storage_context_wrapper.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -411,14 +412,14 @@ void DOMStorageContextWrapper::AddNamespace(
     const std::string& namespace_id,
     SessionStorageNamespaceImpl* session_namespace) {
   base::AutoLock lock(alive_namespaces_lock_);
-  DCHECK(!alive_namespaces_.contains(namespace_id));
+  DCHECK(!base::Contains(alive_namespaces_, namespace_id));
   alive_namespaces_[namespace_id] = session_namespace;
 }
 
 void DOMStorageContextWrapper::RemoveNamespace(
     const std::string& namespace_id) {
   base::AutoLock lock(alive_namespaces_lock_);
-  DCHECK(alive_namespaces_.contains(namespace_id));
+  DCHECK(base::Contains(alive_namespaces_, namespace_id));
   alive_namespaces_.erase(namespace_id);
 }
 
diff --git a/content/browser/download/download_browsertest.cc b/content/browser/download/download_browsertest.cc
index b354903a78..fc386f96ac 100644
--- a/content/browser/download/download_browsertest.cc
+++ b/content/browser/download/download_browsertest.cc
@@ -1290,19 +1290,19 @@ class DownloadContentTestWithoutStrongValidators : public DownloadContentTest {
 
     // The second request is a range request.
     std::string value;
-    ASSERT_FALSE(requests[1]->http_request.headers.contains(
-        net::HttpRequestHeaders::kIfRange));
+    ASSERT_FALSE(base::Contains(requests[1]->http_request.headers,
+                                net::HttpRequestHeaders::kIfRange));
 
-    ASSERT_TRUE(requests[1]->http_request.headers.contains(
-        net::HttpRequestHeaders::kRange));
+    ASSERT_TRUE(base::Contains(requests[1]->http_request.headers,
+                               net::HttpRequestHeaders::kRange));
     EXPECT_EQ(
         base::StringPrintf("bytes=%" PRId64 "-",
                            interruption_offset - kValidationLength),
         requests[1]->http_request.headers.at(net::HttpRequestHeaders::kRange));
     if (fail_content_validation) {
       // The third request is a restart request.
-      ASSERT_FALSE(requests[2]->http_request.headers.contains(
-          net::HttpRequestHeaders::kRange));
+      ASSERT_FALSE(base::Contains(requests[2]->http_request.headers,
+                                  net::HttpRequestHeaders::kRange));
       EXPECT_EQ(parameters.size, requests[2]->transferred_byte_count);
     }
   }
@@ -2173,13 +2173,13 @@ IN_PROC_BROWSER_TEST_F(DownloadContentTest, ResumeWithStrongValidators) {
             requests[1]->transferred_byte_count);
 
   std::string value;
-  ASSERT_TRUE(requests[1]->http_request.headers.contains(
-      net::HttpRequestHeaders::kIfRange));
+  ASSERT_TRUE(base::Contains(requests[1]->http_request.headers,
+                             net::HttpRequestHeaders::kIfRange));
   EXPECT_EQ(parameters.etag, requests[1]->http_request.headers.at(
                                  net::HttpRequestHeaders::kIfRange));
 
-  ASSERT_TRUE(requests[1]->http_request.headers.contains(
-      net::HttpRequestHeaders::kRange));
+  ASSERT_TRUE(base::Contains(requests[1]->http_request.headers,
+                             net::HttpRequestHeaders::kRange));
   EXPECT_EQ(
       base::StringPrintf("bytes=%" PRId64 "-", interruption_offset),
       requests[1]->http_request.headers.at(net::HttpRequestHeaders::kRange));
@@ -2650,13 +2650,13 @@ IN_PROC_BROWSER_TEST_F(DownloadContentTest, RestartIfNotPartialResponse) {
   // The second request transfers the entire response.
   EXPECT_EQ(parameters.size, requests[1]->transferred_byte_count);
 
-  ASSERT_TRUE(requests[1]->http_request.headers.contains(
-      net::HttpRequestHeaders::kIfRange));
+  ASSERT_TRUE(base::Contains(requests[1]->http_request.headers,
+                             net::HttpRequestHeaders::kIfRange));
   EXPECT_EQ(parameters.etag, requests[1]->http_request.headers.at(
                                  net::HttpRequestHeaders::kIfRange));
 
-  ASSERT_TRUE(requests[1]->http_request.headers.contains(
-      net::HttpRequestHeaders::kRange));
+  ASSERT_TRUE(base::Contains(requests[1]->http_request.headers,
+                             net::HttpRequestHeaders::kRange));
   EXPECT_EQ(
       base::StringPrintf("bytes=%" PRId64 "-", interruption_offset),
       requests[1]->http_request.headers.at(net::HttpRequestHeaders::kRange));
@@ -2701,10 +2701,10 @@ IN_PROC_BROWSER_TEST_F(DownloadContentTest, RestartIfNoETag) {
 
   // Neither If-Range nor Range headers should be present in the second request.
   ASSERT_EQ(2u, requests.size());
-  EXPECT_FALSE(requests[1]->http_request.headers.contains(
-      net::HttpRequestHeaders::kIfRange));
-  EXPECT_FALSE(requests[1]->http_request.headers.contains(
-      net::HttpRequestHeaders::kRange));
+  EXPECT_FALSE(base::Contains(requests[1]->http_request.headers,
+                              net::HttpRequestHeaders::kIfRange));
+  EXPECT_FALSE(base::Contains(requests[1]->http_request.headers,
+                              net::HttpRequestHeaders::kRange));
 }
 
 // Partial file goes missing before the download is resumed. The download should
@@ -3709,7 +3709,8 @@ IN_PROC_BROWSER_TEST_F(DownloadContentTest, ReferrerForPartialResumption) {
 
   ASSERT_GE(2u, requests.size());
   net::test_server::HttpRequest last_request = requests.back()->http_request;
-  ASSERT_TRUE(last_request.headers.contains(net::HttpRequestHeaders::kReferer));
+  ASSERT_TRUE(
+      base::Contains(last_request.headers, net::HttpRequestHeaders::kReferer));
   EXPECT_EQ(last_request.headers.at(net::HttpRequestHeaders::kReferer),
             document_url.DeprecatedGetOriginAsURL().spec());
 }
diff --git a/content/browser/download/download_manager_impl.cc b/content/browser/download/download_manager_impl.cc
index 7bedb1b17a..3ea58b8651 100644
--- a/content/browser/download/download_manager_impl.cc
+++ b/content/browser/download/download_manager_impl.cc
@@ -7,6 +7,7 @@
 #include <iterator>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -325,7 +326,7 @@ download::DownloadItemImpl* DownloadManagerImpl::CreateActiveItem(
     const download::DownloadCreateInfo& info) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
 
-  if (downloads_by_guid_.contains(info.guid))
+  if (base::Contains(downloads_by_guid_, info.guid))
     return nullptr;
 
   download::DownloadItemImpl* download =
@@ -930,7 +931,7 @@ void DownloadManagerImpl::CreateSavePackageDownloadItemWithId(
     uint32_t id) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   DCHECK_NE(download::DownloadItem::kInvalidId, id);
-  DCHECK(!downloads_.contains(id));
+  DCHECK(!base::Contains(downloads_, id));
 
   download::DownloadItemImpl* download_item = item_factory_->CreateSavePageItem(
       this, id, main_file_path, main_file_display_name, page_url, mime_type,
@@ -1136,7 +1137,7 @@ download::DownloadItem* DownloadManagerImpl::CreateDownloadItem(
   auto in_progress_download = RetrieveInProgressDownload(id);
 
   // Return null to clear cancelled or non-resumable download.
-  if (cleared_download_guids_on_startup_.contains(guid)) {
+  if (base::Contains(cleared_download_guids_on_startup_, guid)) {
     return nullptr;
   }
 
@@ -1198,8 +1199,8 @@ download::DownloadItem* DownloadManagerImpl::CreateDownloadItem(
 
 void DownloadManagerImpl::OnDownloadCreated(
     std::unique_ptr<download::DownloadItemImpl> download) {
-  DCHECK(!downloads_.contains(download->GetId()));
-  DCHECK(!downloads_by_guid_.contains(download->GetGuid()));
+  DCHECK(!base::Contains(downloads_, download->GetId()));
+  DCHECK(!base::Contains(downloads_by_guid_, download->GetGuid()));
   download::DownloadItemImpl* item = download.get();
   downloads_[item->GetId()] = std::move(download);
   downloads_by_guid_[item->GetGuid()] = item;
diff --git a/content/browser/download/save_file_manager.cc b/content/browser/download/save_file_manager.cc
index 0b6f3ee29e..5e1057ce7e 100644
--- a/content/browser/download/save_file_manager.cc
+++ b/content/browser/download/save_file_manager.cc
@@ -7,6 +7,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
@@ -229,7 +230,7 @@ void SaveFileManager::SaveURL(
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
 
   // Insert started saving job to tracking list.
-  DCHECK(!packages_.contains(save_item_id));
+  DCHECK(!base::Contains(packages_, save_item_id));
   packages_[save_item_id] = save_package;
 
   base::OnceClosure quarantine_callback = base::BindOnce(
diff --git a/content/browser/download/save_package.cc b/content/browser/download/save_package.cc
index 83a40d396c..7c89971074 100644
--- a/content/browser/download/save_package.cc
+++ b/content/browser/download/save_package.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -590,7 +591,7 @@ bool SavePackage::GenerateFileName(const std::string& disposition,
                              base::StrCat({"(", base::NumberToString(i), ")"}))
                          .AddExtension(file_name_ext);
       base::FilePath::StringType new_name = new_filepath.value();
-      if (!file_name_set_.contains(new_name)) {
+      if (!base::Contains(file_name_set_, new_name)) {
         // Resolved name conflict.
         file_name = new_name;
         file_name_count_map_[base_file_name] = ++i;
@@ -698,7 +699,7 @@ void SavePackage::PutInProgressItemToSavedMap(SaveItem* save_item) {
 
   SaveItemIdMap& map = save_item->success() ?
       saved_success_items_ : saved_failed_items_;
-  DCHECK(!map.contains(save_item->id()));
+  DCHECK(!base::Contains(map, save_item->id()));
   map[save_item->id()] = std::move(owned_item);
 }
 
@@ -960,7 +961,7 @@ void SavePackage::SaveNextFile(bool process_all_remaining_items) {
     waiting_item_queue_.pop_front();
 
     // Add the item to |in_progress_items_|.
-    DCHECK(!in_progress_items_.contains(save_item->id()));
+    DCHECK(!base::Contains(in_progress_items_, save_item->id()));
     in_progress_items_[save_item_ptr->id()] = std::move(save_item);
     save_item_ptr->Start();
 
@@ -1218,7 +1219,7 @@ void SavePackage::OnDidReceiveSerializedHtmlData(
       }
     }
 
-    if (saved_failed_items_.contains(save_item->id()))
+    if (base::Contains(saved_failed_items_, save_item->id()))
       wrote_to_failed_file_ = true;
 
     return;
diff --git a/content/browser/fenced_frame/fenced_frame_browsertest.cc b/content/browser/fenced_frame/fenced_frame_browsertest.cc
index c3bed47ab6..a2dc8d161b 100644
--- a/content/browser/fenced_frame/fenced_frame_browsertest.cc
+++ b/content/browser/fenced_frame/fenced_frame_browsertest.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <tuple>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/memory/ref_counted.h"
 #include "base/strings/strcat.h"
@@ -2695,7 +2696,7 @@ class FencedFrameParameterizedBrowserTest : public FencedFrameBrowserTestBase {
     SCOPED_TRACE(from_here.ToString());
     DCHECK_CURRENTLY_ON(BrowserThread::UI);
     std::string file_name = url.GetPath();
-    CHECK(cookie_headers_map_.contains(file_name));
+    CHECK(base::Contains(cookie_headers_map_, file_name));
     std::string header = cookie_headers_map_[file_name];
     EXPECT_EQ(expected_value, header);
     cookie_headers_map_.erase(file_name);
@@ -2710,7 +2711,7 @@ class FencedFrameParameterizedBrowserTest : public FencedFrameBrowserTestBase {
     SCOPED_TRACE(from_here.ToString());
     DCHECK_CURRENTLY_ON(BrowserThread::UI);
     std::string file_name = url.GetPath();
-    CHECK(sec_fetch_dest_headers_map_.contains(file_name));
+    CHECK(base::Contains(sec_fetch_dest_headers_map_, file_name));
     std::string header = sec_fetch_dest_headers_map_[file_name];
     EXPECT_EQ(expected_value, header);
     sec_fetch_dest_headers_map_.erase(file_name);
@@ -7137,10 +7138,10 @@ class FencedFrameReportEventBrowserTest
     response->set_code(net::HTTP_OK);
     response->AddCustomHeader(cors::kAccessControlAllowMethods,
                               request->method_string);
-    if (request->headers.contains("Origin")) {
+    if (base::Contains(request->headers, "Origin")) {
       response->AddCustomHeader(cors::kAccessControlAllowOrigin, "*");
     }
-    if (request->headers.contains(cors::kAccessControlRequestHeaders)) {
+    if (base::Contains(request->headers, cors::kAccessControlRequestHeaders)) {
       response->AddCustomHeader(
           cors::kAccessControlAllowHeaders,
           request->headers.at(cors::kAccessControlRequestHeaders));
@@ -7200,7 +7201,7 @@ class FencedFrameReportEventBrowserTest
         ASSERT_FALSE(step.destination.path.empty());
         int redirect_index = 0;
         for (auto& redirect_destination : step.redirects) {
-          ASSERT_FALSE(paths.contains(redirect_destination.path));
+          ASSERT_FALSE(base::Contains(paths, redirect_destination.path));
           ASSERT_FALSE(redirect_destination.origin.empty());
           ASSERT_FALSE(redirect_destination.path.empty());
           paths.insert(redirect_destination.path);
@@ -7467,10 +7468,10 @@ class FencedFrameReportEventBrowserTest
               /*web_expected=*/true,
               /*os_expected=*/false);
         } else {
-          EXPECT_FALSE(response.http_request()->headers.contains(
-              "Attribution-Reporting-Eligible"));
-          EXPECT_FALSE(response.http_request()->headers.contains(
-              "Attribution-Reporting-Support"));
+          EXPECT_FALSE(base::Contains(response.http_request()->headers,
+                                      "Attribution-Reporting-Eligible"));
+          EXPECT_FALSE(base::Contains(response.http_request()->headers,
+                                      "Attribution-Reporting-Support"));
         }
 
         // TODO(crbug.com/40286778): Remove this check after 3PCD.
@@ -8234,9 +8235,11 @@ IN_PROC_BROWSER_TEST_F(FencedFrameReportEventBrowserTest,
         response.http_request()->headers.at("Attribution-Reporting-Support"),
         /*web_expected=*/true,
         /*os_expected=*/false);
-    EXPECT_TRUE(response.http_request()->headers.contains("Content-Length"));
-    EXPECT_TRUE(response.http_request()->headers.contains("Content-Type"));
-    EXPECT_TRUE(response.http_request()->headers.contains("Origin"));
+    EXPECT_TRUE(
+        base::Contains(response.http_request()->headers, "Content-Length"));
+    EXPECT_TRUE(
+        base::Contains(response.http_request()->headers, "Content-Type"));
+    EXPECT_TRUE(base::Contains(response.http_request()->headers, "Origin"));
 
     // Send 302 redirect response.
     GURL redirect_url = https_server()->GetURL("a.test", "/redirect.html");
@@ -8255,11 +8258,12 @@ IN_PROC_BROWSER_TEST_F(FencedFrameReportEventBrowserTest,
     EXPECT_EQ(redirect_response.http_request()->method,
               net::test_server::HttpMethod::METHOD_GET);
     // Check that POST-specific headers were stripped.
+    EXPECT_FALSE(base::Contains(redirect_response.http_request()->headers,
+                                "Content-Length"));
+    EXPECT_FALSE(base::Contains(redirect_response.http_request()->headers,
+                                "Content-Type"));
     EXPECT_FALSE(
-        redirect_response.http_request()->headers.contains("Content-Length"));
-    EXPECT_FALSE(
-        redirect_response.http_request()->headers.contains("Content-Type"));
-    EXPECT_FALSE(redirect_response.http_request()->headers.contains("Origin"));
+        base::Contains(redirect_response.http_request()->headers, "Origin"));
     // Check that the content body was stripped.
     EXPECT_TRUE(redirect_response.http_request()->content.empty());
     // These extra request headers were not stripped.
@@ -8353,11 +8357,12 @@ IN_PROC_BROWSER_TEST_F(FencedFrameReportEventBrowserTest,
     EXPECT_EQ(reporting_response.http_request()->method,
               net::test_server::HttpMethod::METHOD_POST);
     EXPECT_EQ(reporting_response.http_request()->content, event_data);
+    EXPECT_TRUE(base::Contains(reporting_response.http_request()->headers,
+                               "Content-Length"));
+    EXPECT_TRUE(base::Contains(reporting_response.http_request()->headers,
+                               "Content-Type"));
     EXPECT_TRUE(
-        reporting_response.http_request()->headers.contains("Content-Length"));
-    EXPECT_TRUE(
-        reporting_response.http_request()->headers.contains("Content-Type"));
-    EXPECT_TRUE(reporting_response.http_request()->headers.contains("Origin"));
+        base::Contains(reporting_response.http_request()->headers, "Origin"));
     ExpectValidAttributionReportingEligibleHeaderForEventBeacon(
         reporting_response.http_request()->headers.at(
             "Attribution-Reporting-Eligible"));
@@ -8385,10 +8390,10 @@ IN_PROC_BROWSER_TEST_F(FencedFrameReportEventBrowserTest,
     EXPECT_EQ(redirect_response.http_request()->method,
               net::test_server::HttpMethod::METHOD_GET);
     EXPECT_EQ(redirect_response.http_request()->headers.at("Origin"), "null");
-    EXPECT_FALSE(
-        redirect_response.http_request()->headers.contains("Content-Length"));
-    EXPECT_FALSE(
-        redirect_response.http_request()->headers.contains("Content-Type"));
+    EXPECT_FALSE(base::Contains(redirect_response.http_request()->headers,
+                                "Content-Length"));
+    EXPECT_FALSE(base::Contains(redirect_response.http_request()->headers,
+                                "Content-Type"));
     // Check that the content body was stripped.
     EXPECT_TRUE(redirect_response.http_request()->content.empty());
     // These extra request headers were not stripped.
@@ -9186,10 +9191,10 @@ class FencedFrameAutomaticBeaconBrowserTest
     response->set_code(net::HTTP_OK);
     response->AddCustomHeader(cors::kAccessControlAllowMethods,
                               request->method_string);
-    if (request->headers.contains("Origin")) {
+    if (base::Contains(request->headers, "Origin")) {
       response->AddCustomHeader(cors::kAccessControlAllowOrigin, "*");
     }
-    if (request->headers.contains(cors::kAccessControlRequestHeaders)) {
+    if (base::Contains(request->headers, cors::kAccessControlRequestHeaders)) {
       response->AddCustomHeader(
           cors::kAccessControlAllowHeaders,
           request->headers.at(cors::kAccessControlRequestHeaders));
diff --git a/content/browser/fenced_frame/fenced_frame_url_mapping.cc b/content/browser/fenced_frame/fenced_frame_url_mapping.cc
index 42b9de3b58..1de5f54c32 100644
--- a/content/browser/fenced_frame/fenced_frame_url_mapping.cc
+++ b/content/browser/fenced_frame/fenced_frame_url_mapping.cc
@@ -10,6 +10,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback.h"
 #include "base/memory/ref_counted.h"
@@ -496,11 +497,11 @@ void FencedFrameURLMapping::SubstituteMappedURL(
 }
 
 bool FencedFrameURLMapping::IsMapped(const GURL& urn_uuid) const {
-  return urn_uuid_to_url_map_.contains(urn_uuid);
+  return base::Contains(urn_uuid_to_url_map_, urn_uuid);
 }
 
 bool FencedFrameURLMapping::IsPendingMapped(const GURL& urn_uuid) const {
-  return pending_urn_uuid_to_url_map_.contains(urn_uuid);
+  return base::Contains(pending_urn_uuid_to_url_map_, urn_uuid);
 }
 
 bool FencedFrameURLMapping::IsFull() const {
diff --git a/content/browser/file_system_access/file_path_watcher/file_path_watcher_fsevents_change_tracker.cc b/content/browser/file_system_access/file_path_watcher/file_path_watcher_fsevents_change_tracker.cc
index ec1674abcf..01ef7ee6bc 100644
--- a/content/browser/file_system_access/file_path_watcher/file_path_watcher_fsevents_change_tracker.cc
+++ b/content/browser/file_system_access/file_path_watcher/file_path_watcher_fsevents_change_tracker.cc
@@ -6,8 +6,7 @@
 
 #include <sys/stat.h>
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "content/browser/file_system_access/file_path_watcher/file_path_watcher.h"
 
 namespace content {
@@ -123,7 +122,7 @@ void FilePathWatcherFSEventsChangeTracker::DispatchEvents(
     const auto& [event_flags, event_path, event_inode] = it->second;
 
     // Skip coalesced events.
-    if (std::ranges::contains(coalesced_event_ids, event_id)) {
+    if (base::Contains(coalesced_event_ids, event_id)) {
       continue;
     }
 
diff --git a/content/browser/file_system_access/file_path_watcher/file_path_watcher_inotify.cc b/content/browser/file_system_access/file_path_watcher/file_path_watcher_inotify.cc
index dd6105021e..6fea4be752 100644
--- a/content/browser/file_system_access/file_path_watcher/file_path_watcher_inotify.cc
+++ b/content/browser/file_system_access/file_path_watcher/file_path_watcher_inotify.cc
@@ -24,6 +24,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -889,7 +890,7 @@ FilePathWatcherImpl::FindChangedPathAndUpdateWatches(
     }
   }
 
-  if (recursive_paths_by_watch_.contains(fired_watch)) {
+  if (Contains(recursive_paths_by_watch_, fired_watch)) {
     base::FilePath child_path =
         recursive_paths_by_watch_[fired_watch].Append(child);
     if (!did_update) {
@@ -1107,7 +1108,7 @@ bool FilePathWatcherImpl::UpdateRecursiveWatches(
 
   // Check to see if this is a forced update or if some component of |target_|
   // has changed. For these cases, redo the watches for |target_| and below.
-  if (!recursive_paths_by_watch_.contains(fired_watch) &&
+  if (!Contains(recursive_paths_by_watch_, fired_watch) &&
       fired_watch != watches_.back().watch) {
     return UpdateRecursiveWatchesForPath(target_);
   }
@@ -1118,7 +1119,7 @@ bool FilePathWatcherImpl::UpdateRecursiveWatches(
   }
 
   const base::FilePath& changed_dir =
-      recursive_paths_by_watch_.contains(fired_watch)
+      Contains(recursive_paths_by_watch_, fired_watch)
           ? recursive_paths_by_watch_[fired_watch]
           : target_;
 
@@ -1181,7 +1182,7 @@ bool FilePathWatcherImpl::UpdateRecursiveWatchesForPath(
 
     // Check `recursive_watches_by_path_` as a heuristic to determine if this
     // needs to be an add or update operation.
-    if (!recursive_watches_by_path_.contains(current)) {
+    if (!Contains(recursive_watches_by_path_, current)) {
       // Try to add new watches.
       InotifyReader::Watch watch = GetInotifyReader().AddWatch(current, this);
       if (watch == InotifyReader::kWatchLimitExceeded) {
@@ -1229,8 +1230,8 @@ void FilePathWatcherImpl::TrackWatchForRecursion(InotifyReader::Watch watch,
     return;
   }
 
-  DUMP_WILL_BE_CHECK(!recursive_paths_by_watch_.contains(watch));
-  DUMP_WILL_BE_CHECK(!recursive_watches_by_path_.contains(path));
+  DUMP_WILL_BE_CHECK(!Contains(recursive_paths_by_watch_, watch));
+  DUMP_WILL_BE_CHECK(!Contains(recursive_watches_by_path_, path));
   recursive_paths_by_watch_[watch] = path;
   recursive_watches_by_path_[path] = watch;
 
diff --git a/content/browser/file_system_access/file_system_access_directory_handle_impl.cc b/content/browser/file_system_access/file_system_access_directory_handle_impl.cc
index b6c388d552..972c5ae40c 100644
--- a/content/browser/file_system_access/file_system_access_directory_handle_impl.cc
+++ b/content/browser/file_system_access/file_system_access_directory_handle_impl.cc
@@ -7,6 +7,7 @@
 #include <optional>
 
 #include "base/barrier_callback.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/i18n/file_util_icu.h"
diff --git a/content/browser/file_system_access/file_system_access_file_writer_impl_browsertest.cc b/content/browser/file_system_access/file_system_access_file_writer_impl_browsertest.cc
index 7f8117edfb..d838bb05a7 100644
--- a/content/browser/file_system_access/file_system_access_file_writer_impl_browsertest.cc
+++ b/content/browser/file_system_access/file_system_access_file_writer_impl_browsertest.cc
@@ -2,6 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/files/scoped_temp_dir.h"
diff --git a/content/browser/file_system_access/file_system_access_manager_impl.cc b/content/browser/file_system_access/file_system_access_manager_impl.cc
index b9ecd894b4..c22510de79 100644
--- a/content/browser/file_system_access/file_system_access_manager_impl.cc
+++ b/content/browser/file_system_access/file_system_access_manager_impl.cc
@@ -2160,7 +2160,8 @@ bool FileSystemAccessManagerImpl::IsSafePathComponent(
     // Check for both '/' and '\' as path separators, regardless of what OS
     // we're running on.
     return component16 != u"." && component16 != u".." &&
-           !component16.contains('/') && !component16.contains('\\');
+           !base::Contains(component16, '/') &&
+           !base::Contains(component16, '\\');
   }
 
   // base::i18n::IsFilenameLegal blocks names that start with '.', so strip out
diff --git a/content/browser/file_system_access/file_system_chooser_browsertest.cc b/content/browser/file_system_access/file_system_chooser_browsertest.cc
index d65d3838e3..2bddf3e38a 100644
--- a/content/browser/file_system_access/file_system_chooser_browsertest.cc
+++ b/content/browser/file_system_access/file_system_chooser_browsertest.cc
@@ -2,11 +2,11 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <memory>
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/files/scoped_temp_dir.h"
@@ -1080,12 +1080,12 @@ IN_PROC_BROWSER_TEST_F(FileSystemChooserBrowserTest, AcceptsOptions) {
             dialog_params_.file_types->extensions[0][0]);
   EXPECT_EQ(FILE_PATH_LITERAL("Js"),
             dialog_params_.file_types->extensions[0][1]);
-  EXPECT_TRUE(std::ranges::contains(dialog_params_.file_types->extensions[1],
-                                    FILE_PATH_LITERAL("jpg")));
-  EXPECT_TRUE(std::ranges::contains(dialog_params_.file_types->extensions[1],
-                                    FILE_PATH_LITERAL("jpeg")));
-  EXPECT_TRUE(std::ranges::contains(dialog_params_.file_types->extensions[2],
-                                    FILE_PATH_LITERAL("svg")));
+  EXPECT_TRUE(base::Contains(dialog_params_.file_types->extensions[1],
+                             FILE_PATH_LITERAL("jpg")));
+  EXPECT_TRUE(base::Contains(dialog_params_.file_types->extensions[1],
+                             FILE_PATH_LITERAL("jpeg")));
+  EXPECT_TRUE(base::Contains(dialog_params_.file_types->extensions[2],
+                             FILE_PATH_LITERAL("svg")));
 
   ASSERT_EQ(3u,
             dialog_params_.file_types->extension_description_overrides.size());
diff --git a/content/browser/find_request_manager.cc b/content/browser/find_request_manager.cc
index 5912020d07..874d90bc76 100644
--- a/content/browser/find_request_manager.cc
+++ b/content/browser/find_request_manager.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/queue.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
@@ -552,7 +553,7 @@ void FindRequestManager::RemoveFrame(RenderFrameHost* rfh) {
 
   // If no pending find replies are expected for the removed frame, then just
   // report the updated results.
-  if (!pending_initial_replies_.contains(rfh) &&
+  if (!base::Contains(pending_initial_replies_, rfh) &&
       pending_find_next_reply_ != rfh) {
     bool final_update =
         pending_initial_replies_.empty() && !pending_find_next_reply_;
@@ -807,7 +808,7 @@ RenderFrameHost* FindRequestManager::Traverse(RenderFrameHost* from_rfh,
     RenderFrameHost* current_rfh = rfh;
     if (!matches_only ||
         find_in_page_clients_.find(current_rfh)->second->number_of_matches() ||
-        pending_initial_replies_.contains(current_rfh)) {
+        base::Contains(pending_initial_replies_, current_rfh)) {
       // Note that if there is still a pending reply expected for this frame,
       // then it may have unaccounted matches and will not be skipped via
       // |matches_only|.
@@ -844,7 +845,7 @@ void FindRequestManager::AddFrame(RenderFrameHost* rfh, bool force) {
 bool FindRequestManager::CheckFrame(RenderFrameHost* rfh) const {
   // TODO(crbug.com/40196212): Convert IsFindInPageDisabled to a DCHECK when we
   // replace DidFinishLoad with DidFinishNavigation in FrameObserver.
-  if (!rfh || !find_in_page_clients_.contains(rfh) ||
+  if (!rfh || !base::Contains(find_in_page_clients_, rfh) ||
       IsFindInPageDisabled(rfh)) {
     return false;
   }
diff --git a/content/browser/first_party_sets/first_party_set_parser.cc b/content/browser/first_party_sets/first_party_set_parser.cc
index 3276dd8a42..e5b29e7597 100644
--- a/content/browser/first_party_sets/first_party_set_parser.cc
+++ b/content/browser/first_party_sets/first_party_set_parser.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/map_util.h"
@@ -500,10 +501,10 @@ class ParseContext {
     std::vector<std::pair<net::SchemefulSite, net::SchemefulSite>> aliases;
     for (const auto [site, site_alias_list] : *cctld_dict) {
       net::SchemefulSite site_as_schemeful_site((GURL(site)));
-      if (!std::ranges::contains(set_entries, site_as_schemeful_site,
-                                 [](const auto& site_and_entry) {
-                                   return site_and_entry.first;
-                                 })) {
+      if (!base::Contains(set_entries, site_as_schemeful_site,
+                          [](const auto& site_and_entry) {
+                            return site_and_entry.first;
+                          })) {
         warnings_.push_back(ParseWarning(
             ParseWarningType::kCctldKeyNotCanonical, {kCCTLDsField, site}));
         continue;
@@ -566,7 +567,7 @@ class ParseContext {
 
     if (result.has_site()) {
       const net::SchemefulSite& site = result.site();
-      if (std::ranges::contains(
+      if (base::Contains(
               set_entries, site,
               &std::pair<net::SchemefulSite, net::FirstPartySetEntry>::first)) {
         if (result.modified_host()) {
diff --git a/content/browser/first_party_sets/first_party_sets_handler_database_helper.cc b/content/browser/first_party_sets/first_party_sets_handler_database_helper.cc
index 0278ba7d6d..9b7d24f2ad 100644
--- a/content/browser/first_party_sets/first_party_sets_handler_database_helper.cc
+++ b/content/browser/first_party_sets/first_party_sets_handler_database_helper.cc
@@ -4,6 +4,7 @@
 
 #include "content/browser/first_party_sets/first_party_sets_handler_database_helper.h"
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "content/browser/first_party_sets/database/first_party_sets_database.h"
 #include "net/base/schemeful_site.h"
diff --git a/content/browser/generic_sensor/web_contents_sensor_provider_proxy.cc b/content/browser/generic_sensor/web_contents_sensor_provider_proxy.cc
index 0a987c03e3..d9c0d7f1f6 100644
--- a/content/browser/generic_sensor/web_contents_sensor_provider_proxy.cc
+++ b/content/browser/generic_sensor/web_contents_sensor_provider_proxy.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/ptr_util.h"
@@ -124,7 +125,7 @@ void WebContentsSensorProviderProxy::CreateVirtualSensor(
     device::mojom::VirtualSensorMetadataPtr metadata,
     device::mojom::SensorProvider::CreateVirtualSensorCallback callback) {
   // CHECK that this was called via CreateVirtualSensorForDevTools().
-  CHECK(virtual_sensor_types_for_devtools_.contains(type));
+  CHECK(base::Contains(virtual_sensor_types_for_devtools_, type));
   EnsureDeviceServiceConnection();
   sensor_provider_->CreateVirtualSensor(type, std::move(metadata),
                                         std::move(callback));
diff --git a/content/browser/gpu/gpu_internals_ui.cc b/content/browser/gpu/gpu_internals_ui.cc
index eb24f05c95..e66db0a495 100644
--- a/content/browser/gpu/gpu_internals_ui.cc
+++ b/content/browser/gpu/gpu_internals_ui.cc
@@ -12,6 +12,7 @@
 
 #include "base/base64.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/environment.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
diff --git a/content/browser/hid/hid_service.cc b/content/browser/hid/hid_service.cc
index ac730953a2..3e563d1ae0 100644
--- a/content/browser/hid/hid_service.cc
+++ b/content/browser/hid/hid_service.cc
@@ -4,11 +4,11 @@
 
 #include "content/browser/hid/hid_service.h"
 
-#include <algorithm>
 #include <map>
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -222,8 +222,8 @@ void HidService::RemoveProtectedReports(device::mojom::HidDeviceInfo& device,
     for (auto& report : collection->input_reports) {
       if ((is_fido && is_fido_allowed) ||
           !device.protected_input_report_ids.has_value() ||
-          !std::ranges::contains(*device.protected_input_report_ids,
-                                 report->report_id)) {
+          !base::Contains(*device.protected_input_report_ids,
+                          report->report_id)) {
         input_reports.push_back(std::move(report));
       }
     }
@@ -231,8 +231,8 @@ void HidService::RemoveProtectedReports(device::mojom::HidDeviceInfo& device,
     for (auto& report : collection->output_reports) {
       if ((is_fido && is_fido_allowed) ||
           !device.protected_output_report_ids.has_value() ||
-          !std::ranges::contains(*device.protected_output_report_ids,
-                                 report->report_id)) {
+          !base::Contains(*device.protected_output_report_ids,
+                          report->report_id)) {
         output_reports.push_back(std::move(report));
       }
     }
@@ -240,8 +240,8 @@ void HidService::RemoveProtectedReports(device::mojom::HidDeviceInfo& device,
     for (auto& report : collection->feature_reports) {
       if ((is_fido && is_fido_allowed) ||
           !device.protected_feature_report_ids.has_value() ||
-          !std::ranges::contains(*device.protected_feature_report_ids,
-                                 report->report_id)) {
+          !base::Contains(*device.protected_feature_report_ids,
+                          report->report_id)) {
         feature_reports.push_back(std::move(report));
       }
     }
diff --git a/content/browser/host_zoom_map_impl.cc b/content/browser/host_zoom_map_impl.cc
index 9652bb025e..d77640b14a 100644
--- a/content/browser/host_zoom_map_impl.cc
+++ b/content/browser/host_zoom_map_impl.cc
@@ -9,6 +9,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/time/default_clock.h"
 #include "base/values.h"
@@ -207,7 +208,7 @@ bool HostZoomMapImpl::HasZoomLevel(const std::string& scheme,
           ? scheme_iterator->second
           : host_zoom_levels_;
 
-  return zoom_levels.contains(host);
+  return base::Contains(zoom_levels, host);
 }
 
 double HostZoomMapImpl::GetZoomLevelForHostAndScheme(const std::string& scheme,
@@ -466,7 +467,7 @@ void HostZoomMapImpl::SetZoomLevelForWebContents(
 bool HostZoomMapImpl::UsesTemporaryZoomLevel(
     const GlobalRenderFrameHostId& rfh_id) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
-  return temporary_zoom_levels_.contains(rfh_id);
+  return base::Contains(temporary_zoom_levels_, rfh_id);
 }
 
 void HostZoomMapImpl::SetNoLongerUsesTemporaryZoomLevel(
diff --git a/content/browser/in_memory_federated_permission_context.cc b/content/browser/in_memory_federated_permission_context.cc
index 9c96aca648..45621b0e41 100644
--- a/content/browser/in_memory_federated_permission_context.cc
+++ b/content/browser/in_memory_federated_permission_context.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
 #include "content/browser/webid/flags.h"
diff --git a/content/browser/indexed_db/indexed_db_context_impl.cc b/content/browser/indexed_db/indexed_db_context_impl.cc
index 70fe94f617..1885f32325 100644
--- a/content/browser/indexed_db/indexed_db_context_impl.cc
+++ b/content/browser/indexed_db/indexed_db_context_impl.cc
@@ -18,6 +18,7 @@
 #include "base/barrier_callback.h"
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/feature_list.h"
 #include "base/files/file.h"
@@ -907,8 +908,8 @@ void IndexedDBContextImpl::PurgeOrigins() {
     // Delete the storage if its origin matches one of the origins to purge, or
     // if it is third-party and the top-level site is same-site with one of
     // those origins.
-    bool delete_bucket = origins_to_purge_on_shutdown_.contains(
-        bucket_locator.storage_key.origin());
+    bool delete_bucket = base::Contains(origins_to_purge_on_shutdown_,
+                                        bucket_locator.storage_key.origin());
 
     if (!delete_bucket && bucket_locator.storage_key.IsThirdPartyContext()) {
       delete_bucket = std::ranges::any_of(
diff --git a/content/browser/indexed_db/instance/bucket_context.cc b/content/browser/indexed_db/instance/bucket_context.cc
index 550e75f85f..a7f9b569f7 100644
--- a/content/browser/indexed_db/instance/bucket_context.cc
+++ b/content/browser/indexed_db/instance/bucket_context.cc
@@ -25,6 +25,7 @@
 #include "base/auto_reset.h"
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/feature_list.h"
 #include "base/files/file_path.h"
@@ -674,7 +675,7 @@ void BucketContext::DeleteDatabase(
     }
   }
 
-  if (!databases_.contains(name)) {
+  if (!base::Contains(databases_, name)) {
     // This adds `Database` in an uninitialized state.
     CreateAndAddDatabase(name);
   }
diff --git a/content/browser/indexed_db/instance/connection.cc b/content/browser/indexed_db/instance/connection.cc
index 5cf8ac86ee..edbfc9cad2 100644
--- a/content/browser/indexed_db/instance/connection.cc
+++ b/content/browser/indexed_db/instance/connection.cc
@@ -27,6 +27,7 @@
 #include "base/strings/strcat.h"
 #include "base/time/time.h"
 #include "base/trace_event/trace_event.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/unguessable_token.h"
 #include "components/services/storage/indexed_db/locks/partitioned_lock_id.h"
 #include "components/services/storage/indexed_db/locks/partitioned_lock_manager.h"
@@ -208,7 +209,7 @@ void Connection::DisallowInactiveClient(
     return;
   }
 
-  size_t reason_index = std::to_underlying(reason);
+  size_t reason_index = base::to_underlying(reason);
 
   if (client_keep_active_remotes_[reason_index].is_bound()) {
     // Since the keep_active remote is found in client_keep_active_remotes_,
diff --git a/content/browser/indexed_db/instance/database.cc b/content/browser/indexed_db/instance/database.cc
index b301ff2035..88fc27df61 100644
--- a/content/browser/indexed_db/instance/database.cc
+++ b/content/browser/indexed_db/instance/database.cc
@@ -963,7 +963,7 @@ Status Database::ClearOperation(
 }
 
 bool Database::IsObjectStoreIdInMetadata(int64_t object_store_id) const {
-  return metadata().object_stores.contains(object_store_id);
+  return base::Contains(metadata().object_stores, object_store_id);
 }
 
 bool Database::IsObjectStoreIdAndMaybeIndexIdInMetadata(
@@ -975,7 +975,7 @@ bool Database::IsObjectStoreIdAndMaybeIndexIdInMetadata(
   const IndexedDBObjectStoreMetadata& object_store_metadata =
       GetObjectStoreMetadata(object_store_id);
   return index_id == IndexedDBIndexMetadata::kInvalidId ||
-         object_store_metadata.indexes.contains(index_id);
+         base::Contains(object_store_metadata.indexes, index_id);
 }
 
 storage::mojom::IdbDatabaseMetadataPtr Database::GetIdbInternalsMetadata()
diff --git a/content/browser/indexed_db/instance/leveldb/active_blob_registry.cc b/content/browser/indexed_db/instance/leveldb/active_blob_registry.cc
index 9a3de20394..ad7c0059dc 100644
--- a/content/browser/indexed_db/instance/leveldb/active_blob_registry.cc
+++ b/content/browser/indexed_db/instance/leveldb/active_blob_registry.cc
@@ -4,6 +4,7 @@
 
 #include "content/browser/indexed_db/instance/leveldb/active_blob_registry.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/task/task_runner.h"
@@ -81,7 +82,7 @@ void ActiveBlobRegistry::MarkBlobActive(int64_t database_id,
 
   CHECK(KeyPrefix::IsValidDatabaseId(database_id));
   CHECK(DatabaseMetaDataKey::IsValidBlobNumber(blob_number));
-  CHECK(!deleted_dbs_.contains(database_id));
+  CHECK(!base::Contains(deleted_dbs_, database_id));
   bool outstanding_blobs_in_backing_store = !blob_reference_tracker_.empty();
   SingleDBMap& blobs_in_db = blob_reference_tracker_[database_id];
   auto iter = blobs_in_db.find(blob_number);
diff --git a/content/browser/indexed_db/instance/leveldb/backing_store.cc b/content/browser/indexed_db/instance/leveldb/backing_store.cc
index 9eb0a7b95f..05ec7a2879 100644
--- a/content/browser/indexed_db/instance/leveldb/backing_store.cc
+++ b/content/browser/indexed_db/instance/leveldb/backing_store.cc
@@ -20,6 +20,7 @@
 #include "base/byte_count.h"
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/dcheck_is_on.h"
 #include "base/files/file.h"
@@ -3140,7 +3141,7 @@ StatusOr<IndexedDBKey> BackingStore::Transaction::GetFirstPrimaryKeyForIndexKey(
 StatusOr<bool> BackingStore::DatabaseExists(std::u16string_view database_name) {
   return GetDatabaseNames().transform(
       [&](const std::vector<std::u16string>& names) {
-        return std::ranges::contains(names, database_name);
+        return base::Contains(names, database_name);
       });
 }
 
diff --git a/content/browser/interest_group/auction_nonce_manager.cc b/content/browser/interest_group/auction_nonce_manager.cc
index 82ba410025..e8878bb261 100644
--- a/content/browser/interest_group/auction_nonce_manager.cc
+++ b/content/browser/interest_group/auction_nonce_manager.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/uuid.h"
@@ -63,7 +64,7 @@ bool AuctionNonceManager::ClaimAuctionNonceIfAvailable(AuctionNonce nonce) {
   }
 
   uint32_t nonce_suffix = GetNonceSuffix(nonce);
-  if (claimed_auction_nonce_suffixes_.contains(nonce_suffix)) {
+  if (base::Contains(claimed_auction_nonce_suffixes_, nonce_suffix)) {
     devtools_instrumentation::LogWorkletMessage(
         *frame_host_, blink::mojom::ConsoleMessageLevel::kError,
         "Invalid AuctionConfig. The config provided an auctionNonce value "
diff --git a/content/browser/interest_group/interest_group_auction.cc b/content/browser/interest_group/interest_group_auction.cc
index 7c58726bbb..b0b06db640 100644
--- a/content/browser/interest_group/interest_group_auction.cc
+++ b/content/browser/interest_group/interest_group_auction.cc
@@ -22,6 +22,7 @@
 
 #include "base/base64.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -6599,7 +6600,7 @@ bool InterestGroupAuction::OnParsedServerResponseImpl(
     blink::InterestGroupKey winning_group(response->interest_group_owner,
                                           response->interest_group_name);
     // Winning group must be a bidder.
-    if (!std::ranges::contains(response->bidding_groups, winning_group)) {
+    if (!base::Contains(response->bidding_groups, winning_group)) {
       errors_.push_back("runAdAuction(): Winning group must be a bidder");
       saved_response_.emplace();
       base::UmaHistogramEnumeration(
diff --git a/content/browser/interest_group/interest_group_browsertest.cc b/content/browser/interest_group/interest_group_browsertest.cc
index 5f63b305ed..35d86b681e 100644
--- a/content/browser/interest_group/interest_group_browsertest.cc
+++ b/content/browser/interest_group/interest_group_browsertest.cc
@@ -22,6 +22,7 @@
 #include "base/base64.h"
 #include "base/base64url.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span_reader.h"
@@ -1211,8 +1212,7 @@ class InterestGroupBrowserTest : public ContentBrowserTest {
         // Groups with different origins or joined by different origins should
         // not be modified in any way.
         EXPECT_EQ(group->bidding_browser_signals->join_count, final_join_count);
-      } else if (groups_to_keep &&
-                 std::ranges::contains(*groups_to_keep, name)) {
+      } else if (groups_to_keep && base::Contains(*groups_to_keep, name)) {
         // Interest groups that are excluded by name also should not be
         // modified.
         EXPECT_EQ(group->bidding_browser_signals->join_count, final_join_count);
@@ -19333,10 +19333,9 @@ IN_PROC_BROWSER_TEST_F(InterestGroupBrowserTest,
     // Use a second observer to wait until the last message is received.
     WebContentsConsoleObserver last_message_console_observer(
         shell()->web_contents());
-    if (std::ranges::contains(execution_targets_with_all_warnings,
-                              execution_target) ||
-        std::ranges::contains(execution_targets_with_join_warnings,
-                              execution_target)) {
+    if (base::Contains(execution_targets_with_all_warnings, execution_target) ||
+        base::Contains(execution_targets_with_join_warnings,
+                       execution_target)) {
       last_message_console_observer.SetPattern(WarningPermissionsPolicy(
           "join-ad-interest-group", "leaveAdInterestGroup"));
     } else {
@@ -19376,8 +19375,7 @@ IN_PROC_BROWSER_TEST_F(InterestGroupBrowserTest,
     EXPECT_EQ("done", UpdateInterestGroupsInJS(execution_target));
     EXPECT_EQ(kSuccess, LeaveInterestGroup(origin, "cars", execution_target));
 
-    if (std::ranges::contains(execution_targets_with_all_warnings,
-                              execution_target)) {
+    if (base::Contains(execution_targets_with_all_warnings, execution_target)) {
       EXPECT_TRUE(last_message_console_observer.Wait());
       ASSERT_EQ(4u, console_observer.messages().size());
 
@@ -19392,8 +19390,8 @@ IN_PROC_BROWSER_TEST_F(InterestGroupBrowserTest,
       EXPECT_EQ(WarningPermissionsPolicy("join-ad-interest-group",
                                          "leaveAdInterestGroup"),
                 console_observer.GetMessageAt(3));
-    } else if (std::ranges::contains(execution_targets_with_join_warnings,
-                                     execution_target)) {
+    } else if (base::Contains(execution_targets_with_join_warnings,
+                              execution_target)) {
       EXPECT_TRUE(last_message_console_observer.Wait());
       ASSERT_EQ(3u, console_observer.messages().size());
 
@@ -19406,9 +19404,8 @@ IN_PROC_BROWSER_TEST_F(InterestGroupBrowserTest,
       EXPECT_EQ(WarningPermissionsPolicy("join-ad-interest-group",
                                          "leaveAdInterestGroup"),
                 console_observer.GetMessageAt(2));
-    } else if (std::ranges::contains(
-                   execution_targets_with_run_auction_warnings,
-                   execution_target)) {
+    } else if (base::Contains(execution_targets_with_run_auction_warnings,
+                              execution_target)) {
       EXPECT_TRUE(last_message_console_observer.Wait());
       ASSERT_EQ(1u, console_observer.messages().size());
 
@@ -22655,8 +22652,7 @@ IN_PROC_BROWSER_TEST_F(InterestGroupBiddingAndAuctionServerBrowserTest,
     RenderFrameHost* execution_targets_with_message[] = {
         cross_origin_iframe, inner_cross_origin_iframe,
         same_origin_iframe_in_cross_origin_iframe};
-    if (std::ranges::contains(execution_targets_with_message,
-                              execution_target)) {
+    if (base::Contains(execution_targets_with_message, execution_target)) {
       EXPECT_TRUE(console_observer.Wait());
       EXPECT_EQ(WarningPermissionsPolicy("run-ad-auction",
                                          "getInterestGroupAdAuctionData"),
@@ -23330,8 +23326,7 @@ IN_PROC_BROWSER_TEST_F(
         cross_origin_iframe, inner_cross_origin_iframe,
         same_origin_iframe_in_cross_origin_iframe,
         same_origin_iframe_in_cross_origin_iframe2};
-    if (std::ranges::contains(execution_targets_with_message,
-                              execution_target)) {
+    if (base::Contains(execution_targets_with_message, execution_target)) {
       EXPECT_EQ(
           "NotAllowedError: Failed to execute 'getInterestGroupAdAuctionData' "
           "on 'Navigator': "
diff --git a/content/browser/interest_group/test_interest_group_private_aggregation_manager.cc b/content/browser/interest_group/test_interest_group_private_aggregation_manager.cc
index 647760aabe..858bff0ace 100644
--- a/content/browser/interest_group/test_interest_group_private_aggregation_manager.cc
+++ b/content/browser/interest_group/test_interest_group_private_aggregation_manager.cc
@@ -14,6 +14,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/notreached.h"
@@ -121,7 +122,7 @@ void TestInterestGroupPrivateAggregationManager::EnableDebugMode(
     blink::mojom::DebugKeyPtr debug_key) {
   const mojo::ReceiverId receiver_id = receiver_set_.current_receiver();
 
-  EXPECT_FALSE(private_aggregation_debug_details_.contains(receiver_id));
+  EXPECT_FALSE(base::Contains(private_aggregation_debug_details_, receiver_id));
 
   private_aggregation_debug_details_[receiver_id] =
       blink::mojom::DebugModeDetails::New(/*is_enabled=*/true,
@@ -146,8 +147,9 @@ TestInterestGroupPrivateAggregationManager::TakePrivateAggregationRequests() {
       private_aggregation_requests_map;
 
   for (auto& [receiver_id, requests] : private_aggregation_requests_) {
-    EXPECT_TRUE(private_aggregation_worklet_origins_.contains(receiver_id));
-    if (!private_aggregation_debug_details_.contains(receiver_id)) {
+    EXPECT_TRUE(
+        base::Contains(private_aggregation_worklet_origins_, receiver_id));
+    if (!base::Contains(private_aggregation_debug_details_, receiver_id)) {
       private_aggregation_debug_details_[receiver_id] =
           blink::mojom::DebugModeDetails::New();
     }
diff --git a/content/browser/loader/navigation_url_loader_impl.cc b/content/browser/loader/navigation_url_loader_impl.cc
index 3a3065c5c1..4ea63f72a2 100644
--- a/content/browser/loader/navigation_url_loader_impl.cc
+++ b/content/browser/loader/navigation_url_loader_impl.cc
@@ -4,7 +4,6 @@
 
 #include "content/browser/loader/navigation_url_loader_impl.h"
 
-#include <algorithm>
 #include <map>
 #include <memory>
 #include <optional>
@@ -13,6 +12,7 @@
 
 #include "base/check_is_test.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
@@ -684,8 +684,7 @@ void NavigationURLLoaderImpl::Start() {
     // Requests to WebUI scheme won't get redirected to/from other schemes
     // or be intercepted, so we just let it go here.
     std::string scheme = request_info_->common_params->url.GetScheme();
-    if (std::ranges::contains(URLDataManagerBackend::GetWebUISchemes(),
-                              scheme)) {
+    if (base::Contains(URLDataManagerBackend::GetWebUISchemes(), scheme)) {
       FrameTreeNode* frame_tree_node =
           FrameTreeNode::GloballyFindByID(frame_tree_node_id_);
       CHECK(frame_tree_node);
diff --git a/content/browser/loader/object_navigation_fallback_body_loader.cc b/content/browser/loader/object_navigation_fallback_body_loader.cc
index e50e1de0a9..9bd43dfad0 100644
--- a/content/browser/loader/object_navigation_fallback_body_loader.cc
+++ b/content/browser/loader/object_navigation_fallback_body_loader.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "content/browser/renderer_host/frame_tree_node.h"
diff --git a/content/browser/locks/lock_manager.h b/content/browser/locks/lock_manager.h
index 4ca7d03d9c..ef0f2b3946 100644
--- a/content/browser/locks/lock_manager.h
+++ b/content/browser/locks/lock_manager.h
@@ -15,6 +15,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
diff --git a/content/browser/media/active_media_session_controller.cc b/content/browser/media/active_media_session_controller.cc
index 8215febe5d..3cf58b6771 100644
--- a/content/browser/media/active_media_session_controller.cc
+++ b/content/browser/media/active_media_session_controller.cc
@@ -4,10 +4,10 @@
 
 #include "content/browser/media/active_media_session_controller.h"
 
-#include <algorithm>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/unguessable_token.h"
 #include "content/browser/browser_main_loop.h"
@@ -76,7 +76,7 @@ void ActiveMediaSessionController::MediaSessionActionsChanged(
         MediaSessionActionToKeyCode(action);
     if (!action_key_code.has_value())
       continue;
-    if (!std::ranges::contains(actions, action))
+    if (!base::Contains(actions, action))
       media_keys_listener_manager->StopWatchingMediaKey(*action_key_code, this,
                                                         request_id_);
   }
diff --git a/content/browser/media/audible_metrics.cc b/content/browser/media/audible_metrics.cc
index afbc6c7a71..23f95cafb6 100644
--- a/content/browser/media/audible_metrics.cc
+++ b/content/browser/media/audible_metrics.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/time/default_tick_clock.h"
 
@@ -35,7 +36,7 @@ void AudibleMetrics::UpdateAudibleWebContentsState(
 
 void AudibleMetrics::WebContentsDestroyed(const WebContents* web_contents,
                                           bool recently_audible) {
-  if (audible_web_contents_.contains(web_contents)) {
+  if (base::Contains(audible_web_contents_, web_contents)) {
     RemoveAudibleWebContents(web_contents);
   }
 }
diff --git a/content/browser/media/audio_stream_monitor.cc b/content/browser/media/audio_stream_monitor.cc
index 68ec44f688..be6d25a157 100644
--- a/content/browser/media/audio_stream_monitor.cc
+++ b/content/browser/media/audio_stream_monitor.cc
@@ -6,6 +6,7 @@
 
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "content/browser/renderer_host/render_frame_host_impl.h"
 #include "content/browser/web_contents/web_contents_impl.h"
 #include "content/public/browser/browser_task_traits.h"
@@ -258,7 +259,7 @@ void AudioStreamMonitor::UpdateStreams() {
 void AudioStreamMonitor::MaybeToggle() {
   const base::TimeTicks off_time =
       last_became_silent_time_ +
-      base::Milliseconds(std::to_underlying(kHoldOnMilliseconds));
+      base::Milliseconds(base::to_underlying(kHoldOnMilliseconds));
   const base::TimeTicks now = base::TimeTicks::Now();
   const bool should_stop_timer = is_audible_ || now >= off_time;
   const bool should_indicator_be_on = is_audible_ || !should_stop_timer;
diff --git a/content/browser/media/capture/sub_capture_target_id_web_contents_helper.cc b/content/browser/media/capture/sub_capture_target_id_web_contents_helper.cc
index c79f1692de..c743004d53 100644
--- a/content/browser/media/capture/sub_capture_target_id_web_contents_helper.cc
+++ b/content/browser/media/capture/sub_capture_target_id_web_contents_helper.cc
@@ -4,11 +4,11 @@
 
 #include "content/browser/media/capture/sub_capture_target_id_web_contents_helper.h"
 
-#include <algorithm>
 #include <string>
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
@@ -124,7 +124,7 @@ bool SubCaptureTargetIdWebContentsHelper::IsAssociatedWith(
   const std::vector<base::Token>& ids =
       (type == Type::kCropTarget) ? crop_ids_ : restriction_ids_;
 
-  return std::ranges::contains(ids, id);
+  return base::Contains(ids, id);
 }
 
 void SubCaptureTargetIdWebContentsHelper::ReadyToCommitNavigation(
diff --git a/content/browser/media/cdm_storage_manager.cc b/content/browser/media/cdm_storage_manager.cc
index 5a58e33222..85cd3791ff 100644
--- a/content/browser/media/cdm_storage_manager.cc
+++ b/content/browser/media/cdm_storage_manager.cc
@@ -233,7 +233,7 @@ void CdmStorageManager::DidOpenFile(const blink::StorageKey& storage_key,
 
   // Check whether this CDM file is in-use.
   CdmFileId id(file_name, cdm_type, storage_key);
-  if (cdm_files_.contains(id)) {
+  if (base::Contains(cdm_files_, id)) {
     std::move(callback).Run(Status::kInUse, mojo::NullAssociatedRemote());
     return;
   }
diff --git a/content/browser/media/media_internals.cc b/content/browser/media/media_internals.cc
index 94edb74c40..e543c0a81a 100644
--- a/content/browser/media/media_internals.cc
+++ b/content/browser/media/media_internals.cc
@@ -302,7 +302,7 @@ void MediaInternals::AudioLogImpl::SetWebContentsTitle() {
 
 std::string MediaInternals::AudioLogImpl::FormatCacheKey() {
   return base::StringPrintf("%d:%d:%d", owner_id_,
-                            std::to_underlying(component_), component_id_);
+                            base::to_underlying(component_), component_id_);
 }
 
 // static
@@ -349,7 +349,7 @@ void MediaInternals::AudioLogImpl::StoreComponentMetadata(
     base::DictValue* dict) {
   dict->Set("owner_id", owner_id_);
   dict->Set("component_id", component_id_);
-  dict->Set("component_type", std::to_underlying(component_));
+  dict->Set("component_type", base::to_underlying(component_));
 }
 
 MediaInternals* MediaInternals::GetInstance() {
@@ -640,7 +640,7 @@ MediaInternals::CreateAudioLogImpl(
     int render_frame_id) {
   base::AutoLock auto_lock(lock_);
   return std::make_unique<AudioLogImpl>(
-      owner_ids_[std::to_underlying(component)]++, component, this,
+      owner_ids_[base::to_underlying(component)]++, component, this,
       component_id, render_process_id, render_frame_id);
 }
 
diff --git a/content/browser/media/media_internals.h b/content/browser/media/media_internals.h
index 498c11f797..b5d25ae378 100644
--- a/content/browser/media/media_internals.h
+++ b/content/browser/media/media_internals.h
@@ -12,12 +12,12 @@
 #include <string>
 #include <string_view>
 #include <tuple>
-#include <utility>
 #include <vector>
 
 #include "base/functional/callback_forward.h"
 #include "base/scoped_multi_source_observation.h"
 #include "base/synchronization/lock.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "content/browser/media/media_internals_audio_focus_helper.h"
 #include "content/browser/media/media_internals_cdm_helper.h"
@@ -197,7 +197,7 @@ class CONTENT_EXPORT MediaInternals : public media::AudioLogFactory,
   bool can_update_ = false;
   base::DictValue audio_streams_cached_data_;
   std::array<int,
-             std::to_underlying(
+             base::to_underlying(
                  media::AudioLogFactory::AudioComponent::kAudiocomponentMax)>
       owner_ids_ = {};
 };
diff --git a/content/browser/media/midi_host.cc b/content/browser/media/midi_host.cc
index 787afee5c6..3e2cb2b69b 100644
--- a/content/browser/media/midi_host.cc
+++ b/content/browser/media/midi_host.cc
@@ -4,8 +4,7 @@
 
 #include "content/browser/media/midi_host.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/ptr_util.h"
@@ -235,7 +234,7 @@ void MidiHost::SendData(uint32_t port,
   // in JavaScript. The actual permission check for security purposes
   // happens here in the browser process.
   if (base::FeatureList::IsEnabled(blink::features::kBlockMidiByDefault)) {
-    if (!has_midi_permission_ && !std::ranges::contains(data, kSysExByte)) {
+    if (!has_midi_permission_ && !base::Contains(data, kSysExByte)) {
       has_midi_permission_ =
           ChildProcessSecurityPolicyImpl::GetInstance()->CanSendMidiMessage(
               renderer_process_id_);
@@ -249,7 +248,7 @@ void MidiHost::SendData(uint32_t port,
 
   // Check `has_midi_sysex_permission_` here to avoid searching kSysExByte in
   // large bulk data transfers for correct uses.
-  if (!has_midi_sysex_permission_ && std::ranges::contains(data, kSysExByte)) {
+  if (!has_midi_sysex_permission_ && base::Contains(data, kSysExByte)) {
     has_midi_sysex_permission_ =
         ChildProcessSecurityPolicyImpl::GetInstance()->CanSendMidiSysExMessage(
             renderer_process_id_);
diff --git a/content/browser/media/session/media_session_browsertest.cc b/content/browser/media/session/media_session_browsertest.cc
index 6966319c03..46edf62121 100644
--- a/content/browser/media/session/media_session_browsertest.cc
+++ b/content/browser/media/session/media_session_browsertest.cc
@@ -7,6 +7,7 @@
 #include <optional>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/run_loop.h"
 #include "base/strings/utf_string_conversions.h"
@@ -140,7 +141,7 @@ class MediaSessionBrowserTestBase : public ContentBrowserTest {
 
   bool WasURLVisited(const GURL& url) {
     base::AutoLock lock(visited_urls_lock_);
-    return visited_urls_.contains(url);
+    return base::Contains(visited_urls_, url);
   }
 
   MediaSession* SetupMediaImageTest(bool expect_media_image = true) {
diff --git a/content/browser/media/session/media_session_impl.cc b/content/browser/media/session/media_session_impl.cc
index 542bd7ee9a..d6540fb28c 100644
--- a/content/browser/media/session/media_session_impl.cc
+++ b/content/browser/media/session/media_session_impl.cc
@@ -1385,7 +1385,7 @@ void MediaSessionImpl::GetMediaImageBitmap(
   bool found = false;
   bool source_icon = false;
   for (auto& image_type : images_) {
-    if (std::ranges::contains(image_type.second, image)) {
+    if (base::Contains(image_type.second, image)) {
       found = true;
 
       if (image_type.first ==
@@ -1399,7 +1399,7 @@ void MediaSessionImpl::GetMediaImageBitmap(
   // Or the `image` is in chapters.
   if (!found) {
     for (auto& chapter : metadata_.chapters) {
-      if (std::ranges::contains(chapter.artwork(), image)) {
+      if (base::Contains(chapter.artwork(), image)) {
         found = true;
         break;
       }
diff --git a/content/browser/memory_coordinator/browser_memory_consumer_registry.cc b/content/browser/memory_coordinator/browser_memory_consumer_registry.cc
index 96e9e1a346..01f36518df 100644
--- a/content/browser/memory_coordinator/browser_memory_consumer_registry.cc
+++ b/content/browser/memory_coordinator/browser_memory_consumer_registry.cc
@@ -9,6 +9,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 
 namespace content {
 
@@ -45,7 +46,7 @@ void BrowserMemoryConsumerRegistry::ConsumerGroup::OnUpdateMemoryLimit() {
 
 void BrowserMemoryConsumerRegistry::ConsumerGroup::AddMemoryConsumer(
     base::RegisteredMemoryConsumer consumer) {
-  CHECK(!std::ranges::contains(memory_consumers_, consumer));
+  CHECK(!base::Contains(memory_consumers_, consumer));
   memory_consumers_.push_back(consumer);
 
   // Ensure the added consumer is up to date with the current memory limit
diff --git a/content/browser/mojo_binder_policy_applier.cc b/content/browser/mojo_binder_policy_applier.cc
index 7c673551f1..5058068160 100644
--- a/content/browser/mojo_binder_policy_applier.cc
+++ b/content/browser/mojo_binder_policy_applier.cc
@@ -6,6 +6,7 @@
 
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "content/public/browser/mojo_binder_policy_map.h"
 #include "mojo/public/cpp/bindings/message.h"
@@ -83,7 +84,7 @@ void MojoBinderPolicyApplier::ApplyPolicyToNonAssociatedBinder(
         std::move(binder_callback).Run();
         break;
       case MojoBinderNonAssociatedPolicy::kDefer:
-        if (kSyncMethodInterfaces.contains(interface_name)) {
+        if (base::Contains(kSyncMethodInterfaces, interface_name)) {
           std::move(binder_callback).Run();
         } else {
           deferred_binders_.push_back(std::move(binder_callback));
@@ -104,7 +105,7 @@ void MojoBinderPolicyApplier::ApplyPolicyToNonAssociatedBinder(
       }
       break;
     case MojoBinderNonAssociatedPolicy::kDefer:
-      if (kSyncMethodInterfaces.contains(interface_name)) {
+      if (base::Contains(kSyncMethodInterfaces, interface_name)) {
         deferred_sync_binders_.push_back(std::move(binder_callback));
       } else {
         deferred_binders_.push_back(std::move(binder_callback));
diff --git a/content/browser/navigation_browsertest.cc b/content/browser/navigation_browsertest.cc
index 271b514615..a4556b5511 100644
--- a/content/browser/navigation_browsertest.cc
+++ b/content/browser/navigation_browsertest.cc
@@ -11,6 +11,7 @@
 #include <string_view>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/files/scoped_temp_dir.h"
 #include "base/functional/bind.h"
@@ -2014,7 +2015,8 @@ IN_PROC_BROWSER_TEST_F(NavigationBaseBrowserTest, AddRequestHeaderOnRedirect) {
   // 1) There is no "header_name" header in the initial request.
   shell()->LoadURL(embedded_test_server()->GetURL("/doc"));
   response_1.WaitForRequest();
-  EXPECT_FALSE(response_1.http_request()->headers.contains("header_name"));
+  EXPECT_FALSE(
+      base::Contains(response_1.http_request()->headers, "header_name"));
   response_1.Send(
       "HTTP/1.1 302 Moved Temporarily\r\nLocation: /new_doc\r\n\r\n");
   response_1.Done();
@@ -2113,7 +2115,8 @@ IN_PROC_BROWSER_TEST_F(NavigationBaseBrowserTest,
 
   // 2) The header is removed from the second request after the redirect.
   response_2.WaitForRequest();
-  EXPECT_FALSE(response_2.http_request()->headers.contains("header_name"));
+  EXPECT_FALSE(
+      base::Contains(response_2.http_request()->headers, "header_name"));
 }
 
 // Test NavigationRequest::CheckAboutSrcDoc()
@@ -5308,7 +5311,7 @@ class SubresourceLoadingTest : public NavigationBrowserTest {
       WebContents* current_contents =
           WebContents::FromRenderFrameHost(current_frame);
       DCHECK(current_contents);
-      if (visited_contents.contains(current_contents)) {
+      if (base::Contains(visited_contents, current_contents)) {
         break;
       }
       visited_contents.insert(current_contents);
diff --git a/content/browser/network/sandboxed_nqe_browsertest.cc b/content/browser/network/sandboxed_nqe_browsertest.cc
index d7878f43ac..fe8710bfc4 100644
--- a/content/browser/network/sandboxed_nqe_browsertest.cc
+++ b/content/browser/network/sandboxed_nqe_browsertest.cc
@@ -2,9 +2,9 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/run_loop.h"
 #include "base/test/scoped_feature_list.h"
 #include "build/build_config.h"
@@ -58,7 +58,7 @@ class TestNetworkQualityObserver
   }
 
   void WaitForNotification(net::EffectiveConnectionType run_loop_wait_type) {
-    if (std::ranges::contains(received_types_, run_loop_wait_type)) {
+    if (base::Contains(received_types_, run_loop_wait_type)) {
       received_types_.clear();
       return;
     }
diff --git a/content/browser/network/shared_dictionary_browsertest.cc b/content/browser/network/shared_dictionary_browsertest.cc
index 8ee9b3490a..2c57fc48c2 100644
--- a/content/browser/network/shared_dictionary_browsertest.cc
+++ b/content/browser/network/shared_dictionary_browsertest.cc
@@ -958,7 +958,7 @@ class SharedDictionaryBrowserTest
       return nullptr;
     }
     auto response = std::make_unique<net::test_server::BasicHttpResponse>();
-    if (request.headers.contains("Authorization")) {
+    if (base::Contains(request.headers, "Authorization")) {
       response->set_code(net::HTTP_OK);
       std::optional<std::string> dict_hash =
           GetAvailableDictionary(request.headers);
diff --git a/content/browser/network/trust_token_parameters_browsertest.cc b/content/browser/network/trust_token_parameters_browsertest.cc
index 658de0fec1..50fb54d485 100644
--- a/content/browser/network/trust_token_parameters_browsertest.cc
+++ b/content/browser/network/trust_token_parameters_browsertest.cc
@@ -2,6 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+#include "base/containers/contains.h"
 #include "base/strings/escape.h"
 #include "base/strings/strcat.h"
 #include "base/test/bind.h"
@@ -183,7 +184,7 @@ IN_PROC_BROWSER_TEST_F(TrustTokenPermissionsPolicyBrowsertest,
           [&](const network::mojom::URLLoaderFactoryParams* params,
               const url::Origin& origin, bool unused_is_for_isolated_world,
               bool unused_is_for_service_worker) {
-            if (origin.host().contains('b')) {
+            if (base::Contains(origin.host(), 'b')) {
               ASSERT_TRUE(params);
 
               ASSERT_THAT(params->trust_token_redemption_policy,
@@ -221,7 +222,7 @@ IN_PROC_BROWSER_TEST_F(TrustTokenPermissionsPolicyBrowsertest,
           [&](const network::mojom::URLLoaderFactoryParams* params,
               const url::Origin& origin, bool unused_is_for_isolated_world,
               bool unused_is_for_service_worker) {
-            if (origin.host().contains("b")) {
+            if (base::Contains(origin.host(), "b")) {
               ASSERT_TRUE(params);
 
               ASSERT_THAT(
@@ -260,7 +261,7 @@ IN_PROC_BROWSER_TEST_F(TrustTokenPermissionsPolicyBrowsertest,
           [&](const network::mojom::URLLoaderFactoryParams* params,
               const url::Origin& origin, bool unused_is_for_isolated_world,
               bool unused_is_for_service_worker) {
-            if (origin.host().contains("b")) {
+            if (base::Contains(origin.host(), "b")) {
               ASSERT_TRUE(params);
 
               ASSERT_THAT(params->trust_token_redemption_policy,
@@ -304,7 +305,7 @@ IN_PROC_BROWSER_TEST_F(TrustTokenPermissionsPolicyBrowsertest,
           [&](const network::mojom::URLLoaderFactoryParams* params,
               const url::Origin& origin, bool unused_is_for_isolated_world,
               bool unused_is_for_service_worker) {
-            if (origin.host().contains('b')) {
+            if (base::Contains(origin.host(), 'b')) {
               ASSERT_TRUE(params);
 
               ASSERT_THAT(params->trust_token_redemption_policy,
@@ -349,7 +350,7 @@ IN_PROC_BROWSER_TEST_F(TrustTokenPermissionsPolicyBrowsertest,
           [&](const network::mojom::URLLoaderFactoryParams* params,
               const url::Origin& origin, bool unused_is_for_isolated_world,
               bool unused_is_for_service_worker) {
-            if (origin.host().contains("b")) {
+            if (base::Contains(origin.host(), "b")) {
               ASSERT_TRUE(params);
 
               ASSERT_THAT(
@@ -396,7 +397,7 @@ IN_PROC_BROWSER_TEST_F(TrustTokenPermissionsPolicyBrowsertest,
           [&](const network::mojom::URLLoaderFactoryParams* params,
               const url::Origin& origin, bool unused_is_for_isolated_world,
               bool unused_is_for_service_worker) {
-            if (origin.host().contains("b")) {
+            if (base::Contains(origin.host(), "b")) {
               ASSERT_TRUE(params);
 
               ASSERT_THAT(params->trust_token_redemption_policy,
diff --git a/content/browser/payments/payment_app_content_unittest_base.cc b/content/browser/payments/payment_app_content_unittest_base.cc
index 556c982091..5258c038fb 100644
--- a/content/browser/payments/payment_app_content_unittest_base.cc
+++ b/content/browser/payments/payment_app_content_unittest_base.cc
@@ -10,6 +10,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/run_loop.h"
@@ -248,7 +249,7 @@ PaymentAppContentUnitTestBase::CreateUninitializedPaymentManager(
   // Find a last registered payment manager.
   for (const auto& candidate_manager :
        payment_app_context()->payment_managers_) {
-    if (!existing_managers.contains(candidate_manager.first)) {
+    if (!base::Contains(existing_managers, candidate_manager.first)) {
       return candidate_manager.first;
     }
   }
diff --git a/content/browser/payments/payment_app_context_impl.cc b/content/browser/payments/payment_app_context_impl.cc
index ffa395b300..f91b31051c 100644
--- a/content/browser/payments/payment_app_context_impl.cc
+++ b/content/browser/payments/payment_app_context_impl.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "content/browser/payments/payment_manager.h"
 #include "content/public/browser/browser_thread.h"
@@ -35,7 +36,7 @@ void PaymentAppContextImpl::CreatePaymentManagerForOrigin(
 void PaymentAppContextImpl::PaymentManagerHadConnectionError(
     PaymentManager* payment_manager) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
-  DCHECK(payment_managers_.contains(payment_manager));
+  DCHECK(base::Contains(payment_managers_, payment_manager));
 
   payment_managers_.erase(payment_manager);
 }
diff --git a/content/browser/payments/payment_app_database.cc b/content/browser/payments/payment_app_database.cc
index f6104b74cd..680df0a0f9 100644
--- a/content/browser/payments/payment_app_database.cc
+++ b/content/browser/payments/payment_app_database.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/time/time.h"
@@ -732,7 +733,7 @@ void PaymentAppDatabase::DidReadAllPaymentInstruments(
       continue;
 
     int64_t id = instrument_proto.registration_id();
-    if (!apps.contains(id))
+    if (!base::Contains(apps, id))
       continue;
 
     apps[id]->enabled_methods.emplace_back(instrument_proto.method());
diff --git a/content/browser/permissions/permission_overrides.cc b/content/browser/permissions/permission_overrides.cc
index 918ced38d0..7fc0d16645 100644
--- a/content/browser/permissions/permission_overrides.cc
+++ b/content/browser/permissions/permission_overrides.cc
@@ -4,10 +4,10 @@
 
 #include "content/browser/permissions/permission_overrides.h"
 
-#include <algorithm>
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/feature_list.h"
 #include "base/types/optional_ref.h"
@@ -214,8 +214,8 @@ void PermissionOverrides::GrantPermissions(
     // GEOLOCATION_APPROXIMATE.
     if (type != blink::PermissionType::GEOLOCATION_APPROXIMATE) {
       Set(requesting_origin, embedding_origin, type,
-          std::ranges::contains(permissions, type) ? PermissionStatus::GRANTED
-                                                   : PermissionStatus::DENIED);
+          base::Contains(permissions, type) ? PermissionStatus::GRANTED
+                                            : PermissionStatus::DENIED);
     }
   }
 }
diff --git a/content/browser/picture_in_picture/video_picture_in_picture_content_browsertest.cc b/content/browser/picture_in_picture/video_picture_in_picture_content_browsertest.cc
index fc56134bd2..b6ed565fae 100644
--- a/content/browser/picture_in_picture/video_picture_in_picture_content_browsertest.cc
+++ b/content/browser/picture_in_picture/video_picture_in_picture_content_browsertest.cc
@@ -697,7 +697,7 @@ IN_PROC_BROWSER_TEST_F(VideoPictureInPictureContentBrowserTest,
   // The overlay window should have received the favicon image.
   ASSERT_EQ(overlay_window()->favicon_images().size(), 1u);
   const std::string icon_src = overlay_window()->favicon_images()[0].src.spec();
-  EXPECT_TRUE(icon_src.contains("test.ico"))
+  EXPECT_TRUE(base::Contains(icon_src, "test.ico"))
       << "The icon source: \"" << icon_src << "\" should contain \"test.ico\"";
 
   // The overlay window should be able to retrieve the favicon image.
@@ -717,7 +717,7 @@ IN_PROC_BROWSER_TEST_F(VideoPictureInPictureContentBrowserTest,
       .WillOnce([](const std::vector<media_session::MediaImage>& images) {
         ASSERT_EQ(images.size(), 1u);
         const std::string icon_src = images[0].src.spec();
-        EXPECT_TRUE(icon_src.contains("new.ico"))
+        EXPECT_TRUE(base::Contains(icon_src, "new.ico"))
             << "The icon source: \"" << icon_src
             << "\" should contain \"new.ico\"";
       });
diff --git a/content/browser/plugin_list.cc b/content/browser/plugin_list.cc
index c5559ca290..ee18582595 100644
--- a/content/browser/plugin_list.cc
+++ b/content/browser/plugin_list.cc
@@ -12,6 +12,7 @@
 #include "base/check.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/lazy_instance.h"
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
@@ -106,7 +107,7 @@ void PluginList::LoadPlugins() {
   std::vector<base::FilePath> seen_plugin_paths;
   plugins_list_.clear();
   for (const WebPluginInfo& plugin_info : internal_plugins_) {
-    if (std::ranges::contains(seen_plugin_paths, plugin_info.path)) {
+    if (base::Contains(seen_plugin_paths, plugin_info.path)) {
       continue;
     }
     seen_plugin_paths.push_back(plugin_info.path);
diff --git a/content/browser/posix_file_descriptor_info_impl.cc b/content/browser/posix_file_descriptor_info_impl.cc
index 33d805488c..64d039fa48 100644
--- a/content/browser/posix_file_descriptor_info_impl.cc
+++ b/content/browser/posix_file_descriptor_info_impl.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 
 namespace content {
 
@@ -66,7 +67,7 @@ bool PosixFileDescriptorInfoImpl::HasID(int id) const {
 }
 
 bool PosixFileDescriptorInfoImpl::OwnsFD(base::PlatformFile file) {
-  return std::ranges::contains(owned_descriptors_, file, &base::ScopedFD::get);
+  return base::Contains(owned_descriptors_, file, &base::ScopedFD::get);
 }
 
 base::ScopedFD PosixFileDescriptorInfoImpl::ReleaseFD(base::PlatformFile file) {
diff --git a/content/browser/preloading/prefetch/prefetch_canary_checker.cc b/content/browser/preloading/prefetch/prefetch_canary_checker.cc
index 97b36cdf4d..fbce6b094f 100644
--- a/content/browser/preloading/prefetch/prefetch_canary_checker.cc
+++ b/content/browser/preloading/prefetch/prefetch_canary_checker.cc
@@ -15,6 +15,7 @@
 #include "base/strings/string_number_conversions.h"
 #include "base/task/thread_pool.h"
 #include "base/trace_event/trace_event.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "content/browser/preloading/prefetch/prefetch_dns_prober.h"
 #include "content/public/browser/browser_context.h"
@@ -81,7 +82,7 @@ std::string GenerateNetworkID(
       is_cellular
           // Don't care about cell connection type.
           ? "cell"
-          : base::NumberToString(std::to_underlying(connection_type));
+          : base::NumberToString(base::to_underlying(connection_type));
 
 // Further identify WiFi and cell connections. These calls are only supported
 // for Android devices.
diff --git a/content/browser/preloading/prefetch/prefetch_document_manager.cc b/content/browser/preloading/prefetch/prefetch_document_manager.cc
index 6e0c51ae21..3412483b8c 100644
--- a/content/browser/preloading/prefetch/prefetch_document_manager.cc
+++ b/content/browser/preloading/prefetch/prefetch_document_manager.cc
@@ -6,6 +6,7 @@
 
 #include <tuple>
 
+#include "base/containers/contains.h"
 #include "content/browser/browser_context_impl.h"
 #include "content/browser/preloading/prefetch/no_vary_search_helper.h"
 #include "content/browser/preloading/prefetch/prefetch_container.h"
@@ -125,7 +126,7 @@ void PrefetchDocumentManager::ProcessCandidates(
       continue;
     }
 
-    if (!url_set.contains(url)) {
+    if (!base::Contains(url_set, url)) {
       static_cast<PrefetchHandleImpl*>(prefetch.get())
           ->SetPrefetchStatusOnReleaseStartedPrefetch(
               PrefetchStatus::kPrefetchEvictedAfterCandidateRemoved);
diff --git a/content/browser/preloading/prefetch/prefetch_service.cc b/content/browser/preloading/prefetch/prefetch_service.cc
index b98c1999cf..9759994930 100644
--- a/content/browser/preloading/prefetch/prefetch_service.cc
+++ b/content/browser/preloading/prefetch/prefetch_service.cc
@@ -1437,7 +1437,7 @@ void PrefetchService::MayReleasePrefetch(
     return;
   }
 
-  if (!owned_prefetches().contains(prefetch_container->key())) {
+  if (!base::Contains(owned_prefetches(), prefetch_container->key())) {
     return;
   }
 
diff --git a/content/browser/preloading/preloading_decider.cc b/content/browser/preloading/preloading_decider.cc
index d26f1ba1de..63528618bf 100644
--- a/content/browser/preloading/preloading_decider.cc
+++ b/content/browser/preloading/preloading_decider.cc
@@ -609,7 +609,7 @@ PreloadingDecider::GetMergedSpeculationTagsFromSuitableCandidates(
   // Iterate through all suitable candidates and merge their tags.
   for (const auto& candidate_pair : suitable_candidates) {
     for (const auto& tag : candidate_pair.second->tags) {
-      if (!std::ranges::contains(merged_tags, tag)) {
+      if (!base::Contains(merged_tags, tag)) {
         merged_tags.push_back(tag);
       }
     }
diff --git a/content/browser/preloading/prerender/prerender_browsertest.cc b/content/browser/preloading/prerender/prerender_browsertest.cc
index 0b8550aad9..e8c4aebd43 100644
--- a/content/browser/preloading/prerender/prerender_browsertest.cc
+++ b/content/browser/preloading/prerender/prerender_browsertest.cc
@@ -2,7 +2,6 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <cstdint>
 #include <optional>
 #include <tuple>
@@ -8783,7 +8782,7 @@ IN_PROC_BROWSER_TEST_F(PrerenderBrowserTest, MultipleNewTabPrerendering) {
     PrerenderHostId host_id = prerender_helper()->AddPrerender(
         prerendering_url, /*eagerness=*/std::nullopt, "_blank");
 
-    EXPECT_FALSE(std::ranges::contains(prerender_host_ids, host_id));
+    EXPECT_FALSE(base::Contains(prerender_host_ids, host_id));
     prerender_host_ids.push_back(host_id);
 
     // Make sure that prerendering in a new tab creates new WebContentsImpl, not
@@ -8793,8 +8792,8 @@ IN_PROC_BROWSER_TEST_F(PrerenderBrowserTest, MultipleNewTabPrerendering) {
     ASSERT_TRUE(prerender_web_contents);
     EXPECT_NE(prerender_web_contents, web_contents_impl());
     ExpectWebContentsIsForNewTabPrerendering(*prerender_web_contents);
-    EXPECT_FALSE(std::ranges::contains(prerender_web_contents_list,
-                                       prerender_web_contents));
+    EXPECT_FALSE(
+        base::Contains(prerender_web_contents_list, prerender_web_contents));
     prerender_web_contents_list.push_back(prerender_web_contents);
   }
 
diff --git a/content/browser/preloading/prerender/prerender_host.cc b/content/browser/preloading/prerender/prerender_host.cc
index 3c59151d27..69eafcc3dd 100644
--- a/content/browser/preloading/prerender/prerender_host.cc
+++ b/content/browser/preloading/prerender/prerender_host.cc
@@ -4,7 +4,6 @@
 
 #include "content/browser/preloading/prerender/prerender_host.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 
@@ -747,7 +746,7 @@ void PrerenderHost::ReadyToCommitNavigation(
         base::FeatureList::IsEnabled(
             blink::features::kPrerender2CrossOriginIframes);
     if (is_prerender_2_cross_origin_iframes_enabled &&
-        std::ranges::contains(
+        base::Contains(
             parsed_headers->supports_loading_mode,
             network::mojom::LoadingMode::kPrerenderCrossOriginFrames)) {
       allow_cross_origin_subframe_navigation_ = true;
diff --git a/content/browser/preloading/prerender/prerender_host_registry.cc b/content/browser/preloading/prerender/prerender_host_registry.cc
index 0cdaa3f62b..8a7431d44d 100644
--- a/content/browser/preloading/prerender/prerender_host_registry.cc
+++ b/content/browser/preloading/prerender/prerender_host_registry.cc
@@ -4,10 +4,9 @@
 
 #include "content/browser/preloading/prerender/prerender_host_registry.h"
 
-#include <algorithm>
-
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/memory/memory_pressure_monitor.h"
@@ -1033,12 +1032,12 @@ void PrerenderHostRegistry::CancelHostsForTriggers(
   std::vector<PrerenderHostId> ids_to_be_deleted;
 
   for (auto& iter : prerender_host_by_id_) {
-    if (std::ranges::contains(trigger_types, iter.second->trigger_type())) {
+    if (base::Contains(trigger_types, iter.second->trigger_type())) {
       ids_to_be_deleted.push_back(iter.first);
     }
   }
   for (auto& iter : prerender_new_tab_handle_by_id_) {
-    if (std::ranges::contains(trigger_types, iter.second->trigger_type())) {
+    if (base::Contains(trigger_types, iter.second->trigger_type())) {
       // Prerendering into a new tab can be triggered by speculation rules only.
       CHECK(IsSpeculationRuleType(iter.second->trigger_type()));
       ids_to_be_deleted.push_back(iter.first);
diff --git a/content/browser/preloading/prerender/prerender_navigation_throttle.cc b/content/browser/preloading/prerender/prerender_navigation_throttle.cc
index 3817a26122..bfbfdfbad3 100644
--- a/content/browser/preloading/prerender/prerender_navigation_throttle.cc
+++ b/content/browser/preloading/prerender/prerender_navigation_throttle.cc
@@ -4,8 +4,6 @@
 
 #include "content/browser/preloading/prerender/prerender_navigation_throttle.h"
 
-#include <algorithm>
-
 #include "base/memory/ptr_util.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_split.h"
@@ -253,7 +251,7 @@ PrerenderNavigationThrottle::WillProcessResponse() {
   bool is_credentialed_prerender =
       navigation_request->response() &&
       navigation_request->response()->parsed_headers &&
-      std::ranges::contains(
+      base::Contains(
           navigation_request->response()->parsed_headers->supports_loading_mode,
           network::mojom::LoadingMode::kCredentialedPrerender);
   // Cancel prerendering when this is same-site cross-origin navigation but the
diff --git a/content/browser/preloading/prerenderer_impl.cc b/content/browser/preloading/prerenderer_impl.cc
index 96bf4ac5ce..8b4a279df5 100644
--- a/content/browser/preloading/prerenderer_impl.cc
+++ b/content/browser/preloading/prerenderer_impl.cc
@@ -279,8 +279,8 @@ void PrerendererImpl::ProcessCandidatesForPrerender(
   // removed from `started_prerenders_` via `OnCancel`.
   CHECK(std::find_if(started_prerenders_.begin(), started_prerenders_.end(),
                      [&](const PrerenderInfo& x) {
-                       return canceled_prerender_rules_set.contains(
-                           x.prerender_host_id);
+                       return base::Contains(canceled_prerender_rules_set,
+                                             x.prerender_host_id);
                      }) == started_prerenders_.end());
 
   // Actually start the candidates in their original order once the diffing is
diff --git a/content/browser/private_aggregation/private_aggregation_pending_contributions.cc b/content/browser/private_aggregation/private_aggregation_pending_contributions.cc
index 09f34506f3..7a6996282f 100644
--- a/content/browser/private_aggregation/private_aggregation_pending_contributions.cc
+++ b/content/browser/private_aggregation/private_aggregation_pending_contributions.cc
@@ -19,6 +19,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/numerics/safe_conversions.h"
@@ -103,7 +104,7 @@ void PrivateAggregationPendingContributions::AddToFinalUnmergedContributions(
        contributions) {
     ContributionMergeKey merge_key(contribution);
 
-    if (accepted_merge_keys.contains(merge_key)) {
+    if (base::Contains(accepted_merge_keys, merge_key)) {
       // Bound worst-case memory usage.
       constexpr size_t kMaxUnmergedContributions = 10'000;
       if (final_unmerged_contributions_.size() < kMaxUnmergedContributions) {
@@ -199,7 +200,7 @@ PrivateAggregationPendingContributions::CompileFinalUnmergedContributions(
   for (auto i = static_cast<int>(PAErrorEvent::kMinValue);
        i <= static_cast<int>(PAErrorEvent::kMaxValue); i++) {
     auto error_event = static_cast<PAErrorEvent>(i);
-    CHECK(was_error_triggered_.contains(error_event));
+    CHECK(base::Contains(was_error_triggered_, error_event));
     if (was_error_triggered_[error_event]) {
       AddToFinalUnmergedContributions(
           std::move(conditional_contributions_[error_event]),
diff --git a/content/browser/process_internals/process_internals_handler_impl.cc b/content/browser/process_internals/process_internals_handler_impl.cc
index 1a34ef6e25..66d9611b56 100644
--- a/content/browser/process_internals/process_internals_handler_impl.cc
+++ b/content/browser/process_internals/process_internals_handler_impl.cc
@@ -135,7 +135,7 @@ using IsolatedOriginSource = ChildProcessSecurityPolicy::IsolatedOriginSource;
             RenderFrameHostToFrameInfoNoTraverse(rfh, type);
         all_frame_info[rfh] = frame_info.get();
         RenderFrameHostImpl* parent = rfh->GetParentOrOuterDocumentOrEmbedder();
-        DCHECK(all_frame_info.contains(parent));
+        DCHECK(base::Contains(all_frame_info, parent));
         all_frame_info[parent]->subframes.push_back(std::move(frame_info));
         return RenderFrameHost::FrameIterationAction::kContinue;
       });
diff --git a/content/browser/renderer_host/agent_scheduling_group_host.cc b/content/browser/renderer_host/agent_scheduling_group_host.cc
index 7c27f68f07..929fadf596 100644
--- a/content/browser/renderer_host/agent_scheduling_group_host.cc
+++ b/content/browser/renderer_host/agent_scheduling_group_host.cc
@@ -5,6 +5,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/containers/unique_ptr_adapters.h"
 #include "base/feature_list.h"
 #include "base/memory/raw_ptr.h"
@@ -130,7 +131,7 @@ AgentSchedulingGroupHost* AgentSchedulingGroupHost::GetOrCreate(
   // RenderProcessHosts throughout its lifetime, but it should only ever see a
   // single AgentSchedulingGroupHost for a given RenderProcessHost.
 #if DCHECK_IS_ON()
-  DCHECK(!data->site_instance_groups.contains(&site_instance_group));
+  DCHECK(!base::Contains(data->site_instance_groups, &site_instance_group));
   data->site_instance_groups.insert(&site_instance_group);
 #endif
 
diff --git a/content/browser/renderer_host/back_forward_cache_impl.cc b/content/browser/renderer_host/back_forward_cache_impl.cc
index 8169853107..3abdf05934 100644
--- a/content/browser/renderer_host/back_forward_cache_impl.cc
+++ b/content/browser/renderer_host/back_forward_cache_impl.cc
@@ -13,6 +13,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/functional/bind.h"
 #include "base/memory/weak_ptr.h"
@@ -1923,7 +1924,7 @@ bool BackForwardCacheImpl::IsQueryAllowed(const GURL& current_url) {
       base::SplitString(current_url.query(), "&", base::TRIM_WHITESPACE,
                         base::SplitResult::SPLIT_WANT_NONEMPTY);
   for (const std::string& cgi_param : cgi_params) {
-    if (blocked_cgi_params_.contains(cgi_param)) {
+    if (base::Contains(blocked_cgi_params_, cgi_param)) {
       return false;
     }
   }
diff --git a/content/browser/renderer_host/compositor_impl_android.cc b/content/browser/renderer_host/compositor_impl_android.cc
index c3e36be987..81f220d4a9 100644
--- a/content/browser/renderer_host/compositor_impl_android.cc
+++ b/content/browser/renderer_host/compositor_impl_android.cc
@@ -17,6 +17,7 @@
 #include "base/android/scoped_java_ref.h"
 #include "base/auto_reset.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/memory/raw_ptr.h"
diff --git a/content/browser/renderer_host/cookie_utils.cc b/content/browser/renderer_host/cookie_utils.cc
index c0350929dc..9db660d5f3 100644
--- a/content/browser/renderer_host/cookie_utils.cc
+++ b/content/browser/renderer_host/cookie_utils.cc
@@ -511,7 +511,7 @@ void EmitCookieWarningsAndMetrics(
         cookie->cookie_or_line->is_cookie() &&
         cookie->cookie_or_line->get_cookie().Name().empty()) {
       cookie_set_has_empty_name = true;
-      if (cookie->cookie_or_line->get_cookie().Value().contains('=')) {
+      if (base::Contains(cookie->cookie_or_line->get_cookie().Value(), '=')) {
         cookie_set_has_empty_name_and_ambiguous_value = true;
       }
     }
diff --git a/content/browser/renderer_host/delegated_frame_host.cc b/content/browser/renderer_host/delegated_frame_host.cc
index a00f9447da..31acb51738 100644
--- a/content/browser/renderer_host/delegated_frame_host.cc
+++ b/content/browser/renderer_host/delegated_frame_host.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/bind.h"
diff --git a/content/browser/renderer_host/frame_tree.cc b/content/browser/renderer_host/frame_tree.cc
index 80a9205c74..1602a31070 100644
--- a/content/browser/renderer_host/frame_tree.cc
+++ b/content/browser/renderer_host/frame_tree.cc
@@ -8,10 +8,10 @@
 
 #include <algorithm>
 #include <queue>
-#include <ranges>
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/bind.h"
@@ -23,6 +23,7 @@
 #include "base/metrics/histogram_functions.h"
 #include "base/trace_event/optional_trace_event.h"
 #include "base/trace_event/typed_macros.h"
+#include "base/types/cxx23_from_range.h"
 #include "base/unguessable_token.h"
 #include "content/browser/renderer_host/batched_proxy_ipc_sender.h"
 #include "content/browser/renderer_host/navigation_controller_impl.h"
@@ -150,7 +151,7 @@ FrameTree::NodeIterator::NodeIterator(
       should_descend_into_inner_trees_(should_descend_into_inner_trees),
       include_delegate_nodes_for_inner_frame_trees_(
           include_delegate_nodes_for_inner_frame_trees),
-      queue_(std::from_range, starting_nodes) {
+      queue_(base::from_range, starting_nodes) {
   // If `include_delegate_nodes_for_inner_frame_trees_` is true then
   // `should_descend_into_inner_trees_` must be true.
   DCHECK(!include_delegate_nodes_for_inner_frame_trees_ ||
@@ -735,7 +736,7 @@ void FrameTree::RegisterRenderViewHost(RenderViewHostMapId id,
   TRACE_EVENT_INSTANT("navigation", "FrameTree::RegisterRenderViewHost",
                       ChromeTrackEvent::kRenderViewHost, *rvh);
   CHECK(!rvh->is_speculative());
-  bool rvh_id_already_in_map = render_view_host_map_.contains(id);
+  bool rvh_id_already_in_map = base::Contains(render_view_host_map_, id);
   bool rfh_in_bfcache =
       controller()
           .GetBackForwardCache()
diff --git a/content/browser/renderer_host/input/touch_selection_controller_client_aura.cc b/content/browser/renderer_host/input/touch_selection_controller_client_aura.cc
index 975cc01b9c..bab0af17fc 100644
--- a/content/browser/renderer_host/input/touch_selection_controller_client_aura.cc
+++ b/content/browser/renderer_host/input/touch_selection_controller_client_aura.cc
@@ -9,6 +9,7 @@
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/observer_list.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "content/browser/renderer_host/render_widget_host_delegate.h"
 #include "content/browser/renderer_host/render_widget_host_impl.h"
 #include "content/browser/renderer_host/render_widget_host_view_aura.h"
@@ -507,11 +508,11 @@ bool TouchSelectionControllerClientAura::IsCommandIdEnabled(
   bool readable = rwhva_->GetTextInputType() != ui::TEXT_INPUT_TYPE_PASSWORD;
   bool has_selection = !rwhva_->GetSelectedText().empty();
   switch (command_id) {
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCut):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCut):
       return editable && readable && has_selection;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
       return readable && has_selection;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kPaste): {
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kPaste): {
       std::u16string result;
       ui::DataTransferEndpoint data_dst = ui::DataTransferEndpoint(
           ui::EndpointType::kDefault, {.notify_if_restricted = false});
@@ -519,14 +520,14 @@ bool TouchSelectionControllerClientAura::IsCommandIdEnabled(
           ui::ClipboardBuffer::kCopyPaste, &data_dst, &result);
       return editable && !result.empty();
     }
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll): {
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll): {
       gfx::Range text_range;
       if (rwhva_->GetTextRange(&text_range)) {
         return text_range.length() > rwhva_->GetSelectedText().length();
       }
       return true;
     }
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord): {
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord): {
       gfx::Range text_range;
       if (rwhva_->GetTextRange(&text_range)) {
         return readable && !has_selection && !text_range.is_empty();
@@ -541,9 +542,9 @@ bool TouchSelectionControllerClientAura::IsCommandIdEnabled(
 void TouchSelectionControllerClientAura::ExecuteCommand(int command_id,
                                                         int event_flags) {
   if (command_id !=
-          std::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll) &&
+          base::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll) &&
       command_id !=
-          std::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord)) {
+          base::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord)) {
     rwhva_->selection_controller()->HideAndDisallowShowingAutomatically();
   }
   RenderWidgetHostDelegate* host_delegate = rwhva_->host()->delegate();
@@ -551,19 +552,19 @@ void TouchSelectionControllerClientAura::ExecuteCommand(int command_id,
     return;
 
   switch (command_id) {
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCut):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCut):
       host_delegate->Cut();
       break;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
       host_delegate->Copy();
       break;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kPaste):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kPaste):
       host_delegate->Paste();
       break;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll):
       host_delegate->SelectAll();
       break;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord):
       host_delegate->SelectAroundCaret(
           blink::mojom::SelectionGranularity::kWord,
           /*should_show_handle=*/true,
diff --git a/content/browser/renderer_host/input/touch_selection_controller_client_child_frame.cc b/content/browser/renderer_host/input/touch_selection_controller_client_child_frame.cc
index b0bb8e1e24..1f9ddb7fe3 100644
--- a/content/browser/renderer_host/input/touch_selection_controller_client_child_frame.cc
+++ b/content/browser/renderer_host/input/touch_selection_controller_client_child_frame.cc
@@ -6,6 +6,7 @@
 
 #include "base/check.h"
 #include "base/notreached.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "content/browser/renderer_host/render_widget_host_delegate.h"
 #include "content/browser/renderer_host/render_widget_host_impl.h"
 #include "content/browser/renderer_host/render_widget_host_view_child_frame.h"
@@ -172,11 +173,11 @@ bool TouchSelectionControllerClientChildFrame::IsCommandIdEnabled(
   bool readable = rwhv_->GetTextInputType() != ui::TEXT_INPUT_TYPE_PASSWORD;
   bool has_selection = !rwhv_->GetSelectedText().empty();
   switch (command_id) {
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCut):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCut):
       return editable && readable && has_selection;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
       return readable && has_selection;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kPaste): {
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kPaste): {
       std::u16string result;
       ui::DataTransferEndpoint data_dst = ui::DataTransferEndpoint(
           ui::EndpointType::kDefault, {.notify_if_restricted = false});
@@ -184,14 +185,14 @@ bool TouchSelectionControllerClientChildFrame::IsCommandIdEnabled(
           ui::ClipboardBuffer::kCopyPaste, &data_dst, &result);
       return editable && !result.empty();
     }
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll): {
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll): {
       gfx::Range text_range;
       if (rwhv_->GetTextRange(&text_range)) {
         return text_range.length() > rwhv_->GetSelectedText().length();
       }
       return true;
     }
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord): {
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord): {
       gfx::Range text_range;
       if (rwhv_->GetTextRange(&text_range)) {
         return readable && !has_selection && !text_range.is_empty();
@@ -206,9 +207,9 @@ bool TouchSelectionControllerClientChildFrame::IsCommandIdEnabled(
 void TouchSelectionControllerClientChildFrame::ExecuteCommand(int command_id,
                                                               int event_flags) {
   if (command_id !=
-          std::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll) &&
+          base::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll) &&
       command_id !=
-          std::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord)) {
+          base::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord)) {
     manager_->GetTouchSelectionController()
         ->HideAndDisallowShowingAutomatically();
   }
@@ -217,19 +218,19 @@ void TouchSelectionControllerClientChildFrame::ExecuteCommand(int command_id,
     return;
 
   switch (command_id) {
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCut):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCut):
       host_delegate->Cut();
       break;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
       host_delegate->Copy();
       break;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kPaste):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kPaste):
       host_delegate->Paste();
       break;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kSelectAll):
       host_delegate->SelectAll();
       break;
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kSelectWord):
       host_delegate->SelectAroundCaret(
           blink::mojom::SelectionGranularity::kWord,
           /*should_show_handle=*/true,
diff --git a/content/browser/renderer_host/media/media_devices_manager.cc b/content/browser/renderer_host/media/media_devices_manager.cc
index 63e6c1a2af..3d0b03fccd 100644
--- a/content/browser/renderer_host/media/media_devices_manager.cc
+++ b/content/browser/renderer_host/media/media_devices_manager.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -1525,8 +1526,8 @@ void MediaDevicesManager::MaybeStopRemovedInputDevices(
        current_snapshot_[static_cast<size_t>(type)]) {
     // If a device was removed, notify the MediaStreamManager to stop all
     // streams using that device.
-    if (!std::ranges::contains(new_snapshot, old_device_info.device_id,
-                               &blink::WebMediaDeviceInfo::device_id)) {
+    if (!base::Contains(new_snapshot, old_device_info.device_id,
+                        &blink::WebMediaDeviceInfo::device_id)) {
       stop_removed_input_device_cb_.Run(type, old_device_info);
 
       if (type == MediaDeviceType::kMediaAudioInput) {
diff --git a/content/browser/renderer_host/media/media_stream_manager.cc b/content/browser/renderer_host/media/media_stream_manager.cc
index ed51cf269d..29f944028f 100644
--- a/content/browser/renderer_host/media/media_stream_manager.cc
+++ b/content/browser/renderer_host/media/media_stream_manager.cc
@@ -15,6 +15,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/memory/ptr_util.h"
@@ -446,18 +447,17 @@ bool ChangeSourceSupported(const MediaStreamDevices& devices) {
     return false;  // Killswitch engaged.
   }
 
-  if (!std::ranges::contains(devices, MediaStreamType::DISPLAY_VIDEO_CAPTURE,
-                             &MediaStreamDevice::type) &&
-      !std::ranges::contains(devices,
-                             MediaStreamType::DISPLAY_VIDEO_CAPTURE_THIS_TAB,
-                             &MediaStreamDevice::type)) {
+  if (!base::Contains(devices, MediaStreamType::DISPLAY_VIDEO_CAPTURE,
+                      &MediaStreamDevice::type) &&
+      !base::Contains(devices, MediaStreamType::DISPLAY_VIDEO_CAPTURE_THIS_TAB,
+                      &MediaStreamDevice::type)) {
     return false;  // Not an API call that supports share-this-tab-instead.
   }
 
   if (!base::FeatureList::IsEnabled(
           media::kShareThisTabInsteadButtonGetDisplayMediaAudio) &&
-      std::ranges::contains(devices, MediaStreamType::DISPLAY_AUDIO_CAPTURE,
-                            &MediaStreamDevice::type)) {
+      base::Contains(devices, MediaStreamType::DISPLAY_AUDIO_CAPTURE,
+                     &MediaStreamDevice::type)) {
     // The user chose to capture audio, but the killswitch against
     // share-this-tab-instead with audio is engaged.
     return false;
diff --git a/content/browser/renderer_host/media/video_capture_controller.cc b/content/browser/renderer_host/media/video_capture_controller.cc
index 7f570e18fb..2185ba70c7 100644
--- a/content/browser/renderer_host/media/video_capture_controller.cc
+++ b/content/browser/renderer_host/media/video_capture_controller.cc
@@ -12,6 +12,7 @@
 #include <set>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/raw_ptr.h"
@@ -537,8 +538,8 @@ void VideoCaptureController::MakeClientUseBufferContext(
   DCHECK_CURRENTLY_ON(BrowserThread::IO);
   // On the first use of a BufferContext for a particular client, call
   // OnBufferCreated().
-  if (!std::ranges::contains(client->known_buffer_context_ids,
-                             frame_context->buffer_context_id())) {
+  if (!base::Contains(client->known_buffer_context_ids,
+                      frame_context->buffer_context_id())) {
     client->known_buffer_context_ids.push_back(
         frame_context->buffer_context_id());
     client->event_handler->OnNewBuffer(client->controller_id,
@@ -546,8 +547,8 @@ void VideoCaptureController::MakeClientUseBufferContext(
                                        frame_context->buffer_context_id());
   }
   // Ensure buffer is registered as in use by the client.
-  if (!std::ranges::contains(client->buffers_in_use,
-                             frame_context->buffer_context_id())) {
+  if (!base::Contains(client->buffers_in_use,
+                      frame_context->buffer_context_id())) {
     client->buffers_in_use.push_back(frame_context->buffer_context_id());
   } else {
     NOTREACHED() << "Unexpected duplicate buffer: "
diff --git a/content/browser/renderer_host/media/video_capture_host.cc b/content/browser/renderer_host/media/video_capture_host.cc
index 39cb85f38a..d34e5fd3de 100644
--- a/content/browser/renderer_host/media/video_capture_host.cc
+++ b/content/browser/renderer_host/media/video_capture_host.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/token.h"
@@ -151,8 +152,8 @@ void VideoCaptureHost::OnCaptureConfigurationChanged(
     const VideoCaptureControllerID& controller_id) {
   DCHECK_CURRENTLY_ON(BrowserThread::IO);
 
-  if (!controllers_.contains(controller_id) ||
-      !device_id_to_observer_map_.contains(controller_id)) {
+  if (!base::Contains(controllers_, controller_id) ||
+      !base::Contains(device_id_to_observer_map_, controller_id)) {
     return;
   }
 
@@ -288,7 +289,7 @@ void VideoCaptureHost::Start(
     return;
   }
 
-  DCHECK(!device_id_to_observer_map_.contains(device_id));
+  DCHECK(!base::Contains(device_id_to_observer_map_, device_id));
   auto& observer_in_map = device_id_to_observer_map_[device_id];
   observer_in_map.Bind(std::move(observer));
 
@@ -444,7 +445,7 @@ void VideoCaptureHost::OnNewCaptureVersion(
   DCHECK_CURRENTLY_ON(BrowserThread::IO);
 
   const VideoCaptureControllerID controller_id(device_id);
-  if (!controllers_.contains(controller_id)) {
+  if (!base::Contains(controllers_, controller_id)) {
     return;
   }
 
diff --git a/content/browser/renderer_host/media/video_capture_manager.cc b/content/browser/renderer_host/media/video_capture_manager.cc
index 2d5bb7c85a..e53b31e083 100644
--- a/content/browser/renderer_host/media/video_capture_manager.cc
+++ b/content/browser/renderer_host/media/video_capture_manager.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -262,7 +263,7 @@ void VideoCaptureManager::DoStopDevice(VideoCaptureController* controller) {
   DCHECK_CURRENTLY_ON(BrowserThread::IO);
   TRACE_EVENT0(TRACE_DISABLED_BY_DEFAULT("video_and_image_capture"),
                "VideoCaptureManager::DoStopDevice");
-  DCHECK(std::ranges::contains(controllers_, controller));
+  DCHECK(base::Contains(controllers_, controller));
 
   // If start request has not yet started processing, i.e. if it is not at the
   // beginning of the queue, remove it from the queue.
@@ -895,8 +896,8 @@ VideoCaptureManager::LookupControllerByMediaTypeAndDeviceId(
 bool VideoCaptureManager::IsControllerPointerValid(
     const VideoCaptureController* controller) const {
   DCHECK_CURRENTLY_ON(BrowserThread::IO);
-  return std::ranges::contains(controllers_, controller,
-                               &scoped_refptr<VideoCaptureController>::get);
+  return base::Contains(controllers_, controller,
+                        &scoped_refptr<VideoCaptureController>::get);
 }
 
 scoped_refptr<VideoCaptureController>
diff --git a/content/browser/renderer_host/mixed_content_checker.cc b/content/browser/renderer_host/mixed_content_checker.cc
index f5f14eaa1f..0595f14b81 100644
--- a/content/browser/renderer_host/mixed_content_checker.cc
+++ b/content/browser/renderer_host/mixed_content_checker.cc
@@ -4,8 +4,7 @@
 
 #include "content/browser/renderer_host/mixed_content_checker.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/metrics/field_trial_params.h"
 #include "content/browser/preloading/prerender/prerender_final_status.h"
@@ -38,13 +37,13 @@ bool ShouldFetchKeepAliveCancelPrerenderingOnMixedContent() {
 }
 
 bool IsSecureScheme(const std::string& scheme) {
-  return std::ranges::contains(url::GetSecureSchemes(), scheme);
+  return base::Contains(url::GetSecureSchemes(), scheme);
 }
 
 // Should return the same value as `SecurityOrigin::IsLocal()` and
 // `blink::SchemeRegistry::ShouldTreatURLSchemeAsCorsEnabled()`.
 bool ShouldTreatURLSchemeAsCorsEnabled(const GURL& url) {
-  return std::ranges::contains(url::GetCorsEnabledSchemes(), url.GetScheme());
+  return base::Contains(url::GetCorsEnabledSchemes(), url.GetScheme());
 }
 
 // Should return the same value as the resource URL checks result from
diff --git a/content/browser/renderer_host/navigation_controller_impl_browsertest.cc b/content/browser/renderer_host/navigation_controller_impl_browsertest.cc
index 7e4a0503a7..97a95a8c0e 100644
--- a/content/browser/renderer_host/navigation_controller_impl_browsertest.cc
+++ b/content/browser/renderer_host/navigation_controller_impl_browsertest.cc
@@ -16884,7 +16884,7 @@ IN_PROC_BROWSER_TEST_P(NavigationControllerBrowserTest,
   EXPECT_TRUE(NavigateToURL(shell(), first_url));
   curr_spare_ids = spare_manager.GetSpareIds();
   curr_host = shell()->web_contents()->GetPrimaryMainFrame()->GetProcess();
-  EXPECT_FALSE(std::ranges::contains(curr_spare_ids, curr_host->GetID()));
+  EXPECT_FALSE(base::Contains(curr_spare_ids, curr_host->GetID()));
 
   // No process swap when navigating away from the initial blank page.
   EXPECT_EQ(prev_host, curr_host);
@@ -16919,7 +16919,7 @@ IN_PROC_BROWSER_TEST_P(NavigationControllerBrowserTest,
   EXPECT_NE(prev_host, curr_host);
   // If present, the spare RenderProcessHost should have been be used.
   if (!prev_spare_ids.empty()) {
-    EXPECT_TRUE(std::ranges::contains(prev_spare_ids, curr_host->GetID()));
+    EXPECT_TRUE(base::Contains(prev_spare_ids, curr_host->GetID()));
   }
   // A new spare should be warmed-up in site-per-process mode.
   if (AreAllSitesIsolatedForTesting()) {
@@ -16941,7 +16941,7 @@ IN_PROC_BROWSER_TEST_P(NavigationControllerBrowserTest,
   EXPECT_NE(prev_host, curr_host);
   // If present, the spare RenderProcessHost should have been used.
   if (!prev_spare_ids.empty()) {
-    EXPECT_TRUE(std::ranges::contains(prev_spare_ids, curr_host->GetID()));
+    EXPECT_TRUE(base::Contains(prev_spare_ids, curr_host->GetID()));
   }
   // A new spare should be warmed-up in site-per-process mode.
   if (AreAllSitesIsolatedForTesting()) {
diff --git a/content/browser/renderer_host/navigation_request.cc b/content/browser/renderer_host/navigation_request.cc
index 5b79df01e0..4febf7751b 100644
--- a/content/browser/renderer_host/navigation_request.cc
+++ b/content/browser/renderer_host/navigation_request.cc
@@ -4,7 +4,6 @@
 
 #include "content/browser/renderer_host/navigation_request.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <string>
@@ -15,6 +14,7 @@
 #include "base/auto_reset.h"
 #include "base/check_is_test.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/debug/alias.h"
 #include "base/debug/crash_logging.h"
@@ -826,8 +826,8 @@ bool IsOptedInFencedFrame(const net::HttpResponseHeaders& http_headers) {
   network::mojom::SupportsLoadingModePtr result =
       network::ParseSupportsLoadingMode(http_headers);
   return !result.is_null() &&
-         std::ranges::contains(result->supported_modes,
-                               network::mojom::LoadingMode::kFencedFrame);
+         base::Contains(result->supported_modes,
+                        network::mojom::LoadingMode::kFencedFrame);
 }
 
 // If there are any "Origin-Trial" headers on the |response|, persist those
diff --git a/content/browser/renderer_host/plugin_registry_impl.cc b/content/browser/renderer_host/plugin_registry_impl.cc
index 21342e79f4..af3355c9e2 100644
--- a/content/browser/renderer_host/plugin_registry_impl.cc
+++ b/content/browser/renderer_host/plugin_registry_impl.cc
@@ -4,6 +4,7 @@
 
 #include "content/browser/renderer_host/plugin_registry_impl.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "content/browser/plugin_service_impl.h"
 #include "content/public/browser/content_browser_client.h"
@@ -55,7 +56,7 @@ void PluginRegistryImpl::GetPlugins(GetPluginsCallback callback) {
         plugin_blink->mime_types.push_back(std::move(mime_type_blink));
         if (!plugin_blink->may_use_external_handler) {
           plugin_blink->may_use_external_handler =
-              mime_handler_view_mime_types.contains(mime_type.mime_type);
+              base::Contains(mime_handler_view_mime_types, mime_type.mime_type);
         }
       }
       plugins.push_back(std::move(plugin_blink));
diff --git a/content/browser/renderer_host/render_frame_host_impl.cc b/content/browser/renderer_host/render_frame_host_impl.cc
index 46368e70af..75cb871259 100644
--- a/content/browser/renderer_host/render_frame_host_impl.cc
+++ b/content/browser/renderer_host/render_frame_host_impl.cc
@@ -18,6 +18,7 @@
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span_reader.h"
 #include "base/debug/crash_logging.h"
@@ -7896,8 +7897,8 @@ void RenderFrameHostImpl::AllowBindings(BindingsPolicySet bindings) {
       !RenderProcessHost::run_renderer_in_process()) {
     ProcessLock process_lock = GetProcess()->GetProcessLock();
     if (!process_lock.IsLockedToSite() ||
-        !std::ranges::contains(URLDataManagerBackend::GetWebUISchemes(),
-                               process_lock.GetProcessLockURL().GetScheme())) {
+        !base::Contains(URLDataManagerBackend::GetWebUISchemes(),
+                        process_lock.GetProcessLockURL().GetScheme())) {
       SCOPED_CRASH_KEY_STRING256("AllowBindings", "process_lock",
                                  process_lock.ToString());
       NOTREACHED() << "Calling AllowBindings for a process not locked to WebUI:"
@@ -10609,7 +10610,7 @@ void RenderFrameHostImpl::MaybeSendFencedFrameAutomaticReportingBeacon(
     // the event's "destination" for setReportEventDataForAutomaticBeacons().
     // For cross-origin frames, the data must be opted in to being used for
     // cross-origin beacons.
-    if (info && std::ranges::contains(info->destinations, destination) &&
+    if (info && base::Contains(info->destinations, destination) &&
         (is_same_origin || info->cross_origin_exposed)) {
       data = info->data;
     }
@@ -12618,7 +12619,7 @@ void RenderFrameHostImpl::CommitNavigation(
 
     // See if this is for WebUI.
     const auto& webui_schemes = URLDataManagerBackend::GetWebUISchemes();
-    if (std::ranges::contains(webui_schemes, effective_scheme)) {
+    if (base::Contains(webui_schemes, effective_scheme)) {
       mojo::PendingRemote<network::mojom::URLLoaderFactory> factory_for_webui =
           url_loader_factory::CreatePendingRemote(
               ContentBrowserClient::URLLoaderFactoryType::kDocumentSubResource,
@@ -13058,8 +13059,8 @@ void RenderFrameHostImpl::AddResourceTimingEntryForFailedSubframeNavigation(
       response_head->request_start, response_head->response_start, status_code,
       mime_type, response_head->load_timing, response_head->connection_info,
       response_head->alpn_negotiated_protocol,
-      std::ranges::contains(url::GetSecureSchemes(),
-                            url::Origin::Create(final_url).scheme()),
+      base::Contains(url::GetSecureSchemes(),
+                     url::Origin::Create(final_url).scheme()),
       response_head->is_validated, normalized_server_timing, completion_status);
 }
 
diff --git a/content/browser/renderer_host/render_frame_host_impl_browsertest.cc b/content/browser/renderer_host/render_frame_host_impl_browsertest.cc
index 47f75908c4..61bde1d068 100644
--- a/content/browser/renderer_host/render_frame_host_impl_browsertest.cc
+++ b/content/browser/renderer_host/render_frame_host_impl_browsertest.cc
@@ -4,7 +4,6 @@
 
 #include "content/browser/renderer_host/render_frame_host_impl.h"
 
-#include <algorithm>
 #include <initializer_list>
 #include <memory>
 #include <optional>
@@ -13,6 +12,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -5850,7 +5850,7 @@ ObjectData kInnerObject{10, {"getInnerId"}};
 class MockInnerObject : public blink::mojom::RemoteObject {
  public:
   void HasMethod(const std::string& name, HasMethodCallback callback) override {
-    std::move(callback).Run(std::ranges::contains(kInnerObject.methods, name));
+    std::move(callback).Run(base::Contains(kInnerObject.methods, name));
   }
   void GetMethods(GetMethodsCallback callback) override {
     std::move(callback).Run(kInnerObject.methods);
@@ -5876,7 +5876,7 @@ class MockObject : public blink::mojom::RemoteObject {
       mojo::PendingReceiver<blink::mojom::RemoteObject> receiver)
       : receiver_(this, std::move(receiver)) {}
   void HasMethod(const std::string& name, HasMethodCallback callback) override {
-    std::move(callback).Run(std::ranges::contains(kMainObject.methods, name));
+    std::move(callback).Run(base::Contains(kMainObject.methods, name));
   }
 
   void GetMethods(GetMethodsCallback callback) override {
diff --git a/content/browser/renderer_host/render_frame_host_manager.cc b/content/browser/renderer_host/render_frame_host_manager.cc
index 40bec6c27e..c677464d2d 100644
--- a/content/browser/renderer_host/render_frame_host_manager.cc
+++ b/content/browser/renderer_host/render_frame_host_manager.cc
@@ -15,6 +15,7 @@
 #include "base/check_op.h"
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
@@ -27,6 +28,7 @@
 #include "base/timer/elapsed_timer.h"
 #include "base/trace_event/named_trigger.h"
 #include "base/trace_event/typed_macros.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected.h"
 #include "base/unguessable_token.h"
 #include "build/build_config.h"
@@ -1139,8 +1141,8 @@ void RenderFrameHostManager::PrepareForCollectingPage(
     // new cross-process, cross-BrowsingInstance navigation, as well as any
     // restored proxies which are also in a different BrowsingInstance.
     if (group->IsRelatedSiteInstanceGroup(it.second->site_instance_group())) {
-      DCHECK(render_view_hosts->contains(
-          it.second->GetRenderViewHost()->GetSafeRef()));
+      DCHECK(base::Contains(*render_view_hosts,
+                            it.second->GetRenderViewHost()->GetSafeRef()));
       auto pair = proxy_hosts->insert({it.first, std::move(it.second)});
       bool insertion_took_place = pair.second;
       // There should be only one proxy for any given SiteInstanceGroup, so this
@@ -5175,9 +5177,9 @@ void RenderFrameHostManager::CommitPending(
       for (auto& proxy : proxy_hosts_to_restore) {
         // We only cache pages when swapping BrowsingInstance, so we should
         // never be reusing SiteInstanceGroups.
-        CHECK(!render_frame_host_->browsing_context_state()
-                   ->proxy_hosts()
-                   .contains(proxy.second->site_instance_group()->GetId()));
+        CHECK(!base::Contains(
+            render_frame_host_->browsing_context_state()->proxy_hosts(),
+            proxy.second->site_instance_group()->GetId()));
         proxy.second->site_instance_group()->AddObserver(
             render_frame_host_->browsing_context_state().get());
         TRACE_EVENT_INSTANT(
diff --git a/content/browser/renderer_host/render_frame_proxy_host.cc b/content/browser/renderer_host/render_frame_proxy_host.cc
index bce2b4cafc..c2074dd5a0 100644
--- a/content/browser/renderer_host/render_frame_proxy_host.cc
+++ b/content/browser/renderer_host/render_frame_proxy_host.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/containers/circular_deque.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/hash/hash.h"
 #include "base/lazy_instance.h"
diff --git a/content/browser/renderer_host/render_process_host_impl.cc b/content/browser/renderer_host/render_process_host_impl.cc
index 8c6e66e4d3..b9d602a782 100644
--- a/content/browser/renderer_host/render_process_host_impl.cc
+++ b/content/browser/renderer_host/render_process_host_impl.cc
@@ -21,6 +21,7 @@
 #include "base/clang_profiling_buildflags.h"
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/map_util.h"
 #include "base/debug/alias.h"
@@ -3395,8 +3396,8 @@ bool RenderProcessHostImpl::HostHasNotBeenUsed() {
 }
 
 bool RenderProcessHostImpl::IsSpare() const {
-  return std::ranges::contains(
-      SpareRenderProcessHostManagerImpl::Get().GetSpares(), this);
+  return base::Contains(SpareRenderProcessHostManagerImpl::Get().GetSpares(),
+                        this);
 }
 
 void RenderProcessHostImpl::SetProcessLock(
diff --git a/content/browser/renderer_host/render_widget_host_browsertest.cc b/content/browser/renderer_host/render_widget_host_browsertest.cc
index 5d165b7ed6..b78a8db80f 100644
--- a/content/browser/renderer_host/render_widget_host_browsertest.cc
+++ b/content/browser/renderer_host/render_widget_host_browsertest.cc
@@ -2,10 +2,10 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <memory>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/run_loop.h"
@@ -295,8 +295,8 @@ IN_PROC_BROWSER_TEST_F(RenderWidgetHostTouchEmulatorBrowserTest,
     // that we generated a GestureScrollEnd and routed it without crashing.
     TestInputEventObserver::EventTypeVector dispatched_events =
         observer.GetAndResetDispatchedEventTypes();
-    EXPECT_TRUE(std::ranges::contains(
-        dispatched_events, blink::WebInputEvent::Type::kGestureScrollEnd));
+    EXPECT_TRUE(base::Contains(dispatched_events,
+                               blink::WebInputEvent::Type::kGestureScrollEnd));
   } while (!touch_emulator->suppress_next_fling_cancel_for_testing());
 }
 #endif  // !BUILDFLAG(IS_ANDROID)
diff --git a/content/browser/renderer_host/render_widget_host_impl.cc b/content/browser/renderer_host/render_widget_host_impl.cc
index 5f820ecdcb..122dabd5e6 100644
--- a/content/browser/renderer_host/render_widget_host_impl.cc
+++ b/content/browser/renderer_host/render_widget_host_impl.cc
@@ -16,6 +16,7 @@
 #include "base/auto_reset.h"
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/files/file_path.h"
@@ -1878,7 +1879,7 @@ input::InputRouter* RenderWidgetHostImpl::input_router() {
 
 void RenderWidgetHostImpl::AddKeyPressEventCallback(
     const KeyPressEventCallback& callback) {
-  CHECK(!std::ranges::contains(key_press_event_callbacks_, callback));
+  CHECK(!base::Contains(key_press_event_callbacks_, callback));
   key_press_event_callbacks_.push_back(callback);
 }
 
@@ -1889,7 +1890,7 @@ void RenderWidgetHostImpl::RemoveKeyPressEventCallback(
 
 void RenderWidgetHostImpl::AddMouseEventCallback(
     const MouseEventCallback& callback) {
-  CHECK(!std::ranges::contains(mouse_event_callbacks_, callback));
+  CHECK(!base::Contains(mouse_event_callbacks_, callback));
   mouse_event_callbacks_.push_back(callback);
 }
 
@@ -1900,7 +1901,7 @@ void RenderWidgetHostImpl::RemoveMouseEventCallback(
 
 void RenderWidgetHostImpl::AddSuppressShowingImeCallback(
     const SuppressShowingImeCallback& callback) {
-  CHECK(!std::ranges::contains(suppress_showing_ime_callbacks_, callback));
+  CHECK(!base::Contains(suppress_showing_ime_callbacks_, callback));
   suppress_showing_ime_callbacks_.push_back(callback);
 }
 
diff --git a/content/browser/renderer_host/render_widget_host_view_base.cc b/content/browser/renderer_host/render_widget_host_view_base.cc
index f63a7b0b7a..65d8fae81d 100644
--- a/content/browser/renderer_host/render_widget_host_view_base.cc
+++ b/content/browser/renderer_host/render_widget_host_view_base.cc
@@ -7,6 +7,7 @@
 #include <optional>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/lazy_instance.h"
diff --git a/content/browser/renderer_host/render_widget_host_view_browsertest.cc b/content/browser/renderer_host/render_widget_host_view_browsertest.cc
index 5ed6d64d6c..0c8b7aa5f2 100644
--- a/content/browser/renderer_host/render_widget_host_view_browsertest.cc
+++ b/content/browser/renderer_host/render_widget_host_view_browsertest.cc
@@ -4,7 +4,6 @@
 
 #include <stdint.h>
 
-#include <algorithm>
 #include <memory>
 #include <string>
 #include <utility>
@@ -884,7 +883,7 @@ IN_PROC_BROWSER_TEST_F(BFCachedRenderWidgetHostViewBrowserTest,
     const auto evicted_ids =
         static_cast<RenderWidgetHostImpl*>(rfh1->GetRenderWidgetHost())
             ->CollectSurfaceIdsForEviction();
-    ASSERT_TRUE(std::ranges::contains(evicted_ids, id_after_cached));
+    ASSERT_TRUE(base::Contains(evicted_ids, id_after_cached));
   }
 }
 
diff --git a/content/browser/renderer_host/spare_render_process_host_manager_browsertest.cc b/content/browser/renderer_host/spare_render_process_host_manager_browsertest.cc
index f1a3a4ea7a..e79106f2f6 100644
--- a/content/browser/renderer_host/spare_render_process_host_manager_browsertest.cc
+++ b/content/browser/renderer_host/spare_render_process_host_manager_browsertest.cc
@@ -2,7 +2,6 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <utility>
 
 #include "base/callback_list.h"
@@ -510,7 +509,7 @@ class SpareRendererContentBrowserClient
                       const GURL& site_url) override {
     const auto& spares = SpareRenderProcessHostManagerImpl::Get().GetSpares();
     if (!spares.empty()) {
-      return std::ranges::contains(spares, process_host);
+      return base::Contains(spares, process_host);
     }
     return true;
   }
diff --git a/content/browser/renderer_host/spare_render_process_host_manager_impl.cc b/content/browser/renderer_host/spare_render_process_host_manager_impl.cc
index b83abee880..704cfe5f48 100644
--- a/content/browser/renderer_host/spare_render_process_host_manager_impl.cc
+++ b/content/browser/renderer_host/spare_render_process_host_manager_impl.cc
@@ -4,7 +4,6 @@
 
 #include "content/browser/renderer_host/spare_render_process_host_manager_impl.h"
 
-#include <algorithm>
 #include <optional>
 
 #include "base/check.h"
@@ -895,7 +894,7 @@ void SpareRenderProcessHostManagerImpl::ReleaseSpare(
 
 void SpareRenderProcessHostManagerImpl::RenderProcessReady(
     RenderProcessHost* host) {
-  CHECK(std::ranges::contains(spare_rphs_, host));
+  CHECK(base::Contains(spare_rphs_, host));
 
   CHECK(process_startup_timer_);
   UMA_HISTOGRAM_TIMES("BrowserRenderProcessHost.SpareProcessStartupTime",
diff --git a/content/browser/renderer_host/stored_page.cc b/content/browser/renderer_host/stored_page.cc
index 5720148c13..b7412a99f3 100644
--- a/content/browser/renderer_host/stored_page.cc
+++ b/content/browser/renderer_host/stored_page.cc
@@ -4,6 +4,7 @@
 
 #include "content/browser/renderer_host/stored_page.h"
 
+#include "base/containers/contains.h"
 #include "base/trace_event/typed_macros.h"
 #include "content/browser/renderer_host/render_frame_host_impl.h"
 #include "content/browser/renderer_host/render_frame_proxy_host.h"
diff --git a/content/browser/service_worker/embedded_worker_instance.cc b/content/browser/service_worker/embedded_worker_instance.cc
index 194552ae4e..6a9e6ff546 100644
--- a/content/browser/service_worker/embedded_worker_instance.cc
+++ b/content/browser/service_worker/embedded_worker_instance.cc
@@ -4,10 +4,10 @@
 
 #include "content/browser/service_worker/embedded_worker_instance.h"
 
-#include <algorithm>
 #include <utility>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -951,7 +951,7 @@ EmbeddedWorkerInstance::CreateFactoryBundle(
     // redirects to data: URLs in ServiceWorkerGlobalScope
     // (https://crbug.com/1334249).
     if (scheme != url::kDataScheme &&
-        !std::ranges::contains(GetServiceWorkerSchemes(), scheme)) {
+        !base::Contains(GetServiceWorkerSchemes(), scheme)) {
       continue;
     }
 
diff --git a/content/browser/service_worker/service_worker_browsertest.cc b/content/browser/service_worker/service_worker_browsertest.cc
index 5167c8c8c6..af41161aca 100644
--- a/content/browser/service_worker/service_worker_browsertest.cc
+++ b/content/browser/service_worker/service_worker_browsertest.cc
@@ -13,6 +13,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -5175,25 +5176,27 @@ class ServiceWorkerStaticRouterRaceNetworkAndFetchHandlerSourceBrowserTest
     test_server->RegisterRequestHandler(base::BindRepeating(
         [](const net::test_server::HttpRequest& request)
             -> std::unique_ptr<net::test_server::HttpResponse> {
-          if (!request.GetURL().GetPath().contains(
-                  "/service_worker/mock_response") &&
-              !request.GetURL().GetPath().contains("/service_worker/no_race")) {
+          if (!base::Contains(request.GetURL().GetPath(),
+                              "/service_worker/mock_response") &&
+              !base::Contains(request.GetURL().GetPath(),
+                              "/service_worker/no_race")) {
             return nullptr;
           }
 
-          if (request.GetURL().GetQuery().contains("server_close_socket")) {
+          if (base::Contains(request.GetURL().GetQuery(),
+                             "server_close_socket")) {
             return std::make_unique<net::test_server::RawHttpResponse>("", "");
           }
 
           const bool is_slow =
-              request.GetURL().GetQuery().contains("server_slow");
+              base::Contains(request.GetURL().GetQuery(), "server_slow");
           auto http_response =
               is_slow ? std::make_unique<net::test_server::DelayedHttpResponse>(
                             base::Seconds(2))
                       : std::make_unique<net::test_server::BasicHttpResponse>();
 
           const char kQueryForRedirect[] = "server_redirect";
-          if (request.GetURL().GetQuery().contains(kQueryForRedirect)) {
+          if (base::Contains(request.GetURL().GetQuery(), kQueryForRedirect)) {
             http_response->set_code(net::HTTP_TEMPORARY_REDIRECT);
 
             const int pos = request.GetURL().GetQuery().find(kQueryForRedirect);
@@ -5206,12 +5209,13 @@ class ServiceWorkerStaticRouterRaceNetworkAndFetchHandlerSourceBrowserTest
             return http_response;
           }
 
-          if (!request.GetURL().GetQuery().contains(
-                  "server_unknown_mime_type")) {
+          if (!base::Contains(request.GetURL().GetQuery(),
+                              "server_unknown_mime_type")) {
             http_response->set_content_type("text/plain");
           }
 
-          if (request.GetURL().GetQuery().contains("server_large_data")) {
+          if (base::Contains(request.GetURL().GetQuery(),
+                             "server_large_data")) {
             // The data pipe buffer size created for the RaceNetworkRequest test
             // is 1024 byte. Set large data to overflow the buffer.
             http_response->set_content(std::string(1024 * 3, 'A'));
@@ -5221,7 +5225,7 @@ class ServiceWorkerStaticRouterRaceNetworkAndFetchHandlerSourceBrowserTest
             return http_response;
           }
 
-          if (request.GetURL().GetQuery().contains("server_notfound")) {
+          if (base::Contains(request.GetURL().GetQuery(), "server_notfound")) {
             http_response->set_code(net::HTTP_NOT_FOUND);
             http_response->set_content(
                 "[ServiceWorkerRaceNetworkRequest] Not found");
@@ -6997,15 +7001,16 @@ class ServiceWorkerStaticRouterBrowserTest : public ServiceWorkerBrowserTest {
     embedded_test_server()->RegisterRequestHandler(base::BindRepeating(
         [](const net::test_server::HttpRequest& request)
             -> std::unique_ptr<net::test_server::HttpResponse> {
-          if (request.GetURL().GetPath().contains("/service_worker/direct") ||
-              request.GetURL().GetPath().contains(
-                  "/service_worker/direct_if_not_running") ||
-              request.GetURL().GetPath().contains(
-                  "/service_worker/cache_with_wrong_name") ||
-              request.GetURL().GetPath().contains(
-                  "/service_worker/cache_miss") ||
-              request.GetURL().GetPath().contains(
-                  "/service_worker/not_not_match")) {
+          if (base::Contains(request.GetURL().GetPath(),
+                             "/service_worker/direct") ||
+              base::Contains(request.GetURL().GetPath(),
+                             "/service_worker/direct_if_not_running") ||
+              base::Contains(request.GetURL().GetPath(),
+                             "/service_worker/cache_with_wrong_name") ||
+              base::Contains(request.GetURL().GetPath(),
+                             "/service_worker/cache_miss") ||
+              base::Contains(request.GetURL().GetPath(),
+                             "/service_worker/not_not_match")) {
             auto http_response =
                 std::make_unique<net::test_server::BasicHttpResponse>();
             http_response->set_code(net::HTTP_OK);
@@ -7015,8 +7020,8 @@ class ServiceWorkerStaticRouterBrowserTest : public ServiceWorkerBrowserTest {
                 "Response from the network");
             return http_response;
           }
-          if (request.GetURL().GetPath().contains(
-                  "/service_worker/race_network_and_fetch")) {
+          if (base::Contains(request.GetURL().GetPath(),
+                             "/service_worker/race_network_and_fetch")) {
             auto http_response =
                 std::make_unique<net::test_server::BasicHttpResponse>();
             http_response->set_code(net::HTTP_OK);
@@ -7693,12 +7698,12 @@ class ServiceWorkerSyntheticResponseBrowserTest
     test_server->RegisterRequestHandler(base::BindRepeating(
         [](const net::test_server::HttpRequest& request)
             -> std::unique_ptr<net::test_server::HttpResponse> {
-          if (!request.GetURL().GetPath().contains(
-                  "/service_worker/synthetic_response")) {
+          if (!base::Contains(request.GetURL().GetPath(),
+                              "/service_worker/synthetic_response")) {
             return nullptr;
           }
 
-          if (request.GetURL().GetQuery().contains("redirect")) {
+          if (base::Contains(request.GetURL().GetQuery(), "redirect")) {
             auto response =
                 std::make_unique<net::test_server::BasicHttpResponse>();
             response->set_code(net::HTTP_TEMPORARY_REDIRECT);
@@ -7708,7 +7713,7 @@ class ServiceWorkerSyntheticResponseBrowserTest
           }
 
           const bool is_slow =
-              request.GetURL().GetQuery().contains("server_slow");
+              base::Contains(request.GetURL().GetQuery(), "server_slow");
 
           std::string headers =
               "HTTP/1.1 200 OK\r\n"
@@ -7718,28 +7723,29 @@ class ServiceWorkerSyntheticResponseBrowserTest
               "Date: Fri, 27 Jun 2025 10:50:00 JST\r\n"
               "Test-Duplicated-Header: x\r\n";
 
-          if (request.GetURL().GetQuery().contains(
-                  "header_mismatch_ignored_header")) {
+          if (base::Contains(request.GetURL().GetQuery(),
+                             "header_mismatch_ignored_header")) {
             headers += "Alt-Svc: h2=\":443\"; ma=2592000;\r\n";
-          } else if (request.GetURL().GetQuery().contains(
-                         "header_mismatch_with_duplicated_header")) {
+          } else if (base::Contains(request.GetURL().GetQuery(),
+                                    "header_mismatch_with_duplicated_header")) {
             headers +=
                 "Test-Duplicated-Header: y, z\r\n"
                 "Test-Duplicated-Header: x\r\n";
-          } else if (request.GetURL().GetQuery().contains("header_mismatch")) {
+          } else if (base::Contains(request.GetURL().GetQuery(),
+                                    "header_mismatch")) {
             headers += "X-Inconsistent-Header: ?1\r\n";
           }
 
           std::string content;
-          if (request.GetURL().GetQuery().contains("echo=foo")) {
+          if (base::Contains(request.GetURL().GetQuery(), "echo=foo")) {
             content = "[SyntheticResponse] foo";
-          } else if (request.GetURL().GetQuery().contains("echo=bar")) {
+          } else if (base::Contains(request.GetURL().GetQuery(), "echo=bar")) {
             content = "[SyntheticResponse] bar";
-          } else if (request.GetURL().GetQuery().contains(
-                         "inline_script_without_csp")) {
+          } else if (base::Contains(request.GetURL().GetQuery(),
+                                    "inline_script_without_csp")) {
             content = "<script>window.is_inline_script_executed=true;</script>";
-          } else if (request.GetURL().GetQuery().contains(
-                         "inline_script_with_csp")) {
+          } else if (base::Contains(request.GetURL().GetQuery(),
+                                    "inline_script_with_csp")) {
             content =
                 "<meta http-equiv=\"Content-Security-Policy\" "
                 "content=\"script-src 'nonce-jDHFShrQe4XmmH47DWyhaQ'\" />"
diff --git a/content/browser/service_worker/service_worker_client.cc b/content/browser/service_worker/service_worker_client.cc
index 2ca12d307d..dbedf555a6 100644
--- a/content/browser/service_worker/service_worker_client.cc
+++ b/content/browser/service_worker/service_worker_client.cc
@@ -9,6 +9,7 @@
 
 #include "base/check_is_test.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/debug/crash_logging.h"
 #include "base/notreached.h"
@@ -330,7 +331,7 @@ void ServiceWorkerClient::AddMatchingRegistration(
     return;
   }
   size_t key = registration->scope().spec().size();
-  if (matching_registrations_.contains(key)) {
+  if (base::Contains(matching_registrations_, key)) {
     return;
   }
   registration->AddListener(this);
diff --git a/content/browser/service_worker/service_worker_container_host.cc b/content/browser/service_worker/service_worker_container_host.cc
index dfb38e8a8a..3fd77e4d09 100644
--- a/content/browser/service_worker/service_worker_container_host.cc
+++ b/content/browser/service_worker/service_worker_container_host.cc
@@ -7,6 +7,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
@@ -600,7 +601,7 @@ ServiceWorkerRegistrationObjectManager::CreateInfo(
 void ServiceWorkerRegistrationObjectManager::RemoveHost(
     int64_t registration_id) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  DCHECK(registration_object_hosts_.contains(registration_id));
+  DCHECK(base::Contains(registration_object_hosts_, registration_id));
   // This is a workaround for a really unfavorable ownership structure of
   // service worker content code. This boils down to the following ownership
   // cycle:
@@ -672,7 +673,7 @@ ServiceWorkerObjectManager::GetOrCreateHost(
 
 void ServiceWorkerObjectManager::RemoveHost(int64_t version_id) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  DCHECK(service_worker_object_hosts_.contains(version_id));
+  DCHECK(base::Contains(service_worker_object_hosts_, version_id));
 
   // ServiceWorkerObjectHost to be deleted may have the last reference to
   // ServiceWorkerVersion that indirectly owns this ServiceWorkerContainerHost.
diff --git a/content/browser/service_worker/service_worker_process_manager.cc b/content/browser/service_worker/service_worker_process_manager.cc
index 93be2cb0c0..64afbc8e82 100644
--- a/content/browser/service_worker/service_worker_process_manager.cc
+++ b/content/browser/service_worker/service_worker_process_manager.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "content/browser/process_lock.h"
 #include "content/browser/service_worker/service_worker_context_wrapper.h"
 #include "content/browser/site_instance_impl.h"
@@ -104,7 +105,7 @@ ServiceWorkerProcessManager::AllocateWorkerProcess(
     return blink::ServiceWorkerStatusCode::kErrorAbort;
   }
 
-  DCHECK(!worker_process_map_.contains(embedded_worker_id))
+  DCHECK(!base::Contains(worker_process_map_, embedded_worker_id))
       << embedded_worker_id << " already has a process allocated";
 
   // Create a SiteInstance to get the renderer process from.
diff --git a/content/browser/service_worker/service_worker_registry.cc b/content/browser/service_worker/service_worker_registry.cc
index 9833e28325..f4188fdea2 100644
--- a/content/browser/service_worker/service_worker_registry.cc
+++ b/content/browser/service_worker/service_worker_registry.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
@@ -687,7 +688,7 @@ void ServiceWorkerRegistry::DeleteRegistration(
                      std::move(callback)),
       registration->id(), registration->key());
 
-  DCHECK(!uninstalling_registrations_.contains(registration->id()));
+  DCHECK(!base::Contains(uninstalling_registrations_, registration->id()));
   uninstalling_registrations_[registration->id()] = registration;
   registration->SetStatus(ServiceWorkerRegistration::Status::kUninstalling);
 }
@@ -1211,7 +1212,7 @@ ServiceWorkerRegistry::GetOrCreateRegistration(
   registration->SetStored();
   registration->set_resources_total_size_bytes(data.resources_total_size_bytes);
   registration->set_last_update_check(data.last_update_check);
-  DCHECK(!uninstalling_registrations_.contains(data.registration_id));
+  DCHECK(!base::Contains(uninstalling_registrations_, data.registration_id));
 
   scoped_refptr<ServiceWorkerVersion> version =
       context_->GetLiveVersion(data.version_id);
@@ -1276,7 +1277,7 @@ ServiceWorkerRegistry::FindFromLiveRegistrationsForId(int64_t registration_id) {
     // The registration is considered as findable when it's stored or in
     // installing state.
     if (registration->IsStored() ||
-        installing_registrations_.contains(registration_id)) {
+        base::Contains(installing_registrations_, registration_id)) {
       return registration;
     }
     // Otherwise, the registration should not be findable even if it's still
@@ -2101,7 +2102,7 @@ void ServiceWorkerRegistry::StartRemoteCall(
 }
 
 void ServiceWorkerRegistry::FinishRemoteCall(const InflightCall* call) {
-  DCHECK(inflight_calls_.contains(call));
+  DCHECK(base::Contains(inflight_calls_, call));
   inflight_calls_.erase(call);
 }
 
diff --git a/content/browser/service_worker/service_worker_script_cache_map.cc b/content/browser/service_worker/service_worker_script_cache_map.cc
index 2f841ea053..3267d2e83f 100644
--- a/content/browser/service_worker/service_worker_script_cache_map.cc
+++ b/content/browser/service_worker/service_worker_script_cache_map.cc
@@ -7,6 +7,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "content/browser/service_worker/service_worker_consts.h"
 #include "content/browser/service_worker/service_worker_context_core.h"
@@ -107,7 +108,7 @@ void ServiceWorkerScriptCacheMap::SetResources(
 void ServiceWorkerScriptCacheMap::UpdateSha256Checksum(
     const GURL& url,
     const std::string& sha256_checksum) {
-  DCHECK(resource_map_.contains(url));
+  DCHECK(base::Contains(resource_map_, url));
   resource_map_[url]->sha256_checksum = sha256_checksum;
 }
 
@@ -132,7 +133,7 @@ void ServiceWorkerScriptCacheMap::WriteMetadata(
   uint64_t callback_id = next_callback_id_++;
   mojo_base::BigBuffer buffer(base::as_bytes(data));
 
-  DCHECK(!callbacks_.contains(callback_id));
+  DCHECK(!base::Contains(callbacks_, callback_id));
   callbacks_[callback_id] = std::move(callback);
 
   mojo::Remote<storage::mojom::ServiceWorkerResourceMetadataWriter> writer;
diff --git a/content/browser/service_worker/service_worker_version.cc b/content/browser/service_worker/service_worker_version.cc
index b06ff89c38..761aef6f0e 100644
--- a/content/browser/service_worker/service_worker_version.cc
+++ b/content/browser/service_worker/service_worker_version.cc
@@ -13,6 +13,7 @@
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/bind.h"
@@ -788,7 +789,7 @@ ServiceWorkerExternalRequestResult ServiceWorkerVersion::StartExternalRequest(
     return ServiceWorkerExternalRequestResult::kWorkerNotRunning;
   }
 
-  if (external_request_uuid_to_request_id_.contains(request_uuid)) {
+  if (base::Contains(external_request_uuid_to_request_id_, request_uuid)) {
     return ServiceWorkerExternalRequestResult::kBadRequestId;
   }
 
@@ -932,7 +933,7 @@ void ServiceWorkerVersion::AddControllee(
   CHECK(!service_worker_client->client_uuid().empty());
   // TODO(crbug.com/40657227): Change to DCHECK once we figure out the cause of
   // crash.
-  CHECK(!controllee_map_.contains(uuid));
+  CHECK(!base::Contains(controllee_map_, uuid));
 
   controllee_map_[uuid] = service_worker_client->AsWeakPtr();
   // Even if `context_` is invalid, `controllee_map_` should have `uuid`.
@@ -972,7 +973,7 @@ void ServiceWorkerVersion::RemoveControllee(const std::string& client_uuid) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   // TODO(crbug.com/40653867): Remove this once RemoveControllee() matches with
   // AddControllee().
-  if (!controllee_map_.contains(client_uuid)) {
+  if (!base::Contains(controllee_map_, client_uuid)) {
     return;
   }
 
@@ -1011,8 +1012,8 @@ void ServiceWorkerVersion::OnControlleeNavigationCommitted(
 void ServiceWorkerVersion::MoveControlleeToBackForwardCacheMap(
     const std::string& client_uuid) {
   DCHECK(IsBackForwardCacheEnabled());
-  CHECK(controllee_map_.contains(client_uuid));
-  CHECK(!bfcached_controllee_map_.contains(client_uuid));
+  CHECK(base::Contains(controllee_map_, client_uuid));
+  CHECK(!base::Contains(bfcached_controllee_map_, client_uuid));
   bfcached_controllee_map_[client_uuid] = controllee_map_[client_uuid];
   RemoveControllee(client_uuid);
 }
@@ -1022,14 +1023,14 @@ void ServiceWorkerVersion::RestoreControlleeFromBackForwardCacheMap(
   // TODO(crbug.com/40657227): Change these to DCHECK once we figure out the
   // cause of crash.
   CHECK(IsBackForwardCacheEnabled());
-  CHECK(!controllee_map_.contains(client_uuid));
-  if (!bfcached_controllee_map_.contains(client_uuid)) {
+  CHECK(!base::Contains(controllee_map_, client_uuid));
+  if (!base::Contains(bfcached_controllee_map_, client_uuid)) {
     // We are navigating to the page using BackForwardCache, which is being
     // evicted due to activation, postMessage or claim. In this case, we reload
     // the page without using BackForwardCache, so we can assume that
     // ContainerHost will be deleted soon.
     // TODO(crbug.com/40657227): Remove this CHECK once we fix the crash.
-    CHECK(controllees_to_be_evicted_.contains(client_uuid));
+    CHECK(base::Contains(controllees_to_be_evicted_, client_uuid));
     // TODO(crbug.com/40657227): Remove DumpWithoutCrashing once we confirm the
     // cause of the crash.
     BackForwardCacheCanStoreDocumentResult can_store;
@@ -1053,8 +1054,8 @@ void ServiceWorkerVersion::RemoveControlleeFromBackForwardCacheMap(
   // TODO(crbug.com/341322515): Investigate why sometimes
   // `bfcache_controllee_map_` does not contain the client.
   SCOPED_CRASH_KEY_BOOL("ServiceWorkerBfcache", "in_controllee_map",
-                        controllee_map_.contains(client_uuid));
-  CHECK(bfcached_controllee_map_.contains(client_uuid));
+                        base::Contains(controllee_map_, client_uuid));
+  CHECK(base::Contains(bfcached_controllee_map_, client_uuid));
   bfcached_controllee_map_.erase(client_uuid);
 }
 
@@ -1062,9 +1063,9 @@ void ServiceWorkerVersion::Uncontrol(const std::string& client_uuid) {
   if (!IsBackForwardCacheEnabled()) {
     RemoveControllee(client_uuid);
   } else {
-    if (controllee_map_.contains(client_uuid)) {
+    if (base::Contains(controllee_map_, client_uuid)) {
       RemoveControllee(client_uuid);
-    } else if (bfcached_controllee_map_.contains(client_uuid)) {
+    } else if (base::Contains(bfcached_controllee_map_, client_uuid)) {
       RemoveControlleeFromBackForwardCacheMap(client_uuid);
     } else {
       // It is possible that the controllee belongs to neither |controllee_map_|
@@ -1074,7 +1075,7 @@ void ServiceWorkerVersion::Uncontrol(const std::string& client_uuid) {
       // In this case, |controllees_to_be_evicted_| should contain the
       // controllee.
       // TODO(crbug.com/40657227): Remove this CHECK once we fix the crash.
-      CHECK(controllees_to_be_evicted_.contains(client_uuid));
+      CHECK(base::Contains(controllees_to_be_evicted_, client_uuid));
       controllees_to_be_evicted_.erase(client_uuid);
     }
   }
@@ -3311,7 +3312,7 @@ void ServiceWorkerVersion::GetAssociatedInterface(
 
 bool ServiceWorkerVersion::BFCacheContainsControllee(
     const std::string& uuid) const {
-  return bfcached_controllee_map_.contains(uuid);
+  return base::Contains(bfcached_controllee_map_, uuid);
 }
 
 base::WeakPtr<ServiceWorkerVersion> ServiceWorkerVersion::GetWeakPtr() {
diff --git a/content/browser/shared_storage/shared_storage_browsertest.cc b/content/browser/shared_storage/shared_storage_browsertest.cc
index 044880542d..ebf1a64bae 100644
--- a/content/browser/shared_storage/shared_storage_browsertest.cc
+++ b/content/browser/shared_storage/shared_storage_browsertest.cc
@@ -16,6 +16,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -5902,11 +5903,11 @@ class SharedStorageHeaderObserverBrowserTest
       const std::vector<std::string>& extra_headers) {
     response.WaitForRequest();
     if (expect_writable_header) {
-      ASSERT_TRUE(response.http_request()->headers.contains(
-          "Sec-Shared-Storage-Writable"));
+      ASSERT_TRUE(base::Contains(response.http_request()->headers,
+                                 "Sec-Shared-Storage-Writable"));
     } else {
-      EXPECT_FALSE(response.http_request()->headers.contains(
-          "Sec-Shared-Storage-Writable"));
+      EXPECT_FALSE(base::Contains(response.http_request()->headers,
+                                  "Sec-Shared-Storage-Writable"));
     }
     EXPECT_EQ(response.http_request()->content, "");
     response.Send(http_status, content_type,
diff --git a/content/browser/site_info.cc b/content/browser/site_info.cc
index bf9f6c94e5..9c0438f051 100644
--- a/content/browser/site_info.cc
+++ b/content/browser/site_info.cc
@@ -8,6 +8,7 @@
 #include <optional>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/memory/safe_ref.h"
 #include "base/no_destructor.h"
@@ -52,8 +53,7 @@ WebUIDomains GetWebUIDomains(const GURL& url) {
 // to share a process whilst maintaining independent SiteURLs to allow for
 // WebUIType differentiation.
 bool IsWebUIAndUsesTLDForProcessLockURL(const GURL& url) {
-  if (!std::ranges::contains(URLDataManagerBackend::GetWebUISchemes(),
-                             url.scheme())) {
+  if (!base::Contains(URLDataManagerBackend::GetWebUISchemes(), url.scheme())) {
     return false;
   }
 
@@ -714,7 +714,7 @@ bool SiteInfo::ShouldLockProcessToSite(
   // Most WebUI processes should be locked on all platforms.  The only exception
   // is NTP, handled via the separate callout to the embedder.
   const auto& webui_schemes = URLDataManagerBackend::GetWebUISchemes();
-  if (std::ranges::contains(webui_schemes, site_url_.scheme())) {
+  if (base::Contains(webui_schemes, site_url_.scheme())) {
     return GetContentClient()->browser()->DoesWebUIUrlRequireProcessLock(
         site_url_);
   }
diff --git a/content/browser/site_instance_impl.cc b/content/browser/site_instance_impl.cc
index 7fff40908c..9e85bfc6eb 100644
--- a/content/browser/site_instance_impl.cc
+++ b/content/browser/site_instance_impl.cc
@@ -4,11 +4,11 @@
 
 #include "content/browser/site_instance_impl.h"
 
-#include <algorithm>
 #include <string>
 #include <tuple>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/no_destructor.h"
@@ -339,8 +339,8 @@ scoped_refptr<SiteInstanceImpl> SiteInstanceImpl::CreateForTesting(
 // static
 bool SiteInstanceImpl::ShouldAssignSiteForUrlInfo(const UrlInfo& url_info) {
   // Only empty document schemes can leave SiteInstances unassigned.
-  if (!std::ranges::contains(url::GetEmptyDocumentSchemes(),
-                             url_info.url.GetScheme())) {
+  if (!base::Contains(url::GetEmptyDocumentSchemes(),
+                      url_info.url.GetScheme())) {
     return true;
   }
 
diff --git a/content/browser/site_per_process_browsertest.cc b/content/browser/site_per_process_browsertest.cc
index 670bd8e6da..45fcdb8935 100644
--- a/content/browser/site_per_process_browsertest.cc
+++ b/content/browser/site_per_process_browsertest.cc
@@ -19,6 +19,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -14686,9 +14687,9 @@ IN_PROC_BROWSER_TEST_P(SitePerProcessBrowserTest,
   crash_observer.Wait();
 
   EXPECT_EQ(test_client.crashed_rfhs().size(), 2u);
-  EXPECT_TRUE(std::ranges::contains(test_client.crashed_rfhs(), rfh_b1));
-  EXPECT_TRUE(std::ranges::contains(test_client.crashed_rfhs(), rfh_b2));
-  EXPECT_FALSE(std::ranges::contains(test_client.crashed_rfhs(), rfh_b3));
+  EXPECT_TRUE(base::Contains(test_client.crashed_rfhs(), rfh_b1));
+  EXPECT_TRUE(base::Contains(test_client.crashed_rfhs(), rfh_b2));
+  EXPECT_FALSE(base::Contains(test_client.crashed_rfhs(), rfh_b3));
 }
 
 INSTANTIATE_TEST_SUITE_P(All,
diff --git a/content/browser/site_per_process_hit_test_browsertest.cc b/content/browser/site_per_process_hit_test_browsertest.cc
index eb0f36fc1f..54441aedd9 100644
--- a/content/browser/site_per_process_hit_test_browsertest.cc
+++ b/content/browser/site_per_process_hit_test_browsertest.cc
@@ -2,13 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <tuple>
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/json/json_reader.h"
@@ -2547,8 +2547,8 @@ IN_PROC_BROWSER_TEST_F(SitePerProcessHitTestBrowserTest, ScrollEventToOOPIF) {
 
   // Verify that this a mouse wheel event was sent to the child frame renderer.
   EXPECT_TRUE(child_frame_monitor.EventWasReceived());
-  EXPECT_TRUE(std::ranges::contains(child_frame_monitor.events_received(),
-                                    blink::WebInputEvent::Type::kMouseWheel));
+  EXPECT_TRUE(base::Contains(child_frame_monitor.events_received(),
+                             blink::WebInputEvent::Type::kMouseWheel));
 }
 
 IN_PROC_BROWSER_TEST_F(SitePerProcessHitTestBrowserTest,
@@ -3250,7 +3250,7 @@ class TooltipMonitor : public RenderWidgetHostViewBase::TooltipObserver {
 
   void WaitUntil(const std::u16string& tooltip_text) {
     tooltip_text_wanted_ = tooltip_text;
-    if (std::ranges::contains(tooltips_received_, tooltip_text))
+    if (base::Contains(tooltips_received_, tooltip_text))
       return;
     run_loop_->Run();
   }
@@ -4800,8 +4800,8 @@ IN_PROC_BROWSER_TEST_F(SitePerProcessMouseWheelHitTestBrowserTest,
 
   // Verify that this a mouse wheel event was sent to the child frame renderer.
   EXPECT_TRUE(child_frame_monitor.EventWasReceived());
-  EXPECT_TRUE(std::ranges::contains(child_frame_monitor.events_received(),
-                                    blink::WebInputEvent::Type::kMouseWheel));
+  EXPECT_TRUE(base::Contains(child_frame_monitor.events_received(),
+                             blink::WebInputEvent::Type::kMouseWheel));
 
   // Kill the wheel target view process. This must reset the wheel_target_.
   RenderProcessHost* child_process =
@@ -5298,9 +5298,8 @@ void SendTouchpadPinchSequenceWithExpectedTarget(
   UpdateEventRootLocation(&pinch_end, root_view_aura);
   root_view_aura->OnGestureEvent(&pinch_end);
   EXPECT_TRUE(target_monitor.EventWasReceived());
-  EXPECT_TRUE(
-      std::ranges::contains(target_monitor.events_received(),
-                            blink::WebInputEvent::Type::kGesturePinchEnd));
+  EXPECT_TRUE(base::Contains(target_monitor.events_received(),
+                             blink::WebInputEvent::Type::kGesturePinchEnd));
   EXPECT_EQ(nullptr, router_touchpad_gesture_target);
 }
 
diff --git a/content/browser/snapshot_browsertest.cc b/content/browser/snapshot_browsertest.cc
index a5ed85a696..c354805311 100644
--- a/content/browser/snapshot_browsertest.cc
+++ b/content/browser/snapshot_browsertest.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/rand_util.h"
@@ -355,7 +356,7 @@ IN_PROC_BROWSER_TEST_F(SnapshotBrowserTest, MAYBE_AsyncMultiWindowTest) {
       SkColor expected;
       do {
         expected = PickRandomColor();
-      } while (std::ranges::contains(expected_snapshots, expected));
+      } while (base::Contains(expected_snapshots, expected));
       expected_snapshots.push_back(expected);
 
       std::string html = SkColorToHtmlColor(expected);
diff --git a/content/browser/storage_partition_impl.cc b/content/browser/storage_partition_impl.cc
index c3e9ef303e..e032755db3 100644
--- a/content/browser/storage_partition_impl.cc
+++ b/content/browser/storage_partition_impl.cc
@@ -16,6 +16,7 @@
 #include "base/barrier_closure.h"
 #include "base/check_deref.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
diff --git a/content/browser/storage_partition_impl_map.cc b/content/browser/storage_partition_impl_map.cc
index 35c5b7da14..a71f31aa7f 100644
--- a/content/browser/storage_partition_impl_map.cc
+++ b/content/browser/storage_partition_impl_map.cc
@@ -9,6 +9,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
@@ -275,7 +276,7 @@ void BlockingGarbageCollect(
   }
   for (base::FilePath path = enumerator.Next(); !path.empty();
        path = enumerator.Next()) {
-    if (!active_paths.contains(path) && path != trash_directory) {
+    if (!base::Contains(active_paths, path) && path != trash_directory) {
       // Since |trash_directory| is unique for each run of this function there
       // can be no colllisions on the move.
       base::Move(path, trash_directory.Append(path.BaseName()));
diff --git a/content/browser/tracing/background_tracing_manager_impl.cc b/content/browser/tracing/background_tracing_manager_impl.cc
index 5eedfbdee8..1d1b9d5e64 100644
--- a/content/browser/tracing/background_tracing_manager_impl.cc
+++ b/content/browser/tracing/background_tracing_manager_impl.cc
@@ -4,7 +4,6 @@
 
 #include "content/browser/tracing/background_tracing_manager_impl.h"
 
-#include <algorithm>
 #include <optional>
 #include <utility>
 
@@ -634,8 +633,7 @@ BackgroundTracingManagerImpl::GetAllScenarios() const {
     new_scenario->scenario_name = scenario->scenario_name();
     new_scenario->description = scenario->description();
     new_scenario->is_local_scenario = scenario->is_local_scenario();
-    new_scenario->is_enabled =
-        std::ranges::contains(enabled_scenarios_, scenario);
+    new_scenario->is_enabled = base::Contains(enabled_scenarios_, scenario);
     new_scenario->current_state = scenario->current_state();
     return new_scenario;
   };
diff --git a/content/browser/web_contents/web_contents_impl.cc b/content/browser/web_contents/web_contents_impl.cc
index 345951d78d..a15ffe486a 100644
--- a/content/browser/web_contents/web_contents_impl.cc
+++ b/content/browser/web_contents/web_contents_impl.cc
@@ -21,6 +21,7 @@
 #include "base/base_switches.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/debug/crash_logging.h"
 #include "base/feature_list.h"
@@ -1051,7 +1052,7 @@ void WebContentsImpl::WebContentsTreeNode::DetachInnerWebContents(
 bool WebContentsImpl::WebContentsTreeNode::IsUnownedInnerWebContents(
     WebContents* inner_web_contents) const {
   CHECK_EQ(inner_web_contents->GetOuterWebContents(), current_web_contents_);
-  return std::ranges::contains(unowned_inner_web_contents_, inner_web_contents);
+  return base::Contains(unowned_inner_web_contents_, inner_web_contents);
 }
 
 void WebContentsImpl::WebContentsTreeNode::DetachUnownedInnerWebContents(
@@ -6032,7 +6033,7 @@ std::string WebContentsImpl::DumpAccessibilityTree(
   // This only runs during integration tests, or if a developer is
   // using an inspection tool, e.g. chrome://accessibility.
   ui::AXTreeManager::AlwaysFailFast();
-  DCHECK(std::ranges::contains(AXInspectFactory::SupportedApis(), api_type));
+  DCHECK(base::Contains(AXInspectFactory::SupportedApis(), api_type));
   std::unique_ptr<ui::AXTreeFormatter> formatter =
       AXInspectFactory::CreateFormatter(api_type);
 
@@ -6067,7 +6068,7 @@ void WebContentsImpl::RecordAccessibilityEvents(
         ax_mgr->GetBrowserAccessibilityRoot()
             ->GetTargetForNativeAccessibilityEvent();
 
-    DCHECK(std::ranges::contains(AXInspectFactory::SupportedApis(), api_type));
+    DCHECK(base::Contains(AXInspectFactory::SupportedApis(), api_type));
     event_recorder_ = content::AXInspectFactory::CreateRecorder(
         api_type, ax_mgr, pid, ui::AXTreeSelector(widget));
     event_recorder_->ListenToEvents(*callback);
diff --git a/content/browser/web_contents/web_contents_impl_browsertest.cc b/content/browser/web_contents/web_contents_impl_browsertest.cc
index e139cc339c..634b0d1586 100644
--- a/content/browser/web_contents/web_contents_impl_browsertest.cc
+++ b/content/browser/web_contents/web_contents_impl_browsertest.cc
@@ -13,6 +13,7 @@
 
 #include "base/allocator/partition_alloc_features.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -3676,7 +3677,7 @@ IN_PROC_BROWSER_TEST_F(WebContentsImplBrowserTest, SyncRendererPrefs) {
     DLOG(INFO) << "render_view_host=" << render_view_host;
 
     // Multiple frame hosts can be associated to the same RenderViewHost.
-    if (!std::ranges::contains(render_view_hosts, render_view_host)) {
+    if (!base::Contains(render_view_hosts, render_view_host)) {
       render_view_hosts.push_back(render_view_host);
     }
   }
@@ -4596,11 +4597,10 @@ IN_PROC_BROWSER_TEST_F(UnownedInnerWebContentsBrowserTest,
       inner_wc->GetPrimaryMainFrame()->GetParentOrOuterDocumentOrEmbedder());
   EXPECT_EQ(nullptr,
             inner_wc->GetPrimaryMainFrame()->GetParentOrOuterDocument());
-  EXPECT_TRUE(std::ranges::contains(CollectAllRenderFrameHosts(outer_wc),
-                                    inner_wc->GetPrimaryMainFrame()));
+  EXPECT_TRUE(base::Contains(CollectAllRenderFrameHosts(outer_wc),
+                             inner_wc->GetPrimaryMainFrame()));
 
-  EXPECT_TRUE(
-      std::ranges::contains(outer_wc->GetInnerWebContents(), inner_wc.get()));
+  EXPECT_TRUE(base::Contains(outer_wc->GetInnerWebContents(), inner_wc.get()));
 
   // Verify that the inner WebContents can navigate while attached.
   EXPECT_TRUE(NavigateToURL(inner_wc.get(), another_url));
@@ -4700,13 +4700,12 @@ IN_PROC_BROWSER_TEST_F(UnownedInnerWebContentsBrowserTest,
   // Verify that the connection is broken.
   EXPECT_EQ(nullptr, inner_wc->GetOuterWebContents());
   EXPECT_TRUE(inner_wc_impl->GetOuterDelegateFrameTreeNodeId().is_null());
-  EXPECT_FALSE(
-      std::ranges::contains(outer_wc->GetInnerWebContents(), inner_wc.get()));
+  EXPECT_FALSE(base::Contains(outer_wc->GetInnerWebContents(), inner_wc.get()));
   EXPECT_EQ(
       nullptr,
       inner_wc->GetPrimaryMainFrame()->GetParentOrOuterDocumentOrEmbedder());
-  EXPECT_FALSE(std::ranges::contains(CollectAllRenderFrameHosts(outer_wc),
-                                     inner_wc->GetPrimaryMainFrame()));
+  EXPECT_FALSE(base::Contains(CollectAllRenderFrameHosts(outer_wc),
+                              inner_wc->GetPrimaryMainFrame()));
   EXPECT_FALSE(iframe_rfh->frame_tree_node()->render_manager()
             ->is_attaching_inner_delegate());
 
diff --git a/content/browser/webauth/authenticator_common_impl.cc b/content/browser/webauth/authenticator_common_impl.cc
index a01341bacd..7f38b1dbcf 100644
--- a/content/browser/webauth/authenticator_common_impl.cc
+++ b/content/browser/webauth/authenticator_common_impl.cc
@@ -19,6 +19,7 @@
 #include "base/barrier_callback.h"
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
 #include "base/containers/to_vector.h"
@@ -642,8 +643,7 @@ void DeleteUnacceptedVirtualAuthenticatorCreds(
       if (registration.second.user && registration.second.rp &&
           registration.second.rp->id == relying_party_id &&
           registration.second.user->id == user_id &&
-          !std::ranges::contains(all_accepted_credentials_ids,
-                                 registration.first)) {
+          !base::Contains(all_accepted_credentials_ids, registration.first)) {
         credential_ids_to_remove.push_back(registration.first);
       }
     }
diff --git a/content/browser/webauth/authenticator_environment.cc b/content/browser/webauth/authenticator_environment.cc
index 6e2f4a4cda..d97f3a8cf5 100644
--- a/content/browser/webauth/authenticator_environment.cc
+++ b/content/browser/webauth/authenticator_environment.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/not_fatal_until.h"
 #include "content/browser/webauth/virtual_authenticator.h"
@@ -56,7 +57,7 @@ void AuthenticatorEnvironment::EnableVirtualAuthenticatorFor(
     bool enable_ui) {
   // Do not create a new virtual authenticator if there is one already defined
   // for the |node|.
-  if (virtual_authenticator_managers_.contains(node)) {
+  if (base::Contains(virtual_authenticator_managers_, node)) {
     return;
   }
 
@@ -70,7 +71,7 @@ void AuthenticatorEnvironment::EnableVirtualAuthenticatorFor(
 
 void AuthenticatorEnvironment::DisableVirtualAuthenticatorFor(
     FrameTreeNode* node) {
-  if (!virtual_authenticator_managers_.contains(node)) {
+  if (!base::Contains(virtual_authenticator_managers_, node)) {
     return;
   }
 
@@ -87,7 +88,7 @@ VirtualAuthenticatorManagerImpl*
 AuthenticatorEnvironment::MaybeGetVirtualAuthenticatorManager(
     FrameTreeNode* node) {
   for (; node; node = FrameTreeNode::From(node->parent())) {
-    if (virtual_authenticator_managers_.contains(node)) {
+    if (base::Contains(virtual_authenticator_managers_, node)) {
       return virtual_authenticator_managers_[node].get();
     }
   }
diff --git a/content/browser/webid/accounts_fetcher.cc b/content/browser/webid/accounts_fetcher.cc
index d7e65056f3..1ef1fa56cb 100644
--- a/content/browser/webid/accounts_fetcher.cc
+++ b/content/browser/webid/accounts_fetcher.cc
@@ -4,9 +4,9 @@
 
 #include "content/browser/webid/accounts_fetcher.h"
 
-#include <algorithm>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "content/browser/webid/config_fetcher.h"
 #include "content/browser/webid/flags.h"
 #include "content/browser/webid/idp_network_request_manager.h"
@@ -242,9 +242,8 @@ void AccountsFetcher::OnAllConfigAndWellKnownFetched(
 
     if (IsIdPRegistrationEnabled()) {
       if (get_info_it->second.provider->config->type) {
-        if (!std::ranges::contains(
-                fetch_result.metadata->types,
-                get_info_it->second.provider->config->type)) {
+        if (!base::Contains(fetch_result.metadata->types,
+                            get_info_it->second.provider->config->type)) {
           result.idp_info = std::move(idp_info);
           result.error = FederatedAuthRequestResult::kTypeNotMatching;
           result.token_status = TokenStatus::kConfigNotMatchingType;
@@ -259,7 +258,7 @@ void AccountsFetcher::OnAllConfigAndWellKnownFetched(
       // If a token format was specified, make sure that the configURL
       // supports it as well as the feature is enabled.
       if (!IsDelegationEnabled() ||
-          !std::ranges::contains(fetch_result.metadata->formats, kVcSdJwt)) {
+          !base::Contains(fetch_result.metadata->formats, kVcSdJwt)) {
         result.idp_info = std::move(idp_info);
         result.error = FederatedAuthRequestResult::kConfigInvalidResponse;
         result.token_status = TokenStatus::kConfigInvalidResponse;
@@ -492,7 +491,7 @@ void AccountsFetcher::FilterAccountsWithLabel(
   // would be shown.
   size_t accounts_remaining = 0u;
   for (auto& account : accounts) {
-    if (!std::ranges::contains(account->labels, label)) {
+    if (!base::Contains(account->labels, label)) {
       account->is_filtered_out = true;
     } else {
       ++accounts_remaining;
@@ -517,7 +516,7 @@ void AccountsFetcher::FilterAccountsWithLoginHint(
     if (account->is_filtered_out) {
       continue;
     }
-    if (!std::ranges::contains(account->login_hints, login_hint)) {
+    if (!base::Contains(account->login_hints, login_hint)) {
       account->is_filtered_out = true;
     } else {
       ++accounts_remaining;
@@ -543,7 +542,7 @@ void AccountsFetcher::FilterAccountsWithDomainHint(
         account->is_filtered_out = true;
         continue;
       }
-    } else if (!std::ranges::contains(account->domain_hints, domain_hint)) {
+    } else if (!base::Contains(account->domain_hints, domain_hint)) {
       account->is_filtered_out = true;
       continue;
     }
diff --git a/content/browser/webid/url_computations.cc b/content/browser/webid/url_computations.cc
index eca676b920..509d7f470d 100644
--- a/content/browser/webid/url_computations.cc
+++ b/content/browser/webid/url_computations.cc
@@ -4,9 +4,9 @@
 
 #include "content/browser/webid/url_computations.h"
 
-#include <algorithm>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/strings/escape.h"
 #include "base/strings/string_util.h"
 #include "base/strings/to_string.h"
@@ -24,9 +24,9 @@ namespace webid {
 namespace {
 
 bool IsRequestingDefaultPermissions(const std::vector<std::string>& fields) {
-  return std::ranges::contains(fields, webid::kDefaultFieldName) &&
-         std::ranges::contains(fields, webid::kDefaultFieldEmail) &&
-         std::ranges::contains(fields, webid::kDefaultFieldPicture);
+  return base::Contains(fields, webid::kDefaultFieldName) &&
+         base::Contains(fields, webid::kDefaultFieldEmail) &&
+         base::Contains(fields, webid::kDefaultFieldPicture);
 }
 
 }  // namespace
diff --git a/content/browser/websockets/websocket_connector_impl.cc b/content/browser/websockets/websocket_connector_impl.cc
index 08053b7f5a..940c78ed66 100644
--- a/content/browser/websockets/websocket_connector_impl.cc
+++ b/content/browser/websockets/websocket_connector_impl.cc
@@ -4,9 +4,8 @@
 
 #include "content/browser/websockets/websocket_connector_impl.h"
 
-#include <algorithm>
-
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "content/browser/renderer_host/render_frame_host_impl.h"
 #include "content/public/browser/browser_thread.h"
 #include "content/public/browser/render_frame_host.h"
@@ -24,7 +23,7 @@ namespace content {
 namespace {
 
 url::Origin MaybeTreatLocalOriginAsOpaque(const url::Origin& origin) {
-  if (std::ranges::contains(url::GetLocalSchemes(), origin.scheme()) &&
+  if (base::Contains(url::GetLocalSchemes(), origin.scheme()) &&
       !base::CommandLine::ForCurrentProcess()->HasSwitch(
           switches::kAllowFileAccessFromFiles)) {
     // For local origins we should use an opaque origin unless
diff --git a/content/browser/webui/shared_resources_data_source.cc b/content/browser/webui/shared_resources_data_source.cc
index ed876dcc31..a6b5360469 100644
--- a/content/browser/webui/shared_resources_data_source.cc
+++ b/content/browser/webui/shared_resources_data_source.cc
@@ -6,6 +6,7 @@
 
 #include <set>
 
+#include "base/containers/contains.h"
 #include "content/public/browser/web_ui_data_source.h"
 #include "services/network/public/mojom/content_security_policy.mojom.h"
 #include "ui/base/webui/web_ui_util.h"
@@ -37,7 +38,7 @@ void AddResources(const std::set<int>& resource_ids,
                   base::span<const webui::ResourcePath> resources,
                   WebUIDataSource* source) {
   for (const auto& resource : resources) {
-    if (resource_ids.contains(resource.id)) {
+    if (base::Contains(resource_ids, resource.id)) {
       source->AddResourcePath(resource.path, resource.id);
     }
   }
diff --git a/content/browser/webui/url_data_manager.cc b/content/browser/webui/url_data_manager.cc
index 5973143d40..64ca75f830 100644
--- a/content/browser/webui/url_data_manager.cc
+++ b/content/browser/webui/url_data_manager.cc
@@ -6,10 +6,10 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/ref_counted_memory.h"
@@ -137,7 +137,7 @@ void URLDataManager::UpdateWebUIDataSource(BrowserContext* browser_context,
 bool URLDataManager::IsScheduledForDeletion(
     const URLDataSourceImpl* data_source) {
   base::AutoLock lock(GetDeleteLock());
-  return data_sources_ && std::ranges::contains(*data_sources_, data_source);
+  return data_sources_ && base::Contains(*data_sources_, data_source);
 }
 
 }  // namespace content
diff --git a/content/browser/webui/url_data_manager_backend.cc b/content/browser/webui/url_data_manager_backend.cc
index 155c29dc58..efe1823064 100644
--- a/content/browser/webui/url_data_manager_backend.cc
+++ b/content/browser/webui/url_data_manager_backend.cc
@@ -4,10 +4,10 @@
 
 #include "content/browser/webui/url_data_manager_backend.h"
 
-#include <algorithm>
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/memory/ptr_util.h"
@@ -256,7 +256,7 @@ bool URLDataManagerBackend::CheckURLIsValid(const GURL& url) {
          url.SchemeIs(kChromeUIUntrustedScheme) ||
          (GetContentClient()->browser()->GetAdditionalWebUISchemes(
               &additional_schemes),
-          std::ranges::contains(additional_schemes, url.GetScheme())));
+          base::Contains(additional_schemes, url.GetScheme())));
 
   if (!url.is_valid()) {
     NOTREACHED();
diff --git a/content/browser/xr/service/browser_xr_runtime_impl.cc b/content/browser/xr/service/browser_xr_runtime_impl.cc
index 86355279e5..5aec152cbf 100644
--- a/content/browser/xr/service/browser_xr_runtime_impl.cc
+++ b/content/browser/xr/service/browser_xr_runtime_impl.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
 #include "base/observer_list.h"
@@ -167,7 +168,7 @@ bool BrowserXRRuntimeImpl::SupportsFeature(
      id_ == device::mojom::XRDeviceId::FAKE_DEVICE_ID)
       return true;
 
-  return std::ranges::contains(device_data_->supported_features, feature);
+  return base::Contains(device_data_->supported_features, feature);
 }
 
 bool BrowserXRRuntimeImpl::SupportsAllFeatures(
@@ -361,10 +362,10 @@ void BrowserXRRuntimeImpl::OnRequestSessionResult(
         }
 
         // The overlay code requires the left and right views to render.
-        if (!std::ranges::contains(views, device::mojom::XREye::kLeft,
-                                   &device::mojom::XRView::eye) ||
-            !std::ranges::contains(views, device::mojom::XREye::kRight,
-                                   &device::mojom::XRView::eye)) {
+        if (!base::Contains(views, device::mojom::XREye::kLeft,
+                            &device::mojom::XRView::eye) ||
+            !base::Contains(views, device::mojom::XREye::kRight,
+                            &device::mojom::XRView::eye)) {
           // Notify the service to cleanup any session that it's started to
           // setup, and when that and our corresponding runtime shutdown have
           // finished, notify the page that the session request failed.
@@ -381,8 +382,8 @@ void BrowserXRRuntimeImpl::OnRequestSessionResult(
       }
 
       immersive_session_has_camera_access_ =
-          std::ranges::contains(session_result->session->enabled_features,
-                                device::mojom::XRSessionFeature::CAMERA_ACCESS);
+          base::Contains(session_result->session->enabled_features,
+                         device::mojom::XRSessionFeature::CAMERA_ACCESS);
       if (immersive_session_has_camera_access_) {
         for (Observer& observer : observers_) {
           observer.WebXRCameraInUseChanged(web_contents, true);
diff --git a/content/browser/xr/service/vr_service_impl.cc b/content/browser/xr/service/vr_service_impl.cc
index 63eba5c1c2..1dc3d7337e 100644
--- a/content/browser/xr/service/vr_service_impl.cc
+++ b/content/browser/xr/service/vr_service_impl.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -84,7 +85,7 @@ std::vector<blink::PermissionType> GetRequiredPermissionsForFeatures(
     auto feature_permission =
         content::XrPermissionResults::GetPermissionFor(required_feature);
     if (feature_permission &&
-        !std::ranges::contains(permissions, *feature_permission)) {
+        !base::Contains(permissions, *feature_permission)) {
       permissions.push_back(*feature_permission);
     }
   }
@@ -93,7 +94,7 @@ std::vector<blink::PermissionType> GetRequiredPermissionsForFeatures(
     auto feature_permission =
         content::XrPermissionResults::GetPermissionFor(optional_feature);
     if (feature_permission &&
-        !std::ranges::contains(permissions, *feature_permission)) {
+        !base::Contains(permissions, *feature_permission)) {
       permissions.push_back(*feature_permission);
     }
   }
@@ -815,10 +816,10 @@ void VRServiceImpl::DoRequestSession(SessionRequestData request) {
   }
 
   bool use_dom_overlay =
-      std::ranges::contains(runtime_options->required_features,
-                            device::mojom::XRSessionFeature::DOM_OVERLAY) ||
-      std::ranges::contains(runtime_options->optional_features,
-                            device::mojom::XRSessionFeature::DOM_OVERLAY);
+      base::Contains(runtime_options->required_features,
+                     device::mojom::XRSessionFeature::DOM_OVERLAY) ||
+      base::Contains(runtime_options->optional_features,
+                     device::mojom::XRSessionFeature::DOM_OVERLAY);
 
   if (use_dom_overlay) {
     // Tell RenderFrameHostImpl that we're setting up the WebXR DOM Overlay,
diff --git a/content/browser/xr/service/xr_permission_results.cc b/content/browser/xr/service/xr_permission_results.cc
index bffe823e56..69fe0408ea 100644
--- a/content/browser/xr/service/xr_permission_results.cc
+++ b/content/browser/xr/service/xr_permission_results.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "content/public/browser/permission_result.h"
 #include "device/vr/public/mojom/xr_session.mojom-shared.h"
 #include "third_party/blink/public/common/permissions/permission_utils.h"
@@ -64,7 +65,7 @@ bool XrPermissionResults::HasPermissionsFor(
 
 bool XrPermissionResults::HasPermissionsFor(
     blink::PermissionType permission_type) const {
-  if (!permission_type_to_status_.contains(permission_type)) {
+  if (!base::Contains(permission_type_to_status_, permission_type)) {
     return false;
   }
 
diff --git a/content/child/font_warmup_win.cc b/content/child/font_warmup_win.cc
index 5b17346712..b01fc8b2c6 100644
--- a/content/child/font_warmup_win.cc
+++ b/content/child/font_warmup_win.cc
@@ -7,7 +7,6 @@
 #include <dwrite.h>
 #include <stdint.h>
 
-#include <bit>
 #include <map>
 #include <string>
 #include <utility>
@@ -292,7 +291,7 @@ DWORD WINAPI GetFontDataPatch(HDC dc_handle,
   // the correct answer for emulating GDI. |table_tag| must also have its
   // byte order swapped to counter the swap which occurs in the called method.
   size_t length =
-      typeface->getTableData(std::byteswap(uint32_t{table_tag}), table_offset,
+      typeface->getTableData(base::ByteSwap(uint32_t{table_tag}), table_offset,
                              buffer ? buffer_length : INT32_MAX, buffer);
   // We can't distinguish between an empty table and an error.
   if (length == 0)
diff --git a/content/child/memory_coordinator/child_memory_consumer_registry.cc b/content/child/memory_coordinator/child_memory_consumer_registry.cc
index 8f7390632b..7f788a303a 100644
--- a/content/child/memory_coordinator/child_memory_consumer_registry.cc
+++ b/content/child/memory_coordinator/child_memory_consumer_registry.cc
@@ -11,6 +11,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/memory_coordinator/memory_consumer.h"
 #include "content/public/common/child_process_id.h"
 #include "content/public/common/process_type.h"
@@ -41,7 +42,7 @@ void ChildMemoryConsumerRegistry::ConsumerGroup::OnUpdateMemoryLimit() {
 
 void ChildMemoryConsumerRegistry::ConsumerGroup::AddMemoryConsumer(
     base::RegisteredMemoryConsumer consumer) {
-  CHECK(!std::ranges::contains(memory_consumers_, consumer));
+  CHECK(!base::Contains(memory_consumers_, consumer));
   memory_consumers_.push_back(consumer);
 
   // Ensure the added consumer is up to date with the current memory limit
diff --git a/content/common/color_parser.cc b/content/common/color_parser.cc
index ea063f5306..71222376bf 100644
--- a/content/common/color_parser.cc
+++ b/content/common/color_parser.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 #include <cmath>
 
+#include "base/containers/contains.h"
 #include "base/notimplemented.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/strings/string_number_conversions.h"
@@ -129,7 +130,7 @@ bool ParseRgbColorString(const std::string& color_string, SkColor* result) {
   double alpha = 1.0;
 
   // Percentage rgb values are not supported.
-  if (color_string.contains('%')) {
+  if (base::Contains(color_string, '%')) {
     NOTIMPLEMENTED();
     return false;
   }
diff --git a/content/common/font_cache_dispatcher_win.cc b/content/common/font_cache_dispatcher_win.cc
index fe58e76cd7..196500e7b7 100644
--- a/content/common/font_cache_dispatcher_win.cc
+++ b/content/common/font_cache_dispatcher_win.cc
@@ -4,13 +4,13 @@
 
 #include "content/public/common/font_cache_dispatcher_win.h"
 
-#include <algorithm>
 #include <map>
 #include <memory>
 #include <string>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/singleton.h"
 #include "base/numerics/checked_math.h"
@@ -48,7 +48,7 @@ class FontCache {
 
     std::wstring font_name = font.lfFaceName;
     bool inc_ref_count = true;
-    if (!std::ranges::contains(dispatcher_font_map_[dispatcher], font_name)) {
+    if (!base::Contains(dispatcher_font_map_[dispatcher], font_name)) {
       // Requested font is new to cache.
       dispatcher_font_map_[dispatcher].push_back(font_name);
     } else {
diff --git a/content/common/input/actions_parser.cc b/content/common/input/actions_parser.cc
index 0c7ff633f1..bb836eb8ea 100644
--- a/content/common/input/actions_parser.cc
+++ b/content/common/input/actions_parser.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/format_macros.h"
 #include "base/strings/string_split.h"
 #include "base/strings/stringprintf.h"
@@ -363,7 +364,7 @@ bool ActionsParser::ParsePointerParameters(
     return false;
   }
 
-  if (pointer_name_set_.contains(*pointer_name)) {
+  if (base::Contains(pointer_name_set_, *pointer_name)) {
     error_message_ = std::string("pointer name already exists");
     return false;
   }
diff --git a/content/common/memory_coordinator/mojom/memory_consumer_traits_mojom_traits.h b/content/common/memory_coordinator/mojom/memory_consumer_traits_mojom_traits.h
index b05e3a10c3..1303541207 100644
--- a/content/common/memory_coordinator/mojom/memory_consumer_traits_mojom_traits.h
+++ b/content/common/memory_coordinator/mojom/memory_consumer_traits_mojom_traits.h
@@ -5,9 +5,8 @@
 #ifndef CONTENT_COMMON_MEMORY_COORDINATOR_MOJOM_MEMORY_CONSUMER_TRAITS_MOJOM_TRAITS_H_
 #define CONTENT_COMMON_MEMORY_COORDINATOR_MOJOM_MEMORY_CONSUMER_TRAITS_MOJOM_TRAITS_H_
 
-#include <utility>
-
 #include "base/memory_coordinator/traits.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "content/common/memory_coordinator/mojom/memory_coordinator.mojom.h"
 #include "mojo/public/cpp/bindings/struct_traits.h"
 
@@ -18,7 +17,7 @@ struct StructTraits<content::mojom::MemoryConsumerTraitsDataView,
                     base::MemoryConsumerTraits> {
   template <class EnumType>
   static bool ConvertToEnum(uint8_t input, EnumType* out) {
-    if (input < 0 || input > std::to_underlying(EnumType::kMaxValue)) {
+    if (input < 0 || input > base::to_underlying(EnumType::kMaxValue)) {
       return false;
     }
 
@@ -28,39 +27,39 @@ struct StructTraits<content::mojom::MemoryConsumerTraitsDataView,
 
   static uint8_t supports_memory_limit(
       const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.supports_memory_limit);
+    return base::to_underlying(input.supports_memory_limit);
   }
   static uint8_t in_process(const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.in_process);
+    return base::to_underlying(input.in_process);
   }
   static uint8_t estimated_memory_usage(
       const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.estimated_memory_usage);
+    return base::to_underlying(input.estimated_memory_usage);
   }
   static uint8_t release_memory_cost(const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.release_memory_cost);
+    return base::to_underlying(input.release_memory_cost);
   }
   static uint8_t recreate_memory_cost(const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.recreate_memory_cost);
+    return base::to_underlying(input.recreate_memory_cost);
   }
   static uint8_t information_retention(
       const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.information_retention);
+    return base::to_underlying(input.information_retention);
   }
   static uint8_t memory_release_behavior(
       const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.memory_release_behavior);
+    return base::to_underlying(input.memory_release_behavior);
   }
   static uint8_t execution_type(const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.execution_type);
+    return base::to_underlying(input.execution_type);
   }
   static uint8_t release_gc_references(
       const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.release_gc_references);
+    return base::to_underlying(input.release_gc_references);
   }
   static uint8_t garbage_collects_v8_heap(
       const base::MemoryConsumerTraits& input) {
-    return std::to_underlying(input.garbage_collects_v8_heap);
+    return base::to_underlying(input.garbage_collects_v8_heap);
   }
 
   static bool Read(content::mojom::MemoryConsumerTraitsDataView input,
diff --git a/content/common/origin_util.cc b/content/common/origin_util.cc
index 44d47285b9..c0abdbac49 100644
--- a/content/common/origin_util.cc
+++ b/content/common/origin_util.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/lazy_instance.h"
 #include "base/strings/pattern.h"
 #include "content/common/url_schemes.h"
@@ -24,7 +25,7 @@ bool OriginCanAccessServiceWorkers(const GURL& url) {
     return true;
   }
 
-  if (std::ranges::contains(GetServiceWorkerSchemes(), url.GetScheme())) {
+  if (base::Contains(GetServiceWorkerSchemes(), url.GetScheme())) {
     return true;
   }
 
diff --git a/content/common/webid/identity_url_loader_throttle.cc b/content/common/webid/identity_url_loader_throttle.cc
index 581106193f..8a9c0b690e 100644
--- a/content/common/webid/identity_url_loader_throttle.cc
+++ b/content/common/webid/identity_url_loader_throttle.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/string_split.h"
@@ -131,7 +132,7 @@ bool IdentityUrlLoaderThrottle::HeaderHasToken(
 
   std::vector<std::string_view> tokens = base::SplitStringPiece(
       value, ";", base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-  return std::ranges::contains(tokens, token);
+  return base::Contains(tokens, token);
 }
 
 }  // namespace content
diff --git a/content/public/browser/internal_webui_config.cc b/content/public/browser/internal_webui_config.cc
index 3852a806e7..4a40b0917b 100644
--- a/content/public/browser/internal_webui_config.cc
+++ b/content/public/browser/internal_webui_config.cc
@@ -6,6 +6,7 @@
 
 #include <set>
 
+#include "base/containers/contains.h"
 #include "content/public/browser/content_browser_client.h"
 #include "content/public/browser/web_ui_controller.h"
 #include "content/public/common/content_client.h"
diff --git a/content/public/browser/web_contents_delegate.cc b/content/public/browser/web_contents_delegate.cc
index 509256e4c3..9f2180563e 100644
--- a/content/public/browser/web_contents_delegate.cc
+++ b/content/public/browser/web_contents_delegate.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/singleton.h"
 #include "base/notreached.h"
@@ -329,12 +330,12 @@ WebContentsDelegate::~WebContentsDelegate() {
 }
 
 void WebContentsDelegate::Attach(WebContents* web_contents) {
-  DCHECK(!attached_contents_.contains(web_contents));
+  DCHECK(!base::Contains(attached_contents_, web_contents));
   attached_contents_.insert(web_contents);
 }
 
 void WebContentsDelegate::Detach(WebContents* web_contents) {
-  DCHECK(attached_contents_.contains(web_contents));
+  DCHECK(base::Contains(attached_contents_, web_contents));
   attached_contents_.erase(web_contents);
 }
 
diff --git a/content/public/browser/webui_config_map.cc b/content/public/browser/webui_config_map.cc
index e74da278fe..094f55ddfa 100644
--- a/content/public/browser/webui_config_map.cc
+++ b/content/public/browser/webui_config_map.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ref.h"
 #include "base/no_destructor.h"
 #include "base/strings/strcat.h"
diff --git a/content/public/common/content_client.cc b/content/public/common/content_client.cc
index 4d72993163..4b96cb2535 100644
--- a/content/public/common/content_client.cc
+++ b/content/public/common/content_client.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/memory/ref_counted_memory.h"
 #include "base/no_destructor.h"
@@ -195,7 +196,7 @@ bool ContentClient::ShouldIgnoreDuplicateNavs(
       }());
 
   const url::Origin navigation_origin = url::Origin::Create(url);
-  return std::ranges::contains(*target_origin_ignorelist, navigation_origin);
+  return base::Contains(*target_origin_ignorelist, navigation_origin);
 }
 
 bool ContentClient::IsFilePickerAllowedForCrossOriginSubframe(
diff --git a/content/public/test/back_forward_cache_util.cc b/content/public/test/back_forward_cache_util.cc
index afaf576f91..a1328ddbdd 100644
--- a/content/public/test/back_forward_cache_util.cc
+++ b/content/public/test/back_forward_cache_util.cc
@@ -8,6 +8,7 @@
 #include <map>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/test/scoped_feature_list.h"
 #include "content/browser/renderer_host/back_forward_cache_impl.h"
@@ -82,7 +83,7 @@ std::vector<base::test::FeatureRefAndParams> Merge(
   }
   // Add any default features we didn't have additional params for.
   for (const auto& feature_and_params : default_features_and_params) {
-    if (!std::ranges::contains(
+    if (!base::Contains(
             final_features_and_params, feature_and_params.feature->name,
             [](const base::test::FeatureRefAndParams default_feature) {
               return default_feature.feature->name;
@@ -202,11 +203,10 @@ GetDefaultDisabledBackForwardCacheFeaturesForTesting(
       features::kBackForwardCacheMemoryControls};
 
   for (auto additional_feature : additional_features) {
-    if (!std::ranges::contains(
-            final_features, additional_feature->name,
-            [](const base::test::FeatureRef default_feature) {
-              return default_feature->name;
-            })) {
+    if (!base::Contains(final_features, additional_feature->name,
+                        [](const base::test::FeatureRef default_feature) {
+                          return default_feature->name;
+                        })) {
       final_features.emplace_back(additional_feature);
     }
   }
diff --git a/content/public/test/browser_test_utils.cc b/content/public/test/browser_test_utils.cc
index d5d5c139fc..d95d64c975 100644
--- a/content/public/test/browser_test_utils.cc
+++ b/content/public/test/browser_test_utils.cc
@@ -14,6 +14,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -2604,7 +2605,7 @@ void TitleWatcher::TitleWasSet(NavigationEntry* entry) {
 
 void TitleWatcher::TestTitle() {
   const std::u16string& current_title = web_contents()->GetTitle();
-  if (std::ranges::contains(expected_titles_, current_title)) {
+  if (base::Contains(expected_titles_, current_title)) {
     observed_title_ = current_title;
     run_loop_.Quit();
   }
diff --git a/content/public/test/download_test_observer.cc b/content/public/test/download_test_observer.cc
index 1a3836de68..82f7a7f32d 100644
--- a/content/public/test/download_test_observer.cc
+++ b/content/public/test/download_test_observer.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/notreached.h"
 #include "base/run_loop.h"
@@ -123,7 +124,7 @@ void DownloadTestObserver::OnDownloadCreated(DownloadManager* manager,
   // existed before |this| was created.
   OnDownloadUpdated(item);
   // If it isn't finished, start observing it.
-  if (!finished_downloads_.contains(item)) {
+  if (!base::Contains(finished_downloads_, item)) {
     item->AddObserver(this);
     downloads_observed_.insert(item);
   }
@@ -132,7 +133,7 @@ void DownloadTestObserver::OnDownloadCreated(DownloadManager* manager,
 void DownloadTestObserver::OnDownloadDestroyed(
     download::DownloadItem* download) {
   // Stop observing. Do not do anything with it, as it is about to be gone.
-  CHECK(downloads_observed_.contains(download));
+  CHECK(base::Contains(downloads_observed_, download));
   downloads_observed_.erase(download);
   download->RemoveObserver(this);
 }
@@ -140,7 +141,7 @@ void DownloadTestObserver::OnDownloadDestroyed(
 void DownloadTestObserver::OnDownloadUpdated(download::DownloadItem* download) {
   // Real UI code gets the user's response after returning from the observer.
   if (download->IsDangerous() &&
-      !dangerous_downloads_seen_.contains(download->GetId())) {
+      !base::Contains(dangerous_downloads_seen_, download->GetId())) {
     dangerous_downloads_seen_.insert(download->GetId());
 
     // Calling ValidateDangerousDownload() at this point will
@@ -201,7 +202,7 @@ size_t DownloadTestObserver::NumDownloadsSeenInState(
 
 void DownloadTestObserver::DownloadInFinalState(
     download::DownloadItem* download) {
-  if (finished_downloads_.contains(download)) {
+  if (base::Contains(finished_downloads_, download)) {
     // We've already seen the final state on this download.
     return;
   }
@@ -356,7 +357,7 @@ void DownloadTestFlushObserver::ManagerGoingDown(DownloadManager* manager) {
 void DownloadTestFlushObserver::OnDownloadDestroyed(
     download::DownloadItem* download) {
   // Stop observing. Do not do anything with it, as it is about to be gone.
-  CHECK(downloads_observed_.contains(download));
+  CHECK(base::Contains(downloads_observed_, download));
   downloads_observed_.erase(download);
   download->RemoveObserver(this);
 }
@@ -395,7 +396,7 @@ void DownloadTestFlushObserver::CheckDownloadsInProgress(
       if ((*it)->GetState() == download::DownloadItem::IN_PROGRESS)
         count++;
       if (observe_downloads) {
-        if (!downloads_observed_.contains(*it)) {
+        if (!base::Contains(downloads_observed_, *it)) {
           (*it)->AddObserver(this);
           downloads_observed_.insert(*it);
         }
diff --git a/content/public/test/fake_file_system_access_permission_context.cc b/content/public/test/fake_file_system_access_permission_context.cc
index cc166c152b..9dfb493863 100644
--- a/content/public/test/fake_file_system_access_permission_context.cc
+++ b/content/public/test/fake_file_system_access_permission_context.cc
@@ -4,6 +4,7 @@
 
 #include "content/public/test/fake_file_system_access_permission_context.h"
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "content/browser/file_system_access/fixed_file_system_access_permission_grant.h"
 
@@ -82,7 +83,8 @@ void FakeFileSystemAccessPermissionContext::SetLastPickedDirectory(
 PathInfo FakeFileSystemAccessPermissionContext::GetLastPickedDirectory(
     const url::Origin& origin,
     const std::string& id) {
-  return id_pathinfo_map_.contains(id) ? id_pathinfo_map_[id] : PathInfo();
+  return base::Contains(id_pathinfo_map_, id) ? id_pathinfo_map_[id]
+                                              : PathInfo();
 }
 
 void FakeFileSystemAccessPermissionContext::SetWellKnownDirectoryPath(
@@ -94,7 +96,7 @@ void FakeFileSystemAccessPermissionContext::SetWellKnownDirectoryPath(
 base::FilePath FakeFileSystemAccessPermissionContext::GetWellKnownDirectoryPath(
     blink::mojom::WellKnownDirectory directory,
     const url::Origin& origin) {
-  return well_known_directory_map_.contains(directory)
+  return base::Contains(well_known_directory_map_, directory)
              ? well_known_directory_map_[directory]
              : base::FilePath();
 }
diff --git a/content/public/test/fake_service_worker_context.cc b/content/public/test/fake_service_worker_context.cc
index e7a0214994..6b67efba32 100644
--- a/content/public/test/fake_service_worker_context.cc
+++ b/content/public/test/fake_service_worker_context.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/no_destructor.h"
 #include "base/notreached.h"
@@ -74,7 +75,7 @@ bool FakeServiceWorkerContext::ExecuteScriptForTest(
 }
 bool FakeServiceWorkerContext::MaybeHasRegistrationForStorageKey(
     const blink::StorageKey& key) {
-  return registered_storage_keys_.contains(key);
+  return base::Contains(registered_storage_keys_, key);
 }
 void FakeServiceWorkerContext::GetAllStorageKeysInfo(
     GetUsageInfoCallback callback) {
diff --git a/content/public/test/mock_render_process_host.cc b/content/public/test/mock_render_process_host.cc
index 986773bf9a..f9bb9ea1ea 100644
--- a/content/public/test/mock_render_process_host.cc
+++ b/content/public/test/mock_render_process_host.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/lazy_instance.h"
@@ -560,8 +561,8 @@ bool MockRenderProcessHost::HostHasNotBeenUsed() {
 }
 
 bool MockRenderProcessHost::IsSpare() const {
-  return std::ranges::contains(
-      SpareRenderProcessHostManagerImpl::Get().GetSpares(), this);
+  return base::Contains(SpareRenderProcessHostManagerImpl::Get().GetSpares(),
+                        this);
 }
 
 void MockRenderProcessHost::SetProcessLock(
diff --git a/content/public/test/prerender_test_util.cc b/content/public/test/prerender_test_util.cc
index 944e56b845..a2d240b9a6 100644
--- a/content/public/test/prerender_test_util.cc
+++ b/content/public/test/prerender_test_util.cc
@@ -5,7 +5,6 @@
 #include "content/public/test/prerender_test_util.h"
 
 #include <tuple>
-#include <utility>
 
 #include "base/functional/callback_helpers.h"
 #include "base/strings/string_util.h"
@@ -13,6 +12,7 @@
 #include "base/strings/utf_string_conversions.h"
 #include "base/test/bind.h"
 #include "base/trace_event/typed_macros.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "content/browser/preloading/prerender/prerender_features.h"
 #include "content/browser/preloading/prerender/prerender_final_status.h"
 #include "content/browser/preloading/prerender/prerender_handle_impl.h"
@@ -342,7 +342,7 @@ class PrerenderHostObserverImpl : public PrerenderHost::Observer {
     }
     EXPECT_FALSE(waiting_for_activation_)
         << "A prerender was destroyed, with status "
-        << std::to_underlying(final_status)
+        << base::to_underlying(final_status)
         << ", while waiting for activation.";
   }
 
@@ -353,7 +353,7 @@ class PrerenderHostObserverImpl : public PrerenderHost::Observer {
 
     EXPECT_FALSE(did_observe_ && !observation_.IsObserving())
         << "A prerender was destroyed, with status "
-        << std::to_underlying(
+        << base::to_underlying(
                last_status_.value_or(PrerenderFinalStatus::kDestroyed))
         << ", before waiting for activation.";
 
@@ -372,7 +372,7 @@ class PrerenderHostObserverImpl : public PrerenderHost::Observer {
 
     EXPECT_FALSE(did_observe_ && !observation_.IsObserving())
         << "A prerender was destroyed, with status "
-        << std::to_underlying(
+        << base::to_underlying(
                last_status_.value_or(PrerenderFinalStatus::kDestroyed))
         << ", before waiting for headers.";
 
@@ -960,7 +960,7 @@ net::test_server::HttpRequest::HeaderMap PrerenderTestHelper::GetRequestHeaders(
   EXPECT_TRUE(content::BrowserThread::CurrentlyOn(BrowserThread::UI));
   base::AutoLock auto_lock(lock_);
   std::string path = url.PathForRequest();
-  DCHECK(request_headers_by_path_.contains(path)) << path;
+  DCHECK(base::Contains(request_headers_by_path_, path)) << path;
   return request_headers_by_path_[path];
 }
 
diff --git a/content/public/test/test_download_http_response.cc b/content/public/test/test_download_http_response.cc
index 338c22127e..191846f7c3 100644
--- a/content/public/test/test_download_http_response.cc
+++ b/content/public/test/test_download_http_response.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
@@ -335,12 +336,12 @@ std::string TestDownloadHttpResponse::GetDefaultResponseHeaders() {
   // Send partial response.
   if (parameters_.support_partial_response && parameters_.support_byte_ranges) {
     bool has_if_range =
-        request_.headers.contains(net::HttpRequestHeaders::kIfRange);
+        base::Contains(request_.headers, net::HttpRequestHeaders::kIfRange);
     if (((has_if_range &&
           request_.headers.at(net::HttpRequestHeaders::kIfRange) ==
               parameters_.etag) ||
          (!has_if_range &&
-          request_.headers.contains(net::HttpRequestHeaders::kRange))) &&
+          base::Contains(request_.headers, net::HttpRequestHeaders::kRange))) &&
         HandleRangeAssumingValidatorMatch(headers)) {
       return headers;
     }
@@ -348,7 +349,7 @@ std::string TestDownloadHttpResponse::GetDefaultResponseHeaders() {
 
   // Send precondition failed for "If-Match" request header.
   if (parameters_.support_partial_response && parameters_.support_byte_ranges &&
-      request_.headers.contains(net::HttpRequestHeaders::kIfMatch)) {
+      base::Contains(request_.headers, net::HttpRequestHeaders::kIfMatch)) {
     if (request_.headers.at(net::HttpRequestHeaders::kIfMatch) !=
             parameters_.etag ||
         !HandleRangeAssumingValidatorMatch(headers)) {
diff --git a/content/public/test/test_launcher.cc b/content/public/test/test_launcher.cc
index 4e3eb3db8f..42c137ac4c 100644
--- a/content/public/test/test_launcher.cc
+++ b/content/public/test/test_launcher.cc
@@ -15,6 +15,7 @@
 #include "base/base_paths.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/debug/debugger.h"
 #include "base/environment.h"
@@ -233,7 +234,7 @@ base::CommandLine WrapperTestLauncherDelegate::GetCommandLine(
   new_cmd_line.AppendSwitchPath(switches::kTestLauncherOutput, *output_file);
 
   // Selecting sample tests to enable switches::kEnableTracing.
-  if (switches.contains(switches::kEnableTracingFraction)) {
+  if (base::Contains(switches, switches::kEnableTracingFraction)) {
     double enable_tracing_fraction = 0;
     if (!base::StringToDouble(switches[switches::kEnableTracingFraction],
                               &enable_tracing_fraction) ||
diff --git a/content/renderer/accessibility/annotations/ax_image_annotator.cc b/content/renderer/accessibility/annotations/ax_image_annotator.cc
index d9ef0d3e6b..429fe9f866 100644
--- a/content/renderer/accessibility/annotations/ax_image_annotator.cc
+++ b/content/renderer/accessibility/annotations/ax_image_annotator.cc
@@ -11,6 +11,7 @@
 
 #include "base/base64.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/i18n/char_iterator.h"
 #include "base/metrics/histogram_functions.h"
@@ -538,12 +539,12 @@ bool AXImageAnnotator::HasAnnotationInCache(blink::WebAXObject& image) const {
 
 bool AXImageAnnotator::HasImageInCache(const blink::WebAXObject& image) const {
   DCHECK(!image.IsDetached());
-  return image_annotations_.contains(image.AxID());
+  return base::Contains(image_annotations_, image.AxID());
 }
 
 void AXImageAnnotator::OnImageAdded(blink::WebAXObject& image) {
   DCHECK(!image.IsDetached());
-  DCHECK(!image_annotations_.contains(image.AxID()));
+  DCHECK(!base::Contains(image_annotations_, image.AxID()));
   const std::string image_id = GenerateImageSourceId(image);
   if (image_id.empty())
     return;
@@ -563,7 +564,7 @@ void AXImageAnnotator::OnImageAdded(blink::WebAXObject& image) {
 
 void AXImageAnnotator::OnImageUpdated(blink::WebAXObject& image) {
   DCHECK(!image.IsDetached());
-  DCHECK(image_annotations_.contains(image.AxID()));
+  DCHECK(base::Contains(image_annotations_, image.AxID()));
   const std::string image_id = GenerateImageSourceId(image);
   if (image_id.empty())
     return;
@@ -579,7 +580,7 @@ void AXImageAnnotator::OnImageUpdated(blink::WebAXObject& image) {
 
 void AXImageAnnotator::OnImageRemoved(blink::WebAXObject& image) {
   DCHECK(!image.IsDetached());
-  DCHECK(image_annotations_.contains(image.AxID()));
+  DCHECK(base::Contains(image_annotations_, image.AxID()));
   image_annotations_.erase(image.AxID());
 }
 
@@ -735,7 +736,7 @@ void AXImageAnnotator::OnImageAnnotated(
   DCHECK(render_accessibility_->GetAXContext());
   render_accessibility_->GetAXContext()->UpdateAXForAllDocuments();
 
-  if (!image_annotations_.contains(image.AxID()))
+  if (!base::Contains(image_annotations_, image.AxID()))
     return;
 
   if (image.IsDetached()) {
diff --git a/content/renderer/accessibility/annotations/ax_image_stopwords.cc b/content/renderer/accessibility/annotations/ax_image_stopwords.cc
index fd1f556fbb..1bd4014ff5 100644
--- a/content/renderer/accessibility/annotations/ax_image_stopwords.cc
+++ b/content/renderer/accessibility/annotations/ax_image_stopwords.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/i18n/case_conversion.h"
 #include "base/i18n/char_iterator.h"
@@ -568,7 +569,7 @@ bool AXImageStopwords::IsImageStopword(const char* word_utf8) const {
     return true;
   }
 
-  return stopword_set_.contains(base::UTF16ToUTF8(word_utf16));
+  return base::Contains(stopword_set_, base::UTF16ToUTF8(word_utf16));
 }
 
 }  // namespace content
diff --git a/content/renderer/agent_scheduling_group.cc b/content/renderer/agent_scheduling_group.cc
index d0447a81b5..3c21eb381e 100644
--- a/content/renderer/agent_scheduling_group.cc
+++ b/content/renderer/agent_scheduling_group.cc
@@ -184,7 +184,7 @@ void AgentSchedulingGroup::AddFrameRoute(
     const blink::LocalFrameToken& frame_token,
     RenderFrameImpl* render_frame,
     scoped_refptr<base::SingleThreadTaskRunner> task_runner) {
-  DCHECK(!listener_map_.contains(frame_token));
+  DCHECK(!base::Contains(listener_map_, frame_token));
   listener_map_.insert({frame_token, render_frame});
 
   // See warning in `GetAssociatedInterface`.
@@ -200,7 +200,7 @@ void AgentSchedulingGroup::AddFrameRoute(
 
 void AgentSchedulingGroup::RemoveFrameRoute(
     const blink::LocalFrameToken& frame_token) {
-  DCHECK(listener_map_.contains(frame_token));
+  DCHECK(base::Contains(listener_map_, frame_token));
   listener_map_.erase(frame_token);
 }
 
diff --git a/content/renderer/dom_serializer_browsertest.cc b/content/renderer/dom_serializer_browsertest.cc
index 4ee0bee412..ebdf4e65de 100644
--- a/content/renderer/dom_serializer_browsertest.cc
+++ b/content/renderer/dom_serializer_browsertest.cc
@@ -9,6 +9,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -319,13 +320,13 @@ IN_PROC_BROWSER_TEST_F(MAYBE_DomSerializerTests,
     ASSERT_FALSE(motw_declaration.empty());
     // The encoding of original contents is ISO-8859-1, so we convert the MOTW
     // declaration to ASCII and search whether original contents has it or not.
-    ASSERT_FALSE(original_contents.contains(motw_declaration));
+    ASSERT_FALSE(base::Contains(original_contents, motw_declaration));
 
     // Do serialization.
     SerializeDomForURL(file_url, false);
     // Make sure the serialized contents have MOTW ;
     ASSERT_TRUE(serialization_reported_end_of_data());
-    ASSERT_TRUE(serialized_contents().contains(motw_declaration));
+    ASSERT_TRUE(base::Contains(serialized_contents(), motw_declaration));
   }));
 }
 
@@ -357,13 +358,13 @@ IN_PROC_BROWSER_TEST_F(MAYBE_DomSerializerTests,
     ASSERT_FALSE(motw_declaration.empty());
     // The encoding of original contents is ISO-8859-1, so we convert the MOTW
     // declaration to ASCII and search whether original contents has it or not.
-    ASSERT_TRUE(!original_contents.contains(motw_declaration));
+    ASSERT_TRUE(!base::Contains(original_contents, motw_declaration));
 
     // Do serialization.
     SerializeDomForURL(file_url, true);
     // Make sure the serialized contents have MOTW;
     ASSERT_TRUE(serialization_reported_end_of_data());
-    ASSERT_TRUE(serialized_contents().contains(motw_declaration));
+    ASSERT_TRUE(base::Contains(serialized_contents(), motw_declaration));
   }));
 }
 
diff --git a/content/renderer/media/media_permission_dispatcher.cc b/content/renderer/media/media_permission_dispatcher.cc
index 5c310e7f03..08f4c95324 100644
--- a/content/renderer/media/media_permission_dispatcher.cc
+++ b/content/renderer/media/media_permission_dispatcher.cc
@@ -4,13 +4,12 @@
 
 #include "content/renderer/media/media_permission_dispatcher.h"
 
-#include <utility>
-
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/notreached.h"
 #include "base/task/bind_post_task.h"
 #include "base/task/single_thread_task_runner.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "third_party/blink/public/web/web_local_frame.h"
 #include "url/gurl.h"
 
@@ -33,7 +32,7 @@ blink::mojom::PermissionDescriptorPtr MediaPermissionTypeToPermissionDescriptor(
       descriptor->name = blink::mojom::PermissionName::VIDEO_CAPTURE;
       break;
     default:
-      NOTREACHED() << std::to_underlying(type);
+      NOTREACHED() << base::to_underlying(type);
   }
   return descriptor;
 }
diff --git a/content/renderer/navigation_client.cc b/content/renderer/navigation_client.cc
index c01edf2527..a92dc72e65 100644
--- a/content/renderer/navigation_client.cc
+++ b/content/renderer/navigation_client.cc
@@ -4,11 +4,10 @@
 
 #include "content/renderer/navigation_client.h"
 
-#include <utility>
-
 #include "base/check.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/uuid.h"
 #include "content/common/features.h"
 #include "content/common/frame.mojom.h"
@@ -182,24 +181,24 @@ void NavigationClient::SetUpRendererInitiatedNavigation(
 
 void NavigationClient::ResetWithoutCancelling() {
   navigation_client_receiver_.ResetWithReason(
-      std::to_underlying(
+      base::to_underlying(
           mojom::NavigationClientDisconnectReason::kResetForSwap),
       "");
 }
 
 void NavigationClient::ResetForNewNavigation(bool is_duplicate_navigation) {
   navigation_client_receiver_.ResetWithReason(
-      std::to_underlying(is_duplicate_navigation
-                             ? mojom::NavigationClientDisconnectReason::
-                                   kResetForDuplicateNavigation
-                             : mojom::NavigationClientDisconnectReason::
-                                   kResetForNewNavigation),
+      base::to_underlying(is_duplicate_navigation
+                              ? mojom::NavigationClientDisconnectReason::
+                                    kResetForDuplicateNavigation
+                              : mojom::NavigationClientDisconnectReason::
+                                    kResetForNewNavigation),
       "");
 }
 
 void NavigationClient::ResetForAbort() {
   navigation_client_receiver_.ResetWithReason(
-      std::to_underlying(
+      base::to_underlying(
           mojom::NavigationClientDisconnectReason::kResetForAbort),
       "");
 }
diff --git a/content/renderer/render_frame_impl.cc b/content/renderer/render_frame_impl.cc
index cd9a5bb5b1..67415bac38 100644
--- a/content/renderer/render_frame_impl.cc
+++ b/content/renderer/render_frame_impl.cc
@@ -15,6 +15,7 @@
 #include "base/byte_count.h"
 #include "base/check_deref.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/to_vector.h"
 #include "base/debug/alias.h"
diff --git a/content/renderer/render_frame_impl_browsertest.cc b/content/renderer/render_frame_impl_browsertest.cc
index 19a018c97e..78fa1ad536 100644
--- a/content/renderer/render_frame_impl_browsertest.cc
+++ b/content/renderer/render_frame_impl_browsertest.cc
@@ -1189,7 +1189,7 @@ bool HasText(blink::WebLocalFrame* frame, const std::string& text) {
           frame, blink::TestWebFrameContentDumper::kLayoutAsTextNormal)
           .Utf8();
 
-  return layout_tree.contains(text);
+  return base::Contains(layout_tree, text);
 }
 
 // Waits for the navigation to finish.
diff --git a/content/renderer/render_thread_impl.cc b/content/renderer/render_thread_impl.cc
index 231e13b24a..ba834ad3d5 100644
--- a/content/renderer/render_thread_impl.cc
+++ b/content/renderer/render_thread_impl.cc
@@ -17,6 +17,7 @@
 #include "base/allocator/partition_alloc_support.h"
 #include "base/at_exit.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -325,7 +326,7 @@ std::string RenderThreadImpl::HistogramCustomizer::ConvertToCustomHistogramName(
     const char* histogram_name) const {
   std::string name(histogram_name);
   if (!common_host_histogram_suffix_.empty() &&
-      custom_histograms_.contains(name)) {
+      base::Contains(custom_histograms_, name)) {
     name += common_host_histogram_suffix_;
   }
   return name;
diff --git a/content/renderer/renderer_navigation_metrics_manager.cc b/content/renderer/renderer_navigation_metrics_manager.cc
index 2c4353a08a..b52dfe8a73 100644
--- a/content/renderer/renderer_navigation_metrics_manager.cc
+++ b/content/renderer/renderer_navigation_metrics_manager.cc
@@ -5,6 +5,7 @@
 #include "content/renderer/renderer_navigation_metrics_manager.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/no_destructor.h"
diff --git a/content/renderer/worker/worker_thread_registry.cc b/content/renderer/worker/worker_thread_registry.cc
index 742553b06b..f3eab61949 100644
--- a/content/renderer/worker/worker_thread_registry.cc
+++ b/content/renderer/worker/worker_thread_registry.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/location.h"
 #include "base/memory/ptr_util.h"
 #include "base/no_destructor.h"
@@ -129,7 +130,7 @@ void WorkerThreadRegistry::WillStopCurrentWorkerThread() {
 base::SequencedTaskRunner* WorkerThreadRegistry::GetTaskRunnerFor(
     int worker_id) {
   base::AutoLock locker(task_runner_map_lock_);
-  return task_runner_map_.contains(worker_id)
+  return base::Contains(task_runner_map_, worker_id)
              ? task_runner_map_[worker_id]
              : task_runner_for_dead_worker_.get();
 }
diff --git a/content/services/auction_worklet/auction_v8_helper.cc b/content/services/auction_worklet/auction_v8_helper.cc
index 2015e125f6..0fb70ca482 100644
--- a/content/services/auction_worklet/auction_v8_helper.cc
+++ b/content/services/auction_worklet/auction_v8_helper.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/location.h"
 #include "base/memory/raw_ptr.h"
@@ -763,7 +764,7 @@ int AuctionV8Helper::AllocContextGroupId() {
     int candidate = ++last_context_group_id_;
     DCHECK_GT(candidate, 0);
 
-    if (!resume_callbacks_.contains(candidate)) {
+    if (!base::Contains(resume_callbacks_, candidate)) {
       resume_callbacks_.emplace(candidate, base::OnceClosure());
       return candidate;
     }
diff --git a/content/services/auction_worklet/bidder_worklet.cc b/content/services/auction_worklet/bidder_worklet.cc
index 38f7021578..123a27c761 100644
--- a/content/services/auction_worklet/bidder_worklet.cc
+++ b/content/services/auction_worklet/bidder_worklet.cc
@@ -16,6 +16,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
diff --git a/content/services/auction_worklet/debug_command_queue.cc b/content/services/auction_worklet/debug_command_queue.cc
index 4245110771..362108a7de 100644
--- a/content/services/auction_worklet/debug_command_queue.cc
+++ b/content/services/auction_worklet/debug_command_queue.cc
@@ -4,6 +4,7 @@
 
 #include "content/services/auction_worklet/debug_command_queue.h"
 
+#include "base/containers/contains.h"
 #include "base/task/sequenced_task_runner.h"
 
 namespace auction_worklet {
@@ -23,7 +24,7 @@ void DebugCommandQueue::PauseForDebuggerAndRunCommands(
   base::AutoLock auto_lock(lock_);
   CHECK(!v8_thread_paused_);
   DCHECK(!pause_abort_helper_);
-  if (aborted_context_group_ids_.contains(context_group_id)) {
+  if (base::Contains(aborted_context_group_ids_, context_group_id)) {
     // Pauses disallowed since worklet is in process of being destroyed
     return;
   }
diff --git a/content/shell/browser/shell_devtools_bindings.cc b/content/shell/browser/shell_devtools_bindings.cc
index 6486639ced..ffa3aec6ba 100644
--- a/content/shell/browser/shell_devtools_bindings.cc
+++ b/content/shell/browser/shell_devtools_bindings.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/json/json_reader.h"
@@ -177,7 +178,7 @@ ShellDevToolsBindings::ShellDevToolsBindings(WebContents* devtools_contents,
       inspect_element_at_x_(-1),
       inspect_element_at_y_(-1) {
   auto* bindings = GetShellDevtoolsBindingsInstances();
-  DCHECK(!std::ranges::contains(*bindings, this));
+  DCHECK(!base::Contains(*bindings, this));
   bindings->push_back(this);
 }
 
@@ -186,7 +187,7 @@ ShellDevToolsBindings::~ShellDevToolsBindings() {
     agent_host_->DetachClient(this);
 
   auto* bindings = GetShellDevtoolsBindingsInstances();
-  DCHECK(std::ranges::contains(*bindings, this));
+  DCHECK(base::Contains(*bindings, this));
   std::erase(*bindings, this);
 }
 
diff --git a/content/shell/browser/shell_devtools_manager_delegate.cc b/content/shell/browser/shell_devtools_manager_delegate.cc
index 856b49d3cc..28409f562e 100644
--- a/content/shell/browser/shell_devtools_manager_delegate.cc
+++ b/content/shell/browser/shell_devtools_manager_delegate.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -196,7 +197,7 @@ BrowserContext* ShellDevToolsManagerDelegate::GetDefaultBrowserContext() {
 void ShellDevToolsManagerDelegate::ClientAttached(
     content::DevToolsAgentHostClientChannel* channel) {
   // Make sure we don't receive notifications twice for the same client.
-  CHECK(!sessions_.contains(channel));
+  CHECK(!base::Contains(sessions_, channel));
   sessions_.emplace(
       channel,
       std::make_unique<shell::protocol::ShellDevToolsSession>(
diff --git a/content/shell/browser/shell_platform_delegate_aura.cc b/content/shell/browser/shell_platform_delegate_aura.cc
index 907350d598..42ced1ae15 100644
--- a/content/shell/browser/shell_platform_delegate_aura.cc
+++ b/content/shell/browser/shell_platform_delegate_aura.cc
@@ -4,6 +4,7 @@
 
 #include "content/shell/browser/shell_platform_delegate.h"
 
+#include "base/containers/contains.h"
 #include "content/public/browser/render_widget_host_view.h"
 #include "content/public/browser/web_contents.h"
 #include "content/shell/browser/shell.h"
diff --git a/content/shell/browser/shell_platform_delegate_views.cc b/content/shell/browser/shell_platform_delegate_views.cc
index 1e0887ae0c..b2088dab7d 100644
--- a/content/shell/browser/shell_platform_delegate_views.cc
+++ b/content/shell/browser/shell_platform_delegate_views.cc
@@ -9,6 +9,7 @@
 #include <memory>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/strings/utf_string_conversions.h"
@@ -351,7 +352,7 @@ ShellPlatformDelegate::~ShellPlatformDelegate() = default;
 void ShellPlatformDelegate::CreatePlatformWindow(
     Shell* shell,
     const gfx::Size& initial_size) {
-  DCHECK(!shell_data_map_.contains(shell));
+  DCHECK(!base::Contains(shell_data_map_, shell));
   ShellData& shell_data = shell_data_map_[shell];
 
   shell_data.content_size = initial_size;
@@ -385,19 +386,19 @@ void ShellPlatformDelegate::CreatePlatformWindow(
 }
 
 gfx::NativeWindow ShellPlatformDelegate::GetNativeWindow(Shell* shell) {
-  DCHECK(shell_data_map_.contains(shell));
+  DCHECK(base::Contains(shell_data_map_, shell));
   ShellData& shell_data = shell_data_map_[shell];
 
   return shell_data.window_widget->GetNativeWindow();
 }
 
 void ShellPlatformDelegate::CleanUp(Shell* shell) {
-  DCHECK(shell_data_map_.contains(shell));
+  DCHECK(base::Contains(shell_data_map_, shell));
   shell_data_map_.erase(shell);
 }
 
 void ShellPlatformDelegate::SetContents(Shell* shell) {
-  DCHECK(shell_data_map_.contains(shell));
+  DCHECK(base::Contains(shell_data_map_, shell));
   ShellData& shell_data = shell_data_map_[shell];
 
   ShellViewForWidget(shell_data.window_widget)
@@ -417,7 +418,7 @@ void ShellPlatformDelegate::EnableUIControl(Shell* shell,
   if (Shell::ShouldHideToolbar())
     return;
 
-  DCHECK(shell_data_map_.contains(shell));
+  DCHECK(base::Contains(shell_data_map_, shell));
   ShellData& shell_data = shell_data_map_[shell];
 
   auto* view = ShellViewForWidget(shell_data.window_widget);
@@ -434,7 +435,7 @@ void ShellPlatformDelegate::SetAddressBarURL(Shell* shell, const GURL& url) {
   if (Shell::ShouldHideToolbar())
     return;
 
-  DCHECK(shell_data_map_.contains(shell));
+  DCHECK(base::Contains(shell_data_map_, shell));
   ShellData& shell_data = shell_data_map_[shell];
 
   ShellViewForWidget(shell_data.window_widget)->SetAddressBarURL(url);
@@ -444,7 +445,7 @@ void ShellPlatformDelegate::SetIsLoading(Shell* shell, bool loading) {}
 
 void ShellPlatformDelegate::SetTitle(Shell* shell,
                                      const std::u16string& title) {
-  DCHECK(shell_data_map_.contains(shell));
+  DCHECK(base::Contains(shell_data_map_, shell));
   ShellData& shell_data = shell_data_map_[shell];
 
   shell_data.window_widget->widget_delegate()->SetTitle(title);
@@ -454,7 +455,7 @@ void ShellPlatformDelegate::MainFrameCreated(Shell* shell,
                                              RenderFrameHost* main_frame) {}
 
 bool ShellPlatformDelegate::DestroyShell(Shell* shell) {
-  DCHECK(shell_data_map_.contains(shell));
+  DCHECK(base::Contains(shell_data_map_, shell));
   ShellData& shell_data = shell_data_map_[shell];
 
   shell_data.window_widget->CloseNow();
diff --git a/content/test/content_browser_test_test.cc b/content/test/content_browser_test_test.cc
index 8f64d2952f..a94ac283ba 100644
--- a/content/test/content_browser_test_test.cc
+++ b/content/test/content_browser_test_test.cc
@@ -8,6 +8,7 @@
 
 #include "base/base_switches.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/scoped_temp_dir.h"
 #include "base/functional/bind.h"
@@ -140,7 +141,7 @@ IN_PROC_BROWSER_TEST_F(ContentBrowserTest, MAYBE_RendererCrashCallStack) {
       "#0 ";
 #endif
 
-  if (!output.contains(crash_string)) {
+  if (!base::Contains(output, crash_string)) {
     GTEST_FAIL() << "Couldn't find\n" << crash_string << "\n in output\n "
                  << output;
   }
@@ -193,7 +194,7 @@ IN_PROC_BROWSER_TEST_F(ContentBrowserTest, MAYBE_BrowserCrashCallStack) {
       "#0 ";
 #endif
 
-  if (!output.contains(crash_string)) {
+  if (!base::Contains(output, crash_string)) {
     GTEST_FAIL() << "Couldn't find\n"
                  << crash_string << "\n in output\n " << output;
   }
diff --git a/content/test/mock_background_sync_controller.cc b/content/test/mock_background_sync_controller.cc
index 634d763dc3..d4b6f13a0e 100644
--- a/content/test/mock_background_sync_controller.cc
+++ b/content/test/mock_background_sync_controller.cc
@@ -3,6 +3,7 @@
 // found in the LICENSE file.
 
 #include "content/test/mock_background_sync_controller.h"
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/time/time.h"
@@ -56,7 +57,7 @@ void MockBackgroundSyncController::ApplyFieldTrialParamsOverrides() {
   if (!base::GetFieldTrialParams(kFieldTrialName, &field_params))
     return;
 
-  if (field_params.contains(kMaxAttemptsParameterName)) {
+  if (base::Contains(field_params, kMaxAttemptsParameterName)) {
     int max_attempts;
     if (base::StringToInt(field_params[kMaxAttemptsParameterName],
                           &max_attempts)) {
@@ -64,7 +65,7 @@ void MockBackgroundSyncController::ApplyFieldTrialParamsOverrides() {
     }
   }
 
-  if (field_params.contains(kMinPeriodicSyncEventsInterval)) {
+  if (base::Contains(field_params, kMinPeriodicSyncEventsInterval)) {
     int min_periodic_sync_events_interval_sec;
     if (base::StringToInt(field_params[kMinPeriodicSyncEventsInterval],
                           &min_periodic_sync_events_interval_sec)) {
diff --git a/content/test/mock_clipboard_host.cc b/content/test/mock_clipboard_host.cc
index a255a9e642..ae3c3a772a 100644
--- a/content/test/mock_clipboard_host.cc
+++ b/content/test/mock_clipboard_host.cc
@@ -4,9 +4,9 @@
 
 #include "content/test/mock_clipboard_host.h"
 
-#include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "base/numerics/byte_conversions.h"
 #include "base/strings/strcat.h"
@@ -63,7 +63,7 @@ std::vector<std::u16string> MockClipboardHost::ReadStandardFormatNames() {
     types.push_back(ui::kMimeTypePng16);
   }
   for (auto& it : custom_data_) {
-    CHECK(!std::ranges::contains(types, it.first));
+    CHECK(!base::Contains(types, it.first));
     types.push_back(it.first);
   }
   return types;
diff --git a/content/test/mock_platform_notification_service.cc b/content/test/mock_platform_notification_service.cc
index ea62ad4af0..32a0cf3838 100644
--- a/content/test/mock_platform_notification_service.cc
+++ b/content/test/mock_platform_notification_service.cc
@@ -7,6 +7,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/strings/utf_string_conversions.h"
@@ -64,7 +65,7 @@ void MockPlatformNotificationService::CloseNotification(
     const std::string& notification_id) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
 
-  if (!non_persistent_notifications_.contains(notification_id)) {
+  if (!base::Contains(non_persistent_notifications_, notification_id)) {
     return;
   }
 
diff --git a/content/test/mock_ssl_host_state_delegate.cc b/content/test/mock_ssl_host_state_delegate.cc
index 9e320b6644..06b16745d0 100644
--- a/content/test/mock_ssl_host_state_delegate.cc
+++ b/content/test/mock_ssl_host_state_delegate.cc
@@ -4,6 +4,7 @@
 
 #include "content/test/mock_ssl_host_state_delegate.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "url/gurl.h"
 
@@ -42,7 +43,7 @@ SSLHostStateDelegate::CertJudgment MockSSLHostStateDelegate::QueryPolicy(
     const net::X509Certificate& cert,
     int error,
     StoragePartition* storage_partition) {
-  if (!exceptions_.contains(host)) {
+  if (!base::Contains(exceptions_, host)) {
     return SSLHostStateDelegate::DENIED;
   }
 
@@ -58,7 +59,7 @@ void MockSSLHostStateDelegate::HostRanInsecureContent(
 bool MockSSLHostStateDelegate::DidHostRunInsecureContent(
     const std::string& host,
     InsecureContentType content_type) {
-  return hosts_ran_insecure_content_.contains(host);
+  return base::Contains(hosts_ran_insecure_content_, host);
 }
 
 void MockSSLHostStateDelegate::AllowHttpForHost(
@@ -70,7 +71,7 @@ void MockSSLHostStateDelegate::AllowHttpForHost(
 bool MockSSLHostStateDelegate::IsHttpAllowedForHost(
     const std::string& host,
     StoragePartition* storage_partition) {
-  return allow_http_hosts_.contains(host);
+  return base::Contains(allow_http_hosts_, host);
 }
 
 void MockSSLHostStateDelegate::SetHttpsEnforcementForHost(
@@ -91,7 +92,7 @@ bool MockSSLHostStateDelegate::IsHttpsEnforcedForUrl(
   if (!url.GetPort().empty()) {
     return false;
   }
-  return enforce_https_hosts_.contains(url.GetHost());
+  return base::Contains(enforce_https_hosts_, url.GetHost());
 }
 
 void MockSSLHostStateDelegate::RevokeUserAllowExceptions(
@@ -102,7 +103,7 @@ void MockSSLHostStateDelegate::RevokeUserAllowExceptions(
 bool MockSSLHostStateDelegate::HasAllowException(
     const std::string& host,
     StoragePartition* storage_partition) {
-  return exceptions_.contains(host);
+  return base::Contains(exceptions_, host);
 }
 
 bool MockSSLHostStateDelegate::HasAllowExceptionForAnyHost(
diff --git a/content/test/web_contents_observer_consistency_checker.cc b/content/test/web_contents_observer_consistency_checker.cc
index f0a652c801..ec289dc5a1 100644
--- a/content/test/web_contents_observer_consistency_checker.cc
+++ b/content/test/web_contents_observer_consistency_checker.cc
@@ -3,10 +3,9 @@
 // found in the LICENSE file.
 
 #include "content/test/web_contents_observer_consistency_checker.h"
-
-#include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/pending_task.h"
 #include "base/strings/stringprintf.h"
@@ -340,7 +339,7 @@ void WebContentsObserverConsistencyChecker::MediaStartedPlaying(
     const MediaPlayerInfo& media_info,
     const MediaPlayerId& id) {
   CHECK(!web_contents_destroyed_);
-  CHECK(!std::ranges::contains(active_media_players_, id));
+  CHECK(!base::Contains(active_media_players_, id));
   active_media_players_.push_back(id);
 }
 
@@ -349,7 +348,7 @@ void WebContentsObserverConsistencyChecker::MediaStoppedPlaying(
     const MediaPlayerId& id,
     WebContentsObserver::MediaStoppedReason reason) {
   CHECK(!web_contents_destroyed_);
-  CHECK(std::ranges::contains(active_media_players_, id));
+  CHECK(base::Contains(active_media_players_, id));
   std::erase(active_media_players_, id);
 }
 
diff --git a/content/web_test/browser/fake_bluetooth_delegate.cc b/content/web_test/browser/fake_bluetooth_delegate.cc
index 60b619fa64..8c94055bdb 100644
--- a/content/web_test/browser/fake_bluetooth_delegate.cc
+++ b/content/web_test/browser/fake_bluetooth_delegate.cc
@@ -4,6 +4,7 @@
 
 #include "content/web_test/browser/fake_bluetooth_delegate.h"
 
+#include "base/containers/contains.h"
 #include "content/public/browser/web_contents.h"
 #include "content/web_test/browser/web_test_control_host.h"
 #include "device/bluetooth/bluetooth_device.h"
@@ -99,7 +100,7 @@ WebBluetoothDeviceId FakeBluetoothDelegate::GrantServiceAccessPermission(
 bool FakeBluetoothDelegate::HasDevicePermission(
     RenderFrameHost* frame,
     const WebBluetoothDeviceId& device_id) {
-  return device_id_to_services_map_.contains(device_id);
+  return base::Contains(device_id_to_services_map_, device_id);
 }
 
 void FakeBluetoothDelegate::RevokeDevicePermissionWebInitiated(
@@ -125,7 +126,7 @@ bool FakeBluetoothDelegate::IsAllowedToAccessService(
   if (id_to_services_it == device_id_to_services_map_.end())
     return false;
 
-  return id_to_services_it->second.contains(service);
+  return base::Contains(id_to_services_it->second, service);
 }
 
 bool FakeBluetoothDelegate::IsAllowedToAccessAtLeastOneService(
@@ -147,7 +148,7 @@ bool FakeBluetoothDelegate::IsAllowedToAccessManufacturerData(
   if (id_to_manufacturer_data_it == device_id_to_manufacturer_code_map_.end())
     return false;
 
-  return id_to_manufacturer_data_it->second.contains(manufacturer_code);
+  return base::Contains(id_to_manufacturer_data_it->second, manufacturer_code);
 }
 
 void FakeBluetoothDelegate::AddFramePermissionObserver(
diff --git a/content/web_test/browser/web_test_control_host.cc b/content/web_test/browser/web_test_control_host.cc
index 34711c544d..98faa282bc 100644
--- a/content/web_test/browser/web_test_control_host.cc
+++ b/content/web_test/browser/web_test_control_host.cc
@@ -21,6 +21,7 @@
 #include "base/base64.h"
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -1253,8 +1254,8 @@ void WebTestControlHost::HandleNewRenderFrameHost(RenderFrameHost* frame) {
   // TODO(rakina): Understand the fetch tests to figure out if it's possible to
   // remove RenderProcessHost tracking here.
   if (main_window &&
-      (!main_window_render_view_hosts_.contains(view_host) ||
-       !main_window_render_process_hosts_.contains(process_host))) {
+      (!base::Contains(main_window_render_view_hosts_, view_host) ||
+       !base::Contains(main_window_render_process_hosts_, process_host))) {
     // When we find the main window's main frame for the first time, we mark the
     // test as starting for the renderer.
     const bool starting_test = main_window_render_process_hosts_.empty();
@@ -2191,7 +2192,7 @@ mojo::AssociatedRemote<mojom::WebTestRenderFrame>&
 WebTestControlHost::GetWebTestRenderFrameRemote(RenderFrameHost* frame) {
   GlobalRenderFrameHostId key(frame->GetProcess()->GetDeprecatedID(),
                               frame->GetRoutingID());
-  if (!web_test_render_frame_map_.contains(key)) {
+  if (!base::Contains(web_test_render_frame_map_, key)) {
     mojo::AssociatedRemote<mojom::WebTestRenderFrame>& new_ptr =
         web_test_render_frame_map_[key];
     frame->GetRemoteAssociatedInterfaces()->GetInterface(&new_ptr);
diff --git a/content/web_test/browser/web_test_permission_manager.cc b/content/web_test/browser/web_test_permission_manager.cc
index b13c16a75c..c5c30e0103 100644
--- a/content/web_test/browser/web_test_permission_manager.cc
+++ b/content/web_test/browser/web_test_permission_manager.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/barrier_callback.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -195,7 +196,7 @@ void WebTestPermissionManager::ResetPermission(blink::PermissionType permission,
       PermissionDescriptorUtil::CreatePermissionDescriptorForPermissionType(
           permission),
       requesting_origin, embedding_origin);
-  if (!permissions_.contains(key)) {
+  if (!base::Contains(permissions_, key)) {
     return;
   }
   permissions_.erase(key);
diff --git a/content/web_test/browser/web_test_shell_platform_delegate_views.cc b/content/web_test/browser/web_test_shell_platform_delegate_views.cc
index 52982c07d3..d79981352b 100644
--- a/content/web_test/browser/web_test_shell_platform_delegate_views.cc
+++ b/content/web_test/browser/web_test_shell_platform_delegate_views.cc
@@ -2,6 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+#include "base/containers/contains.h"
 #include "content/public/browser/render_widget_host_view.h"
 #include "content/public/browser/web_contents.h"
 #include "content/shell/browser/shell.h"
@@ -48,7 +49,7 @@ void WebTestShellPlatformDelegate::CreatePlatformWindow(
     return;
   }
 
-  DCHECK(!web_test_shell_data_map_.contains(shell));
+  DCHECK(!base::Contains(web_test_shell_data_map_, shell));
   WebTestShellData& shell_data = web_test_shell_data_map_[shell];
 
   shell_data.content_size = initial_size;
@@ -69,7 +70,7 @@ void WebTestShellPlatformDelegate::CleanUp(Shell* shell) {
     return;
   }
 
-  DCHECK(web_test_shell_data_map_.contains(shell));
+  DCHECK(base::Contains(web_test_shell_data_map_, shell));
   web_test_shell_data_map_.erase(shell);
 }
 
@@ -79,7 +80,7 @@ void WebTestShellPlatformDelegate::SetContents(Shell* shell) {
     return;
   }
 
-  DCHECK(web_test_shell_data_map_.contains(shell));
+  DCHECK(base::Contains(web_test_shell_data_map_, shell));
   WebTestShellData& shell_data = web_test_shell_data_map_[shell];
 
   aura::Window* content = shell->web_contents()->GetNativeView();
diff --git a/content/web_test/renderer/test_runner.cc b/content/web_test/renderer/test_runner.cc
index dfb183a72a..db6ddfae17 100644
--- a/content/web_test/renderer/test_runner.cc
+++ b/content/web_test/renderer/test_runner.cc
@@ -14,6 +14,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/unique_ptr_adapters.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
@@ -3063,7 +3064,7 @@ void TestRunner::AddLoadingFrame(blink::WebLocalFrame* frame) {
 
 void TestRunner::RemoveLoadingFrame(blink::WebLocalFrame* frame) {
   // We don't track frames that were started between tests.
-  if (!std::ranges::contains(loading_frames_, frame))
+  if (!base::Contains(loading_frames_, frame))
     return;
 
   // We had a DCHECK checking
