From 57494b4faa15732057dc129b8a18f0cbbeab3d91 Mon Sep 17 00:00:00 2001
From: Chongyun Lee <licy183@termux.dev>
Date: Tue, 10 Mar 2026 01:58:54 +0800
Subject: [PATCH 2/8] chrome

---
 .../live_caption_speech_recognition_host.cc   |   3 +-
 .../accessibility/page_colors_controller.cc   |  12 +-
 chrome/browser/actor/actor_metrics.cc         |   5 +-
 chrome/browser/actor/actor_test_util.cc       |   4 +-
 chrome/browser/actor/site_policy.cc           |   5 +-
 chrome/browser/ai/ai_data_keyed_service.cc    |  15 +-
 chrome/browser/app_mode/app_mode_utils.cc     |   5 +-
 .../app_service/app_icon/app_icon_loader.cc   |   3 +-
 .../browser/apps/app_service/policy_util.cc   |   3 +-
 ...os_reimpl_navigation_capturing_throttle.cc |   4 +-
 .../link_capturing_navigation_throttle.cc     |   3 +-
 .../platform_apps/app_window_registry_util.cc |   3 +-
 .../accessibility/accessibility_manager.cc    |   3 +-
 ...rome_app_list_model_updater_browsertest.cc |   4 +-
 .../ash/app_mode/startup_app_launcher.cc      |   4 +-
 .../ash/arc/input_overlay/actions/action.cc   |   4 +-
 .../browser/ash/arc/tracing/arc_cpu_event.cc  |   5 +-
 .../arc/tracing/arc_tracing_graphics_model.cc |   4 +-
 chrome/browser/ash/base/locale_util.cc        |   2 +-
 .../file_manager/private_api_file_system.cc   |  13 +-
 .../system_notification_manager.cc            |   6 +-
 .../file_manager_browsertest_base.cc          |   6 +-
 .../ash/file_manager/office_file_tasks.cc     |   4 +-
 .../ash/input_method/editor_consent_store.cc  |   5 +-
 .../browser/ash/input_method/editor_switch.cc |   9 +-
 chrome/browser/ash/lobster/lobster_service.cc |   7 +-
 .../magic_boost/magic_boost_browsertest.cc    |  70 ++++-----
 .../ash/note_taking/note_taking_helper.cc     |   2 +-
 .../cros_healthd_psr_sampler_handler.cc       |   3 +-
 .../metric_reporting_manager.cc               |  10 +-
 chrome/browser/ash/preferences/preferences.cc |  14 +-
 .../automatic_usb_printer_configurer.h        |   1 +
 .../browser/chrome_content_browser_client.cc  |   2 +-
 .../app_mode/kiosk_app_service_launcher.cc    |   5 +-
 .../cleanup/extension_cleanup_handler.cc      |   2 +-
 .../extensions/vpn_provider/vpn_service.cc    |  15 +-
 ...wa_key_distribution_component_installer.cc |   3 +-
 .../compose/proactive_nudge_tracker.cc        |   4 +-
 .../generated_javascript_optimizer_pref.cc    |   1 +
 .../contextual_tasks_context_service.cc       |   3 +-
 ...al_tasks_url_loader_factory_interceptor.cc |   3 +-
 .../device_connection_tracker.cc              |   5 +-
 .../devtools/device/tcp_device_provider.cc    |   3 +-
 .../devtools/device/usb/android_usb_device.cc |   3 +-
 .../multi_profile_download_notifier.cc        |   3 +-
 .../api/autofill_private/autofill_ai_util.cc  |  14 +-
 .../bookmark_manager_private_api.cc           |   3 +-
 .../declarative_net_request_browsertest.cc    |  13 +-
 .../developer_private_functions.cc            |   3 +-
 .../extensions/api/identity/identity_api.cc   |   3 +-
 .../identity_launch_web_auth_flow_function.cc |   3 +-
 .../language_settings_private_api.cc          |  12 +-
 .../api/vpn_provider/vpn_provider_apitest.cc  |   7 +-
 .../browser/extensions/browser_window_util.cc |   3 +-
 ..._content_browser_client_extensions_part.cc |   3 +-
 chrome/browser/extensions/cws_info_service.cc |   3 +-
 .../extensions/extension_management.cc        |  14 +-
 .../browser/extensions/extension_service.cc   |   3 +-
 .../extensions/external_provider_impl.cc      |   3 +-
 .../mv2_deprecation_impact_checker.cc         |   3 +-
 .../browser/extensions/window_controller.cc   |  20 +--
 .../extensions/window_controller_list.cc      |   5 +-
 chrome/browser/file_select_helper.cc          |   3 +-
 chrome/browser/glic/glic_pref_names.cc        |   5 +-
 .../glic_zero_state_suggestions_manager.cc    |   6 +-
 .../glic/host/context/glic_sharing_utils.cc   |   3 +-
 .../glic_actor_general_interactive_uitest.cc  |   5 +-
 .../glic_actor_interactive_uitest_common.cc   |   8 +-
 .../browser/glic/host/glic_api_browsertest.cc |  10 +-
 chrome/browser/glic/host/host.cc              |   3 +-
 .../browser/glic/test_support/glic_api_test.h |   5 +-
 .../glic/test_support/interactive_glic_test.h |  10 +-
 chrome/browser/google/google_brand.cc         |   3 +-
 chrome/browser/hid/hid_chooser_context.cc     |  26 ++--
 .../history_clusters_tab_helper.cc            |   3 +-
 .../history_embeddings_tab_helper.cc          |   3 +-
 .../android/accessory_sheet_data.cc           |   4 +-
 .../lookalikes/lookalike_url_service.cc       |   3 +-
 .../dial/dial_media_sink_service_impl.cc      |   7 +-
 .../mdns/cast_media_sink_service_impl.cc      |   5 +-
 .../media/router/mojo/media_router_desktop.cc |   9 +-
 .../webrtc/desktop_media_picker_controller.cc |   3 +-
 .../desktop_media_picker_factory_impl.cc      |   5 +-
 .../metrics/tab_footprint_aggregator.cc       |   5 +-
 .../mac/mac_notification_provider_factory.cc  |   3 +-
 .../scheduler/internal/icon_store.cc          |   3 +-
 .../internal/impression_history_tracker.cc    |   3 +-
 .../unified_password_manager_proto_utils.cc   |   1 +
 .../chrome_password_change_service.cc         |   6 +-
 ...nager_captured_sites_interactive_uitest.cc |   4 +-
 chrome/browser/pdf/pdf_extension_util.cc      |   3 +-
 .../policies/background_tab_loading_policy.cc |  19 +--
 .../permissions/chrome_permissions_client.cc  |   3 +-
 .../messaging_layer/upload/server_uploader.cc |   3 +-
 .../test/autofill_ai_policy_browsertest.cc    |  10 +-
 .../autocomplete_action_predictor.cc          |   3 +-
 .../search_preload_unified_browsertest.cc     |   4 +-
 .../browser/printing/pwg_raster_converter.cc  |   3 +-
 .../batch_upload/batch_upload_service.cc      |   3 +-
 .../profiles/profile_attributes_entry.cc      |  11 +-
 chrome/browser/profiles/profile_manager.cc    |   7 +-
 chrome/browser/promos/promos_utils.cc         |   9 +-
 .../chrome_password_protection_service.cc     |   3 +-
 ...tential_password_theft_signal_processor.cc |   5 +-
 .../generated_safe_browsing_pref.cc           |   7 +-
 ...generated_security_settings_bundle_pref.cc |   1 +
 .../test_safe_browsing_database_helper.cc     |   2 +-
 chrome/browser/share/share_ranking.cc         |  17 ++-
 ...dice_intercepted_session_startup_helper.cc |   3 +-
 .../on_device_speech_recognition_impl.cc      |   3 +-
 .../spellchecker/spellcheck_service.cc        |   7 +-
 .../browser/ssl/https_only_mode_tab_helper.h  |   1 +
 .../supervised_user_extensions_manager.cc     |  15 +-
 .../support_tool/ash/shill_data_collector.cc  |   6 +-
 .../ash/system_logs_data_collector.cc         |   1 +
 .../support_tool/policy_data_collector.cc     |   2 +-
 ...client_web_apps_generated_icon_fix_test.cc |   2 +-
 .../drive_backend/metadata_database.cc        |   3 +-
 .../drive_backend/metadata_database_index.cc  |  51 +++----
 .../metadata_database_index_on_disk.cc        |   5 +-
 .../drive_backend/tracker_id_set.cc           |   7 +-
 .../local/local_file_change_tracker.cc        |  23 +--
 .../local/local_file_sync_context.cc          |   7 +-
 .../local/local_file_sync_service.cc          |  27 ++--
 .../local/local_file_sync_status.cc           |   3 +-
 .../browser/sync_file_system/subtree_set.cc   |   4 +-
 .../providers/fallback_task_provider.cc       |   3 +-
 chrome/browser/themes/browser_theme_pack.cc   |   5 +-
 .../touch_to_fill_delegate_android_impl.cc    |   5 +-
 .../touch_to_fill_payment_method_view_impl.cc |   3 +-
 .../full_browser_transition_manager.cc        |   3 +-
 chrome/browser/ui/accelerator_table.cc        |   3 +-
 .../autofill_keyboard_accessory_view_impl.cc  |   3 +-
 .../back_gesture/back_gesture_browsertest.cc  |   3 +-
 .../ash/birch/birch_recent_tabs_provider.cc   |   5 +-
 .../ui/ash/birch/birch_self_share_provider.cc |   5 +-
 .../desks_templates_app_launch_handler.cc     |   2 +-
 .../glanceables/glanceables_browsertest.cc    |  56 +++----
 .../glanceables_classroom_client_impl.cc      |   6 +-
 ...dia_notification_producer_keyed_service.cc |   2 +-
 .../holding_space_downloads_delegate.cc       |   2 +-
 .../holding_space_persistence_delegate.cc     |   2 +-
 .../holding_space_suggestions_delegate.cc     |   2 +-
 .../holding_space_ui_browsertest.cc           |   3 +-
 .../chrome_keyboard_controller_client.cc      |   3 +-
 .../ui/ash/media_client/media_client_impl.cc  |   6 +-
 .../app_service_app_window_arc_tracker.cc     |   9 +-
 ...app_service_app_window_shelf_controller.cc |   5 +-
 .../app_service_instance_registry_helper.cc   |   8 +-
 .../ui/ash/shelf/browser_status_monitor.cc    |   9 +-
 .../ui/ash/shelf/chrome_shelf_controller.cc   |   5 +-
 .../ui/ash/shelf/chrome_shelf_prefs.cc        |  12 +-
 .../ui/ash/wm/tab_cluster_ui_client.cc        |   1 +
 ...ing_triggered_popup_blocker_browsertest.cc |   5 +-
 .../ui/bookmarks/bookmark_utils_desktop.cc    |   7 +-
 chrome/browser/ui/browser_actions.cc          |   4 +-
 chrome/browser/ui/browser_list.cc             |   5 +-
 chrome/browser/ui/browser_list_enumerator.cc  |   3 +-
 ...edit_card_autofill_touch_bar_controller.mm |   1 +
 .../file_system_access_ui_helpers.cc          |  10 +-
 .../cast_device_list_host.cc                  |   8 +-
 .../cast_media_notification_producer.cc       |   5 +-
 .../browser/ui/hats/hats_service_desktop.cc   |   5 +-
 .../browser/ui/hid/hid_chooser_controller.cc  |  13 +-
 .../ui/lens/lens_overlay_controller.cc        |   3 +-
 .../cast_modes_with_media_sources.cc          |   5 +-
 .../ui/media_router/media_route_starter.cc    |   8 +-
 .../ui/media_router/media_router_ui.cc        |   3 +-
 .../ui/media_router/query_result_manager.cc   |  11 +-
 .../save_update_bubble_controller.cc          |   3 +-
 .../memory_saver_utils.cc                     |   1 +
 ...formance_intervention_button_controller.cc |   3 +-
 .../ui/profiles/profile_colors_util.cc        |   3 +-
 .../notification_permission_review_service.cc |   3 +-
 .../template_url_table_model.cc               |   3 +-
 .../ui/serial/serial_chooser_controller.cc    |   3 +-
 .../ui/settings_window_manager_chromeos.cc    |   5 +-
 .../default_browser_infobar_manager.cc        |   3 +-
 .../ui/startup/startup_browser_creator.cc     |   1 +
 .../chrome_web_contents_view_handle_drop.cc   |   5 +-
 .../tabs/existing_tab_group_sub_menu_model.cc |   1 +
 .../ui/tabs/organization/tab_organization.cc  |   5 +-
 .../organization/tab_organization_service.cc  |  13 +-
 .../saved_tab_group_model_listener.cc         |  13 +-
 .../tab_group_sync_delegate_desktop.cc        |   3 +-
 chrome/browser/ui/tabs/tab_group_model.cc     |   3 +-
 .../browser/ui/test/browser_ui_test_base.cc   |   1 +
 .../ui/toolbar/back_forward_menu_model.cc     |   3 +-
 .../toolbar/chrome_labs/chrome_labs_utils.cc  |   3 +-
 .../ui/toolbar/toolbar_actions_model.cc       |  13 +-
 chrome/browser/ui/unload_controller.cc        |   3 +-
 .../app_dialog/app_uninstall_dialog_view.cc   |   6 +-
 .../app_uninstall_dialog_view_browsertest.cc  |   4 +-
 ...t_bnpl_issuer_dialog_interactive_uitest.cc |   4 +-
 .../views/autofill/popup/popup_view_views.cc  |   1 +
 .../saved_tab_groups/saved_tab_group_bar.cc   |   2 +-
 ...xtual_tasks_ephemeral_button_controller.cc |   3 +-
 .../desktop_media_picker_views.cc             |   1 +
 .../bubble/download_bubble_row_list_view.cc   |   3 +-
 .../views/extensions/extensions_menu_view.cc  |   3 +-
 .../file_system_access_usage_bubble_view.cc   |   5 +-
 chrome/browser/ui/views/frame/browser_view.cc |   5 +-
 chrome/browser/ui/views/frame/dbus_appmenu.cc |   9 +-
 .../ui/views/frame/dbus_appmenu_registrar.cc  |   9 +-
 ...menu_model_builder_browsertest_chromeos.cc |   1 +
 .../media_item_ui_device_selector_view.cc     |   1 +
 .../media_router/cast_browser_controller.cc   |   3 +-
 .../ui/views/media_router/cast_dialog_view.cc |   7 +-
 .../ui/views/omnibox/omnibox_view_views.cc    |   7 +-
 .../page_action_icon_controller.cc            |   5 +-
 .../about_this_site_side_panel_coordinator.cc |   4 +-
 ...ermission_prompt_bubble_one_origin_view.cc |   3 +-
 .../permissions/permission_prompt_chip.cc     |   1 +
 .../ui/views/profiles/profile_picker_view.cc  |   3 +-
 .../session_restore_infobar_manager.cc        |   2 +-
 ...t_integration_interaction_test_internal.cc |   5 +-
 .../browser/ui/views/status_bubble_views.cc   |   3 +-
 .../views/sync/inline_login_ui_browsertest.cc |   1 +
 .../tab_capture_contents_border_helper.cc     |   5 +-
 .../views/tab_sharing/tab_sharing_ui_views.cc |   3 +-
 .../tabs/dragging/tab_drag_controller.cc      |   3 +-
 .../tabs/fake_base_tab_strip_controller.cc    |   6 +-
 chrome/browser/ui/views/tabs/tab_strip.cc     |   3 +-
 ...tab_collection_animating_layout_manager.cc |   2 +-
 .../tab_collection_node_browsertest.cc        |   4 +-
 .../tabs/vertical/vertical_split_tab_view.cc  |   3 +-
 .../chrome_labs_view_controller.cc            |   3 +-
 .../pinned_toolbar_actions_container.cc       |   5 +-
 .../toolbar/toolbar_icon_container_view.cc    |   3 +-
 ...wser_user_education_service_browsertest.cc |   4 +-
 ...ture_promo_lifecycle_interactive_uitest.cc |   5 +-
 ...preinstalled_deprecated_app_dialog_view.cc |   3 +-
 .../protocol_handler_picker_coordinator.cc    |   4 +-
 .../views/web_apps/sub_apps_install_dialog.cc |   7 +-
 .../web_app_integration_test_driver.cc        |  11 +-
 ...eb_app_update_review_dialog_browsertest.cc |   8 +-
 .../app_browser_controller.cc                 |   4 +-
 .../create_shortcut_browsertest.cc            |   1 +
 .../navigation_capturing_process.cc           |   5 +-
 .../web_applications/web_app_browsertest.cc   |   3 +-
 .../web_app_protocol_handling_browsertest.cc  |   7 +-
 .../authenticator_dialog_browsertest.cc       |   5 +-
 .../access_code_cast_handler.cc               |   7 +-
 .../app_management_shelf_delegate_chromeos.cc |   5 +-
 .../ash/cloud_upload/cloud_open_metrics.cc    |   4 +-
 .../ash/cloud_upload/cloud_upload_dialog.cc   |   5 +-
 .../ash/cloud_upload/drive_upload_handler.cc  |  10 +-
 .../edu_coexistence_state_tracker.cc          |  13 +-
 .../lock_screen_reauth_dialogs.cc             |   6 +-
 .../lock_screen_reauth_handler.cc             |   3 +-
 .../ui/webui/ash/login/gaia_screen_handler.cc |   1 +
 .../browser/ui/webui/ash/login/l10n_util.cc   |   7 +-
 .../webui/ash/login/oobe_display_chooser.cc   |   5 +-
 chrome/browser/ui/webui/ash/mako/mako_ui.cc   |   6 +-
 .../settings/calculator/size_calculator.cc    |   4 +-
 .../pages/a11y/accessibility_handler.cc       |   2 +-
 .../fast_pair_saved_devices_handler.cc        |   4 +-
 .../pages/internet/internet_section.cc        |  10 +-
 .../settings/pages/os_settings_sections.cc    |   3 +-
 .../pages/people/fingerprint_handler.cc       |   3 +-
 .../ui/webui/ash/settings/search/hierarchy.cc |   5 +-
 .../test_support/fake_os_settings_section.cc  |   3 +-
 .../system_web_dialog_delegate.cc             |   9 +-
 .../browser_command_handler.cc                |   7 +-
 .../chrome_url_data_manager_browsertest.cc    |   8 +-
 .../searchbox/searchbox_handler.cc            |   3 +-
 .../history_clusters_handler.cc               |   1 +
 .../webui/management/management_ui_handler.cc |   1 +
 .../management_ui_handler_chromeos.cc         |   4 +-
 .../nearby_internals_ui_presence_handler.cc   |   3 +-
 .../new_tab_page/new_tab_page_handler.cc      |  16 +-
 .../ui/webui/new_tab_page/new_tab_page_ui.cc  |  10 +-
 .../ui/webui/new_tab_page/untrusted_source.cc |   4 +-
 .../new_tab_page/webui_ntp_browsertest.cc     |   6 +-
 .../ui/webui/ntp_tiles_internals_ui.cc        |   8 +-
 .../print_preview/pdf_printer_handler.cc      |   3 +-
 .../print_preview/print_preview_handler.cc    |  16 +-
 .../print_preview/print_preview_utils.cc      |   3 +-
 .../ui/webui/profile_helper_browsertest.cc    |   1 +
 .../ui/webui/sanitized_image_source.cc        |   1 +
 .../searchbox/realbox_handler_browsertest.cc  |  16 +-
 .../ui/webui/settings/captions_handler.cc     |   3 +-
 ...settings_secure_dns_handler_browsertest.cc |   3 +-
 .../settings/settings_security_key_handler.cc |   3 +-
 .../webui/settings/site_settings_handler.cc   |   7 +-
 .../ui/webui/settings/site_settings_helper.cc |   1 +
 .../customize_chrome_page_handler.cc          |  31 ++--
 .../wallpaper_search_handler.cc               |   7 +-
 .../read_anything_untrusted_page_handler.cc   |   9 +-
 .../signin/ash/inline_login_handler_impl.cc   |   3 +-
 .../webui/signin/inline_login_handler_impl.cc |   4 +-
 .../ui/webui/signin/signin_ui_error.cc        |   3 +-
 .../support_tool/support_tool_ui_utils.cc     |   4 +-
 .../tab_search/tab_search_page_handler.cc     |  18 ++-
 .../ui/webui/tab_search/tab_search_prefs.cc   |   7 +-
 .../tab_search/tab_search_ui_browsertest.cc   |   6 +-
 ...tab_strip_internals_handler_browsertest.cc |   5 +-
 chrome/browser/ui/webui/theme_source.cc       |   3 +-
 .../top_chrome/preload_candidate_selector.cc  |   4 +-
 .../top_chrome/top_chrome_webui_config.cc     |   1 +
 .../webui_contents_preload_manager.cc         |   4 +-
 .../webui_contents_warmup_level_recorder.cc   |   5 +-
 .../iwa_internals_handler.cc                  |   5 +-
 .../whats_new_storage_service_impl.cc         |   1 +
 chrome/browser/unexpire_flags.cc              |   5 +-
 chrome/browser/usb/chrome_usb_delegate.cc     |   9 +-
 chrome/browser/usb/usb_chooser_context.cc     |  16 +-
 chrome/browser/vr/ui_host/vr_ui_host_impl.cc  |  10 +-
 .../browser/vr/webxr_vr_input_browser_test.cc |   5 +-
 .../app_service/web_app_publisher_helper.cc   |   3 +-
 .../fetch_manifest_and_install_command.cc     |   2 +-
 .../fetch_manifest_and_update_command.cc      |   3 +-
 ...tall_app_from_verified_manifest_command.cc |   1 +
 .../install_from_sync_command_browsertest.cc  |   2 +-
 .../manifest_silent_update_command.cc         |   1 +
 ...pdate_protocol_handler_approval_command.cc |   1 +
 ...ol_handler_approval_command_browsertest.cc |   1 +
 .../externally_managed_app_manager.cc         |   3 +-
 .../icons/trusted_icon_filter.cc              |   2 +-
 .../commands/cleanup_bundle_cache_command.cc  |   3 +-
 ...p_managed_configuration_api_browsertest.cc |   2 +-
 ..._web_app_url_loader_factory_browsertest.cc |   3 +-
 .../iwa_key_distribution_info_provider.cc     |   9 +-
 .../policy/isolated_web_app_policy_manager.cc |   3 +-
 ...ated_web_app_policy_manager_browsertest.cc |   4 +-
 .../test/bundle_versions_storage.cc           |   1 +
 .../fake_chrome_iwa_runtime_data_provider.cc  |  15 +-
 .../isolated_web_app_update_discovery_task.cc |   3 +-
 .../manifest_to_web_app_install_info_job.cc   |   9 +-
 .../uninstall/remove_install_source_job.cc    |   3 +-
 .../jobs/uninstall/remove_install_url_job.cc  |   5 +-
 .../locks/partitioned_lock_manager.cc         |   6 +-
 .../manifest_update_manager.cc                |   7 +-
 .../manifest_update_manager_browsertest.cc    |  21 +--
 .../protocol_handling_sub_manager.cc          |   5 +-
 .../web_app_file_handler_manager.cc           |   5 +-
 ..._handler_registration_linux_browsertest.cc |   5 +-
 .../web_app_protocol_handler_manager.cc       |   5 +-
 .../policy/app_service_web_app_policy.cc      |   5 +-
 .../policy/web_app_policy_manager.cc          |  19 ++-
 .../preinstalled_web_app_manager.cc           |   3 +-
 .../sampling_metrics_provider.cc              |   5 +-
 .../web_applications/test/fake_environment.cc |   3 +-
 .../test/fake_os_integration_manager.cc       |   1 +
 .../test/fake_web_app_ui_manager.cc           |   1 +
 .../test/os_integration_test_override_impl.cc |  18 ++-
 .../test/test_web_app_url_loader.cc           |   7 +-
 .../test/web_app_icon_test_utils.cc           |   5 +-
 ..._uninstalled_preinstalled_web_app_prefs.cc |   3 +-
 .../visited_manifest_manager.cc               |   1 +
 .../web_app_command_manager.cc                |   3 +-
 .../web_app_database_serialization.cc         |   5 +-
 .../web_applications/web_app_install_utils.cc |   9 +-
 ...ink_capturing_parameterized_browsertest.cc |  33 +++--
 ..._origin_association_manager_browsertest.cc |   8 +-
 .../web_applications/web_app_registrar.cc     |  22 +--
 .../web_app_registry_update.cc                |   5 +-
 .../browser/web_applications/web_app_utils.cc |   5 +-
 .../web_install_service_impl.cc               |   2 +-
 ...authenticator_request_dialog_controller.cc |  22 +--
 chrome/browser/webauthn/cablev2_devices.cc    |   1 +
 .../chrome_authenticator_request_delegate.cc  |  15 +-
 .../chrome_web_authentication_delegate.cc     |  18 +--
 chrome/common/actor/action_result.cc          |   1 +
 .../controlled_frame/controlled_frame.cc      |   3 +-
 chrome/common/extensions/webstore_override.cc |   3 +-
 .../notification_image_retainer.cc            |   3 +-
 chrome/common/printing/print_media_l10n.cc    |   1 +
 .../common/printing/printer_capabilities.cc   |   3 +-
 .../gaiacp/gaia_credential_base.cc            |   4 +-
 .../gaiacp/gaia_credential_provider.cc        |   4 +-
 .../gaiacp/win_http_url_fetcher.cc            |   4 +-
 .../accessibility/ax_tree_distiller.cc        |   9 +-
 .../read_anything/read_aloud_app_model.cc     |   6 +-
 .../read_aloud_app_model_browsertest.cc       |  23 ++-
 .../read_anything_app_controller.cc           |  87 +++++------
 ...ead_anything_app_controller_browsertest.cc | 105 ++++++-------
 .../read_anything/read_anything_app_model.cc  |  22 +--
 .../read_anything_app_model_browsertest.cc    | 140 +++++++++---------
 .../renderer/actor/page_stability_monitor.cc  |   1 +
 chrome/renderer/actor/tool_executor.cc        |   3 +-
 .../chrome_content_settings_agent_delegate.cc |   5 +-
 .../web_url_pattern_natives.cc                |   3 +-
 .../translate/translate_agent_browsertest.cc  |   8 +-
 .../translate/translate_script_browsertest.cc |  27 ++--
 .../sharing/nearby/nearby_connections.cc      |   2 +-
 ...earby_connections_stream_buffer_manager.cc |   3 +-
 .../sharing/nearby/platform/ble_medium.cc     |  13 +-
 .../nearby/platform/ble_v2_gatt_server.cc     |   1 +
 .../platform/bluetooth_classic_medium.cc      |   5 +-
 .../nearby/test_support/fake_adapter.cc       |   9 +-
 ...cros_speech_recognition_recognizer_impl.cc |   4 +-
 .../base/ash/extension_js_browser_test.cc     |   3 +-
 .../interactive/network/shill_service_util.cc |   4 +-
 chrome/test/base/ash/web_ui_browser_test.cc   |   7 +-
 .../test/base/chromeos/crosier/annotations.cc |   3 +-
 .../base/devtools_agent_coverage_observer.cc  |   5 +-
 chrome/test/base/testing_profile.cc           |   3 +-
 chrome/test/base/ui_test_utils.cc             |   3 +-
 chrome/test/chromedriver/capabilities.cc      |   3 +-
 .../test/chromedriver/chrome/chrome_impl.cc   |   3 +-
 .../chromedriver/chrome/device_manager.cc     |   5 +-
 chrome/test/chromedriver/commands.cc          |   3 +-
 chrome/test/chromedriver/logging.cc           |   4 +-
 .../server/chromedriver_server.cc             |   3 +-
 ...ccess_code_cast_integration_browsertest.cc |   2 +-
 .../media_router_e2e_browsertest.cc           |   3 +-
 .../media_router_integration_browsertest.cc   |   4 +-
 ...interactive_feature_promo_test_internal.cc |   3 +-
 chrome/updater/enum_traits.h                  |   7 +-
 .../updater/handle_inconsistent_apps_task.cc  |  26 ++--
 chrome/updater/mac/keystone/agent_main.cc     |   6 +-
 chrome/updater/policy/service.cc              |  12 +-
 chrome/updater/tag.cc                         |   6 +-
 chrome/updater/test/integration_tests_impl.cc |   9 +-
 chrome/updater/test/request_matcher.cc        |   4 +-
 chrome/updater/tools/updater_util.cc          |   6 +-
 chrome/updater/update_service_impl_impl.cc    |   3 +-
 chrome/updater/util/util.h                    |   4 +-
 chrome/updater/win/installer_api.cc           |   3 +-
 chrome/updater/win/setup/uninstall.cc         |   1 +
 .../importer/bookmarks_file_importer.cc       |   3 +-
 chrome/utility/importer/ie_importer_win.cc    |   4 +-
 423 files changed, 1565 insertions(+), 1266 deletions(-)

diff --git a/chrome/browser/accessibility/live_caption/live_caption_speech_recognition_host.cc b/chrome/browser/accessibility/live_caption/live_caption_speech_recognition_host.cc
index 839f36c99c..10ee372d30 100644
--- a/chrome/browser/accessibility/live_caption/live_caption_speech_recognition_host.cc
+++ b/chrome/browser/accessibility/live_caption/live_caption_speech_recognition_host.cc
@@ -11,6 +11,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/strcat.h"
@@ -88,7 +89,7 @@ bool IsLanguageInstallable(std::string_view language_code) {
     }
   }
 
-  return std::ranges::contains(
+  return base::Contains(
       speech::SodaInstaller::GetInstance()->GetLiveCaptionEnabledLanguages(),
       language_code);
 }
diff --git a/chrome/browser/accessibility/page_colors_controller.cc b/chrome/browser/accessibility/page_colors_controller.cc
index d4bc75210f..65e0fe04a9 100644
--- a/chrome/browser/accessibility/page_colors_controller.cc
+++ b/chrome/browser/accessibility/page_colors_controller.cc
@@ -4,9 +4,8 @@
 
 #include "chrome/browser/accessibility/page_colors_controller.h"
 
-#include <utility>
-
 #include "base/containers/fixed_flat_map.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/common/pref_names.h"
 #include "components/prefs/pref_registry_simple.h"
@@ -44,7 +43,7 @@ PageColorsController::~PageColorsController() = default;
 // static
 void PageColorsController::RegisterProfilePrefs(PrefRegistrySimple* registry) {
   registry->RegisterIntegerPref(prefs::kRequestedPageColors,
-                                std::to_underlying(PageColors::kNoPreference));
+                                base::to_underlying(PageColors::kNoPreference));
   registry->RegisterBooleanPref(prefs::kApplyPageColorsOnlyOnIncreasedContrast,
                                 false);
   registry->RegisterListPref(prefs::kPageColorsBlockList);
@@ -74,7 +73,7 @@ void PageColorsController::MigrateObsoleteProfilePrefs(
         requested_page_colors = PageColors::kNoPreference;
       }
       profile_prefs->SetInteger(prefs::kRequestedPageColors,
-                                std::to_underlying(requested_page_colors));
+                                base::to_underlying(requested_page_colors));
     }
   }
   profile_prefs->ClearPref(kPageColors);
@@ -89,7 +88,7 @@ void PageColorsController::OnNativeThemeUpdated(
 void PageColorsController::SetRequestedPageColors(PageColors page_colors) {
   // Setting the pref will automatically trigger `RecomputePageColors()`.
   profile_prefs_->SetInteger(prefs::kRequestedPageColors,
-                             std::to_underlying(page_colors));
+                             base::to_underlying(page_colors));
 }
 
 void PageColorsController::RecomputePageColors() {
@@ -106,7 +105,8 @@ void PageColorsController::RecomputePageColors() {
   const int pref_value =
       profile_prefs_->GetInteger(prefs::kRequestedPageColors);
   PageColors page_colors =
-      (pref_value < 0 || pref_value > std::to_underlying(PageColors::kMaxValue))
+      (pref_value < 0 ||
+       pref_value > base::to_underlying(PageColors::kMaxValue))
           ? PageColors::kNoPreference
           : static_cast<PageColors>(pref_value);
   if (preferred_contrast != ui::NativeTheme::PreferredContrast::kMore &&
diff --git a/chrome/browser/actor/actor_metrics.cc b/chrome/browser/actor/actor_metrics.cc
index dfad4f3694..9312aeb2f8 100644
--- a/chrome/browser/actor/actor_metrics.cc
+++ b/chrome/browser/actor/actor_metrics.cc
@@ -4,12 +4,11 @@
 
 #include "chrome/browser/actor/actor_metrics.h"
 
-#include <utility>
-
 #include "base/metrics/histogram_functions.h"
 #include "base/not_fatal_until.h"
 #include "base/notreached.h"
 #include "base/strings/strcat.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/actor/actor_task.h"
 #include "components/optimization_guide/proto/features/actions_data.pb.h"
 
@@ -109,7 +108,7 @@ void RecordActionResultCode(actor::mojom::ActionResultCode action_result_code) {
   // histogram here because, the linear histograms are limited to 1000 values in
   // base/metrics/histogram.cc.
   base::UmaHistogramSparse("Actor.ExecutionEngine.Action.ResultCode",
-                           std::to_underlying(action_result_code));
+                           base::to_underlying(action_result_code));
 }
 
 void RecordPageContextApcDuration(base::TimeDelta duration) {
diff --git a/chrome/browser/actor/actor_test_util.cc b/chrome/browser/actor/actor_test_util.cc
index 0e4178d7e0..011042c7f1 100644
--- a/chrome/browser/actor/actor_test_util.cc
+++ b/chrome/browser/actor/actor_test_util.cc
@@ -6,13 +6,13 @@
 
 #include <memory>
 #include <string_view>
-#include <utility>
 
 #include "base/base64.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
 #include "base/files/file_util.h"
 #include "base/no_destructor.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/browser/actor/enterprise_policy_url_checker.h"
 #include "chrome/browser/actor/execution_engine.h"
@@ -655,7 +655,7 @@ void ExpectErrorResult(PerformActionsFuture& future,
 }
 
 void PrintTo(const mojom::ActionResultCode& code, std::ostream* os) {
-  *os << std::to_underlying(code);
+  *os << base::to_underlying(code);
 }
 
 bool SetUpOptimizationGuideComponentBlocklist(const base::FilePath& path,
diff --git a/chrome/browser/actor/site_policy.cc b/chrome/browser/actor/site_policy.cc
index 04cfedff5a..dc4fc343f8 100644
--- a/chrome/browser/actor/site_policy.cc
+++ b/chrome/browser/actor/site_policy.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback.h"
 #include "base/notimplemented.h"
@@ -96,12 +97,12 @@ bool IsHostInAllowList(const std::vector<std::string_view>& allowlist,
                        const GURL& url,
                        bool include_subdomains) {
   if (!include_subdomains) {
-    return std::ranges::contains(allowlist, url.host());
+    return base::Contains(allowlist, url.host());
   }
 
   std::string host = url.GetHost();
   while (!host.empty()) {
-    if (std::ranges::contains(allowlist, host)) {
+    if (base::Contains(allowlist, host)) {
       return true;
     }
     host = net::GetSuperdomain(host);
diff --git a/chrome/browser/ai/ai_data_keyed_service.cc b/chrome/browser/ai/ai_data_keyed_service.cc
index 1f1f754955..7427592413 100644
--- a/chrome/browser/ai/ai_data_keyed_service.cc
+++ b/chrome/browser/ai/ai_data_keyed_service.cc
@@ -12,6 +12,7 @@
 #include "base/barrier_callback.h"
 #include "base/base64.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -767,7 +768,7 @@ bool AiDataKeyedService::IsExtensionAllowlistedForData(
   std::vector<std::string> blocklisted_extensions =
       base::SplitString(kBlocklistedExtensionsForData.Get(), ",",
                         base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-  if (std::ranges::contains(blocklisted_extensions, extension_id)) {
+  if (base::Contains(blocklisted_extensions, extension_id)) {
     return false;
   }
 
@@ -786,14 +787,14 @@ bool AiDataKeyedService::IsExtensionAllowlistedForData(
                                        "fiamdfnbelfkjlacoaeiclobkdmckaoa",
                                        // https://issues.chromium.org/427296150
                                        "mofldjifenhadohlkkngamgbifiofbnd"});
-  if (std::ranges::contains(*kHardcodedAllowlistedExtensions, extension_id)) {
+  if (base::Contains(*kHardcodedAllowlistedExtensions, extension_id)) {
     return true;
   }
 
   std::vector<std::string> allowlisted_extensions =
       base::SplitString(kAllowlistedExtensionsForData.Get(), ",",
                         base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-  if (std::ranges::contains(allowlisted_extensions, extension_id)) {
+  if (base::Contains(allowlisted_extensions, extension_id)) {
     return true;
   }
 
@@ -805,7 +806,7 @@ bool AiDataKeyedService::IsExtensionAllowlistedForActions(
   std::vector<std::string> blocklisted_extensions =
       base::SplitString(kBlocklistedExtensionsForActions.Get(), ",",
                         base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-  if (std::ranges::contains(blocklisted_extensions, extension_id)) {
+  if (base::Contains(blocklisted_extensions, extension_id)) {
     return false;
   }
 
@@ -815,14 +816,14 @@ bool AiDataKeyedService::IsExtensionAllowlistedForActions(
           // api_test/experimental_actor/manifest.json
           "kbanhggbnnaciicfpdkheonkpkeakfal",
       });
-  if (std::ranges::contains(*kHardcodedAllowlistedExtensions, extension_id)) {
+  if (base::Contains(*kHardcodedAllowlistedExtensions, extension_id)) {
     return true;
   }
 
   std::vector<std::string> allowlisted_extensions =
       base::SplitString(kAllowlistedExtensionsForActions.Get(), ",",
                         base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-  if (std::ranges::contains(allowlisted_extensions, extension_id)) {
+  if (base::Contains(allowlisted_extensions, extension_id)) {
     return true;
   }
 
@@ -841,5 +842,5 @@ bool AiDataKeyedService::IsExtensionAllowlistedForStable(
   static const base::NoDestructor<std::vector<std::string>>
       kStableChannelAllowlistedIds({// https://issues.chromium.org/427296150
                                     "mofldjifenhadohlkkngamgbifiofbnd"});
-  return std::ranges::contains(*kStableChannelAllowlistedIds, extension_id);
+  return base::Contains(*kStableChannelAllowlistedIds, extension_id);
 }
diff --git a/chrome/browser/app_mode/app_mode_utils.cc b/chrome/browser/app_mode/app_mode_utils.cc
index 1bbcd3d78e..453a7b6856 100644
--- a/chrome/browser/app_mode/app_mode_utils.cc
+++ b/chrome/browser/app_mode/app_mode_utils.cc
@@ -13,6 +13,7 @@
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_split.h"
 #include "build/buildflag.h"
 #include "chrome/app/chrome_command_ids.h"
@@ -95,8 +96,8 @@ bool IsCommandAllowedInAppMode(int command_id, bool is_popup) {
 
   constexpr int kAllowedPopup[] = {IDC_CLOSE_TAB};
 
-  return std::ranges::contains(kAllowed, command_id) ||
-         (is_popup && std::ranges::contains(kAllowedPopup, command_id));
+  return base::Contains(kAllowed, command_id) ||
+         (is_popup && base::Contains(kAllowedPopup, command_id));
 }
 
 bool IsRunningInAppMode() {
diff --git a/chrome/browser/apps/app_service/app_icon/app_icon_loader.cc b/chrome/browser/apps/app_service/app_icon/app_icon_loader.cc
index 9bb74e18fd..e97abebe8a 100644
--- a/chrome/browser/apps/app_service/app_icon/app_icon_loader.cc
+++ b/chrome/browser/apps/app_service/app_icon/app_icon_loader.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -511,7 +512,7 @@ void AppIconLoader::LoadWebAppIcon(const std::string& web_app_id,
                 size_hint_in_dip_,
                 ui::GetScaleForResourceScaleFactor(scale_factor)));
         DCHECK(size_and_purpose.has_value());
-        if (!std::ranges::contains(icon_pixel_sizes,
+        if (!base::Contains(icon_pixel_sizes,
                                    size_and_purpose->size_px)) {
           icon_pixel_sizes.emplace_back(size_and_purpose->size_px);
         }
diff --git a/chrome/browser/apps/app_service/policy_util.cc b/chrome/browser/apps/app_service/policy_util.cc
index 5adf9a2e09..16cd935b5a 100644
--- a/chrome/browser/apps/app_service/policy_util.cc
+++ b/chrome/browser/apps/app_service/policy_util.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/strings/string_util.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
@@ -89,7 +90,7 @@ std::vector<std::string> GetAppIdsFromPolicyId(Profile* profile,
       ->AppRegistryCache()
       .ForEachApp([&policy_id, &app_ids](const apps::AppUpdate& update) {
         if (IsInstalled(update.Readiness()) &&
-            std::ranges::contains(update.PolicyIds(), policy_id)) {
+            base::Contains(update.PolicyIds(), policy_id)) {
           app_ids.push_back(update.AppId());
         }
       });
diff --git a/chrome/browser/apps/link_capturing/chromeos_reimpl_navigation_capturing_throttle.cc b/chrome/browser/apps/link_capturing/chromeos_reimpl_navigation_capturing_throttle.cc
index 44cef5907d..ca9bbe0b55 100644
--- a/chrome/browser/apps/link_capturing/chromeos_reimpl_navigation_capturing_throttle.cc
+++ b/chrome/browser/apps/link_capturing/chromeos_reimpl_navigation_capturing_throttle.cc
@@ -7,7 +7,6 @@
 #include <algorithm>
 #include <memory>
 #include <string>
-#include <utility>
 
 #include "ash/constants/web_app_id_constants.h"
 #include "ash/webui/projector_app/public/cpp/projector_app_constants.h"
@@ -22,6 +21,7 @@
 #include "base/task/single_thread_task_runner.h"
 #include "base/time/default_tick_clock.h"
 #include "base/time/tick_clock.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
 #include "chrome/browser/apps/app_service/app_service_proxy_factory.h"
 #include "chrome/browser/apps/app_service/launch_utils.h"
@@ -184,7 +184,7 @@ bool IsCapturableLinkNavigation(ui::PageTransition page_transition,
     return false;
   }
 
-  if (std::to_underlying(ui::PageTransitionGetQualifier(page_transition)) !=
+  if (base::to_underlying(ui::PageTransitionGetQualifier(page_transition)) !=
       0) {
     // Qualifiers indicate that this navigation was the result of a click on a
     // forward/back button, or typing in the URL bar. Don't handle any of those
diff --git a/chrome/browser/apps/link_capturing/link_capturing_navigation_throttle.cc b/chrome/browser/apps/link_capturing/link_capturing_navigation_throttle.cc
index a532608564..905c869744 100644
--- a/chrome/browser/apps/link_capturing/link_capturing_navigation_throttle.cc
+++ b/chrome/browser/apps/link_capturing/link_capturing_navigation_throttle.cc
@@ -9,6 +9,7 @@
 #include "base/memory/ptr_util.h"
 #include "base/no_destructor.h"
 #include "base/task/single_thread_task_runner.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/apps/link_capturing/link_capturing_tab_data.h"
 #include "chrome/browser/preloading/prefetch/no_state_prefetch/chrome_no_state_prefetch_contents_delegate.h"  // nogncheck https://crbug.com/1474116
 #include "chrome/browser/profiles/keep_alive/profile_keep_alive_types.h"  // nogncheck https://crbug.com/1474116
@@ -108,7 +109,7 @@ bool LinkCapturingNavigationThrottle::IsCapturableLinkNavigation(
     return false;
   }
 
-  if (std::to_underlying(ui::PageTransitionGetQualifier(page_transition)) !=
+  if (base::to_underlying(ui::PageTransitionGetQualifier(page_transition)) !=
       0) {
     // Qualifiers indicate that this navigation was the result of a click on a
     // forward/back button, or typing in the URL bar. Don't handle any of those
diff --git a/chrome/browser/apps/platform_apps/app_window_registry_util.cc b/chrome/browser/apps/platform_apps/app_window_registry_util.cc
index 4bdfb93100..772cc0b445 100644
--- a/chrome/browser/apps/platform_apps/app_window_registry_util.cc
+++ b/chrome/browser/apps/platform_apps/app_window_registry_util.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "chrome/browser/browser_process.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/profiles/profile_manager.h"
@@ -82,7 +83,7 @@ void AppWindowRegistryUtil::CloseAllAppWindows() {
     AppWindowList window_list_copy(registry->app_windows());
     for (AppWindow* window : window_list_copy) {
       // Ensure window is still valid.
-      if (std::ranges::contains(registry->app_windows(), window)) {
+      if (base::Contains(registry->app_windows(), window)) {
         window->GetBaseWindow()->Close();
       }
     }
diff --git a/chrome/browser/ash/accessibility/accessibility_manager.cc b/chrome/browser/ash/accessibility/accessibility_manager.cc
index fb94856cc9..a845d3c02d 100644
--- a/chrome/browser/ash/accessibility/accessibility_manager.cc
+++ b/chrome/browser/ash/accessibility/accessibility_manager.cc
@@ -47,6 +47,7 @@
 #include "base/task/task_traits.h"
 #include "base/task/thread_pool.h"
 #include "base/threading/scoped_blocking_call.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/browser/accessibility/accessibility_extension_api_ash.h"
 #include "chrome/browser/ash/accessibility/accessibility_dlc_installer.h"
@@ -2661,7 +2662,7 @@ void AccessibilityManager::SendMouseEventToSelectToSpeak(
       return;
     default:
       NOTIMPLEMENTED() << "Received unexpected event: "
-                       << std::to_underlying(type);
+                       << base::to_underlying(type);
       break;
   }
 
diff --git a/chrome/browser/ash/app_list/chrome_app_list_model_updater_browsertest.cc b/chrome/browser/ash/app_list/chrome_app_list_model_updater_browsertest.cc
index a9a3e93105..07f9aec434 100644
--- a/chrome/browser/ash/app_list/chrome_app_list_model_updater_browsertest.cc
+++ b/chrome/browser/ash/app_list/chrome_app_list_model_updater_browsertest.cc
@@ -53,7 +53,7 @@ std::vector<std::string> GetOrderedShelfItems(
     const std::set<std::string>& filter) {
   std::vector<std::string> result;
   for (const auto& item : ash::ShelfModel::Get()->items()) {
-    if (filter.contains(item.id.app_id)) {
+    if (base::Contains(filter, item.id.app_id)) {
       result.push_back(item.id.app_id);
     }
   }
@@ -461,7 +461,7 @@ IN_PROC_BROWSER_TEST_F(ChromeAppListModelUpdaterTest,
       app_list_test_api_.GetTopLevelViewIdList();
   std::vector<std::string> filtered_top_level_id_list;
   for (const auto& item : top_level_id_list) {
-    if (app_filter.contains(item)) {
+    if (base::Contains(app_filter, item)) {
       filtered_top_level_id_list.push_back(item);
     }
   }
diff --git a/chrome/browser/ash/app_mode/startup_app_launcher.cc b/chrome/browser/ash/app_mode/startup_app_launcher.cc
index 4f1c4a8022..24c0c24bd9 100644
--- a/chrome/browser/ash/app_mode/startup_app_launcher.cc
+++ b/chrome/browser/ash/app_mode/startup_app_launcher.cc
@@ -6,13 +6,13 @@
 
 #include <memory>
 #include <string>
-#include <utility>
 
 #include "base/check.h"
 #include "base/check_op.h"
 #include "base/functional/bind.h"
 #include "base/notreached.h"
 #include "base/syslog_logging.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/app_mode/kiosk_app_launch_error.h"
 #include "chrome/browser/ash/app_mode/kiosk_app_launcher.h"
 #include "chrome/browser/ash/app_mode/kiosk_app_types.h"
@@ -98,7 +98,7 @@ void StartupAppLauncher::Initialize() {
 
 void StartupAppLauncher::ContinueWithNetworkReady() {
   SYSLOG(INFO) << "ContinueWithNetworkReady"
-               << ", state_=" << std::to_underlying(state_);
+               << ", state_=" << base::to_underlying(state_);
 
   if (state_ != LaunchState::kInitializingNetwork &&
       state_ != LaunchState::kNotStarted) {
diff --git a/chrome/browser/ash/arc/input_overlay/actions/action.cc b/chrome/browser/ash/arc/input_overlay/actions/action.cc
index 084a257d2a..5f1b5eb3de 100644
--- a/chrome/browser/ash/arc/input_overlay/actions/action.cc
+++ b/chrome/browser/ash/arc/input_overlay/actions/action.cc
@@ -5,10 +5,10 @@
 #include "chrome/browser/ash/arc/input_overlay/actions/action.h"
 
 #include <memory>
-#include <utility>
 
 #include "base/check_op.h"
 #include "base/strings/string_util.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/arc/input_overlay/actions/position.h"
 #include "chrome/browser/ash/arc/input_overlay/display_overlay_controller.h"
 #include "chrome/browser/ash/arc/input_overlay/touch_id_manager.h"
@@ -104,7 +104,7 @@ void LogEvent(const ui::Event& event) {
             << ui::KeycodeConverter::DomKeyToKeyString(key_event->GetDomKey())
             << "}. DomCode{"
             << ui::KeycodeConverter::DomCodeToCodeString(key_event->code())
-            << "}. Type{" << std::to_underlying(key_event->type()) << "}. "
+            << "}. Type{" << base::to_underlying(key_event->type()) << "}. "
             << key_event->ToString();
   } else if (event.IsTouchEvent()) {
     const auto* touch_event = event.AsTouchEvent();
diff --git a/chrome/browser/ash/arc/tracing/arc_cpu_event.cc b/chrome/browser/ash/arc/tracing/arc_cpu_event.cc
index d1c5a54f58..c359c08d35 100644
--- a/chrome/browser/ash/arc/tracing/arc_cpu_event.cc
+++ b/chrome/browser/ash/arc/tracing/arc_cpu_event.cc
@@ -4,9 +4,8 @@
 
 #include "chrome/browser/ash/arc/tracing/arc_cpu_event.h"
 
-#include <utility>
-
 #include "base/logging.h"
+#include "base/types/cxx23_to_underlying.h"
 
 namespace arc {
 
@@ -202,7 +201,7 @@ bool LoadAllCpuEvents(const base::Value* value, AllCpuEvents* all_cpu_events) {
 }
 
 std::ostream& operator<<(std::ostream& os, ArcCpuEvent::Type event_type) {
-  return os << std::to_underlying(event_type);
+  return os << base::to_underlying(event_type);
 }
 
 }  // namespace arc
diff --git a/chrome/browser/ash/arc/tracing/arc_tracing_graphics_model.cc b/chrome/browser/ash/arc/tracing/arc_tracing_graphics_model.cc
index 31273ced1e..53b9495ae3 100644
--- a/chrome/browser/ash/arc/tracing/arc_tracing_graphics_model.cc
+++ b/chrome/browser/ash/arc/tracing/arc_tracing_graphics_model.cc
@@ -8,7 +8,6 @@
 
 #include <algorithm>
 #include <set>
-#include <utility>
 
 #include "base/base64.h"
 #include "base/functional/bind.h"
@@ -24,6 +23,7 @@
 #include "base/strings/stringprintf.h"
 #include "base/time/time.h"
 #include "base/trace_event/common/trace_event_common.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/arc/tracing/arc_graphics_jank_detector.h"
 #include "chrome/browser/ash/arc/tracing/arc_tracing_event.h"
 #include "chrome/browser/ash/arc/tracing/arc_tracing_event_matcher.h"
@@ -727,7 +727,7 @@ bool ArcTracingGraphicsModel::EventsContainer::operator==(
 
 std::ostream& operator<<(std::ostream& os,
                          ArcTracingGraphicsModel::EventType event_type) {
-  return os << std::to_underlying(event_type);
+  return os << base::to_underlying(event_type);
 }
 
 }  // namespace arc
diff --git a/chrome/browser/ash/base/locale_util.cc b/chrome/browser/ash/base/locale_util.cc
index 6e055ce136..2caf63391d 100644
--- a/chrome/browser/ash/base/locale_util.cc
+++ b/chrome/browser/ash/base/locale_util.cc
@@ -192,7 +192,7 @@ bool IsAllowedLanguage(const std::string& language, const PrefService* prefs) {
     return true;
 
   // Check if locale is in list of allowed UI locales.
-  return allowed_languages.contains(language);
+  return base::Contains(allowed_languages, base::Value(language));
 }
 
 bool IsAllowedUILanguage(const std::string& language,
diff --git a/chrome/browser/ash/extensions/file_manager/private_api_file_system.cc b/chrome/browser/ash/extensions/file_manager/private_api_file_system.cc
index 8deccbbb3a..96033af2fb 100644
--- a/chrome/browser/ash/extensions/file_manager/private_api_file_system.cc
+++ b/chrome/browser/ash/extensions/file_manager/private_api_file_system.cc
@@ -36,6 +36,7 @@
 #include "base/system/sys_info.h"
 #include "base/task/thread_pool.h"
 #include "base/threading/platform_thread.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/browser/ash/app_list/search/local_image_search/local_image_search_service.h"
 #include "chrome/browser/ash/app_list/search/local_image_search/local_image_search_service_factory.h"
@@ -185,7 +186,7 @@ ash::disks::FormatFileSystemType ApiFormatFileSystemToChromeEnum(
       return ash::disks::FormatFileSystemType::kNtfs;
   }
   NOTREACHED() << "Unknown format filesystem "
-               << std::to_underlying(filesystem);
+               << base::to_underlying(filesystem);
 }
 
 std::optional<file_manager::io_task::OperationType> IoTaskTypeToChromeEnum(
@@ -212,7 +213,7 @@ std::optional<file_manager::io_task::OperationType> IoTaskTypeToChromeEnum(
     case api::file_manager_private::IoTaskType::kNone:
       return {};
   }
-  NOTREACHED() << "Unknown I/O task type " << std::to_underlying(type);
+  NOTREACHED() << "Unknown I/O task type " << base::to_underlying(type);
 }
 
 extensions::api::file_manager_private::DlpLevel DlpRulesManagerLevelToApiEnum(
@@ -266,7 +267,7 @@ policy::FilesDialogType ApiPolicyDialogTypeToChromeEnum(
     case api::file_manager_private::PolicyDialogType::kError:
       return policy::FilesDialogType::kError;
   }
-  NOTREACHED() << "Unknown policy dialog type " << std::to_underlying(type);
+  NOTREACHED() << "Unknown policy dialog type " << base::to_underlying(type);
 }
 
 std::optional<policy::Policy> ApiPolicyErrorTypeToChromeEnum(
@@ -279,9 +280,9 @@ std::optional<policy::Policy> ApiPolicyErrorTypeToChromeEnum(
     case api::file_manager_private::PolicyErrorType::kNone:
       return std::nullopt;
     case api::file_manager_private::PolicyErrorType::kDlpWarningTimeout:
-      NOTREACHED() << "Unexpected policy type " << std::to_underlying(type);
+      NOTREACHED() << "Unexpected policy type " << base::to_underlying(type);
   }
-  NOTREACHED() << "Unknown policy error type " << std::to_underlying(type);
+  NOTREACHED() << "Unknown policy error type " << base::to_underlying(type);
 }
 
 // Handles a callback from the LocalImageSearchService. The job of this function
@@ -1171,7 +1172,7 @@ FileManagerPrivateGetDialogCallerFunction::Run() {
     }
     if (caller->component().has_value()) {
       info.Set("component",
-               std::to_underlying(DlpRulesManagerComponentToApiEnum(
+               base::to_underlying(DlpRulesManagerComponentToApiEnum(
                    caller->component().value())));
     }
   }
diff --git a/chrome/browser/ash/extensions/file_manager/system_notification_manager.cc b/chrome/browser/ash/extensions/file_manager/system_notification_manager.cc
index ce07d4b800..6500c6cb69 100644
--- a/chrome/browser/ash/extensions/file_manager/system_notification_manager.cc
+++ b/chrome/browser/ash/extensions/file_manager/system_notification_manager.cc
@@ -6,7 +6,6 @@
 
 #include <optional>
 #include <string>
-#include <utility>
 
 #include "ash/resources/vector_icons/vector_icons.h"
 #include "ash/webui/file_manager/file_manager_ui.h"
@@ -22,6 +21,7 @@
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/drive/file_system_util.h"
 #include "chrome/browser/ash/extensions/file_manager/drivefs_event_router.h"
 #include "chrome/browser/ash/file_manager/fileapi_util.h"
@@ -614,7 +614,7 @@ NotificationPtr SystemNotificationManager::MakeDriveSyncErrorNotification(
   }
 
   LOG(ERROR) << "Unexpected Drive sync error: "
-             << std::to_underlying(sync_error->type);
+             << base::to_underlying(sync_error->type);
   return nullptr;
 }
 
@@ -1119,7 +1119,7 @@ void SystemNotificationManager::HandleMountCompletedEvent(
     case fmp::MountCompletedEventType::kNone:
     default:
       VLOG(1) << "Unexpected mount event "
-              << std::to_underlying(event.event_type);
+              << base::to_underlying(event.event_type);
       break;
   }
 
diff --git a/chrome/browser/ash/file_manager/file_manager_browsertest_base.cc b/chrome/browser/ash/file_manager/file_manager_browsertest_base.cc
index f193477311..b75e63865b 100644
--- a/chrome/browser/ash/file_manager/file_manager_browsertest_base.cc
+++ b/chrome/browser/ash/file_manager/file_manager_browsertest_base.cc
@@ -3007,7 +3007,7 @@ void FileManagerBrowserTestBase::OnCommand(const std::string& name,
 
     content::WebContents* web_contents;
     if (app_id && !app_id->empty()) {
-      CHECK(swa_web_contents_.contains(*app_id))
+      CHECK(base::Contains(swa_web_contents_, *app_id))
           << "Couldn't find the SWA WebContents for appId: " << *app_id
           << " command data: " << *data;
       web_contents = swa_web_contents_[*app_id];
@@ -3701,7 +3701,7 @@ void FileManagerBrowserTestBase::OnCommand(const std::string& name,
     ASSERT_TRUE(app_id);
 
     content::WebContents* web_contents;
-    CHECK(swa_web_contents_.contains(*app_id))
+    CHECK(base::Contains(swa_web_contents_, *app_id))
         << "Couldn't find the SWA WebContents for appId: " << *app_id;
     web_contents = swa_web_contents_[*app_id];
 
@@ -4060,7 +4060,7 @@ void FileManagerBrowserTestBase::OnCommand(const std::string& name,
     ASSERT_TRUE(app_id);
 
     content::WebContents* web_contents;
-    CHECK(swa_web_contents_.contains(*app_id))
+    CHECK(base::Contains(swa_web_contents_, *app_id))
         << "Couldn't find the SWA WebContents for appId: " << *app_id;
     web_contents = swa_web_contents_[*app_id];
     web_contents->Focus();
diff --git a/chrome/browser/ash/file_manager/office_file_tasks.cc b/chrome/browser/ash/file_manager/office_file_tasks.cc
index e7c55dcbbd..3c181e7a5c 100644
--- a/chrome/browser/ash/file_manager/office_file_tasks.cc
+++ b/chrome/browser/ash/file_manager/office_file_tasks.cc
@@ -7,12 +7,12 @@
 #include <algorithm>
 #include <initializer_list>
 #include <string_view>
-#include <utility>
 
 #include "base/metrics/histogram_macros.h"
 #include "base/no_destructor.h"
 #include "base/notreached.h"
 #include "base/strings/string_util.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
 #include "chrome/browser/apps/app_service/app_service_proxy_factory.h"
 #include "chrome/browser/ash/drive/drive_integration_service.h"
@@ -239,7 +239,7 @@ void LaunchQuickOffice(Profile* profile,
               LOG(ERROR) << "Fallback to QuickOffice for opening office file "
                             "with error message: "
                          << error_message
-                         << " and result: " << std::to_underlying(result);
+                         << " and result: " << base::to_underlying(result);
             }
           }));
 
diff --git a/chrome/browser/ash/input_method/editor_consent_store.cc b/chrome/browser/ash/input_method/editor_consent_store.cc
index 5b64a46896..2fe3f27901 100644
--- a/chrome/browser/ash/input_method/editor_consent_store.cc
+++ b/chrome/browser/ash/input_method/editor_consent_store.cc
@@ -4,9 +4,8 @@
 
 #include "chrome/browser/ash/input_method/editor_consent_store.h"
 
-#include <utility>
-
 #include "ash/constants/ash_pref_names.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/input_method/editor_metrics_enums.h"
 #include "chrome/browser/ash/input_method/editor_metrics_recorder.h"
 #include "chromeos/ash/components/editor_menu/public/cpp/editor_consent_status.h"
@@ -30,7 +29,7 @@ EditorConsentStore::GetConsentStatus() const {
 void EditorConsentStore::SetConsentStatus(
     chromeos::editor_menu::EditorConsentStatus consent_status) {
   pref_service_->SetInteger(prefs::kOrcaConsentStatus,
-                            std::to_underlying(consent_status));
+                            base::to_underlying(consent_status));
 }
 
 void EditorConsentStore::ProcessConsentAction(ConsentAction consent_action) {
diff --git a/chrome/browser/ash/input_method/editor_switch.cc b/chrome/browser/ash/input_method/editor_switch.cc
index 2c5f4a56b1..2496191b28 100644
--- a/chrome/browser/ash/input_method/editor_switch.cc
+++ b/chrome/browser/ash/input_method/editor_switch.cc
@@ -4,8 +4,6 @@
 
 #include "chrome/browser/ash/input_method/editor_switch.h"
 
-#include <utility>
-
 #include "ash/constants/ash_features.h"
 #include "ash/constants/ash_pref_names.h"
 #include "ash/constants/generative_ai_country_restrictions.h"
@@ -14,6 +12,7 @@
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/to_vector.h"
 #include "base/json/json_reader.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/input_method/editor_consent_enums.h"
 #include "chrome/browser/ash/input_method/input_methods_by_language.h"
 #include "chrome/browser/ash/input_method/url_utils.h"
@@ -312,8 +311,8 @@ bool EditorSwitch::IsFeedbackEnabled() const {
 
   // If managed, check the enablement value.
   return profile_->GetPrefs()->GetInteger(prefs::kHmwManagedSettings) ==
-         std::to_underlying(chromeos::editor_menu::EditorEnterprisePolicy::
-                                kAllowedWithModelImprovement);
+         base::to_underlying(chromeos::editor_menu::EditorEnterprisePolicy::
+                                 kAllowedWithModelImprovement);
 }
 
 bool EditorSwitch::CanShowNoticeBanner() const {
@@ -443,7 +442,7 @@ bool EditorSwitch::CanBeTriggered() const {
          !net::NetworkChangeNotifier::IsOffline() &&
          !context_->InTabletMode() &&
          profile_->GetPrefs()->GetInteger(prefs::kHmwManagedSettings) !=
-             std::to_underlying(
+             base::to_underlying(
                  chromeos::editor_menu::EditorEnterprisePolicy::kDisallowed) &&
          // user pref value
          profile_->GetPrefs()->GetBoolean(prefs::kOrcaEnabled) &&
diff --git a/chrome/browser/ash/lobster/lobster_service.cc b/chrome/browser/ash/lobster/lobster_service.cc
index 3214a5f947..3f689f95df 100644
--- a/chrome/browser/ash/lobster/lobster_service.cc
+++ b/chrome/browser/ash/lobster/lobster_service.cc
@@ -16,6 +16,7 @@
 #include "base/check_deref.h"
 #include "base/command_line.h"
 #include "base/task/single_thread_task_runner.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/lobster/lobster_candidate_id_generator.h"
 #include "chrome/browser/ash/lobster/lobster_image_fetcher.h"
 #include "chrome/browser/ash/lobster/lobster_image_provider_from_memory.h"
@@ -78,7 +79,7 @@ LobsterService::LobsterService(
                       chromeos::editor_menu::EditorConsentStatus::kDeclined) {
                 pref_service->SetInteger(
                     ash::prefs::kOrcaConsentStatus,
-                    std::to_underlying(
+                    base::to_underlying(
                         chromeos::editor_menu::EditorConsentStatus::kUnset));
               }
             },
@@ -136,8 +137,8 @@ void LobsterService::LoadUI(std::optional<std::string> query,
       /*should_show_feedback=*/
       profile_->GetPrefs()->GetInteger(
           ash::prefs::kLobsterEnterprisePolicySettings) ==
-              std::to_underlying(ash::LobsterEnterprisePolicyValue::
-                                     kAllowedWithModelImprovement) &&
+              base::to_underlying(ash::LobsterEnterprisePolicyValue::
+                                      kAllowedWithModelImprovement) &&
           base::FeatureList::IsEnabled(ash::features::kLobsterFeedback));
 }
 
diff --git a/chrome/browser/ash/magic_boost/magic_boost_browsertest.cc b/chrome/browser/ash/magic_boost/magic_boost_browsertest.cc
index 90550ef4e6..69359398fa 100644
--- a/chrome/browser/ash/magic_boost/magic_boost_browsertest.cc
+++ b/chrome/browser/ash/magic_boost/magic_boost_browsertest.cc
@@ -4,7 +4,6 @@
 
 #include <memory>
 #include <tuple>
-#include <utility>
 
 #include "ash/constants/ash_pref_names.h"
 #include "ash/shell.h"
@@ -17,6 +16,7 @@
 #include "base/command_line.h"
 #include "base/test/bind.h"
 #include "base/test/scoped_feature_list.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/input_method/editor_consent_enums.h"
 #include "chrome/browser/ash/input_method/editor_mediator_factory.h"
 #include "chrome/browser/ash/mahi/mahi_test_util.h"
@@ -315,12 +315,12 @@ class MagicBoostBrowserTest
     // Sets the Orca consent status.
     browser()->profile()->GetPrefs()->SetInteger(
         prefs::kOrcaConsentStatus,
-        std::to_underlying(GetInitEditorConsentStatus()));
+        base::to_underlying(GetInitEditorConsentStatus()));
 
     // Sets the Hmr consent status.
     browser()->profile()->GetPrefs()->SetInteger(
         prefs::kHMRConsentStatus,
-        std::to_underlying(GetInitHmrConsentStatus()));
+        base::to_underlying(GetInitHmrConsentStatus()));
   }
 
   base::test::ScopedFeatureList feature_list_;
@@ -362,9 +362,9 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, AcceptOptInFromReadOnlyContent) {
   EXPECT_TRUE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(GetInitHmrConsentStatus()));
+            base::to_underlying(GetInitHmrConsentStatus()));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(GetInitEditorConsentStatus()));
+            base::to_underlying(GetInitEditorConsentStatus()));
 
   // Right click on the web content to show the opt in card.
   NavigateAndRightClickReadOnlyWeb();
@@ -430,17 +430,17 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, AcceptOptInFromReadOnlyContent) {
   EXPECT_TRUE(chromeos::MagicBoostState::Get()->hmr_enabled().value());
   EXPECT_TRUE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(chromeos::HMRConsentStatus::kApproved));
+            base::to_underlying(chromeos::HMRConsentStatus::kApproved));
 
   if (ShouldIncludeOrca()) {
     EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
     EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-              std::to_underlying(
+              base::to_underlying(
                   chromeos::editor_menu::EditorConsentStatus::kApproved));
   } else {
     EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
     EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-              std::to_underlying(GetInitEditorConsentStatus()));
+              base::to_underlying(GetInitEditorConsentStatus()));
   }
 
   // Not showing the Editor Menu when opt in from read only content.
@@ -471,9 +471,9 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest,
   EXPECT_TRUE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(GetInitHmrConsentStatus()));
+            base::to_underlying(GetInitHmrConsentStatus()));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(GetInitEditorConsentStatus()));
+            base::to_underlying(GetInitEditorConsentStatus()));
 
   // Right click on the web content to show the opt in card.
   NavigateAndRightClickReadOnlyWeb();
@@ -503,17 +503,17 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest,
   EXPECT_FALSE(chromeos::MagicBoostState::Get()->hmr_enabled().value());
   EXPECT_FALSE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(chromeos::HMRConsentStatus::kDeclined));
+            base::to_underlying(chromeos::HMRConsentStatus::kDeclined));
 
   if (ShouldIncludeOrca()) {
     EXPECT_FALSE(prefs->GetBoolean(prefs::kOrcaEnabled));
     EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-              std::to_underlying(
+              base::to_underlying(
                   chromeos::editor_menu::EditorConsentStatus::kDeclined));
   } else {
     EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
     EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-              std::to_underlying(GetInitEditorConsentStatus()));
+              base::to_underlying(GetInitEditorConsentStatus()));
   }
 
   // Not showing the Editor Menu when opt in from read only content.
@@ -540,9 +540,9 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest,
   EXPECT_TRUE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(GetInitHmrConsentStatus()));
+            base::to_underlying(GetInitHmrConsentStatus()));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(GetInitEditorConsentStatus()));
+            base::to_underlying(GetInitEditorConsentStatus()));
 
   // Right click on the web content to show the opt in card.
   NavigateAndRightClickReadOnlyWeb();
@@ -608,17 +608,17 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest,
   EXPECT_FALSE(chromeos::MagicBoostState::Get()->hmr_enabled().value());
   EXPECT_FALSE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(chromeos::HMRConsentStatus::kDeclined));
+            base::to_underlying(chromeos::HMRConsentStatus::kDeclined));
 
   if (ShouldIncludeOrca()) {
     EXPECT_FALSE(prefs->GetBoolean(prefs::kOrcaEnabled));
     EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-              std::to_underlying(
+              base::to_underlying(
                   chromeos::editor_menu::EditorConsentStatus::kDeclined));
   } else {
     EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
     EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-              std::to_underlying(GetInitEditorConsentStatus()));
+              base::to_underlying(GetInitEditorConsentStatus()));
   }
 
   // Not showing the Editor Menu when opt in from read only content.
@@ -661,9 +661,9 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, AcceptOptInFromInputFieldWeb) {
   EXPECT_TRUE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(GetInitHmrConsentStatus()));
+            base::to_underlying(GetInitHmrConsentStatus()));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(GetInitEditorConsentStatus()));
+            base::to_underlying(GetInitEditorConsentStatus()));
 
   // Right click on the input.
   NavigateAndRightClickInputTextWeb();
@@ -725,10 +725,10 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, AcceptOptInFromInputFieldWeb) {
   EXPECT_TRUE(chromeos::MagicBoostState::Get()->hmr_enabled().value());
   EXPECT_TRUE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(chromeos::HMRConsentStatus::kApproved));
+            base::to_underlying(chromeos::HMRConsentStatus::kApproved));
   EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(
+            base::to_underlying(
                 chromeos::editor_menu::EditorConsentStatus::kApproved));
 
   // Right click on the input again.
@@ -756,9 +756,9 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest,
   EXPECT_TRUE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(GetInitHmrConsentStatus()));
+            base::to_underlying(GetInitHmrConsentStatus()));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(GetInitEditorConsentStatus()));
+            base::to_underlying(GetInitEditorConsentStatus()));
 
   // Right click on the input.
   NavigateAndRightClickInputTextWeb();
@@ -794,10 +794,10 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest,
   EXPECT_FALSE(chromeos::MagicBoostState::Get()->hmr_enabled().value());
   EXPECT_FALSE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(chromeos::HMRConsentStatus::kDeclined));
+            base::to_underlying(chromeos::HMRConsentStatus::kDeclined));
   EXPECT_FALSE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(
+            base::to_underlying(
                 chromeos::editor_menu::EditorConsentStatus::kDeclined));
 
   // Not showing the Editor Menu after declined.
@@ -824,9 +824,9 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest,
   EXPECT_TRUE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_TRUE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(GetInitHmrConsentStatus()));
+            base::to_underlying(GetInitHmrConsentStatus()));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(GetInitEditorConsentStatus()));
+            base::to_underlying(GetInitEditorConsentStatus()));
 
   // Right click on the input.
   NavigateAndRightClickInputTextWeb();
@@ -879,10 +879,10 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest,
   EXPECT_FALSE(chromeos::MagicBoostState::Get()->hmr_enabled().value());
   EXPECT_FALSE(prefs->GetBoolean(prefs::kHmrEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kHMRConsentStatus),
-            std::to_underlying(chromeos::HMRConsentStatus::kDeclined));
+            base::to_underlying(chromeos::HMRConsentStatus::kDeclined));
   EXPECT_FALSE(prefs->GetBoolean(prefs::kOrcaEnabled));
   EXPECT_EQ(prefs->GetInteger(prefs::kOrcaConsentStatus),
-            std::to_underlying(
+            base::to_underlying(
                 chromeos::editor_menu::EditorConsentStatus::kDeclined));
 
   // Not showing the Editor Menu after declined.
@@ -929,7 +929,7 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, ShowDisclaimerViewOnMultiScreen) {
 
   if (IsMagicBoostRevampEnabled()) {
     browser()->profile()->GetPrefs()->SetInteger(
-        prefs::kHMRConsentStatus, std::to_underlying(init_hmr_status));
+        prefs::kHMRConsentStatus, base::to_underlying(init_hmr_status));
     if (!ShouldShowHmrMenuCard()) {
       EXPECT_FALSE(
           FindWidgetWithName(chromeos::mahi::MahiMenuView::GetWidgetName()));
@@ -959,7 +959,7 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, ShowDisclaimerViewOnMultiScreen) {
     // Resets the Hmr consent status to continue testing showing disclaimer view
     // on the second screen.
     browser()->profile()->GetPrefs()->SetInteger(
-        prefs::kHMRConsentStatus, std::to_underlying(init_hmr_status));
+        prefs::kHMRConsentStatus, base::to_underlying(init_hmr_status));
     browser()->window()->SetBounds(displays[0].work_area());
     event_generator().SetTargetWindow(root_windows[0]);
     NavigateToReadOnlyWeb();
@@ -982,7 +982,7 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, ShowDisclaimerViewOnMultiScreen) {
     // Resets the Hmr consent status to continue testing showing disclaimer view
     // on the third screen.
     browser()->profile()->GetPrefs()->SetInteger(
-        prefs::kHMRConsentStatus, std::to_underlying(init_hmr_status));
+        prefs::kHMRConsentStatus, base::to_underlying(init_hmr_status));
     browser()->window()->SetBounds(displays[2].work_area());
     event_generator().SetTargetWindow(root_windows[2]);
     NavigateToReadOnlyWeb();
@@ -1049,7 +1049,7 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, ShowDisclaimerViewOnMultiScreen) {
   // Resets the Hmr consent status to continue testing showing disclaimer view
   // on the first screen.
   browser()->profile()->GetPrefs()->SetInteger(
-      prefs::kHMRConsentStatus, std::to_underlying(init_hmr_status));
+      prefs::kHMRConsentStatus, base::to_underlying(init_hmr_status));
   browser()->window()->SetBounds(displays[0].work_area());
   event_generator().SetTargetWindow(root_windows[0]);
   NavigateToReadOnlyWeb();
@@ -1070,7 +1070,7 @@ IN_PROC_BROWSER_TEST_P(MagicBoostBrowserTest, ShowDisclaimerViewOnMultiScreen) {
   // Resets the Hmr consent status to continue testing showing disclaimer view
   // on the third screen.
   browser()->profile()->GetPrefs()->SetInteger(
-      prefs::kHMRConsentStatus, std::to_underlying(init_hmr_status));
+      prefs::kHMRConsentStatus, base::to_underlying(init_hmr_status));
   browser()->window()->SetBounds(displays[2].work_area());
   event_generator().SetTargetWindow(root_windows[2]);
   NavigateToReadOnlyWeb();
diff --git a/chrome/browser/ash/note_taking/note_taking_helper.cc b/chrome/browser/ash/note_taking/note_taking_helper.cc
index 30fa436f71..79c8ec32ca 100644
--- a/chrome/browser/ash/note_taking/note_taking_helper.cc
+++ b/chrome/browser/ash/note_taking/note_taking_helper.cc
@@ -96,7 +96,7 @@ bool LooksLikeAndroidPackageName(const std::string& app_id) {
   // Android package names are required to contain at least one period (see
   // validateName() in PackageParser.java), while Chrome extension IDs and web
   // app IDs contain only characters in [a-p].
-  return app_id.contains('.');
+  return base::Contains(app_id, '.');
 }
 
 bool IsInstalledApp(const std::string& app_id, Profile* profile) {
diff --git a/chrome/browser/ash/policy/reporting/metrics_reporting/cros_healthd_sampler_handlers/cros_healthd_psr_sampler_handler.cc b/chrome/browser/ash/policy/reporting/metrics_reporting/cros_healthd_sampler_handlers/cros_healthd_psr_sampler_handler.cc
index ff439cbafc..25ee2a13e2 100644
--- a/chrome/browser/ash/policy/reporting/metrics_reporting/cros_healthd_sampler_handlers/cros_healthd_psr_sampler_handler.cc
+++ b/chrome/browser/ash/policy/reporting/metrics_reporting/cros_healthd_sampler_handlers/cros_healthd_psr_sampler_handler.cc
@@ -12,6 +12,7 @@
 #include "base/metrics/histogram_functions.h"
 #include "base/task/task_runner.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chromeos/ash/services/cros_healthd/public/cpp/service_connection.h"
 #include "components/reporting/metrics/sampler.h"
 #include "components/reporting/proto/synced/metric_data.pb.h"
@@ -108,7 +109,7 @@ void CrosHealthdPsrSamplerHandler::HandleResultImpl(
       default:
         RecordPsrResult(EnterpriseReportingPsrResult::kUnknownSystemResultType);
         LOG(ERROR) << "cros_healthd: Unknown system result type: "
-                   << std::to_underlying(system_result->which());
+                   << base::to_underlying(system_result->which());
         return;
     }
   }
diff --git a/chrome/browser/ash/policy/reporting/metrics_reporting/metric_reporting_manager.cc b/chrome/browser/ash/policy/reporting/metrics_reporting/metric_reporting_manager.cc
index 0b952b2876..4f2128b910 100644
--- a/chrome/browser/ash/policy/reporting/metrics_reporting/metric_reporting_manager.cc
+++ b/chrome/browser/ash/policy/reporting/metrics_reporting/metric_reporting_manager.cc
@@ -228,7 +228,7 @@ MetricReportingManager::GetTelemetryCollectors(MetricEventType event_type) {
           ::ash::kReportDeviceSignalStrengthEventDrivenTelemetry);
     case USB_ADDED:
     case USB_REMOVED:
-      if (telemetry_collectors_.contains(kDelayedPeripheralTelemetry)) {
+      if (base::Contains(telemetry_collectors_, kDelayedPeripheralTelemetry)) {
         return {telemetry_collectors_.at(kDelayedPeripheralTelemetry).get()};
       }
       // Return statement or `ABSL_FALLTHROUGH_INTENDED` is necessary to silence
@@ -433,7 +433,7 @@ void MetricReportingManager::InitOneShotTelemetryCollector(
     bool enable_default_value,
     base::TimeDelta init_delay) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  CHECK(!telemetry_collectors_.contains(collector_name));
+  CHECK(!base::Contains(telemetry_collectors_, collector_name));
   if (!metric_report_queue) {
     return;
   }
@@ -451,7 +451,7 @@ void MetricReportingManager::InitManualTelemetryCollector(
     const std::string& enable_setting_path,
     bool enable_default_value) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  CHECK(!telemetry_collectors_.contains(collector_name));
+  CHECK(!base::Contains(telemetry_collectors_, collector_name));
   if (!metric_report_queue) {
     return;
   }
@@ -473,7 +473,7 @@ void MetricReportingManager::InitPeriodicTelemetryCollector(
     int rate_unit_to_ms,
     base::TimeDelta init_delay) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  CHECK(!telemetry_collectors_.contains(collector_name));
+  CHECK(!base::Contains(telemetry_collectors_, collector_name));
   if (!metric_report_queue) {
     return;
   }
@@ -598,7 +598,7 @@ void MetricReportingManager::InitNetworkPeriodicCollector(
 
 void MetricReportingManager::InitAppCollectors(Profile* profile) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  if (telemetry_collectors_.contains(kAppTelemetry) ||
+  if (base::Contains(telemetry_collectors_, kAppTelemetry) ||
       !user_event_report_queue_ || !user_reporting_settings_ ||
       !user_telemetry_report_queue_) {
     return;
diff --git a/chrome/browser/ash/preferences/preferences.cc b/chrome/browser/ash/preferences/preferences.cc
index 1cfa766053..6ca992374d 100644
--- a/chrome/browser/ash/preferences/preferences.cc
+++ b/chrome/browser/ash/preferences/preferences.cc
@@ -7,7 +7,6 @@
 #include <limits>
 #include <memory>
 #include <optional>
-#include <utility>
 #include <vector>
 
 #include "ash/birch/coral_util.h"
@@ -33,6 +32,7 @@
 #include "base/strings/utf_string_conversions.h"
 #include "base/system/sys_info.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/accessibility/magnification_manager.h"
 #include "chrome/browser/ash/base/locale_util.h"
 #include "chrome/browser/ash/child_accounts/parent_access_code/parent_access_service.h"
@@ -285,15 +285,15 @@ void Preferences::RegisterProfilePrefs(
   registry->RegisterBooleanPref(prefs::kEmojiSuggestionEnterpriseAllowed, true);
   registry->RegisterIntegerPref(
       prefs::kHmwManagedSettings,
-      std::to_underlying(chromeos::editor_menu::EditorEnterprisePolicy::
-                             kAllowedWithModelImprovement));
+      base::to_underlying(chromeos::editor_menu::EditorEnterprisePolicy::
+                              kAllowedWithModelImprovement));
   registry->RegisterBooleanPref(prefs::kOrcaEnabled, true);
   registry->RegisterBooleanPref(prefs::kOrcaFeedbackEnabled, true);
   registry->RegisterBooleanPref(prefs::kManagedOrcaEnabled, true);
   registry->RegisterBooleanPref(prefs::kLobsterEnabled, true);
   registry->RegisterIntegerPref(
       prefs::kLobsterEnterprisePolicySettings,
-      std::to_underlying(
+      base::to_underlying(
           ash::LobsterEnterprisePolicyValue::kAllowedWithModelImprovement));
   registry->RegisterBooleanPref(
       prefs::kManagedPhysicalKeyboardAutocorrectAllowed, true);
@@ -301,7 +301,7 @@ void Preferences::RegisterProfilePrefs(
       prefs::kManagedPhysicalKeyboardPredictiveWritingAllowed, true);
   registry->RegisterIntegerPref(
       prefs::kOrcaConsentStatus,
-      std::to_underlying(chromeos::editor_menu::EditorConsentStatus::kUnset));
+      base::to_underlying(chromeos::editor_menu::EditorConsentStatus::kUnset));
   registry->RegisterIntegerPref(prefs::kOrcaConsentWindowDismissCount, 0);
   registry->RegisterBooleanPref(prefs::kEmojiPickerGifSupportEnabled, true);
   registry->RegisterDictionaryPref(prefs::kEmojiPickerHistory);
@@ -598,7 +598,7 @@ void Preferences::RegisterProfilePrefs(
 
   registry->RegisterIntegerPref(
       prefs::kHMRConsentStatus,
-      std::to_underlying(chromeos::HMRConsentStatus::kUnset));
+      base::to_underlying(chromeos::HMRConsentStatus::kUnset));
 
   registry->RegisterIntegerPref(prefs::kHMRConsentWindowDismissCount, 0);
 
@@ -606,7 +606,7 @@ void Preferences::RegisterProfilePrefs(
 
   registry->RegisterIntegerPref(
       prefs::kGenAISmartGroupingSettings,
-      std::to_underlying(coral_util::GenAISmartGroupingSettings::kAllowed));
+      base::to_underlying(coral_util::GenAISmartGroupingSettings::kAllowed));
 
   registry->RegisterBooleanPref(
       prefs::kLauncherResultEverLaunched, false,
diff --git a/chrome/browser/ash/printing/automatic_usb_printer_configurer.h b/chrome/browser/ash/printing/automatic_usb_printer_configurer.h
index e72adca349..c9bf55d418 100644
--- a/chrome/browser/ash/printing/automatic_usb_printer_configurer.h
+++ b/chrome/browser/ash/printing/automatic_usb_printer_configurer.h
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/functional/callback.h"
diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index 7ca1e306d9..132a450542 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -8354,7 +8354,7 @@ bool ChromeContentBrowserClient::
 #if BUILDFLAG(IS_MAC)
 std::string ChromeContentBrowserClient::GetChildProcessSuffix(int child_flags) {
   if (child_flags ==
-      std::to_underlying(ChildProcessHostFlags::kChildProcessHelperAlerts)) {
+      base::to_underlying(ChildProcessHostFlags::kChildProcessHelperAlerts)) {
     return chrome::kMacHelperSuffixAlerts;
   }
   NOTREACHED() << "Unsupported child process flags!";
diff --git a/chrome/browser/chromeos/app_mode/kiosk_app_service_launcher.cc b/chrome/browser/chromeos/app_mode/kiosk_app_service_launcher.cc
index 1ea464f9a4..0c81a19e35 100644
--- a/chrome/browser/chromeos/app_mode/kiosk_app_service_launcher.cc
+++ b/chrome/browser/chromeos/app_mode/kiosk_app_service_launcher.cc
@@ -13,6 +13,7 @@
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/syslog_logging.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/buildflag.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
 #include "chrome/browser/apps/app_service/app_service_proxy_factory.h"
@@ -55,7 +56,7 @@ void KioskAppServiceLauncher::CheckAndMaybeLaunchApp(
     case apps::Readiness::kUnknown:
     case apps::Readiness::kTerminated:
       SYSLOG(WARNING) << "Kiosk app not ready yet: "
-                      << std::to_underlying(readiness);
+                      << base::to_underlying(readiness);
       app_registry_observation_.Observe(&app_service_->AppRegistryCache());
       break;
     case apps::Readiness::kReady:
@@ -69,7 +70,7 @@ void KioskAppServiceLauncher::CheckAndMaybeLaunchApp(
     case apps::Readiness::kUninstalledByNonUser:
     case apps::Readiness::kDisabledByLocalSettings:
       SYSLOG(ERROR) << "Kiosk app should not have readiness "
-                    << std::to_underlying(readiness);
+                    << base::to_underlying(readiness);
       if (!app_launched_callback_.is_null()) {
         std::move(app_launched_callback_).Run(false);
       }
diff --git a/chrome/browser/chromeos/extensions/login_screen/login/cleanup/extension_cleanup_handler.cc b/chrome/browser/chromeos/extensions/login_screen/login/cleanup/extension_cleanup_handler.cc
index 24b8fccc26..4362ec48d7 100644
--- a/chrome/browser/chromeos/extensions/login_screen/login/cleanup/extension_cleanup_handler.cc
+++ b/chrome/browser/chromeos/extensions/login_screen/login/cleanup/extension_cleanup_handler.cc
@@ -76,7 +76,7 @@ void ExtensionCleanupHandler::UninstallExtensions() {
     }
 
     // Skip extensions exempt by policy.
-    if (policy_exempt_extensions.contains(extension_id)) {
+    if (base::Contains(policy_exempt_extensions, extension_id)) {
       continue;
     }
 
diff --git a/chrome/browser/chromeos/extensions/vpn_provider/vpn_service.cc b/chrome/browser/chromeos/extensions/vpn_provider/vpn_service.cc
index 1fe28d3e7c..50eb1c7d27 100644
--- a/chrome/browser/chromeos/extensions/vpn_provider/vpn_service.cc
+++ b/chrome/browser/chromeos/extensions/vpn_provider/vpn_service.cc
@@ -13,6 +13,7 @@
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
 #include "base/task/sequenced_task_runner_helpers.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/uuid.h"
 #include "base/values.h"
 #include "chrome/browser/ash/profiles/profile_helper.h"
@@ -100,12 +101,12 @@ void VpnService::VpnConfiguration::OnPlatformMessage(
             platform_message);
 
   if (platform_message ==
-      std::to_underlying(api_vpn::PlatformMessage::kConnected)) {
+      base::to_underlying(api_vpn::PlatformMessage::kConnected)) {
     vpn_service_->SetActiveConfiguration(this);
   } else if (platform_message ==
-                 std::to_underlying(api_vpn::PlatformMessage::kDisconnected) ||
+                 base::to_underlying(api_vpn::PlatformMessage::kDisconnected) ||
              platform_message ==
-                 std::to_underlying(api_vpn::PlatformMessage::kError)) {
+                 base::to_underlying(api_vpn::PlatformMessage::kError)) {
     vpn_service_->SetActiveConfiguration(nullptr);
   }
 
@@ -358,7 +359,7 @@ void VpnService::DestroyConfiguration(const std::string& extension_id,
 
   if (active_configuration_ == configuration) {
     configuration->OnPlatformMessage(
-        std::to_underlying(api_vpn::PlatformMessage::kDisconnected));
+        base::to_underlying(api_vpn::PlatformMessage::kDisconnected));
   }
 
   DestroyConfigurationInternal(configuration);
@@ -459,9 +460,9 @@ void VpnService::NotifyConnectionStateChanged(const std::string& extension_id,
   ash::ShillThirdPartyVpnDriverClient::Get()->UpdateConnectionState(
       active_configuration->object_path(),
       connection_success
-          ? std::to_underlying(
+          ? base::to_underlying(
                 extensions::api::vpn_provider::VpnConnectionState::kConnected)
-          : std::to_underlying(
+          : base::to_underlying(
                 extensions::api::vpn_provider::VpnConnectionState::kFailure),
       std::move(success), std::move(failure));
 }
@@ -481,7 +482,7 @@ void VpnService::OnExtensionUnloaded(
   if (active_configuration) {
     ash::ShillThirdPartyVpnDriverClient::Get()->UpdateConnectionState(
         active_configuration->object_path(),
-        std::to_underlying(api_vpn::VpnConnectionState::kFailure),
+        base::to_underlying(api_vpn::VpnConnectionState::kFailure),
         base::DoNothing(), base::DoNothing());
   }
   if (reason == extensions::UnloadedExtensionReason::DISABLE ||
diff --git a/chrome/browser/component_updater/iwa_key_distribution_component_installer.cc b/chrome/browser/component_updater/iwa_key_distribution_component_installer.cc
index 1a6080a7f2..72e3c99d1e 100644
--- a/chrome/browser/component_updater/iwa_key_distribution_component_installer.cc
+++ b/chrome/browser/component_updater/iwa_key_distribution_component_installer.cc
@@ -20,6 +20,7 @@
 #include "base/logging.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/task/task_traits.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/pass_key.h"
 #include "base/values.h"
 #include "base/version.h"
@@ -52,7 +53,7 @@ void OnDemandUpdateCompleted(update_client::Error err) {
   VLOG(1) << "On-demand update for the "
              "Iwa Key Distribution Component "
              "finished with result "
-          << std::to_underlying(err);
+          << base::to_underlying(err);
 }
 
 component_updater::OnDemandUpdater::Priority GetOnDemandUpdatePriority() {
diff --git a/chrome/browser/compose/proactive_nudge_tracker.cc b/chrome/browser/compose/proactive_nudge_tracker.cc
index e3deb0e1f4..f2e01e7d87 100644
--- a/chrome/browser/compose/proactive_nudge_tracker.cc
+++ b/chrome/browser/compose/proactive_nudge_tracker.cc
@@ -5,7 +5,6 @@
 #include "chrome/browser/compose/proactive_nudge_tracker.h"
 
 #include <memory>
-#include <utility>
 
 #include "base/functional/callback_helpers.h"
 #include "base/memory/scoped_refptr.h"
@@ -14,6 +13,7 @@
 #include "base/rand_util.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/compose/proto/compose_optimization_guide.pb.h"
 #include "components/autofill/content/browser/content_autofill_client.h"
 #include "components/autofill/core/common/signatures.h"
@@ -46,7 +46,7 @@ scoped_refptr<segmentation_platform::InputContext> PopulateInputContextForField(
       "field_height_pixels",
       ProcessedValue::FromFloat(signals.field.bounds().height()));
   input_context->metadata_args.emplace(
-      "field_form_control_type", ProcessedValue::FromFloat(std::to_underlying(
+      "field_form_control_type", ProcessedValue::FromFloat(base::to_underlying(
                                      signals.field.form_control_type())));
   input_context->metadata_args.emplace(
       "total_field_count",
diff --git a/chrome/browser/content_settings/generated_javascript_optimizer_pref.cc b/chrome/browser/content_settings/generated_javascript_optimizer_pref.cc
index b1122bf865..91d906f900 100644
--- a/chrome/browser/content_settings/generated_javascript_optimizer_pref.cc
+++ b/chrome/browser/content_settings/generated_javascript_optimizer_pref.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/content_settings/host_content_settings_map_factory.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/site_protection/site_familiarity_utils.h"
diff --git a/chrome/browser/contextual_tasks/contextual_tasks_context_service.cc b/chrome/browser/contextual_tasks/contextual_tasks_context_service.cc
index e6d5ee9b70..1bdd7566a9 100644
--- a/chrome/browser/contextual_tasks/contextual_tasks_context_service.cc
+++ b/chrome/browser/contextual_tasks/contextual_tasks_context_service.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/stl_util.h"
 #include "base/strings/stringprintf.h"
@@ -408,7 +409,7 @@ ContextualTasksContextService::SelectRelevantTabs(
       relevant_tabs.push_back(tab_signals.web_contents);
     }
 
-    tab_context->set_was_explicitly_chosen(std::ranges::contains(
+    tab_context->set_was_explicitly_chosen(base::Contains(
         explicit_urls, web_contents->GetLastCommittedURL()));
 
     base::UmaHistogramSparse("ContextualTasks.Context.TabScore",
diff --git a/chrome/browser/contextual_tasks/contextual_tasks_url_loader_factory_interceptor.cc b/chrome/browser/contextual_tasks/contextual_tasks_url_loader_factory_interceptor.cc
index 07868adcc0..5c7f887243 100644
--- a/chrome/browser/contextual_tasks/contextual_tasks_url_loader_factory_interceptor.cc
+++ b/chrome/browser/contextual_tasks/contextual_tasks_url_loader_factory_interceptor.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/contextual_tasks/contextual_tasks_url_loader_factory_interceptor.h"
 
+#include "base/containers/contains.h"
 #include "base/memory/weak_ptr.h"
 #include "base/task/sequenced_task_runner.h"
 #include "chrome/browser/contextual_tasks/contextual_tasks_ui_service.h"
@@ -50,7 +51,7 @@ const char kOneGoogleIdentifier[] = "onegoogle";
 bool ShouldAddAuthHeader(const GURL& url) {
   // Don't add the Authorization header to OGB URLs, since they don't support
   // OAuth. Attaching an OAuth token can result in 403 errors.
-  if (url.spec().contains(kOneGoogleIdentifier)) {
+  if (base::Contains(url.spec(), kOneGoogleIdentifier)) {
     return false;
   }
 
diff --git a/chrome/browser/device_notifications/device_connection_tracker.cc b/chrome/browser/device_notifications/device_connection_tracker.cc
index e7f99c5cb6..82820d9245 100644
--- a/chrome/browser/device_notifications/device_connection_tracker.cc
+++ b/chrome/browser/device_notifications/device_connection_tracker.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "build/build_config.h"
 #include "chrome/browser/device_notifications/device_system_tray_icon.h"
@@ -53,7 +54,7 @@ DeviceConnectionTracker::~DeviceConnectionTracker() {
 
 void DeviceConnectionTracker::IncrementConnectionCount(
     const url::Origin& origin) {
-  if (std::ranges::contains(whitelisted_origins_, origin)) {
+  if (base::Contains(whitelisted_origins_, origin)) {
     return;
   }
   bool to_stage_profile = origins_.empty();
@@ -80,7 +81,7 @@ void DeviceConnectionTracker::IncrementConnectionCount(
 
 void DeviceConnectionTracker::DecrementConnectionCount(
     const url::Origin& origin) {
-  if (std::ranges::contains(whitelisted_origins_, origin)) {
+  if (base::Contains(whitelisted_origins_, origin)) {
     return;
   }
   auto it = origins_.find(origin);
diff --git a/chrome/browser/devtools/device/tcp_device_provider.cc b/chrome/browser/devtools/device/tcp_device_provider.cc
index 54e004865e..ffeea6c0ab 100644
--- a/chrome/browser/devtools/device/tcp_device_provider.cc
+++ b/chrome/browser/devtools/device/tcp_device_provider.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -119,7 +120,7 @@ void TCPDeviceProvider::QueryDevices(SerialsCallback callback) {
     const std::pair<std::string,
                     AndroidDeviceManager::DeviceInfo::ConnectedState>
         host = {target.host(), AndroidDeviceManager::DeviceInfo::kUnknown};
-    if (std::ranges::contains(result, host)) {
+    if (base::Contains(result, host)) {
       continue;
     }
     result.push_back(host);
diff --git a/chrome/browser/devtools/device/usb/android_usb_device.cc b/chrome/browser/devtools/device/usb/android_usb_device.cc
index 84ab7743c6..f483e02b58 100644
--- a/chrome/browser/devtools/device/usb/android_usb_device.cc
+++ b/chrome/browser/devtools/device/usb/android_usb_device.cc
@@ -12,6 +12,7 @@
 #include "base/barrier_closure.h"
 #include "base/base64.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ref_counted_memory.h"
 #include "base/no_destructor.h"
@@ -175,7 +176,7 @@ void OpenAndroidDevices(crypto::keypair::PrivateKey rsa_key,
       base::BindOnce(&OnProbeFinished, std::move(callback), devices));
 
   for (const auto& device_info : device_info_list) {
-    if (std::ranges::contains(GetOpenDevices(), device_info.guid)) {
+    if (base::Contains(GetOpenDevices(), device_info.guid)) {
       // This device is already open, do not make parallel attempts to connect
       // to it.
       barrier.Run();
diff --git a/chrome/browser/download/notification/multi_profile_download_notifier.cc b/chrome/browser/download/notification/multi_profile_download_notifier.cc
index 717f377fc0..a1fb418e06 100644
--- a/chrome/browser/download/notification/multi_profile_download_notifier.cc
+++ b/chrome/browser/download/notification/multi_profile_download_notifier.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/task/sequenced_task_runner.h"
 #include "chrome/browser/profiles/profile_selections.h"
@@ -37,7 +38,7 @@ void MultiProfileDownloadNotifier::AddProfile(Profile* profile) {
   }
 
   content::DownloadManager* manager = profile->GetDownloadManager();
-  if (std::ranges::contains(download_notifiers_, manager,
+  if (base::Contains(download_notifiers_, manager,
                             &download::AllDownloadItemNotifier::GetManager)) {
     return;
   }
diff --git a/chrome/browser/extensions/api/autofill_private/autofill_ai_util.cc b/chrome/browser/extensions/api/autofill_private/autofill_ai_util.cc
index 34ed4e392b..649686c865 100644
--- a/chrome/browser/extensions/api/autofill_private/autofill_ai_util.cc
+++ b/chrome/browser/extensions/api/autofill_private/autofill_ai_util.cc
@@ -7,7 +7,6 @@
 #include <optional>
 #include <string>
 #include <string_view>
-#include <utility>
 
 #include "base/containers/flat_set.h"
 #include "base/containers/to_vector.h"
@@ -17,6 +16,7 @@
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/common/extensions/api/autofill_private.h"
 #include "components/autofill/core/browser/autofill_field.h"
@@ -92,7 +92,7 @@ void EntityInstanceToPrivateApiEntityInstanceWithLabels(
 
     const EntityType entity_type = entity_instance.type();
     entity_instance_with_labels.type.type_name =
-        std::to_underlying(entity_type.name());
+        base::to_underlying(entity_type.name());
     entity_instance_with_labels.type.type_name_as_string =
         base::UTF16ToUTF8(entity_type.GetNameForI18n());
     entity_instance_with_labels.type.add_entity_type_string =
@@ -141,7 +141,7 @@ std::optional<EntityInstance> PrivateApiEntityInstanceToEntityInstance(
        private_api_entity_instance.attribute_instances) {
     if (private_api_attribute_instance.type.type_name < 0 ||
         private_api_attribute_instance.type.type_name >
-            std::to_underlying(AttributeTypeName::kMaxValue)) {
+            base::to_underlying(AttributeTypeName::kMaxValue)) {
       return std::nullopt;
     }
 
@@ -227,7 +227,7 @@ autofill_private::EntityInstance EntityInstanceToPrivateApiEntityInstance(
        entity_instance.attributes()) {
     private_api_attribute_instances.emplace_back();
     private_api_attribute_instances.back().type.type_name =
-        std::to_underlying(attribute_instance.type().name());
+        base::to_underlying(attribute_instance.type().name());
     private_api_attribute_instances.back().type.type_name_as_string =
         base::UTF16ToUTF8(attribute_instance.type().GetNameForI18n());
 
@@ -271,7 +271,7 @@ autofill_private::EntityInstance EntityInstanceToPrivateApiEntityInstance(
 
   autofill_private::EntityInstance private_api_entity_instance;
   private_api_entity_instance.type.type_name =
-      std::to_underlying(entity_instance.type().name());
+      base::to_underlying(entity_instance.type().name());
   private_api_entity_instance.type.type_name_as_string =
       base::UTF16ToUTF8(entity_instance.type().GetNameForI18n());
   private_api_entity_instance.type.add_entity_type_string =
@@ -319,7 +319,7 @@ api::autofill_private::EntityType EntityTypeToPrivateApiEntityType(
     EntityType entity_type,
     bool supports_wallet_storage) {
   autofill_private::EntityType api_type;
-  api_type.type_name = std::to_underlying(entity_type.name());
+  api_type.type_name = base::to_underlying(entity_type.name());
   api_type.type_name_as_string =
       base::UTF16ToUTF8(entity_type.GetNameForI18n());
   api_type.add_entity_type_string =
@@ -335,7 +335,7 @@ api::autofill_private::EntityType EntityTypeToPrivateApiEntityType(
 api::autofill_private::AttributeType AttributeTypeToPrivateApiAttributeType(
     autofill::AttributeType attribute_type) {
   api::autofill_private::AttributeType api_attr;
-  api_attr.type_name = std::to_underlying(attribute_type.name());
+  api_attr.type_name = base::to_underlying(attribute_type.name());
   api_attr.type_name_as_string =
       base::UTF16ToUTF8(attribute_type.GetNameForI18n());
   api_attr.data_type = AttributeTypeDataTypeToPrivateApiAttributeTypeDataType(
diff --git a/chrome/browser/extensions/api/bookmark_manager_private/bookmark_manager_private_api.cc b/chrome/browser/extensions/api/bookmark_manager_private/bookmark_manager_private_api.cc
index cfda67872e..202952302f 100644
--- a/chrome/browser/extensions/api/bookmark_manager_private/bookmark_manager_private_api.cc
+++ b/chrome/browser/extensions/api/bookmark_manager_private/bookmark_manager_private_api.cc
@@ -12,6 +12,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/i18n/file_util_icu.h"
 #include "base/i18n/time_formatting.h"
 #include "base/lazy_instance.h"
@@ -760,7 +761,7 @@ BookmarkManagerPrivateOpenInNewWindowFunction::RunOnReady() {
   std::vector<UrlAndId> url_and_ids;
   urls.reserve(nodes.size());
   for (const bookmarks::BookmarkNode* node : nodes) {
-    if (!std::ranges::contains(urls, node->url())) {
+    if (!base::Contains(urls, node->url())) {
       continue;  // The URL was filtered out; ignore this node.
     }
     UrlAndId url_and_id;
diff --git a/chrome/browser/extensions/api/declarative_net_request/declarative_net_request_browsertest.cc b/chrome/browser/extensions/api/declarative_net_request/declarative_net_request_browsertest.cc
index 6548b4cbd7..954da2e12b 100644
--- a/chrome/browser/extensions/api/declarative_net_request/declarative_net_request_browsertest.cc
+++ b/chrome/browser/extensions/api/declarative_net_request/declarative_net_request_browsertest.cc
@@ -2633,8 +2633,9 @@ IN_PROC_BROWSER_TEST_P(DeclarativeNetRequestBrowserTest,
       content::EvalJs(GetActiveWebContents(), "document.body.textContent")
           .ExtractString();
 
-  EXPECT_TRUE(body.contains("This page has been blocked by an extension"));
-  EXPECT_TRUE(body.contains("Try disabling your extensions."));
+  EXPECT_TRUE(
+      base::Contains(body, "This page has been blocked by an extension"));
+  EXPECT_TRUE(base::Contains(body, "Try disabling your extensions."));
 }
 
 // Test an extension with multiple static rulesets.
@@ -3355,9 +3356,9 @@ IN_PROC_BROWSER_TEST_P(DeclarativeNetRequestBrowserTest,
     std::map<GURL, net::test_server::HttpRequest> seen_requests =
         GetAndResetRequestsToServer();
     EXPECT_EQ(!expect_script_redirected,
-              seen_requests.contains(requested_script_url));
+              base::Contains(seen_requests, requested_script_url));
     EXPECT_EQ(expect_script_redirected,
-              seen_requests.contains(redirected_script_url));
+              base::Contains(seen_requests, redirected_script_url));
 
     ExtensionActionRunner* runner =
         ExtensionActionRunner::GetForWebContents(GetActiveWebContents());
@@ -5580,14 +5581,14 @@ class DeclarativeNetRequestAllowAllRequestsBrowserTest
 
     for (const auto& path : paths_seen) {
       GURL expected_request_url = embedded_test_server()->GetURL(path);
-      EXPECT_TRUE(requests_seen.contains(expected_request_url))
+      EXPECT_TRUE(base::Contains(requests_seen, expected_request_url))
           << expected_request_url.spec()
           << " was not requested from the server.";
     }
 
     for (const auto& path : paths_not_seen) {
       GURL expected_request_url = embedded_test_server()->GetURL(path);
-      EXPECT_FALSE(requests_seen.contains(expected_request_url))
+      EXPECT_FALSE(base::Contains(requests_seen, expected_request_url))
           << expected_request_url.spec() << " request seen unexpectedly.";
     }
   }
diff --git a/chrome/browser/extensions/api/developer_private/developer_private_functions.cc b/chrome/browser/extensions/api/developer_private/developer_private_functions.cc
index 1c49881731..e2439ba899 100644
--- a/chrome/browser/extensions/api/developer_private/developer_private_functions.cc
+++ b/chrome/browser/extensions/api/developer_private/developer_private_functions.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/files/file_util.h"
 #include "base/memory/ref_counted.h"
@@ -245,7 +246,7 @@ void AddSiteToSiteGroups(
   if (inserted) {
     it->second.etld_plus_one = etld_plus_one;
     it->second.sites.push_back(CreateSiteInfo(site, site_set));
-  } else if (!std::ranges::contains(it->second.sites, site,
+  } else if (!base::Contains(it->second.sites, site,
                                     &developer::SiteInfo::site)) {
     it->second.sites.push_back(CreateSiteInfo(site, site_set));
   }
diff --git a/chrome/browser/extensions/api/identity/identity_api.cc b/chrome/browser/extensions/api/identity/identity_api.cc
index 97feeb8af1..e8dda04d5e 100644
--- a/chrome/browser/extensions/api/identity/identity_api.cc
+++ b/chrome/browser/extensions/api/identity/identity_api.cc
@@ -13,6 +13,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/lazy_instance.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/trace_event/trace_event.h"
@@ -105,7 +106,7 @@ void IdentityAPI::EraseStaleGaiaIdsForAllExtensions() {
     std::optional<GaiaId> gaia_id = GetGaiaIdForExtension(extension_id);
     if (!gaia_id)
       continue;
-    if (!std::ranges::contains(accounts, *gaia_id, &CoreAccountInfo::gaia)) {
+    if (!base::Contains(accounts, *gaia_id, &CoreAccountInfo::gaia)) {
       EraseGaiaIdForExtension(extension_id);
     }
   }
diff --git a/chrome/browser/extensions/api/identity/identity_launch_web_auth_flow_function.cc b/chrome/browser/extensions/api/identity/identity_launch_web_auth_flow_function.cc
index 331dc6dd57..55f3518483 100644
--- a/chrome/browser/extensions/api/identity/identity_launch_web_auth_flow_function.cc
+++ b/chrome/browser/extensions/api/identity/identity_launch_web_auth_flow_function.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/stringprintf.h"
 #include "base/time/time.h"
@@ -238,7 +239,7 @@ void IdentityLaunchWebAuthFlowFunction::OnAuthFlowFailure(
 
 void IdentityLaunchWebAuthFlowFunction::OnAuthFlowURLChange(
     const GURL& redirect_url) {
-  if (!std::ranges::contains(final_url_domains_, redirect_url.Resolve("/"))) {
+  if (!base::Contains(final_url_domains_, redirect_url.Resolve("/"))) {
     return;
   }
   RecordHistogramFunctionResult(
diff --git a/chrome/browser/extensions/api/language_settings_private/language_settings_private_api.cc b/chrome/browser/extensions/api/language_settings_private/language_settings_private_api.cc
index 70f069ab39..5b49b979da 100644
--- a/chrome/browser/extensions/api/language_settings_private/language_settings_private_api.cc
+++ b/chrome/browser/extensions/api/language_settings_private/language_settings_private_api.cc
@@ -114,7 +114,7 @@ std::vector<std::string> GetSortedComponentIMEs(
     // Append the enabled ones to the new list. Also remove them from the set
     // so they aren't duplicated for other languages.
     for (const auto& input_method_id : input_method_ids) {
-      if (available_component_imes.contains(input_method_id)) {
+      if (base::Contains(available_component_imes, input_method_id)) {
         component_ime_list.push_back(input_method_id);
         available_component_imes.erase(input_method_id);
       }
@@ -170,7 +170,7 @@ std::vector<std::string> GetSortedThirdPartyIMEs(
   // Add the rest of the third party IMEs
   for (const InputMethodDescriptor& descriptor : descriptors) {
     const std::string& id = descriptor.id();
-    if (!ime_set.contains(id)) {
+    if (!base::Contains(ime_set, id)) {
       ime_list.push_back(id);
     }
   }
@@ -239,7 +239,7 @@ LanguageSettingsPrivateGetLanguageListFunction::Run() {
     language.native_display_name = entry.native_display_name;
 
     // Set optional fields only if they differ from the default.
-    if (spellcheck_language_set.contains(entry.code)) {
+    if (base::Contains(spellcheck_language_set, entry.code)) {
       language.supports_spellcheck = true;
     }
     if (entry.supports_translate) {
@@ -251,7 +251,7 @@ LanguageSettingsPrivateGetLanguageListFunction::Run() {
     }
 #if BUILDFLAG(IS_CHROMEOS)
     if (!allowed_ui_locales.empty() &&
-        !allowed_ui_locales.contains(language.code)) {
+        !base::Contains(allowed_ui_locales, language.code)) {
       language.is_prohibited_language = true;
     }
 #endif  // BUILDFLAG(IS_CHROMEOS)
@@ -716,12 +716,12 @@ void PopulateInputMethodListFromDescriptors(
     input_method.display_name = util->GetLocalizedDisplayName(descriptor);
     input_method.language_codes = descriptor.language_codes();
     input_method.tags = GetInputMethodTags(&input_method);
-    if (enabled_ids.contains(input_method.id)) {
+    if (base::Contains(enabled_ids, input_method.id)) {
       input_method.enabled = true;
     }
     if (descriptor.options_page_url().is_valid())
       input_method.has_options_page = true;
-    if (!allowed_ids.empty() && !allowed_ids.contains(input_method.id)) {
+    if (!allowed_ids.empty() && !base::Contains(allowed_ids, input_method.id)) {
       input_method.is_prohibited_by_policy = true;
     }
     input_map[base::UTF8ToUTF16(util->GetLocalizedDisplayName(descriptor))] =
diff --git a/chrome/browser/extensions/api/vpn_provider/vpn_provider_apitest.cc b/chrome/browser/extensions/api/vpn_provider/vpn_provider_apitest.cc
index 832dccd679..22a7937726 100644
--- a/chrome/browser/extensions/api/vpn_provider/vpn_provider_apitest.cc
+++ b/chrome/browser/extensions/api/vpn_provider/vpn_provider_apitest.cc
@@ -18,6 +18,7 @@
 #include "base/functional/callback_helpers.h"
 #include "base/memory/raw_ptr.h"
 #include "base/test/test_future.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/profiles/profile_helper.h"
 #include "chrome/browser/chromeos/extensions/vpn_provider/vpn_provider_api.h"
 #include "chrome/browser/chromeos/extensions/vpn_provider/vpn_service.h"
@@ -184,7 +185,7 @@ class VpnProviderApiTest : public VpnProviderApiTestBase {
                          api_vpn::PlatformMessage message) override {
     test_client_->OnPlatformMessage(
         shill::kObjectPathBase + GetKey(configuration_name),
-        std::to_underlying(message));
+        base::to_underlying(message));
   }
   void OnPacketReceived(const std::string& configuration_name,
                         const std::vector<char>& data) override {
@@ -248,7 +249,7 @@ class VpnProviderApiTest : public VpnProviderApiTestBase {
                          const std::string& configuration_name) {
     service()->SendOnPlatformMessageToExtension(
         extension_id, configuration_name,
-        std::to_underlying(api_vpn::PlatformMessage::kError));
+        base::to_underlying(api_vpn::PlatformMessage::kError));
   }
 
   void ClearNetworkProfiles() {
@@ -418,7 +419,7 @@ IN_PROC_BROWSER_TEST_F(VpnProviderApiTest, VpnSuccess) {
   EXPECT_EQ(1, test_client_->set_parameters_counter_);
   EXPECT_EQ(1, test_client_->update_connection_state_counter_);
   EXPECT_EQ(1, test_client_->send_packet_counter_);
-  EXPECT_EQ(std::to_underlying(api_vpn::VpnConnectionState::kConnected),
+  EXPECT_EQ(base::to_underlying(api_vpn::VpnConnectionState::kConnected),
             test_client_->update_connection_state_counter_);
   for (size_t i = 0; i < std::size(kParameterValues); ++i) {
     const std::string* value =
diff --git a/chrome/browser/extensions/browser_window_util.cc b/chrome/browser/extensions/browser_window_util.cc
index 1490e44d3f..5af8b82010 100644
--- a/chrome/browser/extensions/browser_window_util.cc
+++ b/chrome/browser/extensions/browser_window_util.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/tab_list/tab_list_interface.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
@@ -72,7 +73,7 @@ BrowserWindowInterface* GetBrowserForTabContents(
       continue;
     }
     std::vector<tabs::TabInterface*> all_tabs = tab_list->GetAllTabs();
-    if (std::ranges::contains(all_tabs, tab)) {
+    if (base::Contains(all_tabs, tab)) {
       return browser;  // Found it!
     }
   }
diff --git a/chrome/browser/extensions/chrome_content_browser_client_extensions_part.cc b/chrome/browser/extensions/chrome_content_browser_client_extensions_part.cc
index 14590bcb06..5ec48e1f3e 100644
--- a/chrome/browser/extensions/chrome_content_browser_client_extensions_part.cc
+++ b/chrome/browser/extensions/chrome_content_browser_client_extensions_part.cc
@@ -15,6 +15,7 @@
 #include "base/auto_reset.h"
 #include "base/check_deref.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
@@ -927,7 +928,7 @@ void ChromeContentBrowserClientExtensionsPart::
     // visibility, and benefit from being started in foreground mode. We can
     // safely start those processes in foreground mode, knowing that
     // RenderThreadImpl::OnRendererHidden will be called when appropriate.
-    if (std::ranges::contains(MimeTypesHandler::GetMIMETypeAllowlist(),
+    if (base::Contains(MimeTypesHandler::GetMIMETypeAllowlist(),
                               extension->id())) {
       command_line->AppendSwitch(::switches::kInitIsolateAsForeground);
     }
diff --git a/chrome/browser/extensions/cws_info_service.cc b/chrome/browser/extensions/cws_info_service.cc
index 5989abdf93..35405c0c77 100644
--- a/chrome/browser/extensions/cws_info_service.cc
+++ b/chrome/browser/extensions/cws_info_service.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/queue.h"
 #include "base/features.h"
@@ -186,7 +187,7 @@ base::DictValue GetDictFromStoreMetadataProto(const StoreMetadata* metadata) {
 
     const auto& proto_labels = metadata->labels();
     for (const auto* label : kLabels) {
-      dict.Set(label, std::ranges::contains(proto_labels, label));
+      dict.Set(label, base::Contains(proto_labels, label));
     }
   }
 
diff --git a/chrome/browser/extensions/extension_management.cc b/chrome/browser/extensions/extension_management.cc
index bb0e7b5cba..d1202f64cc 100644
--- a/chrome/browser/extensions/extension_management.cc
+++ b/chrome/browser/extensions/extension_management.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -258,7 +259,7 @@ bool ExtensionManagement::HasAllowlistedExtension() {
     auto extension_id = *deferred_ids_.begin();
     // This will remove the entry from |deferred_ids_|.
     LoadDeferredExtensionSetting(extension_id);
-    DCHECK(!deferred_ids_.contains(extension_id));
+    DCHECK(!base::Contains(deferred_ids_, extension_id));
     if (AccessById(extension_id)->installation_mode ==
         ManagedInstallationMode::kAllowed) {
       NotifyExtensionManagementPrefChanged();
@@ -365,7 +366,7 @@ bool ExtensionManagement::IsAllowedManifestType(
     return true;
   const std::vector<Manifest::Type>& allowed_types =
       *global_settings_->allowed_types;
-  return std::ranges::contains(allowed_types, manifest_type);
+  return base::Contains(allowed_types, manifest_type);
 }
 
 bool ExtensionManagement::IsAllowedManifestVersion(
@@ -735,7 +736,8 @@ void ExtensionManagement::Refresh() {
   default_settings_ = std::make_unique<internal::IndividualSettings>();
 
   // Parse default settings.
-  if ((denied_list_pref && denied_list_pref->contains("*")) ||
+  const base::Value wildcard("*");
+  if ((denied_list_pref && base::Contains(*denied_list_pref, wildcard)) ||
       (extension_request_pref && extension_request_pref->GetBool())) {
     default_settings_->installation_mode = ManagedInstallationMode::kBlocked;
   }
@@ -934,7 +936,7 @@ bool ExtensionManagement::ParseById(const std::string& extension_id,
 
 internal::IndividualSettings* ExtensionManagement::GetSettingsForId(
     const std::string& extension_id) {
-  if (deferred_ids_.contains(extension_id)) {
+  if (base::Contains(deferred_ids_, extension_id)) {
     LoadDeferredExtensionSetting(extension_id);
     NotifyExtensionManagementPrefChanged();
   }
@@ -948,7 +950,7 @@ internal::IndividualSettings* ExtensionManagement::GetSettingsForId(
 
 void ExtensionManagement::LoadDeferredExtensionSetting(
     const std::string& extension_id) {
-  DCHECK(deferred_ids_.contains(extension_id));
+  DCHECK(base::Contains(deferred_ids_, extension_id));
 
   // No need to check again later.
   deferred_ids_.erase(extension_id);
@@ -968,7 +970,7 @@ void ExtensionManagement::LoadDeferredExtensionSetting(
 
     auto extension_ids = base::SplitStringPiece(
         iter.first, ",", base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-    if (std::ranges::contains(extension_ids, extension_id)) {
+    if (base::Contains(extension_ids, extension_id)) {
       // Found our settings. After parsing, continue looking for more entries.
       ParseById(extension_id, *subdict);
       found = true;
diff --git a/chrome/browser/extensions/extension_service.cc b/chrome/browser/extensions/extension_service.cc
index d3fe3370c5..bc41f0096e 100644
--- a/chrome/browser/extensions/extension_service.cc
+++ b/chrome/browser/extensions/extension_service.cc
@@ -16,6 +16,7 @@
 #include "base/auto_reset.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/bind.h"
@@ -521,7 +522,7 @@ void ExtensionService::DisableUserExtensionsExcept(
       continue;
     }
     const std::string& id = extension->id();
-    if (!std::ranges::contains(except_ids, id)) {
+    if (!base::Contains(except_ids, id)) {
       extension_registrar_->DisableExtension(
           id, {disable_reason::DISABLE_USER_ACTION});
     }
diff --git a/chrome/browser/extensions/external_provider_impl.cc b/chrome/browser/extensions/external_provider_impl.cc
index 06fbe5a802..adf32f9f4d 100644
--- a/chrome/browser/extensions/external_provider_impl.cc
+++ b/chrome/browser/extensions/external_provider_impl.cc
@@ -15,6 +15,7 @@
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/logging.h"
 #include "base/memory/scoped_refptr.h"
@@ -355,7 +356,7 @@ void ExternalProviderImpl::RetrieveExtensionsFromPrefs(
         if (current_locale && l10n_util::IsValidLocaleSyntax(*current_locale)) {
           std::string normalized_locale =
               l10n_util::NormalizeLocale(*current_locale);
-          if (std::ranges::contains(browser_locales, normalized_locale)) {
+          if (base::Contains(browser_locales, normalized_locale)) {
             locale_supported = true;
             break;
           }
diff --git a/chrome/browser/extensions/mv2_deprecation_impact_checker.cc b/chrome/browser/extensions/mv2_deprecation_impact_checker.cc
index d5b1ce407d..2185d67066 100644
--- a/chrome/browser/extensions/mv2_deprecation_impact_checker.cc
+++ b/chrome/browser/extensions/mv2_deprecation_impact_checker.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_split.h"
 #include "chrome/browser/extensions/extension_management.h"
@@ -79,7 +80,7 @@ bool MV2DeprecationImpactChecker::IsExtensionAffected(
   }
 
   // Extensions with a temporary exception.
-  if (std::ranges::contains(GetHashedExceptionList(), hashed_id.value())) {
+  if (base::Contains(GetHashedExceptionList(), hashed_id.value())) {
     return false;
   }
 
diff --git a/chrome/browser/extensions/window_controller.cc b/chrome/browser/extensions/window_controller.cc
index 363e3fd973..b17a7f46c9 100644
--- a/chrome/browser/extensions/window_controller.cc
+++ b/chrome/browser/extensions/window_controller.cc
@@ -7,8 +7,8 @@
 #include <stddef.h>
 
 #include <memory>
-#include <utility>
 
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/browser/extensions/window_controller_list.h"
 #include "chrome/browser/profiles/profile.h"
@@ -26,13 +26,13 @@ namespace extensions {
 WindowController::TypeFilter WindowController::GetAllWindowFilter() {
   // This needs to be updated if there is a change to
   // extensions::api::windows:WindowType.
-  static_assert(std::to_underlying(api::windows::WindowType::kMaxValue) == 5,
+  static_assert(base::to_underlying(api::windows::WindowType::kMaxValue) == 5,
                 "Update extensions WindowController to match WindowType");
-  return ((1 << std::to_underlying(api::windows::WindowType::kNormal)) |
-          (1 << std::to_underlying(api::windows::WindowType::kPanel)) |
-          (1 << std::to_underlying(api::windows::WindowType::kPopup)) |
-          (1 << std::to_underlying(api::windows::WindowType::kApp)) |
-          (1 << std::to_underlying(api::windows::WindowType::kDevtools)));
+  return ((1 << base::to_underlying(api::windows::WindowType::kNormal)) |
+          (1 << base::to_underlying(api::windows::WindowType::kPanel)) |
+          (1 << base::to_underlying(api::windows::WindowType::kPopup)) |
+          (1 << base::to_underlying(api::windows::WindowType::kApp)) |
+          (1 << base::to_underlying(api::windows::WindowType::kDevtools)));
 }
 
 // static
@@ -40,7 +40,7 @@ WindowController::TypeFilter WindowController::GetFilterFromWindowTypes(
     const std::vector<api::windows::WindowType>& types) {
   WindowController::TypeFilter filter = kNoWindowFilter;
   for (auto& window_type : types)
-    filter |= 1 << std::to_underlying(window_type);
+    filter |= 1 << base::to_underlying(window_type);
   return filter;
 }
 
@@ -55,7 +55,7 @@ WindowController::TypeFilter WindowController::GetFilterFromWindowTypesValues(
     if (!type.is_string()) {
       continue;
     }
-    filter |= 1 << std::to_underlying(
+    filter |= 1 << base::to_underlying(
                   api::windows::ParseWindowType(type.GetString()));
   }
   return filter;
@@ -78,7 +78,7 @@ Browser* WindowController::GetBrowser() const {
 #endif
 
 bool WindowController::MatchesFilter(TypeFilter filter) const {
-  TypeFilter type = 1 << std::to_underlying(
+  TypeFilter type = 1 << base::to_underlying(
                         api::windows::ParseWindowType(GetWindowTypeText()));
   return (type & filter) != 0;
 }
diff --git a/chrome/browser/extensions/window_controller_list.cc b/chrome/browser/extensions/window_controller_list.cc
index b668ff197d..91c68c128f 100644
--- a/chrome/browser/extensions/window_controller_list.cc
+++ b/chrome/browser/extensions/window_controller_list.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "chrome/browser/extensions/api/tabs/windows_util.h"
 #include "chrome/browser/extensions/window_controller_list_observer.h"
@@ -47,7 +48,7 @@ void WindowControllerList::RemoveExtensionWindow(WindowController* window) {
 }
 
 void WindowControllerList::NotifyWindowBoundsChanged(WindowController* window) {
-  if (std::ranges::contains(windows_, window)) {
+  if (base::Contains(windows_, window)) {
     for (auto& observer : observers_)
       observer.OnWindowBoundsChanged(window);
   }
@@ -55,7 +56,7 @@ void WindowControllerList::NotifyWindowBoundsChanged(WindowController* window) {
 
 void WindowControllerList::NotifyWindowFocusChanged(WindowController* window,
                                                     bool has_focus) {
-  if (std::ranges::contains(windows_, window)) {
+  if (base::Contains(windows_, window)) {
     for (auto& observer : observers_) {
       observer.OnWindowFocusChanged(window, has_focus);
     }
diff --git a/chrome/browser/file_select_helper.cc b/chrome/browser/file_select_helper.cc
index 70918f07be..93b4154eec 100644
--- a/chrome/browser/file_select_helper.cc
+++ b/chrome/browser/file_select_helper.cc
@@ -11,6 +11,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
@@ -360,7 +361,7 @@ void FileSelectHelper::ContentAnalysisCompletionCallback(
   // files, block the entire folder and update `result` to reflect the block
   // verdict for all files scanned.
   if (dialog_type_ == ui::SelectFileDialog::SELECT_UPLOAD_FOLDER) {
-    if (std::ranges::contains(result.paths_results, false)) {
+    if (base::Contains(result.paths_results, false)) {
       list.clear();
       for (size_t index = 0; index < data.paths.size(); ++index) {
         result.paths_results[index] = false;
diff --git a/chrome/browser/glic/glic_pref_names.cc b/chrome/browser/glic/glic_pref_names.cc
index 5a522c66a0..1a618f7a3a 100644
--- a/chrome/browser/glic/glic_pref_names.cc
+++ b/chrome/browser/glic/glic_pref_names.cc
@@ -4,8 +4,7 @@
 
 #include "chrome/browser/glic/glic_pref_names.h"
 
-#include <utility>
-
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/background/glic/glic_launcher_configuration.h"
 #include "chrome/browser/glic/common/local_hotkey_manager.h"
 #include "chrome/common/chrome_features.h"
@@ -59,7 +58,7 @@ void RegisterProfilePrefs(user_prefs::PrefRegistrySyncable* registry) {
 
   registry->RegisterIntegerPref(
       prefs::kGlicActuationOnWeb,
-      std::to_underlying(GetGlicActuationOnWebPolicyState()));
+      base::to_underlying(GetGlicActuationOnWebPolicyState()));
 
   registry->RegisterListPref(prefs::kGlicActuationOnWebAllowedForURLs);
   registry->RegisterListPref(prefs::kGlicActuationOnWebBlockedForURLs);
diff --git a/chrome/browser/glic/glic_zero_state_suggestions_manager.cc b/chrome/browser/glic/glic_zero_state_suggestions_manager.cc
index 5bc58c4e7b..3f010dc24a 100644
--- a/chrome/browser/glic/glic_zero_state_suggestions_manager.cc
+++ b/chrome/browser/glic/glic_zero_state_suggestions_manager.cc
@@ -7,8 +7,10 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/id_type.h"
 #include "build/build_config.h"
 #include "chrome/browser/contextual_cueing/caching_zero_state_suggestions_manager.h"
@@ -65,7 +67,7 @@ bool HasUserNavigationBeyondInitial(content::WebContents& web_contents) {
   // user selected default page.
   while (index > 0) {
     content::NavigationEntry* entry = controller.GetEntryAtIndex(index);
-    auto transition = std::to_underlying(entry->GetTransitionType());
+    auto transition = base::to_underlying(entry->GetTransitionType());
     bool automatic = 0 != (transition & (ui::PAGE_TRANSITION_FORWARD_BACK |
                                          ui::PAGE_TRANSITION_HOME_PAGE |
                                          ui::PAGE_TRANSITION_FROM_API |
@@ -176,7 +178,7 @@ void GlicZeroStateSuggestionsManager::
                                : nullptr;
   std::vector<content::WebContents*> contents_for_request = pinned_tab_data;
   if (active_web_contents &&
-      !std::ranges::contains(contents_for_request, active_web_contents)) {
+      !base::Contains(contents_for_request, active_web_contents)) {
     contents_for_request.push_back(active_web_contents);
   }
   FilterTabs(contents_for_request);
diff --git a/chrome/browser/glic/host/context/glic_sharing_utils.cc b/chrome/browser/glic/host/context/glic_sharing_utils.cc
index 69161527c6..0f62993805 100644
--- a/chrome/browser/glic/host/context/glic_sharing_utils.cc
+++ b/chrome/browser/glic/host/context/glic_sharing_utils.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "build/build_config.h"
 #include "chrome/browser/glic/common/future_browser_features.h"
@@ -47,7 +48,7 @@ bool IsTabValidForSharing(content::WebContents* web_contents) {
   }
   const GURL& url = web_contents->GetLastCommittedURL();
   return url.SchemeIsHTTPOrHTTPS() || url.SchemeIsFile() ||
-         std::ranges::contains(*kUrlAllowList, url);
+         base::Contains(*kUrlAllowList, url);
 }
 
 GlicPinEvent GetEmptyPinEvent() {
diff --git a/chrome/browser/glic/host/glic_actor_general_interactive_uitest.cc b/chrome/browser/glic/host/glic_actor_general_interactive_uitest.cc
index d8395b17b8..237623b500 100644
--- a/chrome/browser/glic/host/glic_actor_general_interactive_uitest.cc
+++ b/chrome/browser/glic/host/glic_actor_general_interactive_uitest.cc
@@ -2,12 +2,11 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <utility>
-
 #include "base/base64.h"
 #include "base/test/metrics/histogram_tester.h"
 #include "base/test/protobuf_matchers.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "chrome/browser/actor/actor_features.h"
 #include "chrome/browser/actor/actor_proto_conversion.h"
@@ -726,7 +725,7 @@ class GlicActorCallbackOrderGeneralUiTest : public GlicActorGeneralUiTest {
               return state == $1;
             });
           )JS",
-              std::to_underlying(state));
+              base::to_underlying(state));
           ASSERT_TRUE(content::ExecJs(glic_contents, script));
         })));
   }
diff --git a/chrome/browser/glic/host/glic_actor_interactive_uitest_common.cc b/chrome/browser/glic/host/glic_actor_interactive_uitest_common.cc
index c7c1391356..41f4b53ea9 100644
--- a/chrome/browser/glic/host/glic_actor_interactive_uitest_common.cc
+++ b/chrome/browser/glic/host/glic_actor_interactive_uitest_common.cc
@@ -8,7 +8,6 @@
 #include <optional>
 #include <string>
 #include <string_view>
-#include <utility>
 
 #include "base/base64.h"
 #include "base/functional/callback.h"
@@ -16,6 +15,7 @@
 #include "base/strings/to_string.h"
 #include "base/test/bind.h"
 #include "base/test/scoped_feature_list.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "chrome/browser/actor/actor_keyed_service.h"
 #include "chrome/browser/actor/actor_proto_conversion.h"
@@ -121,7 +121,7 @@ content::WebContents* GlicActorUiTest::GetGlicHost(actor::TaskId& task_id) {
 MultiStep GlicActorUiTest::ExecuteAction(ActionProtoProvider proto_provider,
                                          ExpectedErrorResult expected_result) {
   static constexpr int kResultSuccess =
-      std::to_underlying(actor::mojom::ActionResultCode::kOk);
+      base::to_underlying(actor::mojom::ActionResultCode::kOk);
   static constexpr std::string_view kSuccessString = "<Success>";
 
   const std::string expected_result_string = std::visit(
@@ -523,7 +523,7 @@ MultiStep GlicActorUiTest::WaitForActorTaskState(
                 return state == $2;
               });
               )js",
-        task_id.value(), std::to_underlying(expected_state));
+        task_id.value(), base::to_underlying(expected_state));
     ASSERT_TRUE(content::ExecJs(glic_contents, script));
   }));
 }
@@ -556,7 +556,7 @@ MultiStep GlicActorUiTest::WaitForActorTaskStateChangeToStopped() {
         "window.taskStateObs.waitUntil((state) => { "
         "  return state == $1; "
         "});",
-        std::to_underlying(mojom::ActorTaskState::kStopped));
+        base::to_underlying(mojom::ActorTaskState::kStopped));
     ASSERT_TRUE(content::ExecJs(glic_contents, script));
   }));
 }
diff --git a/chrome/browser/glic/host/glic_api_browsertest.cc b/chrome/browser/glic/host/glic_api_browsertest.cc
index fcb426c187..87f24c0499 100644
--- a/chrome/browser/glic/host/glic_api_browsertest.cc
+++ b/chrome/browser/glic/host/glic_api_browsertest.cc
@@ -10,7 +10,6 @@
 #include <ranges>
 #include <string>
 #include <string_view>
-#include <utility>
 
 #include "base/callback_list.h"
 #include "base/command_line.h"
@@ -32,6 +31,7 @@
 #include "base/test/test_future.h"
 #include "base/test/test_timeouts.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "base/version_info/version_info.h"
 #include "build/build_config.h"
@@ -642,7 +642,7 @@ class GlicApiTestWithGeminiActOnWebPolicy : public GlicApiTestWithOneTab {
     policies.Set(policy::key::kGeminiActOnWebSettings,
                  policy::POLICY_LEVEL_MANDATORY, policy::POLICY_SCOPE_USER,
                  policy::POLICY_SOURCE_ENTERPRISE_DEFAULT,
-                 base::Value(std::to_underlying(value)), nullptr);
+                 base::Value(base::to_underlying(value)), nullptr);
     policy_provider_.UpdateChromePolicy(policies);
   }
 
@@ -3558,16 +3558,16 @@ IN_PROC_BROWSER_TEST_P(GlicGetHostCapabilityApiTest, testGetHostCapabilities) {
   if (GetParam().enable_scroll_to_pdf) {
 #if BUILDFLAG(ENABLE_PDF)
     expected_capabilities.Append(
-        std::to_underlying(mojom::HostCapability::kScrollToPdf));
+        base::to_underlying(mojom::HostCapability::kScrollToPdf));
 #endif
   }
   if (GetParam().trust_first_onboarding_arm1) {
     expected_capabilities.Append(
-        std::to_underlying(mojom::HostCapability::kTrustFirstOnboardingArm1));
+        base::to_underlying(mojom::HostCapability::kTrustFirstOnboardingArm1));
   }
   if (GetParam().trust_first_onboarding_arm2) {
     expected_capabilities.Append(
-        std::to_underlying(mojom::HostCapability::kTrustFirstOnboardingArm2));
+        base::to_underlying(mojom::HostCapability::kTrustFirstOnboardingArm2));
   }
   if (GetParam().auto_open_pdf) {
     expected_capabilities.Append(
diff --git a/chrome/browser/glic/host/host.cc b/chrome/browser/glic/host/host.cc
index 4c0e4d20d3..e63474fddf 100644
--- a/chrome/browser/glic/host/host.cc
+++ b/chrome/browser/glic/host/host.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <ranges>
 
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/notimplemented.h"
@@ -883,7 +884,7 @@ void HostManager::WebUIPageHandlerRemoved(GlicPageHandler* page_handler) {
   for (Host* host : GetAllHosts()) {
     if (host->page_handler() == page_handler) {
       host->WebUIPageHandlerRemoved(page_handler);
-      if (std::ranges::contains(instance_hosts, host)) {
+      if (base::Contains(instance_hosts, host)) {
         std::erase_if(tab_hosts_, [host](std::unique_ptr<Host>& h) {
           return h.get() == host;
         });
diff --git a/chrome/browser/glic/test_support/glic_api_test.h b/chrome/browser/glic/test_support/glic_api_test.h
index b362bb51ca..a7b9c08808 100644
--- a/chrome/browser/glic/test_support/glic_api_test.h
+++ b/chrome/browser/glic/test_support/glic_api_test.h
@@ -5,7 +5,6 @@
 #ifndef CHROME_BROWSER_GLIC_TEST_SUPPORT_GLIC_API_TEST_H_
 #define CHROME_BROWSER_GLIC_TEST_SUPPORT_GLIC_API_TEST_H_
 
-#include <algorithm>
 #include <type_traits>
 
 #include "base/json/json_writer.h"
@@ -344,8 +343,8 @@ class GlicApiTestBase : public T {
     }
     for (int i = 0; i < unit_test->total_test_suite_count(); ++i) {
       const auto* test_suite = unit_test->GetTestSuite(i);
-      if (!std::ranges::contains(gunit_test_suite_names,
-                                 std::string(test_suite->name()))) {
+      if (!base::Contains(gunit_test_suite_names,
+                          std::string(test_suite->name()))) {
         continue;
       }
       for (int j = 0; j < test_suite->total_test_count(); ++j) {
diff --git a/chrome/browser/glic/test_support/interactive_glic_test.h b/chrome/browser/glic/test_support/interactive_glic_test.h
index e3bbcf0cf1..7ff651d5f2 100644
--- a/chrome/browser/glic/test_support/interactive_glic_test.h
+++ b/chrome/browser/glic/test_support/interactive_glic_test.h
@@ -5,7 +5,6 @@
 #ifndef CHROME_BROWSER_GLIC_TEST_SUPPORT_INTERACTIVE_GLIC_TEST_H_
 #define CHROME_BROWSER_GLIC_TEST_SUPPORT_INTERACTIVE_GLIC_TEST_H_
 
-#include <algorithm>
 #include <map>
 #include <sstream>
 #include <string_view>
@@ -769,11 +768,10 @@ class InteractiveGlicTestMixin : public T {
   auto CheckOcclusionTracked(bool expect_is_tracked) {
     return Api::CheckResult(
         [this]() {
-          return std::ranges::contains(
-              PictureInPictureWindowManager::GetInstance()
-                  ->GetOcclusionTracker()
-                  ->GetPictureInPictureWidgetsForTesting(),
-              GetGlicWidget());
+          return base::Contains(PictureInPictureWindowManager::GetInstance()
+                                    ->GetOcclusionTracker()
+                                    ->GetPictureInPictureWidgetsForTesting(),
+                                GetGlicWidget());
         },
         expect_is_tracked, "CheckOcclusionTracked");
   }
diff --git a/chrome/browser/google/google_brand.cc b/chrome/browser/google/google_brand.cc
index 2f716dcf7e..19e9891c05 100644
--- a/chrome/browser/google/google_brand.cc
+++ b/chrome/browser/google/google_brand.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_util.h"
@@ -139,7 +140,7 @@ bool IsInternetCafeBrandCode(const std::string& brand) {
       "CHIQ", "CHSG", "HLJY", "NTMO", "OOBA", "OOBB", "OOBC", "OOBD",
       "OOBE", "OOBF", "OOBG", "OOBH", "OOBI", "OOBJ", "IDCM",
   };
-  return std::ranges::contains(kBrands, brand);
+  return base::Contains(kBrands, brand);
 }
 
 bool IsEnterprise(const std::string& brand) {
diff --git a/chrome/browser/hid/hid_chooser_context.cc b/chrome/browser/hid/hid_chooser_context.cc
index 88d3d33b17..017fe86dbc 100644
--- a/chrome/browser/hid/hid_chooser_context.cc
+++ b/chrome/browser/hid/hid_chooser_context.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/observer_list.h"
 #include "base/strings/string_number_conversions.h"
@@ -231,7 +232,7 @@ HidChooserContext::GetGrantedObjects(const url::Origin& origin) {
         // always be called after device initialization in HidChooserController
         // which always returns after the device list initialization in this
         // class.
-        DCHECK(devices_.contains(guid));
+        DCHECK(base::Contains(devices_, guid));
         objects.push_back(std::make_unique<Object>(
             origin, DeviceInfoToValue(*devices_[guid]),
             content_settings::SettingSource::kUser, IsOffTheRecord()));
@@ -242,7 +243,7 @@ HidChooserContext::GetGrantedObjects(const url::Origin& origin) {
   if (CanApplyPolicy()) {
     auto* policy = HidPolicyAllowedDevicesFactory::GetForProfile(profile_);
     for (const auto& entry : policy->device_policy()) {
-      if (!entry.second.contains(origin)) {
+      if (!base::Contains(entry.second, origin)) {
         continue;
       }
 
@@ -253,7 +254,7 @@ HidChooserContext::GetGrantedObjects(const url::Origin& origin) {
     }
 
     for (const auto& entry : policy->vendor_policy()) {
-      if (!entry.second.contains(origin)) {
+      if (!base::Contains(entry.second, origin)) {
         continue;
       }
 
@@ -263,7 +264,7 @@ HidChooserContext::GetGrantedObjects(const url::Origin& origin) {
     }
 
     for (const auto& entry : policy->usage_policy()) {
-      if (!entry.second.contains(origin)) {
+      if (!base::Contains(entry.second, origin)) {
         continue;
       }
 
@@ -274,7 +275,7 @@ HidChooserContext::GetGrantedObjects(const url::Origin& origin) {
     }
 
     for (const auto& entry : policy->usage_page_policy()) {
-      if (!entry.second.contains(origin)) {
+      if (!base::Contains(entry.second, origin)) {
         continue;
       }
 
@@ -308,7 +309,7 @@ HidChooserContext::GetAllGrantedObjects() {
       continue;
 
     for (const auto& guid : map_entry.second) {
-      DCHECK(devices_.contains(guid));
+      DCHECK(base::Contains(devices_, guid));
       objects.push_back(
           std::make_unique<Object>(origin, DeviceInfoToValue(*devices_[guid]),
                                    SettingSource::kUser, IsOffTheRecord()));
@@ -468,7 +469,7 @@ bool HidChooserContext::HasDevicePermission(
     const device::mojom::HidDeviceInfo& device,
     const std::optional<url::Origin>& embedding_origin_of_web_view) {
   if (device.is_excluded_by_blocklist) {
-    bool is_device_protected_due_to_fido = std::ranges::contains(
+    bool is_device_protected_due_to_fido = base::Contains(
         device.collections, device::mojom::kPageFido,
         [](const auto& c) { return c->usage->usage_page; });
     if (base::FeatureList::IsEnabled(
@@ -496,7 +497,8 @@ bool HidChooserContext::HasDevicePermission(
   }
 
   auto it = ephemeral_devices_.find(origin);
-  if (it != ephemeral_devices_.end() && it->second.contains(device.guid)) {
+  if (it != ephemeral_devices_.end() &&
+      base::Contains(it->second, device.guid)) {
     return true;
   }
 
@@ -522,7 +524,7 @@ bool HidChooserContext::HasDevicePermission(
 bool HidChooserContext::IsFidoAllowedForOrigin(const url::Origin& origin) {
 #if BUILDFLAG(ENABLE_EXTENSIONS)
   if (origin.scheme() == extensions::kExtensionScheme &&
-      kPrivilegedFidoExtensionIds.contains(origin.host())) {
+      base::Contains(kPrivilegedFidoExtensionIds, origin.host())) {
     return true;
   }
 #endif  // BUILDFLAG(ENABLE_EXTENSIONS)
@@ -617,7 +619,7 @@ void HidChooserContext::DeviceAdded(device::mojom::HidDeviceInfoPtr device) {
   DCHECK(device);
 
   // Update the device list.
-  if (!devices_.contains(device->guid)) {
+  if (!base::Contains(devices_, device->guid)) {
     devices_.insert({device->guid, device->Clone()});
   }
 
@@ -628,7 +630,7 @@ void HidChooserContext::DeviceAdded(device::mojom::HidDeviceInfoPtr device) {
 
 void HidChooserContext::DeviceRemoved(device::mojom::HidDeviceInfoPtr device) {
   DCHECK(device);
-  DCHECK(devices_.contains(device->guid));
+  DCHECK(base::Contains(devices_, device->guid));
 
   // Update the device list.
   devices_.erase(device->guid);
@@ -662,7 +664,7 @@ void HidChooserContext::DeviceRemoved(device::mojom::HidDeviceInfoPtr device) {
 
 void HidChooserContext::DeviceChanged(device::mojom::HidDeviceInfoPtr device) {
   DCHECK(device);
-  DCHECK(devices_.contains(device->guid));
+  DCHECK(base::Contains(devices_, device->guid));
 
   // Update the device list.
   devices_[device->guid] = device->Clone();
diff --git a/chrome/browser/history_clusters/history_clusters_tab_helper.cc b/chrome/browser/history_clusters/history_clusters_tab_helper.cc
index 90d3b9a8dd..9b6e33bc99 100644
--- a/chrome/browser/history_clusters/history_clusters_tab_helper.cc
+++ b/chrome/browser/history_clusters/history_clusters_tab_helper.cc
@@ -9,6 +9,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/time/time.h"
 #include "build/build_config.h"
 #include "chrome/browser/bookmarks/bookmark_model_factory.h"
@@ -419,7 +420,7 @@ void HistoryClustersTabHelper::RecordPageEndMetricsIfNeeded(
           ->GetPrefs();
   ntp_tiles::CustomLinksStore custom_link_store(pref_service);
   incomplete_visit_context_annotations.context_annotations.is_ntp_custom_link =
-      std::ranges::contains(custom_link_store.RetrieveLinks(),
+      base::Contains(custom_link_store.RetrieveLinks(),
                             incomplete_visit_context_annotations.url_row.url(),
                             [](const auto& link) { return link.url; });
 #endif  // !BUILDFLAG(IS_ANDROID)
diff --git a/chrome/browser/history_embeddings/history_embeddings_tab_helper.cc b/chrome/browser/history_embeddings/history_embeddings_tab_helper.cc
index f35c3047bb..45daaff097 100644
--- a/chrome/browser/history_embeddings/history_embeddings_tab_helper.cc
+++ b/chrome/browser/history_embeddings/history_embeddings_tab_helper.cc
@@ -8,6 +8,7 @@
 #include <numeric>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -93,7 +94,7 @@ void OnGotInnerText(mojo::Remote<blink::mojom::InnerTextAgent> remote,
 
   bool title_inserted = false;
   if (history_embeddings::GetFeatureParameters().insert_title_passage &&
-      !title.empty() && !std::ranges::contains(valid_passages, title)) {
+      !title.empty() && !base::Contains(valid_passages, title)) {
     VLOG(2) << "Title passage inserted: " << title;
     valid_passages.insert(valid_passages.begin(), std::move(title));
     if (valid_passages.size() >
diff --git a/chrome/browser/keyboard_accessory/android/accessory_sheet_data.cc b/chrome/browser/keyboard_accessory/android/accessory_sheet_data.cc
index ec8e37194f..a3b2beaaea 100644
--- a/chrome/browser/keyboard_accessory/android/accessory_sheet_data.cc
+++ b/chrome/browser/keyboard_accessory/android/accessory_sheet_data.cc
@@ -6,11 +6,11 @@
 
 #include <algorithm>
 #include <ios>
-#include <utility>
 
 #include "base/base64.h"
 #include "base/logging.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/keyboard_accessory/android/accessory_sheet_enums.h"
 #include "components/password_manager/core/browser/origin_credential_store.h"
 
@@ -31,7 +31,7 @@ AccessorySheetField& AccessorySheetField::operator=(AccessorySheetField&&) =
     default;
 
 std::ostream& operator<<(std::ostream& os, const AccessorySheetField& field) {
-  os << "(suggestion_type: " << std::to_underlying(field.suggestion_type())
+  os << "(suggestion_type: " << base::to_underlying(field.suggestion_type())
      << ", " << "display text: \"" << field.display_text() << "\", "
      << "text_to_fill: \"" << field.text_to_fill() << "\", "
      << "a11y_description: \"" << field.a11y_description() << "\", " << "id: \""
diff --git a/chrome/browser/lookalikes/lookalike_url_service.cc b/chrome/browser/lookalikes/lookalike_url_service.cc
index a6a56872a6..d35cda32fc 100644
--- a/chrome/browser/lookalikes/lookalike_url_service.cc
+++ b/chrome/browser/lookalikes/lookalike_url_service.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/scoped_refptr.h"
@@ -202,7 +203,7 @@ LookalikeUrlService::CheckUrlForLookalikes(
   // ignores the scheme which is okay since it's more conservative: If the user
   // is engaged with http://domain.test, not showing the warning on
   // https://domain.test is acceptable.
-  if (std::ranges::contains(engaged_sites, navigated_domain.domain_and_registry,
+  if (base::Contains(engaged_sites, navigated_domain.domain_and_registry,
                             &DomainInfo::domain_and_registry)) {
     return result;
   }
diff --git a/chrome/browser/media/router/discovery/dial/dial_media_sink_service_impl.cc b/chrome/browser/media/router/discovery/dial/dial_media_sink_service_impl.cc
index d027d85da8..463c8a2ba0 100644
--- a/chrome/browser/media/router/discovery/dial/dial_media_sink_service_impl.cc
+++ b/chrome/browser/media/router/discovery/dial/dial_media_sink_service_impl.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/strcat.h"
 #include "base/strings/string_util.h"
@@ -50,7 +51,7 @@ std::string EnumToString(DialRegistry::DialErrorCode code) {
 // |model_name|: device model name.
 bool IsDiscoveryOnly(const std::string& model_name) {
   std::string lower_model_name = base::ToLowerASCII(model_name);
-  return std::ranges::contains(kDiscoveryOnlyModelNames, lower_model_name);
+  return base::Contains(kDiscoveryOnlyModelNames, lower_model_name);
 }
 
 SinkAppStatus GetSinkAppStatusFromResponse(const DialAppInfoResult& result) {
@@ -184,7 +185,7 @@ void DialMediaSinkServiceImpl::OnDiscoveryComplete() {
   std::vector<MediaSinkInternal> sinks_to_update;
   std::vector<MediaSinkInternal> sinks_to_remove;
   for (const auto& sink : GetSinks()) {
-    if (!latest_sinks_.contains(sink.first)) {
+    if (!base::Contains(latest_sinks_, sink.first)) {
       sinks_to_remove.push_back(sink.second);
     }
   }
@@ -239,7 +240,7 @@ void DialMediaSinkServiceImpl::OnDeviceDescriptionAvailable(
     const DialDeviceData& device_data,
     const ParsedDialDeviceDescription& description_data) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  if (!std::ranges::contains(current_devices_, device_data)) {
+  if (!base::Contains(current_devices_, device_data)) {
     return;
   }
 
diff --git a/chrome/browser/media/router/discovery/mdns/cast_media_sink_service_impl.cc b/chrome/browser/media/router/discovery/mdns/cast_media_sink_service_impl.cc
index ba0b7f74d7..5d20109fe8 100644
--- a/chrome/browser/media/router/discovery/mdns/cast_media_sink_service_impl.cc
+++ b/chrome/browser/media/router/discovery/mdns/cast_media_sink_service_impl.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/rand_util.h"
@@ -770,7 +771,7 @@ void CastMediaSinkServiceImpl::SetCastAllowAllIPs(bool allow_all_ips) {
 }
 
 bool CastMediaSinkServiceImpl::HasSink(const MediaSink::Id& sink_id) {
-  return GetSinks().contains(sink_id);
+  return base::Contains(GetSinks(), sink_id);
 }
 
 void CastMediaSinkServiceImpl::DisconnectAndRemoveSink(
@@ -791,7 +792,7 @@ void CastMediaSinkServiceImpl::DisconnectAndRemoveSink(
 
 bool CastMediaSinkServiceImpl::HasSinkWithIPAddress(
     const net::IPAddress& ip_address) const {
-  return std::ranges::contains(GetSinks(), ip_address, [](const auto& sink) {
+  return base::Contains(GetSinks(), ip_address, [](const auto& sink) {
     return sink.second.cast_data().ip_endpoint.address();
   });
 }
diff --git a/chrome/browser/media/router/mojo/media_router_desktop.cc b/chrome/browser/media/router/mojo/media_router_desktop.cc
index 7518ac7180..103e1bb996 100644
--- a/chrome/browser/media/router/mojo/media_router_desktop.cc
+++ b/chrome/browser/media/router/mojo/media_router_desktop.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_functions.h"
@@ -523,7 +524,7 @@ void MediaRouterDesktop::RegisterMediaRouteProvider(
     mojo::PendingRemote<mojom::MediaRouteProvider>
         media_route_provider_remote) {
   DCHECK_CURRENTLY_ON(content::BrowserThread::UI);
-  DCHECK(!media_route_providers_.contains(provider_id));
+  DCHECK(!base::Contains(media_route_providers_, provider_id));
   mojo::Remote<mojom::MediaRouteProvider> bound_remote(
       std::move(media_route_provider_remote));
   bound_remote.set_disconnect_handler(
@@ -899,7 +900,7 @@ MediaRouterDesktop::GetProviderIdForPresentation(
     DCHECK_LE(std::ranges::count(routes, presentation_id,
                                  &MediaRoute::presentation_id),
               1);
-    if (std::ranges::contains(routes, presentation_id,
+    if (base::Contains(routes, presentation_id,
                               &MediaRoute::presentation_id)) {
       return provider_id;
     }
@@ -913,7 +914,7 @@ MediaRouterDesktop::GetProviderIdForRoute(const MediaRoute::Id& route_id) {
   for (const auto& provider_to_routes : routes_query_.providers_to_routes()) {
     const mojom::MediaRouteProviderId provider_id = provider_to_routes.first;
     const std::vector<MediaRoute>& routes = provider_to_routes.second;
-    if (std::ranges::contains(routes, route_id, &MediaRoute::media_route_id)) {
+    if (base::Contains(routes, route_id, &MediaRoute::media_route_id)) {
       return provider_id;
     }
   }
@@ -1085,7 +1086,7 @@ bool MediaRouterDesktop::MediaRoutesQuery::AddRouteForProvider(
     mojom::MediaRouteProviderId provider_id,
     const MediaRoute& route) {
   std::vector<MediaRoute>& routes = providers_to_routes_[provider_id];
-  if (!std::ranges::contains(routes, route.media_route_id(),
+  if (!base::Contains(routes, route.media_route_id(),
                              &MediaRoute::media_route_id)) {
     routes.push_back(route);
     UpdateCachedRouteList();
diff --git a/chrome/browser/media/webrtc/desktop_media_picker_controller.cc b/chrome/browser/media/webrtc/desktop_media_picker_controller.cc
index 9156ed888d..14eaf439fc 100644
--- a/chrome/browser/media/webrtc/desktop_media_picker_controller.cc
+++ b/chrome/browser/media/webrtc/desktop_media_picker_controller.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/types/expected.h"
@@ -46,7 +47,7 @@ void DesktopMediaPickerController::Show(
     const Params& params,
     const std::vector<DesktopMediaList::Type>& sources,
     DoneCallback done_callback) {
-  DCHECK(!std::ranges::contains(sources, DesktopMediaList::Type::kNone));
+  DCHECK(!base::Contains(sources, DesktopMediaList::Type::kNone));
   DCHECK(!done_callback_);
 
   done_callback_ = std::move(done_callback);
diff --git a/chrome/browser/media/webrtc/desktop_media_picker_factory_impl.cc b/chrome/browser/media/webrtc/desktop_media_picker_factory_impl.cc
index 52a81ecef2..6bc5c59b28 100644
--- a/chrome/browser/media/webrtc/desktop_media_picker_factory_impl.cc
+++ b/chrome/browser/media/webrtc/desktop_media_picker_factory_impl.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "build/build_config.h"
 #include "chrome/browser/media/webrtc/current_tab_desktop_media_list.h"
@@ -101,8 +102,8 @@ DesktopMediaPickerFactoryImpl::CreateMediaList(
   // If we're supposed to include Tabs, but aren't including Windows (either
   // directly or indirectly), then we need to add Chrome App Windows back in.
   const bool add_chrome_app_windows =
-      !std::ranges::contains(types, DesktopMediaList::Type::kWindow) &&
-      std::ranges::contains(types, DesktopMediaList::Type::kWebContents);
+      !base::Contains(types, DesktopMediaList::Type::kWindow) &&
+      base::Contains(types, DesktopMediaList::Type::kWebContents);
   // Keep same order as the input |sources| and avoid duplicates.
   std::vector<std::unique_ptr<DesktopMediaList>> source_lists;
   bool have_screen_list = false;
diff --git a/chrome/browser/metrics/tab_footprint_aggregator.cc b/chrome/browser/metrics/tab_footprint_aggregator.cc
index edd3fb90be..5c29c527f4 100644
--- a/chrome/browser/metrics/tab_footprint_aggregator.cc
+++ b/chrome/browser/metrics/tab_footprint_aggregator.cc
@@ -9,6 +9,7 @@
 #include <numeric>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "services/metrics/public/cpp/ukm_builders.h"
 #include "services/metrics/public/cpp/ukm_recorder.h"
 
@@ -97,12 +98,12 @@ void TabFootprintAggregator::AssociateFrame(ukm::SourceId sid,
       << "Can't associate multiple SourceIds to a single PageId.";
 
   std::vector<PageId>& pages = process_to_pages_[pid];
-  DCHECK(!std::ranges::contains(pages, page_id))
+  DCHECK(!base::Contains(pages, page_id))
       << "Can't duplicate associations between a process and a page.";
   pages.push_back(page_id);
 
   std::vector<base::ProcessId>& processes = page_to_processes_[page_id];
-  DCHECK(!std::ranges::contains(processes, pid))
+  DCHECK(!base::Contains(processes, pid))
       << "Can't duplicate associations between a page and a process.";
   processes.push_back(pid);
 
diff --git a/chrome/browser/notifications/mac/mac_notification_provider_factory.cc b/chrome/browser/notifications/mac/mac_notification_provider_factory.cc
index fdecdafde9..1ccb99ea4b 100644
--- a/chrome/browser/notifications/mac/mac_notification_provider_factory.cc
+++ b/chrome/browser/notifications/mac/mac_notification_provider_factory.cc
@@ -10,6 +10,7 @@
 #include "base/functional/bind.h"
 #include "base/task/task_traits.h"
 #include "base/task/thread_pool.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/apps/app_shim/app_shim_manager_mac.h"
 #include "chrome/browser/child_process_host_flags.h"
 #include "chrome/browser/web_applications/os_integration/mac/web_app_shortcut_mac.h"
@@ -30,7 +31,7 @@ LaunchOutOfProcessProvider() {
       content::ServiceProcessHost::Options()
           .WithDisplayName("Notification Service")
           .WithExtraCommandLineSwitches({switches::kMessageLoopTypeUi})
-          .WithChildFlags(std::to_underlying(
+          .WithChildFlags(base::to_underlying(
               ChildProcessHostFlags::kChildProcessHelperAlerts))
           .Pass());
 }
diff --git a/chrome/browser/notifications/scheduler/internal/icon_store.cc b/chrome/browser/notifications/scheduler/internal/icon_store.cc
index 06bf6b7314..6a16b3be87 100644
--- a/chrome/browser/notifications/scheduler/internal/icon_store.cc
+++ b/chrome/browser/notifications/scheduler/internal/icon_store.cc
@@ -8,6 +8,7 @@
 #include <map>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/uuid.h"
 #include "chrome/browser/notifications/scheduler/internal/icon_entry.h"
 #include "chrome/browser/notifications/scheduler/internal/proto_conversion.h"
@@ -32,7 +33,7 @@ namespace notifications {
 namespace {
 bool HasKeyInDb(const std::vector<std::string>& key_dict,
                 const std::string& key) {
-  return std::ranges::contains(key_dict, key);
+  return base::Contains(key_dict, key);
 }
 }  // namespace
 
diff --git a/chrome/browser/notifications/scheduler/internal/impression_history_tracker.cc b/chrome/browser/notifications/scheduler/internal/impression_history_tracker.cc
index 43f7589561..dfd48d7971 100644
--- a/chrome/browser/notifications/scheduler/internal/impression_history_tracker.cc
+++ b/chrome/browser/notifications/scheduler/internal/impression_history_tracker.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/notimplemented.h"
@@ -207,7 +208,7 @@ void ImpressionHistoryTrackerImpl::SyncRegisteredClients() {
   // Remove deprecated clients.
   for (auto it = client_states_.begin(); it != client_states_.end();) {
     auto client_type = it->first;
-    if (!std::ranges::contains(registered_clients_, client_type)) {
+    if (!base::Contains(registered_clients_, client_type)) {
       store_->Delete(ToDatabaseKey(client_type),
                      /*callback=*/base::DoNothing());
       client_states_.erase(it++);
diff --git a/chrome/browser/password_manager/android/unified_password_manager_proto_utils.cc b/chrome/browser/password_manager/android/unified_password_manager_proto_utils.cc
index 5c8684e61d..1cb5cad1d2 100644
--- a/chrome/browser/password_manager/android/unified_password_manager_proto_utils.cc
+++ b/chrome/browser/password_manager/android/unified_password_manager_proto_utils.cc
@@ -10,6 +10,7 @@
 #include "base/json/json_reader.h"
 #include "base/json/json_writer.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/browser/password_manager/android/protos/list_affiliated_passwords_result.pb.h"
 #include "chrome/browser/password_manager/android/protos/list_passwords_result.pb.h"
diff --git a/chrome/browser/password_manager/chrome_password_change_service.cc b/chrome/browser/password_manager/chrome_password_change_service.cc
index 1f80f7778c..f0622d6068 100644
--- a/chrome/browser/password_manager/chrome_password_change_service.cc
+++ b/chrome/browser/password_manager/chrome_password_change_service.cc
@@ -4,8 +4,6 @@
 
 #include "chrome/browser/password_manager/chrome_password_change_service.h"
 
-#include <utility>
-
 #include "base/command_line.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/task/single_thread_task_runner.h"
@@ -269,8 +267,8 @@ PasswordChangeAvailability ChromePasswordChangeService::GetGeneralAvailability()
 
   // The feature is disabled by enterprise policy.
   constexpr int kPolicyDisabled =
-      std::to_underlying(optimization_guide::model_execution::prefs::
-                             ModelExecutionEnterprisePolicyValue::kDisable);
+      base::to_underlying(optimization_guide::model_execution::prefs::
+                              ModelExecutionEnterprisePolicyValue::kDisable);
   if (pref_service_->GetInteger(
           optimization_guide::prefs::
               kAutomatedPasswordChangeEnterprisePolicyAllowed) ==
diff --git a/chrome/browser/password_manager/password_manager_captured_sites_interactive_uitest.cc b/chrome/browser/password_manager/password_manager_captured_sites_interactive_uitest.cc
index 74a74eb4ac..17f0b0f03c 100644
--- a/chrome/browser/password_manager/password_manager_captured_sites_interactive_uitest.cc
+++ b/chrome/browser/password_manager/password_manager_captured_sites_interactive_uitest.cc
@@ -394,12 +394,12 @@ class CapturedSitesAutomatedPasswordChangeBrowserTest
     PasswordChangeDelegate::State expected_state =
         static_cast<PasswordChangeDelegate::State>(state);
 
-    EXPECT_TRUE(std::ranges::contains(terminal_states, expected_state))
+    EXPECT_TRUE(base::Contains(terminal_states, expected_state))
         << "Unexpected password change state. It's possible to wait only for "
            "a terminal state.";
 
     return base::test::RunUntil([&]() {
-      if (std::ranges::contains(terminal_states, delegate->GetCurrentState())) {
+      if (base::Contains(terminal_states, delegate->GetCurrentState())) {
         EXPECT_EQ(expected_state, delegate->GetCurrentState())
             << "Password change reached an unexpected terminal state";
         return true;
diff --git a/chrome/browser/pdf/pdf_extension_util.cc b/chrome/browser/pdf/pdf_extension_util.cc
index 83bc44f0c1..f1f96d76c0 100644
--- a/chrome/browser/pdf/pdf_extension_util.cc
+++ b/chrome/browser/pdf/pdf_extension_util.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/notreached.h"
 #include "base/strings/string_util.h"
@@ -404,7 +405,7 @@ std::vector<webui::ResourcePath> GetResources(PdfViewerContext context) {
   std::vector<webui::ResourcePath> resources;
   resources.reserve(std::size(kPdfResources));
   for (const webui::ResourcePath& resource : kPdfResources) {
-    if (std::ranges::contains(exclusions, resource.path)) {
+    if (base::Contains(exclusions, resource.path)) {
       continue;
     }
     resources.push_back(resource);
diff --git a/chrome/browser/performance_manager/policies/background_tab_loading_policy.cc b/chrome/browser/performance_manager/policies/background_tab_loading_policy.cc
index c79f534856..33161796a7 100644
--- a/chrome/browser/performance_manager/policies/background_tab_loading_policy.cc
+++ b/chrome/browser/performance_manager/policies/background_tab_loading_policy.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/numerics/safe_conversions.h"
@@ -207,7 +208,7 @@ void BackgroundTabLoadingPolicy::OnLoadingStateChanged(
         // from |kLoading| to |kLoadedBusy|, so no change is necessary when it
         // transitions back to |kLoading|.
         DCHECK(page_nodes_loading_.contains(page_node));
-        DCHECK(!std::ranges::contains(page_nodes_load_initiated_, page_node));
+        DCHECK(!base::Contains(page_nodes_load_initiated_, page_node));
         DCHECK(!FindPageNodeToLoadData(page_node));
         return;
       }
@@ -224,7 +225,7 @@ void BackgroundTabLoadingPolicy::OnLoadingStateChanged(
 
       // Keep track of all PageNodes that are loading, even when the load isn't
       // initiated by this policy.
-      DCHECK(!page_nodes_loading_.contains(page_node));
+      DCHECK(!base::Contains(page_nodes_loading_, page_node));
       page_nodes_loading_.emplace(page_node, erased);
 
       if (erased_page_node_to_load) {
@@ -241,7 +242,7 @@ void BackgroundTabLoadingPolicy::OnLoadingStateChanged(
       // The PageNode should have been added to |page_nodes_loading_| when it
       // transitioned to |kLoading|.
       DCHECK(page_nodes_loading_.contains(page_node));
-      DCHECK(!std::ranges::contains(page_nodes_load_initiated_, page_node));
+      DCHECK(!base::Contains(page_nodes_load_initiated_, page_node));
       DCHECK(!FindPageNodeToLoadData(page_node));
       return;
     }
@@ -272,7 +273,7 @@ void BackgroundTabLoadingPolicy::ScheduleLoadForRestoredTabs(
 
     DCHECK_EQ(page_node->GetType(), PageType::kTab);
     DCHECK(!FindPageNodeToLoadData(page_node));
-    DCHECK(!std::ranges::contains(page_nodes_load_initiated_, page_node));
+    DCHECK(!base::Contains(page_nodes_load_initiated_, page_node));
 
     // Setting main frame restored state ensures that the notification
     // permission status and background title/favicon update properties are set
@@ -282,7 +283,7 @@ void BackgroundTabLoadingPolicy::ScheduleLoadForRestoredTabs(
         page_node_data.notification_permission_status);
 
     // No need to schedule a load if the page is already loading.
-    if (page_nodes_loading_.contains(page_node)) {
+    if (base::Contains(page_nodes_loading_, page_node)) {
       // Track that this policy was responsible for scheduling the load.
       page_nodes_loading_[page_node] = true;
       continue;
@@ -359,12 +360,12 @@ struct BackgroundTabLoadingPolicy::ScoredTabComparator {
 base::DictValue BackgroundTabLoadingPolicy::DescribePageNodeData(
     const PageNode* node) const {
   base::DictValue dict;
-  if (std::ranges::contains(page_nodes_load_initiated_, node)) {
+  if (base::Contains(page_nodes_load_initiated_, node)) {
     // Transient state between InitiateLoad() and OnLoadingStateChanged(),
     // shouldn't be sticking around for long.
     dict.Set("page_load_initiated", true);
   }
-  if (page_nodes_loading_.contains(node)) {
+  if (base::Contains(page_nodes_loading_, node)) {
     dict.Set("page_loading", true);
   }
   return !dict.empty() ? std::move(dict) : base::DictValue();
@@ -535,7 +536,7 @@ void BackgroundTabLoadingPolicy::InitiateLoad(const PageNode* page_node) {
     // Extra page nodes that weren't passed to ScheduleLoadForRestoredTabs() may
     // already be loading.
     if (to_load != page_node && page_nodes_loading_.contains(to_load)) {
-      DCHECK(!std::ranges::contains(page_nodes_load_initiated_, to_load));
+      DCHECK(!base::Contains(page_nodes_load_initiated_, to_load));
       continue;
     }
     InitiateSinglePageLoad(to_load);
@@ -545,7 +546,7 @@ void BackgroundTabLoadingPolicy::InitiateLoad(const PageNode* page_node) {
 void BackgroundTabLoadingPolicy::InitiateSinglePageLoad(
     const PageNode* page_node) {
   // The page shouldn't already be loading.
-  DCHECK(!std::ranges::contains(page_nodes_load_initiated_, page_node));
+  DCHECK(!base::Contains(page_nodes_load_initiated_, page_node));
   DCHECK(!page_nodes_loading_.contains(page_node));
   DCHECK_EQ(tabs_scored_, page_nodes_to_load_.size());
 
diff --git a/chrome/browser/permissions/chrome_permissions_client.cc b/chrome/browser/permissions/chrome_permissions_client.cc
index 0ca5585996..0bffa44c81 100644
--- a/chrome/browser/permissions/chrome_permissions_client.cc
+++ b/chrome/browser/permissions/chrome_permissions_client.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/check_deref.h"
 #include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
@@ -318,7 +319,7 @@ void ChromePermissionsClient::AreSitesImportant(
       registerable_domain = host;  // IP address or internal hostname.
     }
     entry.second =
-        std::ranges::contains(important_domains, registerable_domain,
+        base::Contains(important_domains, registerable_domain,
                               &site_engagement::ImportantSitesUtil::
                                   ImportantDomainInfo::registerable_domain);
   }
diff --git a/chrome/browser/policy/messaging_layer/upload/server_uploader.cc b/chrome/browser/policy/messaging_layer/upload/server_uploader.cc
index 2f782ce6ba..31ace46e7a 100644
--- a/chrome/browser/policy/messaging_layer/upload/server_uploader.cc
+++ b/chrome/browser/policy/messaging_layer/upload/server_uploader.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/task/bind_post_task.h"
 #include "base/task/sequenced_task_runner.h"
@@ -149,7 +150,7 @@ void ServerUploader::Finalize(CompletionResponse upload_result) {
         error::OUT_OF_RANGE,
     };
     LOG_IF(WARNING,
-           !std::ranges::contains(kIgnoredCodes, upload_result.error().code()))
+           !base::Contains(kIgnoredCodes, upload_result.error().code()))
         << upload_result.error();
   }
   Response(upload_result);
diff --git a/chrome/browser/policy/test/autofill_ai_policy_browsertest.cc b/chrome/browser/policy/test/autofill_ai_policy_browsertest.cc
index f969d84d42..531a9177fc 100644
--- a/chrome/browser/policy/test/autofill_ai_policy_browsertest.cc
+++ b/chrome/browser/policy/test/autofill_ai_policy_browsertest.cc
@@ -4,9 +4,9 @@
 
 #include <string>
 #include <unordered_map>
-#include <utility>
 
 #include "base/strings/strcat.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/browser/policy/policy_test_utils.h"
 #include "chrome/browser/profiles/profile.h"
@@ -38,9 +38,9 @@ using enum ModelExecutionEnterprisePolicyValue;
 
 // The policy's value are 0, 1, 2 and Autofill expects that the enum
 // ModelExecutionEnterprisePolicyValue matches those values.
-static_assert(std::to_underlying(kAllow) == 0);
-static_assert(std::to_underlying(kAllowWithoutLogging) == 1);
-static_assert(std::to_underlying(kDisable) == 2);
+static_assert(base::to_underlying(kAllow) == 0);
+static_assert(base::to_underlying(kAllowWithoutLogging) == 1);
+static_assert(base::to_underlying(kDisable) == 2);
 
 // This test has two parameters:
 //  * Policy value.
@@ -86,7 +86,7 @@ class AutofillAiPolicyTest
 
     PolicyMap policies;
     SetPolicy(&policies, key::kAutofillPredictionSettings,
-              base::Value(std::to_underlying(policy_value())));
+              base::Value(base::to_underlying(policy_value())));
     UpdateProviderPolicy(policies);
 
     // The base test fixture creates a tab before we set the policy. We create a
diff --git a/chrome/browser/predictors/autocomplete_action_predictor.cc b/chrome/browser/predictors/autocomplete_action_predictor.cc
index d93f3f02f2..31f4877fdd 100644
--- a/chrome/browser/predictors/autocomplete_action_predictor.cc
+++ b/chrome/browser/predictors/autocomplete_action_predictor.cc
@@ -10,6 +10,7 @@
 #include <algorithm>
 #include <queue>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/i18n/case_conversion.h"
 #include "base/metrics/histogram_functions.h"
@@ -184,7 +185,7 @@ void AutocompleteActionPredictor::RegisterTransitionalMatches(
   for (const auto& match : result) {
     const GURL& url = match.destination_url;
     const size_t size = url.spec().size();
-    if (!std::ranges::contains(match_it->urls, url) &&
+    if (!base::Contains(match_it->urls, url) &&
         size <= kMaximumStringLength &&
         transitional_matches_size_ + size <= kMaximumTransitionalMatchesSize) {
       match_it->urls.push_back(url);
diff --git a/chrome/browser/preloading/prefetch/search_prefetch/search_preload_unified_browsertest.cc b/chrome/browser/preloading/prefetch/search_prefetch/search_preload_unified_browsertest.cc
index accba4f684..73c5617ad6 100644
--- a/chrome/browser/preloading/prefetch/search_prefetch/search_preload_unified_browsertest.cc
+++ b/chrome/browser/preloading/prefetch/search_prefetch/search_preload_unified_browsertest.cc
@@ -2357,11 +2357,11 @@ IN_PROC_BROWSER_TEST_F(SearchPreloadUnifiedBrowserTest,
   std::string inner_html = content::EvalJs(GetActiveWebContents(),
                                            "document.documentElement.innerHTML")
                                .ExtractString();
-  EXPECT_TRUE(inner_html.contains("PREFETCH"));
+  EXPECT_TRUE(base::Contains(inner_html, "PREFETCH"));
   std::string prefetch_inner_html =
       content::EvalJs(new_prefetch_tab, "document.documentElement.innerHTML")
           .ExtractString();
-  EXPECT_TRUE(prefetch_inner_html.contains("PREFETCH"));
+  EXPECT_TRUE(base::Contains(prefetch_inner_html, "PREFETCH"));
   EXPECT_EQ(0, prerender_helper().GetRequestCount(expected_prerender_url));
   EXPECT_EQ(2, prerender_helper().GetRequestCount(expected_prefetch_url));
 
diff --git a/chrome/browser/printing/pwg_raster_converter.cc b/chrome/browser/printing/pwg_raster_converter.cc
index ea1bd4981b..27650da4e0 100644
--- a/chrome/browser/printing/pwg_raster_converter.cc
+++ b/chrome/browser/printing/pwg_raster_converter.cc
@@ -11,6 +11,7 @@
 
 #include "base/cancelable_callback.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/notreached.h"
@@ -287,7 +288,7 @@ PwgRasterSettings PwgRasterConverter::GetBitmapSettings(
   const auto& types = raster_capability.value().document_types_supported;
   result.use_color =
       use_color ||
-      !std::ranges::contains(
+      !base::Contains(
           types, cloud_devices::printer::PwgDocumentTypeSupported::SGRAY_8);
 
   return result;
diff --git a/chrome/browser/profiles/batch_upload/batch_upload_service.cc b/chrome/browser/profiles/batch_upload/batch_upload_service.cc
index 065156b9ab..1c69fbf4f4 100644
--- a/chrome/browser/profiles/batch_upload/batch_upload_service.cc
+++ b/chrome/browser/profiles/batch_upload/batch_upload_service.cc
@@ -9,6 +9,7 @@
 #include <map>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -88,7 +89,7 @@ syncer::LocalDataDescription GetPrimaryTypeLocalDataDescription(
     const std::map<syncer::DataType, syncer::LocalDataDescription>&
         local_data_descriptions_map,
     syncer::DataType primary_type) {
-  CHECK(std::ranges::contains(kBatchUploadAvailableTypesOrder, primary_type));
+  CHECK(base::Contains(kBatchUploadAvailableTypesOrder, primary_type));
   CHECK(local_data_descriptions_map.contains(primary_type));
   const syncer::LocalDataDescription& primary_local_data_description =
       local_data_descriptions_map.at(primary_type);
diff --git a/chrome/browser/profiles/profile_attributes_entry.cc b/chrome/browser/profiles/profile_attributes_entry.cc
index 37210cb33b..c0ea5757c2 100644
--- a/chrome/browser/profiles/profile_attributes_entry.cc
+++ b/chrome/browser/profiles/profile_attributes_entry.cc
@@ -13,6 +13,7 @@
 #include "base/notreached.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "build/chromeos_buildflags.h"
 #include "chrome/app/vector_icons/vector_icons.h"
@@ -597,9 +598,9 @@ std::string ProfileAttributesEntry::GetHostedDomain() const {
 
 signin::Tribool ProfileAttributesEntry::GetIsManaged() const {
   static_assert(kIntegerNotSet ==
-                std::to_underlying(signin::Tribool::kUnknown));
-  static_assert(std::to_underlying(signin::Tribool::kFalse) == 0);
-  static_assert(std::to_underlying(signin::Tribool::kTrue) == 1);
+                base::to_underlying(signin::Tribool::kUnknown));
+  static_assert(base::to_underlying(signin::Tribool::kFalse) == 0);
+  static_assert(base::to_underlying(signin::Tribool::kTrue) == 1);
 
   int value = GetInteger(kIsManaged);
 
@@ -615,7 +616,7 @@ signin::Tribool ProfileAttributesEntry::GetIsManaged() const {
 
   // If the value is invalid, or is not a valid Tribool value, return unknown.
   if (value < kIntegerNotSet ||
-      value > std::to_underlying(signin::Tribool::kTrue)) {
+      value > base::to_underlying(signin::Tribool::kTrue)) {
     return signin::Tribool::kUnknown;
   }
   return static_cast<signin::Tribool>(value);
@@ -859,7 +860,7 @@ void ProfileAttributesEntry::SetHostedDomain(std::string hosted_domain) {
 }
 
 void ProfileAttributesEntry::SetIsManaged(signin::Tribool value) {
-  if (SetInteger(kIsManaged, std::to_underlying(value))) {
+  if (SetInteger(kIsManaged, base::to_underlying(value))) {
     profile_attributes_storage_->NotifyProfileIsManagedChanged(GetPath());
   }
 }
diff --git a/chrome/browser/profiles/profile_manager.cc b/chrome/browser/profiles/profile_manager.cc
index c278b86f0e..973866b0fb 100644
--- a/chrome/browser/profiles/profile_manager.cc
+++ b/chrome/browser/profiles/profile_manager.cc
@@ -16,6 +16,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/debug/stack_trace.h"
@@ -891,7 +892,7 @@ bool ProfileManager::IsValidProfile(const void* profile) {
       return true;
     std::vector<Profile*> otr_profiles =
         candidate->GetAllOffTheRecordProfiles();
-    if (std::ranges::contains(otr_profiles, profile)) {
+    if (base::Contains(otr_profiles, profile)) {
       return true;
     }
   }
@@ -1414,7 +1415,7 @@ void ProfileManager::RemoveKeepAlive(Profile* profile,
     return;
   }
 
-  DCHECK(info->keep_alives.contains(origin));
+  DCHECK(base::Contains(info->keep_alives, origin));
 
 #if !BUILDFLAG(IS_ANDROID)
   // When removing the last keep alive of an ephemeral profile, schedule the
@@ -1744,7 +1745,7 @@ Profile* ProfileManager::GetActiveUserOrOffTheRecordProfile() {
 void ProfileManager::UnloadProfile(const base::FilePath& profile_dir) {
   TRACE_EVENT0("browser", "ProfileManager::UnloadProfile");
 
-  DCHECK(profiles_info_.contains(profile_dir));
+  DCHECK(base::Contains(profiles_info_, profile_dir));
 
   bool ephemeral =
       IsRegisteredAsEphemeral(&GetProfileAttributesStorage(), profile_dir);
diff --git a/chrome/browser/promos/promos_utils.cc b/chrome/browser/promos/promos_utils.cc
index 3e696a5ddc..455e9dd939 100644
--- a/chrome/browser/promos/promos_utils.cc
+++ b/chrome/browser/promos/promos_utils.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/json/values_util.h"
 #include "base/metrics/histogram_functions.h"
@@ -459,16 +460,16 @@ bool ShouldShowIOSDesktopPromo(Profile* profile,
 bool UserNotClassifiedAsMobileDeviceSwitcher(
     const segmentation_platform::ClassificationResult& result) {
   return result.status == segmentation_platform::PredictionStatus::kSucceeded &&
-         !std::ranges::contains(
+         !base::Contains(
              result.ordered_labels,
              segmentation_platform::DeviceSwitcherModel::kAndroidPhoneLabel) &&
-         !std::ranges::contains(result.ordered_labels,
+         !base::Contains(result.ordered_labels,
                                 segmentation_platform::DeviceSwitcherModel::
                                     kIosPhoneChromeLabel) &&
-         !std::ranges::contains(
+         !base::Contains(
              result.ordered_labels,
              segmentation_platform::DeviceSwitcherModel::kAndroidTabletLabel) &&
-         !std::ranges::contains(
+         !base::Contains(
              result.ordered_labels,
              segmentation_platform::DeviceSwitcherModel::kIosTabletLabel);
 }
diff --git a/chrome/browser/safe_browsing/chrome_password_protection_service.cc b/chrome/browser/safe_browsing/chrome_password_protection_service.cc
index 0ba7511610..6f69608367 100644
--- a/chrome/browser/safe_browsing/chrome_password_protection_service.cc
+++ b/chrome/browser/safe_browsing/chrome_password_protection_service.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
@@ -1777,7 +1778,7 @@ bool ChromePasswordProtectionService::IsInExcludedCountry() {
       g_browser_process->variations_service();
   if (!variations_service)
     return false;
-  return std::ranges::contains(GetExcludedCountries(),
+  return base::Contains(GetExcludedCountries(),
                                variations_service->GetLatestCountry());
 }
 
diff --git a/chrome/browser/safe_browsing/extension_telemetry/potential_password_theft_signal_processor.cc b/chrome/browser/safe_browsing/extension_telemetry/potential_password_theft_signal_processor.cc
index f650b44118..e3dffce29d 100644
--- a/chrome/browser/safe_browsing/extension_telemetry/potential_password_theft_signal_processor.cc
+++ b/chrome/browser/safe_browsing/extension_telemetry/potential_password_theft_signal_processor.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "chrome/browser/safe_browsing/extension_telemetry/extension_telemetry_service.h"
 #include "chrome/browser/safe_browsing/extension_telemetry/password_reuse_signal.h"
 #include "chrome/browser/safe_browsing/extension_telemetry/remote_host_contacted_signal.h"
@@ -112,7 +113,7 @@ void PotentialPasswordTheftSignalProcessor::UpdateDataStores(
       // Each password reuse event has a reputable domain list, aka
       // matching_domains. Remote host urls that are in the reputable domain
       // lists associated with the password reuse events will not be added.
-      if (!std::ranges::contains(matching_domains, host_url)) {
+      if (!base::Contains(matching_domains, host_url)) {
         temp_remote_host_url_queue.push_back(host_url);
       }
     }
@@ -158,7 +159,7 @@ void PotentialPasswordTheftSignalProcessor::UpdatePasswordReuseInfo(
   reuse_info.count += 1;
   for (auto& domain : incoming_reuse_info.matching_domains) {
     // Update the matching domain list once we find new domains.
-    if (!std::ranges::contains(matching_domains, domain)) {
+    if (!base::Contains(matching_domains, domain)) {
       matching_domains.push_back(domain);
     }
   }
diff --git a/chrome/browser/safe_browsing/generated_safe_browsing_pref.cc b/chrome/browser/safe_browsing/generated_safe_browsing_pref.cc
index 8c3b2d874a..0e39fd45b0 100644
--- a/chrome/browser/safe_browsing/generated_safe_browsing_pref.cc
+++ b/chrome/browser/safe_browsing/generated_safe_browsing_pref.cc
@@ -4,8 +4,7 @@
 
 #include "chrome/browser/safe_browsing/generated_safe_browsing_pref.h"
 
-#include <utility>
-
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/common/extensions/api/settings_private.h"
 #include "components/prefs/pref_service.h"
@@ -166,9 +165,9 @@ void GeneratedSafeBrowsingPref::ApplySafeBrowsingManagementState(
 
     pref_object.user_selectable_values.emplace();
     pref_object.user_selectable_values->Append(
-        std::to_underlying(SafeBrowsingState::STANDARD_PROTECTION));
+        base::to_underlying(SafeBrowsingState::STANDARD_PROTECTION));
     pref_object.user_selectable_values->Append(
-        std::to_underlying(SafeBrowsingState::NO_SAFE_BROWSING));
+        base::to_underlying(SafeBrowsingState::NO_SAFE_BROWSING));
   }
 }
 
diff --git a/chrome/browser/safe_browsing/generated_security_settings_bundle_pref.cc b/chrome/browser/safe_browsing/generated_security_settings_bundle_pref.cc
index 68f373769a..633e09fc62 100644
--- a/chrome/browser/safe_browsing/generated_security_settings_bundle_pref.cc
+++ b/chrome/browser/safe_browsing/generated_security_settings_bundle_pref.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/safe_browsing/generated_security_settings_bundle_pref.h"
 
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/browser_process.h"
 #include "chrome/browser/net/secure_dns_config.h"
 #include "chrome/browser/profiles/profile.h"
diff --git a/chrome/browser/safe_browsing/test_safe_browsing_database_helper.cc b/chrome/browser/safe_browsing/test_safe_browsing_database_helper.cc
index deb73f12c2..39fdc738ba 100644
--- a/chrome/browser/safe_browsing/test_safe_browsing_database_helper.cc
+++ b/chrome/browser/safe_browsing/test_safe_browsing_database_helper.cc
@@ -62,7 +62,7 @@ class InsertingDatabaseFactory : public safe_browsing::TestV4DatabaseFactory {
       std::unique_ptr<safe_browsing::StoreMap> store_map) override {
     const base::FilePath base_store_path(FILE_PATH_LITERAL("UrlDb.store"));
     for (const auto& id : lists_to_insert_) {
-      if (!store_map->contains(id)) {
+      if (!base::Contains(*store_map, id)) {
         const base::FilePath store_path = base::GetUniquePath(base_store_path);
         store_map->insert(
             {id, store_factory_->CreateV4Store(
diff --git a/chrome/browser/share/share_ranking.cc b/chrome/browser/share/share_ranking.cc
index 23df083c44..42ddad434f 100644
--- a/chrome/browser/share/share_ranking.cc
+++ b/chrome/browser/share/share_ranking.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/containers/to_vector.h"
 #include "base/functional/callback_helpers.h"
@@ -113,7 +114,7 @@ std::vector<std::string> ReplaceUnavailableEntries(
   std::vector<std::string> result;
   std::ranges::transform(
       ranking, std::back_inserter(result), [&](const std::string& e) {
-        return std::ranges::contains(available, e) ? e : std::string();
+        return base::Contains(available, e) ? e : std::string();
       });
   return result;
 }
@@ -127,7 +128,7 @@ void FillGaps(std::vector<std::string>& ranking,
   std::vector<std::string> unused_available =
       base::ToVector(base::span(ranking).subspan(length));
   std::erase_if(unused_available, [&](const std::string& e) {
-    return !std::ranges::contains(available, e);
+    return !base::Contains(available, e);
   });
 
   // Now, append the rest of the system apps (those not already included) to
@@ -136,7 +137,7 @@ void FillGaps(std::vector<std::string>& ranking,
   // all, so these are the lowest priority targets, because they are likely to
   // be things like "Bluetooth" which users almost never share to.
   for (const auto& app : available) {
-    if (!std::ranges::contains(unused_available, app)) {
+    if (!base::Contains(unused_available, app)) {
       unused_available.push_back(app);
     }
   }
@@ -217,7 +218,7 @@ ShareRanking::Ranking AppendUpToLength(
   ShareRanking::Ranking all = OrderByUses(history_keys, history);
   ShareRanking::Ranking result = ranking;
   while (result.size() < length && !all.empty()) {
-    if (!std::ranges::contains(result, all.front())) {
+    if (!base::Contains(result, all.front())) {
       result.push_back(all.front());
       all.erase(all.begin());
     }
@@ -238,7 +239,7 @@ void RunJniRankCallback(base::android::ScopedJavaGlobalRef<jobject> callback,
 bool EveryElementInList(const std::vector<std::string>& ranking,
                         const std::vector<std::string>& available) {
   for (const auto& e : ranking) {
-    if (!std::ranges::contains(available, e)) {
+    if (!base::Contains(available, e)) {
       return false;
     }
   }
@@ -249,7 +250,7 @@ bool ElementIndexesAreUnchanged(const std::vector<std::string>& display,
                                 const std::vector<std::string>& old,
                                 size_t length) {
   for (size_t i = 0u; i < display.size() && i < length; ++i) {
-    if (std::ranges::contains(base::span(old).subspan(/*offset=*/0u, length),
+    if (base::Contains(base::span(old).subspan(/*offset=*/0u, length),
                               display[i]) &&
         display[i] != old[i]) {
       return false;
@@ -288,7 +289,7 @@ std::vector<std::string> AddMissingItemsFromHistory(
     const std::map<std::string, int>& history) {
   std::vector<std::string> updated = existing;
   for (const auto& item : history) {
-    if (!std::ranges::contains(updated, item.first)) {
+    if (!base::Contains(updated, item.first)) {
       updated.push_back(item.first);
     }
   }
@@ -450,7 +451,7 @@ void ShareRanking::ComputeRanking(
     DCHECK(ElementIndexesAreUnchanged(*display_ranking, old_ranking, fold - 1));
     DCHECK(AtMostOneSlotChanged(old_ranking, *persisted_ranking, fold - 1));
 
-    DCHECK(std::ranges::contains(*display_ranking, kMoreTarget));
+    DCHECK(base::Contains(*display_ranking, kMoreTarget));
   }
 #endif  // DCHECK_IS_ON()
 
diff --git a/chrome/browser/signin/dice_intercepted_session_startup_helper.cc b/chrome/browser/signin/dice_intercepted_session_startup_helper.cc
index da26bf0dc8..0a5411ea6d 100644
--- a/chrome/browser/signin/dice_intercepted_session_startup_helper.cc
+++ b/chrome/browser/signin/dice_intercepted_session_startup_helper.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/task/single_thread_task_runner.h"
@@ -34,7 +35,7 @@ bool CookieInfoContains(const signin::AccountsInCookieJarInfo& cookie_info,
                         const CoreAccountId& account_id) {
   const std::vector<gaia::ListedAccount>& accounts =
       cookie_info.GetPotentiallyInvalidSignedInAccounts();
-  return std::ranges::contains(accounts, account_id, &gaia::ListedAccount::id);
+  return base::Contains(accounts, account_id, &gaia::ListedAccount::id);
 }
 
 }  // namespace
diff --git a/chrome/browser/speech/on_device_speech_recognition_impl.cc b/chrome/browser/speech/on_device_speech_recognition_impl.cc
index f4b2ff49a2..c2efd505c5 100644
--- a/chrome/browser/speech/on_device_speech_recognition_impl.cc
+++ b/chrome/browser/speech/on_device_speech_recognition_impl.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "chrome/browser/browser_process.h"
 #include "chrome/browser/content_settings/host_content_settings_map_factory.h"
@@ -40,7 +41,7 @@ const char kEnglishLanguageCodeKey[] = "en-US";
 // Returns a boolean indicating whether the language is enabled.
 bool IsLanguageInstallable(std::string_view language_code,
                            bool is_soda_binary_installed) {
-  return std::ranges::contains(
+  return base::Contains(
       speech::SodaInstaller::GetInstance()->GetLiveCaptionEnabledLanguages(),
       language_code);
 }
diff --git a/chrome/browser/spellchecker/spellcheck_service.cc b/chrome/browser/spellchecker/spellcheck_service.cc
index c138bc5b00..d1057a12d0 100644
--- a/chrome/browser/spellchecker/spellcheck_service.cc
+++ b/chrome/browser/spellchecker/spellcheck_service.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/no_destructor.h"
@@ -348,7 +349,7 @@ void SpellcheckService::EnableFirstUserLanguageForSpellcheck(
   std::vector<std::string> accept_languages;
   l10n_util::GetAcceptLanguages(&accept_languages);
   for (const auto& user_language : user_languages) {
-    if (std::ranges::contains(accept_languages, user_language)) {
+    if (base::Contains(accept_languages, user_language)) {
       first_user_language = user_language;
       break;
     }
@@ -755,7 +756,7 @@ bool SpellcheckService::HasPrivateUseSubTag(const std::string& full_tag) {
 
   // Private use subtags are separated from the other subtags by the reserved
   // single-character subtag 'x'.
-  return std::ranges::contains(subtags, "x");
+  return base::Contains(subtags, "x");
 }
 
 // static
@@ -865,7 +866,7 @@ void SpellcheckService::OnAcceptLanguagesChanged() {
   std::vector<std::string> filtered_dictionaries;
 
   for (const auto& dictionary : dictionaries) {
-    if (std::ranges::contains(accept_languages, dictionary)) {
+    if (base::Contains(accept_languages, dictionary)) {
       filtered_dictionaries.push_back(dictionary);
     }
   }
diff --git a/chrome/browser/ssl/https_only_mode_tab_helper.h b/chrome/browser/ssl/https_only_mode_tab_helper.h
index 74603b9bba..7cadf1fb46 100644
--- a/chrome/browser/ssl/https_only_mode_tab_helper.h
+++ b/chrome/browser/ssl/https_only_mode_tab_helper.h
@@ -5,6 +5,7 @@
 #ifndef CHROME_BROWSER_SSL_HTTPS_ONLY_MODE_TAB_HELPER_H_
 #define CHROME_BROWSER_SSL_HTTPS_ONLY_MODE_TAB_HELPER_H_
 
+#include "base/containers/contains.h"
 #include "content/public/browser/web_contents_observer.h"
 #include "content/public/browser/web_contents_user_data.h"
 
diff --git a/chrome/browser/supervised_user/supervised_user_extensions_manager.cc b/chrome/browser/supervised_user/supervised_user_extensions_manager.cc
index ca90de9c9a..29e3545238 100644
--- a/chrome/browser/supervised_user/supervised_user_extensions_manager.cc
+++ b/chrome/browser/supervised_user/supervised_user_extensions_manager.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/values.h"
@@ -123,7 +124,7 @@ void SupervisedUserExtensionsManager::AddExtensionApproval(
   if (!is_active_policy_for_supervised_users_) {
     return;
   }
-  if (!approved_extensions_set_.contains(extension.id())) {
+  if (!base::Contains(approved_extensions_set_, extension.id())) {
     UpdateApprovedExtension(extension.id(), extension.VersionString(),
                             ApprovedExtensionChange::kAdd);
   }
@@ -146,7 +147,7 @@ void SupervisedUserExtensionsManager::RemoveExtensionApproval(
   if (!is_active_policy_for_supervised_users_) {
     return;
   }
-  if (approved_extensions_set_.contains(extension.id())) {
+  if (base::Contains(approved_extensions_set_, extension.id())) {
     UpdateApprovedExtension(extension.id(), extension.VersionString(),
                             ApprovedExtensionChange::kRemove);
   }
@@ -236,7 +237,7 @@ void SupervisedUserExtensionsManager::OnExtensionInstalled(
       return;
     }
     CHECK(extension);
-    if (!approved_extensions_set_.contains(extension->id())) {
+    if (!base::Contains(approved_extensions_set_, extension->id())) {
       AddExtensionApproval(*extension);
     }
   }
@@ -253,7 +254,7 @@ void SupervisedUserExtensionsManager::OnExtensionUninstalled(
   if (!is_active_policy_for_supervised_users_) {
     return;
   }
-  if (approved_extensions_set_.contains(extension->id())) {
+  if (base::Contains(approved_extensions_set_, extension->id())) {
     UpdateApprovedExtension(extension->id(), extension->VersionString(),
                             ApprovedExtensionChange::kRemove);
   }
@@ -288,7 +289,7 @@ SupervisedUserExtensionsManager::GetExtensionState(
     return SupervisedUserExtensionsManager::ExtensionState::kAllowed;
   }
 
-  if (std::ranges::contains(kAllowlistExtensionIds, extension.id())) {
+  if (base::Contains(kAllowlistExtensionIds, extension.id())) {
     return SupervisedUserExtensionsManager::ExtensionState::kAllowed;
   }
 
@@ -296,7 +297,7 @@ SupervisedUserExtensionsManager::GetExtensionState(
     return SupervisedUserExtensionsManager::ExtensionState::kBlocked;
   }
 
-  if (approved_extensions_set_.contains(extension.id())) {
+  if (base::Contains(approved_extensions_set_, extension.id())) {
     return SupervisedUserExtensionsManager::ExtensionState::kAllowed;
   }
   if (IsLocallyParentApprovedExtension(extension.id())) {
@@ -498,7 +499,7 @@ bool SupervisedUserExtensionsManager::IsLocallyParentApprovedExtension(
     const std::string& extension_id) const {
   const base::DictValue& current_locally_approved_dict = user_prefs_->GetDict(
       prefs::kSupervisedUserLocallyParentApprovedExtensions);
-  return current_locally_approved_dict.contains(extension_id);
+  return base::Contains(current_locally_approved_dict, extension_id);
 }
 
 void SupervisedUserExtensionsManager::RemoveLocalParentalApproval(
diff --git a/chrome/browser/support_tool/ash/shill_data_collector.cc b/chrome/browser/support_tool/ash/shill_data_collector.cc
index 28951e4121..62c5babc9a 100644
--- a/chrome/browser/support_tool/ash/shill_data_collector.cc
+++ b/chrome/browser/support_tool/ash/shill_data_collector.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/support_tool/ash/shill_data_collector.h"
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -116,8 +117,9 @@ void DetectOrScrubPIIInDictionary(
     // values don't contain PII anyway.
     if (HasEmptyContents(entry.second))
       continue;
-    if (scrub && !pii_types_to_keep.contains(
-                     system_logs::kShillPIIMaskedMap.at(entry.first))) {
+    if (scrub &&
+        !base::Contains(pii_types_to_keep,
+                        system_logs::kShillPIIMaskedMap.at(entry.first))) {
       entry.second = base::Value(kMaskedString);
       continue;
     }
diff --git a/chrome/browser/support_tool/ash/system_logs_data_collector.cc b/chrome/browser/support_tool/ash/system_logs_data_collector.cc
index abbb343924..edf4801575 100644
--- a/chrome/browser/support_tool/ash/system_logs_data_collector.cc
+++ b/chrome/browser/support_tool/ash/system_logs_data_collector.cc
@@ -12,6 +12,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
diff --git a/chrome/browser/support_tool/policy_data_collector.cc b/chrome/browser/support_tool/policy_data_collector.cc
index abafbd977f..3f5fbca896 100644
--- a/chrome/browser/support_tool/policy_data_collector.cc
+++ b/chrome/browser/support_tool/policy_data_collector.cc
@@ -184,7 +184,7 @@ void PolicyDataCollector::RedactPIIInPolicyStatus(
          entry.second.GetDict()) {
       std::optional<redaction::PIIType> pii_type =
           GetPIITypeOfStatusField(status_pair.first);
-      if (!pii_type || pii_types_to_keep.contains(pii_type.value())) {
+      if (!pii_type || base::Contains(pii_types_to_keep, pii_type.value())) {
         continue;
       }
       // Replace the value with `kRedactedPlaceholder` if it's a PII type that
diff --git a/chrome/browser/sync/test/integration/web_apps/two_client_web_apps_generated_icon_fix_test.cc b/chrome/browser/sync/test/integration/web_apps/two_client_web_apps_generated_icon_fix_test.cc
index 83c84c9c5c..4036cba948 100644
--- a/chrome/browser/sync/test/integration/web_apps/two_client_web_apps_generated_icon_fix_test.cc
+++ b/chrome/browser/sync/test/integration/web_apps/two_client_web_apps_generated_icon_fix_test.cc
@@ -146,7 +146,7 @@ class TwoClientGeneratedIconFixSyncTest
         .ReadTrustedIconsWithFallbackToManifestIcons(
             app_id, {kIconSize}, IconPurpose::ANY, icons_future.GetCallback());
     SizeToBitmap icons_bitmap = std::move(icons_future.Take().icons_map);
-    CHECK(icons_bitmap.contains(kIconSize));
+    CHECK(base::Contains(icons_bitmap, kIconSize));
 
     return {
         .is_generated = fake_providers_[profile]
diff --git a/chrome/browser/sync_file_system/drive_backend/metadata_database.cc b/chrome/browser/sync_file_system/drive_backend/metadata_database.cc
index 22389344f3..3d48aa3909 100644
--- a/chrome/browser/sync_file_system/drive_backend/metadata_database.cc
+++ b/chrome/browser/sync_file_system/drive_backend/metadata_database.cc
@@ -11,6 +11,7 @@
 
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -1370,7 +1371,7 @@ void MetadataDatabase::MaybeAddTrackersForNewFile(
       if (!parent_tracker.active())
         continue;
 
-      if (parents_to_exclude.contains(parent_tracker.tracker_id())) {
+      if (base::Contains(parents_to_exclude, parent_tracker.tracker_id())) {
         continue;
       }
 
diff --git a/chrome/browser/sync_file_system/drive_backend/metadata_database_index.cc b/chrome/browser/sync_file_system/drive_backend/metadata_database_index.cc
index 16ef6033a1..78cdc264ab 100644
--- a/chrome/browser/sync_file_system/drive_backend/metadata_database_index.cc
+++ b/chrome/browser/sync_file_system/drive_backend/metadata_database_index.cc
@@ -7,6 +7,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/notreached.h"
 #include "base/strings/string_number_conversions.h"
@@ -488,7 +489,7 @@ void MetadataDatabaseIndex::AddToAppIDIndex(
   DVLOG(3) << "  Add to app_root_by_app_id_: " << new_tracker.app_id();
 
   DCHECK(new_tracker.active());
-  DCHECK(!app_root_by_app_id_.contains(new_tracker.app_id()));
+  DCHECK(!base::Contains(app_root_by_app_id_, new_tracker.app_id()));
   app_root_by_app_id_[new_tracker.app_id()] = new_tracker.tracker_id();
 }
 
@@ -500,7 +501,7 @@ void MetadataDatabaseIndex::UpdateInAppIDIndex(
   if (IsAppRoot(old_tracker) && !IsAppRoot(new_tracker)) {
     DCHECK(old_tracker.active());
     DCHECK(!new_tracker.active());
-    DCHECK(app_root_by_app_id_.contains(old_tracker.app_id()));
+    DCHECK(base::Contains(app_root_by_app_id_, old_tracker.app_id()));
 
     DVLOG(3) << "  Remove from app_root_by_app_id_: " << old_tracker.app_id();
 
@@ -508,7 +509,7 @@ void MetadataDatabaseIndex::UpdateInAppIDIndex(
   } else if (!IsAppRoot(old_tracker) && IsAppRoot(new_tracker)) {
     DCHECK(!old_tracker.active());
     DCHECK(new_tracker.active());
-    DCHECK(!app_root_by_app_id_.contains(new_tracker.app_id()));
+    DCHECK(!base::Contains(app_root_by_app_id_, new_tracker.app_id()));
 
     DVLOG(3) << "  Add to app_root_by_app_id_: " << new_tracker.app_id();
 
@@ -520,7 +521,7 @@ void MetadataDatabaseIndex::RemoveFromAppIDIndex(
     const FileTracker& tracker) {
   if (IsAppRoot(tracker)) {
     DCHECK(tracker.active());
-    DCHECK(app_root_by_app_id_.contains(tracker.app_id()));
+    DCHECK(base::Contains(app_root_by_app_id_, tracker.app_id()));
 
     DVLOG(3) << "  Remove from app_root_by_app_id_: " << tracker.app_id();
 
@@ -535,7 +536,7 @@ void MetadataDatabaseIndex::AddToFileIDIndexes(
   trackers_by_file_id_[new_tracker.file_id()].Insert(new_tracker);
 
   if (trackers_by_file_id_[new_tracker.file_id()].size() > 1) {
-    DVLOG_IF(3, !multi_tracker_file_ids_.contains(new_tracker.file_id()))
+    DVLOG_IF(3, !base::Contains(multi_tracker_file_ids_, new_tracker.file_id()))
         << "  Add to multi_tracker_file_ids_: " << new_tracker.file_id();
     multi_tracker_file_ids_.insert(new_tracker.file_id());
   }
@@ -548,7 +549,7 @@ void MetadataDatabaseIndex::UpdateInFileIDIndexes(
   DCHECK_EQ(old_tracker.file_id(), new_tracker.file_id());
 
   std::string file_id = new_tracker.file_id();
-  DCHECK(trackers_by_file_id_.contains(file_id));
+  DCHECK(base::Contains(trackers_by_file_id_, file_id));
 
   if (old_tracker.active() && !new_tracker.active())
     trackers_by_file_id_[file_id].Deactivate(new_tracker.tracker_id());
@@ -568,7 +569,7 @@ void MetadataDatabaseIndex::RemoveFromFileIDIndexes(
   found->second.Erase(tracker.tracker_id());
 
   if (trackers_by_file_id_[tracker.file_id()].size() <= 1) {
-    DVLOG_IF(3, multi_tracker_file_ids_.contains(tracker.file_id()))
+    DVLOG_IF(3, base::Contains(multi_tracker_file_ids_, tracker.file_id()))
         << "  Remove from multi_tracker_file_ids_: " << tracker.file_id();
     multi_tracker_file_ids_.erase(tracker.file_id());
   }
@@ -589,8 +590,8 @@ void MetadataDatabaseIndex::AddToPathIndexes(
 
   if (trackers_by_parent_and_title_[parent][title].size() > 1 &&
       !title.empty()) {
-    DVLOG_IF(
-        3, !multi_backing_file_paths_.contains(ParentIDAndTitle(parent, title)))
+    DVLOG_IF(3, !base::Contains(multi_backing_file_paths_,
+                                ParentIDAndTitle(parent, title)))
         << "  Add to multi_backing_file_paths_: " << parent << " " << title;
     multi_backing_file_paths_.insert(ParentIDAndTitle(parent, title));
   }
@@ -631,8 +632,8 @@ void MetadataDatabaseIndex::UpdateInPathIndexes(
 
     if (trackers_by_parent_and_title_[parent][old_title].size() <= 1 &&
         !old_title.empty()) {
-      DVLOG_IF(3, multi_backing_file_paths_.contains(
-                      ParentIDAndTitle(parent, old_title)))
+      DVLOG_IF(3, base::Contains(multi_backing_file_paths_,
+                                 ParentIDAndTitle(parent, old_title)))
           << "  Remove from multi_backing_file_paths_: " << parent << " "
           << old_title;
       multi_backing_file_paths_.erase(ParentIDAndTitle(parent, old_title));
@@ -640,8 +641,8 @@ void MetadataDatabaseIndex::UpdateInPathIndexes(
 
     if (trackers_by_parent_and_title_[parent][title].size() > 1 &&
         !title.empty()) {
-      DVLOG_IF(3, !multi_backing_file_paths_.contains(
-                      ParentIDAndTitle(parent, title)))
+      DVLOG_IF(3, !base::Contains(multi_backing_file_paths_,
+                                  ParentIDAndTitle(parent, title)))
           << "  Add to multi_backing_file_paths_: " << parent << " " << title;
       multi_backing_file_paths_.insert(ParentIDAndTitle(parent, title));
     }
@@ -661,8 +662,8 @@ void MetadataDatabaseIndex::RemoveFromPathIndexes(
   int64_t parent = tracker.parent_tracker_id();
   std::string title = GetTrackerTitle(tracker);
 
-  DCHECK(trackers_by_parent_and_title_.contains(parent));
-  DCHECK(trackers_by_parent_and_title_[parent].contains(title));
+  DCHECK(base::Contains(trackers_by_parent_and_title_, parent));
+  DCHECK(base::Contains(trackers_by_parent_and_title_[parent], title));
 
   DVLOG(3) << "  Remove from trackers_by_parent_and_title_: "
            << parent << " " << title;
@@ -671,8 +672,8 @@ void MetadataDatabaseIndex::RemoveFromPathIndexes(
 
   if (trackers_by_parent_and_title_[parent][title].size() <= 1 &&
       !title.empty()) {
-    DVLOG_IF(
-        3, multi_backing_file_paths_.contains(ParentIDAndTitle(parent, title)))
+    DVLOG_IF(3, base::Contains(multi_backing_file_paths_,
+                               ParentIDAndTitle(parent, title)))
         << "  Remove from multi_backing_file_paths_: " << parent << " "
         << title;
     multi_backing_file_paths_.erase(ParentIDAndTitle(parent, title));
@@ -687,8 +688,8 @@ void MetadataDatabaseIndex::RemoveFromPathIndexes(
 
 void MetadataDatabaseIndex::AddToDirtyTrackerIndexes(
     const FileTracker& new_tracker) {
-  DCHECK(!dirty_trackers_.contains(new_tracker.tracker_id()));
-  DCHECK(!demoted_dirty_trackers_.contains(new_tracker.tracker_id()));
+  DCHECK(!base::Contains(dirty_trackers_, new_tracker.tracker_id()));
+  DCHECK(!base::Contains(demoted_dirty_trackers_, new_tracker.tracker_id()));
 
   if (new_tracker.dirty()) {
     DVLOG(3) << "  Add to dirty_trackers_: " << new_tracker.tracker_id();
@@ -703,16 +704,16 @@ void MetadataDatabaseIndex::UpdateInDirtyTrackerIndexes(
 
   int64_t tracker_id = new_tracker.tracker_id();
   if (old_tracker.dirty() && !new_tracker.dirty()) {
-    DCHECK(dirty_trackers_.contains(tracker_id) ||
-           demoted_dirty_trackers_.contains(tracker_id));
+    DCHECK(base::Contains(dirty_trackers_, tracker_id) ||
+           base::Contains(demoted_dirty_trackers_, tracker_id));
 
     DVLOG(3) << "  Remove from dirty_trackers_: " << tracker_id;
 
     dirty_trackers_.erase(tracker_id);
     demoted_dirty_trackers_.erase(tracker_id);
   } else if (!old_tracker.dirty() && new_tracker.dirty()) {
-    DCHECK(!dirty_trackers_.contains(tracker_id));
-    DCHECK(!demoted_dirty_trackers_.contains(tracker_id));
+    DCHECK(!base::Contains(dirty_trackers_, tracker_id));
+    DCHECK(!base::Contains(demoted_dirty_trackers_, tracker_id));
 
     DVLOG(3) << "  Add to dirty_trackers_: " << tracker_id;
 
@@ -724,8 +725,8 @@ void MetadataDatabaseIndex::RemoveFromDirtyTrackerIndexes(
     const FileTracker& tracker) {
   if (tracker.dirty()) {
     int64_t tracker_id = tracker.tracker_id();
-    DCHECK(dirty_trackers_.contains(tracker_id) ||
-           demoted_dirty_trackers_.contains(tracker_id));
+    DCHECK(base::Contains(dirty_trackers_, tracker_id) ||
+           base::Contains(demoted_dirty_trackers_, tracker_id));
 
     DVLOG(3) << "  Remove from dirty_trackers_: " << tracker_id;
     dirty_trackers_.erase(tracker_id);
diff --git a/chrome/browser/sync_file_system/drive_backend/metadata_database_index_on_disk.cc b/chrome/browser/sync_file_system/drive_backend/metadata_database_index_on_disk.cc
index 2bb8647d4b..53ec24adca 100644
--- a/chrome/browser/sync_file_system/drive_backend/metadata_database_index_on_disk.cc
+++ b/chrome/browser/sync_file_system/drive_backend/metadata_database_index_on_disk.cc
@@ -6,6 +6,7 @@
 
 #include <array>
 
+#include "base/containers/contains.h"
 #include "base/format_macros.h"
 #include "base/logging.h"
 #include "base/notreached.h"
@@ -224,7 +225,7 @@ void RemoveUnreachableItemsFromDB(LevelDBWrapper* db,
         continue;
       }
 
-      if (visited_trackers.contains(tracker->tracker_id())) {
+      if (base::Contains(visited_trackers, tracker->tracker_id())) {
         referred_file_ids.insert(tracker->file_id());
       } else {
         PutFileTrackerDeletionToDB(tracker->tracker_id(), db);
@@ -246,7 +247,7 @@ void RemoveUnreachableItemsFromDB(LevelDBWrapper* db,
         continue;
       }
 
-      if (!referred_file_ids.contains(metadata->file_id())) {
+      if (!base::Contains(referred_file_ids, metadata->file_id())) {
         PutFileMetadataDeletionToDB(metadata->file_id(), db);
       }
     }
diff --git a/chrome/browser/sync_file_system/drive_backend/tracker_id_set.cc b/chrome/browser/sync_file_system/drive_backend/tracker_id_set.cc
index 1ab5828162..7b88f8aeb7 100644
--- a/chrome/browser/sync_file_system/drive_backend/tracker_id_set.cc
+++ b/chrome/browser/sync_file_system/drive_backend/tracker_id_set.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/sync_file_system/drive_backend/tracker_id_set.h"
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "chrome/browser/sync_file_system/drive_backend/metadata_database.pb.h"
 
 namespace sync_file_system {
@@ -39,7 +40,7 @@ void TrackerIDSet::InsertInactiveTracker(int64_t tracker_id) {
 }
 
 void TrackerIDSet::Erase(int64_t tracker_id) {
-  DCHECK(tracker_ids_.contains(tracker_id));
+  DCHECK(base::Contains(tracker_ids_, tracker_id));
 
   if (active_tracker_ == tracker_id)
     active_tracker_ = 0;
@@ -48,13 +49,13 @@ void TrackerIDSet::Erase(int64_t tracker_id) {
 
 void TrackerIDSet::Activate(int64_t tracker_id) {
   DCHECK(!active_tracker_);
-  DCHECK(tracker_ids_.contains(tracker_id));
+  DCHECK(base::Contains(tracker_ids_, tracker_id));
   active_tracker_ = tracker_id;
 }
 
 void TrackerIDSet::Deactivate(int64_t tracker_id) {
   DCHECK_EQ(active_tracker_, tracker_id);
-  DCHECK(tracker_ids_.contains(tracker_id));
+  DCHECK(base::Contains(tracker_ids_, tracker_id));
   active_tracker_ = 0;
 }
 
diff --git a/chrome/browser/sync_file_system/local/local_file_change_tracker.cc b/chrome/browser/sync_file_system/local/local_file_change_tracker.cc
index 630cd06714..3b21f31817 100644
--- a/chrome/browser/sync_file_system/local/local_file_change_tracker.cc
+++ b/chrome/browser/sync_file_system/local/local_file_change_tracker.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/containers/circular_deque.h"
+#include "base/containers/contains.h"
 #include "base/containers/queue.h"
 #include "base/location.h"
 #include "base/logging.h"
@@ -93,7 +94,7 @@ LocalFileChangeTracker::~LocalFileChangeTracker() {
 
 void LocalFileChangeTracker::OnStartUpdate(const FileSystemURL& url) {
   DCHECK(file_task_runner_->RunsTasksInCurrentSequence());
-  if (changes_.contains(url) || demoted_changes_.contains(url)) {
+  if (base::Contains(changes_, url) || base::Contains(demoted_changes_, url)) {
     return;
   }
   // TODO(nhiroki): propagate the error code (see http://crbug.com/40950150).
@@ -187,7 +188,7 @@ void LocalFileChangeTracker::ClearChangesForURL(const FileSystemURL& url) {
 void LocalFileChangeTracker::CreateFreshMirrorForURL(
     const storage::FileSystemURL& url) {
   DCHECK(file_task_runner_->RunsTasksInCurrentSequence());
-  DCHECK(!mirror_changes_.contains(url));
+  DCHECK(!base::Contains(mirror_changes_, url));
   mirror_changes_[url] = ChangeInfo();
 }
 
@@ -199,7 +200,7 @@ void LocalFileChangeTracker::RemoveMirrorAndCommitChangesForURL(
     return;
   mirror_changes_.erase(found);
 
-  if (changes_.contains(url) || demoted_changes_.contains(url)) {
+  if (base::Contains(changes_, url) || base::Contains(demoted_changes_, url)) {
     MarkDirtyOnDatabase(url);
   } else {
     ClearDirtyOnDatabase(url);
@@ -216,11 +217,11 @@ void LocalFileChangeTracker::ResetToMirrorAndCommitChangesForURL(
     return;
   }
   const ChangeInfo& info = found->second;
-  if (demoted_changes_.contains(url)) {
-    DCHECK(!changes_.contains(url));
+  if (base::Contains(demoted_changes_, url)) {
+    DCHECK(!base::Contains(changes_, url));
     demoted_changes_[url] = info;
   } else {
-    DCHECK(!demoted_changes_.contains(url));
+    DCHECK(!base::Contains(demoted_changes_, url));
     change_seqs_[info.change_seq] = url;
     changes_[url] = info;
   }
@@ -234,7 +235,7 @@ void LocalFileChangeTracker::DemoteChangesForURL(
   auto found = changes_.find(url);
   if (found == changes_.end())
     return;
-  DCHECK(!demoted_changes_.contains(url));
+  DCHECK(!base::Contains(demoted_changes_, url));
   change_seqs_.erase(found->second.change_seq);
   demoted_changes_.insert(*found);
   changes_.erase(found);
@@ -251,8 +252,8 @@ void LocalFileChangeTracker::PromoteDemotedChangesForURL(
 
   FileChangeList::List change_list = iter->second.change_list.list();
   // Make sure that this URL is in no queues.
-  DCHECK(!change_seqs_.contains(iter->second.change_seq));
-  DCHECK(!changes_.contains(url));
+  DCHECK(!base::Contains(change_seqs_, iter->second.change_seq));
+  DCHECK(!base::Contains(changes_, url));
 
   change_seqs_[iter->second.change_seq] = url;
   changes_.insert(*iter);
@@ -425,13 +426,13 @@ void LocalFileChangeTracker::RecordChange(
     const FileSystemURL& url, const FileChange& change) {
   DCHECK(file_task_runner_->RunsTasksInCurrentSequence());
   int change_seq = current_change_seq_number_++;
-  if (demoted_changes_.contains(url)) {
+  if (base::Contains(demoted_changes_, url)) {
     RecordChangeToChangeMaps(url, change, change_seq,
                              &demoted_changes_, nullptr);
   } else {
     RecordChangeToChangeMaps(url, change, change_seq, &changes_, &change_seqs_);
   }
-  if (mirror_changes_.contains(url)) {
+  if (base::Contains(mirror_changes_, url)) {
     RecordChangeToChangeMaps(url, change, change_seq, &mirror_changes_,
                              nullptr);
   }
diff --git a/chrome/browser/sync_file_system/local/local_file_sync_context.cc b/chrome/browser/sync_file_system/local/local_file_sync_context.cc
index 0d6eae18e6..d60e23a53e 100644
--- a/chrome/browser/sync_file_system/local/local_file_sync_context.cc
+++ b/chrome/browser/sync_file_system/local/local_file_sync_context.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -73,7 +74,7 @@ void LocalFileSyncContext::MaybeInitializeFileSystemContext(
     FileSystemContext* file_system_context,
     SyncStatusCallback callback) {
   DCHECK(ui_task_runner_->RunsTasksInCurrentSequence());
-  if (file_system_contexts_.contains(file_system_context)) {
+  if (base::Contains(file_system_contexts_, file_system_context)) {
     // The context has been already initialized. Just dispatch the callback
     // with SYNC_STATUS_OK.
     ui_task_runner_->PostTask(
@@ -749,8 +750,8 @@ void LocalFileSyncContext::DidInitialize(
     return;
   }
   DCHECK(ui_task_runner_->RunsTasksInCurrentSequence());
-  DCHECK(!file_system_contexts_.contains(file_system_context));
-  DCHECK(pending_initialize_callbacks_.contains(file_system_context));
+  DCHECK(!base::Contains(file_system_contexts_, file_system_context));
+  DCHECK(base::Contains(pending_initialize_callbacks_, file_system_context));
 
   SyncFileSystemBackend* backend =
       SyncFileSystemBackend::GetBackend(file_system_context);
diff --git a/chrome/browser/sync_file_system/local/local_file_sync_service.cc b/chrome/browser/sync_file_system/local/local_file_sync_service.cc
index 2da34577ce..af8f0b1624 100644
--- a/chrome/browser/sync_file_system/local/local_file_sync_service.cc
+++ b/chrome/browser/sync_file_system/local/local_file_sync_service.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/sync_file_system/local/local_file_sync_service.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
 #include "base/observer_list.h"
@@ -68,7 +69,7 @@ bool LocalFileSyncService::OriginChangeMap::NextOriginToProcess(GURL* origin) {
       next_ = change_count_map_.begin();
     DCHECK_NE(0, next_->second);
     *origin = next_++->first;
-    if (!disabled_origins_.contains(*origin)) {
+    if (!base::Contains(disabled_origins_, *origin)) {
       return true;
     }
   } while (next_ != begin);
@@ -79,7 +80,7 @@ int64_t LocalFileSyncService::OriginChangeMap::GetTotalChangeCount() const {
   int64_t num_changes = 0;
   for (auto iter = change_count_map_.begin(); iter != change_count_map_.end();
        ++iter) {
-    if (disabled_origins_.contains(iter->first)) {
+    if (base::Contains(disabled_origins_, iter->first)) {
       continue;
     }
     num_changes += iter->second;
@@ -166,7 +167,7 @@ void LocalFileSyncService::ProcessLocalChange(SyncFileCallback callback) {
     return;
   }
   DCHECK(!origin.is_empty());
-  DCHECK(origin_to_contexts_.contains(origin));
+  DCHECK(base::Contains(origin_to_contexts_, origin));
 
   DVLOG(1) << "Starting ProcessLocalChange";
 
@@ -189,7 +190,7 @@ void LocalFileSyncService::SetLocalChangeProcessorCallback(
 void LocalFileSyncService::HasPendingLocalChanges(
     const FileSystemURL& url,
     HasPendingLocalChangeCallback callback) {
-  if (!origin_to_contexts_.contains(url.origin().GetURL())) {
+  if (!base::Contains(origin_to_contexts_, url.origin().GetURL())) {
     base::SingleThreadTaskRunner::GetCurrentDefault()->PostTask(
         FROM_HERE, base::BindOnce(std::move(callback),
                                   SYNC_FILE_ERROR_INVALID_URL, false));
@@ -220,7 +221,7 @@ void LocalFileSyncService::PromoteDemotedChanges(
 void LocalFileSyncService::GetLocalFileMetadata(
     const FileSystemURL& url,
     SyncFileMetadataCallback callback) {
-  DCHECK(origin_to_contexts_.contains(url.origin().GetURL()));
+  DCHECK(base::Contains(origin_to_contexts_, url.origin().GetURL()));
   sync_context_->GetFileMetadata(origin_to_contexts_[url.origin().GetURL()],
                                  url, std::move(callback));
 }
@@ -230,7 +231,7 @@ void LocalFileSyncService::PrepareForProcessRemoteChange(
     PrepareChangeCallback callback) {
   DVLOG(1) << "PrepareForProcessRemoteChange: " << url.DebugString();
 
-  if (!origin_to_contexts_.contains(url.origin().GetURL())) {
+  if (!base::Contains(origin_to_contexts_, url.origin().GetURL())) {
     // This could happen if a remote sync is triggered for the app that hasn't
     // been initialized in this service.
     DCHECK(profile_);
@@ -264,7 +265,7 @@ void LocalFileSyncService::PrepareForProcessRemoteChange(
     return;
   }
 
-  DCHECK(origin_to_contexts_.contains(url.origin().GetURL()));
+  DCHECK(base::Contains(origin_to_contexts_, url.origin().GetURL()));
   sync_context_->PrepareForSync(
       origin_to_contexts_[url.origin().GetURL()], url,
       LocalFileSyncContext::SYNC_EXCLUSIVE,
@@ -276,7 +277,7 @@ void LocalFileSyncService::ApplyRemoteChange(const FileChange& change,
                                              const base::FilePath& local_path,
                                              const FileSystemURL& url,
                                              SyncStatusCallback callback) {
-  DCHECK(origin_to_contexts_.contains(url.origin().GetURL()));
+  DCHECK(base::Contains(origin_to_contexts_, url.origin().GetURL()));
   util::Log(logging::LOGGING_VERBOSE, FROM_HERE,
             "[Remote -> Local] ApplyRemoteChange: %s on %s",
             change.DebugString().c_str(), url.DebugString().c_str());
@@ -291,7 +292,7 @@ void LocalFileSyncService::FinalizeRemoteSync(
     const FileSystemURL& url,
     bool clear_local_changes,
     base::OnceClosure completion_callback) {
-  DCHECK(origin_to_contexts_.contains(url.origin().GetURL()));
+  DCHECK(base::Contains(origin_to_contexts_, url.origin().GetURL()));
   sync_context_->FinalizeExclusiveSync(
       origin_to_contexts_[url.origin().GetURL()], url, clear_local_changes,
       std::move(completion_callback));
@@ -300,7 +301,7 @@ void LocalFileSyncService::FinalizeRemoteSync(
 void LocalFileSyncService::RecordFakeLocalChange(const FileSystemURL& url,
                                                  const FileChange& change,
                                                  SyncStatusCallback callback) {
-  DCHECK(origin_to_contexts_.contains(url.origin().GetURL()));
+  DCHECK(base::Contains(origin_to_contexts_, url.origin().GetURL()));
   sync_context_->RecordFakeLocalChange(
       origin_to_contexts_[url.origin().GetURL()], url, change,
       std::move(callback));
@@ -311,7 +312,7 @@ void LocalFileSyncService::OnChangesAvailableInOrigins(
   bool need_notification = false;
   for (auto iter = origins.begin(); iter != origins.end(); ++iter) {
     const GURL& origin = *iter;
-    if (!origin_to_contexts_.contains(origin)) {
+    if (!base::Contains(origin_to_contexts_, origin)) {
       // This could happen if this is called for apps/origins that haven't
       // been initialized yet, or for apps/origins that are disabled.
       // (Local change tracker could call this for uninitialized origins
@@ -336,7 +337,7 @@ void LocalFileSyncService::OnChangesAvailableInOrigins(
 }
 
 void LocalFileSyncService::SetOriginEnabled(const GURL& origin, bool enabled) {
-  if (!origin_to_contexts_.contains(origin)) {
+  if (!base::Contains(origin_to_contexts_, origin)) {
     return;
   }
   origin_change_map_.SetOriginEnabled(origin, enabled);
@@ -469,7 +470,7 @@ void LocalFileSyncService::ProcessNextChangeForURL(
 
   const FileSystemURL& url = sync_file_info.url;
   if (status != SYNC_STATUS_OK || changes.empty()) {
-    DCHECK(origin_to_contexts_.contains(url.origin().GetURL()));
+    DCHECK(base::Contains(origin_to_contexts_, url.origin().GetURL()));
     sync_context_->FinalizeSnapshotSync(
         origin_to_contexts_[url.origin().GetURL()], url, status,
         base::BindOnce(std::move(callback), status, url));
diff --git a/chrome/browser/sync_file_system/local/local_file_sync_status.cc b/chrome/browser/sync_file_system/local/local_file_sync_status.cc
index 28d51f944e..d24017516d 100644
--- a/chrome/browser/sync_file_system/local/local_file_sync_status.cc
+++ b/chrome/browser/sync_file_system/local/local_file_sync_status.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/sync_file_system/local/local_file_sync_status.h"
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "content/public/browser/browser_thread.h"
 #include "storage/common/file_system/file_system_util.h"
@@ -60,7 +61,7 @@ bool ContainsChildOrParent(const Container& paths,
 
   // Check if any ancestor of |normalized_path| is in |writing_|.
   while (true) {
-    if (paths.contains(normalized_path)) {
+    if (base::Contains(paths, normalized_path)) {
       return true;
     }
 
diff --git a/chrome/browser/sync_file_system/subtree_set.cc b/chrome/browser/sync_file_system/subtree_set.cc
index bf85ac52c8..c7e1d5844e 100644
--- a/chrome/browser/sync_file_system/subtree_set.cc
+++ b/chrome/browser/sync_file_system/subtree_set.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/sync_file_system/subtree_set.h"
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "storage/common/file_system/file_system_util.h"
 
@@ -30,7 +31,8 @@ bool SubtreeSet::IsDisjointWith(const base::FilePath& subtree_root) const {
       storage::VirtualPath::GetNormalizedFilePath(subtree_root);
 
   // Check if |subtree_root| contains any of subtrees in the container.
-  if (inclusive_ancestors_of_subtree_roots_.contains(normalized_subtree_root)) {
+  if (base::Contains(inclusive_ancestors_of_subtree_roots_,
+                     normalized_subtree_root)) {
     return false;
   }
 
diff --git a/chrome/browser/task_manager/providers/fallback_task_provider.cc b/chrome/browser/task_manager/providers/fallback_task_provider.cc
index d3357675c4..3e41b991f6 100644
--- a/chrome/browser/task_manager/providers/fallback_task_provider.cc
+++ b/chrome/browser/task_manager/providers/fallback_task_provider.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/memory/raw_ptr.h"
@@ -199,7 +200,7 @@ void FallbackTaskProvider::OnTaskRemovedBySource(Task* task,
 
 void FallbackTaskProvider::OnTaskUnresponsive(Task* task) {
   DCHECK(task);
-  if (std::ranges::contains(shown_tasks_, task)) {
+  if (base::Contains(shown_tasks_, task)) {
     NotifyObserverTaskUnresponsive(task);
   }
 }
diff --git a/chrome/browser/themes/browser_theme_pack.cc b/chrome/browser/themes/browser_theme_pack.cc
index 21e9c39cfb..ff4d35ae82 100644
--- a/chrome/browser/themes/browser_theme_pack.cc
+++ b/chrome/browser/themes/browser_theme_pack.cc
@@ -16,6 +16,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/heap_array.h"
@@ -1048,7 +1049,7 @@ bool BrowserThemePack::GetColor(int id, SkColor* color) const {
     for (size_t i = 0; i < kColorsArrayLength; ++i) {
       if (UNSAFE_TODO(colors_[i]).id == id) {
         *color = UNSAFE_TODO(colors_[i]).color;
-        if (kOpaqueColors.contains(id)) {
+        if (base::Contains(kOpaqueColors, id)) {
           *color = SkColorSetA(*color, SK_AlphaOPAQUE);
         }
         return true;
@@ -1673,7 +1674,7 @@ bool BrowserThemePack::LoadRawBitmapsTo(const FilePathMap& file_paths,
     PersistentID prs_id = entry.first;
     // Some images need to go directly into |image_memory_|. No modification is
     // necessary or desirable.
-    const bool is_copyable = std::ranges::contains(kPreloadIDs, prs_id);
+    const bool is_copyable = base::Contains(kPreloadIDs, prs_id);
     gfx::ImageSkia image_skia;
     for (int pass = 0; pass < 2; ++pass) {
       // Two passes: In the first pass, we process only scale factor
diff --git a/chrome/browser/touch_to_fill/autofill/android/touch_to_fill_delegate_android_impl.cc b/chrome/browser/touch_to_fill/autofill/android/touch_to_fill_delegate_android_impl.cc
index ff3c49abc8..b6b2be8992 100644
--- a/chrome/browser/touch_to_fill/autofill/android/touch_to_fill_delegate_android_impl.cc
+++ b/chrome/browser/touch_to_fill/autofill/android/touch_to_fill_delegate_android_impl.cc
@@ -5,7 +5,6 @@
 #include "chrome/browser/touch_to_fill/autofill/android/touch_to_fill_delegate_android_impl.h"
 
 #include <optional>
-#include <utility>
 #include <variant>
 #include <vector>
 
@@ -237,7 +236,7 @@ bool TouchToFillDelegateAndroidImpl::IntendsToShowTouchToFill(
       << LoggingScope::kTouchToFill << LogMessage::kTouchToFill
       << "dry run before parsing for form " << form_id << " and field "
       << field_id << " was " << (outcome == TriggerOutcome::kShown ? "" : "un")
-      << "successful (" << std::to_underlying(outcome) << ")";
+      << "successful (" << base::to_underlying(outcome) << ")";
   return outcome == TriggerOutcome::kShown;
 }
 
@@ -282,7 +281,7 @@ bool TouchToFillDelegateAndroidImpl::TryToShowTouchToFill(
       << "dry run after parsing for form " << form.global_id() << " and field "
       << field.global_id() << " was "
       << (dry_run.outcome == TriggerOutcome::kShown ? "" : "un")
-      << "successful (" << std::to_underlying(dry_run.outcome) << ")";
+      << "successful (" << base::to_underlying(dry_run.outcome) << ")";
 
   if (dry_run.outcome != TriggerOutcome::kShown) {
     return false;
diff --git a/chrome/browser/touch_to_fill/autofill/android/touch_to_fill_payment_method_view_impl.cc b/chrome/browser/touch_to_fill/autofill/android/touch_to_fill_payment_method_view_impl.cc
index 92bda2bacd..a8f11617dc 100644
--- a/chrome/browser/touch_to_fill/autofill/android/touch_to_fill_payment_method_view_impl.cc
+++ b/chrome/browser/touch_to_fill/autofill/android/touch_to_fill_payment_method_view_impl.cc
@@ -6,7 +6,6 @@
 
 #include <algorithm>
 #include <string>
-#include <utility>
 #include <variant>
 #include <vector>
 
@@ -156,7 +155,7 @@ bool TouchToFillPaymentMethodViewImpl::ShowPaymentMethods(
         Java_TouchToFillPaymentMethodViewBridge_createAutofillSuggestion(
             env, suggestion.main_text.value, minor_text,
             suggestion.labels[0][0].value, secondarySubLabel,
-            std::to_underlying(suggestion.type),
+            base::to_underlying(suggestion.type),
             custom_icon_url ? url::GURLAndroid::FromNativeGURL(
                                   env, custom_icon_url->value())
                             : url::GURLAndroid::EmptyGURL(env),
diff --git a/chrome/browser/transition_manager/full_browser_transition_manager.cc b/chrome/browser/transition_manager/full_browser_transition_manager.cc
index defded3b87..e45e88d547 100644
--- a/chrome/browser/transition_manager/full_browser_transition_manager.cc
+++ b/chrome/browser/transition_manager/full_browser_transition_manager.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/transition_manager/full_browser_transition_manager.h"
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/profiles/profile_key.h"
@@ -37,7 +38,7 @@ void FullBrowserTransitionManager::OnProfileCreated(Profile* profile) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
 
   SimpleFactoryKey* key = profile->GetProfileKey();
-  DCHECK(!simple_key_to_profile_.contains(key));
+  DCHECK(!base::Contains(simple_key_to_profile_, key));
 
   // Register the mapping so that it can be used if deferred callbacks are added
   // later.
diff --git a/chrome/browser/ui/accelerator_table.cc b/chrome/browser/ui/accelerator_table.cc
index 20a937a432..6cfb2d5667 100644
--- a/chrome/browser/ui/accelerator_table.cc
+++ b/chrome/browser/ui/accelerator_table.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/no_destructor.h"
 #include "base/notreached.h"
@@ -380,5 +381,5 @@ bool GetStandardAcceleratorForCommandId(int command_id,
 }
 
 bool IsCommandRepeatable(int command_id) {
-  return std::ranges::contains(kRepeatableCommandIds, command_id);
+  return base::Contains(kRepeatableCommandIds, command_id);
 }
diff --git a/chrome/browser/ui/android/autofill/autofill_keyboard_accessory_view_impl.cc b/chrome/browser/ui/android/autofill/autofill_keyboard_accessory_view_impl.cc
index 32ebfde9df..6d4071c3a7 100644
--- a/chrome/browser/ui/android/autofill/autofill_keyboard_accessory_view_impl.cc
+++ b/chrome/browser/ui/android/autofill/autofill_keyboard_accessory_view_impl.cc
@@ -18,6 +18,7 @@
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/trace_event/trace_event.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/android/resource_mapper.h"
 #include "chrome/browser/ui/autofill/autofill_keyboard_accessory_controller.h"
 #include "components/autofill/core/browser/suggestions/suggestion.h"
@@ -126,7 +127,7 @@ void AutofillKeyboardAccessoryViewImpl::Show() {
     java_suggestions.push_back(
         Java_AutofillKeyboardAccessoryViewBridge_createAutofillSuggestion(
             env, label, sublabel, suggestion.voice_over.value_or(u""),
-            android_icon_id, std::to_underlying(suggestion.type),
+            android_icon_id, base::to_underlying(suggestion.type),
             controller_->GetRemovalConfirmationText(i, nullptr),
             suggestion.iph_metadata.feature
                 ? suggestion.iph_metadata.feature->name
diff --git a/chrome/browser/ui/ash/back_gesture/back_gesture_browsertest.cc b/chrome/browser/ui/ash/back_gesture/back_gesture_browsertest.cc
index b7a409a317..bf41bf9551 100644
--- a/chrome/browser/ui/ash/back_gesture/back_gesture_browsertest.cc
+++ b/chrome/browser/ui/ash/back_gesture/back_gesture_browsertest.cc
@@ -4,6 +4,7 @@
 
 #include "ash/public/cpp/test/shell_test_api.h"
 #include "ash/wm/tablet_mode/tablet_mode_controller_test_api.h"
+#include "base/containers/contains.h"
 #include "base/path_service.h"
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_window.h"
@@ -56,7 +57,7 @@ class BackGestureEventRecorder : public ui::EventHandler {
   }
 
   bool HasReceivedEvent(ui::EventType event_type) {
-    return received_event_types_.contains(event_type);
+    return base::Contains(received_event_types_, event_type);
   }
 
   void Reset() {
diff --git a/chrome/browser/ui/ash/birch/birch_recent_tabs_provider.cc b/chrome/browser/ui/ash/birch/birch_recent_tabs_provider.cc
index 71b6887af6..43e79d4b4e 100644
--- a/chrome/browser/ui/ash/birch/birch_recent_tabs_provider.cc
+++ b/chrome/browser/ui/ash/birch/birch_recent_tabs_provider.cc
@@ -65,8 +65,9 @@ void BirchRecentTabsProvider::RequestBirchDataFetch() {
 
   const auto* const pref_service = profile_->GetPrefs();
   if (!pref_service ||
-      !pref_service->GetList(prefs::kContextualGoogleIntegrationsConfiguration)
-           .contains(prefs::kChromeSyncIntegrationName)) {
+      !base::Contains(pref_service->GetList(
+                          prefs::kContextualGoogleIntegrationsConfiguration),
+                      prefs::kChromeSyncIntegrationName)) {
     // ChromeSync integration is disabled by policy.
     Shell::Get()->birch_model()->SetRecentTabItems({});
     return;
diff --git a/chrome/browser/ui/ash/birch/birch_self_share_provider.cc b/chrome/browser/ui/ash/birch/birch_self_share_provider.cc
index 2961bbe853..9b1e61f956 100644
--- a/chrome/browser/ui/ash/birch/birch_self_share_provider.cc
+++ b/chrome/browser/ui/ash/birch/birch_self_share_provider.cc
@@ -41,8 +41,9 @@ BirchSelfShareProvider::~BirchSelfShareProvider() = default;
 void BirchSelfShareProvider::RequestBirchDataFetch() {
   const auto* const pref_service = profile_->GetPrefs();
   if (!pref_service ||
-      !pref_service->GetList(prefs::kContextualGoogleIntegrationsConfiguration)
-           .contains(prefs::kChromeSyncIntegrationName)) {
+      !base::Contains(pref_service->GetList(
+                          prefs::kContextualGoogleIntegrationsConfiguration),
+                      prefs::kChromeSyncIntegrationName)) {
     // ChromeSync integration is disabled by policy.
     Shell::Get()->birch_model()->SetSelfShareItems({});
     return;
diff --git a/chrome/browser/ui/ash/desks/desks_templates_app_launch_handler.cc b/chrome/browser/ui/ash/desks/desks_templates_app_launch_handler.cc
index 52455ce605..6e27c0f108 100644
--- a/chrome/browser/ui/ash/desks/desks_templates_app_launch_handler.cc
+++ b/chrome/browser/ui/ash/desks/desks_templates_app_launch_handler.cc
@@ -318,7 +318,7 @@ void DesksTemplatesAppLaunchHandler::MaybeLaunchArcApps() {
       [&app_ids, &app_id_to_launch_list](const apps::AppUpdate& update) {
         if (update.Readiness() == apps::Readiness::kReady &&
             update.AppType() == apps::AppType::kArc &&
-            app_id_to_launch_list.contains(update.AppId())) {
+            base::Contains(app_id_to_launch_list, update.AppId())) {
           app_ids.insert(update.AppId());
         }
       });
diff --git a/chrome/browser/ui/ash/glanceables/glanceables_browsertest.cc b/chrome/browser/ui/ash/glanceables/glanceables_browsertest.cc
index c1c3e3c3d6..83450ea1f5 100644
--- a/chrome/browser/ui/ash/glanceables/glanceables_browsertest.cc
+++ b/chrome/browser/ui/ash/glanceables/glanceables_browsertest.cc
@@ -3,7 +3,6 @@
 // found in the LICENSE file.
 
 #include <memory>
-#include <utility>
 #include <vector>
 
 #include "ash/api/tasks/fake_tasks_client.h"
@@ -28,6 +27,7 @@
 #include "ash/test/ash_test_util.h"
 #include "base/test/gtest_tags.h"
 #include "base/test/scoped_feature_list.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ash/profiles/profile_helper.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/profiles/profile_manager.h"
@@ -160,29 +160,29 @@ class GlanceablesBrowserTest : public InProcessBrowserTest {
 
   Combobox* GetTasksComboBoxView() const {
     return views::AsViewClass<Combobox>(GetTasksView()->GetViewByID(
-        std::to_underlying(GlanceablesViewId::kTimeManagementBubbleComboBox)));
+        base::to_underlying(GlanceablesViewId::kTimeManagementBubbleComboBox)));
   }
 
   views::ScrollView* GetTasksScrollView() const {
     return views::AsViewClass<views::ScrollView>(GetTasksView()->GetViewByID(
-        std::to_underlying(GlanceablesViewId::kContentsScrollView)));
+        base::to_underlying(GlanceablesViewId::kContentsScrollView)));
   }
 
   views::View* GetTasksItemContainerView() const {
     return views::AsViewClass<views::View>(
-        GetTasksView()->GetViewByID(std::to_underlying(
+        GetTasksView()->GetViewByID(base::to_underlying(
             GlanceablesViewId::kTimeManagementBubbleListContainer)));
   }
 
   CounterExpandButton* GetTasksExpandButtonView() const {
     return views::AsViewClass<CounterExpandButton>(
-        GetTasksView()->GetViewByID(std::to_underlying(
+        GetTasksView()->GetViewByID(base::to_underlying(
             GlanceablesViewId::kTimeManagementBubbleExpandButton)));
   }
 
   views::LabelButton* GetAddNewTaskButton() const {
     return views::AsViewClass<views::LabelButton>(GetTasksView()->GetViewByID(
-        std::to_underlying(GlanceablesViewId::kTasksBubbleAddNewButton)));
+        base::to_underlying(GlanceablesViewId::kTasksBubbleAddNewButton)));
   }
 
   std::vector<std::string> GetCurrentTaskListItemTitles() const {
@@ -191,7 +191,7 @@ class GlanceablesBrowserTest : public InProcessBrowserTest {
       if (views::View* task_item = views::AsViewClass<views::View>(child)) {
         current_items.push_back(
             base::UTF16ToUTF8(views::AsViewClass<views::Label>(
-                                  task_item->GetViewByID(std::to_underlying(
+                                  task_item->GetViewByID(base::to_underlying(
                                       GlanceablesViewId::kTaskItemTitleLabel)))
                                   ->GetText()));
       }
@@ -209,18 +209,18 @@ class GlanceablesBrowserTest : public InProcessBrowserTest {
 
   views::View* GetStudentComboBoxView() const {
     return views::AsViewClass<views::View>(GetStudentView()->GetViewByID(
-        std::to_underlying(GlanceablesViewId::kTimeManagementBubbleComboBox)));
+        base::to_underlying(GlanceablesViewId::kTimeManagementBubbleComboBox)));
   }
 
   CounterExpandButton* GetStudentExpandButtonView() const {
     return views::AsViewClass<CounterExpandButton>(
-        GetStudentView()->GetViewByID(std::to_underlying(
+        GetStudentView()->GetViewByID(base::to_underlying(
             GlanceablesViewId::kTimeManagementBubbleExpandButton)));
   }
 
   views::View* GetStudentItemContainerView() const {
     return views::AsViewClass<views::View>(
-        GetStudentView()->GetViewByID(std::to_underlying(
+        GetStudentView()->GetViewByID(base::to_underlying(
             GlanceablesViewId::kTimeManagementBubbleListContainer)));
   }
 
@@ -230,7 +230,7 @@ class GlanceablesBrowserTest : public InProcessBrowserTest {
       if (views::View* assignment = views::AsViewClass<views::View>(child)) {
         assignment_titles.push_back(base::UTF16ToUTF8(
             views::AsViewClass<views::Label>(
-                assignment->GetViewByID(std::to_underlying(
+                assignment->GetViewByID(base::to_underlying(
                     GlanceablesViewId::kClassroomItemCourseWorkTitleLabel)))
                 ->GetText()));
       }
@@ -245,7 +245,7 @@ class GlanceablesBrowserTest : public InProcessBrowserTest {
 
   views::LabelButton* GetStudentFooterSeeAllButton() const {
     return views::AsViewClass<views::LabelButton>(GetStudentView()->GetViewByID(
-        std::to_underlying(GlanceablesViewId::kListFooterSeeAllButton)));
+        base::to_underlying(GlanceablesViewId::kListFooterSeeAllButton)));
   }
 
  private:
@@ -522,10 +522,10 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest, AddTaskItem) {
   {
     const auto* const title_label =
         views::AsViewClass<views::Label>(pending_task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
     const auto* const title_text_field =
         views::AsViewClass<views::Textfield>(pending_task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
 
     // Check that the view is in "edit" mode (the text field is displayed).
     EXPECT_FALSE(title_label);
@@ -551,10 +551,10 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest, AddTaskItem) {
   {
     const auto* const title_label =
         views::AsViewClass<views::Label>(pending_task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
     const auto* const title_text_field =
         views::AsViewClass<views::Textfield>(pending_task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
 
     // Check that the view is in "view" mode with the expected label
     ASSERT_TRUE(title_label);
@@ -582,10 +582,10 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest, EditTaskItem) {
   {
     const auto* const title_label =
         views::AsViewClass<views::Label>(task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
     const auto* const title_text_field =
         views::AsViewClass<views::Textfield>(task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
 
     // Check that the view is in "view" mode (the label is displayed).
     ASSERT_TRUE(title_label);
@@ -602,10 +602,10 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest, EditTaskItem) {
   {
     const auto* const title_label =
         views::AsViewClass<views::Label>(task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
     const auto* const title_text_field =
         views::AsViewClass<views::Textfield>(task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
 
     // Check that the view is in "edit" mode (the text field is displayed).
     EXPECT_FALSE(title_label);
@@ -627,10 +627,10 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest, EditTaskItem) {
   {
     const auto* const title_label =
         views::AsViewClass<views::Label>(task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleLabel)));
     const auto* const title_text_field =
         views::AsViewClass<views::Textfield>(task_view->GetViewByID(
-            std::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
+            base::to_underlying(GlanceablesViewId::kTaskItemTitleTextField)));
 
     // Check that the view is in "view" mode with the updated label
     ASSERT_TRUE(title_label);
@@ -853,7 +853,7 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest,
   EXPECT_TRUE(GetTasksView());
 
   auto* error_view = views::AsViewClass<ErrorMessageToast>(
-      GetTasksView()->GetViewByID(std::to_underlying(
+      GetTasksView()->GetViewByID(base::to_underlying(
           GlanceablesViewId::kTimeManagementErrorMessageToast)));
   ASSERT_TRUE(error_view);
   EXPECT_EQ(error_view->GetMessageForTest(), u"Couldn't load items.");
@@ -866,8 +866,8 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest,
       error_view->GetButtonForTest()->GetBoundsInScreen().CenterPoint());
   GetEventGenerator()->ClickLeftButton();
 
-  EXPECT_FALSE(GetTasksView()->GetViewByID(
-      std::to_underlying(GlanceablesViewId::kTimeManagementErrorMessageToast)));
+  EXPECT_FALSE(GetTasksView()->GetViewByID(base::to_underlying(
+      GlanceablesViewId::kTimeManagementErrorMessageToast)));
   auto* combobox = GetTasksComboBoxView();
   EXPECT_EQ(combobox->GetTextForRow(combobox->GetSelectedIndex().value()),
             u"Task List 1 Title");
@@ -911,7 +911,7 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest, SwitchTaskListsWithError) {
             u"Task List 1 Title");
 
   auto* error_view = views::AsViewClass<ErrorMessageToast>(
-      GetTasksView()->GetViewByID(std::to_underlying(
+      GetTasksView()->GetViewByID(base::to_underlying(
           GlanceablesViewId::kTimeManagementErrorMessageToast)));
   ASSERT_TRUE(error_view);
   EXPECT_EQ(error_view->GetMessageForTest(), u"Couldn't load items.");
@@ -958,7 +958,7 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest,
   auto* first_task_view_label =
       GetTaskItemView(/*item_index=*/0)
           ->GetViewByID(
-              std::to_underlying(GlanceablesViewId::kTaskItemTitleLabel));
+              base::to_underlying(GlanceablesViewId::kTaskItemTitleLabel));
   GetEventGenerator()->MoveMouseTo(
       first_task_view_label->GetBoundsInScreen().CenterPoint());
   GetEventGenerator()->ClickLeftButton();
@@ -966,7 +966,7 @@ IN_PROC_BROWSER_TEST_F(GlanceablesTasksBrowserTest,
   auto* first_task_view_textfield =
       GetTaskItemView(/*item_index=*/0)
           ->GetViewByID(
-              std::to_underlying(GlanceablesViewId::kTaskItemTitleTextField));
+              base::to_underlying(GlanceablesViewId::kTaskItemTitleTextField));
   ASSERT_TRUE(first_task_view_textfield);
   ASSERT_TRUE(first_task_view_textfield->GetVisible());
 
diff --git a/chrome/browser/ui/ash/glanceables/glanceables_classroom_client_impl.cc b/chrome/browser/ui/ash/glanceables/glanceables_classroom_client_impl.cc
index 61cde87f6b..b060b2e46f 100644
--- a/chrome/browser/ui/ash/glanceables/glanceables_classroom_client_impl.cc
+++ b/chrome/browser/ui/ash/glanceables/glanceables_classroom_client_impl.cc
@@ -20,6 +20,7 @@
 #include "ash/glanceables/glanceables_metrics.h"
 #include "base/barrier_closure.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/memory/weak_ptr.h"
@@ -219,8 +220,9 @@ GlanceablesClassroomClientImpl::~GlanceablesClassroomClientImpl() = default;
 bool GlanceablesClassroomClientImpl::IsDisabledByAdmin() const {
   // 1) Check the pref.
   if (!pref_service_ ||
-      !pref_service_->GetList(prefs::kContextualGoogleIntegrationsConfiguration)
-           .contains(prefs::kGoogleClassroomIntegrationName)) {
+      !base::Contains(pref_service_->GetList(
+                          prefs::kContextualGoogleIntegrationsConfiguration),
+                      prefs::kGoogleClassroomIntegrationName)) {
     RecordContextualGoogleIntegrationStatus(
         prefs::kGoogleClassroomIntegrationName,
         ContextualGoogleIntegrationStatus::kDisabledByPolicy);
diff --git a/chrome/browser/ui/ash/global_media_controls/cast_media_notification_producer_keyed_service.cc b/chrome/browser/ui/ash/global_media_controls/cast_media_notification_producer_keyed_service.cc
index 948904226c..4f1ec9e8ff 100644
--- a/chrome/browser/ui/ash/global_media_controls/cast_media_notification_producer_keyed_service.cc
+++ b/chrome/browser/ui/ash/global_media_controls/cast_media_notification_producer_keyed_service.cc
@@ -32,7 +32,7 @@ void CastMediaNotificationProducerKeyedService::AddMediaItemManager(
 void CastMediaNotificationProducerKeyedService::RemoveMediaItemManager(
     global_media_controls::MediaItemManager* media_item_manager) {
   // Reset() may have already removed it.
-  if (managers_and_producers_.contains(media_item_manager)) {
+  if (base::Contains(managers_and_producers_, media_item_manager)) {
     media_item_manager->RemoveItemProducer(
         managers_and_producers_[media_item_manager].get());
     managers_and_producers_.erase(media_item_manager);
diff --git a/chrome/browser/ui/ash/holding_space/holding_space_downloads_delegate.cc b/chrome/browser/ui/ash/holding_space/holding_space_downloads_delegate.cc
index 59d5d8ece4..8801f0a342 100644
--- a/chrome/browser/ui/ash/holding_space/holding_space_downloads_delegate.cc
+++ b/chrome/browser/ui/ash/holding_space/holding_space_downloads_delegate.cc
@@ -635,7 +635,7 @@ void HoldingSpaceDownloadsDelegate::OnDownloadFailed(
     // NOTE: Removing `item` from the `model()` will result in the
     // `in_progress_download` being erased.
     model()->RemoveItem(item->id());
-    DCHECK(!in_progress_downloads_.contains(in_progress_download));
+    DCHECK(!base::Contains(in_progress_downloads_, in_progress_download));
     return;
   }
   EraseDownload(in_progress_download);
diff --git a/chrome/browser/ui/ash/holding_space/holding_space_persistence_delegate.cc b/chrome/browser/ui/ash/holding_space/holding_space_persistence_delegate.cc
index 87ca5c0dce..cafebc2459 100644
--- a/chrome/browser/ui/ash/holding_space/holding_space_persistence_delegate.cc
+++ b/chrome/browser/ui/ash/holding_space/holding_space_persistence_delegate.cc
@@ -180,7 +180,7 @@ void HoldingSpacePersistenceDelegate::MaybeRemoveItemsFromPersistence() {
   ScopedListPrefUpdate update(profile()->GetPrefs(), kPersistencePath);
   update->EraseIf([&](const base::Value& persisted_item) {
     auto type = HoldingSpaceItem::DeserializeType(persisted_item.GetDict());
-    return !known_types.contains(type);
+    return !base::Contains(known_types, type);
   });
 }
 
diff --git a/chrome/browser/ui/ash/holding_space/holding_space_suggestions_delegate.cc b/chrome/browser/ui/ash/holding_space/holding_space_suggestions_delegate.cc
index fb3f4c52ac..81488e67d9 100644
--- a/chrome/browser/ui/ash/holding_space/holding_space_suggestions_delegate.cc
+++ b/chrome/browser/ui/ash/holding_space/holding_space_suggestions_delegate.cc
@@ -127,7 +127,7 @@ void HoldingSpaceSuggestionsDelegate::MaybeFetchSuggestions(
     FileSuggestionType type) {
   // A data query on `type` has been sent so it is unnecessary to send a request
   // again. Return early.
-  if (pending_fetches_.contains(type)) {
+  if (base::Contains(pending_fetches_, type)) {
     return;
   }
 
diff --git a/chrome/browser/ui/ash/holding_space/holding_space_ui_browsertest.cc b/chrome/browser/ui/ash/holding_space/holding_space_ui_browsertest.cc
index a2870dba8d..915af2c3e8 100644
--- a/chrome/browser/ui/ash/holding_space/holding_space_ui_browsertest.cc
+++ b/chrome/browser/ui/ash/holding_space/holding_space_ui_browsertest.cc
@@ -33,6 +33,7 @@
 #include "ash/system/progress_indicator/progress_ring_animation.h"
 #include "ash/system/status_area_widget.h"
 #include "ash/test/view_drawn_waiter.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -1130,7 +1131,7 @@ IN_PROC_BROWSER_TEST_F(HoldingSpaceUiBrowserTest, RemoveItem) {
           .WillOnce([&](const std::vector<const HoldingSpaceItem*>& items) {
             ASSERT_EQ(items.size(), item_ids.size());
             for (const HoldingSpaceItem* item : items) {
-              ASSERT_TRUE(item_ids.contains(item->id()));
+              ASSERT_TRUE(base::Contains(item_ids, item->id()));
             }
             run_loop.Quit();
           });
diff --git a/chrome/browser/ui/ash/keyboard/chrome_keyboard_controller_client.cc b/chrome/browser/ui/ash/keyboard/chrome_keyboard_controller_client.cc
index a7ebe6062a..1c48fefa62 100644
--- a/chrome/browser/ui/ash/keyboard/chrome_keyboard_controller_client.cc
+++ b/chrome/browser/ui/ash/keyboard/chrome_keyboard_controller_client.cc
@@ -13,6 +13,7 @@
 #include "ash/keyboard/ui/resources/keyboard_resource_util.h"
 #include "ash/public/cpp/keyboard/keyboard_switches.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/ptr_util.h"
@@ -183,7 +184,7 @@ void ChromeKeyboardControllerClient::ClearEnableFlag(
 
 bool ChromeKeyboardControllerClient::IsEnableFlagSet(
     const keyboard::KeyboardEnableFlag& flag) {
-  return keyboard_enable_flags_.contains(flag);
+  return base::Contains(keyboard_enable_flags_, flag);
 }
 
 void ChromeKeyboardControllerClient::ReloadKeyboardIfNeeded() {
diff --git a/chrome/browser/ui/ash/media_client/media_client_impl.cc b/chrome/browser/ui/ash/media_client/media_client_impl.cc
index 4049bc60ff..3aa95fbbac 100644
--- a/chrome/browser/ui/ash/media_client/media_client_impl.cc
+++ b/chrome/browser/ui/ash/media_client/media_client_impl.cc
@@ -20,6 +20,7 @@
 #include "ash/system/privacy_hub/sensor_disabled_notification_delegate.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -483,7 +484,8 @@ void MediaClientImpl::EnableCustomMediaKeyHandler(
     ui::MediaKeysListener::Delegate* delegate) {
   auto it = media_key_delegates_.find(context);
 
-  DCHECK(!media_key_delegates_.contains(context) || it->second == delegate);
+  DCHECK(!base::Contains(media_key_delegates_, context) ||
+         it->second == delegate);
 
   media_key_delegates_.emplace(context, delegate);
 
@@ -493,7 +495,7 @@ void MediaClientImpl::EnableCustomMediaKeyHandler(
 void MediaClientImpl::DisableCustomMediaKeyHandler(
     content::BrowserContext* context,
     ui::MediaKeysListener::Delegate* delegate) {
-  if (!media_key_delegates_.contains(context)) {
+  if (!base::Contains(media_key_delegates_, context)) {
     return;
   }
 
diff --git a/chrome/browser/ui/ash/shelf/app_service/app_service_app_window_arc_tracker.cc b/chrome/browser/ui/ash/shelf/app_service/app_service_app_window_arc_tracker.cc
index a5c6f326d2..a14253351c 100644
--- a/chrome/browser/ui/ash/shelf/app_service/app_service_app_window_arc_tracker.cc
+++ b/chrome/browser/ui/ash/shelf/app_service/app_service_app_window_arc_tracker.cc
@@ -11,6 +11,7 @@
 #include "ash/public/cpp/window_properties.h"
 #include "ash/shell.h"
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/task/thread_pool.h"
@@ -602,7 +603,7 @@ void AppServiceAppWindowArcTracker::AttachControllerToTask(int task_id) {
   }
 
   const arc::ArcAppShelfId& app_shelf_id = app_window_info->app_shelf_id();
-  if (app_shelf_group_to_controller_map_.contains(app_shelf_id)) {
+  if (base::Contains(app_shelf_group_to_controller_map_, app_shelf_id)) {
     app_shelf_group_to_controller_map_[app_shelf_id]->AddTaskId(task_id);
     return;
   }
@@ -635,7 +636,7 @@ void AppServiceAppWindowArcTracker::AttachControllerToSession(
   // `session_id_to_arc_app_window_info_` directly.
   // TODO(b/274950968): Add a test for this case.
   const arc::ArcAppShelfId& app_shelf_id = app_window_info.app_shelf_id();
-  if (app_shelf_group_to_controller_map_.contains(app_shelf_id)) {
+  if (base::Contains(app_shelf_group_to_controller_map_, app_shelf_id)) {
     app_shelf_group_to_controller_map_[app_shelf_id]->AddSessionId(session_id);
     return;
   }
@@ -762,9 +763,9 @@ ArcAppWindowInfo* AppServiceAppWindowArcTracker::GetArcAppWindowInfo(
   // some cases, the task has beend created but window property in aura window
   // haven't been updated and still kept "session_id". In this case, if the
   // session's task created, just return the latest task info.
-  if (session_id_to_task_id_map_.contains(*session_id)) {
+  if (base::Contains(session_id_to_task_id_map_, *session_id)) {
     auto mapped_task_id = session_id_to_task_id_map_[*session_id];
-    if (task_id_to_arc_app_window_info_.contains(mapped_task_id)) {
+    if (base::Contains(task_id_to_arc_app_window_info_, mapped_task_id)) {
       return task_id_to_arc_app_window_info_[mapped_task_id].get();
     }
   }
diff --git a/chrome/browser/ui/ash/shelf/app_service/app_service_app_window_shelf_controller.cc b/chrome/browser/ui/ash/shelf/app_service/app_service_app_window_shelf_controller.cc
index b321bf7f65..a36658f73a 100644
--- a/chrome/browser/ui/ash/shelf/app_service/app_service_app_window_shelf_controller.cc
+++ b/chrome/browser/ui/ash/shelf/app_service/app_service_app_window_shelf_controller.cc
@@ -14,6 +14,7 @@
 #include "ash/public/cpp/shelf_model.h"
 #include "ash/public/cpp/window_properties.h"
 #include "ash/shell.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
 #include "chrome/browser/apps/app_service/app_service_proxy_factory.h"
@@ -430,7 +431,7 @@ void AppServiceAppWindowShelfController::UnregisterWindow(
 void AppServiceAppWindowShelfController::AddWindowToShelf(
     aura::Window* window,
     const ash::ShelfID& shelf_id) {
-  if (aura_window_to_app_window_.contains(window)) {
+  if (base::Contains(aura_window_to_app_window_, window)) {
     return;
   }
 
@@ -462,7 +463,7 @@ void AppServiceAppWindowShelfController::AddWindowToShelf(
 
 AppWindowBase* AppServiceAppWindowShelfController::GetAppWindow(
     aura::Window* window) {
-  if (!aura_window_to_app_window_.contains(window)) {
+  if (!base::Contains(aura_window_to_app_window_, window)) {
     return nullptr;
   }
   return aura_window_to_app_window_[window].get();
diff --git a/chrome/browser/ui/ash/shelf/app_service/app_service_instance_registry_helper.cc b/chrome/browser/ui/ash/shelf/app_service/app_service_instance_registry_helper.cc
index b607636e1b..fc082964b8 100644
--- a/chrome/browser/ui/ash/shelf/app_service/app_service_instance_registry_helper.cc
+++ b/chrome/browser/ui/ash/shelf/app_service/app_service_instance_registry_helper.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/stl_util.h"
 #include "base/time/time.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
@@ -161,7 +162,8 @@ void AppServiceInstanceRegistryHelper::OnBrowserRemoved() {
             instance->Window())) {
       // The tabs in the browser should be closed, and tab windows have been
       // removed from |browser_window_to_tab_windows_|.
-      DCHECK(!browser_window_to_tab_windows_.contains(instance->Window()));
+      DCHECK(
+          !base::Contains(browser_window_to_tab_windows_, instance->Window()));
 
       // The browser is removed if the window can't be found, so update the
       // Chrome window instance as destroyed.
@@ -278,7 +280,7 @@ void AppServiceInstanceRegistryHelper::OnWindowVisibilityChanged(
   OnInstances(app_constants::kChromeAppId, window, std::string(),
               CalculateVisibilityState(window, visible));
 
-  if (!browser_window_to_tab_windows_.contains(window)) {
+  if (!base::Contains(browser_window_to_tab_windows_, window)) {
     return;
   }
 
@@ -326,7 +328,7 @@ void AppServiceInstanceRegistryHelper::SetWindowActivated(
   OnInstances(app_constants::kChromeAppId, window, std::string(),
               CalculateActivatedState(window, active));
 
-  if (!browser_window_to_tab_windows_.contains(window)) {
+  if (!base::Contains(browser_window_to_tab_windows_, window)) {
     return;
   }
 
diff --git a/chrome/browser/ui/ash/shelf/browser_status_monitor.cc b/chrome/browser/ui/ash/shelf/browser_status_monitor.cc
index d54a1eb58c..ae1b292e6d 100644
--- a/chrome/browser/ui/ash/shelf/browser_status_monitor.cc
+++ b/chrome/browser/ui/ash/shelf/browser_status_monitor.cc
@@ -7,6 +7,7 @@
 #include <memory>
 
 #include "ash/public/cpp/shelf_types.h"
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/memory/raw_ptr.h"
 #include "chrome/browser/ash/browser_delegate/browser_controller.h"
@@ -254,12 +255,12 @@ void BrowserStatusMonitor::OnTabStripModelChanged(
   BrowserWindowInterface* const browser =
       GetBrowserWithTabStripModel(tab_strip_model);
 #if DCHECK_IS_ON()
-  DCHECK(known_browsers_.contains(browser));
+  DCHECK(base::Contains(known_browsers_, browser));
 #endif
 
   if (change.type() == TabStripModelChange::kInserted) {
     for (const auto& contents : change.GetInsert()->contents) {
-      if (webcontents_to_observer_map_.contains(contents.contents)) {
+      if (base::Contains(webcontents_to_observer_map_, contents.contents)) {
 #if DCHECK_IS_ON()
         {
           // The tab must be in the set of tabs in transit.
@@ -270,7 +271,7 @@ void BrowserStatusMonitor::OnTabStripModelChanged(
         OnTabMoved(tab_strip_model, contents.contents);
       } else {
 #if DCHECK_IS_ON()
-        DCHECK(!tabs_in_transit_.contains(contents.contents));
+        DCHECK(!base::Contains(tabs_in_transit_, contents.contents));
 #endif
         OnTabInserted(tab_strip_model, contents.contents);
       }
@@ -283,7 +284,7 @@ void BrowserStatusMonitor::OnTabStripModelChanged(
         case TabRemovedReason::kDeleted:
         case TabRemovedReason::kInsertedIntoSidePanel:
 #if DCHECK_IS_ON()
-          DCHECK(!tabs_in_transit_.contains(contents.contents));
+          DCHECK(!base::Contains(tabs_in_transit_, contents.contents));
 #endif
           OnTabClosing(contents.contents);
           break;
diff --git a/chrome/browser/ui/ash/shelf/chrome_shelf_controller.cc b/chrome/browser/ui/ash/shelf/chrome_shelf_controller.cc
index f02f76e87c..30aa255291 100644
--- a/chrome/browser/ui/ash/shelf/chrome_shelf_controller.cc
+++ b/chrome/browser/ui/ash/shelf/chrome_shelf_controller.cc
@@ -20,6 +20,7 @@
 #include "ash/public/cpp/window_properties.h"
 #include "ash/shell.h"
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -1653,7 +1654,7 @@ void ChromeShelfController::AddAppUpdaterAndIconLoader(Profile* profile) {
     }
   }
 
-  if (!app_updaters_.contains(profile)) {
+  if (!base::Contains(app_updaters_, profile)) {
     std::vector<std::unique_ptr<ShelfAppUpdater>>& app_updaters_for_profile =
         app_updaters_[profile];
     app_updaters_for_profile.push_back(
@@ -1671,7 +1672,7 @@ void ChromeShelfController::AddAppUpdaterAndIconLoader(Profile* profile) {
         std::make_unique<ShelfPromiseAppUpdater>(this, profile));
   }
 
-  if (!app_icon_loaders_.contains(profile)) {
+  if (!base::Contains(app_icon_loaders_, profile)) {
     std::vector<std::unique_ptr<AppIconLoader>>& app_icon_loaders_for_profile =
         app_icon_loaders_[profile];
     app_icon_loaders_for_profile.push_back(
diff --git a/chrome/browser/ui/ash/shelf/chrome_shelf_prefs.cc b/chrome/browser/ui/ash/shelf/chrome_shelf_prefs.cc
index 5c9d390cb4..41fd8f8ed7 100644
--- a/chrome/browser/ui/ash/shelf/chrome_shelf_prefs.cc
+++ b/chrome/browser/ui/ash/shelf/chrome_shelf_prefs.cc
@@ -755,9 +755,9 @@ void ChromeShelfPrefs::EnsureChromePinned() {
 }
 
 bool ChromeShelfPrefs::DidAddDefaultApps() const {
-  return profile_->GetPrefs()
-      ->GetList(GetShelfDefaultPinLayoutPref())
-      .contains(kDefaultPinnedAppsKey);
+  return base::Contains(
+      profile_->GetPrefs()->GetList(GetShelfDefaultPinLayoutPref()),
+      kDefaultPinnedAppsKey);
 }
 
 bool ChromeShelfPrefs::ShouldAddDefaultApps() const {
@@ -791,9 +791,9 @@ void ChromeShelfPrefs::AddDefaultApps() {
 }
 
 bool ChromeShelfPrefs::DidAddPreloadApps() const {
-  return profile_->GetPrefs()
-      ->GetList(GetShelfDefaultPinLayoutPref())
-      .contains(kPreloadPinnedAppsKey);
+  return base::Contains(
+      profile_->GetPrefs()->GetList(GetShelfDefaultPinLayoutPref()),
+      kPreloadPinnedAppsKey);
 }
 
 void ChromeShelfPrefs::PinPreloadApps() {
diff --git a/chrome/browser/ui/ash/wm/tab_cluster_ui_client.cc b/chrome/browser/ui/ash/wm/tab_cluster_ui_client.cc
index 2d3bddfe88..51cff97416 100644
--- a/chrome/browser/ui/ash/wm/tab_cluster_ui_client.cc
+++ b/chrome/browser/ui/ash/wm/tab_cluster_ui_client.cc
@@ -6,6 +6,7 @@
 
 #include "ash/public/cpp/tab_cluster/tab_cluster_ui_controller.h"
 #include "ash/public/cpp/tab_cluster/tab_cluster_ui_item.h"
+#include "base/containers/contains.h"
 #include "base/strings/strcat.h"
 #include "base/strings/utf_string_conversions.h"
 #include "chrome/browser/ash/browser_delegate/browser_controller.h"
diff --git a/chrome/browser/ui/blocked_content/safe_browsing_triggered_popup_blocker_browsertest.cc b/chrome/browser/ui/blocked_content/safe_browsing_triggered_popup_blocker_browsertest.cc
index 32e81aefcf..5fb01f63ce 100644
--- a/chrome/browser/ui/blocked_content/safe_browsing_triggered_popup_blocker_browsertest.cc
+++ b/chrome/browser/ui/blocked_content/safe_browsing_triggered_popup_blocker_browsertest.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/path_service.h"
 #include "base/test/bind.h"
 #include "base/test/metrics/histogram_tester.h"
@@ -70,11 +71,11 @@ void RoundTripAndVerifyLogMessages(
 
   for (size_t i = 0u; i < observer.messages().size(); ++i) {
     std::string message = observer.GetMessageAt(i);
-    if (messages_expected.contains(message)) {
+    if (base::Contains(messages_expected, message)) {
       messages_expected.erase(message);
       continue;
     }
-    if (messages_not_expected.contains(message)) {
+    if (base::Contains(messages_not_expected, message)) {
       ADD_FAILURE() << "Saw anti-expected message: " << message;
     }
   }
diff --git a/chrome/browser/ui/bookmarks/bookmark_utils_desktop.cc b/chrome/browser/ui/bookmarks/bookmark_utils_desktop.cc
index 7c70fa65b7..b89cc3038d 100644
--- a/chrome/browser/ui/bookmarks/bookmark_utils_desktop.cc
+++ b/chrome/browser/ui/bookmarks/bookmark_utils_desktop.cc
@@ -7,6 +7,7 @@
 #include <iterator>
 #include <numeric>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -364,7 +365,7 @@ void DoOpen(Browser* browser,
     // aren't already in groups.
     std::vector<int> tab_indices;
     for (int i = 0; i < model->count(); ++i) {
-      if (opened_web_contents.contains(model->GetWebContentsAt(i)) &&
+      if (base::Contains(opened_web_contents, model->GetWebContentsAt(i)) &&
           !model->GetTabGroupForTab(i).has_value()) {
         tab_indices.push_back(i);
       }
@@ -806,7 +807,7 @@ std::u16string SuggestUniqueTabGroupName(
     existing_titles.insert(group.title());
   }
 
-  if (!existing_titles.contains(folder_title)) {
+  if (!base::Contains(existing_titles, folder_title)) {
     return folder_title;
   }
 
@@ -814,7 +815,7 @@ std::u16string SuggestUniqueTabGroupName(
   for (int i = 1; i < kMaxAttempts; ++i) {
     std::u16string new_title =
         folder_title + u" (" + base::NumberToString16(i) + u")";
-    if (!existing_titles.contains(new_title)) {
+    if (!base::Contains(existing_titles, new_title)) {
       return new_title;
     }
   }
diff --git a/chrome/browser/ui/browser_actions.cc b/chrome/browser/ui/browser_actions.cc
index 19d0181ac8..4a26de9ba0 100644
--- a/chrome/browser/ui/browser_actions.cc
+++ b/chrome/browser/ui/browser_actions.cc
@@ -7,12 +7,12 @@
 #include <memory>
 #include <optional>
 #include <string>
-#include <utility>
 
 #include "base/check_deref.h"
 #include "base/check_op.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/app/chrome_command_ids.h"
 #include "chrome/app/vector_icons/vector_icons.h"
 #include "chrome/browser/contextual_tasks/contextual_tasks_side_panel_coordinator.h"
@@ -1150,7 +1150,7 @@ void BrowserActions::InitializeBrowserActions() {
                 if ((page_action_trigger !=
                      page_actions::kInvalidPageActionTrigger) &&
                     page_action_trigger ==
-                        std::to_underlying(
+                        base::to_underlying(
                             page_actions::PageActionTrigger::kKeyboard)) {
                   via_keyboard = true;
                 }
diff --git a/chrome/browser/ui/browser_list.cc b/chrome/browser/ui/browser_list.cc
index 0ff6437a32..b5b6021b9f 100644
--- a/chrome/browser/ui/browser_list.cc
+++ b/chrome/browser/ui/browser_list.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/user_metrics.h"
 #include "base/observer_list.h"
@@ -131,7 +132,7 @@ void BrowserList::RemoveObserver(BrowserListObserver* observer) {
 // static
 void BrowserList::SetLastActive(Browser* browser) {
   BrowserList* instance = GetInstance();
-  DCHECK(std::ranges::contains(instance->browsers_, browser))
+  DCHECK(base::Contains(instance->browsers_, browser))
       << "SetLastActive called for a browser before the browser was added to "
          "the BrowserList.";
   DCHECK(browser->window())
@@ -150,7 +151,7 @@ void BrowserList::SetLastActive(Browser* browser) {
 // static
 void BrowserList::NotifyBrowserNoLongerActive(Browser* browser) {
   BrowserList* instance = GetInstance();
-  DCHECK(std::ranges::contains(instance->browsers_, browser))
+  DCHECK(base::Contains(instance->browsers_, browser))
       << "NotifyBrowserNoLongerActive called for a browser before the browser "
          "was added to the BrowserList.";
   DCHECK(browser->window())
diff --git a/chrome/browser/ui/browser_list_enumerator.cc b/chrome/browser/ui/browser_list_enumerator.cc
index 060f2a9753..8c776b318b 100644
--- a/chrome/browser/ui/browser_list_enumerator.cc
+++ b/chrome/browser/ui/browser_list_enumerator.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 
 BrowserListEnumerator::BrowserListEnumerator(bool enumerate_new_browser)
     : enumerate_new_browser_(enumerate_new_browser),
@@ -29,7 +30,7 @@ BrowserListEnumerator::~BrowserListEnumerator() {
 }
 
 void BrowserListEnumerator::OnBrowserAdded(Browser* browser) {
-  DCHECK(!std::ranges::contains(browsers_, browser));
+  DCHECK(!base::Contains(browsers_, browser));
   if (enumerate_new_browser_) {
     browsers_.push_back(browser);
   }
diff --git a/chrome/browser/ui/cocoa/touchbar/credit_card_autofill_touch_bar_controller.mm b/chrome/browser/ui/cocoa/touchbar/credit_card_autofill_touch_bar_controller.mm
index df18f968c0..b497e38771 100644
--- a/chrome/browser/ui/cocoa/touchbar/credit_card_autofill_touch_bar_controller.mm
+++ b/chrome/browser/ui/cocoa/touchbar/credit_card_autofill_touch_bar_controller.mm
@@ -9,6 +9,7 @@
 #include "base/memory/raw_ptr.h"
 #include "base/strings/sys_string_conversions.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/app/vector_icons/vector_icons.h"
 #import "chrome/browser/ui/autofill/autofill_popup_controller.h"
 #include "components/autofill/core/browser/filling/filling_product.h"
diff --git a/chrome/browser/ui/file_system_access/file_system_access_ui_helpers.cc b/chrome/browser/ui/file_system_access/file_system_access_ui_helpers.cc
index 184411448b..3008ebe04b 100644
--- a/chrome/browser/ui/file_system_access/file_system_access_ui_helpers.cc
+++ b/chrome/browser/ui/file_system_access/file_system_access_ui_helpers.cc
@@ -7,6 +7,7 @@
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/url_identity.h"
@@ -72,10 +73,11 @@ std::u16string GetElidedPathForDisplayAsTitle(
   // containing a space will bump to the next line if the file name + preceding
   // text in the title is too long, which is still easy to read because the file
   // name is contiguous.
-  int scalar_numerators = GetPathForDisplayAsPath(path_info).value().contains(
-                              FILE_PATH_LITERAL(" "))
-                              ? 2
-                              : 3;
+  int scalar_numerators =
+      base::Contains(GetPathForDisplayAsPath(path_info).value(),
+                     FILE_PATH_LITERAL(" "))
+          ? 2
+          : 3;
   std::optional<int> preferred_width;
 #if defined(TOOLKIT_VIEWS)
   // views::LayoutProvider::Get() may be null in tests.
diff --git a/chrome/browser/ui/global_media_controls/cast_device_list_host.cc b/chrome/browser/ui/global_media_controls/cast_device_list_host.cc
index 50ef9c75a8..ba25584c57 100644
--- a/chrome/browser/ui/global_media_controls/cast_device_list_host.cc
+++ b/chrome/browser/ui/global_media_controls/cast_device_list_host.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/global_media_controls/cast_device_list_host.h"
 
+#include "base/containers/contains.h"
 #include "base/strings/utf_string_conversions.h"
 #include "chrome/browser/media/router/media_router_feature.h"
 #include "chrome/browser/ui/global_media_controls/media_item_ui_metrics.h"
@@ -46,7 +47,7 @@ bool SupportsTabAudioMirroring(media_router::CastModeSet cast_mode,
                                media_router::SinkIconType icon_type) {
   return base::FeatureList::IsEnabled(
              media_router::kFallbackToAudioTabMirroring) &&
-         cast_mode.contains(media_router::MediaCastMode::TAB_MIRROR) &&
+         base::Contains(cast_mode, media_router::MediaCastMode::TAB_MIRROR) &&
          (icon_type == media_router::SinkIconType::CAST_AUDIO ||
           icon_type == media_router::SinkIconType::CAST_AUDIO_GROUP);
 }
@@ -54,9 +55,10 @@ bool SupportsTabAudioMirroring(media_router::CastModeSet cast_mode,
 std::optional<media_router::MediaCastMode> GetPreferredCastMode(
     media_router::CastModeSet cast_mode,
     media_router::SinkIconType icon_type) {
-  if (cast_mode.contains(media_router::MediaCastMode::PRESENTATION)) {
+  if (base::Contains(cast_mode, media_router::MediaCastMode::PRESENTATION)) {
     return media_router::MediaCastMode::PRESENTATION;
-  } else if (cast_mode.contains(media_router::MediaCastMode::REMOTE_PLAYBACK)) {
+  } else if (base::Contains(cast_mode,
+                            media_router::MediaCastMode::REMOTE_PLAYBACK)) {
     return media_router::MediaCastMode::REMOTE_PLAYBACK;
   } else if (SupportsTabAudioMirroring(cast_mode, icon_type)) {
     return media_router::MediaCastMode::TAB_MIRROR;
diff --git a/chrome/browser/ui/global_media_controls/cast_media_notification_producer.cc b/chrome/browser/ui/global_media_controls/cast_media_notification_producer.cc
index 8801ac34a4..abdcc48484 100644
--- a/chrome/browser/ui/global_media_controls/cast_media_notification_producer.cc
+++ b/chrome/browser/ui/global_media_controls/cast_media_notification_producer.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <map>
 
+#include "base/containers/contains.h"
 #include "chrome/browser/feature_engagement/tracker_factory.h"
 #include "chrome/browser/media/router/media_router_feature.h"
 #include "chrome/browser/profiles/profile.h"
@@ -63,7 +64,7 @@ bool ShouldHideNotification(Profile* profile,
   // If the session is multizone member, then it would appear as a duplicate of
   // the multizone group's session, so it should instead be hidden.
   return source && source->GetAppIds().size() == 1 &&
-         std::ranges::contains(media_router::kMultizoneMemberAppIds,
+         base::Contains(media_router::kMultizoneMemberAppIds,
                                source->GetAppIds()[0]);
 }
 
@@ -157,7 +158,7 @@ void CastMediaNotificationProducer::OnRoutesUpdated(
   const bool had_items = HasActiveItems();
 
   std::erase_if(items_, [&routes](const auto& item) {
-    return !std::ranges::contains(routes, item.first,
+    return !base::Contains(routes, item.first,
                                   &media_router::MediaRoute::media_route_id);
   });
 
diff --git a/chrome/browser/ui/hats/hats_service_desktop.cc b/chrome/browser/ui/hats/hats_service_desktop.cc
index cc320cb4a7..31740b7c46 100644
--- a/chrome/browser/ui/hats/hats_service_desktop.cc
+++ b/chrome/browser/ui/hats/hats_service_desktop.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -743,7 +744,7 @@ void HatsServiceDesktop::CheckSurveyStatusAndMaybeShow(
   CHECK_EQ(product_specific_bits_data.size(),
            survey_config.product_specific_bits_data_fields.size());
   for (const auto& field_value : product_specific_bits_data) {
-    CHECK(std::ranges::contains(survey_config.product_specific_bits_data_fields,
+    CHECK(base::Contains(survey_config.product_specific_bits_data_fields,
                                 field_value.first));
   }
 
@@ -752,7 +753,7 @@ void HatsServiceDesktop::CheckSurveyStatusAndMaybeShow(
   CHECK_EQ(product_specific_string_data.size(),
            survey_config.product_specific_string_data_fields.size());
   for (const auto& field_value : product_specific_string_data) {
-    CHECK(std::ranges::contains(
+    CHECK(base::Contains(
         survey_config.product_specific_string_data_fields, field_value.first));
   }
 
diff --git a/chrome/browser/ui/hid/hid_chooser_controller.cc b/chrome/browser/ui/hid/hid_chooser_controller.cc
index 3ed18c0098..c24efcab61 100644
--- a/chrome/browser/ui/hid/hid_chooser_controller.cc
+++ b/chrome/browser/ui/hid/hid_chooser_controller.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/stringprintf.h"
 #include "chrome/browser/chooser_controller/title_util.h"
@@ -58,7 +59,7 @@ bool FilterMatch(const blink::mojom::HidDeviceFilterPtr& filter,
 
   if (filter->usage) {
     if (filter->usage->is_page()) {
-      if (!std::ranges::contains(
+      if (!base::Contains(
               device.collections, filter->usage->get_page(),
               [](const device::mojom::HidCollectionInfoPtr& c) {
                 return c->usage->usage_page;
@@ -140,7 +141,7 @@ size_t HidChooserController::NumOptions() const {
 
 std::u16string HidChooserController::GetOption(size_t index) const {
   DCHECK_LT(index, items_.size());
-  DCHECK(device_map_.contains(items_[index]));
+  DCHECK(base::Contains(device_map_, items_[index]));
   const auto& devices = device_map_.find(items_[index])->second;
   auto device = std::ranges::find_if(
       devices, [](const auto& d) { return !d->product_name.empty(); });
@@ -157,7 +158,7 @@ bool HidChooserController::IsPaired(size_t index) const {
     return false;
   }
 
-  DCHECK(device_map_.contains(items_[index]));
+  DCHECK(base::Contains(device_map_, items_[index]));
   const auto& device_infos = device_map_.find(items_[index])->second;
   DCHECK_GT(device_infos.size(), 0u);
   for (const auto& device : device_infos) {
@@ -179,7 +180,7 @@ void HidChooserController::Select(const std::vector<size_t>& indices) {
     return;
   }
 
-  DCHECK(device_map_.contains(items_[index]));
+  DCHECK(base::Contains(device_map_, items_[index]));
   auto& device_infos = device_map_.find(items_[index])->second;
   DCHECK_GT(device_infos.size(), 0u);
   std::vector<device::mojom::HidDeviceInfoPtr> devices;
@@ -260,7 +261,7 @@ void HidChooserController::OnDeviceRemoved(
 void HidChooserController::OnDeviceChanged(
     const device::mojom::HidDeviceInfo& device) {
   bool has_chooser_item =
-      std::ranges::contains(items_, PhysicalDeviceIdFromDeviceInfo(device));
+      base::Contains(items_, PhysicalDeviceIdFromDeviceInfo(device));
   if (!DisplayDevice(device)) {
     if (has_chooser_item) {
       OnDeviceRemoved(device);
@@ -310,7 +311,7 @@ bool HidChooserController::DisplayDevice(
   // devices may be displayed if the origin is privileged or the blocklist is
   // disabled.
   const bool has_fido_collection =
-      std::ranges::contains(device.collections, device::mojom::kPageFido,
+      base::Contains(device.collections, device::mojom::kPageFido,
                             [](const auto& c) { return c->usage->usage_page; });
   if (has_fido_collection) {
     if (base::CommandLine::ForCurrentProcess()->HasSwitch(
diff --git a/chrome/browser/ui/lens/lens_overlay_controller.cc b/chrome/browser/ui/lens/lens_overlay_controller.cc
index 3205b8e30b..18bd3e5b75 100644
--- a/chrome/browser/ui/lens/lens_overlay_controller.cc
+++ b/chrome/browser/ui/lens/lens_overlay_controller.cc
@@ -11,6 +11,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/json/json_reader.h"
 #include "base/metrics/histogram_functions.h"
@@ -434,7 +435,7 @@ void LensOverlayController::SendRegionText(lens::mojom::TextPtr text,
 
 lens::mojom::OverlayThemePtr LensOverlayController::CreateTheme(
     lens::PaletteId palette_id) {
-  CHECK(lens::kPaletteColors.contains(palette_id));
+  CHECK(base::Contains(lens::kPaletteColors, palette_id));
   const auto& palette = lens::kPaletteColors.at(palette_id);
   auto theme = lens::mojom::OverlayTheme::New();
   theme->primary = palette.at(lens::ColorId::kPrimary);
diff --git a/chrome/browser/ui/media_router/cast_modes_with_media_sources.cc b/chrome/browser/ui/media_router/cast_modes_with_media_sources.cc
index 1d8b59455f..5052f89f65 100644
--- a/chrome/browser/ui/media_router/cast_modes_with_media_sources.cc
+++ b/chrome/browser/ui/media_router/cast_modes_with_media_sources.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/media_router/cast_modes_with_media_sources.h"
 
+#include "base/containers/contains.h"
 
 namespace media_router {
 
@@ -32,8 +33,8 @@ void CastModesWithMediaSources::RemoveSource(MediaCastMode cast_mode,
 
 bool CastModesWithMediaSources::HasSource(MediaCastMode cast_mode,
                                           const MediaSource& source) const {
-  return cast_modes_.contains(cast_mode)
-             ? cast_modes_.at(cast_mode).contains(source)
+  return base::Contains(cast_modes_, cast_mode)
+             ? base::Contains(cast_modes_.at(cast_mode), source)
              : false;
 }
 
diff --git a/chrome/browser/ui/media_router/media_route_starter.cc b/chrome/browser/ui/media_router/media_route_starter.cc
index e1422a5054..509f0d9d5b 100644
--- a/chrome/browser/ui/media_router/media_route_starter.cc
+++ b/chrome/browser/ui/media_router/media_route_starter.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/strings/strcat.h"
 #include "base/strings/utf_string_conversions.h"
 #include "build/build_config.h"
@@ -99,8 +100,9 @@ MediaRouteStarter::~MediaRouteStarter() {
     bool presentation_sinks_available = std::ranges::any_of(
         GetQueryResultManager()->GetSinksWithCastModes(),
         [](const MediaSinkWithCastModes& sink) {
-          return sink.cast_modes.contains(MediaCastMode::PRESENTATION) ||
-                 sink.cast_modes.contains(MediaCastMode::REMOTE_PLAYBACK);
+          return base::Contains(sink.cast_modes, MediaCastMode::PRESENTATION) ||
+                 base::Contains(sink.cast_modes,
+                                MediaCastMode::REMOTE_PLAYBACK);
         });
     if (presentation_sinks_available) {
       start_presentation_context_->InvokeErrorCallback(
@@ -365,7 +367,7 @@ url::Origin MediaRouteStarter::GetFrameOrigin() const {
 
 bool MediaRouteStarter::IsCastModeAvailable(const CastModeSet& modes,
                                             MediaCastMode mode) {
-  return modes.contains(mode);
+  return base::Contains(modes, mode);
 }
 
 }  // namespace media_router
diff --git a/chrome/browser/ui/media_router/media_router_ui.cc b/chrome/browser/ui/media_router/media_router_ui.cc
index c98a2216a4..ff4725acc4 100644
--- a/chrome/browser/ui/media_router/media_router_ui.cc
+++ b/chrome/browser/ui/media_router/media_router_ui.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/atomic_sequence_num.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/strcat.h"
@@ -584,7 +585,7 @@ void MediaRouterUI::OnRoutesUpdated(const std::vector<MediaRoute>& routes) {
   }
 
   if (terminating_route_id_ &&
-      !std::ranges::contains(routes, terminating_route_id_.value(),
+      !base::Contains(routes, terminating_route_id_.value(),
                              &MediaRoute::media_route_id)) {
     terminating_route_id_.reset();
   }
diff --git a/chrome/browser/ui/media_router/query_result_manager.cc b/chrome/browser/ui/media_router/query_result_manager.cc
index 7be675759a..684ef704fd 100644
--- a/chrome/browser/ui/media_router/query_result_manager.cc
+++ b/chrome/browser/ui/media_router/query_result_manager.cc
@@ -8,6 +8,7 @@
 #include <unordered_set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/observer_list.h"
 #include "components/media_router/browser/media_router.h"
@@ -172,7 +173,7 @@ std::vector<MediaSinkWithCastModes> QueryResultManager::GetSinksWithCastModes()
     sinks.push_back(sink_with_cast_modes);
   }
   for (const auto& sink : all_sinks_) {
-    if (!sinks_with_sources_.contains(sink.id())) {
+    if (!base::Contains(sinks_with_sources_, sink.id())) {
       sinks.emplace_back(sink);
     }
   }
@@ -189,7 +190,7 @@ void QueryResultManager::RemoveOldSourcesForCastMode(
   }
 
   for (const MediaSource& source : cast_mode_it->second) {
-    if (!std::ranges::contains(new_sources, source)) {
+    if (!base::Contains(new_sources, source)) {
       sinks_observers_.erase(source);
       SetSinksCompatibleWithSource(cast_mode, source, std::vector<MediaSink>());
     }
@@ -201,7 +202,7 @@ void QueryResultManager::AddObserversForCastMode(
     const std::vector<MediaSource>& sources,
     const url::Origin& origin) {
   for (const MediaSource& source : sources) {
-    if (!sinks_observers_.contains(source)) {
+    if (!base::Contains(sinks_observers_, source)) {
       auto observer = std::make_unique<MediaSourceMediaSinksObserver>(
           cast_mode, source, origin, router_, this);
       observer->Init();
@@ -225,7 +226,7 @@ void QueryResultManager::SetSinksCompatibleWithSource(
        /* no-op */) {
     const MediaSink::Id& sink_id = it->first;
     CastModesWithMediaSources& sources_for_sink = it->second;
-    if (!new_sink_ids.contains(sink_id)) {
+    if (!base::Contains(new_sink_ids, sink_id)) {
       sources_for_sink.RemoveSource(cast_mode, source);
     }
     if (sources_for_sink.IsEmpty()) {
@@ -280,7 +281,7 @@ bool QueryResultManager::AreSourcesValidForCastMode(
   return std::ranges::none_of(sources, [=, this](const MediaSource& source) {
     return sinks_observers_.contains(source) &&
            (!has_cast_mode ||
-            !std::ranges::contains(cast_mode_it->second, source));
+            !base::Contains(cast_mode_it->second, source));
   });
 }
 
diff --git a/chrome/browser/ui/passwords/bubble_controllers/save_update_bubble_controller.cc b/chrome/browser/ui/passwords/bubble_controllers/save_update_bubble_controller.cc
index 41b9241477..3b1c11a810 100644
--- a/chrome/browser/ui/passwords/bubble_controllers/save_update_bubble_controller.cc
+++ b/chrome/browser/ui/passwords/bubble_controllers/save_update_bubble_controller.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/time/default_clock.h"
@@ -175,7 +176,7 @@ bool SaveUpdateBubbleController::IsCurrentStateUpdate() const {
   if (original_username_ == GetPendingPassword().username_value) {
     return GetState() == password_manager::ui::PENDING_PASSWORD_UPDATE_STATE;
   }
-  return std::ranges::contains(existing_credentials_,
+  return base::Contains(existing_credentials_,
                                GetPendingPassword().username_value,
                                &password_manager::PasswordForm::username_value);
 }
diff --git a/chrome/browser/ui/performance_controls/memory_saver_utils.cc b/chrome/browser/ui/performance_controls/memory_saver_utils.cc
index d1897b1db1..00e06f283a 100644
--- a/chrome/browser/ui/performance_controls/memory_saver_utils.cc
+++ b/chrome/browser/ui/performance_controls/memory_saver_utils.cc
@@ -6,6 +6,7 @@
 
 #include "base/byte_size.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "chrome/browser/performance_manager/public/user_tuning/user_performance_tuning_manager.h"
 #include "components/performance_manager/public/user_tuning/prefs.h"
 #include "content/public/common/url_constants.h"
diff --git a/chrome/browser/ui/performance_controls/performance_intervention_button_controller.cc b/chrome/browser/ui/performance_controls/performance_intervention_button_controller.cc
index c6736653ce..dd587934ed 100644
--- a/chrome/browser/ui/performance_controls/performance_intervention_button_controller.cc
+++ b/chrome/browser/ui/performance_controls/performance_intervention_button_controller.cc
@@ -8,6 +8,7 @@
 #include <cmath>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -149,7 +150,7 @@ void PerformanceInterventionButtonController::OnTabStripModelChanged(
     // Invalidate the actionable tab list since one of the actionable tabs is no
     // longer eligible and taking action on the remaining tabs no longer improve
     // resource health.
-    if (std::ranges::contains(actionable_cpu_tabs_,
+    if (base::Contains(actionable_cpu_tabs_,
                               current_page_context.value())) {
       actionable_cpu_tabs_.clear();
       HideToolbarButton(false);
diff --git a/chrome/browser/ui/profiles/profile_colors_util.cc b/chrome/browser/ui/profiles/profile_colors_util.cc
index 8178825f91..ab9bb4e883 100644
--- a/chrome/browser/ui/profiles/profile_colors_util.cc
+++ b/chrome/browser/ui/profiles/profile_colors_util.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/profiles/profile_colors_util.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/rand_util.h"
 #include "build/build_config.h"
@@ -69,7 +70,7 @@ std::vector<int> GetAvailableColorIndices(
     ProfileThemeColors theme_colors =
         GetProfileThemeColorsForAutogeneratedColor(
             chrome_colors::kGeneratedColorsInfo[i].color);
-    if (used_theme_colors.contains(theme_colors)) {
+    if (base::Contains(used_theme_colors, theme_colors)) {
       continue;
     }
 
diff --git a/chrome/browser/ui/safety_hub/notification_permission_review_service.cc b/chrome/browser/ui/safety_hub/notification_permission_review_service.cc
index 20ebfb257f..3bd1f4aa4e 100644
--- a/chrome/browser/ui/safety_hub/notification_permission_review_service.cc
+++ b/chrome/browser/ui/safety_hub/notification_permission_review_service.cc
@@ -9,6 +9,7 @@
 #include <set>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "chrome/browser/ui/safety_hub/safety_hub_result.h"
@@ -157,7 +158,7 @@ NotificationPermissionsReviewService::UpdateOnUIThread(
     }
 
     // Blocklisted permissions should not be in the review list.
-    if (ignored_patterns_set.contains(pair)) {
+    if (base::Contains(ignored_patterns_set, pair)) {
       continue;
     }
 
diff --git a/chrome/browser/ui/search_engines/template_url_table_model.cc b/chrome/browser/ui/search_engines/template_url_table_model.cc
index 58dca56af1..2cbcf6c651 100644
--- a/chrome/browser/ui/search_engines/template_url_table_model.cc
+++ b/chrome/browser/ui/search_engines/template_url_table_model.cc
@@ -10,6 +10,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/i18n/rtl.h"
 #include "base/strings/string_util.h"
@@ -239,7 +240,7 @@ void TemplateURLTableModel::ModifyTemplateURL(size_t index,
 TemplateURL* TemplateURLTableModel::GetTemplateURL(size_t index) {
   // Sanity checks for https://crbug.com/781703.
   CHECK_LT(index, entries_.size());
-  CHECK(std::ranges::contains(template_url_service_->GetTemplateURLs(),
+  CHECK(base::Contains(template_url_service_->GetTemplateURLs(),
                               entries_[index]))
       << "TemplateURLTableModel is returning a pointer to a TemplateURL "
          "that has already been freed by TemplateURLService.";
diff --git a/chrome/browser/ui/serial/serial_chooser_controller.cc b/chrome/browser/ui/serial/serial_chooser_controller.cc
index 467a005dbf..ae367435ad 100644
--- a/chrome/browser/ui/serial/serial_chooser_controller.cc
+++ b/chrome/browser/ui/serial/serial_chooser_controller.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
@@ -83,7 +84,7 @@ bool BluetoothPortIsAllowed(
   if (*port.bluetooth_service_class_id == device::GetSerialPortProfileUUID()) {
     return true;
   }
-  return std::ranges::contains(allowed_ids,
+  return base::Contains(allowed_ids,
                                port.bluetooth_service_class_id.value());
 }
 
diff --git a/chrome/browser/ui/settings_window_manager_chromeos.cc b/chrome/browser/ui/settings_window_manager_chromeos.cc
index b06c7d878d..c43f3f9039 100644
--- a/chrome/browser/ui/settings_window_manager_chromeos.cc
+++ b/chrome/browser/ui/settings_window_manager_chromeos.cc
@@ -4,8 +4,6 @@
 
 #include "chrome/browser/ui/settings_window_manager_chromeos.h"
 
-#include <utility>
-
 #include "ash/public/cpp/app_list/internal_app_id_constants.h"
 #include "ash/public/cpp/resources/grit/ash_public_unscaled_resources.h"
 #include "ash/public/cpp/shelf_item.h"
@@ -14,6 +12,7 @@
 #include "ash/wm/window_properties.h"
 #include "base/strings/strcat.h"
 #include "base/strings/string_number_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/app_mode/app_mode_utils.h"
 #include "chrome/browser/apps/app_service/launch_utils.h"
 #include "chrome/browser/profiles/profile.h"
@@ -228,7 +227,7 @@ void SettingsWindowManager::ShowOSSettings(
     int64_t display_id) {
   std::string path_with_setting_id =
       base::StrCat({sub_page, std::string("?settingId="),
-                    base::NumberToString(std::to_underlying(setting_id))});
+                    base::NumberToString(base::to_underlying(setting_id))});
 
   ShowOSSettings(profile, path_with_setting_id, display_id);
 }
diff --git a/chrome/browser/ui/startup/default_browser_prompt/default_browser_infobar_manager.cc b/chrome/browser/ui/startup/default_browser_prompt/default_browser_infobar_manager.cc
index 005743f21b..8d5bc9ff67 100644
--- a/chrome/browser/ui/startup/default_browser_prompt/default_browser_infobar_manager.cc
+++ b/chrome/browser/ui/startup/default_browser_prompt/default_browser_infobar_manager.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_functions.h"
@@ -158,7 +159,7 @@ void DefaultBrowserInfoBarManager::OnTabStripModelChanged(
     const TabStripSelectionChange& selection) {
   if (change.type() == TabStripModelChange::kInserted) {
     for (const auto& contents : change.GetInsert()->contents) {
-      if (!infobars_.contains(contents.contents)) {
+      if (!base::Contains(infobars_, contents.contents)) {
         CreateInfoBarForWebContents(contents.contents,
                                     tab_strip_model->profile());
       }
diff --git a/chrome/browser/ui/startup/startup_browser_creator.cc b/chrome/browser/ui/startup/startup_browser_creator.cc
index 26e845c75d..3a70a5fbf2 100644
--- a/chrome/browser/ui/startup/startup_browser_creator.cc
+++ b/chrome/browser/ui/startup/startup_browser_creator.cc
@@ -13,6 +13,7 @@
 
 #include "apps/switches.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
 #include "base/files/file_path.h"
diff --git a/chrome/browser/ui/tab_contents/chrome_web_contents_view_handle_drop.cc b/chrome/browser/ui/tab_contents/chrome_web_contents_view_handle_drop.cc
index a394b1be32..63081def9f 100644
--- a/chrome/browser/ui/tab_contents/chrome_web_contents_view_handle_drop.cc
+++ b/chrome/browser/ui/tab_contents/chrome_web_contents_view_handle_drop.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/memory/weak_ptr.h"
 #include "base/strings/utf_string_conversions.h"
@@ -39,9 +40,9 @@ void CompletionCallback(
     enterprise_connectors::ContentAnalysisDelegate::Result& result) {
   // If there are no negative results, proceed with just `drop_data`.
   bool all_text_results_allowed =
-      !std::ranges::contains(result.text_results, false);
+      !base::Contains(result.text_results, false);
   bool all_file_results_allowed =
-      !std::ranges::contains(result.paths_results, false);
+      !base::Contains(result.paths_results, false);
   if (all_text_results_allowed && all_file_results_allowed) {
     std::move(callback).Run(std::move(drop_data));
     return;
diff --git a/chrome/browser/ui/tabs/existing_tab_group_sub_menu_model.cc b/chrome/browser/ui/tabs/existing_tab_group_sub_menu_model.cc
index ff6fc95a07..25277e3716 100644
--- a/chrome/browser/ui/tabs/existing_tab_group_sub_menu_model.cc
+++ b/chrome/browser/ui/tabs/existing_tab_group_sub_menu_model.cc
@@ -7,6 +7,7 @@
 #include <optional>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/metrics/user_metrics.h"
 #include "base/metrics/user_metrics_action.h"
 #include "base/notreached.h"
diff --git a/chrome/browser/ui/tabs/organization/tab_organization.cc b/chrome/browser/ui/tabs/organization/tab_organization.cc
index 679f188d6d..d961334d21 100644
--- a/chrome/browser/ui/tabs/organization/tab_organization.cc
+++ b/chrome/browser/ui/tabs/organization/tab_organization.cc
@@ -10,6 +10,7 @@
 #include <unordered_set>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "chrome/browser/ui/tabs/organization/tab_data.h"
 #include "chrome/browser/ui/tabs/tab_group_model.h"
@@ -42,7 +43,7 @@ TabOrganization::TabOrganization(
   // TabDatas must not be duplicates, immediately destroy TabDatas that are.
   std::vector<const tabs::TabInterface*> existing_tabs;
   for (auto& tab_data : tab_datas) {
-    if (!std::ranges::contains(existing_tabs, tab_data->tab())) {
+    if (!base::Contains(existing_tabs, tab_data->tab())) {
       existing_tabs.emplace_back(tab_data->tab());
       tab_data->AddObserver(this);
       tab_datas_.emplace_back(std::move(tab_data));
@@ -161,7 +162,7 @@ void TabOrganization::Accept() {
     tab_data_tabs.insert(tab);
     const int index = tab_strip_model->GetIndexOfTab(tab);
     if (tab_data->IsValidForOrganizing() &&
-        !std::ranges::contains(valid_indices, index)) {
+        !base::Contains(valid_indices, index)) {
       valid_indices.emplace_back(index);
     }
   }
diff --git a/chrome/browser/ui/tabs/organization/tab_organization_service.cc b/chrome/browser/ui/tabs/organization/tab_organization_service.cc
index 963e5d8e06..5b0c6bc3b1 100644
--- a/chrome/browser/ui/tabs/organization/tab_organization_service.cc
+++ b/chrome/browser/ui/tabs/organization/tab_organization_service.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "chrome/browser/about_flags.h"
 #include "chrome/browser/browser_process.h"
 #include "chrome/browser/flag_descriptions.h"
@@ -47,7 +48,7 @@ TabOrganizationService::TabOrganizationService(
 TabOrganizationService::~TabOrganizationService() = default;
 
 void TabOrganizationService::OnTriggerOccured(const Browser* browser) {
-  if (browser_session_map_.contains(browser)) {
+  if (base::Contains(browser_session_map_, browser)) {
     // If the organizations havent been fully accepted or rejected, then it does
     // not need to be reset.
     if (!GetSessionForBrowser(browser)->IsComplete()) {
@@ -64,7 +65,7 @@ void TabOrganizationService::OnTriggerOccured(const Browser* browser) {
 
 const TabOrganizationSession* TabOrganizationService::GetSessionForBrowser(
     const Browser* browser) const {
-  if (browser_session_map_.contains(browser)) {
+  if (base::Contains(browser_session_map_, browser)) {
     return browser_session_map_.at(browser).get();
   }
   return nullptr;
@@ -72,7 +73,7 @@ const TabOrganizationSession* TabOrganizationService::GetSessionForBrowser(
 
 TabOrganizationSession* TabOrganizationService::GetSessionForBrowser(
     const Browser* browser) {
-  if (browser_session_map_.contains(browser)) {
+  if (base::Contains(browser_session_map_, browser)) {
     return browser_session_map_.at(browser).get();
   }
   return nullptr;
@@ -81,7 +82,7 @@ TabOrganizationSession* TabOrganizationService::GetSessionForBrowser(
 TabOrganizationSession* TabOrganizationService::CreateSessionForBrowser(
     const Browser* browser,
     const tabs::TabInterface* base_session_tab) {
-  CHECK(!browser_session_map_.contains(browser));
+  CHECK(!base::Contains(browser_session_map_, browser));
   CHECK(browser->tab_strip_model()->SupportsTabGroups());
   std::pair<BrowserSessionMap::iterator, bool> pair =
       browser_session_map_.emplace(
@@ -100,7 +101,7 @@ TabOrganizationSession* TabOrganizationService::ResetSessionForBrowser(
     const Browser* browser,
     const tabs::TabInterface* base_session_tab) {
   browser->tab_strip_model()->RemoveObserver(this);
-  if (browser_session_map_.contains(browser)) {
+  if (base::Contains(browser_session_map_, browser)) {
     RemoveBrowserFromSessionMap(browser);
   }
 
@@ -293,7 +294,7 @@ void TabOrganizationService::OnActionUIDismissed(const Browser* browser) {
 
 void TabOrganizationService::RemoveBrowserFromSessionMap(
     const Browser* browser) {
-  CHECK(browser_session_map_.contains(browser));
+  CHECK(base::Contains(browser_session_map_, browser));
   browser->tab_strip_model()->RemoveObserver(this);
   browser_session_map_.erase(browser);
 }
diff --git a/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_model_listener.cc b/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_model_listener.cc
index cdd4a4be2b..026e31b178 100644
--- a/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_model_listener.cc
+++ b/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_model_listener.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_window/public/browser_collection.h"
@@ -155,7 +156,7 @@ void SavedTabGroupModelListener::TabGroupedStateChanged(
 
   // Add it to its new group.
   if (new_local_group_id.has_value() &&
-      local_tab_group_listeners_.contains(new_local_group_id.value())) {
+      base::Contains(local_tab_group_listeners_, new_local_group_id.value())) {
     LocalTabGroupListener& listener =
         local_tab_group_listeners_.at(new_local_group_id.value());
     const Browser* const browser = SavedTabGroupUtils::GetBrowserWithTabGroupId(
@@ -221,7 +222,7 @@ void SavedTabGroupModelListener::WillCloseAllTabs(
 
   for (const tab_groups::TabGroupId& group_id :
        tab_strip_model->group_model()->ListTabGroups()) {
-    if (local_tab_group_listeners_.contains(group_id)) {
+    if (base::Contains(local_tab_group_listeners_, group_id)) {
       DisconnectLocalTabGroup(group_id, ClosingSource::kCloseAllTabs);
     }
   }
@@ -263,7 +264,7 @@ void SavedTabGroupModelListener::ConnectToLocalTabGroup(
 
 void SavedTabGroupModelListener::PauseTrackingLocalTabGroup(
     const tab_groups::TabGroupId& group_id) {
-  if (!local_tab_group_listeners_.contains(group_id)) {
+  if (!base::Contains(local_tab_group_listeners_, group_id)) {
     return;
   }
   local_tab_group_listeners_.at(group_id).PauseTracking();
@@ -271,7 +272,7 @@ void SavedTabGroupModelListener::PauseTrackingLocalTabGroup(
 
 void SavedTabGroupModelListener::ResumeTrackingLocalTabGroup(
     const tab_groups::TabGroupId& group_id) {
-  if (!local_tab_group_listeners_.contains(group_id)) {
+  if (!base::Contains(local_tab_group_listeners_, group_id)) {
     return;
   }
   local_tab_group_listeners_.at(group_id).ResumeTracking();
@@ -300,7 +301,7 @@ void SavedTabGroupModelListener::DisconnectLocalTabGroup(
 
 void SavedTabGroupModelListener::RemoveLocalGroupFromSync(
     tab_groups::TabGroupId local_group_id) {
-  if (local_tab_group_listeners_.contains(local_group_id)) {
+  if (base::Contains(local_tab_group_listeners_, local_group_id)) {
     // Prevent further observations for `local_group_id` as we attempt to close
     // the tab group.
     DisconnectLocalTabGroup(local_group_id, ClosingSource::kDeletedFromSync);
@@ -312,7 +313,7 @@ void SavedTabGroupModelListener::RemoveLocalGroupFromSync(
 
 void SavedTabGroupModelListener::UpdateLocalGroupFromSync(
     tab_groups::TabGroupId local_group_id) {
-  if (!local_tab_group_listeners_.contains(local_group_id)) {
+  if (!base::Contains(local_tab_group_listeners_, local_group_id)) {
     return;
   }
 
diff --git a/chrome/browser/ui/tabs/saved_tab_groups/tab_group_sync_delegate_desktop.cc b/chrome/browser/ui/tabs/saved_tab_groups/tab_group_sync_delegate_desktop.cc
index 38ad917e5a..49978a635e 100644
--- a/chrome/browser/ui/tabs/saved_tab_groups/tab_group_sync_delegate_desktop.cc
+++ b/chrome/browser/ui/tabs/saved_tab_groups/tab_group_sync_delegate_desktop.cc
@@ -7,6 +7,7 @@
 #include <iterator>
 #include <map>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/uuid.h"
 #include "chrome/browser/profiles/profile.h"
@@ -367,7 +368,7 @@ TabGroupId TabGroupSyncDelegateDesktop::AddOpenedTabsToGroup(
     bool switch_focus) {
   std::vector<int> tab_indices;
   for (int i = 0; tabs::TabInterface* tab : *tab_strip_model) {
-    if (tab_guid_mapping.contains(tab) && !tab->GetGroup().has_value()) {
+    if (base::Contains(tab_guid_mapping, tab) && !tab->GetGroup().has_value()) {
       tab_indices.push_back(i);
     }
     ++i;
diff --git a/chrome/browser/ui/tabs/tab_group_model.cc b/chrome/browser/ui/tabs/tab_group_model.cc
index 066a510941..03c9906eb8 100644
--- a/chrome/browser/ui/tabs/tab_group_model.cc
+++ b/chrome/browser/ui/tabs/tab_group_model.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/types/pass_key.h"
 #include "components/tab_groups/tab_group_color.h"
 #include "components/tab_groups/tab_group_id.h"
@@ -31,7 +32,7 @@ void TabGroupModel::AddTabGroup(TabGroup* group, base::PassKey<TabStripModel>) {
 }
 
 bool TabGroupModel::ContainsTabGroup(const tab_groups::TabGroupId& id) const {
-  return groups_.contains(id);
+  return base::Contains(groups_, id);
 }
 
 TabGroup* TabGroupModel::GetTabGroup(const tab_groups::TabGroupId& id) const {
diff --git a/chrome/browser/ui/test/browser_ui_test_base.cc b/chrome/browser/ui/test/browser_ui_test_base.cc
index 1aef5eadcf..a64cbc9e27 100644
--- a/chrome/browser/ui/test/browser_ui_test_base.cc
+++ b/chrome/browser/ui/test/browser_ui_test_base.cc
@@ -8,6 +8,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/process/launch.h"
 #include "base/strings/strcat.h"
diff --git a/chrome/browser/ui/toolbar/back_forward_menu_model.cc b/chrome/browser/ui/toolbar/back_forward_menu_model.cc
index 73ad633513..210af20e63 100644
--- a/chrome/browser/ui/toolbar/back_forward_menu_model.cc
+++ b/chrome/browser/ui/toolbar/back_forward_menu_model.cc
@@ -8,6 +8,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_functions.h"
@@ -442,7 +443,7 @@ bool BackForwardMenuModel::IsSeparator(size_t index) const {
 void BackForwardMenuModel::FetchFavicon(NavigationEntry* entry) {
   // If the favicon has already been requested for this menu, don't do
   // anything.
-  if (requested_favicons_.contains(entry->GetUniqueID())) {
+  if (base::Contains(requested_favicons_, entry->GetUniqueID())) {
     return;
   }
 
diff --git a/chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.cc b/chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.cc
index d95754feea..e7300c3f9f 100644
--- a/chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.cc
+++ b/chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/rand_util.h"
 #include "chrome/browser/about_flags.h"
@@ -88,7 +89,7 @@ void UpdateChromeLabsNewBadgePrefs(Profile* profile) {
   std::vector<std::string> entries_to_remove;
   for (auto pref : new_badge_prefs) {
     // The size of |lab_internal_names| is capped around 3-5 elements.
-    if (!std::ranges::contains(lab_internal_names, pref.first)) {
+    if (!base::Contains(lab_internal_names, pref.first)) {
       entries_to_remove.push_back(pref.first);
     }
   }
diff --git a/chrome/browser/ui/toolbar/toolbar_actions_model.cc b/chrome/browser/ui/toolbar/toolbar_actions_model.cc
index 89c4445bf4..6c1f017994 100644
--- a/chrome/browser/ui/toolbar/toolbar_actions_model.cc
+++ b/chrome/browser/ui/toolbar/toolbar_actions_model.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/metrics/histogram_base.h"
@@ -186,7 +187,7 @@ void ToolbarActionsModel::OnExtensionManagementSettingsChanged() {
       continue;
     }
 
-    const bool is_pinned = std::ranges::contains(new_pinned_list, action_id);
+    const bool is_pinned = base::Contains(new_pinned_list, action_id);
     const extensions::ManagedToolbarPinMode pin_mode =
         extension_management->GetToolbarPinMode(action_id);
 
@@ -305,7 +306,7 @@ const std::u16string ToolbarActionsModel::GetExtensionName(
 }
 
 bool ToolbarActionsModel::HasAction(const ActionId& action_id) const {
-  return action_ids_.contains(action_id);
+  return base::Contains(action_ids_, action_id);
 }
 
 bool ToolbarActionsModel::CanShowActionsInToolbar(
@@ -379,13 +380,13 @@ bool ToolbarActionsModel::IsPolicyBlockedHost(const GURL& url) const {
 }
 
 bool ToolbarActionsModel::IsActionPinned(const ActionId& action_id) const {
-  return std::ranges::contains(pinned_action_ids_, action_id);
+  return base::Contains(pinned_action_ids_, action_id);
 }
 
 bool ToolbarActionsModel::IsActionForcePinned(const ActionId& action_id) const {
   auto* management =
       extensions::ExtensionManagementFactory::GetForBrowserContext(profile_);
-  return management->GetForcePinnedList().contains(action_id);
+  return base::Contains(management->GetForcePinnedList(), action_id);
 }
 
 void ToolbarActionsModel::MovePinnedAction(const ActionId& action_id,
@@ -577,7 +578,7 @@ void ToolbarActionsModel::SetActionVisibility(const ActionId& action_id,
 
   auto stored_pinned_action_ids = extension_prefs_->GetPinnedExtensions();
   DCHECK_NE(is_now_visible,
-            std::ranges::contains(stored_pinned_action_ids, action_id));
+            base::Contains(stored_pinned_action_ids, action_id));
   if (is_now_visible) {
     stored_pinned_action_ids.push_back(action_id);
   } else {
@@ -625,7 +626,7 @@ ToolbarActionsModel::GetFilteredPinnedActionIds() const {
   std::ranges::copy_if(management->GetForcePinnedList(),
                        std::back_inserter(pinned),
                        [&pinned](const std::string& id) {
-                         return !std::ranges::contains(pinned, id);
+                         return !base::Contains(pinned, id);
                        });
 
   // TODO(pbos): Make sure that the pinned IDs are pruned from ExtensionPrefs on
diff --git a/chrome/browser/ui/unload_controller.cc b/chrome/browser/ui/unload_controller.cc
index cbb1624059..ca702e966a 100644
--- a/chrome/browser/ui/unload_controller.cc
+++ b/chrome/browser/ui/unload_controller.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/task/single_thread_task_runner.h"
@@ -285,7 +286,7 @@ UnloadController::GetTabsNeedingBeforeUnloadFired() const {
     // Note that we filter out tabs in `tabs_needing_unload_fired_` as they have
     // already had their BeforeUnload fired (and don't need it fired again
     // unless browser closing gets cancelled).
-    if (!tabs_needing_unload_fired_.contains(contents) &&
+    if (!base::Contains(tabs_needing_unload_fired_, contents) &&
         should_fire_beforeunload) {
       tabs_needing_beforeunload.insert(contents);
     }
diff --git a/chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view.cc b/chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view.cc
index ada54e0694..ee29aa4dc7 100644
--- a/chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view.cc
+++ b/chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view.cc
@@ -5,7 +5,6 @@
 #include "chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view.h"
 
 #include <string>
-#include <utility>
 #include <vector>
 
 #include "base/barrier_callback.h"
@@ -16,6 +15,7 @@
 #include "base/memory/weak_ptr.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
 #include "chrome/browser/apps/app_service/app_service_proxy_factory.h"
@@ -425,7 +425,7 @@ void AppUninstallDialogView::InitializeSubAppList(
     auto* sub_app_label =
         box->AddChildView(std::make_unique<views::Label>(sub_app.app_name));
 
-    sub_app_label->SetGroup(std::to_underlying(DialogViewID::SUB_APP_LABEL));
+    sub_app_label->SetGroup(base::to_underlying(DialogViewID::SUB_APP_LABEL));
 
     sub_app_label->SetHorizontalAlignment(gfx::ALIGN_LEFT);
     sub_app_label->SetMultiLine(true);
@@ -434,7 +434,7 @@ void AppUninstallDialogView::InitializeSubAppList(
         box->AddChildView(std::make_unique<views::ImageView>());
     sub_app_icon->SetImage(
         ui::ImageModel::FromImageSkia(sub_app.icon->uncompressed));
-    sub_app_icon->SetGroup(std::to_underlying(DialogViewID::SUB_APP_ICON));
+    sub_app_icon->SetGroup(base::to_underlying(DialogViewID::SUB_APP_ICON));
 
     box->SetBetweenChildSpacing(
         provider->GetDistanceMetric(views::DISTANCE_RELATED_LABEL_HORIZONTAL));
diff --git a/chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view_browsertest.cc b/chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view_browsertest.cc
index 5918bb3c48..2a412cc108 100644
--- a/chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view_browsertest.cc
+++ b/chrome/browser/ui/views/apps/app_dialog/app_uninstall_dialog_view_browsertest.cc
@@ -7,7 +7,6 @@
 #include <memory>
 #include <optional>
 #include <string>
-#include <utility>
 
 #include "base/containers/to_vector.h"
 #include "base/memory/raw_ptr.h"
@@ -17,6 +16,7 @@
 #include "base/test/bind.h"
 #include "base/test/gmock_expected_support.h"
 #include "base/test/run_until.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
 #include "chrome/browser/apps/app_service/app_service_proxy_factory.h"
 #include "chrome/browser/ash/app_list/arc/arc_app_icon.h"
@@ -325,7 +325,7 @@ class IsolatedWebAppsUninstallDialogViewBrowserTest
       AppUninstallDialogView::DialogViewID view_type) {
     std::vector<raw_ptr<views::View, VectorExperimental>> views_group;
 
-    view->GetViewsInGroup(std::to_underlying(view_type), &views_group);
+    view->GetViewsInGroup(base::to_underlying(view_type), &views_group);
     return base::ToVector(views_group, [](auto item) { return item.get(); });
   }
 };
diff --git a/chrome/browser/ui/views/autofill/payments/select_bnpl_issuer_dialog_interactive_uitest.cc b/chrome/browser/ui/views/autofill/payments/select_bnpl_issuer_dialog_interactive_uitest.cc
index e1688efa91..a81c43108c 100644
--- a/chrome/browser/ui/views/autofill/payments/select_bnpl_issuer_dialog_interactive_uitest.cc
+++ b/chrome/browser/ui/views/autofill/payments/select_bnpl_issuer_dialog_interactive_uitest.cc
@@ -463,7 +463,7 @@ IN_PROC_BROWSER_TEST_F(SelectBnplIssuerDialogInteractiveUiTest,
                 IDS_AUTOFILL_CARD_BNPL_SELECT_PROVIDER_FOOTNOTE_FOR_AI_AMOUNT_EXTRACTION_NOTE);
 
             // Verify the AI note text is present in the footnote.
-            EXPECT_TRUE(styled_label->GetText().contains(ai_note_text));
+            EXPECT_TRUE(base::Contains(styled_label->GetText(), ai_note_text));
 
             // Verify that none of the parts of the styled label that make up
             // the AI note are bold.
@@ -471,7 +471,7 @@ IN_PROC_BROWSER_TEST_F(SelectBnplIssuerDialogInteractiveUiTest,
               const views::Label* label_child =
                   views::AsViewClass<views::Label>(child);
               if (label_child &&
-                  ai_note_text.contains(label_child->GetText())) {
+                  base::Contains(ai_note_text, label_child->GetText())) {
                 EXPECT_NE(label_child->GetTextStyle(),
                           views::style::TextStyle::STYLE_EMPHASIZED);
               }
diff --git a/chrome/browser/ui/views/autofill/popup/popup_view_views.cc b/chrome/browser/ui/views/autofill/popup/popup_view_views.cc
index 9ea8231546..460143b68e 100644
--- a/chrome/browser/ui/views/autofill/popup/popup_view_views.cc
+++ b/chrome/browser/ui/views/autofill/popup/popup_view_views.cc
@@ -16,6 +16,7 @@
 #include <vector>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
diff --git a/chrome/browser/ui/views/bookmarks/saved_tab_groups/saved_tab_group_bar.cc b/chrome/browser/ui/views/bookmarks/saved_tab_groups/saved_tab_group_bar.cc
index 31be060a09..f3ed293e00 100644
--- a/chrome/browser/ui/views/bookmarks/saved_tab_groups/saved_tab_group_bar.cc
+++ b/chrome/browser/ui/views/bookmarks/saved_tab_groups/saved_tab_group_bar.cc
@@ -452,7 +452,7 @@ void SavedTabGroupBar::SavedTabGroupReordered() {
   for (size_t i = 0; i < groups.size(); ++i) {
     const base::Uuid& guid = groups[i]->saved_guid();
 
-    if (buttons_by_guid.contains(guid)) {
+    if (base::Contains(buttons_by_guid, guid)) {
       views::View* const button = buttons_by_guid[guid];
       ReorderChildView(button, i);
     }
diff --git a/chrome/browser/ui/views/contextual_tasks/contextual_tasks_ephemeral_button_controller.cc b/chrome/browser/ui/views/contextual_tasks/contextual_tasks_ephemeral_button_controller.cc
index deafe6eb85..c62b11cc58 100644
--- a/chrome/browser/ui/views/contextual_tasks/contextual_tasks_ephemeral_button_controller.cc
+++ b/chrome/browser/ui/views/contextual_tasks/contextual_tasks_ephemeral_button_controller.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "chrome/browser/autocomplete/aim_eligibility_service_factory.h"
 #include "chrome/browser/contextual_tasks/contextual_tasks_service_factory.h"
@@ -161,7 +162,7 @@ bool ContextualTasksEphemeralButtonController::ShouldShowEphemeralButton() {
   // The ephemeral toolbar button should show if the contextual task side panel
   // was closed.
   return current_task.has_value() &&
-         std::ranges::contains(ephemeral_button_eligible_tasks_,
+         base::Contains(ephemeral_button_eligible_tasks_,
                                current_task->GetTaskId());
 }
 
diff --git a/chrome/browser/ui/views/desktop_capture/desktop_media_picker_views.cc b/chrome/browser/ui/views/desktop_capture/desktop_media_picker_views.cc
index 4b24ea03ab..9b0ce07c2a 100644
--- a/chrome/browser/ui/views/desktop_capture/desktop_media_picker_views.cc
+++ b/chrome/browser/ui/views/desktop_capture/desktop_media_picker_views.cc
@@ -10,6 +10,7 @@
 
 #include "audio_capture_permission_checker_mac.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
diff --git a/chrome/browser/ui/views/download/bubble/download_bubble_row_list_view.cc b/chrome/browser/ui/views/download/bubble/download_bubble_row_list_view.cc
index 1c42d01429..6a7ab3088f 100644
--- a/chrome/browser/ui/views/download/bubble/download_bubble_row_list_view.cc
+++ b/chrome/browser/ui/views/download/bubble/download_bubble_row_list_view.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/views/download/bubble/download_bubble_row_list_view.h"
 
+#include "base/containers/contains.h"
 #include "chrome/browser/download/bubble/download_bubble_ui_controller.h"
 #include "chrome/browser/ui/views/chrome_layout_provider.h"
 #include "chrome/browser/ui/views/download/bubble/download_bubble_row_view.h"
@@ -40,7 +41,7 @@ DownloadBubbleRowListView::~DownloadBubbleRowListView() {
 void DownloadBubbleRowListView::AddRow(
     const DownloadBubbleRowViewInfo& row_info) {
   const ContentId& id = row_info.model()->GetContentId();
-  CHECK(!rows_by_id_.contains(id));
+  CHECK(!base::Contains(rows_by_id_, id));
   auto* child = AddChildView(std::make_unique<DownloadBubbleRowView>(
       row_info, bubble_controller_, navigation_handler_, browser_,
       fixed_width_));
diff --git a/chrome/browser/ui/views/extensions/extensions_menu_view.cc b/chrome/browser/ui/views/extensions/extensions_menu_view.cc
index 58c8896f08..52f5245487 100644
--- a/chrome/browser/ui/views/extensions/extensions_menu_view.cc
+++ b/chrome/browser/ui/views/extensions/extensions_menu_view.cc
@@ -8,6 +8,7 @@
 
 #include "base/auto_reset.h"
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/i18n/case_conversion.h"
 #include "base/memory/ptr_util.h"
@@ -419,7 +420,7 @@ void ExtensionsMenuView::SanityCheck() {
   // guarantees that we have a view per item in |action_ids| as well).
   for (ExtensionMenuItemView* item : extensions_menu_items_) {
     DCHECK(Contains(item));
-    DCHECK(action_ids.contains(item->view_model()->GetId()));
+    DCHECK(base::Contains(action_ids, item->view_model()->GetId()));
   }
 #endif
 }
diff --git a/chrome/browser/ui/views/file_system_access/file_system_access_usage_bubble_view.cc b/chrome/browser/ui/views/file_system_access/file_system_access_usage_bubble_view.cc
index e7b48cc6ef..7753242315 100644
--- a/chrome/browser/ui/views/file_system_access/file_system_access_usage_bubble_view.cc
+++ b/chrome/browser/ui/views/file_system_access/file_system_access_usage_bubble_view.cc
@@ -6,6 +6,7 @@
 
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/i18n/message_formatter.h"
 #include "base/i18n/unicodestring.h"
 #include "base/memory/raw_ptr.h"
@@ -331,12 +332,12 @@ void FileSystemAccessUsageBubbleView::ShowBubble(
   std::set<base::FilePath> writable_directories(
       usage.writable_directories.begin(), usage.writable_directories.end());
   std::erase_if(usage.readable_directories, [&](const base::FilePath& path) {
-    return writable_directories.contains(path);
+    return base::Contains(writable_directories, path);
   });
   std::set<base::FilePath> writable_files(usage.writable_files.begin(),
                                           usage.writable_files.end());
   std::erase_if(usage.readable_files, [&](const base::FilePath& path) {
-    return writable_files.contains(path);
+    return base::Contains(writable_files, path);
   });
 
   // TODO(crbug.com/376282751): An action ID should be created and used here
diff --git a/chrome/browser/ui/views/frame/browser_view.cc b/chrome/browser/ui/views/frame/browser_view.cc
index a29e3f73ef..b7410be10e 100644
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -18,6 +18,7 @@
 #include "base/check_deref.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -429,7 +430,7 @@ std::string FocusListToString(views::View* view) {
   base::flat_set<views::View*> seen_views;
 
   while (view) {
-    if (seen_views.contains(view)) {
+    if (base::Contains(seen_views, view)) {
       result << "*CYCLE TO " << view->GetClassName() << "*";
       break;
     }
@@ -448,7 +449,7 @@ void CheckFocusListForCycles(views::View* const start_view) {
   base::flat_set<views::View*> seen_views;
 
   while (view) {
-    DCHECK(!seen_views.contains(view)) << FocusListToString(start_view);
+    DCHECK(!base::Contains(seen_views, view)) << FocusListToString(start_view);
     seen_views.insert(view);
 
     views::View* next_view = view->GetNextFocusableView();
diff --git a/chrome/browser/ui/views/frame/dbus_appmenu.cc b/chrome/browser/ui/views/frame/dbus_appmenu.cc
index 1607101eb2..71e6bccbfe 100644
--- a/chrome/browser/ui/views/frame/dbus_appmenu.cc
+++ b/chrome/browser/ui/views/frame/dbus_appmenu.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -474,8 +475,8 @@ int DbusAppmenu::NextCommandId() {
     } else {
       last_command_id_++;
     }
-  } while (history_items_.contains(last_command_id_) ||
-           profile_commands_.contains(last_command_id_));
+  } while (base::Contains(history_items_, last_command_id_) ||
+           base::Contains(profile_commands_, last_command_id_));
   return last_command_id_;
 }
 
@@ -597,7 +598,7 @@ void DbusAppmenu::ExecuteCommand(int command_id, int event_flags) {
   } else if (command_id == kTagProfileCreate) {
     ProfilePicker::Show(ProfilePicker::Params::FromEntryPoint(
         ProfilePicker::EntryPoint::kProfileMenuAddNewProfile));
-  } else if (history_items_.contains(command_id)) {
+  } else if (base::Contains(history_items_, command_id)) {
     HistoryItem* item = history_items_[command_id].get();
     // If this item can be restored using TabRestoreService, do so.
     // Otherwise, just load the URL.
@@ -615,7 +616,7 @@ void DbusAppmenu::ExecuteCommand(int command_id, int event_flags) {
                                  ui::PAGE_TRANSITION_AUTO_BOOKMARK, false),
           /*navigation_handle_callback=*/{});
     }
-  } else if (profile_commands_.contains(command_id)) {
+  } else if (base::Contains(profile_commands_, command_id)) {
     avatar_menu_->SwitchToProfile(profile_commands_[command_id], false);
   }
 }
diff --git a/chrome/browser/ui/views/frame/dbus_appmenu_registrar.cc b/chrome/browser/ui/views/frame/dbus_appmenu_registrar.cc
index 78d1a4221e..35d08bb207 100644
--- a/chrome/browser/ui/views/frame/dbus_appmenu_registrar.cc
+++ b/chrome/browser/ui/views/frame/dbus_appmenu_registrar.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/views/frame/dbus_appmenu_registrar.h"
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/no_destructor.h"
@@ -41,7 +42,7 @@ void DbusAppmenuRegistrar::OnMenuBarCreated(DbusAppmenu* menu) {
 }
 
 void DbusAppmenuRegistrar::OnMenuBarDestroyed(DbusAppmenu* menu) {
-  DCHECK(menus_.contains(menu));
+  DCHECK(base::Contains(menus_, menu));
   if (menus_[menu] == kRegistered) {
     if (auto* toplevel_extension =
             ui::GetWaylandToplevelExtension(*menu->platform_window())) {
@@ -68,7 +69,7 @@ DbusAppmenuRegistrar::DbusAppmenuRegistrar()
 }
 
 void DbusAppmenuRegistrar::InitializeMenu(DbusAppmenu* menu) {
-  DCHECK(menus_.contains(menu));
+  DCHECK(base::Contains(menus_, menu));
   DCHECK_EQ(menus_[menu], kUninitialized);
   menus_[menu] = kInitializing;
   menu->Initialize(base::BindOnce(&DbusAppmenuRegistrar::OnMenuInitialized,
@@ -76,7 +77,7 @@ void DbusAppmenuRegistrar::InitializeMenu(DbusAppmenu* menu) {
 }
 
 void DbusAppmenuRegistrar::RegisterMenu(DbusAppmenu* menu) {
-  DCHECK(menus_.contains(menu));
+  DCHECK(base::Contains(menus_, menu));
   DCHECK(menus_[menu] == kInitializeSucceeded || menus_[menu] == kRegistered);
   menus_[menu] = kRegistered;
 
@@ -92,7 +93,7 @@ void DbusAppmenuRegistrar::RegisterMenu(DbusAppmenu* menu) {
 }
 
 void DbusAppmenuRegistrar::OnMenuInitialized(DbusAppmenu* menu, bool success) {
-  DCHECK(menus_.contains(menu));
+  DCHECK(base::Contains(menus_, menu));
   DCHECK(menus_[menu] == kInitializing);
   menus_[menu] = success ? kInitializeSucceeded : kInitializeFailed;
   if (success && service_has_owner_) {
diff --git a/chrome/browser/ui/views/frame/system_menu_model_builder_browsertest_chromeos.cc b/chrome/browser/ui/views/frame/system_menu_model_builder_browsertest_chromeos.cc
index 7f3bf36bc0..d711cdbdc7 100644
--- a/chrome/browser/ui/views/frame/system_menu_model_builder_browsertest_chromeos.cc
+++ b/chrome/browser/ui/views/frame/system_menu_model_builder_browsertest_chromeos.cc
@@ -3,6 +3,7 @@
 // found in the LICENSE file.
 
 #include "ash/public/cpp/autotest_desks_api.h"
+#include "base/containers/contains.h"
 #include "chrome/app/chrome_command_ids.h"
 #include "chrome/browser/ash/boca/on_task/on_task_locked_controller.h"
 #include "chrome/browser/ash/login/login_manager_test.h"
diff --git a/chrome/browser/ui/views/global_media_controls/media_item_ui_device_selector_view.cc b/chrome/browser/ui/views/global_media_controls/media_item_ui_device_selector_view.cc
index 161b643422..71d18a9119 100644
--- a/chrome/browser/ui/views/global_media_controls/media_item_ui_device_selector_view.cc
+++ b/chrome/browser/ui/views/global_media_controls/media_item_ui_device_selector_view.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/observer_list.h"
diff --git a/chrome/browser/ui/views/media_router/cast_browser_controller.cc b/chrome/browser/ui/views/media_router/cast_browser_controller.cc
index 0f3beb207f..5a6c8689aa 100644
--- a/chrome/browser/ui/views/media_router/cast_browser_controller.cc
+++ b/chrome/browser/ui/views/media_router/cast_browser_controller.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "chrome/browser/ui/actions/chrome_action_id.h"
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_actions.h"
@@ -58,7 +59,7 @@ void CastBrowserController::OnIssuesCleared() {
 void CastBrowserController::OnRoutesUpdated(
     const std::vector<media_router::MediaRoute>& routes) {
   has_local_route_ =
-      std::ranges::contains(routes, true, &media_router::MediaRoute::is_local);
+      base::Contains(routes, true, &media_router::MediaRoute::is_local);
   StopObservingMirroringMediaControllerHosts();
   for (const auto& route : routes) {
     const auto& route_id = route.media_route_id();
diff --git a/chrome/browser/ui/views/media_router/cast_dialog_view.cc b/chrome/browser/ui/views/media_router/cast_dialog_view.cc
index 8139ae2e93..c7fa31110a 100644
--- a/chrome/browser/ui/views/media_router/cast_dialog_view.cc
+++ b/chrome/browser/ui/views/media_router/cast_dialog_view.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/notreached.h"
@@ -430,15 +431,15 @@ std::optional<MediaCastMode> CastDialogView::GetCastModeToUse(
   // supported and selected.
   switch (selected_source_) {
     case SourceType::kTab:
-      if (sink.cast_modes.contains(PRESENTATION)) {
+      if (base::Contains(sink.cast_modes, PRESENTATION)) {
         return std::make_optional<MediaCastMode>(PRESENTATION);
       }
-      if (sink.cast_modes.contains(TAB_MIRROR)) {
+      if (base::Contains(sink.cast_modes, TAB_MIRROR)) {
         return std::make_optional<MediaCastMode>(TAB_MIRROR);
       }
       break;
     case SourceType::kDesktop:
-      if (sink.cast_modes.contains(DESKTOP_MIRROR)) {
+      if (base::Contains(sink.cast_modes, DESKTOP_MIRROR)) {
         return std::make_optional<MediaCastMode>(DESKTOP_MIRROR);
       }
       break;
diff --git a/chrome/browser/ui/views/omnibox/omnibox_view_views.cc b/chrome/browser/ui/views/omnibox/omnibox_view_views.cc
index c581d4f188..337ec8ab2c 100644
--- a/chrome/browser/ui/views/omnibox/omnibox_view_views.cc
+++ b/chrome/browser/ui/views/omnibox/omnibox_view_views.cc
@@ -31,6 +31,7 @@
 #include "base/trace_event/histogram_scope.h"
 #include "base/trace_event/trace_event.h"
 #include "base/trace_event/trace_id_helper.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "chrome/app/chrome_command_ids.h"
 #include "chrome/browser/autocomplete/aim_eligibility_service_factory.h"
@@ -739,7 +740,7 @@ void OmniboxViewViews::ExecuteCommand(int command_id, int event_flags) {
       return;
 
     // These commands do invoke the popup.
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kPaste):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kPaste):
       ExecuteTextEditCommand(ui::TextEditCommand::PASTE);
       return;
     default:
@@ -1783,7 +1784,7 @@ void OmniboxViewViews::OnBlur() {
 
 bool OmniboxViewViews::IsCommandIdEnabled(int command_id) const {
   if (command_id ==
-      std::to_underlying(ui::TouchEditable::MenuCommands::kPaste)) {
+      base::to_underlying(ui::TouchEditable::MenuCommands::kPaste)) {
     return !GetReadOnly() && CanGetClipboardText();
   }
   if (command_id == IDC_PASTE_AND_GO) {
@@ -2325,7 +2326,7 @@ void OmniboxViewViews::UpdateContextMenu(ui::SimpleMenuModel* menu_contents) {
 
   const std::optional<size_t> paste_position =
       menu_contents->GetIndexOfCommandId(
-          std::to_underlying(ui::TouchEditable::MenuCommands::kPaste));
+          base::to_underlying(ui::TouchEditable::MenuCommands::kPaste));
   DCHECK(paste_position.has_value());
   menu_contents->InsertItemWithStringIdAt(paste_position.value() + 1,
                                           IDC_PASTE_AND_GO, IDS_PASTE_AND_GO);
diff --git a/chrome/browser/ui/views/page_action/page_action_icon_controller.cc b/chrome/browser/ui/views/page_action/page_action_icon_controller.cc
index d76c496819..4dd8fda974 100644
--- a/chrome/browser/ui/views/page_action/page_action_icon_controller.cc
+++ b/chrome/browser/ui/views/page_action/page_action_icon_controller.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/immediate_crash.h"
@@ -363,7 +364,7 @@ void PageActionIconController::OnPageActionIconViewShown(
   std::vector<raw_ptr<PageActionIconView, VectorExperimental>>
       excluded_actions_on_page = page_actions_excluded_from_logging_[url];
   if (!view->ephemeral() ||
-      std::ranges::contains(excluded_actions_on_page, view)) {
+      base::Contains(excluded_actions_on_page, view)) {
     return;
   }
   RecordOverallMetrics();
@@ -436,7 +437,7 @@ void PageActionIconController::RecordMetricsOnURLChange(GURL url) {
   RecordOverallMetrics();
   for (auto icon_item : page_action_icon_views_) {
     if (!icon_item.second->ephemeral() || !icon_item.second->GetVisible() ||
-        std::ranges::contains(excluded_actions_on_page, icon_item.second)) {
+        base::Contains(excluded_actions_on_page, icon_item.second)) {
       continue;
     }
     RecordIndividualMetrics(icon_item.first, icon_item.second);
diff --git a/chrome/browser/ui/views/page_info/about_this_site_side_panel_coordinator.cc b/chrome/browser/ui/views/page_info/about_this_site_side_panel_coordinator.cc
index fda02b5424..ddb347ef4f 100644
--- a/chrome/browser/ui/views/page_info/about_this_site_side_panel_coordinator.cc
+++ b/chrome/browser/ui/views/page_info/about_this_site_side_panel_coordinator.cc
@@ -191,8 +191,8 @@ SidePanelUI* AboutThisSideSidePanelCoordinator::GetSidePanelUI() {
 
 GURL AboutThisSideSidePanelCoordinator::GetOpenInNewTabUrl() {
   DCHECK(last_url_info_.has_value());
-  DCHECK(!last_url_info_.value().new_tab_url.query().contains(
-      page_info::AboutThisSiteRenderModeParameterName));
+  DCHECK(!base::Contains(last_url_info_.value().new_tab_url.query(),
+                         page_info::AboutThisSiteRenderModeParameterName));
   return last_url_info_.value().new_tab_url;
 }
 
diff --git a/chrome/browser/ui/views/permissions/permission_prompt_bubble_one_origin_view.cc b/chrome/browser/ui/views/permissions/permission_prompt_bubble_one_origin_view.cc
index cec679747b..44f2a6e253 100644
--- a/chrome/browser/ui/views/permissions/permission_prompt_bubble_one_origin_view.cc
+++ b/chrome/browser/ui/views/permissions/permission_prompt_bubble_one_origin_view.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/memory/weak_ptr.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/string_number_conversions.h"
@@ -99,7 +100,7 @@ bool ShouldShowRequest(permissions::PermissionPrompt::Delegate& delegate,
                        permissions::RequestType type) {
   if (type == permissions::RequestType::kCameraStream) {
     // Hide camera request if camera PTZ request is present as well.
-    return !std::ranges::contains(
+    return !base::Contains(
         delegate.Requests(), permissions::RequestType::kCameraPanTiltZoom,
         &permissions::PermissionRequest::request_type);
   }
diff --git a/chrome/browser/ui/views/permissions/permission_prompt_chip.cc b/chrome/browser/ui/views/permissions/permission_prompt_chip.cc
index b82ad3148f..596fe3b1cd 100644
--- a/chrome/browser/ui/views/permissions/permission_prompt_chip.cc
+++ b/chrome/browser/ui/views/permissions/permission_prompt_chip.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "chrome/browser/ui/views/location_bar/location_bar_view.h"
diff --git a/chrome/browser/ui/views/profiles/profile_picker_view.cc b/chrome/browser/ui/views/profiles/profile_picker_view.cc
index 442fe7dc3f..9897c3a6ab 100644
--- a/chrome/browser/ui/views/profiles/profile_picker_view.cc
+++ b/chrome/browser/ui/views/profiles/profile_picker_view.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
 #include "base/files/file_path.h"
@@ -870,7 +871,7 @@ void ProfilePickerView::NavigateBack() {
 void ProfilePickerView::ConfigureAccelerators() {
   const std::vector<AcceleratorMapping> accelerator_list(GetAcceleratorList());
   for (const auto& entry : accelerator_list) {
-    if (!std::ranges::contains(kSupportedAcceleratorCommands,
+    if (!base::Contains(kSupportedAcceleratorCommands,
                                entry.command_id)) {
       continue;
     }
diff --git a/chrome/browser/ui/views/session_restore_infobar/session_restore_infobar_manager.cc b/chrome/browser/ui/views/session_restore_infobar/session_restore_infobar_manager.cc
index edffbe8e47..e7a72495af 100644
--- a/chrome/browser/ui/views/session_restore_infobar/session_restore_infobar_manager.cc
+++ b/chrome/browser/ui/views/session_restore_infobar/session_restore_infobar_manager.cc
@@ -139,7 +139,7 @@ void SessionRestoreInfoBarManager::OnTabStripModelChanged(
     const TabStripSelectionChange& selection) {
   if (change.type() == TabStripModelChange::kInserted) {
     for (const auto& contents : change.GetInsert()->contents) {
-      if (!infobars_.contains(contents.contents)) {
+      if (!base::Contains(infobars_, contents.contents)) {
         CreateInfoBarForWebContents(contents.contents);
       }
     }
diff --git a/chrome/browser/ui/views/shortcuts/shortcut_integration_interaction_test_internal.cc b/chrome/browser/ui/views/shortcuts/shortcut_integration_interaction_test_internal.cc
index 0c9d4b1a01..8a87739398 100644
--- a/chrome/browser/ui/views/shortcuts/shortcut_integration_interaction_test_internal.cc
+++ b/chrome/browser/ui/views/shortcuts/shortcut_integration_interaction_test_internal.cc
@@ -8,6 +8,7 @@
 #include <set>
 
 #include "base/base_paths.h"
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path_watcher.h"
 #include "base/files/file_util.h"
@@ -170,7 +171,7 @@ class ShortcutIntegrationInteractionTestPrivate::ShortcutTracker {
     // `TrackedShortcut` instance.
     std::vector<TrackedShortcut*> new_shortcuts;
     for (const base::FilePath& path : current_paths) {
-      if (shortcuts_.contains(path)) {
+      if (base::Contains(shortcuts_, path)) {
         continue;
       }
       std::unique_ptr<TrackedShortcut> shortcut;
@@ -192,7 +193,7 @@ class ShortcutIntegrationInteractionTestPrivate::ShortcutTracker {
     // Remove any paths from `shortcuts_` that no longer exist, notifying
     // `ElementTracker` of any that were tracked.
     std::erase_if(shortcuts_, [&](const auto& item) {
-      bool should_erase = !current_paths.contains(item.first);
+      bool should_erase = !base::Contains(current_paths, item.first);
       if (should_erase) {
         ui::ElementTracker::GetFrameworkDelegate()->NotifyElementHidden(
             item.second.get());
diff --git a/chrome/browser/ui/views/status_bubble_views.cc b/chrome/browser/ui/views/status_bubble_views.cc
index 01a7b1aca2..c976bd1c86 100644
--- a/chrome/browser/ui/views/status_bubble_views.cc
+++ b/chrome/browser/ui/views/status_bubble_views.cc
@@ -22,6 +22,7 @@
 #include "base/task/task_traits.h"
 #include "base/task/thread_pool.h"
 #include "base/timer/timer.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "cc/paint/paint_flags.h"
 #include "chrome/browser/ui/color/chrome_color_id.h"
@@ -489,7 +490,7 @@ void StatusView::OnPaint(gfx::Canvas* canvas) {
 
   std::array<SkVector, 4> rad{};
   auto round_corner = [&rad, radius](gfx::RRectF::Corner c) {
-    int index = std::to_underlying(c);
+    int index = base::to_underlying(c);
     rad[index] = {radius, radius};
   };
 
diff --git a/chrome/browser/ui/views/sync/inline_login_ui_browsertest.cc b/chrome/browser/ui/views/sync/inline_login_ui_browsertest.cc
index 504427245e..71fb73a875 100644
--- a/chrome/browser/ui/views/sync/inline_login_ui_browsertest.cc
+++ b/chrome/browser/ui/views/sync/inline_login_ui_browsertest.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/ref_counted.h"
diff --git a/chrome/browser/ui/views/tab_sharing/tab_capture_contents_border_helper.cc b/chrome/browser/ui/views/tab_sharing/tab_capture_contents_border_helper.cc
index 5cf2ae0f4d..fa56f94cac 100644
--- a/chrome/browser/ui/views/tab_sharing/tab_capture_contents_border_helper.cc
+++ b/chrome/browser/ui/views/tab_sharing/tab_capture_contents_border_helper.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/views/tab_sharing/tab_capture_contents_border_helper.h"
 
 #include "base/callback_list.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "build/build_config.h"
 #include "chrome/browser/browser_features.h"
@@ -29,7 +30,7 @@ TabCaptureContentsBorderHelper::~TabCaptureContentsBorderHelper() = default;
 void TabCaptureContentsBorderHelper::OnCapturerAdded(
     CaptureSessionId capture_session_id) {
   DCHECK_CURRENTLY_ON(content::BrowserThread::UI);
-  DCHECK(!session_to_bounds_.contains(capture_session_id));
+  DCHECK(!base::Contains(session_to_bounds_, capture_session_id));
 
   session_to_bounds_[capture_session_id] = std::nullopt;
 
@@ -57,7 +58,7 @@ void TabCaptureContentsBorderHelper::OnRegionCaptureRectChanged(
     CaptureSessionId capture_session_id,
     const std::optional<gfx::Rect>& region_capture_rect) {
   DCHECK_CURRENTLY_ON(content::BrowserThread::UI);
-  DCHECK(session_to_bounds_.contains(capture_session_id));
+  DCHECK(base::Contains(session_to_bounds_, capture_session_id));
 
   if (region_capture_rect &&
       region_capture_rect->width() >= kMinContentsBorderWidth &&
diff --git a/chrome/browser/ui/views/tab_sharing/tab_sharing_ui_views.cc b/chrome/browser/ui/views/tab_sharing/tab_sharing_ui_views.cc
index 878305e2d1..4874c4c89c 100644
--- a/chrome/browser/ui/views/tab_sharing/tab_sharing_ui_views.cc
+++ b/chrome/browser/ui/views/tab_sharing/tab_sharing_ui_views.cc
@@ -13,6 +13,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -467,7 +468,7 @@ void TabSharingUIViews::CreateInfobarForWebContents(WebContents* contents) {
   // active, create an observer that will inform us when its compliance state
   // changes.
   if (capturer_restricted_to_same_origin_ && is_share_instead_button_possible &&
-      !same_origin_observers_.contains(contents)) {
+      !base::Contains(same_origin_observers_, contents)) {
     // We explicitly remove all infobars and clear all policy observers before
     // destruction, so base::Unretained is safe here.
     same_origin_observers_[contents] = std::make_unique<SameOriginObserver>(
diff --git a/chrome/browser/ui/views/tabs/dragging/tab_drag_controller.cc b/chrome/browser/ui/views/tabs/dragging/tab_drag_controller.cc
index a029502d41..c76a5b283c 100644
--- a/chrome/browser/ui/views/tabs/dragging/tab_drag_controller.cc
+++ b/chrome/browser/ui/views/tabs/dragging/tab_drag_controller.cc
@@ -14,6 +14,7 @@
 #include "base/auto_reset.h"
 #include "base/check.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -377,7 +378,7 @@ TabDragController::Liveness TabDragController::Init(
     ui::ListSelectionModel initial_selection_model,
     ui::mojom::DragEventSource event_source) {
   DCHECK(!dragging_views.empty());
-  DCHECK(std::ranges::contains(dragging_views, source_view));
+  DCHECK(base::Contains(dragging_views, source_view));
 
   // There's some rare condition on Windows where we are destroying ourselves
   // during Init() at some point. It's unclear how this happens, so we'll take
diff --git a/chrome/browser/ui/views/tabs/fake_base_tab_strip_controller.cc b/chrome/browser/ui/views/tabs/fake_base_tab_strip_controller.cc
index 4e762e30e6..085ce8dc00 100644
--- a/chrome/browser/ui/views/tabs/fake_base_tab_strip_controller.cc
+++ b/chrome/browser/ui/views/tabs/fake_base_tab_strip_controller.cc
@@ -4,9 +4,9 @@
 
 #include "chrome/browser/ui/views/tabs/fake_base_tab_strip_controller.h"
 
-#include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "chrome/browser/ui/tabs/tab_renderer_data.h"
 #include "chrome/browser/ui/views/tabs/tab_strip.h"
 #include "components/tab_groups/tab_group_color.h"
@@ -136,14 +136,14 @@ void FakeBaseTabStripController::RemoveTabFromGroup(int model_index) {
 void FakeBaseTabStripController::MoveTabIntoGroup(
     int index,
     std::optional<tab_groups::TabGroupId> new_group) {
-  bool group_exists = std::ranges::contains(tab_groups_, new_group);
+  bool group_exists = base::Contains(tab_groups_, new_group);
   std::optional<tab_groups::TabGroupId> old_group = tab_groups_[index];
 
   tab_groups_[index] = new_group;
 
   if (tab_strip_ && old_group.has_value()) {
     tab_strip_->AddTabToGroup(std::nullopt, index);
-    if (!std::ranges::contains(tab_groups_, old_group)) {
+    if (!base::Contains(tab_groups_, old_group)) {
       tab_strip_->OnGroupClosed(old_group.value());
     } else {
       tab_strip_->OnGroupContentsChanged(old_group.value());
diff --git a/chrome/browser/ui/views/tabs/tab_strip.cc b/chrome/browser/ui/views/tabs/tab_strip.cc
index 5c7588e2be..6f48cade35 100644
--- a/chrome/browser/ui/views/tabs/tab_strip.cc
+++ b/chrome/browser/ui/views/tabs/tab_strip.cc
@@ -20,6 +20,7 @@
 #include "base/check.h"
 #include "base/compiler_specific.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -329,7 +330,7 @@ class TabStrip::TabDragContextImpl : public TabDragContext,
 
     CHECK(!dragging_views.empty())
         << "Dragging views cannot be empty during drag initialization.";
-    CHECK(std::ranges::contains(dragging_views, source))
+    CHECK(base::Contains(dragging_views, source))
         << "Source must be part of dragging views.";
 
     // Delete the existing DragController before creating a new one. We do this
diff --git a/chrome/browser/ui/views/tabs/vertical/tab_collection_animating_layout_manager.cc b/chrome/browser/ui/views/tabs/vertical/tab_collection_animating_layout_manager.cc
index 9a60fb04c2..a23571d007 100644
--- a/chrome/browser/ui/views/tabs/vertical/tab_collection_animating_layout_manager.cc
+++ b/chrome/browser/ui/views/tabs/vertical/tab_collection_animating_layout_manager.cc
@@ -292,7 +292,7 @@ void TabCollectionAnimatingLayoutManager::ResetToTargetLayout() {
 
 void TabCollectionAnimatingLayoutManager::AnimateAndDestroyChildView(
     views::View* child_view) {
-  DCHECK(std::ranges::contains(host_view()->children(), child_view));
+  DCHECK(base::Contains(host_view()->children(), child_view));
   child_view->SetCanProcessEventsWithinSubtree(false);
   child_view->SetFocusBehavior(views::View::FocusBehavior::NEVER);
   child_view->SetProperty(kPendingDeletion, true);
diff --git a/chrome/browser/ui/views/tabs/vertical/tab_collection_node_browsertest.cc b/chrome/browser/ui/views/tabs/vertical/tab_collection_node_browsertest.cc
index 4c1f9fe112..c5b299cd27 100644
--- a/chrome/browser/ui/views/tabs/vertical/tab_collection_node_browsertest.cc
+++ b/chrome/browser/ui/views/tabs/vertical/tab_collection_node_browsertest.cc
@@ -398,8 +398,8 @@ IN_PROC_BROWSER_TEST_F(TabCollectionNodeBrowserTest,
 
   const auto& child_views = root_node()->GetDirectChildren();
   ASSERT_GE(child_views.size(), 2u);
-  EXPECT_FALSE(std::ranges::contains(child_views, non_collection_view));
-  EXPECT_FALSE(std::ranges::contains(child_views, non_collection_view_2));
+  EXPECT_FALSE(base::Contains(child_views, non_collection_view));
+  EXPECT_FALSE(base::Contains(child_views, non_collection_view_2));
 }
 
 IN_PROC_BROWSER_TEST_F(TabCollectionNodeBrowserTest,
diff --git a/chrome/browser/ui/views/tabs/vertical/vertical_split_tab_view.cc b/chrome/browser/ui/views/tabs/vertical/vertical_split_tab_view.cc
index 381ebc14f3..af1fa3b26d 100644
--- a/chrome/browser/ui/views/tabs/vertical/vertical_split_tab_view.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_split_tab_view.cc
@@ -7,6 +7,7 @@
 #include <numeric>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "chrome/browser/ui/color/chrome_color_id.h"
 #include "chrome/browser/ui/layout_constants.h"
 #include "chrome/browser/ui/tabs/tab_style.h"
@@ -227,7 +228,7 @@ void VerticalSplitTabView::UpdateHovered(bool hovered) {
 
 std::unique_ptr<views::View>
 VerticalSplitTabView::RemoveChildViewForReparenting(views::View* child_view) {
-  DCHECK(std::ranges::contains(children(), child_view));
+  DCHECK(base::Contains(children(), child_view));
   CHECK(collection_node_);
 
   auto children = collection_node_->GetDirectChildren();
diff --git a/chrome/browser/ui/views/toolbar/chrome_labs/chrome_labs_view_controller.cc b/chrome/browser/ui/views/toolbar/chrome_labs/chrome_labs_view_controller.cc
index 5061fde12c..fadbefbc5e 100644
--- a/chrome/browser/ui/views/toolbar/chrome_labs/chrome_labs_view_controller.cc
+++ b/chrome/browser/ui/views/toolbar/chrome_labs/chrome_labs_view_controller.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/callback_list.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/strings/string_number_conversions.h"
@@ -119,7 +120,7 @@ int ChromeLabsViewController::GetIndexOfEnabledLabState(
   flags_state->GetSanitizedEnabledFlags(flags_storage, &enabled_entries);
   for (int i = 0; i < entry->NumOptions(); i++) {
     const std::string name = entry->NameForOption(i);
-    if (enabled_entries.contains(name)) {
+    if (base::Contains(enabled_entries, name)) {
       return i;
     }
   }
diff --git a/chrome/browser/ui/views/toolbar/pinned_toolbar_actions_container.cc b/chrome/browser/ui/views/toolbar/pinned_toolbar_actions_container.cc
index a1aa92a9dd..60b50682c3 100644
--- a/chrome/browser/ui/views/toolbar/pinned_toolbar_actions_container.cc
+++ b/chrome/browser/ui/views/toolbar/pinned_toolbar_actions_container.cc
@@ -10,6 +10,7 @@
 #include <string>
 #include <type_traits>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/raw_ref.h"
@@ -753,7 +754,7 @@ void PinnedToolbarActionsContainer::UpdateViews() {
   // 1. Remove buttons for actions in the UI that are not present in the
   // model.
   for (actions::ActionId id : old_ids) {
-    if (std::ranges::contains(new_ids, id)) {
+    if (base::Contains(new_ids, id)) {
       continue;
     }
 
@@ -767,7 +768,7 @@ void PinnedToolbarActionsContainer::UpdateViews() {
 
   // 2. Add buttons for actions that are in the model but not in the UI.
   for (actions::ActionId id : new_ids) {
-    if (std::ranges::contains(old_ids, id)) {
+    if (base::Contains(old_ids, id)) {
       continue;
     }
 
diff --git a/chrome/browser/ui/views/toolbar/toolbar_icon_container_view.cc b/chrome/browser/ui/views/toolbar/toolbar_icon_container_view.cc
index b82d4e4f54..130baa284b 100644
--- a/chrome/browser/ui/views/toolbar/toolbar_icon_container_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_icon_container_view.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
@@ -200,7 +201,7 @@ bool ToolbarIconContainerView::GetHighlighted() const {
       return true;
     }
     // The container should also be highlighted if a dialog is anchored to.
-    if (highlighted_buttons_.contains(button)) {
+    if (base::Contains(highlighted_buttons_, button)) {
       return true;
     }
   }
diff --git a/chrome/browser/ui/views/user_education/browser_user_education_service_browsertest.cc b/chrome/browser/ui/views/user_education/browser_user_education_service_browsertest.cc
index 0d63380b0f..fd6bbd229c 100644
--- a/chrome/browser/ui/views/user_education/browser_user_education_service_browsertest.cc
+++ b/chrome/browser/ui/views/user_education/browser_user_education_service_browsertest.cc
@@ -351,7 +351,7 @@ IN_PROC_BROWSER_TEST_F(BrowserUserEducationServiceBrowserTest,
   for (const auto& [feature, spec] : registry.feature_data()) {
     // If the feature is not on the known features list, no configuration is
     // possible.
-    if (!known_features.contains(feature)) {
+    if (!base::Contains(known_features, feature)) {
       failures.emplace_back(feature, IPHFailureReason::kUnlisted, nullptr);
       continue;
     }
@@ -544,7 +544,7 @@ IN_PROC_BROWSER_TEST_F(BrowserUserEducationServiceBrowserTest,
           (step.step_type() == ui::InteractionSequence::StepType::kShown &&
            step.body_text_id());
       const bool is_always_visible =
-          kAlwaysPresentElementIds.contains(step.element_id());
+          base::Contains(kAlwaysPresentElementIds, step.element_id());
       if (is_show_bubble && was_show_bubble && is_always_visible &&
           !step.transition_only_on_event()) {
         failures.push_back(
diff --git a/chrome/browser/ui/views/user_education/feature_promo_lifecycle_interactive_uitest.cc b/chrome/browser/ui/views/user_education/feature_promo_lifecycle_interactive_uitest.cc
index 56e7ba9f78..3dc3743df3 100644
--- a/chrome/browser/ui/views/user_education/feature_promo_lifecycle_interactive_uitest.cc
+++ b/chrome/browser/ui/views/user_education/feature_promo_lifecycle_interactive_uitest.cc
@@ -6,6 +6,7 @@
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
 #include "base/strings/strcat.h"
@@ -571,8 +572,8 @@ class FeaturePromoLifecycleAppUiTest : public FeaturePromoLifecycleUiTest {
     return CheckBrowser(base::BindOnce([](Browser* browser) {
              const auto data = GetStorageService(browser)->ReadPromoData(
                  kFeaturePromoLifecycleTestPromo);
-             return data->shown_for_keys.contains(
-                 browser->app_controller()->app_id());
+             return base::Contains(data->shown_for_keys,
+                                   browser->app_controller()->app_id());
            }))
         .SetDescription("CheckShownForApp()");
   }
diff --git a/chrome/browser/ui/views/web_apps/force_installed_preinstalled_deprecated_app_dialog_view.cc b/chrome/browser/ui/views/web_apps/force_installed_preinstalled_deprecated_app_dialog_view.cc
index 822814aa1c..754818d2dc 100644
--- a/chrome/browser/ui/views/web_apps/force_installed_preinstalled_deprecated_app_dialog_view.cc
+++ b/chrome/browser/ui/views/web_apps/force_installed_preinstalled_deprecated_app_dialog_view.cc
@@ -8,6 +8,7 @@
 
 #include "ash/constants/web_app_id_constants.h"
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
@@ -111,7 +112,7 @@ void ForceInstalledPreinstalledDeprecatedAppDialogView::CreateAndShowDialog(
   if (GetLinkConfigForTesting()) {                    // IN-TEST
     link_config = GetLinkConfigForTesting().value();  // IN-TEST
   } else {
-    CHECK(GetChromeAppConfigs().contains(extension_id));
+    CHECK(base::Contains(GetChromeAppConfigs(), extension_id));
     link_config = GetChromeAppConfigs().at(extension_id);
   }
 
diff --git a/chrome/browser/ui/views/web_apps/protocol_handler_picker_coordinator.cc b/chrome/browser/ui/views/web_apps/protocol_handler_picker_coordinator.cc
index a73d4dc0ee..57c8774a15 100644
--- a/chrome/browser/ui/views/web_apps/protocol_handler_picker_coordinator.cc
+++ b/chrome/browser/ui/views/web_apps/protocol_handler_picker_coordinator.cc
@@ -4,11 +4,11 @@
 
 #include "chrome/browser/ui/views/web_apps/protocol_handler_picker_coordinator.h"
 
-#include <algorithm>
 #include <string>
 #include <vector>
 
 #include "base/barrier_callback.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/memory/weak_ptr.h"
 #include "base/metrics/histogram_functions.h"
@@ -123,7 +123,7 @@ std::optional<std::string> ProtocolHandlerPickerCoordinator::FindPreferredApp(
     const std::vector<std::string>& app_ids) {
   if (std::optional<std::string> app_id =
           proxy_->PreferredAppsList().FindPreferredAppForUrl(protocol_url)) {
-    if (std::ranges::contains(app_ids, *app_id)) {
+    if (base::Contains(app_ids, *app_id)) {
       return app_id;
     }
   }
diff --git a/chrome/browser/ui/views/web_apps/sub_apps_install_dialog.cc b/chrome/browser/ui/views/web_apps/sub_apps_install_dialog.cc
index 094783c893..60b0ec8d9f 100644
--- a/chrome/browser/ui/views/web_apps/sub_apps_install_dialog.cc
+++ b/chrome/browser/ui/views/web_apps/sub_apps_install_dialog.cc
@@ -17,6 +17,7 @@
 #include "base/memory/weak_ptr.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/views/web_apps/sub_apps_install_dialog_controller.h"
 #include "chrome/browser/ui/web_applications/web_app_dialogs.h"
@@ -100,7 +101,7 @@ PermissionsExplanation(int num_sub_apps,
   auto label = std::make_unique<views::StyledLabel>();
   label->SetText(formatted_string);
   label->SetDefaultTextStyle(views::style::STYLE_SECONDARY);
-  label->SetID(std::to_underlying(
+  label->SetID(base::to_underlying(
       SubAppsInstallDialogController::SubAppsInstallDialogViewID::
           MANAGE_PERMISSIONS_LINK));
 
@@ -169,7 +170,7 @@ class SubAppsListView : public views::ScrollView {
                              kSubAppIconSize, image_info.bitmaps),
                          gfx::Size(kSubAppIconSize, kSubAppIconSize))));
       sub_app_icon->SetGroup(
-          std::to_underlying(SubAppsInstallDialogController::
+          base::to_underlying(SubAppsInstallDialogController::
                                  SubAppsInstallDialogViewID::SUB_APP_ICON));
       icon_views_.push_back(sub_app_icon);
 
@@ -190,7 +191,7 @@ class SubAppsListView : public views::ScrollView {
       sub_app_label->SetHorizontalAlignment(gfx::ALIGN_LEFT);
       sub_app_label->SetMultiLine(true);
       sub_app_label->SetGroup(
-          std::to_underlying(SubAppsInstallDialogController::
+          base::to_underlying(SubAppsInstallDialogController::
                                  SubAppsInstallDialogViewID::SUB_APP_LABEL));
 
       icon_index++;
diff --git a/chrome/browser/ui/views/web_apps/web_app_integration_test_driver.cc b/chrome/browser/ui/views/web_apps/web_app_integration_test_driver.cc
index a6c2f1e325..67cd7cdac4 100644
--- a/chrome/browser/ui/views/web_apps/web_app_integration_test_driver.cc
+++ b/chrome/browser/ui/views/web_apps/web_app_integration_test_driver.cc
@@ -16,6 +16,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
@@ -472,17 +473,17 @@ base::flat_map<Site, ScopeConfig> g_scope_configs = {
      {.manifest_url_param = "?manifest=manifest_scope_Standalone.json"}}};
 
 ScopeConfig GetScopeUpdateConfiguration(Site scope) {
-  CHECK(g_scope_configs.contains(scope));
+  CHECK(base::Contains(g_scope_configs, scope));
   return g_scope_configs.find(scope)->second;
 }
 
 DisplayConfig GetDisplayUpdateConfiguration(Display display) {
-  CHECK(g_display_configs.contains(display));
+  CHECK(base::Contains(g_display_configs, display));
   return g_display_configs.find(display)->second;
 }
 
 SiteConfig GetSiteConfiguration(Site site) {
-  CHECK(g_site_configs.contains(site));
+  CHECK(base::Contains(g_site_configs, site));
   return g_site_configs.find(site)->second;
 }
 
@@ -498,7 +499,7 @@ std::string GetRelativeSubAppPath(Site sub_app) {
   return sub_app_path;
 }
 std::string GetSiteId(Site site) {
-  return base::NumberToString(std::to_underlying(site));
+  return base::NumberToString(base::to_underlying(site));
 }
 
 web_package::test::Ed25519KeyPair GetKeyPairForSite(Site site) {
@@ -530,7 +531,7 @@ SiteConfig GetSiteConfigurationFromAppName(const std::string& app_name) {
   bool is_app_found = false;
   for (auto const& [site, check_config] : g_site_configs) {
     if (check_config.app_name == app_name ||
-        check_config.alternate_titles.contains(app_name)) {
+        base::Contains(check_config.alternate_titles, app_name)) {
       config = check_config;
       is_app_found = true;
       break;
diff --git a/chrome/browser/ui/views/web_apps/web_app_update_review_dialog_browsertest.cc b/chrome/browser/ui/views/web_apps/web_app_update_review_dialog_browsertest.cc
index bcf2b3b8b6..09f5df6619 100644
--- a/chrome/browser/ui/views/web_apps/web_app_update_review_dialog_browsertest.cc
+++ b/chrome/browser/ui/views/web_apps/web_app_update_review_dialog_browsertest.cc
@@ -121,18 +121,18 @@ class WebAppUpdateReviewDialog : public DialogBrowserTest {
   // DialogBrowserTest:
   void ShowUi(const std::string& name) override {
     // Modify the update configuration based on the test name.
-    if (name.contains("NameChange")) {
+    if (base::Contains(name, "NameChange")) {
       update_.new_title =
           u"Definitely a longer title that is really really really really "
           u"long.";
     }
-    if (name.contains("IconChange")) {
+    if (base::Contains(name, "IconChange")) {
       update_.new_icon = gfx::Image::CreateFrom1xBitmap(new_icon_);
     }
-    if (name.contains("UrlChange")) {
+    if (base::Contains(name, "UrlChange")) {
       update_.new_start_url = GURL("http://other.test.com");
     }
-    if (name.contains("ForcedMigration")) {
+    if (base::Contains(name, "ForcedMigration")) {
       update_.new_start_url = GURL("http://other.test.com");
       update_.is_forced_migration = true;
     }
diff --git a/chrome/browser/ui/web_applications/app_browser_controller.cc b/chrome/browser/ui/web_applications/app_browser_controller.cc
index 9aec0f703d..2f5f847502 100644
--- a/chrome/browser/ui/web_applications/app_browser_controller.cc
+++ b/chrome/browser/ui/web_applications/app_browser_controller.cc
@@ -4,9 +4,9 @@
 
 #include "chrome/browser/ui/web_applications/app_browser_controller.h"
 
-#include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/memory/values_equivalent.h"
@@ -417,7 +417,7 @@ std::vector<actions::ActionId> AppBrowserController::GetTitleBarPageActions()
 
 #if DCHECK_IS_ON()
   for (auto action_id : types_enabled) {
-    DCHECK(std::ranges::contains(page_actions::kActionIds, action_id));
+    DCHECK(base::Contains(page_actions::kActionIds, action_id));
   }
 #endif
 
diff --git a/chrome/browser/ui/web_applications/create_shortcut_browsertest.cc b/chrome/browser/ui/web_applications/create_shortcut_browsertest.cc
index 80600dea19..bac7212b1c 100644
--- a/chrome/browser/ui/web_applications/create_shortcut_browsertest.cc
+++ b/chrome/browser/ui/web_applications/create_shortcut_browsertest.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/path_service.h"
diff --git a/chrome/browser/ui/web_applications/navigation_capturing_process.cc b/chrome/browser/ui/web_applications/navigation_capturing_process.cc
index a334853869..d58ec3b359 100644
--- a/chrome/browser/ui/web_applications/navigation_capturing_process.cc
+++ b/chrome/browser/ui/web_applications/navigation_capturing_process.cc
@@ -4,14 +4,13 @@
 
 #include "chrome/browser/ui/web_applications/navigation_capturing_process.h"
 
-#include <utility>
-
 #include "base/debug/crash_logging.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/string_split.h"
 #include "base/strings/to_string.h"
 #include "base/time/clock.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/optional_util.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/browser.h"
@@ -94,7 +93,7 @@ bool IsPageTransitionValidForNavigationCapturing(
     default:
       NOTREACHED();
   }
-  if (std::to_underlying(ui::PageTransitionGetQualifier(transition)) != 0) {
+  if (base::to_underlying(ui::PageTransitionGetQualifier(transition)) != 0) {
     // Qualifiers indicate that this navigation was the result of a click on a
     // forward/back button, typing in the URL bar, or client-side redirections.
     // Don't handle any of those types of navigations.
diff --git a/chrome/browser/ui/web_applications/web_app_browsertest.cc b/chrome/browser/ui/web_applications/web_app_browsertest.cc
index e63975fd79..d766f4da8c 100644
--- a/chrome/browser/ui/web_applications/web_app_browsertest.cc
+++ b/chrome/browser/ui/web_applications/web_app_browsertest.cc
@@ -6,7 +6,6 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <set>
@@ -2771,7 +2770,7 @@ IN_PROC_BROWSER_TEST_F(WebAppBrowserTest_FileHandler, FileAssociation) {
         GetFileExtensionsForProgId(file_handler_prog_id);
     for (const auto& file_extension : file_extensions) {
       const std::string extension = base::WideToUTF8(file_extension.substr(1));
-      EXPECT_TRUE(std::ranges::contains(expected_extensions, extension))
+      EXPECT_TRUE(base::Contains(expected_extensions, extension))
           << "Missing file extension: " << extension;
       const std::wstring reg_key =
           L"Software\\Classes\\" + file_extension + L"\\OpenWithProgids";
diff --git a/chrome/browser/ui/web_applications/web_app_protocol_handling_browsertest.cc b/chrome/browser/ui/web_applications/web_app_protocol_handling_browsertest.cc
index 65f6d31796..224e319572 100644
--- a/chrome/browser/ui/web_applications/web_app_protocol_handling_browsertest.cc
+++ b/chrome/browser/ui/web_applications/web_app_protocol_handling_browsertest.cc
@@ -2,8 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/test/metrics/histogram_tester.h"
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_commands.h"
@@ -94,8 +93,8 @@ IN_PROC_BROWSER_TEST_F(WebAppProtocolHandlingBrowserTest,
       "/banners/manifest_protocol_handlers.json?testing=%s"));
 
   ASSERT_EQ(2u, protocol_handlers.size());
-  EXPECT_TRUE(std::ranges::contains(protocol_handlers, protocol_handler1));
-  EXPECT_TRUE(std::ranges::contains(protocol_handlers, protocol_handler2));
+  EXPECT_TRUE(base::Contains(protocol_handlers, protocol_handler1));
+  EXPECT_TRUE(base::Contains(protocol_handlers, protocol_handler2));
 
   histogram_tester_.ExpectBucketCount(kUseCounterHistogram,
                                       protocol_handling_feature, 1);
diff --git a/chrome/browser/ui/webauthn/authenticator_dialog_browsertest.cc b/chrome/browser/ui/webauthn/authenticator_dialog_browsertest.cc
index 6d20c4a8b7..a5216de359 100644
--- a/chrome/browser/ui/webauthn/authenticator_dialog_browsertest.cc
+++ b/chrome/browser/ui/webauthn/authenticator_dialog_browsertest.cc
@@ -75,8 +75,9 @@ void UpdateModelBeforeStartFlow(
       tai.attestation_conveyance_preference;
   model->ble_adapter_is_powered =
       tai.ble_status == device::FidoRequestHandlerBase::BleStatus::kOn;
-  model->show_security_key_on_qr_sheet = tai.available_transports.contains(
-      device::FidoTransportProtocol::kUsbHumanInterfaceDevice);
+  model->show_security_key_on_qr_sheet =
+      base::Contains(tai.available_transports,
+                     device::FidoTransportProtocol::kUsbHumanInterfaceDevice);
   model->is_off_the_record = is_off_the_record;
   model->platform_has_biometrics = tai.platform_has_biometrics;
 }
diff --git a/chrome/browser/ui/webui/access_code_cast/access_code_cast_handler.cc b/chrome/browser/ui/webui/access_code_cast/access_code_cast_handler.cc
index beb593370a..eae1177cd3 100644
--- a/chrome/browser/ui/webui/access_code_cast/access_code_cast_handler.cc
+++ b/chrome/browser/ui/webui/access_code_cast/access_code_cast_handler.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <numeric>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/rand_util.h"
 #include "base/strings/string_number_conversions.h"
@@ -201,7 +202,7 @@ void AccessCodeCastHandler::AddSink(
 }
 
 bool AccessCodeCastHandler::IsCastModeAvailable(MediaCastMode mode) const {
-  return cast_mode_set_.contains(mode);
+  return base::Contains(cast_mode_set_, mode);
 }
 
 // Discovery is not complete until the sink is in QRM. This is because any
@@ -382,8 +383,8 @@ void AccessCodeCastHandler::OnRouteResponse(MediaCastMode cast_mode,
 
 bool AccessCodeCastHandler::HasActiveRoute(const MediaSink::Id& sink_id) {
   return GetMediaRouter() &&
-         std::ranges::contains(GetMediaRouter()->GetCurrentRoutes(), sink_id,
-                               &MediaRoute::media_sink_id);
+         base::Contains(GetMediaRouter()->GetCurrentRoutes(), sink_id,
+                        &MediaRoute::media_sink_id);
 }
 
 void AccessCodeCastHandler::SetIdentityManagerForTesting(
diff --git a/chrome/browser/ui/webui/app_management/app_management_shelf_delegate_chromeos.cc b/chrome/browser/ui/webui/app_management/app_management_shelf_delegate_chromeos.cc
index 336a2d250b..b395f88fdc 100644
--- a/chrome/browser/ui/webui/app_management/app_management_shelf_delegate_chromeos.cc
+++ b/chrome/browser/ui/webui/app_management/app_management_shelf_delegate_chromeos.cc
@@ -4,11 +4,10 @@
 
 #include "chrome/browser/ui/webui/app_management/app_management_shelf_delegate_chromeos.h"
 
-#include <algorithm>
-
 #include "ash/public/cpp/shelf_item.h"
 #include "ash/public/cpp/shelf_model.h"
 #include "ash/public/cpp/shelf_types.h"
+#include "base/containers/contains.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/ash/shelf/app_shortcut_shelf_item_controller.h"
 #include "chrome/browser/ui/ash/shelf/chrome_shelf_controller.h"
@@ -74,7 +73,7 @@ bool AppManagementShelfDelegate::IsPolicyPinned(
   std::vector<std::string> policy_pinned_apps =
       ChromeShelfPrefs::GetAppsPinnedByPolicy(
           shelf_controller_helper_->profile());
-  return std::ranges::contains(policy_pinned_apps, app_id);
+  return base::Contains(policy_pinned_apps, app_id);
 }
 
 void AppManagementShelfDelegate::SetPinned(const std::string& app_id,
diff --git a/chrome/browser/ui/webui/ash/cloud_upload/cloud_open_metrics.cc b/chrome/browser/ui/webui/ash/cloud_upload/cloud_open_metrics.cc
index 814ceef27b..cb13ab6b81 100644
--- a/chrome/browser/ui/webui/ash/cloud_upload/cloud_open_metrics.cc
+++ b/chrome/browser/ui/webui/ash/cloud_upload/cloud_open_metrics.cc
@@ -6,10 +6,10 @@
 
 #include <concepts>
 #include <string>
-#include <utility>
 
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ui/webui/ash/cloud_upload/cloud_upload_util.h"
 
 namespace ash::cloud_upload {
@@ -33,7 +33,7 @@ std::ostream& operator<<(std::ostream& os, T metric) {
         return os << "WVL";
     }
   }
-  return os << std::to_underlying(metric);
+  return os << base::to_underlying(metric);
 }
 
 // Print debug information about this metric.
diff --git a/chrome/browser/ui/webui/ash/cloud_upload/cloud_upload_dialog.cc b/chrome/browser/ui/webui/ash/cloud_upload/cloud_upload_dialog.cc
index 3e958d75e4..060cb67c3a 100644
--- a/chrome/browser/ui/webui/ash/cloud_upload/cloud_upload_dialog.cc
+++ b/chrome/browser/ui/webui/ash/cloud_upload/cloud_upload_dialog.cc
@@ -4,8 +4,6 @@
 
 #include "chrome/browser/ui/webui/ash/cloud_upload/cloud_upload_dialog.h"
 
-#include <utility>
-
 #include "ash/constants/web_app_id_constants.h"
 #include "ash/public/cpp/new_window_delegate.h"
 #include "ash/resources/vector_icons/vector_icons.h"
@@ -26,6 +24,7 @@
 #include "base/strings/utf_string_conversions.h"
 #include "base/time/time.h"
 #include "base/timer/elapsed_timer.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
 #include "chrome/browser/apps/app_service/app_service_proxy_factory.h"
 #include "chrome/browser/ash/arc/fileapi/arc_documents_provider_util.h"
@@ -1482,7 +1481,7 @@ void CloudOpenTask::LocalTaskExecuted(
     LOG(ERROR) << "Execution of local file task with app id " << task.app_id
                << " to open office files. Led to error message: "
                << error_message
-               << " and result: " << std::to_underlying(result);
+               << " and result: " << base::to_underlying(result);
     return;
   }
 
diff --git a/chrome/browser/ui/webui/ash/cloud_upload/drive_upload_handler.cc b/chrome/browser/ui/webui/ash/cloud_upload/drive_upload_handler.cc
index b824b44996..1fc06c56f6 100644
--- a/chrome/browser/ui/webui/ash/cloud_upload/drive_upload_handler.cc
+++ b/chrome/browser/ui/webui/ash/cloud_upload/drive_upload_handler.cc
@@ -44,14 +44,16 @@ const int kAlternateUrlPollInterval = 200;
 
 std::string GetTargetAppName(base::FilePath file_path) {
   const std::string extension = base::ToLowerASCII(file_path.FinalExtension());
-  if (file_manager::file_tasks::WordGroupExtensions().contains(extension)) {
+  if (base::Contains(file_manager::file_tasks::WordGroupExtensions(),
+                     extension)) {
     return l10n_util::GetStringUTF8(IDS_OFFICE_FILE_HANDLER_APP_GOOGLE_DOCS);
   }
-  if (file_manager::file_tasks::ExcelGroupExtensions().contains(extension)) {
+  if (base::Contains(file_manager::file_tasks::ExcelGroupExtensions(),
+                     extension)) {
     return l10n_util::GetStringUTF8(IDS_OFFICE_FILE_HANDLER_APP_GOOGLE_SHEETS);
   }
-  if (file_manager::file_tasks::PowerPointGroupExtensions().contains(
-          extension)) {
+  if (base::Contains(file_manager::file_tasks::PowerPointGroupExtensions(),
+                     extension)) {
     return l10n_util::GetStringUTF8(IDS_OFFICE_FILE_HANDLER_APP_GOOGLE_SLIDES);
   }
   return l10n_util::GetStringUTF8(IDS_OFFICE_FILE_HANDLER_APP_GOOGLE_DOCS);
diff --git a/chrome/browser/ui/webui/ash/edu_coexistence/edu_coexistence_state_tracker.cc b/chrome/browser/ui/webui/ash/edu_coexistence/edu_coexistence_state_tracker.cc
index 327604ea0c..4a18827319 100644
--- a/chrome/browser/ui/webui/ash/edu_coexistence/edu_coexistence_state_tracker.cc
+++ b/chrome/browser/ui/webui/ash/edu_coexistence/edu_coexistence_state_tracker.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/no_destructor.h"
@@ -37,7 +38,7 @@ void EduCoexistenceStateTracker::SetEduConsentCallback(
     const content::WebUI* web_ui,
     const std::string& account_email,
     base::OnceCallback<void(bool)> consent_logged_callback) {
-  DCHECK(state_tracker_.contains(web_ui));
+  DCHECK(base::Contains(state_tracker_, web_ui));
   FlowState& state = state_tracker_[web_ui];
 
   if (state.received_consent) {
@@ -52,7 +53,7 @@ void EduCoexistenceStateTracker::SetEduConsentCallback(
 }
 
 void EduCoexistenceStateTracker::OnDialogClosed(const content::WebUI* web_ui) {
-  if (!state_tracker_.contains(web_ui)) {
+  if (!base::Contains(state_tracker_, web_ui)) {
     return;
   }
 
@@ -77,7 +78,7 @@ void EduCoexistenceStateTracker::OnDialogClosed(const content::WebUI* web_ui) {
 
 void EduCoexistenceStateTracker::OnDialogCreated(const content::WebUI* web_ui,
                                                  bool is_onboarding) {
-  DCHECK(!state_tracker_.contains(web_ui));
+  DCHECK(!base::Contains(state_tracker_, web_ui));
 
   FlowState& state = state_tracker_[web_ui];
 
@@ -89,7 +90,7 @@ void EduCoexistenceStateTracker::OnDialogCreated(const content::WebUI* web_ui,
 void EduCoexistenceStateTracker::OnConsentLogged(
     const content::WebUI* web_ui,
     const std::string& account_email) {
-  DCHECK(state_tracker_.contains(web_ui));
+  DCHECK(base::Contains(state_tracker_, web_ui));
 
   // Update the webui state that consent was logged.
   OnWebUiStateChanged(web_ui, FlowResult::kConsentLogged);
@@ -108,14 +109,14 @@ void EduCoexistenceStateTracker::OnConsentLogged(
 void EduCoexistenceStateTracker::OnWebUiStateChanged(
     const content::WebUI* web_ui,
     FlowResult result) {
-  DCHECK(state_tracker_.contains(web_ui));
+  DCHECK(base::Contains(state_tracker_, web_ui));
   state_tracker_[web_ui].flow_result = result;
 }
 
 const EduCoexistenceStateTracker::FlowState*
 EduCoexistenceStateTracker::GetInfoForWebUIForTest(
     const content::WebUI* web_ui) const {
-  if (!state_tracker_.contains(web_ui)) {
+  if (!base::Contains(state_tracker_, web_ui)) {
     return nullptr;
   }
   return &state_tracker_.at(web_ui);
diff --git a/chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_dialogs.cc b/chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_dialogs.cc
index df963051dc..867e597494 100644
--- a/chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_dialogs.cc
+++ b/chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_dialogs.cc
@@ -4,10 +4,10 @@
 
 #include "chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_dialogs.h"
 
-#include <algorithm>
 #include <memory>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/json/json_writer.h"
@@ -513,8 +513,8 @@ bool LockScreenStartReauthDialog::Matches(content::WebContents* web_contents) {
   // are open in the user's active session. We use NavigationController objects
   // for comparison because `LoginHandler` uses them as the source of
   // proxy-related notifications.
-  return std::ranges::contains(webui()->GetWebContents()->GetInnerWebContents(),
-                               web_contents);
+  return base::Contains(webui()->GetWebContents()->GetInnerWebContents(),
+                        web_contents);
 }
 
 void LockScreenStartReauthDialog::ReenableNetworkUpdates() {
diff --git a/chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_handler.cc b/chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_handler.cc
index caceb30de7..cc00024558 100644
--- a/chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_handler.cc
+++ b/chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_handler.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/browser/ui/webui/ash/lock_screen_reauth/lock_screen_reauth_handler.h"
 
-#include <algorithm>
 #include <memory>
 
 #include "ash/constants/ash_features.h"
@@ -434,7 +433,7 @@ void LockScreenReauthHandler::HandleOnPasswordTyped(
 
 void LockScreenReauthHandler::OnPasswordTyped(const std::string& password) {
   if (scraped_saml_passwords_.empty() ||
-      std::ranges::contains(scraped_saml_passwords_, password)) {
+      base::Contains(scraped_saml_passwords_, password)) {
     OnPasswordConfirmed(password);
     return;
   }
diff --git a/chrome/browser/ui/webui/ash/login/gaia_screen_handler.cc b/chrome/browser/ui/webui/ash/login/gaia_screen_handler.cc
index 11469e8431..2ee275f3c7 100644
--- a/chrome/browser/ui/webui/ash/login/gaia_screen_handler.cc
+++ b/chrome/browser/ui/webui/ash/login/gaia_screen_handler.cc
@@ -15,6 +15,7 @@
 #include "ash/public/cpp/login_screen.h"
 #include "base/check_deref.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
diff --git a/chrome/browser/ui/webui/ash/login/l10n_util.cc b/chrome/browser/ui/webui/ash/login/l10n_util.cc
index 2718b322f5..8ef6c29656 100644
--- a/chrome/browser/ui/webui/ash/login/l10n_util.cc
+++ b/chrome/browser/ui/webui/ash/login/l10n_util.cc
@@ -15,6 +15,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/i18n/rtl.h"
 #include "base/location.h"
@@ -156,7 +157,7 @@ base::ListValue GetLanguageList(
       continue;
     }
 
-    if (std::ranges::contains(base_language_codes, language_id)) {
+    if (base::Contains(base_language_codes, language_id)) {
       // Language is supported. No need to replace
       continue;
     }
@@ -166,7 +167,7 @@ base::ListValue GetLanguageList(
       continue;
     }
 
-    if (!std::ranges::contains(base_language_codes, *resolved_locale)) {
+    if (!base::Contains(base_language_codes, *resolved_locale)) {
       // Resolved locale is not supported.
       continue;
     }
@@ -187,7 +188,7 @@ base::ListValue GetLanguageList(
   for (const auto& language_code : language_codes) {
     // Exclude the language which is not in `base_langauge_codes` even it has
     // input methods.
-    if (!std::ranges::contains(base_language_codes, language_code)) {
+    if (!base::Contains(base_language_codes, language_code)) {
       continue;
     }
 
diff --git a/chrome/browser/ui/webui/ash/login/oobe_display_chooser.cc b/chrome/browser/ui/webui/ash/login/oobe_display_chooser.cc
index 3243cec2bb..27d397af2e 100644
--- a/chrome/browser/ui/webui/ash/login/oobe_display_chooser.cc
+++ b/chrome/browser/ui/webui/ash/login/oobe_display_chooser.cc
@@ -6,9 +6,8 @@
 
 #include <stdint.h>
 
-#include <algorithm>
-
 #include "ash/public/ash_interfaces.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/strings/string_number_conversions.h"
@@ -34,7 +33,7 @@ const uint16_t kDeviceIds[] = {0x0457, 0x266e, 0x222a};
 // Returns true if `vendor_id` is a valid vendor id that may be made the primary
 // display.
 bool IsAllowListedVendorId(uint16_t vendor_id) {
-  return std::ranges::contains(kDeviceIds, vendor_id);
+  return base::Contains(kDeviceIds, vendor_id);
 }
 
 }  // namespace
diff --git a/chrome/browser/ui/webui/ash/mako/mako_ui.cc b/chrome/browser/ui/webui/ash/mako/mako_ui.cc
index 276870da81..7374e3f850 100644
--- a/chrome/browser/ui/webui/ash/mako/mako_ui.cc
+++ b/chrome/browser/ui/webui/ash/mako/mako_ui.cc
@@ -4,8 +4,6 @@
 
 #include "chrome/browser/ui/webui/ash/mako/mako_ui.h"
 
-#include <algorithm>
-
 #include "ash/constants/ash_features.h"
 #include "ash/constants/ash_switches.h"
 #include "ash/lobster/lobster_controller.h"
@@ -85,12 +83,12 @@ MakoUntrustedUI::MakoUntrustedUI(content::WebUI* web_ui)
       [&](const webui::ResourcePath& resource_path) -> bool {
     // when lobster access is not granted, lobster resources are not allowed.
     if (lobster_service == nullptr &&
-        std::ranges::contains(kLobsterResourceIds, resource_path.id)) {
+        base::Contains(kLobsterResourceIds, resource_path.id)) {
       return false;
     }
     // when l10n is disabled, only EN-US resources are allowed.
     if (!should_use_l10n_strings &&
-        !std::ranges::contains(kEnUSResourceIds, resource_path.id)) {
+        !base::Contains(kEnUSResourceIds, resource_path.id)) {
       return false;
     }
     return true;
diff --git a/chrome/browser/ui/webui/ash/settings/calculator/size_calculator.cc b/chrome/browser/ui/webui/ash/settings/calculator/size_calculator.cc
index d20b22aeb5..da6b452fa7 100644
--- a/chrome/browser/ui/webui/ash/settings/calculator/size_calculator.cc
+++ b/chrome/browser/ui/webui/ash/settings/calculator/size_calculator.cc
@@ -4,12 +4,12 @@
 
 #include "chrome/browser/ui/webui/ash/settings/calculator/size_calculator.h"
 
-#include <algorithm>
 #include <cstdint>
 #include <numeric>
 #include <type_traits>
 
 #include "ash/constants/ash_features.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/scoped_refptr.h"
@@ -563,7 +563,7 @@ void OtherUsersSizeCalculator::OnGetOtherUserSize(
 
   // If all the requests succeed, shows the total bytes in the UI.
   const int64_t other_users_total_bytes =
-      std::ranges::contains(user_sizes_, -1)
+      base::Contains(user_sizes_, -1)
           ? -1
           : std::accumulate(user_sizes_.begin(), user_sizes_.end(), 0LL);
   NotifySizeCalculated(other_users_total_bytes);
diff --git a/chrome/browser/ui/webui/ash/settings/pages/a11y/accessibility_handler.cc b/chrome/browser/ui/webui/ash/settings/pages/a11y/accessibility_handler.cc
index eadaa468bf..cc775070d3 100644
--- a/chrome/browser/ui/webui/ash/settings/pages/a11y/accessibility_handler.cc
+++ b/chrome/browser/ui/webui/ash/settings/pages/a11y/accessibility_handler.cc
@@ -305,7 +305,7 @@ void AccessibilityHandler::MaybeAddDictationLocales() {
     // locale, IME languages or enabled preferred languages.
     std::pair<std::string_view, std::string_view> lang_and_locale =
         language::SplitIntoMainAndTail(locale.first);
-    bool is_recommended = ui_languages.contains(lang_and_locale.first);
+    bool is_recommended = base::Contains(ui_languages, lang_and_locale.first);
 
     option.Set("recommended", is_recommended);
     locales_list.Append(std::move(option));
diff --git a/chrome/browser/ui/webui/ash/settings/pages/bluetooth/fast_pair_saved_devices_handler.cc b/chrome/browser/ui/webui/ash/settings/pages/bluetooth/fast_pair_saved_devices_handler.cc
index 1dd3a1f8f9..3a4cbad00c 100644
--- a/chrome/browser/ui/webui/ash/settings/pages/bluetooth/fast_pair_saved_devices_handler.cc
+++ b/chrome/browser/ui/webui/ash/settings/pages/bluetooth/fast_pair_saved_devices_handler.cc
@@ -8,6 +8,7 @@
 #include "ash/quick_pair/repository/fast_pair/fast_pair_image_decoder_impl.h"
 #include "ash/quick_pair/repository/fast_pair_repository.h"
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/strings/string_number_conversions.h"
@@ -227,7 +228,8 @@ void FastPairSavedDevicesHandler::DecodingUrlsFinished() {
     nearby::fastpair::StoredDiscoveryItem item;
     if (item.ParseFromString(device.discovery_item_bytes()) &&
         item.has_icon_png() &&
-        image_byte_string_to_encoded_url_map_.contains(item.icon_png())) {
+        base::Contains(image_byte_string_to_encoded_url_map_,
+                       item.icon_png())) {
       image_url = image_byte_string_to_encoded_url_map_[item.icon_png()];
     }
 
diff --git a/chrome/browser/ui/webui/ash/settings/pages/internet/internet_section.cc b/chrome/browser/ui/webui/ash/settings/pages/internet/internet_section.cc
index 01b1ce5067..c42b71fd90 100644
--- a/chrome/browser/ui/webui/ash/settings/pages/internet/internet_section.cc
+++ b/chrome/browser/ui/webui/ash/settings/pages/internet/internet_section.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/browser/ui/webui/ash/settings/pages/internet/internet_section.h"
 
-#include <algorithm>
 #include <array>
 
 #include "ash/constants/ash_features.h"
@@ -13,6 +12,7 @@
 #include "ash/webui/network_ui/network_health_resource_provider.h"
 #include "ash/webui/network_ui/traffic_counters_resource_provider.h"
 #include "ash/webui/settings/public/constants/routes.mojom.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
@@ -673,13 +673,13 @@ bool IsPartOfDetailsSubpage(mojom::SearchResultType type,
       const mojom::Setting& setting = id.setting;
       switch (details_subpage) {
         case mojom::Subpage::kEthernetDetails:
-          return std::ranges::contains(GetEthernetDetailsSettings(), setting);
+          return base::Contains(GetEthernetDetailsSettings(), setting);
         case mojom::Subpage::kWifiDetails:
-          return std::ranges::contains(GetWifiDetailsSettings(), setting);
+          return base::Contains(GetWifiDetailsSettings(), setting);
         case mojom::Subpage::kCellularDetails:
-          return std::ranges::contains(GetCellularDetailsSettings(), setting);
+          return base::Contains(GetCellularDetailsSettings(), setting);
         case mojom::Subpage::kTetherDetails:
-          return std::ranges::contains(GetTetherDetailsSettings(), setting);
+          return base::Contains(GetTetherDetailsSettings(), setting);
         default:
           return false;
       }
diff --git a/chrome/browser/ui/webui/ash/settings/pages/os_settings_sections.cc b/chrome/browser/ui/webui/ash/settings/pages/os_settings_sections.cc
index 5ff0173699..d15ccb158b 100644
--- a/chrome/browser/ui/webui/ash/settings/pages/os_settings_sections.cc
+++ b/chrome/browser/ui/webui/ash/settings/pages/os_settings_sections.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/webui/ash/settings/pages/os_settings_sections.h"
 
 #include "ash/constants/ash_features.h"
+#include "base/containers/contains.h"
 #include "build/branding_buildflags.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/webui/ash/settings/pages/a11y/accessibility_section.h"
@@ -115,7 +116,7 @@ const OsSettingsSection* OsSettingsSections::GetSection(
 void OsSettingsSections::AddSection(
     chromeos::settings::mojom::Section section_id,
     std::unique_ptr<OsSettingsSection> section) {
-  DCHECK(!sections_map_.contains(section_id));
+  DCHECK(!base::Contains(sections_map_, section_id));
 
   sections_map_[section_id] = section.get();
   sections_.push_back(std::move(section));
diff --git a/chrome/browser/ui/webui/ash/settings/pages/people/fingerprint_handler.cc b/chrome/browser/ui/webui/ash/settings/pages/people/fingerprint_handler.cc
index 4ad033d896..8e55110b1e 100644
--- a/chrome/browser/ui/webui/ash/settings/pages/people/fingerprint_handler.cc
+++ b/chrome/browser/ui/webui/ash/settings/pages/people/fingerprint_handler.cc
@@ -9,6 +9,7 @@
 
 #include "ash/constants/ash_features.h"
 #include "ash/constants/ash_pref_names.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/utf_string_conversions.h"
@@ -208,7 +209,7 @@ void FingerprintHandler::HandleStartEnroll(const base::ListValue& args) {
     std::string fingerprint_name = l10n_util::GetStringFUTF8(
         IDS_SETTINGS_PEOPLE_LOCK_SCREEN_NEW_FINGERPRINT_DEFAULT_NAME,
         base::NumberToString16(i));
-    if (!std::ranges::contains(fingerprints_labels_, fingerprint_name)) {
+    if (!base::Contains(fingerprints_labels_, fingerprint_name)) {
       fp_service_->StartEnrollSession(user_id_, fingerprint_name);
       break;
     }
diff --git a/chrome/browser/ui/webui/ash/settings/search/hierarchy.cc b/chrome/browser/ui/webui/ash/settings/search/hierarchy.cc
index 196ca011ca..6cdfe3d158 100644
--- a/chrome/browser/ui/webui/ash/settings/search/hierarchy.cc
+++ b/chrome/browser/ui/webui/ash/settings/search/hierarchy.cc
@@ -7,6 +7,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "chrome/browser/ui/webui/ash/settings/constants/constants_util.h"
 #include "chrome/browser/ui/webui/ash/settings/pages/os_settings_section.h"
@@ -346,7 +347,7 @@ std::ostream& operator<<(std::ostream& os, const Hierarchy& h) {
     subpage_subpage.insert({subpage_id, {}});
     subpage_setting.insert({subpage_id, {}});
 
-    if (!h.subpage_map_.contains(subpage_id)) {
+    if (!base::Contains(h.subpage_map_, subpage_id)) {
       none_exist_subpage.push_back(subpage_id);
       continue;
     }
@@ -361,7 +362,7 @@ std::ostream& operator<<(std::ostream& os, const Hierarchy& h) {
   }
 
   for (auto& setting_id : AllSettings()) {
-    if (!h.setting_map_.contains(setting_id)) {
+    if (!base::Contains(h.setting_map_, setting_id)) {
       none_exist_setting.push_back(setting_id);
       continue;
     }
diff --git a/chrome/browser/ui/webui/ash/settings/test_support/fake_os_settings_section.cc b/chrome/browser/ui/webui/ash/settings/test_support/fake_os_settings_section.cc
index 10891d08f3..fe5b405dc6 100644
--- a/chrome/browser/ui/webui/ash/settings/test_support/fake_os_settings_section.cc
+++ b/chrome/browser/ui/webui/ash/settings/test_support/fake_os_settings_section.cc
@@ -8,6 +8,7 @@
 #include <sstream>
 
 #include "ash/webui/settings/public/constants/routes.mojom-shared.h"
+#include "base/containers/contains.h"
 #include "chrome/grit/generated_resources.h"
 
 namespace ash::settings {
@@ -27,7 +28,7 @@ void FakeOsSettingsSection::AddSubpageAndSetting(
     std::optional<chromeos::settings::mojom::Subpage> subpage,
     std::optional<chromeos::settings::mojom::Setting> setting) {
   if (subpage) {
-    if (!subpages_.contains(subpage.value())) {
+    if (!base::Contains(subpages_, subpage.value())) {
       // This is a new `subpage` and settings on this subpage.
       subpages_.insert({subpage.value(), {}});
     }
diff --git a/chrome/browser/ui/webui/ash/system_web_dialog/system_web_dialog_delegate.cc b/chrome/browser/ui/webui/ash/system_web_dialog/system_web_dialog_delegate.cc
index 4ec73acf54..59fcffabbe 100644
--- a/chrome/browser/ui/webui/ash/system_web_dialog/system_web_dialog_delegate.cc
+++ b/chrome/browser/ui/webui/ash/system_web_dialog/system_web_dialog_delegate.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "ash/public/cpp/shell_window_ids.h"
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "chrome/browser/profiles/profile_manager.h"
 #include "chrome/browser/ui/views/chrome_web_dialog_view.h"
@@ -93,10 +94,10 @@ SystemWebDialogDelegate* SystemWebDialogDelegate::FindInstance(
 
 // static
 bool SystemWebDialogDelegate::HasInstance(const GURL& url) {
-  return std::ranges::contains(*GetInstances(), url,
-                               [](const SystemWebDialogDelegate* instance) {
-                                 return instance->GetDialogContentURL();
-                               });
+  return base::Contains(*GetInstances(), url,
+                        [](const SystemWebDialogDelegate* instance) {
+                          return instance->GetDialogContentURL();
+                        });
 }
 
 // static
diff --git a/chrome/browser/ui/webui/browser_command/browser_command_handler.cc b/chrome/browser/ui/webui/browser_command/browser_command_handler.cc
index fee5660ebc..25c3ef09f4 100644
--- a/chrome/browser/ui/webui/browser_command/browser_command_handler.cc
+++ b/chrome/browser/ui/webui/browser_command/browser_command_handler.cc
@@ -4,9 +4,8 @@
 
 #include "chrome/browser/ui/webui/browser_command/browser_command_handler.h"
 
-#include <algorithm>
-
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/user_metrics.h"
 #include "chrome/app/chrome_command_ids.h"
@@ -90,7 +89,7 @@ BrowserCommandHandler::~BrowserCommandHandler() = default;
 void BrowserCommandHandler::CanExecuteCommand(
     browser_command::mojom::Command command_id,
     CanExecuteCommandCallback callback) {
-  if (!std::ranges::contains(supported_commands_, command_id)) {
+  if (!base::Contains(supported_commands_, command_id)) {
     std::move(callback).Run(false);
     return;
   }
@@ -169,7 +168,7 @@ void BrowserCommandHandler::CanExecuteCommand(
 void BrowserCommandHandler::ExecuteCommand(Command command_id,
                                            ClickInfoPtr click_info,
                                            ExecuteCommandCallback callback) {
-  if (!std::ranges::contains(supported_commands_, command_id)) {
+  if (!base::Contains(supported_commands_, command_id)) {
     std::move(callback).Run(false);
     return;
   }
diff --git a/chrome/browser/ui/webui/chrome_url_data_manager_browsertest.cc b/chrome/browser/ui/webui/chrome_url_data_manager_browsertest.cc
index 6766b97aa4..57a14e2ede 100644
--- a/chrome/browser/ui/webui/chrome_url_data_manager_browsertest.cc
+++ b/chrome/browser/ui/webui/chrome_url_data_manager_browsertest.cc
@@ -7,6 +7,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/test/scoped_feature_list.h"
 #include "base/test/scoped_run_loop_timeout.h"
 #include "base/test/test_timeouts.h"
@@ -186,8 +187,7 @@ static constexpr const char* const kSlowChromeUrls[] = {
     "chrome://prefs-internals",
 #else
     // Placeholder entry to prevent zero-sized array which causes template
-    // instantiation failures with std::ranges algorithms in
-    // std::ranges::contains.
+    // instantiation failures with std::ranges algorithms in base::Contains.
     "",
 #endif
 };
@@ -216,7 +216,7 @@ class ChromeURLDataManagerWebUITrustedTypesTest
 
   void CheckNoTrustedTypesViolation(std::string_view url) {
     std::unique_ptr<base::test::ScopedRunLoopTimeout> timeout;
-    if (std::ranges::contains(kSlowChromeUrls, url)) {
+    if (base::Contains(kSlowChromeUrls, url)) {
       timeout = std::make_unique<base::test::ScopedRunLoopTimeout>(
           FROM_HERE, GetSlowTestTimeout());
     }
@@ -237,7 +237,7 @@ class ChromeURLDataManagerWebUITrustedTypesTest
 
   void CheckTrustedTypesEnabled(std::string_view url) {
     std::unique_ptr<base::test::ScopedRunLoopTimeout> timeout;
-    if (std::ranges::contains(kSlowChromeUrls, url)) {
+    if (base::Contains(kSlowChromeUrls, url)) {
       timeout = std::make_unique<base::test::ScopedRunLoopTimeout>(
           FROM_HERE, GetSlowTestTimeout());
     }
diff --git a/chrome/browser/ui/webui/cr_components/searchbox/searchbox_handler.cc b/chrome/browser/ui/webui/cr_components/searchbox/searchbox_handler.cc
index 434824759f..f2bdbad236 100644
--- a/chrome/browser/ui/webui/cr_components/searchbox/searchbox_handler.cc
+++ b/chrome/browser/ui/webui/cr_components/searchbox/searchbox_handler.cc
@@ -6,6 +6,7 @@
 
 #include "base/base64.h"
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/string_util.h"
@@ -270,7 +271,7 @@ bool MatchHasSideTypeAndRenderType(
     const omnibox::GroupConfigMap& suggestion_groups_map) {
   omnibox::GroupId group_id =
       match.suggestion_group_id.value_or(omnibox::GROUP_INVALID);
-  return suggestion_groups_map.contains(group_id) &&
+  return base::Contains(suggestion_groups_map, group_id) &&
          suggestion_groups_map.at(group_id).side_type() == side_type &&
          suggestion_groups_map.at(group_id).render_type() == render_type;
 }
diff --git a/chrome/browser/ui/webui/history_clusters/history_clusters_handler.cc b/chrome/browser/ui/webui/history_clusters/history_clusters_handler.cc
index 70b66bc744..e46db5fcf6 100644
--- a/chrome/browser/ui/webui/history_clusters/history_clusters_handler.cc
+++ b/chrome/browser/ui/webui/history_clusters/history_clusters_handler.cc
@@ -10,6 +10,7 @@
 #include <variant>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/memory/weak_ptr.h"
diff --git a/chrome/browser/ui/webui/management/management_ui_handler.cc b/chrome/browser/ui/webui/management/management_ui_handler.cc
index 5b80dfa29a..479de45ed1 100644
--- a/chrome/browser/ui/webui/management/management_ui_handler.cc
+++ b/chrome/browser/ui/webui/management/management_ui_handler.cc
@@ -13,6 +13,7 @@
 #include <vector>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
diff --git a/chrome/browser/ui/webui/management/management_ui_handler_chromeos.cc b/chrome/browser/ui/webui/management/management_ui_handler_chromeos.cc
index 71b592ebde..6976c36887 100644
--- a/chrome/browser/ui/webui/management/management_ui_handler_chromeos.cc
+++ b/chrome/browser/ui/webui/management/management_ui_handler_chromeos.cc
@@ -353,9 +353,9 @@ void AddDeviceReportingInfo(base::ListValue* report_sources,
       ::reporting::kReportWebsiteTelemetryAllowlist);
   const auto& website_activity_allowlist = profile->GetPrefs()->GetList(
       ::reporting::kReportWebsiteActivityAllowlist);
-  if (website_activity_allowlist.contains(wildcard_pattern_string) ||
+  if (base::Contains(website_activity_allowlist, wildcard_pattern_string) ||
       (!website_telemetry_types.empty() &&
-       website_telemetry_allowlist.contains(wildcard_pattern_string))) {
+       base::Contains(website_telemetry_allowlist, wildcard_pattern_string))) {
     // One or more website metrics reporting policies allowlists all website
     // URLs.
     AddDeviceReportingElement(report_sources,
diff --git a/chrome/browser/ui/webui/nearby_internals/nearby_internals_ui_presence_handler.cc b/chrome/browser/ui/webui/nearby_internals/nearby_internals_ui_presence_handler.cc
index 7017fd4644..27d6c0af0c 100644
--- a/chrome/browser/ui/webui/nearby_internals/nearby_internals_ui_presence_handler.cc
+++ b/chrome/browser/ui/webui/nearby_internals/nearby_internals_ui_presence_handler.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/webui/nearby_internals/nearby_internals_ui_presence_handler.h"
 
+#include "base/containers/contains.h"
 #include "chrome/browser/ash/nearby/nearby_process_manager_factory.h"
 #include "chrome/browser/ash/nearby/presence/nearby_presence_service_factory.h"
 #include "chrome/browser/profiles/profile.h"
@@ -263,7 +264,7 @@ void NearbyInternalsPresenceHandler::HandleConnectToPresenceDevice(
       << __func__
       << ": Connection attempt for device with endpoint id: " << endpoint_id;
 
-  if (!endpoint_id_to_presence_device_map_.contains(endpoint_id)) {
+  if (!base::Contains(endpoint_id_to_presence_device_map_, endpoint_id)) {
     CD_LOG(WARNING, Feature::NEARBY_INFRA)
         << __func__ << ": Received endpoint_id for device no longer in map.";
     return;
diff --git a/chrome/browser/ui/webui/new_tab_page/new_tab_page_handler.cc b/chrome/browser/ui/webui/new_tab_page/new_tab_page_handler.cc
index c394f40474..c4772fbddd 100644
--- a/chrome/browser/ui/webui/new_tab_page/new_tab_page_handler.cc
+++ b/chrome/browser/ui/webui/new_tab_page/new_tab_page_handler.cc
@@ -15,6 +15,7 @@
 
 #include "base/base64.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/flat_map.h"
@@ -155,7 +156,7 @@ bool ShouldForceDarkForegroundColorsForLogo(const ThemeService* theme_service) {
       });
 
   const std::string& extension_id = theme_supplier->extension_id();
-  return kPrideThemeExtensionIdsDarkForeground.contains(extension_id);
+  return base::Contains(kPrideThemeExtensionIdsDarkForeground, extension_id);
 }
 
 new_tab_page::mojom::ThemePtr MakeTheme(
@@ -691,12 +692,13 @@ void NewTabPageHandler::SetModulesDisabled(
   ScopedListPrefUpdate update(profile_->GetPrefs(), prefs::kNtpDisabledModules);
   base::ListValue& list = update.Get();
   for (const auto& module_id : module_ids) {
+    base::Value module_id_value(module_id);
     if (disabled) {
-      if (!list.contains(module_id)) {
-        list.Append(module_id);
+      if (!base::Contains(list, module_id_value)) {
+        list.Append(std::move(module_id_value));
       }
     } else {
-      list.EraseValue(base::Value(module_id));
+      list.EraseValue(module_id_value);
     }
   }
 
@@ -753,7 +755,7 @@ void NewTabPageHandler::OnModulesLoadedWithData(
       GetSurveyEligibleModuleIds();
   if (std::any_of(module_ids.begin(), module_ids.end(),
                   [&survey_eligible_module_ids](std::string id) {
-                    return std::ranges::contains(survey_eligible_module_ids,
+                    return base::Contains(survey_eligible_module_ids,
                                                  id);
                   })) {
     HatsService* hats_service = HatsServiceFactory::GetForProfile(
@@ -886,7 +888,7 @@ void NewTabPageHandler::GetModulesOrder(GetModulesOrderCallback callback) {
   std::ranges::copy_if(ntp_features::GetModulesOrder(),
                        std::back_inserter(module_ids),
                        [&module_ids](const std::string& id) {
-                         return !std::ranges::contains(module_ids, id);
+                         return !base::Contains(module_ids, id);
                        });
 
   // Third, append default module order for any modules not ordered by
@@ -894,7 +896,7 @@ void NewTabPageHandler::GetModulesOrder(GetModulesOrderCallback callback) {
   std::ranges::copy_if(ntp_modules::kOrderedModuleIds,
                        std::back_inserter(module_ids),
                        [&module_ids](const std::string& id) {
-                         return !std::ranges::contains(module_ids, id);
+                         return !base::Contains(module_ids, id);
                        });
 
   std::move(callback).Run(std::move(module_ids));
diff --git a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
index b4af135ffa..20d13e7008 100644
--- a/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
+++ b/chrome/browser/ui/webui/new_tab_page/new_tab_page_ui.cc
@@ -1380,11 +1380,11 @@ void NewTabPageUI::UpdateMostVisitedTileTypes() {
     most_visited_page_handler_->EnableTileTypes(
         ntp_tiles::MostVisitedSites::EnableTileTypesOptions()
             .with_top_sites(
-                enabled_types.contains(ntp_tiles::TileType::kTopSites))
-            .with_custom_links(
-                enabled_types.contains(ntp_tiles::TileType::kCustomLinks))
-            .with_enterprise_shortcuts(enabled_types.contains(
-                ntp_tiles::TileType::kEnterpriseShortcuts)));
+                base::Contains(enabled_types, ntp_tiles::TileType::kTopSites))
+            .with_custom_links(base::Contains(
+                enabled_types, ntp_tiles::TileType::kCustomLinks))
+            .with_enterprise_shortcuts(base::Contains(
+                enabled_types, ntp_tiles::TileType::kEnterpriseShortcuts)));
   }
 }
 
diff --git a/chrome/browser/ui/webui/new_tab_page/untrusted_source.cc b/chrome/browser/ui/webui/new_tab_page/untrusted_source.cc
index d765e93843..78f5897a9d 100644
--- a/chrome/browser/ui/webui/new_tab_page/untrusted_source.cc
+++ b/chrome/browser/ui/webui/new_tab_page/untrusted_source.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/i18n/rtl.h"
 #include "base/memory/ref_counted_memory.h"
@@ -299,7 +300,8 @@ bool UntrustedSource::ShouldServiceRequest(
   return path == "one-google-bar" || path == "one_google_bar.js" ||
          path == "one_google_bar_api.js" || path == "image" ||
          path == "background_image" || path == "custom_background_image" ||
-         path == "background_image.js" || path.contains("background.jpg");
+         path == "background_image.js" ||
+         base::Contains(path, "background.jpg");
 }
 
 void UntrustedSource::OnOneGoogleBarDataUpdated() {
diff --git a/chrome/browser/ui/webui/new_tab_page/webui_ntp_browsertest.cc b/chrome/browser/ui/webui/new_tab_page/webui_ntp_browsertest.cc
index 6c9da6507c..5bfab61729 100644
--- a/chrome/browser/ui/webui/new_tab_page/webui_ntp_browsertest.cc
+++ b/chrome/browser/ui/webui/new_tab_page/webui_ntp_browsertest.cc
@@ -2,10 +2,10 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <set>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/stl_util.h"
 #include "base/strings/stringprintf.h"
 #include "base/test/mock_callback.h"
@@ -173,8 +173,8 @@ IN_PROC_BROWSER_TEST_F(WebUiNtpBrowserTest, SpareRenderer) {
 
   // Check spare was taken.
   EXPECT_TRUE(
-      std::ranges::contains(spare_ids_before_navigation,
-                            ntp->GetPrimaryMainFrame()->GetProcess()->GetID()));
+      base::Contains(spare_ids_before_navigation,
+                     ntp->GetPrimaryMainFrame()->GetProcess()->GetID()));
 
   // No processes should be unnecessarily terminated.
   const std::set<content::ChildProcessId> ending_rph_ids =
diff --git a/chrome/browser/ui/webui/ntp_tiles_internals_ui.cc b/chrome/browser/ui/webui/ntp_tiles_internals_ui.cc
index 7dfea178f9..c1d4ada1a2 100644
--- a/chrome/browser/ui/webui/ntp_tiles_internals_ui.cc
+++ b/chrome/browser/ui/webui/ntp_tiles_internals_ui.cc
@@ -107,11 +107,11 @@ ChromeNTPTilesInternalsMessageHandlerClient::MakeMostVisitedSites() {
   most_visited_sites->EnableTileTypes(
       ntp_tiles::MostVisitedSites::EnableTileTypesOptions()
           .with_top_sites(
-              enabled_types.contains(ntp_tiles::TileType::kTopSites))
+              base::Contains(enabled_types, ntp_tiles::TileType::kTopSites))
           .with_custom_links(
-              enabled_types.contains(ntp_tiles::TileType::kCustomLinks))
-          .with_enterprise_shortcuts(enabled_types.contains(
-              ntp_tiles::TileType::kEnterpriseShortcuts)));
+              base::Contains(enabled_types, ntp_tiles::TileType::kCustomLinks))
+          .with_enterprise_shortcuts(base::Contains(
+              enabled_types, ntp_tiles::TileType::kEnterpriseShortcuts)));
 #endif
   return most_visited_sites;
 }
diff --git a/chrome/browser/ui/webui/print_preview/pdf_printer_handler.cc b/chrome/browser/ui/webui/print_preview/pdf_printer_handler.cc
index b5e5f8bc2a..884aaab796 100644
--- a/chrome/browser/ui/webui/print_preview/pdf_printer_handler.cc
+++ b/chrome/browser/ui/webui/print_preview/pdf_printer_handler.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -139,7 +140,7 @@ base::DictValue GetPdfCapabilities(
           .WithNameMaybeBasedOnSize(/*custom_display_name=*/"",
                                     /*vendor_id=*/"")
           .Build();
-  if (!std::ranges::contains(kPdfMedia, default_media.size_name)) {
+  if (!base::Contains(kPdfMedia, default_media.size_name)) {
     default_media =
         cloud_devices::printer::MediaBuilder()
             .WithStandardName(locale == "en-US" ? MediaSize::NA_LETTER
diff --git a/chrome/browser/ui/webui/print_preview/print_preview_handler.cc b/chrome/browser/ui/webui/print_preview/print_preview_handler.cc
index 5fba296db5..1853262887 100644
--- a/chrome/browser/ui/webui/print_preview/print_preview_handler.cc
+++ b/chrome/browser/ui/webui/print_preview/print_preview_handler.cc
@@ -13,6 +13,7 @@
 
 #include "ash/constants/ash_features.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -554,7 +555,7 @@ bool PrintPreviewHandler::ShouldReceiveRendererMessage(int request_id) {
     return false;
   }
 
-  if (!preview_callbacks_.contains(request_id)) {
+  if (!base::Contains(preview_callbacks_, request_id)) {
     BadMessageReceived();
     return false;
   }
@@ -588,7 +589,7 @@ void PrintPreviewHandler::HandleGetPrinters(const base::ListValue& args) {
 
   // Immediately resolve the callback without fetching printers if the printer
   // type is on the deny list.
-  if (printer_type_deny_list_.contains(printer_type)) {
+  if (base::Contains(printer_type_deny_list_, printer_type)) {
     ResolveJavascriptCallback(base::Value(callback_id), base::Value());
     return;
   }
@@ -627,7 +628,7 @@ void PrintPreviewHandler::HandleGetPrinterCapabilities(
   mojom::PrinterType printer_type = static_cast<mojom::PrinterType>(*type);
 
   // Reject the callback if the printer type is on the deny list.
-  if (printer_type_deny_list_.contains(printer_type)) {
+  if (base::Contains(printer_type_deny_list_, printer_type)) {
     RejectJavascriptCallback(base::Value(callback_id), base::Value());
     return;
   }
@@ -651,7 +652,7 @@ void PrintPreviewHandler::HandleGetPreview(const base::ListValue& args) {
   int request_id = settings.FindInt(kPreviewRequestID).value();
   CHECK_GT(request_id, -1);
 
-  CHECK(!preview_callbacks_.contains(request_id));
+  CHECK(!base::Contains(preview_callbacks_, request_id));
   preview_callbacks_[request_id] = callback_id;
   print_preview_ui()->OnPrintPreviewRequest(request_id);
   // Add an additional key in order to identify |print_preview_ui| later on
@@ -970,8 +971,9 @@ void PrintPreviewHandler::SendInitialSettings(
     initial_settings.Set(kPolicies, std::move(policies));
   }
 
-  initial_settings.Set(kPdfPrinterDisabled, printer_type_deny_list_.contains(
-                                                mojom::PrinterType::kPdf));
+  initial_settings.Set(
+      kPdfPrinterDisabled,
+      base::Contains(printer_type_deny_list_, mojom::PrinterType::kPdf));
 
   const bool destinations_managed =
       !printer_type_deny_list_.empty() &&
@@ -1120,7 +1122,7 @@ void PrintPreviewHandler::SendPagePreviewReady(int page_index,
   // gets called, the print preview may have failed. Since the failure message
   // may have arrived first, check for this case and bail out instead of
   // thinking this may be a bad IPC message.
-  if (preview_failures_.contains(preview_request_id)) {
+  if (base::Contains(preview_failures_, preview_request_id)) {
     return;
   }
 
diff --git a/chrome/browser/ui/webui/print_preview/print_preview_utils.cc b/chrome/browser/ui/webui/print_preview/print_preview_utils.cc
index 13f0ab8cb3..b31cd5e253 100644
--- a/chrome/browser/ui/webui/print_preview/print_preview_utils.cc
+++ b/chrome/browser/ui/webui/print_preview/print_preview_utils.cc
@@ -9,6 +9,7 @@
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/memory/ref_counted_memory.h"
@@ -70,7 +71,7 @@ base::ListValue PrintersToValues(const PrinterList& printer_list) {
 #if BUILDFLAG(IS_CHROMEOS)
     printer_info.Set(
         kCUPSEnterprisePrinter,
-        printer.options.contains(kCUPSEnterprisePrinter) &&
+        base::Contains(printer.options, kCUPSEnterprisePrinter) &&
             printer.options.at(kCUPSEnterprisePrinter) == kValueTrue);
 #endif  // BUILDFLAG(IS_CHROMEOS)
 
diff --git a/chrome/browser/ui/webui/profile_helper_browsertest.cc b/chrome/browser/ui/webui/profile_helper_browsertest.cc
index d0f7596b87..4373f04a4b 100644
--- a/chrome/browser/ui/webui/profile_helper_browsertest.cc
+++ b/chrome/browser/ui/webui/profile_helper_browsertest.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/webui/profile_helper.h"
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
diff --git a/chrome/browser/ui/webui/sanitized_image_source.cc b/chrome/browser/ui/webui/sanitized_image_source.cc
index 51f1bd8586..d05fd401b6 100644
--- a/chrome/browser/ui/webui/sanitized_image_source.cc
+++ b/chrome/browser/ui/webui/sanitized_image_source.cc
@@ -10,6 +10,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/memory/ref_counted_memory.h"
 #include "base/strings/strcat.h"
 #include "base/strings/string_util.h"
diff --git a/chrome/browser/ui/webui/searchbox/realbox_handler_browsertest.cc b/chrome/browser/ui/webui/searchbox/realbox_handler_browsertest.cc
index 682d696ebf..dc4dfd7b4d 100644
--- a/chrome/browser/ui/webui/searchbox/realbox_handler_browsertest.cc
+++ b/chrome/browser/ui/webui/searchbox/realbox_handler_browsertest.cc
@@ -215,10 +215,10 @@ IN_PROC_BROWSER_TEST_F(RealboxSearchPreloadWithSearchStatsBrowserTest,
 
   // Verify the prefetch and prerender URLs.
   // Only the prefetch URL should have the "pf=cs".
-  EXPECT_TRUE(prefetch_url.GetQuery().contains("pf=cs&"));
-  EXPECT_FALSE(prerender_url.GetQuery().contains("pf=cs&"));
-  EXPECT_TRUE(prefetch_url.GetQuery().contains("gs_lcrp="));
-  EXPECT_TRUE(prerender_url.GetQuery().contains("gs_lcrp="));
+  EXPECT_TRUE(base::Contains(prefetch_url.GetQuery(), "pf=cs&"));
+  EXPECT_FALSE(base::Contains(prerender_url.GetQuery(), "pf=cs&"));
+  EXPECT_TRUE(base::Contains(prefetch_url.GetQuery(), "gs_lcrp="));
+  EXPECT_TRUE(base::Contains(prerender_url.GetQuery(), "gs_lcrp="));
 
   // The prefetch should match the prerender.
   EXPECT_TRUE(IsSearchDestinationMatch(GetCanonicalSearchURL(prefetch_url),
@@ -231,13 +231,13 @@ IN_PROC_BROWSER_TEST_F(RealboxSearchPreloadWithoutSearchStatsBrowserTest,
 
   // Verify the prefetch and prerender URLs.
   // Only the prefetch URL should have the "pf=cs".
-  EXPECT_TRUE(prefetch_url.GetQuery().contains("pf=cs&"));
-  EXPECT_FALSE(prerender_url.GetQuery().contains("pf=cs&"));
+  EXPECT_TRUE(base::Contains(prefetch_url.GetQuery(), "pf=cs&"));
+  EXPECT_FALSE(base::Contains(prerender_url.GetQuery(), "pf=cs&"));
   // The prefetch URL should not have the "gs_lcrp" if
   // switches::kRemoveSearchboxStatsParamFromPrefetchRequests is true, while the
   // prerender URL should always have that.
-  EXPECT_FALSE(prefetch_url.GetQuery().contains("gs_lcrp="));
-  EXPECT_TRUE(prerender_url.GetQuery().contains("gs_lcrp="));
+  EXPECT_FALSE(base::Contains(prefetch_url.GetQuery(), "gs_lcrp="));
+  EXPECT_TRUE(base::Contains(prerender_url.GetQuery(), "gs_lcrp="));
 
   // The prefetch should match the prerender.
   EXPECT_TRUE(IsSearchDestinationMatch(GetCanonicalSearchURL(prefetch_url),
diff --git a/chrome/browser/ui/webui/settings/captions_handler.cc b/chrome/browser/ui/webui/settings/captions_handler.cc
index b1d09d7dff..37ec2331c3 100644
--- a/chrome/browser/ui/webui/settings/captions_handler.cc
+++ b/chrome/browser/ui/webui/settings/captions_handler.cc
@@ -8,6 +8,7 @@
 #include <iterator>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/timer/timer.h"
 #include "base/values.h"
@@ -194,7 +195,7 @@ base::ListValue CaptionsHandler::GetAvailableLanguagePacks() {
 #else
   for (const auto& config : speech::kLanguageComponentConfigs) {
     if (config.language_code != speech::LanguageCode::kNone &&
-        std::ranges::contains(enabled_and_available_languages,
+        base::Contains(enabled_and_available_languages,
                               config.language_name)) {
       base::DictValue available_language_pack;
       available_language_pack.Set(kCodeKey, config.language_name);
diff --git a/chrome/browser/ui/webui/settings/settings_secure_dns_handler_browsertest.cc b/chrome/browser/ui/webui/settings/settings_secure_dns_handler_browsertest.cc
index 30e3a03cc8..57863c8d05 100644
--- a/chrome/browser/ui/webui/settings/settings_secure_dns_handler_browsertest.cc
+++ b/chrome/browser/ui/webui/settings/settings_secure_dns_handler_browsertest.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/webui/settings/settings_secure_dns_handler.h"
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/test/metrics/histogram_tester.h"
 #include "build/build_config.h"
@@ -118,7 +119,7 @@ bool FindDropdownItem(const base::ListValue& resolvers,
   dict.Set("value", value);
   dict.Set("policy", policy);
 
-  return resolvers.contains(dict);
+  return base::Contains(resolvers, dict);
 }
 
 }  // namespace
diff --git a/chrome/browser/ui/webui/settings/settings_security_key_handler.cc b/chrome/browser/ui/webui/settings/settings_security_key_handler.cc
index 22d412cd44..b5c911d5e0 100644
--- a/chrome/browser/ui/webui/settings/settings_security_key_handler.cc
+++ b/chrome/browser/ui/webui/settings/settings_security_key_handler.cc
@@ -12,6 +12,7 @@
 
 #include "base/base64.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/strings/string_number_conversions.h"
@@ -890,7 +891,7 @@ void SecurityKeysBioEnrollmentHandler::OnHavePostEnrollmentEnumeration(
   DCHECK(!callback_id_.empty());
   state_ = State::kReady;
   if (code != device::CtapDeviceResponseCode::kSuccess || !enrollments ||
-      !enrollments->contains(enrolled_template_id)) {
+      !base::Contains(*enrollments, enrolled_template_id)) {
     OnError(device::BioEnrollmentHandler::Error::kAuthenticatorResponseInvalid);
     return;
   }
diff --git a/chrome/browser/ui/webui/settings/site_settings_handler.cc b/chrome/browser/ui/webui/settings/site_settings_handler.cc
index d0cd2c75ca..03a00c78d2 100644
--- a/chrome/browser/ui/webui/settings/site_settings_handler.cc
+++ b/chrome/browser/ui/webui/settings/site_settings_handler.cc
@@ -15,6 +15,7 @@
 #include "base/barrier_closure.h"
 #include "base/byte_size.h"
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/to_value_list.h"
 #include "base/debug/crash_logging.h"
@@ -556,7 +557,7 @@ void ConvertSiteGroupMapToList(
       origin_object.Set(kIsInstalled, is_installed);
 
       origin_object.Set(kHasPermissionSettings,
-                        origin_permission_set.contains(origin));
+                        base::Contains(origin_permission_set, origin));
       origin_list.Append(std::move(origin_object));
     }
     site_group.Set(kHasInstalledPWA, has_installed_pwa);
@@ -2702,7 +2703,7 @@ base::ListValue SiteSettingsHandler::PopulateFileSystemGrantData() {
     for (auto& path_info : grantObj.directory_read_grants) {
       const std::string file_path_string =
           FilePathToValue(path_info.path).GetString();
-      if (std::ranges::contains(directory_edit_grants_file_paths,
+      if (base::Contains(directory_edit_grants_file_paths,
                                 file_path_string)) {
         continue;
       }
@@ -2731,7 +2732,7 @@ base::ListValue SiteSettingsHandler::PopulateFileSystemGrantData() {
     for (auto& path_info : grantObj.file_read_grants) {
       const std::string file_path_string =
           FilePathToValue(path_info.path).GetString();
-      if (std::ranges::contains(file_edit_grants_file_paths,
+      if (base::Contains(file_edit_grants_file_paths,
                                 file_path_string)) {
         continue;
       }
diff --git a/chrome/browser/ui/webui/settings/site_settings_helper.cc b/chrome/browser/ui/webui/settings/site_settings_helper.cc
index a2e370ba3b..c787f155d1 100644
--- a/chrome/browser/ui/webui/settings/site_settings_helper.cc
+++ b/chrome/browser/ui/webui/settings/site_settings_helper.cc
@@ -15,6 +15,7 @@
 
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/json/values_util.h"
 #include "base/no_destructor.h"
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.cc b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.cc
index 858fb561bf..47c2eeea14 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.cc
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_chrome_page_handler.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -481,24 +482,26 @@ void CustomizeChromePageHandler::SetMostVisitedSettings(
 
   // Disable shortcuts auto-removal upon user interaction.
   DisableShortcutsAutoRemoval(profile_);
-  if ((current_tile_types.contains(ntp_tiles::TileType::kCustomLinks) !=
-           types_set.contains(ntp_tiles::TileType::kCustomLinks) ||
-       (current_tile_types.contains(ntp_tiles::TileType::kTopSites) !=
-        types_set.contains(ntp_tiles::TileType::kTopSites)))) {
-    UpdatePrefAndLogEvent(ntp_prefs::kNtpCustomLinksVisible,
-                          types_set.contains(ntp_tiles::TileType::kCustomLinks),
-                          NTP_CUSTOMIZE_SHORTCUT_TOGGLE_TYPE);
-  }
-
-  if (current_tile_types.contains(ntp_tiles::TileType::kEnterpriseShortcuts) !=
-      types_set.contains(ntp_tiles::TileType::kEnterpriseShortcuts)) {
+  if ((base::Contains(current_tile_types, ntp_tiles::TileType::kCustomLinks) !=
+           base::Contains(types_set, ntp_tiles::TileType::kCustomLinks) ||
+       (base::Contains(current_tile_types, ntp_tiles::TileType::kTopSites) !=
+        base::Contains(types_set, ntp_tiles::TileType::kTopSites)))) {
+    UpdatePrefAndLogEvent(
+        ntp_prefs::kNtpCustomLinksVisible,
+        base::Contains(types_set, ntp_tiles::TileType::kCustomLinks),
+        NTP_CUSTOMIZE_SHORTCUT_TOGGLE_TYPE);
+  }
+
+  if (base::Contains(current_tile_types,
+                     ntp_tiles::TileType::kEnterpriseShortcuts) !=
+      base::Contains(types_set, ntp_tiles::TileType::kEnterpriseShortcuts)) {
     // If enterprise shortcuts are disabled or the policy is not set, skip this
     // update.
     if (base::FeatureList::IsEnabled(ntp_tiles::kNtpEnterpriseShortcuts) &&
         !IsEnterpriseShortcutsEmpty()) {
       UpdatePrefAndLogEvent(
           ntp_prefs::kNtpEnterpriseShortcutsVisible,
-          types_set.contains(ntp_tiles::TileType::kEnterpriseShortcuts),
+          base::Contains(types_set, ntp_tiles::TileType::kEnterpriseShortcuts),
           NTP_CUSTOMIZE_ENTERPRISE_SHORTCUT_TOGGLE_VISIBILITY);
     }
   }
@@ -641,9 +644,9 @@ void CustomizeChromePageHandler::UpdateModulesSettings() {
           l10n_util::GetStringUTF8(description_message_id.value());
     }
     module_settings->enabled =
-        !std::ranges::contains(disabled_module_ids, module_settings->id);
+        !base::Contains(disabled_module_ids, module_settings->id);
     module_settings->visible =
-        !std::ranges::contains(hidden_module_ids, module_settings->id);
+        !base::Contains(hidden_module_ids, module_settings->id);
     modules_settings.push_back(std::move(module_settings));
   }
   page_->SetModulesSettings(
diff --git a/chrome/browser/ui/webui/side_panel/customize_chrome/wallpaper_search/wallpaper_search_handler.cc b/chrome/browser/ui/webui/side_panel/customize_chrome/wallpaper_search/wallpaper_search_handler.cc
index ac82ce65f9..71702d32c8 100644
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/wallpaper_search/wallpaper_search_handler.cc
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/wallpaper_search/wallpaper_search_handler.cc
@@ -13,6 +13,7 @@
 
 #include "base/barrier_callback.h"
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/metrics/histogram_functions.h"
@@ -166,7 +167,7 @@ WallpaperSearchHandler::~WallpaperSearchHandler() {
 
   bool is_result = false;
   if (background_id) {
-    if (wallpaper_search_results_.contains(*background_id)) {
+    if (base::Contains(wallpaper_search_results_, *background_id)) {
       base::UmaHistogramEnumeration(
           "NewTabPage.WallpaperSearch.SessionSetTheme",
           NtpWallpaperSearchThemeType::kResult);
@@ -419,7 +420,7 @@ void WallpaperSearchHandler::SetBackgroundToWallpaperSearchResult(
     const base::Token& result_id,
     double time,
     side_panel::customize_chrome::mojom::ResultDescriptorsPtr descriptors) {
-  CHECK(wallpaper_search_results_.contains(result_id));
+  CHECK(base::Contains(wallpaper_search_results_, result_id));
   auto& [image_quality, render_time, bitmap] =
       wallpaper_search_results_[result_id];
   if (image_quality) {
@@ -1067,7 +1068,7 @@ void WallpaperSearchHandler::SetResultRenderTime(
     const std::vector<base::Token>& result_ids,
     double time) {
   for (const auto& id : result_ids) {
-    CHECK(wallpaper_search_results_.contains(id));
+    CHECK(base::Contains(wallpaper_search_results_, id));
     auto& tuple = wallpaper_search_results_[id];
     std::get<1>(tuple) =
         std::make_optional(base::Time::FromMillisecondsSinceUnixEpoch(time));
diff --git a/chrome/browser/ui/webui/side_panel/read_anything/read_anything_untrusted_page_handler.cc b/chrome/browser/ui/webui/side_panel/read_anything/read_anything_untrusted_page_handler.cc
index 8a639c5c93..ad2466edd4 100644
--- a/chrome/browser/ui/webui/side_panel/read_anything/read_anything_untrusted_page_handler.cc
+++ b/chrome/browser/ui/webui/side_panel/read_anything/read_anything_untrusted_page_handler.cc
@@ -14,6 +14,7 @@
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "chrome/browser/accessibility/phrase_segmentation/dependency_parser_model_loader.h"
 #include "chrome/browser/accessibility/phrase_segmentation/dependency_parser_model_loader_factory.h"
@@ -820,14 +821,14 @@ void ReadAnythingUntrustedPageHandler::OnCopy() {
 void ReadAnythingUntrustedPageHandler::OnLineSpaceChange(
     read_anything::mojom::LineSpacing line_spacing) {
   profile_->GetPrefs()->SetInteger(prefs::kAccessibilityReadAnythingLineSpacing,
-                                   std::to_underlying(line_spacing));
+                                   base::to_underlying(line_spacing));
 }
 
 void ReadAnythingUntrustedPageHandler::OnLetterSpaceChange(
     read_anything::mojom::LetterSpacing letter_spacing) {
   profile_->GetPrefs()->SetInteger(
       prefs::kAccessibilityReadAnythingLetterSpacing,
-      std::to_underlying(letter_spacing));
+      base::to_underlying(letter_spacing));
 }
 void ReadAnythingUntrustedPageHandler::OnFontChange(const std::string& font) {
   profile_->GetPrefs()->SetString(prefs::kAccessibilityReadAnythingFontName,
@@ -852,7 +853,7 @@ void ReadAnythingUntrustedPageHandler::OnImagesEnabledChanged(bool enabled) {
 void ReadAnythingUntrustedPageHandler::OnColorChange(
     read_anything::mojom::Colors color) {
   profile_->GetPrefs()->SetInteger(prefs::kAccessibilityReadAnythingColorInfo,
-                                   std::to_underlying(color));
+                                   base::to_underlying(color));
 }
 
 void ReadAnythingUntrustedPageHandler::OnSpeechRateChange(double rate) {
@@ -874,7 +875,7 @@ void ReadAnythingUntrustedPageHandler::OnLanguagePrefChange(
   ScopedListPrefUpdate update(
       prefs, prefs::kAccessibilityReadAnythingLanguagesEnabled);
   if (enabled) {
-    if (!update.Get().contains(lang)) {
+    if (!base::Contains(update.Get(), lang)) {
       update->Append(lang);
     }
   } else {
diff --git a/chrome/browser/ui/webui/signin/ash/inline_login_handler_impl.cc b/chrome/browser/ui/webui/signin/ash/inline_login_handler_impl.cc
index b556f53311..71d0f1dce2 100644
--- a/chrome/browser/ui/webui/signin/ash/inline_login_handler_impl.cc
+++ b/chrome/browser/ui/webui/signin/ash/inline_login_handler_impl.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/browser/ui/webui/signin/ash/inline_login_handler_impl.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <string>
@@ -351,7 +350,7 @@ void InlineLoginHandlerImpl::HandleDialogClose(const base::ListValue& args) {
 void InlineLoginHandlerImpl::OnGetAccountsToCompleteLogin(
     const CompleteLoginParams& params,
     const std::vector<::account_manager::Account>& accounts) {
-  bool is_new_account = !std::ranges::contains(
+  bool is_new_account = !base::Contains(
       accounts, params.gaia_id.ToString(),
       [](const account_manager::Account& account) { return account.key.id(); });
 
diff --git a/chrome/browser/ui/webui/signin/inline_login_handler_impl.cc b/chrome/browser/ui/webui/signin/inline_login_handler_impl.cc
index 2635ac15dc..bf9eedea0b 100644
--- a/chrome/browser/ui/webui/signin/inline_login_handler_impl.cc
+++ b/chrome/browser/ui/webui/signin/inline_login_handler_impl.cc
@@ -6,11 +6,11 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <string>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/not_fatal_until.h"
@@ -115,7 +115,7 @@ credential_provider::UiExitCodes ValidateSigninEmail(
       GetEmailDomainsFromParameter(email_domains_parameter);
   std::string email_domain = gaia::ExtractDomainName(signin_email);
 
-  return std::ranges::contains(all_email_domains, email_domain)
+  return base::Contains(all_email_domains, email_domain)
              ? credential_provider::kUiecSuccess
              : credential_provider::kUiecInvalidEmailDomain;
 }
diff --git a/chrome/browser/ui/webui/signin/signin_ui_error.cc b/chrome/browser/ui/webui/signin/signin_ui_error.cc
index 8f287e9979..da15dcccdc 100644
--- a/chrome/browser/ui/webui/signin/signin_ui_error.cc
+++ b/chrome/browser/ui/webui/signin/signin_ui_error.cc
@@ -13,6 +13,7 @@
 #include "base/notreached.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "chrome/grit/branded_strings.h"
 #include "chrome/grit/generated_resources.h"
@@ -23,7 +24,7 @@
 namespace {
 
 std::u16string ErrorTypeToString(SigninUIError::Type error_type) {
-  return u"Error " + base::NumberToString16(std::to_underlying(error_type));
+  return u"Error " + base::NumberToString16(base::to_underlying(error_type));
 }
 }  // namespace
 
diff --git a/chrome/browser/ui/webui/support_tool/support_tool_ui_utils.cc b/chrome/browser/ui/webui/support_tool/support_tool_ui_utils.cc
index 32fc11e433..631687d2f9 100644
--- a/chrome/browser/ui/webui/support_tool/support_tool_ui_utils.cc
+++ b/chrome/browser/ui/webui/support_tool/support_tool_ui_utils.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/browser/ui/webui/support_tool/support_tool_ui_utils.h"
 
-#include <algorithm>
 #include <map>
 #include <optional>
 #include <set>
@@ -14,6 +13,7 @@
 
 #include "base/base64url.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_value_list.h"
 #include "base/strings/string_util.h"
 #include "base/values.h"
@@ -140,7 +140,7 @@ base::DictValue GetDataCollectorItemForType(
   dict.Set(support_tool_ui::kDataCollectorName, GetDataCollectorName(type));
   dict.Set(support_tool_ui::kDataCollectorProtoEnum, type);
   dict.Set(support_tool_ui::kDataCollectorIncluded,
-           std::ranges::contains(module.included_data_collectors(), type));
+           base::Contains(module.included_data_collectors(), type));
   return dict;
 }
 
diff --git a/chrome/browser/ui/webui/tab_search/tab_search_page_handler.cc b/chrome/browser/ui/webui/tab_search/tab_search_page_handler.cc
index 62d19c2c9c..2873682f69 100644
--- a/chrome/browser/ui/webui/tab_search/tab_search_page_handler.cc
+++ b/chrome/browser/ui/webui/tab_search/tab_search_page_handler.cc
@@ -15,6 +15,7 @@
 #include <vector>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
@@ -123,7 +124,8 @@ void CreateTabGroupIfNotPresent(
     sessions::tab_restore::Tab* tab,
     std::set<tab_groups::TabGroupId>& tab_group_ids,
     std::vector<tab_search::mojom::TabGroupPtr>& tab_groups) {
-  if (tab->group.has_value() && !tab_group_ids.contains(tab->group.value())) {
+  if (tab->group.has_value() &&
+      !base::Contains(tab_group_ids, tab->group.value())) {
     tab_groups::TabGroupId tab_group_id = tab->group.value();
     const tab_groups::TabGroupVisualData* tab_group_visual_data =
         &tab->group_visual_data.value();
@@ -373,7 +375,7 @@ void TabSearchPageHandler::AcceptTabOrganization(
   std::vector<TabData::TabID> tab_ids_to_remove;
   for (const auto& tab_data : organization->tab_datas()) {
     if (!tab_data->tab()->GetContents() ||
-        !tabs_tab_ids.contains(tab_data->tab_id())) {
+        !base::Contains(tabs_tab_ids, tab_data->tab_id())) {
       tab_ids_to_remove.emplace_back(tab_data->tab_id());
     }
   }
@@ -734,7 +736,7 @@ void TabSearchPageHandler::GetTabOrganizationSession(
     session = organization_service_->CreateSessionForBrowser(browser_);
   }
 
-  if (!std::ranges::contains(listened_sessions_, session)) {
+  if (!base::Contains(listened_sessions_, session)) {
     session->AddObserver(this);
     listened_sessions_.emplace_back(session);
   }
@@ -840,7 +842,7 @@ void TabSearchPageHandler::RequestTabOrganization() {
     session = organization_service_->ResetSessionForBrowser(browser_);
   }
 
-  if (!std::ranges::contains(listened_sessions_, session)) {
+  if (!base::Contains(listened_sessions_, session)) {
     session->AddObserver(this);
     listened_sessions_.emplace_back(session);
   }
@@ -919,7 +921,7 @@ void TabSearchPageHandler::RestartSession() {
   // Don't notify observers to avoid a repaint
   TabOrganizationSession* session =
       organization_service_->ResetSessionForBrowser(browser_, base_session_tab);
-  if (!std::ranges::contains(listened_sessions_, session)) {
+  if (!base::Contains(listened_sessions_, session)) {
     session->AddObserver(this);
     listened_sessions_.emplace_back(session);
   }
@@ -1400,7 +1402,7 @@ bool TabSearchPageHandler::AddRecentlyClosedTab(
   DedupKey dedup_id(recently_closed_tab->url, recently_closed_tab->group_id);
   // Ignore NTP entries, duplicate entries and tabs with invalid URLs such as
   // empty URLs.
-  if (tab_dedup_keys.contains(dedup_id) ||
+  if (base::Contains(tab_dedup_keys, dedup_id) ||
       recently_closed_tab->url == GURL(chrome::kChromeUINewTabPageURL) ||
       !recently_closed_tab->url.is_valid()) {
     return false;
@@ -1569,7 +1571,7 @@ void TabSearchPageHandler::OnTabStripModelChanged(
       // Recently closed entries appear first in the list.
       for (auto& entry : tab_restore_service->entries()) {
         if (entry->type == sessions::tab_restore::Type::TAB &&
-            tab_restore_ids.contains(entry->id)) {
+            base::Contains(tab_restore_ids, entry->id)) {
           // The associated tab group visual data for the recently closed tab is
           // already present at the client side from the initial GetProfileData
           // call.
@@ -1772,7 +1774,7 @@ TabSearchPageHandler::GetMojoForTabOrganizationSession(
 
 void TabSearchPageHandler::OnTabOrganizationSessionUpdated(
     const TabOrganizationSession* session) {
-  if (restarting_ || !std::ranges::contains(listened_sessions_, session)) {
+  if (restarting_ || !base::Contains(listened_sessions_, session)) {
     return;
   }
 
diff --git a/chrome/browser/ui/webui/tab_search/tab_search_prefs.cc b/chrome/browser/ui/webui/tab_search/tab_search_prefs.cc
index d32342dc03..96330491a2 100644
--- a/chrome/browser/ui/webui/tab_search/tab_search_prefs.cc
+++ b/chrome/browser/ui/webui/tab_search/tab_search_prefs.cc
@@ -4,8 +4,7 @@
 
 #include "chrome/browser/ui/webui/tab_search/tab_search_prefs.h"
 
-#include <utility>
-
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/browser/ui/webui/tab_search/tab_search.mojom.h"
 #include "components/pref_registry/pref_registry_syncable.h"
 #include "components/prefs/pref_registry_simple.h"
@@ -63,7 +62,7 @@ tab_search::mojom::TabSearchSection GetTabSearchSectionFromInt(
 
 int GetIntFromTabSearchSection(
     const tab_search::mojom::TabSearchSection section) {
-  return std::to_underlying(section);
+  return base::to_underlying(section);
 }
 
 tab_search::mojom::TabOrganizationFeature GetTabOrganizationFeatureFromInt(
@@ -74,7 +73,7 @@ tab_search::mojom::TabOrganizationFeature GetTabOrganizationFeatureFromInt(
 
 int GetIntFromTabOrganizationFeature(
     const tab_search::mojom::TabOrganizationFeature feature) {
-  return std::to_underlying(feature);
+  return base::to_underlying(feature);
 }
 
 }  // namespace tab_search_prefs
diff --git a/chrome/browser/ui/webui/tab_search/tab_search_ui_browsertest.cc b/chrome/browser/ui/webui/tab_search/tab_search_ui_browsertest.cc
index 721da95658..1290b83972 100644
--- a/chrome/browser/ui/webui/tab_search/tab_search_ui_browsertest.cc
+++ b/chrome/browser/ui/webui/tab_search/tab_search_ui_browsertest.cc
@@ -4,9 +4,9 @@
 
 #include "chrome/browser/ui/webui/tab_search/tab_search_ui.h"
 
-#include <algorithm>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/stringprintf.h"
 #include "base/test/run_until.h"
@@ -145,7 +145,7 @@ IN_PROC_BROWSER_TEST_F(TabSearchUIBrowserTest, DISABLED_CloseTabAction) {
     open_tab_ids.push_back(
         browser()->tab_strip_model()->GetTabAtIndex(tab_index)->GetHandle());
   }
-  ASSERT_FALSE(std::ranges::contains(open_tab_ids, tab_id));
+  ASSERT_FALSE(base::Contains(open_tab_ids, tab_id));
 }
 
 // When hosting the Tab Search UI as a browser tab, ensure that closing the tab
@@ -193,7 +193,7 @@ IN_PROC_BROWSER_TEST_F(TabSearchUIBrowserTest,
     open_tab_ids.push_back(
         tab_strip_model->GetTabAtIndex(tab_index)->GetHandle());
   }
-  ASSERT_FALSE(std::ranges::contains(open_tab_ids, tab_id));
+  ASSERT_FALSE(base::Contains(open_tab_ids, tab_id));
 }
 
 #if BUILDFLAG(ENABLE_WEBUI_GENERATE_CODE_CACHE)
diff --git a/chrome/browser/ui/webui/tab_strip_internals/tab_strip_internals_handler_browsertest.cc b/chrome/browser/ui/webui/tab_strip_internals/tab_strip_internals_handler_browsertest.cc
index 2076d482db..dd11aef26e 100644
--- a/chrome/browser/ui/webui/tab_strip_internals/tab_strip_internals_handler_browsertest.cc
+++ b/chrome/browser/ui/webui/tab_strip_internals/tab_strip_internals_handler_browsertest.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/browser/ui/webui/tab_strip_internals/tab_strip_internals_handler.h"
 
-#include <algorithm>
 #include <memory>
 
 #include "base/functional/bind.h"
@@ -121,8 +120,8 @@ void AssertValidSelectionModel(
   EXPECT_GE(selection->anchor_index, 0);
   EXPECT_LT(selection->anchor_index, static_cast<int>(tab_count));
   EXPECT_FALSE(selection->selected_indices.empty());
-  EXPECT_TRUE(std::ranges::contains(selection->selected_indices,
-                                    selection->active_index));
+  EXPECT_TRUE(
+      base::Contains(selection->selected_indices, selection->active_index));
 
   for (int index : selection->selected_indices) {
     EXPECT_GE(index, 0);
diff --git a/chrome/browser/ui/webui/theme_source.cc b/chrome/browser/ui/webui/theme_source.cc
index 74512212a6..d9b25426b2 100644
--- a/chrome/browser/ui/webui/theme_source.cc
+++ b/chrome/browser/ui/webui/theme_source.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ref_counted_memory.h"
 #include "base/metrics/histogram_functions.h"
@@ -321,7 +322,7 @@ std::optional<std::string> ThemeSource::GenerateColorsCss(
   css_string.append(css_header);
 
   for (const auto& def : definitions) {
-    if (!std::ranges::contains(color_id_sets, def.name)) {
+    if (!base::Contains(color_id_sets, def.name)) {
       continue;
     }
 
diff --git a/chrome/browser/ui/webui/top_chrome/preload_candidate_selector.cc b/chrome/browser/ui/webui/top_chrome/preload_candidate_selector.cc
index a4f3f58cad..8f986edcfd 100644
--- a/chrome/browser/ui/webui/top_chrome/preload_candidate_selector.cc
+++ b/chrome/browser/ui/webui/top_chrome/preload_candidate_selector.cc
@@ -4,10 +4,10 @@
 
 #include "chrome/browser/ui/webui/top_chrome/preload_candidate_selector.h"
 
-#include <algorithm>
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_split.h"
 #include "chrome/browser/ui/ui_features.h"
@@ -34,7 +34,7 @@ namespace webui {
 bool PreloadCandidateSelector::IsUrlExcludedByFlag(const GURL& url) const {
   static base::NoDestructor<std::vector<url::Origin>> excluded_origins_(
       GetOriginsExcludedByFlag());
-  return std::ranges::contains(*excluded_origins_, url::Origin::Create(url));
+  return base::Contains(*excluded_origins_, url::Origin::Create(url));
 }
 
 }  // namespace webui
diff --git a/chrome/browser/ui/webui/top_chrome/top_chrome_webui_config.cc b/chrome/browser/ui/webui/top_chrome/top_chrome_webui_config.cc
index 86ed48a256..bb3d831678 100644
--- a/chrome/browser/ui/webui/top_chrome/top_chrome_webui_config.cc
+++ b/chrome/browser/ui/webui/top_chrome/top_chrome_webui_config.cc
@@ -6,6 +6,7 @@
 
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/functional/function_ref.h"
 #include "base/memory/raw_ptr.h"
 #include "base/no_destructor.h"
diff --git a/chrome/browser/ui/webui/top_chrome/webui_contents_preload_manager.cc b/chrome/browser/ui/webui/top_chrome/webui_contents_preload_manager.cc
index cf8adfb73f..8f2c638386 100644
--- a/chrome/browser/ui/webui/top_chrome/webui_contents_preload_manager.cc
+++ b/chrome/browser/ui/webui/top_chrome/webui_contents_preload_manager.cc
@@ -4,12 +4,12 @@
 
 #include "chrome/browser/ui/webui/top_chrome/webui_contents_preload_manager.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <string>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/memory/weak_ptr.h"
 #include "base/metrics/histogram_functions.h"
@@ -70,7 +70,7 @@ class FixedCandidateSelector : public webui::PreloadCandidateSelector {
 
   // webui::PreloadCandidateSelector:
   void Init(const std::vector<GURL>& preloadable_urls) override {
-    DCHECK(std::ranges::contains(preloadable_urls, webui_url_));
+    DCHECK(base::Contains(preloadable_urls, webui_url_));
   }
   std::optional<GURL> GetURLToPreload(
       const webui::PreloadContext& context) const override {
diff --git a/chrome/browser/ui/webui/top_chrome/webui_contents_warmup_level_recorder.cc b/chrome/browser/ui/webui/top_chrome/webui_contents_warmup_level_recorder.cc
index f2460d20c6..7de17597a4 100644
--- a/chrome/browser/ui/webui/top_chrome/webui_contents_warmup_level_recorder.cc
+++ b/chrome/browser/ui/webui/top_chrome/webui_contents_warmup_level_recorder.cc
@@ -4,8 +4,7 @@
 
 #include "chrome/browser/ui/webui/top_chrome/webui_contents_warmup_level_recorder.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "chrome/browser/ui/webui/top_chrome/webui_contents_preload_manager.h"
 #include "chrome/browser/ui/webui/top_chrome/webui_contents_warmup_level.h"
 #include "chrome/common/webui_url_utils.h"
@@ -72,7 +71,7 @@ void WebUIContentsWarmupLevelRecorder::AfterContentsCreation(
   CHECK(pre_condition_) << "You must call BeforeContentsCreation()";
   CHECK(web_contents);
 
-  if (std::ranges::contains(
+  if (base::Contains(
           pre_condition_->spare_process_ids,
           web_contents->GetPrimaryMainFrame()->GetProcess()->GetID())) {
     level_ = WebUIContentsWarmupLevel::kSpareRenderer;
diff --git a/chrome/browser/ui/webui/web_app_internals/iwa_internals_handler.cc b/chrome/browser/ui/webui/web_app_internals/iwa_internals_handler.cc
index 654b89dc18..cd0fca41d6 100644
--- a/chrome/browser/ui/webui/web_app_internals/iwa_internals_handler.cc
+++ b/chrome/browser/ui/webui/web_app_internals/iwa_internals_handler.cc
@@ -131,7 +131,7 @@ class IwaInternalsHandler::IwaManifestInstallUpdateHandler
       std::optional<IwaVersion> pinned_version,
       bool allow_downgrades,
       Handler::UpdateManifestInstalledIsolatedWebAppCallback callback) {
-    if (update_requests_.contains(app_id)) {
+    if (base::Contains(update_requests_, app_id)) {
       std::move(callback).Run(
           "Update check skipped: please wait for the pending update request to "
           "resolve first.");
@@ -680,7 +680,8 @@ void IwaInternalsHandler::SetAllowDowngradesForIsolatedWebApp(
   }
 
   // Removes `app_id` for which downgrades were turned off.
-  if (app_ids_allowing_downgrades_.contains(app_id) && !allow_downgrades) {
+  if (base::Contains(app_ids_allowing_downgrades_, app_id) &&
+      !allow_downgrades) {
     app_ids_allowing_downgrades_.erase(app_id);
     return;
   }
diff --git a/chrome/browser/ui/webui/whats_new/whats_new_storage_service_impl.cc b/chrome/browser/ui/webui/whats_new/whats_new_storage_service_impl.cc
index 2779327fed..d9bc9c73de 100644
--- a/chrome/browser/ui/webui/whats_new/whats_new_storage_service_impl.cc
+++ b/chrome/browser/ui/webui/whats_new/whats_new_storage_service_impl.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/webui/whats_new/whats_new_storage_service_impl.h"
 
+#include "base/containers/contains.h"
 #include "base/values.h"
 #include "chrome/browser/browser_process.h"
 #include "chrome/common/chrome_version.h"
diff --git a/chrome/browser/unexpire_flags.cc b/chrome/browser/unexpire_flags.cc
index 47f3f1e95f..bd262b6c87 100644
--- a/chrome/browser/unexpire_flags.cc
+++ b/chrome/browser/unexpire_flags.cc
@@ -7,6 +7,7 @@
 #include "base/check.h"
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/feature_list.h"
 #include "base/no_destructor.h"
@@ -27,7 +28,7 @@ static FlagNameToExpirationMap* GetFlagExpirationOverrideMap() {
 }
 
 int ExpirationMilestoneForFlag(const char* flag) {
-  if (GetFlagExpirationOverrideMap()->contains(flag)) {
+  if (base::Contains(*GetFlagExpirationOverrideMap(), flag)) {
     return GetFlagExpirationOverrideMap()->at(flag);
   }
 
@@ -110,7 +111,7 @@ bool IsFlagExpired(const flags_ui::FlagsStorage* storage,
   // unexpiry happens during FeatureList initialization.
   // TODO(ellyjones): what might we do about that?
   std::set<int> unexpired_milestones = UnexpiredMilestonesFromStorage(storage);
-  if (unexpired_milestones.contains(mstone)) {
+  if (base::Contains(unexpired_milestones, mstone)) {
     return false;
   }
 
diff --git a/chrome/browser/usb/chrome_usb_delegate.cc b/chrome/browser/usb/chrome_usb_delegate.cc
index 340854af12..a722c8d98d 100644
--- a/chrome/browser/usb/chrome_usb_delegate.cc
+++ b/chrome/browser/usb/chrome_usb_delegate.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "base/observer_list_types.h"
 #include "base/scoped_observation.h"
@@ -95,7 +96,7 @@ bool IsDevicePermissionAutoGranted(
   // Note: The `DeviceHasInterfaceWithClass()` call is made after checking the
   // origin, since that method call is expensive.
   if (origin.scheme() == extensions::kExtensionScheme &&
-      kSmartCardPrivilegedExtensionIds.contains(origin.host()) &&
+      base::Contains(kSmartCardPrivilegedExtensionIds, origin.host()) &&
       DeviceHasInterfaceWithClass(device_info,
                                   device::mojom::kUsbSmartCardClass)) {
     return true;
@@ -244,12 +245,12 @@ void ChromeUsbDelegate::AdjustProtectedInterfaceClasses(
           "moklfjoegmpoolceggbebbmgbddlhdgp",
       });
 
-  if (kHidPrivilegedExtensionIds.contains(origin.host())) {
+  if (base::Contains(kHidPrivilegedExtensionIds, origin.host())) {
     std::erase(classes, device::mojom::kUsbHidClass);
   }
 #endif  // BUILDFLAG(IS_CHROMEOS)
 
-  if (kSmartCardPrivilegedExtensionIds.contains(origin.host())) {
+  if (base::Contains(kSmartCardPrivilegedExtensionIds, origin.host())) {
     std::erase(classes, device::mojom::kUsbSmartCardClass);
   }
 #endif  // BUILDFLAG(ENABLE_EXTENSIONS)
@@ -392,7 +393,7 @@ void ChromeUsbDelegate::RemoveObserver(content::BrowserContext* browser_context,
 ChromeUsbDelegate::ContextObservation* ChromeUsbDelegate::GetContextObserver(
     content::BrowserContext* browser_context) {
   CHECK(browser_context);
-  if (!observations_.contains(browser_context)) {
+  if (!base::Contains(observations_, browser_context)) {
     observations_.emplace(browser_context, std::make_unique<ContextObservation>(
                                                this, browser_context));
   }
diff --git a/chrome/browser/usb/usb_chooser_context.cc b/chrome/browser/usb/usb_chooser_context.cc
index bf750ed4e4..7e53eacf88 100644
--- a/chrome/browser/usb/usb_chooser_context.cc
+++ b/chrome/browser/usb/usb_chooser_context.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/observer_list.h"
 #include "base/strings/string_number_conversions.h"
@@ -288,7 +289,7 @@ UsbChooserContext::GetGrantedObjects(const url::Origin& origin) {
         // always be called after device initialization in UsbChooserController
         // which always returns after the device list initialization in this
         // class.
-        DCHECK(devices_.contains(guid));
+        DCHECK(base::Contains(devices_, guid));
         objects.push_back(std::make_unique<Object>(
             origin, DeviceInfoToValue(*devices_[guid]),
             content_settings::SettingSource::kUser, is_incognito_));
@@ -358,7 +359,7 @@ UsbChooserContext::GetAllGrantedObjects() {
       continue;
 
     for (const std::string& guid : map_entry.second) {
-      DCHECK(devices_.contains(guid));
+      DCHECK(base::Contains(devices_, guid));
       objects.push_back(std::make_unique<Object>(
           origin, DeviceInfoToValue(*devices_[guid]),
           content_settings::SettingSource::kUser, is_incognito_));
@@ -419,7 +420,7 @@ void UsbChooserContext::RevokeObjectPermission(const url::Origin& origin,
 void UsbChooserContext::RevokeDevicePermissionWebInitiated(
     const url::Origin& origin,
     const device::mojom::UsbDeviceInfo& device) {
-  DCHECK(devices_.contains(device.guid));
+  DCHECK(base::Contains(devices_, device.guid));
   RevokeObjectPermissionInternal(origin, DeviceInfoToValue(device),
                                  /*revoked_by_website=*/true);
 }
@@ -503,7 +504,8 @@ bool UsbChooserContext::HasDevicePermission(
     return false;
 
   auto it = ephemeral_devices_.find(origin);
-  if (it != ephemeral_devices_.end() && it->second.contains(device_info.guid)) {
+  if (it != ephemeral_devices_.end() &&
+      base::Contains(it->second, device_info.guid)) {
     return true;
   }
 
@@ -588,7 +590,7 @@ void UsbChooserContext::OnDeviceAdded(
     device::mojom::UsbDeviceInfoPtr device_info) {
   DCHECK(device_info);
   // Update the device list.
-  DCHECK(!devices_.contains(device_info->guid));
+  DCHECK(!base::Contains(devices_, device_info->guid));
   if (!ShouldExposeDevice(*device_info))
     return;
   devices_.insert(std::make_pair(device_info->guid, device_info->Clone()));
@@ -603,12 +605,12 @@ void UsbChooserContext::OnDeviceRemoved(
   DCHECK(device_info);
 
   if (!ShouldExposeDevice(*device_info)) {
-    DCHECK(!devices_.contains(device_info->guid));
+    DCHECK(!base::Contains(devices_, device_info->guid));
     return;
   }
 
   // Update the device list.
-  DCHECK(devices_.contains(device_info->guid));
+  DCHECK(base::Contains(devices_, device_info->guid));
   devices_.erase(device_info->guid);
 
   // Notify all device observers.
diff --git a/chrome/browser/vr/ui_host/vr_ui_host_impl.cc b/chrome/browser/vr/ui_host/vr_ui_host_impl.cc
index 40c2f8e44d..652c918a50 100644
--- a/chrome/browser/vr/ui_host/vr_ui_host_impl.cc
+++ b/chrome/browser/vr/ui_host/vr_ui_host_impl.cc
@@ -4,9 +4,9 @@
 
 #include "chrome/browser/vr/ui_host/vr_ui_host_impl.h"
 
-#include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/task/single_thread_task_runner.h"
 #include "build/build_config.h"
 #include "chrome/browser/media/webrtc/media_capture_devices_dispatcher.h"
@@ -134,10 +134,10 @@ VRUiHostImpl::VRUiHostImpl(
       triggered_capturing_transience_(&triggered_capturing_state_model_) {
   DVLOG(1) << __func__;
   DCHECK_CALLED_ON_VALID_THREAD(thread_checker_);
-  CHECK(std::ranges::contains(views, device::mojom::XREye::kLeft,
-                              &device::mojom::XRView::eye));
-  CHECK(std::ranges::contains(views, device::mojom::XREye::kRight,
-                              &device::mojom::XRView::eye));
+  CHECK(base::Contains(views, device::mojom::XREye::kLeft,
+                       &device::mojom::XRView::eye));
+  CHECK(base::Contains(views, device::mojom::XREye::kRight,
+                       &device::mojom::XRView::eye));
 
   DesktopMediaPickerManager::Get()->AddObserver(this);
 
diff --git a/chrome/browser/vr/webxr_vr_input_browser_test.cc b/chrome/browser/vr/webxr_vr_input_browser_test.cc
index bfba948f53..26960a3509 100644
--- a/chrome/browser/vr/webxr_vr_input_browser_test.cc
+++ b/chrome/browser/vr/webxr_vr_input_browser_test.cc
@@ -2,8 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_number_conversions.h"
 #include "chrome/browser/vr/test/mock_xr_device_hook_base.h"
@@ -730,7 +729,7 @@ WEBXR_VR_ALL_RUNTIMES_BROWSER_TEST_F(TestAllKnownInteractionProfileTypes) {
       static_cast<uint32_t>(kInitialInteractionProfile) + 1;
   for (uint32_t i = kFirstChangedProfileIndex; i <= kFinalValue; i++) {
     auto profile = static_cast<device::mojom::OpenXrInteractionProfileType>(i);
-    if (std::ranges::contains(kSkippedInteractionProfiles, profile)) {
+    if (base::Contains(kSkippedInteractionProfiles, profile)) {
       continue;
     }
     my_mock.UpdateInteractionProfile(profile);
diff --git a/chrome/browser/web_applications/app_service/web_app_publisher_helper.cc b/chrome/browser/web_applications/app_service/web_app_publisher_helper.cc
index 341d0cbcef..5c78b04ea5 100644
--- a/chrome/browser/web_applications/app_service/web_app_publisher_helper.cc
+++ b/chrome/browser/web_applications/app_service/web_app_publisher_helper.cc
@@ -21,6 +21,7 @@
 #include "base/check.h"
 #include "base/check_op.h"
 #include "base/containers/checked_iterators.h"
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
@@ -546,7 +547,7 @@ WebAppPublisherHelper::~WebAppPublisherHelper() = default;
 // static
 bool WebAppPublisherHelper::IsSupportedWebAppPermissionType(
     ContentSettingsType permission_type) {
-  return std::ranges::contains(kSupportedPermissionTypes, permission_type);
+  return base::Contains(kSupportedPermissionTypes, permission_type);
 }
 
 void WebAppPublisherHelper::Shutdown() {
diff --git a/chrome/browser/web_applications/commands/fetch_manifest_and_install_command.cc b/chrome/browser/web_applications/commands/fetch_manifest_and_install_command.cc
index a58485e4c7..3b64892c7c 100644
--- a/chrome/browser/web_applications/commands/fetch_manifest_and_install_command.cc
+++ b/chrome/browser/web_applications/commands/fetch_manifest_and_install_command.cc
@@ -219,7 +219,7 @@ void FetchManifestAndInstallCommand::GetScreenshot(
         callback) {
   // If the screenshot for a specific index has been downloaded, run the
   // callback instantly.
-  if (screenshots_downloaded_.contains(index)) {
+  if (base::Contains(screenshots_downloaded_, index)) {
     auto screenshot_info = screenshots_downloaded_.at(index);
     std::move(callback).Run(
         std::get<SkBitmap>(screenshot_info),
diff --git a/chrome/browser/web_applications/commands/fetch_manifest_and_update_command.cc b/chrome/browser/web_applications/commands/fetch_manifest_and_update_command.cc
index 48c4eaece6..48b249193a 100644
--- a/chrome/browser/web_applications/commands/fetch_manifest_and_update_command.cc
+++ b/chrome/browser/web_applications/commands/fetch_manifest_and_update_command.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/to_string.h"
@@ -134,7 +135,7 @@ void FetchManifestAndUpdateCommand::OnManifestRetrieved(
 
   bool has_any_icon = false;
   for (const auto& icon : manifest->icons) {
-    if (std::ranges::contains(
+    if (base::Contains(
             icon.purpose, blink::mojom::ManifestImageResource_Purpose::ANY)) {
       has_any_icon = true;
       break;
diff --git a/chrome/browser/web_applications/commands/install_app_from_verified_manifest_command.cc b/chrome/browser/web_applications/commands/install_app_from_verified_manifest_command.cc
index fd23ef891c..2788177bdc 100644
--- a/chrome/browser/web_applications/commands/install_app_from_verified_manifest_command.cc
+++ b/chrome/browser/web_applications/commands/install_app_from_verified_manifest_command.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/flat_tree.h"
 #include "base/functional/bind.h"
diff --git a/chrome/browser/web_applications/commands/install_from_sync_command_browsertest.cc b/chrome/browser/web_applications/commands/install_from_sync_command_browsertest.cc
index 87a7d4c481..3439902d67 100644
--- a/chrome/browser/web_applications/commands/install_from_sync_command_browsertest.cc
+++ b/chrome/browser/web_applications/commands/install_from_sync_command_browsertest.cc
@@ -84,7 +84,7 @@ class InstallFromSyncCommandTest : public base::test::WithFeatureOverride,
     web_app_provider->icon_manager().ReadIconAndResize(
         app_id, IconPurpose::ANY, size, test_future.GetCallback());
     SizeToBitmap bitmaps = test_future.Take();
-    CHECK(bitmaps.contains(size));
+    CHECK(base::Contains(bitmaps, size));
     return bitmaps[size];
   }
 
diff --git a/chrome/browser/web_applications/commands/manifest_silent_update_command.cc b/chrome/browser/web_applications/commands/manifest_silent_update_command.cc
index dd81c1111b..416f76a073 100644
--- a/chrome/browser/web_applications/commands/manifest_silent_update_command.cc
+++ b/chrome/browser/web_applications/commands/manifest_silent_update_command.cc
@@ -12,6 +12,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/functional/concurrent_closures.h"
 #include "base/i18n/time_formatting.h"
diff --git a/chrome/browser/web_applications/commands/update_protocol_handler_approval_command.cc b/chrome/browser/web_applications/commands/update_protocol_handler_approval_command.cc
index 3d6aee93f8..3320d127b5 100644
--- a/chrome/browser/web_applications/commands/update_protocol_handler_approval_command.cc
+++ b/chrome/browser/web_applications/commands/update_protocol_handler_approval_command.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
diff --git a/chrome/browser/web_applications/commands/update_protocol_handler_approval_command_browsertest.cc b/chrome/browser/web_applications/commands/update_protocol_handler_approval_command_browsertest.cc
index 754961bb4e..69b7934307 100644
--- a/chrome/browser/web_applications/commands/update_protocol_handler_approval_command_browsertest.cc
+++ b/chrome/browser/web_applications/commands/update_protocol_handler_approval_command_browsertest.cc
@@ -2,6 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+#include "base/containers/contains.h"
 #include "base/test/test_future.h"
 #include "build/build_config.h"
 #include "chrome/browser/ui/web_applications/web_app_browsertest_base.h"
diff --git a/chrome/browser/web_applications/externally_managed_app_manager.cc b/chrome/browser/web_applications/externally_managed_app_manager.cc
index 6b3c10d4d9..163da66b9c 100644
--- a/chrome/browser/web_applications/externally_managed_app_manager.cc
+++ b/chrome/browser/web_applications/externally_managed_app_manager.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
 #include "base/stl_util.h"
@@ -182,7 +183,7 @@ void ExternallyManagedAppManager::SynchronizeInstalledApps(
       }));
   // Only one concurrent SynchronizeInstalledApps() expected per
   // ExternalInstallSource.
-  CHECK(!synchronize_requests_.contains(install_source));
+  CHECK(!base::Contains(synchronize_requests_, install_source));
   provider_->scheduler().ScheduleCallback<AllAppsLock>(
       "ExternallyManagedAppManager::SynchronizeInstalledApps",
       AllAppsLockDescription(),
diff --git a/chrome/browser/web_applications/icons/trusted_icon_filter.cc b/chrome/browser/web_applications/icons/trusted_icon_filter.cc
index dc56da29f7..d5ad3f81bd 100644
--- a/chrome/browser/web_applications/icons/trusted_icon_filter.cc
+++ b/chrome/browser/web_applications/icons/trusted_icon_filter.cc
@@ -4,10 +4,10 @@
 
 #include "chrome/browser/web_applications/icons/trusted_icon_filter.h"
 
-#include <algorithm>
 #include <optional>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "chrome/browser/web_applications/web_app_icon_operations.h"
 #include "chrome/browser/web_applications/web_app_install_info.h"
diff --git a/chrome/browser/web_applications/isolated_web_apps/commands/cleanup_bundle_cache_command.cc b/chrome/browser/web_applications/isolated_web_apps/commands/cleanup_bundle_cache_command.cc
index a3c69e9393..5971119bd8 100644
--- a/chrome/browser/web_applications/isolated_web_apps/commands/cleanup_bundle_cache_command.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/commands/cleanup_bundle_cache_command.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/browser/web_applications/isolated_web_apps/commands/cleanup_bundle_cache_command.h"
 
-#include <algorithm>
 #include <string>
 #include <vector>
 
@@ -47,7 +46,7 @@ CleanupBundleCacheResult CleanupBundleCacheCommandImpl(
                                          base::FileEnumerator::DIRECTORIES);
   all_iwa_dirs_iter.ForEach(
       [&dirs_to_keep, &dirs_to_delete](const base::FilePath& dir_path) {
-        if (!std::ranges::contains(dirs_to_keep, dir_path)) {
+        if (!base::Contains(dirs_to_keep, dir_path)) {
           dirs_to_delete.push_back(dir_path);
         }
       });
diff --git a/chrome/browser/web_applications/isolated_web_apps/isolated_web_app_managed_configuration_api_browsertest.cc b/chrome/browser/web_applications/isolated_web_apps/isolated_web_app_managed_configuration_api_browsertest.cc
index 4b96151769..4658f6be27 100644
--- a/chrome/browser/web_applications/isolated_web_apps/isolated_web_app_managed_configuration_api_browsertest.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/isolated_web_app_managed_configuration_api_browsertest.cc
@@ -47,7 +47,7 @@ struct ResponseTemplate {
 std::unique_ptr<net::test_server::HttpResponse> HandleRequest(
     std::map<std::string, ResponseTemplate> templates,
     const net::test_server::HttpRequest& request) {
-  if (!templates.contains(request.relative_url)) {
+  if (!base::Contains(templates, request.relative_url)) {
     return std::make_unique<net::test_server::HungResponse>();
   }
 
diff --git a/chrome/browser/web_applications/isolated_web_apps/isolated_web_app_url_loader_factory_browsertest.cc b/chrome/browser/web_applications/isolated_web_apps/isolated_web_app_url_loader_factory_browsertest.cc
index a68db7b0df..06c1a37acf 100644
--- a/chrome/browser/web_applications/isolated_web_apps/isolated_web_app_url_loader_factory_browsertest.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/isolated_web_app_url_loader_factory_browsertest.cc
@@ -78,7 +78,8 @@ class IsolatedWebAppURLLoaderFactoryBrowserTest
         [](const content::WebContentsConsoleObserver::Message& message) {
           return message.log_level ==
                      blink::mojom::ConsoleMessageLevel::kError &&
-                 message.message.contains(
+                 base::Contains(
+                     message.message,
                      u"Failed to read response from Signed Web Bundle");
         }));
 
diff --git a/chrome/browser/web_applications/isolated_web_apps/key_distribution/iwa_key_distribution_info_provider.cc b/chrome/browser/web_applications/isolated_web_apps/key_distribution/iwa_key_distribution_info_provider.cc
index 8def8e2c25..4018e07437 100644
--- a/chrome/browser/web_applications/isolated_web_apps/key_distribution/iwa_key_distribution_info_provider.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/key_distribution/iwa_key_distribution_info_provider.cc
@@ -147,7 +147,8 @@ IwaKeyDistributionInfoProvider::OnRuntimeDataChanged(
 
 bool IwaKeyDistributionInfoProvider::IsBundleBlocklisted(
     std::string_view web_bundle_id) const {
-  return component_ && component_->data.blocklist.contains(web_bundle_id);
+  return component_ &&
+         base::Contains(component_->data.blocklist, web_bundle_id);
 }
 
 bool IwaKeyDistributionInfoProvider::IsManagedInstallPermitted(
@@ -158,7 +159,8 @@ bool IwaKeyDistributionInfoProvider::IsManagedInstallPermitted(
   }
 
   bool is_permitted =
-      component_ && component_->data.managed_allowlist.contains(web_bundle_id);
+      component_ &&
+      base::Contains(component_->data.managed_allowlist, web_bundle_id);
 
   base::UmaHistogramEnumeration(
       kIwaKeyDistributionManagedInstallCheckInfoSourceHistogramName,
@@ -177,7 +179,8 @@ bool IwaKeyDistributionInfoProvider::IsManagedUpdatePermitted(
   }
 
   bool is_permitted =
-      component_ && component_->data.managed_allowlist.contains(web_bundle_id);
+      component_ &&
+      base::Contains(component_->data.managed_allowlist, web_bundle_id);
 
   base::UmaHistogramEnumeration(
       kIwaKeyDistributionManagedUpdateCheckInfoSourceHistogramName,
diff --git a/chrome/browser/web_applications/isolated_web_apps/policy/isolated_web_app_policy_manager.cc b/chrome/browser/web_applications/isolated_web_apps/policy/isolated_web_app_policy_manager.cc
index 32bdedcd84..bbf3cfd8d1 100644
--- a/chrome/browser/web_applications/isolated_web_apps/policy/isolated_web_app_policy_manager.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/policy/isolated_web_app_policy_manager.cc
@@ -16,6 +16,7 @@
 #include "base/barrier_closure.h"
 #include "base/check_deref.h"
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/containers/to_value_list.h"
 #include "base/feature_list.h"
@@ -451,7 +452,7 @@ void IsolatedWebAppPolicyManager::DoProcessPolicy(AllAppsLock& lock,
 
   for (const auto& [web_bundle_id, installed_iwa] : installed_iwas) {
     if (installed_iwa->GetSources().Has(WebAppManagement::kIwaPolicy) &&
-        !std::ranges::contains(
+        !base::Contains(
             apps_in_policy, web_bundle_id,
             &IsolatedWebAppExternalInstallOptions::web_bundle_id)) {
       app_actions.emplace(web_bundle_id, AppActionRemoveInstallSource(
diff --git a/chrome/browser/web_applications/isolated_web_apps/policy/isolated_web_app_policy_manager_browsertest.cc b/chrome/browser/web_applications/isolated_web_apps/policy/isolated_web_app_policy_manager_browsertest.cc
index c03a4a1427..403cd4e74d 100644
--- a/chrome/browser/web_applications/isolated_web_apps/policy/isolated_web_app_policy_manager_browsertest.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/policy/isolated_web_app_policy_manager_browsertest.cc
@@ -7,7 +7,6 @@
 #include <optional>
 #include <string>
 #include <string_view>
-#include <utility>
 #include <vector>
 
 #include "base/check_deref.h"
@@ -24,6 +23,7 @@
 #include "base/test/run_until.h"
 #include "base/test/test_future.h"
 #include "base/threading/thread_restrictions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected.h"
 #include "base/version.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
@@ -818,7 +818,7 @@ class IsolatedWebAppDevToolsTestWithPolicy
   void SetDevToolsAvailability() {
     GetProfileForTest()->GetPrefs()->SetInteger(
         prefs::kDevToolsAvailability,
-        std::to_underlying(
+        base::to_underlying(
             std::get<DeveloperToolsPolicyHandler::Availability>(GetParam())));
   }
   bool AreDevToolsWindowsAllowedByCurrentPolicy() const {
diff --git a/chrome/browser/web_applications/isolated_web_apps/test/bundle_versions_storage.cc b/chrome/browser/web_applications/isolated_web_apps/test/bundle_versions_storage.cc
index 14132134cc..78bb93724b 100644
--- a/chrome/browser/web_applications/isolated_web_apps/test/bundle_versions_storage.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/test/bundle_versions_storage.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/web_applications/isolated_web_apps/test/bundle_versions_storage.h"
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/containers/to_value_list.h"
 #include "base/json/json_writer.h"
diff --git a/chrome/browser/web_applications/isolated_web_apps/test/fake_chrome_iwa_runtime_data_provider.cc b/chrome/browser/web_applications/isolated_web_apps/test/fake_chrome_iwa_runtime_data_provider.cc
index 98d350c2a7..2fdd80ebc7 100644
--- a/chrome/browser/web_applications/isolated_web_apps/test/fake_chrome_iwa_runtime_data_provider.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/test/fake_chrome_iwa_runtime_data_provider.cc
@@ -4,10 +4,9 @@
 
 #include "chrome/browser/web_applications/isolated_web_apps/test/fake_chrome_iwa_runtime_data_provider.h"
 
-#include <algorithm>
-
 #include "base/base64.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/containers/to_vector.h"
 #include "chrome/browser/web_applications/isolated_web_apps/runtime_data/chrome_iwa_runtime_data_provider.h"
@@ -151,20 +150,20 @@ FakeIwaRuntimeDataProvider::GetUserInstallAllowlistData(
 
 bool FakeIwaRuntimeDataProvider::IsManagedInstallPermitted(
     std::string_view web_bundle_id) const {
-  return std::ranges::contains(managed_allowlist_, web_bundle_id,
-                               &web_package::SignedWebBundleId::id);
+  return base::Contains(managed_allowlist_, web_bundle_id,
+                        &web_package::SignedWebBundleId::id);
 }
 
 bool FakeIwaRuntimeDataProvider::IsManagedUpdatePermitted(
     std::string_view web_bundle_id) const {
-  return std::ranges::contains(managed_allowlist_, web_bundle_id,
-                               &web_package::SignedWebBundleId::id);
+  return base::Contains(managed_allowlist_, web_bundle_id,
+                        &web_package::SignedWebBundleId::id);
 }
 
 bool FakeIwaRuntimeDataProvider::IsBundleBlocklisted(
     std::string_view web_bundle_id) const {
-  return std::ranges::contains(blocklist_, web_bundle_id,
-                               &web_package::SignedWebBundleId::id);
+  return base::Contains(blocklist_, web_bundle_id,
+                        &web_package::SignedWebBundleId::id);
 }
 
 const ChromeIwaRuntimeDataProvider::SpecialAppPermissionsInfo*
diff --git a/chrome/browser/web_applications/isolated_web_apps/update/isolated_web_app_update_discovery_task.cc b/chrome/browser/web_applications/isolated_web_apps/update/isolated_web_app_update_discovery_task.cc
index 47a7ca8a49..81dcd46a0c 100644
--- a/chrome/browser/web_applications/isolated_web_apps/update/isolated_web_app_update_discovery_task.cc
+++ b/chrome/browser/web_applications/isolated_web_apps/update/isolated_web_app_update_discovery_task.cc
@@ -372,7 +372,8 @@ void IsolatedWebAppUpdateDiscoveryTask::CheckIntegrityBundleForRotatedKey(
   // potentially big, bundle if it is not signed by the appropriate rotated key.
   if (initial_bytes &&
       initial_bytes->rfind("📦") != initial_bytes->find("📦") &&
-      !initial_bytes.value().contains(base::as_string_view(rotated_key))) {
+      !base::Contains(initial_bytes.value(),
+                      base::as_string_view(rotated_key))) {
     FailWith(Error::kUpdateManifestNoApplicableVersion);
     return;
   }
diff --git a/chrome/browser/web_applications/jobs/manifest_to_web_app_install_info_job.cc b/chrome/browser/web_applications/jobs/manifest_to_web_app_install_info_job.cc
index ea75557f06..0336bf8235 100644
--- a/chrome/browser/web_applications/jobs/manifest_to_web_app_install_info_job.cc
+++ b/chrome/browser/web_applications/jobs/manifest_to_web_app_install_info_job.cc
@@ -13,6 +13,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -226,7 +227,7 @@ void UpdateWebAppInstallInfoIconsFromManifestIfNeeded(
       apps::IconInfo info;
 
       if (!icon.sizes.empty()) {
-        if (std::ranges::contains(icon.sizes, gfx::Size()) &&
+        if (base::Contains(icon.sizes, gfx::Size()) &&
             icon.src.spec().find(".svg") != std::string::npos) {
           web_app_info->icons_with_size_any.manifest_icons[purpose] = icon.src;
         }
@@ -316,13 +317,13 @@ void PopulateWebAppShortcutsMenuItemInfos(
       std::vector<WebAppShortcutsMenuItemInfo::Icon> shortcut_icons;
       for (const auto& icon : shortcut_icons_list) {
         CHECK(!icon.purpose.empty());
-        if (!std::ranges::contains(icon.purpose, purpose)) {
+        if (!base::Contains(icon.purpose, purpose)) {
           continue;
         }
 
         WebAppShortcutsMenuItemInfo::Icon info;
 
-        if (std::ranges::contains(icon.sizes, gfx::Size()) &&
+        if (base::Contains(icon.sizes, gfx::Size()) &&
             icon.src.spec().find(".svg") != std::string::npos) {
           web_app_info->icons_with_size_any.shortcut_menu_icons[purpose] =
               icon.src;
@@ -514,7 +515,7 @@ void PopulateHomeTabIconsFromHomeTabManifestParams(
     CHECK(!icon.purpose.empty());
 
     if (!icon.sizes.empty()) {
-      if (std::ranges::contains(icon.sizes, gfx::Size()) &&
+      if (base::Contains(icon.sizes, gfx::Size()) &&
           icon.src.spec().find(".svg") != std::string::npos) {
         for (const auto& purpose : icon.purpose) {
           web_app_info->icons_with_size_any.home_tab_icons[purpose] = icon.src;
diff --git a/chrome/browser/web_applications/jobs/uninstall/remove_install_source_job.cc b/chrome/browser/web_applications/jobs/uninstall/remove_install_source_job.cc
index 955a0f9e0e..0195a5f863 100644
--- a/chrome/browser/web_applications/jobs/uninstall/remove_install_source_job.cc
+++ b/chrome/browser/web_applications/jobs/uninstall/remove_install_source_job.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/strings/to_string.h"
 #include "chrome/browser/web_applications/jobs/uninstall/remove_web_app_job.h"
 #include "chrome/browser/web_applications/locks/all_apps_lock.h"
@@ -128,7 +129,7 @@ void RemoveInstallSourceJob::
     WebApp::ExternalConfigMap modified_config_map;
     for (const auto& [type, config_data] :
          app->management_to_external_config_map()) {
-      if (!std::ranges::contains(install_managements_to_remove_, type)) {
+      if (!base::Contains(install_managements_to_remove_, type)) {
         modified_config_map.insert_or_assign(type, config_data);
       }
     }
diff --git a/chrome/browser/web_applications/jobs/uninstall/remove_install_url_job.cc b/chrome/browser/web_applications/jobs/uninstall/remove_install_url_job.cc
index 3ef7462a45..c71c1cdda1 100644
--- a/chrome/browser/web_applications/jobs/uninstall/remove_install_url_job.cc
+++ b/chrome/browser/web_applications/jobs/uninstall/remove_install_url_job.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/web_applications/jobs/uninstall/remove_install_url_job.h"
 
+#include "base/containers/contains.h"
 #include "base/strings/to_string.h"
 #include "base/values.h"
 #include "chrome/browser/web_applications/jobs/uninstall/remove_install_source_job.h"
@@ -42,7 +43,7 @@ MatchingWebAppResult FindMatchingWebApp(
     }
 
     const base::flat_set<GURL>& install_urls = map_it->second.install_urls;
-    if (!install_urls.contains(install_url)) {
+    if (!base::Contains(install_urls, install_url)) {
       return {};
     }
 
@@ -58,7 +59,7 @@ MatchingWebAppResult FindMatchingWebApp(
     auto it = config_map.find(install_source);
     if (it != config_map.end()) {
       const base::flat_set<GURL>& install_urls = it->second.install_urls;
-      if (install_urls.contains(install_url)) {
+      if (base::Contains(install_urls, install_url)) {
         return {
             .app = &candidate_app,
             .is_only_install_url = install_urls.size() == 1u,
diff --git a/chrome/browser/web_applications/locks/partitioned_lock_manager.cc b/chrome/browser/web_applications/locks/partitioned_lock_manager.cc
index e59ddb828c..ccd79d5155 100644
--- a/chrome/browser/web_applications/locks/partitioned_lock_manager.cc
+++ b/chrome/browser/web_applications/locks/partitioned_lock_manager.cc
@@ -9,6 +9,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/logging.h"
@@ -134,7 +135,7 @@ void PartitionedLockManager::UpgradeToExclusive(
     const PartitionedLockId& lock_id,
     base::OnceClosure upgrade_complete,
     const base::Location& location) {
-  CHECK(locks_.contains(lock_id));
+  CHECK(base::Contains(locks_, lock_id));
   auto lock_it = std::find_if(
       locks_holder.locks_.begin(), locks_holder.locks_.end(),
       [&](PartitionedLock& lock) { return lock.lock_id() == lock_id; });
@@ -144,7 +145,8 @@ void PartitionedLockManager::UpgradeToExclusive(
   Lock& lock = locks_[partitioned_lock.lock_id()];
 
   CHECK_GT(lock.acquired_count, 0);
-  CHECK(lock.request_locations.contains(partitioned_lock.request_location()));
+  CHECK(base::Contains(lock.request_locations,
+                       partitioned_lock.request_location()));
 
   if (lock.lock_mode == LockType::kExclusive) {
     // Exit early, as it's already exclusive.
diff --git a/chrome/browser/web_applications/manifest_update_manager.cc b/chrome/browser/web_applications/manifest_update_manager.cc
index 9a74527061..b52bb7f123 100644
--- a/chrome/browser/web_applications/manifest_update_manager.cc
+++ b/chrome/browser/web_applications/manifest_update_manager.cc
@@ -11,6 +11,7 @@
 
 #include "base/check_is_test.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/map_util.h"
@@ -314,7 +315,7 @@ void ManifestUpdateManager::MaybeUpdate(
     return;
   }
 
-  if (update_stages_.contains(*app_id)) {
+  if (base::Contains(update_stages_, *app_id)) {
     return;
   }
 
@@ -505,7 +506,7 @@ bool ManifestUpdateManager::IsUpdateConsumed(const webapps::AppId& app_id,
 
 bool ManifestUpdateManager::IsUpdateCommandPending(
     const webapps::AppId& app_id) {
-  return update_stages_.contains(app_id);
+  return base::Contains(update_stages_, app_id);
 }
 
 // WebAppInstallManager:
@@ -518,7 +519,7 @@ void ManifestUpdateManager::OnWebAppWillBeUninstalled(
                  ManifestUpdateResult::kAppUninstalling);
     update_stages_.erase(it);
   }
-  CHECK(!update_stages_.contains(app_id));
+  CHECK(!base::Contains(update_stages_, app_id));
 
   // Clear any data necessary for throttling updates for the current web app.
   last_update_check_.erase(app_id);
diff --git a/chrome/browser/web_applications/manifest_update_manager_browsertest.cc b/chrome/browser/web_applications/manifest_update_manager_browsertest.cc
index b9c6652a4a..5a29f7fb9f 100644
--- a/chrome/browser/web_applications/manifest_update_manager_browsertest.cc
+++ b/chrome/browser/web_applications/manifest_update_manager_browsertest.cc
@@ -17,6 +17,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_tree.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -2569,7 +2570,7 @@ IN_PROC_BROWSER_TEST_F(ManifestUpdateManagerBrowserTestWithFileHandling,
   EXPECT_EQ(1u, old_file_handler.accept.size());
   auto old_extensions = old_file_handler.accept[0].file_extensions;
   EXPECT_EQ(1u, old_extensions.size());
-  EXPECT_TRUE(old_extensions.contains(".txt"));
+  EXPECT_TRUE(base::Contains(old_extensions, ".txt"));
 
   OverrideManifest(kFileHandlerManifestTemplate, {".md", kInstallableIconList});
   EXPECT_EQ(ManifestUpdateResult::kAppUpdated,
@@ -2581,7 +2582,7 @@ IN_PROC_BROWSER_TEST_F(ManifestUpdateManagerBrowserTestWithFileHandling,
   EXPECT_EQ(1u, new_file_handler.accept.size());
   auto new_extensions = new_file_handler.accept[0].file_extensions;
   EXPECT_EQ(1u, new_extensions.size());
-  EXPECT_TRUE(new_extensions.contains(".md"));
+  EXPECT_TRUE(base::Contains(new_extensions, ".md"));
 }
 
 IN_PROC_BROWSER_TEST_F(ManifestUpdateManagerBrowserTestWithFileHandling,
@@ -2617,7 +2618,7 @@ IN_PROC_BROWSER_TEST_F(ManifestUpdateManagerBrowserTestWithFileHandling,
   const WebApp* web_app = GetProvider().registrar_unsafe().GetAppById(app_id);
   const auto& old_file_handler = web_app->file_handlers()[0];
   auto old_extensions = old_file_handler.accept[0].file_extensions;
-  EXPECT_TRUE(old_extensions.contains(".txt"));
+  EXPECT_TRUE(base::Contains(old_extensions, ".txt"));
   const GURL url = GetAppURL();
   const GURL origin = url.DeprecatedGetOriginAsURL();
 
@@ -2635,8 +2636,8 @@ IN_PROC_BROWSER_TEST_F(ManifestUpdateManagerBrowserTestWithFileHandling,
   OverrideManifest(kFileHandlerManifestTemplate, {".md\", \".txt", "red"});
   EXPECT_EQ(ManifestUpdateResult::kAppUpdated, GetResultAfterPageLoad(url));
   auto new_extensions = web_app->file_handlers()[0].accept[0].file_extensions;
-  EXPECT_TRUE(new_extensions.contains(".md"));
-  EXPECT_TRUE(new_extensions.contains(".txt"));
+  EXPECT_TRUE(base::Contains(new_extensions, ".md"));
+  EXPECT_TRUE(base::Contains(new_extensions, ".txt"));
 
   // Set back to allowed.
   EXPECT_EQ(ApiApprovalState::kRequiresPrompt,
@@ -2652,8 +2653,8 @@ IN_PROC_BROWSER_TEST_F(ManifestUpdateManagerBrowserTestWithFileHandling,
   OverrideManifest(kFileHandlerManifestTemplate, {".md\", \".txt", "blue"});
   EXPECT_EQ(ManifestUpdateResult::kAppUpdated, GetResultAfterPageLoad(url));
   new_extensions = web_app->file_handlers()[0].accept[0].file_extensions;
-  EXPECT_TRUE(new_extensions.contains(".md"));
-  EXPECT_TRUE(new_extensions.contains(".txt"));
+  EXPECT_TRUE(base::Contains(new_extensions, ".md"));
+  EXPECT_TRUE(base::Contains(new_extensions, ".txt"));
 
   EXPECT_EQ(ApiApprovalState::kAllowed,
             GetProvider().registrar_unsafe().GetAppFileHandlerUserApprovalState(
@@ -2666,8 +2667,8 @@ IN_PROC_BROWSER_TEST_F(ManifestUpdateManagerBrowserTestWithFileHandling,
   OverrideManifest(kFileHandlerManifestTemplate, {".txt", "blue"});
   EXPECT_EQ(ManifestUpdateResult::kAppUpdated, GetResultAfterPageLoad(url));
   new_extensions = web_app->file_handlers()[0].accept[0].file_extensions;
-  EXPECT_FALSE(new_extensions.contains(".md"));
-  EXPECT_TRUE(new_extensions.contains(".txt"));
+  EXPECT_FALSE(base::Contains(new_extensions, ".md"));
+  EXPECT_TRUE(base::Contains(new_extensions, ".txt"));
   EXPECT_EQ(ApiApprovalState::kAllowed,
             GetProvider().registrar_unsafe().GetAppFileHandlerUserApprovalState(
                 app_id));
@@ -2726,7 +2727,7 @@ IN_PROC_BROWSER_TEST_F(ManifestUpdateManagerBrowserTestWithFileHandling,
   const auto& old_file_handler = web_app->file_handlers()[0];
   ASSERT_FALSE(old_file_handler.accept.empty());
   auto old_extensions = old_file_handler.accept[0].file_extensions;
-  EXPECT_TRUE(old_extensions.contains(".txt"));
+  EXPECT_TRUE(base::Contains(old_extensions, ".txt"));
   const GURL url = GetAppURL();
   const GURL origin = url.DeprecatedGetOriginAsURL();
 
diff --git a/chrome/browser/web_applications/os_integration/protocol_handling_sub_manager.cc b/chrome/browser/web_applications/os_integration/protocol_handling_sub_manager.cc
index a2a9b8d292..d00ade1e98 100644
--- a/chrome/browser/web_applications/os_integration/protocol_handling_sub_manager.cc
+++ b/chrome/browser/web_applications/os_integration/protocol_handling_sub_manager.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_functions.h"
@@ -70,8 +71,8 @@ void ProtocolHandlingSubManager::Configure(
   DCHECK(web_app);
 
   for (const auto& protocol_handler : web_app->protocol_handlers()) {
-    if (web_app->disallowed_launch_protocols().contains(
-            protocol_handler.protocol)) {
+    if (base::Contains(web_app->disallowed_launch_protocols(),
+                       protocol_handler.protocol)) {
       continue;
     }
 
diff --git a/chrome/browser/web_applications/os_integration/web_app_file_handler_manager.cc b/chrome/browser/web_applications/os_integration/web_app_file_handler_manager.cc
index 32ea16be1a..6b0c0968c6 100644
--- a/chrome/browser/web_applications/os_integration/web_app_file_handler_manager.cc
+++ b/chrome/browser/web_applications/os_integration/web_app_file_handler_manager.cc
@@ -10,6 +10,7 @@
 
 #include "base/check.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
@@ -118,8 +119,8 @@ WebAppFileHandlerManager::GetMatchingFileHandlerUrls(
     for (const auto& file_handler : *file_handlers) {
       std::set<std::string> supported_file_extensions =
           apps::GetFileExtensionsFromFileHandlers({file_handler});
-      if (supported_file_extensions.contains(
-              base::ToLowerASCII(file_extension))) {
+      if (base::Contains(supported_file_extensions,
+                         base::ToLowerASCII(file_extension))) {
         launch_handlers[&file_handler].push_back(file_path);
         break;
       }
diff --git a/chrome/browser/web_applications/os_integration/web_app_file_handler_registration_linux_browsertest.cc b/chrome/browser/web_applications/os_integration/web_app_file_handler_registration_linux_browsertest.cc
index 72992e3239..04b736a814 100644
--- a/chrome/browser/web_applications/os_integration/web_app_file_handler_registration_linux_browsertest.cc
+++ b/chrome/browser/web_applications/os_integration/web_app_file_handler_registration_linux_browsertest.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/files/file_path.h"
 #include "base/run_loop.h"
@@ -139,8 +140,8 @@ IN_PROC_BROWSER_TEST_F(
   // the update-desktop-database call.
   EXPECT_TRUE(base::StartsWith(xdg_commands_called[1].xdg_command,
                                "update-desktop-database"));
-  EXPECT_TRUE(xdg_commands_called[1].xdg_command.contains(
-      GetUserApplicationsDir().value()));
+  EXPECT_TRUE(base::Contains(xdg_commands_called[1].xdg_command,
+                             GetUserApplicationsDir().value()));
 }
 
 }  // namespace web_app
diff --git a/chrome/browser/web_applications/os_integration/web_app_protocol_handler_manager.cc b/chrome/browser/web_applications/os_integration/web_app_protocol_handler_manager.cc
index 6961a91b65..356e6cf7ae 100644
--- a/chrome/browser/web_applications/os_integration/web_app_protocol_handler_manager.cc
+++ b/chrome/browser/web_applications/os_integration/web_app_protocol_handler_manager.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/web_applications/os_integration/web_app_protocol_handler_manager.h"
 
+#include "base/containers/contains.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/web_applications/os_integration/web_app_protocol_handler_registration.h"
 #include "chrome/browser/web_applications/proto/web_app_install_state.pb.h"
@@ -55,8 +56,8 @@ WebAppProtocolHandlerManager::GetAppProtocolHandlerInfos(
 
   std::vector<apps::ProtocolHandlerInfo> protocol_handlers_infos;
   for (const auto& handler_info : web_app->protocol_handlers()) {
-    if (!web_app->disallowed_launch_protocols().contains(
-            handler_info.protocol)) {
+    if (!base::Contains(web_app->disallowed_launch_protocols(),
+                        handler_info.protocol)) {
       protocol_handlers_infos.push_back(handler_info);
     }
   }
diff --git a/chrome/browser/web_applications/policy/app_service_web_app_policy.cc b/chrome/browser/web_applications/policy/app_service_web_app_policy.cc
index 060ecf6b37..885d576c6e 100644
--- a/chrome/browser/web_applications/policy/app_service_web_app_policy.cc
+++ b/chrome/browser/web_applications/policy/app_service_web_app_policy.cc
@@ -9,6 +9,7 @@
 
 #include "ash/constants/web_app_id_constants.h"
 #include "ash/webui/system_apps/public/system_web_app_type.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "chrome/browser/web_applications/policy/web_app_policy_manager.h"
 
@@ -78,12 +79,12 @@ std::optional<std::string_view> GetPolicyIdForSystemWebAppType(
 }
 
 bool IsArcAppPolicyId(std::string_view policy_id) {
-  return policy_id.contains('.') &&
+  return base::Contains(policy_id, '.') &&
          !WebAppPolicyManager::IsWebAppPolicyId(policy_id);
 }
 
 bool IsSystemWebAppPolicyId(std::string_view policy_id) {
-  return kSystemWebAppsMapping.contains(policy_id);
+  return base::Contains(kSystemWebAppsMapping, policy_id);
 }
 
 }  // namespace web_app
diff --git a/chrome/browser/web_applications/policy/web_app_policy_manager.cc b/chrome/browser/web_applications/policy/web_app_policy_manager.cc
index 68b8f16857..54966d2ac3 100644
--- a/chrome/browser/web_applications/policy/web_app_policy_manager.cc
+++ b/chrome/browser/web_applications/policy/web_app_policy_manager.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/flat_map.h"
@@ -277,10 +278,10 @@ void WebAppPolicyManager::SetPreinstalledWebAppsMappingForTesting(  // IN-TEST
 bool WebAppPolicyManager::IsPreinstalledWebAppPolicyId(
     std::string_view policy_id) {
   if (auto& mapping = GetPreinstalledWebAppsMappingForTesting()) {  // IN-TEST
-    return mapping->contains(policy_id);
+    return base::Contains(*mapping, policy_id);
   }
 #if BUILDFLAG(IS_CHROMEOS)
-  return kPreinstalledWebAppsMapping.contains(policy_id);
+  return base::Contains(kPreinstalledWebAppsMapping, policy_id);
 #else
   return false;
 #endif  // BUILDFLAG(IS_CHROMEOS)
@@ -387,7 +388,7 @@ void WebAppPolicyManager::OnDisableListPolicyChanged() {
       provider_->registrar_unsafe().GetAppIds();
   WebAppProvider* provider = WebAppProvider::GetForLocalAppsUnchecked(profile_);
   for (const auto& id : app_ids) {
-    const bool is_disabled = disabled_web_apps_.contains(id);
+    const bool is_disabled = base::Contains(disabled_web_apps_, id);
     provider->scheduler().SetAppIsDisabled(id, is_disabled, base::DoNothing());
   }
 #endif  // BUILDFLAG(IS_CHROMEOS)
@@ -409,7 +410,8 @@ WebAppPolicyManager::GetDisabledSystemWebApps() const {
 bool WebAppPolicyManager::IsDisabledAppsModeHidden(
     std::optional<ash::SystemWebAppType> system_app_type) const {
   if (system_app_type.has_value() &&
-      disabled_system_apps_not_hidden_.contains(system_app_type.value())) {
+      base::Contains(disabled_system_apps_not_hidden_,
+                     system_app_type.value())) {
     return false;
   }
   PrefService* const local_state = g_browser_process->local_state();
@@ -422,7 +424,7 @@ bool WebAppPolicyManager::IsDisabledAppsModeHidden(
 
 bool WebAppPolicyManager::IsWebAppInDisabledList(
     const webapps::AppId& app_id) const {
-  return disabled_web_apps_.contains(app_id);
+  return base::Contains(disabled_web_apps_, app_id);
 }
 
 void WebAppPolicyManager::RefreshPolicyInstalledApps(
@@ -782,10 +784,11 @@ void WebAppPolicyManager::MaybeOverrideManifest(
     base::flat_map<webapps::AppId, base::flat_set<GURL>> policy_installed_apps =
         provider_->registrar_unsafe().GetExternallyInstalledApps(
             ExternalInstallSource::kExternalPolicy);
-    if (policy_installed_apps.contains(app_id)) {
+    if (base::Contains(policy_installed_apps, app_id)) {
       DCHECK_GT(policy_installed_apps[app_id].size(), 0UL);
       for (const GURL& policy_install_url : policy_installed_apps[app_id]) {
-        if (custom_manifest_values_by_url_.contains(policy_install_url)) {
+        if (base::Contains(custom_manifest_values_by_url_,
+                           policy_install_url)) {
           OverrideManifest(policy_install_url, manifest);
         }
       }
@@ -802,7 +805,7 @@ void WebAppPolicyManager::MaybeOverrideManifest(
   if (!pre_redirect)
     return;
   GURL install_url = pre_redirect->last_url();
-  if (custom_manifest_values_by_url_.contains(install_url)) {
+  if (base::Contains(custom_manifest_values_by_url_, install_url)) {
     OverrideManifest(install_url, manifest);
   }
 }
diff --git a/chrome/browser/web_applications/preinstalled_web_app_manager.cc b/chrome/browser/web_applications/preinstalled_web_app_manager.cc
index 5dcb71b996..459a859e14 100644
--- a/chrome/browser/web_applications/preinstalled_web_app_manager.cc
+++ b/chrome/browser/web_applications/preinstalled_web_app_manager.cc
@@ -15,6 +15,7 @@
 #include <vector>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
@@ -255,7 +256,7 @@ SynchronizeDecision GetSynchronizeDecision(
 
   // Remove if not applicable to current user type.
   DCHECK_GT(options.user_type_allowlist.size(), 0u);
-  if (!std::ranges::contains(options.user_type_allowlist, user_type)) {
+  if (!base::Contains(options.user_type_allowlist, user_type)) {
     return {.type = SynchronizeDecision::kUninstall,
             .reason = DisabledReason::kUninstallUserTypeNotAllowed,
             .log = base::StrCat({options.install_url.spec(),
diff --git a/chrome/browser/web_applications/sampling_metrics_provider.cc b/chrome/browser/web_applications/sampling_metrics_provider.cc
index 1954ecf652..888260f111 100644
--- a/chrome/browser/web_applications/sampling_metrics_provider.cc
+++ b/chrome/browser/web_applications/sampling_metrics_provider.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/web_applications/sampling_metrics_provider.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "chrome/browser/apps/app_service/app_service_proxy.h"
 #include "chrome/browser/apps/app_service/app_service_proxy_factory.h"
@@ -102,7 +103,7 @@ void MaybeEmitUkmMetricsForTab(tabs::TabInterface* tab, IdSet& emitted_ids) {
   }
 
   // We only emit UKM metrics a single time for a given AppId.
-  if (emitted_ids.contains(*app_id)) {
+  if (base::Contains(emitted_ids, *app_id)) {
     return;
   }
 
@@ -143,7 +144,7 @@ void MaybeEmitUkmMetricsForPromotable(
     const webapps::WebAppBannerData& banner_data,
     IdSet& emitted_ids) {
   const GURL& url = banner_data.manifest().start_url;
-  if (emitted_ids.contains(url.spec())) {
+  if (base::Contains(emitted_ids, url.spec())) {
     return;
   }
 
diff --git a/chrome/browser/web_applications/test/fake_environment.cc b/chrome/browser/web_applications/test/fake_environment.cc
index 41804e5c3f..7bcc0d5692 100644
--- a/chrome/browser/web_applications/test/fake_environment.cc
+++ b/chrome/browser/web_applications/test/fake_environment.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/web_applications/test/fake_environment.h"
 
+#include "base/containers/contains.h"
 #include "testing/gtest/include/gtest/gtest.h"
 
 namespace web_app {
@@ -19,7 +20,7 @@ void FakeEnvironment::Set(base::cstring_view name, const std::string& value) {
 std::optional<std::string> FakeEnvironment::GetVar(
     base::cstring_view variable_name) {
   const std::string key(variable_name);
-  if (!variables_.contains(key)) {
+  if (!base::Contains(variables_, key)) {
     return std::nullopt;
   }
   return variables_[key];
diff --git a/chrome/browser/web_applications/test/fake_os_integration_manager.cc b/chrome/browser/web_applications/test/fake_os_integration_manager.cc
index b7d368a5c9..89480db3b6 100644
--- a/chrome/browser/web_applications/test/fake_os_integration_manager.cc
+++ b/chrome/browser/web_applications/test/fake_os_integration_manager.cc
@@ -6,6 +6,7 @@
 
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/task/sequenced_task_runner.h"
 #include "base/test/bind.h"
 #include "chrome/browser/web_applications/os_integration/os_integration_manager.h"
diff --git a/chrome/browser/web_applications/test/fake_web_app_ui_manager.cc b/chrome/browser/web_applications/test/fake_web_app_ui_manager.cc
index fca0bb4164..ae0507983b 100644
--- a/chrome/browser/web_applications/test/fake_web_app_ui_manager.cc
+++ b/chrome/browser/web_applications/test/fake_web_app_ui_manager.cc
@@ -7,6 +7,7 @@
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
diff --git a/chrome/browser/web_applications/test/os_integration_test_override_impl.cc b/chrome/browser/web_applications/test/os_integration_test_override_impl.cc
index b025b3f101..08167734c9 100644
--- a/chrome/browser/web_applications/test/os_integration_test_override_impl.cc
+++ b/chrome/browser/web_applications/test/os_integration_test_override_impl.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/browser/web_applications/test/os_integration_test_override_impl.h"
 
-#include <algorithm>
 #include <map>
 #include <memory>
 #include <optional>
@@ -15,6 +14,7 @@
 
 #include "base/base_paths.h"
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
@@ -69,6 +69,7 @@
 #include <shellapi.h>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/strings/strcat.h"
@@ -135,7 +136,7 @@ std::optional<SkBitmap> IconManagerReadIconForSize(
       app_id, {size_px}, IconPurpose::ANY,
       base::BindLambdaForTesting([&](IconMetadataFromDisk icon_metadata) {
         SizeToBitmap icon_bitmaps = std::move(icon_metadata.icons_map);
-        CHECK(icon_bitmaps.contains(size_px));
+        CHECK(base::Contains(icon_bitmaps, size_px));
         result = icon_bitmaps[size_px];
         run_loop.Quit();
       }));
@@ -400,7 +401,7 @@ bool OsIntegrationTestOverrideImpl::IsFileExtensionHandled(
   for (const auto& file_handler_prog_id : file_handler_prog_ids) {
     const std::vector<std::wstring> supported_file_extensions =
         GetFileExtensionsForProgId(file_handler_prog_id);
-    if (std::ranges::contains(supported_file_extensions, extension)) {
+    if (base::Contains(supported_file_extensions, extension)) {
       const std::wstring reg_key = std::wstring(ShellUtil::kRegClasses) +
                                    base::FilePath::kSeparators[0] + extension +
                                    base::FilePath::kSeparators[0] +
@@ -425,11 +426,12 @@ bool OsIntegrationTestOverrideImpl::IsFileExtensionHandled(
   base::FilePath user_applications_dir = applications();
   bool database_update_called = false;
   for (const LinuxFileRegistration& command : linux_file_registration_) {
-    if (command.xdg_command.contains(app_id) &&
-        command.xdg_command.contains(profile->GetPath().BaseName().value())) {
+    if (base::Contains(command.xdg_command, app_id) &&
+        base::Contains(command.xdg_command,
+                       profile->GetPath().BaseName().value())) {
       if (base::StartsWith(command.xdg_command, "xdg-mime install")) {
-        is_file_handled =
-            command.file_contents.contains("\"*" + file_extension + "\"");
+        is_file_handled = base::Contains(command.file_contents,
+                                         "\"*" + file_extension + "\"");
       } else {
         CHECK(base::StartsWith(command.xdg_command, "xdg-mime uninstall"))
             << command.xdg_command;
@@ -441,7 +443,7 @@ bool OsIntegrationTestOverrideImpl::IsFileExtensionHandled(
     // web_app_file_handler_registration_linux.cc for more information.
     if (base::StartsWith(command.xdg_command, "update-desktop-database")) {
       database_update_called =
-          command.xdg_command.contains(user_applications_dir.value());
+          base::Contains(command.xdg_command, user_applications_dir.value());
     }
   }
   is_file_handled = is_file_handled && database_update_called;
diff --git a/chrome/browser/web_applications/test/test_web_app_url_loader.cc b/chrome/browser/web_applications/test/test_web_app_url_loader.cc
index f3a7273673..3dcde7b290 100644
--- a/chrome/browser/web_applications/test/test_web_app_url_loader.cc
+++ b/chrome/browser/web_applications/test/test_web_app_url_loader.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/web_applications/test/test_web_app_url_loader.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/task/sequenced_task_runner.h"
 
@@ -22,7 +23,7 @@ void TestWebAppUrlLoader::ProcessLoadUrlRequests() {
     auto [url, callback] = std::move(pending_requests_.front());
     pending_requests_.pop();
 
-    DCHECK(next_result_map_.contains(url));
+    DCHECK(base::Contains(next_result_map_, url));
 
     const UrlResponses& url_responses = next_result_map_[url];
     DCHECK_EQ(1u, url_responses.results.size());
@@ -41,7 +42,7 @@ void TestWebAppUrlLoader::SetNextLoadUrlResult(const GURL& url, Result result) {
 void TestWebAppUrlLoader::AddNextLoadUrlResults(
     const GURL& url,
     const std::vector<Result>& results) {
-  DCHECK(!next_result_map_.contains(url)) << url;
+  DCHECK(!base::Contains(next_result_map_, url)) << url;
   UrlResponses& responses = next_result_map_[url];
 
   for (Result result : results)
@@ -61,7 +62,7 @@ void TestWebAppUrlLoader::LoadUrl(
     return;
   }
 
-  DCHECK(next_result_map_.contains(url)) << url;
+  DCHECK(base::Contains(next_result_map_, url)) << url;
   UrlResponses& responses = next_result_map_[url];
   DCHECK(!responses.results.empty());
 
diff --git a/chrome/browser/web_applications/test/web_app_icon_test_utils.cc b/chrome/browser/web_applications/test/web_app_icon_test_utils.cc
index 2a7fa0d874..99a8b5e1d9 100644
--- a/chrome/browser/web_applications/test/web_app_icon_test_utils.cc
+++ b/chrome/browser/web_applications/test/web_app_icon_test_utils.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
@@ -183,7 +184,7 @@ std::map<SquareSizePx, SkBitmap> ReadPngsFromDirectory(
     EXPECT_EQ(bitmap.width(), bitmap.height());
 
     const int size_px = bitmap.width();
-    EXPECT_FALSE(pngs.contains(size_px));
+    EXPECT_FALSE(base::Contains(pngs, size_px));
 
     base::FilePath size_file_name;
     size_file_name =
@@ -287,7 +288,7 @@ SkColor IconManagerReadAppIconPixel(WebAppIconManager& icon_manager,
   icon_manager.ReadTrustedIconsWithFallbackToManifestIcons(
       app_id, {size_px}, IconPurpose::ANY,
       base::BindLambdaForTesting([&](IconMetadataFromDisk icon_metadata) {
-        DCHECK(icon_metadata.icons_map.contains(size_px));
+        DCHECK(base::Contains(icon_metadata.icons_map, size_px));
         result = icon_metadata.icons_map.at(size_px).getColor(x, y);
         run_loop.Quit();
       }));
diff --git a/chrome/browser/web_applications/user_uninstalled_preinstalled_web_app_prefs.cc b/chrome/browser/web_applications/user_uninstalled_preinstalled_web_app_prefs.cc
index fbe4c86f0f..5d451ab484 100644
--- a/chrome/browser/web_applications/user_uninstalled_preinstalled_web_app_prefs.cc
+++ b/chrome/browser/web_applications/user_uninstalled_preinstalled_web_app_prefs.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/metrics/user_metrics.h"
 #include "base/values.h"
 #include "build/build_config.h"
@@ -195,7 +196,7 @@ bool UserUninstalledPreinstalledWebAppPrefs::AppIdContainsAllUrls(
       continue;
 
     for (const GURL& url_to_insert : it.second.install_urls) {
-      if (!existing_urls.contains(url_to_insert.spec())) {
+      if (!base::Contains(existing_urls, url_to_insert.spec())) {
         return false;
       }
     }
diff --git a/chrome/browser/web_applications/visited_manifest_manager.cc b/chrome/browser/web_applications/visited_manifest_manager.cc
index fc6cd29d40..2efd143f43 100644
--- a/chrome/browser/web_applications/visited_manifest_manager.cc
+++ b/chrome/browser/web_applications/visited_manifest_manager.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/strings/string_util.h"
 #include "third_party/blink/public/common/manifest/manifest_util.h"
diff --git a/chrome/browser/web_applications/web_app_command_manager.cc b/chrome/browser/web_applications/web_app_command_manager.cc
index e5dd65201e..c037156c1c 100644
--- a/chrome/browser/web_applications/web_app_command_manager.cc
+++ b/chrome/browser/web_applications/web_app_command_manager.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -79,7 +80,7 @@ void WebAppCommandManager::ScheduleCommand(
   command->SetScheduledAt(base::PassKey<WebAppCommandManager>());
   command->SetCommandManager(base::PassKey<WebAppCommandManager>(), this);
   internal::CommandBase::Id command_id = command->id();
-  CHECK(!commands_.contains(command_id));
+  CHECK(!base::Contains(commands_, command_id));
 
   if (!started_) {
     commands_waiting_for_start_.emplace_back(std::move(command), location);
diff --git a/chrome/browser/web_applications/web_app_database_serialization.cc b/chrome/browser/web_applications/web_app_database_serialization.cc
index 316d5c8b8a..2db8ae805f 100644
--- a/chrome/browser/web_applications/web_app_database_serialization.cc
+++ b/chrome/browser/web_applications/web_app_database_serialization.cc
@@ -17,6 +17,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
 #include "base/logging.h"
@@ -800,7 +801,7 @@ std::unique_ptr<WebApp> ParseWebAppProto(
       apps::FileHandler::AcceptEntry accept_entry;
       accept_entry.mime_type = accept_entry_proto.mimetype();
       for (const auto& file_extension : accept_entry_proto.file_extensions()) {
-        if (accept_entry.file_extensions.contains(file_extension)) {
+        if (base::Contains(accept_entry.file_extensions, file_extension)) {
           // We intentionally don't return a nullptr here; instead, duplicate
           // entries are absorbed.
           DLOG(ERROR) << "apps::FileHandler::AcceptEntry parsing encountered "
@@ -870,7 +871,7 @@ std::unique_ptr<WebApp> ParseWebAppProto(
       apps::ShareTarget::Files files_entry;
       files_entry.name = share_target_params_file.name();
       for (const auto& file_type : share_target_params_file.accept()) {
-        if (std::ranges::contains(files_entry.accept, file_type)) {
+        if (base::Contains(files_entry.accept, file_type)) {
           // We intentionally don't return a nullptr here; instead, duplicate
           // entries are absorbed.
           DLOG(ERROR) << "apps::ShareTarget::Files parsing encountered "
diff --git a/chrome/browser/web_applications/web_app_install_utils.cc b/chrome/browser/web_applications/web_app_install_utils.cc
index 65c579d737..90649535bf 100644
--- a/chrome/browser/web_applications/web_app_install_utils.cc
+++ b/chrome/browser/web_applications/web_app_install_utils.cc
@@ -17,6 +17,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
@@ -350,7 +351,7 @@ void PopulateTrustedIconsFromDownloadedBitmapsAndMetadata(
   SizeToBitmap sizes_to_icons = ConstrainBitmapsToSizes(
       square_icons_matching_infos, web_app::SizesToGenerate());
   for (auto& [size, icon] : sizes_to_icons) {
-    if (!output_size_to_bitmaps.contains(size)) {
+    if (!base::Contains(output_size_to_bitmaps, size)) {
       output_size_to_bitmaps[size] = std::move(icon);
     }
   }
@@ -390,7 +391,7 @@ void PopulateFileHandlerInfoFromManifest(
           icon_info.url = image_resource.src;
           icon_info.purpose =
               ManifestPurposeToIconInfoPurpose(manifest_purpose);
-          if (std::ranges::contains(image_resource.sizes, gfx::Size()) &&
+          if (base::Contains(image_resource.sizes, gfx::Size()) &&
               image_resource.src.spec().find(".svg") != std::string::npos) {
             web_app_info->icons_with_size_any
                 .file_handling_icons[manifest_purpose] = image_resource.src;
@@ -468,13 +469,13 @@ void PopulateProductIcons(WebAppInstallInfo* web_app_info,
 
   // Retain any bitmaps provided as input to the installation.
   for (auto& icon : square_icons_maskable) {
-    if (!web_app_info->icon_bitmaps.maskable.contains(icon.width())) {
+    if (!base::Contains(web_app_info->icon_bitmaps.maskable, icon.width())) {
       web_app_info->icon_bitmaps.maskable[icon.width()] = std::move(icon);
     }
   }
 
   for (auto& icon : square_icons_monochrome) {
-    if (!web_app_info->icon_bitmaps.monochrome.contains(icon.width())) {
+    if (!base::Contains(web_app_info->icon_bitmaps.monochrome, icon.width())) {
       web_app_info->icon_bitmaps.monochrome[icon.width()] = std::move(icon);
     }
   }
diff --git a/chrome/browser/web_applications/web_app_link_capturing_parameterized_browsertest.cc b/chrome/browser/web_applications/web_app_link_capturing_parameterized_browsertest.cc
index abb7c8a62f..976744bce1 100644
--- a/chrome/browser/web_applications/web_app_link_capturing_parameterized_browsertest.cc
+++ b/chrome/browser/web_applications/web_app_link_capturing_parameterized_browsertest.cc
@@ -7,10 +7,10 @@
 #include <ostream>
 #include <string>
 #include <string_view>
-#include <utility>
 
 #include "base/base_paths.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -35,6 +35,7 @@
 #include "base/test/test_future.h"
 #include "base/threading/scoped_blocking_call.h"
 #include "base/threading/thread_restrictions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected.h"
 #include "build/build_config.h"
 #include "chrome/app/chrome_command_ids.h"
@@ -526,8 +527,8 @@ bool DoesTestMatchFileConfig(std::string_view full_test_params,
       TupleItemToParamString<LinkCapturing>(file_config);
   std::string display_mode_name =
       TupleItemToParamString<AppUserDisplayMode>(file_config);
-  return full_test_params.contains(link_capturing_name) &&
-         full_test_params.contains(display_mode_name);
+  return base::Contains(full_test_params, link_capturing_name) &&
+         base::Contains(full_test_params, display_mode_name);
 }
 
 // Removes all of the parameters from the `full_test_params` that are handled by
@@ -924,7 +925,7 @@ class NavCaptureParameterizedBrowserTest
     // redirection happening on the way from a source to a destination url.
     // Prevent multiple redirections from being triggered which causes a Chrome
     // error page to show up, cancelling the navigation.
-    if (request.GetURL().query().contains("redirect")) {
+    if (base::Contains(request.GetURL().query(), "redirect")) {
       return nullptr;
     }
 
@@ -937,7 +938,7 @@ class NavCaptureParameterizedBrowserTest
     const GURL& final_request_url =
         request.GetURL().ReplaceComponents(request_replacements);
 
-    if (!final_request_url.spec().contains("/destination.html")) {
+    if (!base::Contains(final_request_url.spec(), "/destination.html")) {
       return nullptr;  // Only redirect for destination pages.
     }
 
@@ -1519,7 +1520,8 @@ class NavCaptureParameterizedBrowserTest
     for (int i = 0; i < unit_test->total_test_suite_count(); ++i) {
       const testing::TestSuite* test_suite = unit_test->GetTestSuite(i);
       // We only care about link capturing parameterized tests.
-      if (!(std::string(test_suite->name())).contains(GetTestClassName())) {
+      if (!base::Contains(std::string(test_suite->name()),
+                          GetTestClassName())) {
         continue;
       }
       for (int j = 0; j < test_suite->total_test_count(); ++j) {
@@ -1586,11 +1588,11 @@ class NavCaptureParameterizedBrowserTest
   }
 
   void PerformTestCleanupForAllFilesIfNeeded() {
-    for (auto display_mode = std::to_underlying(AppUserDisplayMode::kMinValue);
-         display_mode <= std::to_underlying(AppUserDisplayMode::kMaxValue);
+    for (auto display_mode = base::to_underlying(AppUserDisplayMode::kMinValue);
+         display_mode <= base::to_underlying(AppUserDisplayMode::kMaxValue);
          ++display_mode) {
-      for (auto link_capturing = std::to_underlying(LinkCapturing::kMinValue);
-           link_capturing <= std::to_underlying(LinkCapturing::kMaxValue);
+      for (auto link_capturing = base::to_underlying(LinkCapturing::kMinValue);
+           link_capturing <= base::to_underlying(LinkCapturing::kMaxValue);
            ++link_capturing) {
         PerformTestCleanupIfNeeded(
             {static_cast<AppUserDisplayMode>(display_mode),
@@ -1830,8 +1832,8 @@ class NavCaptureParameterizedBrowserTest
     }
 
     // Skip tests that are disabled because they are flaky.
-    if (disabled_flaky_tests.contains(TupleToParamString(param.param)) ||
-        disabled_flaky_tests.contains("*")) {
+    if (base::Contains(disabled_flaky_tests, TupleToParamString(param.param)) ||
+        base::Contains(disabled_flaky_tests, "*")) {
       return true;
     }
 
@@ -1889,9 +1891,10 @@ class NavCaptureParameterizedBrowserTest
     // Cleanup tests will call this with every combination of file
     // configuration, and if no tests exist then the load will fail.
     if (!ShouldRebaseline() &&
-        !(std::string(
-              ::testing::UnitTest::GetInstance()->current_test_info()->name()))
-             .contains("Cleanup")) {
+        !base::Contains(std::string(::testing::UnitTest::GetInstance()
+                                        ->current_test_info()
+                                        ->name()),
+                        "Cleanup")) {
       ASSERT_TRUE(file_read_success_)
           << "Failed to read test baselines from "
           << GetExpectationsFile(file_config).value()
diff --git a/chrome/browser/web_applications/web_app_origin_association_manager_browsertest.cc b/chrome/browser/web_applications/web_app_origin_association_manager_browsertest.cc
index b7961e4a45..70ec6776e9 100644
--- a/chrome/browser/web_applications/web_app_origin_association_manager_browsertest.cc
+++ b/chrome/browser/web_applications/web_app_origin_association_manager_browsertest.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/run_loop.h"
 #include "base/test/bind.h"
 #include "base/test/test_future.h"
@@ -108,10 +109,11 @@ class WebAppOriginAssociationManagerTest : public WebAppBrowserTestBase {
             /*has_origin_wildcard*/ valid_and_invalid_app_scope_extension_
                 ->has_origin_wildcard);
 
+    EXPECT_TRUE(base::Contains(result.scope_extensions,
+                               std::move(valid_app_scope_extension)));
     EXPECT_TRUE(
-        result.scope_extensions.contains(std::move(valid_app_scope_extension)));
-    EXPECT_TRUE(result.scope_extensions.contains(
-        std::move(valid_and_invalid_app_scope_extension)));
+        base::Contains(result.scope_extensions,
+                       std::move(valid_and_invalid_app_scope_extension)));
 
     if (callback_count_ == expected_callback_count) {
       callback_count_ = 0;
diff --git a/chrome/browser/web_applications/web_app_registrar.cc b/chrome/browser/web_applications/web_app_registrar.cc
index 0ed98aaa6c..98484c7af9 100644
--- a/chrome/browser/web_applications/web_app_registrar.cc
+++ b/chrome/browser/web_applications/web_app_registrar.cc
@@ -16,6 +16,7 @@
 #include "ash/constants/web_app_id_constants.h"
 #include "base/check_deref.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/containers/extend.h"
 #include "base/containers/flat_map.h"
@@ -320,7 +321,7 @@ std::optional<webapps::AppId> WebAppRegistrar::LookupPlaceholderAppId(
       continue;
     }
 
-    if (it->second.install_urls.contains(install_url) &&
+    if (base::Contains(it->second.install_urls, install_url) &&
         it->second.is_placeholder) {
       return web_app.app_id();
     }
@@ -531,7 +532,8 @@ bool WebAppRegistrar::HasExternalAppWithInstallSource(
   }
 
   const WebApp* web_app = GetAppById(app_id);
-  return web_app && web_app->management_to_external_config_map().contains(
+  return web_app &&
+         base::Contains(web_app->management_to_external_config_map(),
                         ConvertExternalInstallSourceToSource(install_source));
 }
 
@@ -939,7 +941,7 @@ std::optional<webapps::AppId> WebAppRegistrar::LookUpAppIdByInstallUrl(
     const GURL& install_url) const {
   for (const WebApp& web_app : GetApps()) {
     for (const auto& it : web_app.management_to_external_config_map()) {
-      if (it.second.install_urls.contains(install_url)) {
+      if (base::Contains(it.second.install_urls, install_url)) {
         return web_app.app_id();
       }
     }
@@ -955,7 +957,7 @@ const WebApp* WebAppRegistrar::LookUpAppByInstallSourceInstallUrl(
         app.management_to_external_config_map();
     auto it = config_map.find(install_source);
     if (it != config_map.end()) {
-      if (it->second.install_urls.contains(install_url)) {
+      if (base::Contains(it->second.install_urls, install_url)) {
         return &app;
       }
     }
@@ -1295,15 +1297,15 @@ bool WebAppRegistrar::IsAllowedLaunchProtocol(
     const std::string& protocol_scheme) const {
   const WebApp* web_app = GetAppById(app_id);
   return web_app &&
-         web_app->allowed_launch_protocols().contains(protocol_scheme);
+         base::Contains(web_app->allowed_launch_protocols(), protocol_scheme);
 }
 
 bool WebAppRegistrar::IsDisallowedLaunchProtocol(
     const webapps::AppId& app_id,
     const std::string& protocol_scheme) const {
   const WebApp* web_app = GetAppById(app_id);
-  return web_app &&
-         web_app->disallowed_launch_protocols().contains(protocol_scheme);
+  return web_app && base::Contains(web_app->disallowed_launch_protocols(),
+                                   protocol_scheme);
 }
 
 bool WebAppRegistrar::IsRegisteredLaunchProtocol(
@@ -1314,7 +1316,7 @@ bool WebAppRegistrar::IsRegisteredLaunchProtocol(
     return false;
   }
 
-  return std::ranges::contains(web_app->protocol_handlers(), protocol_scheme,
+  return base::Contains(web_app->protocol_handlers(), protocol_scheme,
                                [](const auto& info) { return info.protocol; });
 }
 
@@ -1810,7 +1812,7 @@ bool WebAppRegistrar::IsAppPolicyDefinedHandlerForFileExtension(
       WebAppPolicyManager::GetPolicyIds(profile(), *web_app);
 
   if (!app_policy_ids->empty()) {
-    return std::ranges::contains(app_policy_ids.value(),
+    return base::Contains(app_policy_ids.value(),
                                  *file_extension_policy_id);
   }
 #endif  // BUILDFLAG(IS_CHROMEOS)
@@ -1833,7 +1835,7 @@ bool WebAppRegistrar::IsAppSetAsPolicyDefinedFileHandlerForAnyFileExtension(
 
   if (!app_policy_ids->empty()) {
     return std::ranges::any_of(default_handlers, [&](const auto& handler) {
-      return std::ranges::contains(*app_policy_ids, handler.second.GetString());
+      return base::Contains(*app_policy_ids, handler.second.GetString());
     });
   }
 #endif  // BUILDFLAG(IS_CHROMEOS)
diff --git a/chrome/browser/web_applications/web_app_registry_update.cc b/chrome/browser/web_applications/web_app_registry_update.cc
index 406f7d76df..9ad9fe6850 100644
--- a/chrome/browser/web_applications/web_app_registry_update.cc
+++ b/chrome/browser/web_applications/web_app_registry_update.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
 #include "base/types/pass_key.h"
@@ -36,7 +37,7 @@ void WebAppRegistryUpdate::CreateApp(std::unique_ptr<WebApp> web_app) {
   CHECK(web_app->manifest_id().is_valid());
   DCHECK(!web_app->app_id().empty());
   DCHECK(!registrar_->GetAppById(web_app->app_id()));
-  DCHECK(!std::ranges::contains(update_data_->apps_to_create, web_app));
+  DCHECK(!base::Contains(update_data_->apps_to_create, web_app));
 
   update_data_->apps_to_create.push_back(std::move(web_app));
 }
@@ -45,7 +46,7 @@ void WebAppRegistryUpdate::DeleteApp(const webapps::AppId& app_id) {
   DCHECK(update_data_);
   DCHECK(!app_id.empty());
   DCHECK(registrar_->GetAppById(app_id));
-  DCHECK(!std::ranges::contains(update_data_->apps_to_delete, app_id));
+  DCHECK(!base::Contains(update_data_->apps_to_delete, app_id));
 
   update_data_->apps_to_delete.push_back(app_id);
 }
diff --git a/chrome/browser/web_applications/web_app_utils.cc b/chrome/browser/web_applications/web_app_utils.cc
index 97a078af51..5d32cff174 100644
--- a/chrome/browser/web_applications/web_app_utils.cc
+++ b/chrome/browser/web_applications/web_app_utils.cc
@@ -19,6 +19,7 @@
 #include "base/base64.h"
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/containers/extend.h"
 #include "base/containers/map_util.h"
@@ -387,12 +388,12 @@ bool AreNewFileHandlersASubsetOfOld(const apps::FileHandlers& old_handlers,
 
   for (const apps::FileHandler& new_handler : new_handlers) {
     for (const auto& new_handler_accept : new_handler.accept) {
-      if (!mime_types_set.contains(new_handler_accept.mime_type)) {
+      if (!base::Contains(mime_types_set, new_handler_accept.mime_type)) {
         return false;
       }
 
       for (const auto& new_extension : new_handler_accept.file_extensions) {
-        if (!extensions_set.contains(new_extension)) {
+        if (!base::Contains(extensions_set, new_extension)) {
           return false;
         }
       }
diff --git a/chrome/browser/web_applications/web_install_service_impl.cc b/chrome/browser/web_applications/web_install_service_impl.cc
index 3ef74d6cf1..93bf08d64e 100644
--- a/chrome/browser/web_applications/web_install_service_impl.cc
+++ b/chrome/browser/web_applications/web_install_service_impl.cc
@@ -685,7 +685,7 @@ void WebInstallServiceImpl::OnInstallInfoFromInstallUrlFetched(
   // crbug.com/423906188 for more information. Regardless of OS, we expect an
   // icon of size 32x32 to be available.
   DialogImageInfo dialog_info = install_info->GetIconBitmapsForSecureSurfaces();
-  CHECK(dialog_info.bitmaps.contains(kIconSizeForLaunchDialog));
+  CHECK(base::Contains(dialog_info.bitmaps, kIconSizeForLaunchDialog));
   SkBitmap icon_bitmap_to_use = dialog_info.bitmaps[kIconSizeForLaunchDialog];
 
   // Name to display in the dialog.
diff --git a/chrome/browser/webauthn/authenticator_request_dialog_controller.cc b/chrome/browser/webauthn/authenticator_request_dialog_controller.cc
index d93fcff6bb..afddb0fa1a 100644
--- a/chrome/browser/webauthn/authenticator_request_dialog_controller.cc
+++ b/chrome/browser/webauthn/authenticator_request_dialog_controller.cc
@@ -20,6 +20,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -2034,8 +2035,8 @@ void AuthenticatorRequestDialogController::PopulateMechanisms() {
     }
   }
   if (!did_enumerate_local_passkeys &&
-      transport_availability_.available_transports.contains(
-          AuthenticatorTransport::kInternal)) {
+      base::Contains(transport_availability_.available_transports,
+                     AuthenticatorTransport::kInternal)) {
     transports_to_list_if_active.push_back(AuthenticatorTransport::kInternal);
   }
 
@@ -2046,14 +2047,16 @@ void AuthenticatorRequestDialogController::PopulateMechanisms() {
   if (model_->cable_ui_type) {
     switch (*model_->cable_ui_type) {
       case AuthenticatorRequestDialogModel::CableUIType::CABLE_V2_2ND_FACTOR:
-        if (transport_availability_.available_transports.contains(kCable)) {
+        if (base::Contains(transport_availability_.available_transports,
+                           kCable)) {
           include_add_phone_option = !windows_handles_hybrid;
         }
         break;
 
       case AuthenticatorRequestDialogModel::CableUIType::CABLE_V2_SERVER_LINK:
       case AuthenticatorRequestDialogModel::CableUIType::CABLE_V1: {
-        if (transport_availability_.available_transports.contains(kCable)) {
+        if (base::Contains(transport_availability_.available_transports,
+                           kCable)) {
           transports_to_list_if_active.push_back(kCable);
 
           // If this is a caBLEv1 or server-link request then offering to "Try
@@ -2135,8 +2138,8 @@ void AuthenticatorRequestDialogController::PopulateMechanisms() {
   // not available, if tapping it would trigger a prompt to enable BLE, or if
   // hints suggest that hybrid and USB should be separate options.
   const bool include_usb_option =
-      transport_availability_.available_transports.contains(
-          AuthenticatorTransport::kUsbHumanInterfaceDevice) &&
+      base::Contains(transport_availability_.available_transports,
+                     AuthenticatorTransport::kUsbHumanInterfaceDevice) &&
       (!include_add_phone_option ||
        transport_availability_.ble_status != BleStatus::kOn ||
        hints_.transport == AuthenticatorTransport::kUsbHumanInterfaceDevice ||
@@ -2144,8 +2147,8 @@ void AuthenticatorRequestDialogController::PopulateMechanisms() {
 
   if (include_add_phone_option) {
     model_->show_security_key_on_qr_sheet =
-        transport_availability_.available_transports.contains(
-            AuthenticatorTransport::kUsbHumanInterfaceDevice) &&
+        base::Contains(transport_availability_.available_transports,
+                       AuthenticatorTransport::kUsbHumanInterfaceDevice) &&
         !include_usb_option;
     std::u16string label = l10n_util::GetStringUTF16(
         GetHybridButtonLabel(model_->show_security_key_on_qr_sheet));
@@ -2163,7 +2166,8 @@ void AuthenticatorRequestDialogController::PopulateMechanisms() {
   }
 
   for (const auto transport : transports_to_list_if_active) {
-    if (!transport_availability_.available_transports.contains(transport)) {
+    if (!base::Contains(transport_availability_.available_transports,
+                        transport)) {
       continue;
     }
 
diff --git a/chrome/browser/webauthn/cablev2_devices.cc b/chrome/browser/webauthn/cablev2_devices.cc
index dfba1ca94a..e45e51e17e 100644
--- a/chrome/browser/webauthn/cablev2_devices.cc
+++ b/chrome/browser/webauthn/cablev2_devices.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/i18n/time_formatting.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
diff --git a/chrome/browser/webauthn/chrome_authenticator_request_delegate.cc b/chrome/browser/webauthn/chrome_authenticator_request_delegate.cc
index 9052d3deb7..2f50e3d976 100644
--- a/chrome/browser/webauthn/chrome_authenticator_request_delegate.cc
+++ b/chrome/browser/webauthn/chrome_authenticator_request_delegate.cc
@@ -17,6 +17,7 @@
 #include "base/check.h"
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -542,9 +543,9 @@ void ChromeAuthenticatorRequestDelegate::ConfigureDiscoveries(
 
 #if BUILDFLAG(IS_LINUX)
   // No caBLEv1 on Linux. It tends to crash bluez.
-  if (std::ranges::contains(pairings_from_extension,
-                            device::CableDiscoveryData::Version::V1,
-                            &device::CableDiscoveryData::version)) {
+  if (base::Contains(pairings_from_extension,
+                     device::CableDiscoveryData::Version::V1,
+                     &device::CableDiscoveryData::version)) {
     pairings_from_extension = base::span<const device::CableDiscoveryData>();
   }
 #endif
@@ -556,8 +557,8 @@ void ChromeAuthenticatorRequestDelegate::ConfigureDiscoveries(
   }
   const bool cable_extension_accepted = !pairings.empty();
   const bool cablev2_extension_provided =
-      std::ranges::contains(pairings, device::CableDiscoveryData::Version::V2,
-                            &device::CableDiscoveryData::version);
+      base::Contains(pairings, device::CableDiscoveryData::Version::V2,
+                     &device::CableDiscoveryData::version);
 
   const bool non_extension_cablev2_enabled =
       (!cable_extension_permitted ||
@@ -1279,8 +1280,8 @@ void ChromeAuthenticatorRequestDelegate::UpdateModelForTransportAvailability(
   dialog_model_->ble_adapter_is_powered =
       tai.ble_status == device::FidoRequestHandlerBase::BleStatus::kOn;
   dialog_model_->show_security_key_on_qr_sheet =
-      tai.available_transports.contains(
-          device::FidoTransportProtocol::kUsbHumanInterfaceDevice);
+      base::Contains(tai.available_transports,
+                     device::FidoTransportProtocol::kUsbHumanInterfaceDevice);
   dialog_model_->is_off_the_record = GetBrowserContext()->IsOffTheRecord();
   dialog_model_->platform_has_biometrics = tai.platform_has_biometrics;
 }
diff --git a/chrome/browser/webauthn/chrome_web_authentication_delegate.cc b/chrome/browser/webauthn/chrome_web_authentication_delegate.cc
index 5cd26596de..c47319ff19 100644
--- a/chrome/browser/webauthn/chrome_web_authentication_delegate.cc
+++ b/chrome/browser/webauthn/chrome_web_authentication_delegate.cc
@@ -15,6 +15,7 @@
 
 #include "base/base64.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -108,7 +109,7 @@ bool IsWebAuthnRPIDListedInSecurityKeyPermitAttestationPolicy(
   const PrefService* prefs = profile->GetPrefs();
   const base::ListValue& permit_attestation =
       prefs->GetList(prefs::kSecurityKeyPermitAttestation);
-  return permit_attestation.contains(relying_party_id);
+  return base::Contains(permit_attestation, relying_party_id);
 }
 
 bool IsOriginListedInEnterpriseAttestationSwitch(
@@ -199,10 +200,9 @@ void DeleteUnacceptedPasskeys(
            webauthn::PasskeyModel::ShadowedCredentials::kExclude)) {
     if (std::vector<uint8_t>(passkey.user_id().begin(),
                              passkey.user_id().end()) == user_id &&
-        !std::ranges::contains(
-            all_accepted_credentials_ids,
-            std::vector<uint8_t>(passkey.credential_id().begin(),
-                                 passkey.credential_id().end()))) {
+        !base::Contains(all_accepted_credentials_ids,
+                        std::vector<uint8_t>(passkey.credential_id().begin(),
+                                             passkey.credential_id().end()))) {
       passkey_store->DeletePasskey(passkey.credential_id(), FROM_HERE);
       is_passkey_deleted = true;
     }
@@ -256,10 +256,10 @@ void HideAndRestorePasskeys(
             kNoPasskeyChanged);
     return;
   }
-  bool passkey_in_list = std::ranges::contains(
-      all_accepted_credentials_ids,
-      std::vector<uint8_t>(passkey_it->credential_id().begin(),
-                           passkey_it->credential_id().end()));
+  bool passkey_in_list =
+      base::Contains(all_accepted_credentials_ids,
+                     std::vector<uint8_t>(passkey_it->credential_id().begin(),
+                                          passkey_it->credential_id().end()));
   if ((passkey_in_list && !passkey_it->hidden()) ||
       (!passkey_in_list && passkey_it->hidden())) {
     LogSignalAllAcceptedCredentials(
diff --git a/chrome/common/actor/action_result.cc b/chrome/common/actor/action_result.cc
index 812f67d946..3e3aa90a76 100644
--- a/chrome/common/actor/action_result.cc
+++ b/chrome/common/actor/action_result.cc
@@ -7,6 +7,7 @@
 #include "base/strings/stringprintf.h"
 #include "base/strings/to_string.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 
 namespace actor {
 
diff --git a/chrome/common/controlled_frame/controlled_frame.cc b/chrome/common/controlled_frame/controlled_frame.cc
index ae61cb17d0..77a11d3cce 100644
--- a/chrome/common/controlled_frame/controlled_frame.cc
+++ b/chrome/common/controlled_frame/controlled_frame.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -97,7 +98,7 @@ bool AvailabilityCheck(const std::string& api_full_name,
   return (extension == nullptr) && url.SchemeIs(webapps::kIsolatedAppScheme) &&
          context == extensions::mojom::ContextType::kWebPage &&
          context_data.HasControlledFrameCapability() &&
-         std::ranges::contains(GetControlledFrameFeatureList(), api_full_name);
+         base::Contains(GetControlledFrameFeatureList(), api_full_name);
 #else   // !(BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC) || BUILDFLAG(IS_LINUX) ||
         // BUILDFLAG(IS_CHROMEOS))
   return false;
diff --git a/chrome/common/extensions/webstore_override.cc b/chrome/common/extensions/webstore_override.cc
index 323ee824e0..ea9b549b9d 100644
--- a/chrome/common/extensions/webstore_override.cc
+++ b/chrome/common/extensions/webstore_override.cc
@@ -10,6 +10,7 @@
 #include <string_view>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -82,7 +83,7 @@ bool AreWebstoreFeaturesAvailable(const std::string& api_full_name,
   if (context != extensions::mojom::ContextType::kWebPage) {
     return false;
   }
-  if (!std::ranges::contains(kWebstoreOverrideFeatureList, api_full_name)) {
+  if (!base::Contains(kWebstoreOverrideFeatureList, api_full_name)) {
     return false;
   }
 
diff --git a/chrome/common/notifications/notification_image_retainer.cc b/chrome/common/notifications/notification_image_retainer.cc
index 526c1416ce..6f9ddc6ef8 100644
--- a/chrome/common/notifications/notification_image_retainer.cc
+++ b/chrome/common/notifications/notification_image_retainer.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -56,7 +57,7 @@ std::vector<base::FilePath> GetFilesFromPrevSessions(
   for (base::FilePath current = file_enumerator.Next(); !current.empty();
        current = file_enumerator.Next()) {
     // Exclude any new file created in this session.
-    if (!registered_names.contains(current.BaseName())) {
+    if (!base::Contains(registered_names, current.BaseName())) {
       files.push_back(std::move(current));
     }
   }
diff --git a/chrome/common/printing/print_media_l10n.cc b/chrome/common/printing/print_media_l10n.cc
index 4b9cf0160e..62585b39c9 100644
--- a/chrome/common/printing/print_media_l10n.cc
+++ b/chrome/common/printing/print_media_l10n.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/i18n/number_formatting.h"
 #include "base/i18n/string_compare.h"
diff --git a/chrome/common/printing/printer_capabilities.cc b/chrome/common/printing/printer_capabilities.cc
index 6c79fbdf50..7c4c11535d 100644
--- a/chrome/common/printing/printer_capabilities.cc
+++ b/chrome/common/printing/printer_capabilities.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/strcat.h"
@@ -177,7 +178,7 @@ base::DictValue AssemblePrinterSettings(const std::string& device_name,
 #if BUILDFLAG(IS_CHROMEOS)
   printer_info.Set(
       kCUPSEnterprisePrinter,
-      basic_info.options.contains(kCUPSEnterprisePrinter) &&
+      base::Contains(basic_info.options, kCUPSEnterprisePrinter) &&
           basic_info.options.at(kCUPSEnterprisePrinter) == kValueTrue);
 #endif  // BUILDFLAG(IS_CHROMEOS)
 
diff --git a/chrome/credential_provider/gaiacp/gaia_credential_base.cc b/chrome/credential_provider/gaiacp/gaia_credential_base.cc
index 225b62d2c0..b4aeed9817 100644
--- a/chrome/credential_provider/gaiacp/gaia_credential_base.cc
+++ b/chrome/credential_provider/gaiacp/gaia_credential_base.cc
@@ -2416,7 +2416,7 @@ HRESULT CGaiaCredentialBase::OnUserAuthenticated(BSTR authentication_info,
     const std::wstring email_domain = email.substr(email.find(L"@") + 1);
     const std::vector<std::wstring> allowed_domains = GetEmailDomainsList();
 
-    if (!std::ranges::contains(allowed_domains, email_domain)) {
+    if (!base::Contains(allowed_domains, email_domain)) {
       LOGFN(ERROR) << "Account " << email
                    << " isn't in a domain from allowed domains."
                    << "Allowed Domains: "
@@ -2429,7 +2429,7 @@ HRESULT CGaiaCredentialBase::OnUserAuthenticated(BSTR authentication_info,
 
     std::vector<std::wstring> permitted_accounts = GetPermittedAccounts();
     if (!permitted_accounts.empty() &&
-        !std::ranges::contains(permitted_accounts, email)) {
+        !base::Contains(permitted_accounts, email)) {
       *status_text = AllocErrorString(IDS_EMAIL_MISMATCH_BASE);
       SecurelyClearDictionaryValue(properties);
       return E_FAIL;
diff --git a/chrome/credential_provider/gaiacp/gaia_credential_provider.cc b/chrome/credential_provider/gaiacp/gaia_credential_provider.cc
index 5f255b5315..5b62d99374 100644
--- a/chrome/credential_provider/gaiacp/gaia_credential_provider.cc
+++ b/chrome/credential_provider/gaiacp/gaia_credential_provider.cc
@@ -7,13 +7,13 @@
 #include <credentialprovider.h>
 #include <shlguid.h>
 
-#include <algorithm>
 #include <iomanip>
 #include <map>
 #include <string>
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/json/json_reader.h"
 #include "base/memory/raw_ptr.h"
@@ -180,7 +180,7 @@ bool BackgroundTokenHandleUpdater::IsAuthEnforcedOnAssociatedUsers() {
     const std::wstring& sid = sid_to_association.first;
     // Checks if the login UI was already refreshed due to
     // auth enforcements on this sid.
-    if (reauth_sids_ != nullptr && std::ranges::contains(*reauth_sids_, sid)) {
+    if (reauth_sids_ != nullptr && base::Contains(*reauth_sids_, sid)) {
       continue;
     }
 
diff --git a/chrome/credential_provider/gaiacp/win_http_url_fetcher.cc b/chrome/credential_provider/gaiacp/win_http_url_fetcher.cc
index ae5ea6fd97..aef43ebb26 100644
--- a/chrome/credential_provider/gaiacp/win_http_url_fetcher.cc
+++ b/chrome/credential_provider/gaiacp/win_http_url_fetcher.cc
@@ -10,12 +10,12 @@
 #include <process.h>
 #include <winhttp.h>
 
-#include <algorithm>
 #include <string>
 #include <string_view>
 
 #include "base/base64.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/json/json_reader.h"
 #include "base/json/json_writer.h"
@@ -492,7 +492,7 @@ HRESULT WinHttpUrlFetcher::BuildRequestAndFetchResultFromHttpService(
     std::optional<int> error_code =
         error_detail->FindInt(kHttpErrorCodeKeyNameInResponse);
     if (error_code.has_value() &&
-        !std::ranges::contains(kRetryableHttpErrorCodes, error_code.value())) {
+        !base::Contains(kRetryableHttpErrorCodes, error_code.value())) {
       return E_FAIL;
     }
   }
diff --git a/chrome/renderer/accessibility/ax_tree_distiller.cc b/chrome/renderer/accessibility/ax_tree_distiller.cc
index f5b423a58c..ec6e7968d0 100644
--- a/chrome/renderer/accessibility/ax_tree_distiller.cc
+++ b/chrome/renderer/accessibility/ax_tree_distiller.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/time/time.h"
@@ -110,7 +111,7 @@ void GetContentRootNodes(const ui::AXTree& tree,
 void AddContentNodesToVector(const ui::AXNode* node,
                              std::vector<ui::AXNodeID>* content_node_ids) {
   const auto& role = node->GetRole();
-  if (std::ranges::contains(kContentRoles, role)) {
+  if (base::Contains(kContentRoles, role)) {
     // TODO(crbug.com/40922922): Remove when flag is no longer necessary. Skip
     // these roles if the flag is not enabled.
     if (!features::IsReadAnythingImagesViaAlgorithmEnabled() &&
@@ -133,7 +134,7 @@ void AddContentNodesToVector(const ui::AXNode* node,
     return;
   }
 
-  if (std::ranges::contains(kRolesToSkip, node->GetRole())) {
+  if (base::Contains(kRolesToSkip, node->GetRole())) {
     return;
   }
   for (auto iter = node->UnignoredChildrenBegin();
@@ -256,7 +257,7 @@ void AXTreeDistiller::ProcessScreen2xResult(
                         !content_node_ids_screen2x.empty());
   // Merge the results from the algorithm and from screen2x.
   for (ui::AXNodeID node_id : content_node_ids_screen2x) {
-    if (!std::ranges::contains(content_node_ids_algorithm, node_id)) {
+    if (!base::Contains(content_node_ids_algorithm, node_id)) {
       content_node_ids_algorithm.push_back(node_id);
     }
   }
@@ -269,7 +270,7 @@ void AXTreeDistiller::ProcessScreen2xResult(
   } else {
     bool added = false;
     for (ui::AXNodeID node_id : content_node_ids_algorithm) {
-      if (!std::ranges::contains(content_node_ids_screen2x, node_id)) {
+      if (!base::Contains(content_node_ids_screen2x, node_id)) {
         added = true;
         break;
       }
diff --git a/chrome/renderer/accessibility/read_anything/read_aloud_app_model.cc b/chrome/renderer/accessibility/read_anything/read_aloud_app_model.cc
index e2a1eeebae..1437102de1 100644
--- a/chrome/renderer/accessibility/read_anything/read_aloud_app_model.cc
+++ b/chrome/renderer/accessibility/read_anything/read_aloud_app_model.cc
@@ -638,11 +638,11 @@ int ReadAloudAppModel::GetCurrentTextEndIndex(const ui::AXNodeID& node_id) {
 bool ReadAloudAppModel::NodeBeenOrWillBeSpoken(
     const a11y::ReadAloudCurrentGranularity& current_granularity,
     const ui::AXNodeID& id) const {
-  if (current_granularity.segments.contains(id)) {
+  if (base::Contains(current_granularity.segments, id)) {
     return true;
   }
   for (const auto& granularity : processed_granularities_on_current_page_) {
-    if (granularity.segments.contains(id)) {
+    if (base::Contains(granularity.segments, id)) {
       return true;
     }
   }
@@ -679,7 +679,7 @@ bool ReadAloudAppModel::IsValidAXPosition(
   bool was_previously_spoken =
       NodeBeenOrWillBeSpoken(current_granularity, anchor_node->id());
   bool is_text_node = a11y::IsTextForReadAnything(anchor_node, is_pdf, is_docs);
-  bool contains_node = current_nodes->contains(anchor_node->id());
+  bool contains_node = base::Contains(*current_nodes, anchor_node->id());
   bool is_ignored = a11y::IsIgnored(anchor_node, is_pdf);
 
   return !is_ignored && !was_previously_spoken && is_text_node &&
diff --git a/chrome/renderer/accessibility/read_anything/read_aloud_app_model_browsertest.cc b/chrome/renderer/accessibility/read_anything/read_aloud_app_model_browsertest.cc
index ae0ee820bd..758a9ea82c 100644
--- a/chrome/renderer/accessibility/read_anything/read_aloud_app_model_browsertest.cc
+++ b/chrome/renderer/accessibility/read_anything/read_aloud_app_model_browsertest.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/renderer/accessibility/read_anything/read_aloud_app_model.h"
 
-#include <algorithm>
 #include <memory>
 
 #include "base/memory/raw_ptr.h"
@@ -336,10 +335,10 @@ TEST_F(ReadAnythingReadAloudAppModelTest, EnabledLanguages) {
 
   const std::string enabled_lang = "fr";
   SetLanguageEnabled(enabled_lang, true);
-  EXPECT_TRUE(EnabledLanguages().contains(enabled_lang));
+  EXPECT_TRUE(base::Contains(EnabledLanguages(), enabled_lang));
 
   SetLanguageEnabled(enabled_lang, false);
-  EXPECT_FALSE(EnabledLanguages().contains(enabled_lang));
+  EXPECT_FALSE(base::Contains(EnabledLanguages(), enabled_lang));
 }
 
 TEST_F(ReadAnythingReadAloudAppModelTest, Voices) {
@@ -351,15 +350,15 @@ TEST_F(ReadAnythingReadAloudAppModelTest, Voices) {
   const char* voice2 = "Shang";
   SetVoice(voice1, lang1);
   SetVoice(voice2, lang2);
-  EXPECT_TRUE(Voices().contains(lang1));
-  EXPECT_TRUE(Voices().contains(lang2));
+  EXPECT_TRUE(base::Contains(Voices(), lang1));
+  EXPECT_TRUE(base::Contains(Voices(), lang2));
   EXPECT_STREQ(Voices().FindString(lang1)->c_str(), voice1);
   EXPECT_STREQ(Voices().FindString(lang2)->c_str(), voice2);
 
   const char* voice3 = "Mushu";
   SetVoice(voice3, lang2);
-  EXPECT_TRUE(Voices().contains(lang1));
-  EXPECT_TRUE(Voices().contains(lang2));
+  EXPECT_TRUE(base::Contains(Voices(), lang1));
+  EXPECT_TRUE(base::Contains(Voices(), lang2));
   EXPECT_STREQ(Voices().FindString(lang2)->c_str(), voice3);
 }
 
@@ -621,7 +620,7 @@ TEST_F(ReadAnythingReadAloudAppModelV8SegmentationTest,
   // Expect that GetNextValidPosition fails without inserted the granularity.
   // The first segment was returned correctly.
   EXPECT_EQ(current_granularity.node_ids.size(), 1u);
-  EXPECT_TRUE(std::ranges::contains(current_granularity.node_ids, kId1));
+  EXPECT_TRUE(base::Contains(current_granularity.node_ids, kId1));
 
   ui::AXNodePosition::AXPositionInstance new_position =
       GetNextNodePosition(&current_nodes);
@@ -768,12 +767,12 @@ TEST_F(ReadAnythingReadAloudAppModelV8SegmentationTest,
   a11y::ReadAloudCurrentGranularity first_granularity =
       GetNextNodes(&current_nodes);
   EXPECT_EQ(first_granularity.node_ids.size(), 1u);
-  EXPECT_TRUE(std::ranges::contains(first_granularity.node_ids, kId1));
+  EXPECT_TRUE(base::Contains(first_granularity.node_ids, kId1));
   EXPECT_EQ(first_granularity.text, sentence1);
   a11y::ReadAloudCurrentGranularity next_granularity =
       GetNextNodes(&current_nodes);
   EXPECT_EQ(next_granularity.node_ids.size(), 1u);
-  EXPECT_TRUE(std::ranges::contains(next_granularity.node_ids, kId2));
+  EXPECT_TRUE(base::Contains(next_granularity.node_ids, kId2));
   EXPECT_EQ(next_granularity.text, sentence2);
 
   // If we init without resetting we should just go to the next sentence
@@ -781,7 +780,7 @@ TEST_F(ReadAnythingReadAloudAppModelV8SegmentationTest,
   a11y::ReadAloudCurrentGranularity last_granularity =
       GetNextNodes(&current_nodes);
   EXPECT_EQ(last_granularity.node_ids.size(), 1u);
-  EXPECT_TRUE(std::ranges::contains(last_granularity.node_ids, kId3));
+  EXPECT_TRUE(base::Contains(last_granularity.node_ids, kId3));
   EXPECT_EQ(last_granularity.text, sentence3);
 
   // After reset and then init, we should get the first sentence again.
@@ -789,7 +788,7 @@ TEST_F(ReadAnythingReadAloudAppModelV8SegmentationTest,
   InitAXPositionWithNode(kId1);
   a11y::ReadAloudCurrentGranularity after_reset = GetNextNodes(&current_nodes);
   EXPECT_EQ(after_reset.node_ids.size(), 1u);
-  EXPECT_TRUE(std::ranges::contains(after_reset.node_ids, kId1));
+  EXPECT_TRUE(base::Contains(after_reset.node_ids, kId1));
   EXPECT_EQ(first_granularity.text, sentence1);
 }
 
diff --git a/chrome/renderer/accessibility/read_anything/read_anything_app_controller.cc b/chrome/renderer/accessibility/read_anything/read_anything_app_controller.cc
index 6b5872841e..a9d051d625 100644
--- a/chrome/renderer/accessibility/read_anything/read_anything_app_controller.cc
+++ b/chrome/renderer/accessibility/read_anything/read_anything_app_controller.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/renderer/accessibility/read_anything/read_anything_app_controller.h"
 
-#include <algorithm>
 #include <climits>
 #include <memory>
 #include <optional>
@@ -15,6 +14,7 @@
 
 #include "base/check_deref.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/metrics_hashes.h"
@@ -24,6 +24,7 @@
 #include "base/strings/to_string.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "chrome/common/read_anything/read_anything_util.h"
 #include "chrome/renderer/accessibility/ax_tree_distiller.h"
@@ -383,8 +384,8 @@ template <typename T>
              T::kMaxValue;
            })
 std::optional<T> ToEnum(int value) {
-  if (value >= std::to_underlying(T::kMinValue) &&
-      value <= std::to_underlying(T::kMaxValue)) {
+  if (value >= base::to_underlying(T::kMinValue) &&
+      value <= base::to_underlying(T::kMaxValue)) {
     return static_cast<T>(value);
   }
   return std::nullopt;
@@ -1527,15 +1528,15 @@ void ReadAnythingAppController::OnPinStatusReceived(bool pin_state) {
 }
 
 int ReadAnythingAppController::LetterSpacing() const {
-  return std::to_underlying(model_.letter_spacing());
+  return base::to_underlying(model_.letter_spacing());
 }
 
 int ReadAnythingAppController::LineSpacing() const {
-  return std::to_underlying(model_.line_spacing());
+  return base::to_underlying(model_.line_spacing());
 }
 
 int ReadAnythingAppController::ColorTheme() const {
-  return std::to_underlying(model_.color_theme());
+  return base::to_underlying(model_.color_theme());
 }
 
 double ReadAnythingAppController::SpeechRate() const {
@@ -1564,68 +1565,68 @@ int ReadAnythingAppController::HighlightGranularity() const {
 
 int ReadAnythingAppController::LineFocus() const {
   return IsLineFocusEnabled()
-             ? std::to_underlying(model_.line_focus())
-             : std::to_underlying(read_anything::mojom::LineFocus::kOff);
+             ? base::to_underlying(model_.line_focus())
+             : base::to_underlying(read_anything::mojom::LineFocus::kOff);
 }
 
 int ReadAnythingAppController::StandardLineSpacing() const {
-  return std::to_underlying(read_anything::mojom::LineSpacing::kStandard);
+  return base::to_underlying(read_anything::mojom::LineSpacing::kStandard);
 }
 
 int ReadAnythingAppController::LooseLineSpacing() const {
-  return std::to_underlying(read_anything::mojom::LineSpacing::kLoose);
+  return base::to_underlying(read_anything::mojom::LineSpacing::kLoose);
 }
 
 int ReadAnythingAppController::VeryLooseLineSpacing() const {
-  return std::to_underlying(read_anything::mojom::LineSpacing::kVeryLoose);
+  return base::to_underlying(read_anything::mojom::LineSpacing::kVeryLoose);
 }
 
 int ReadAnythingAppController::StandardLetterSpacing() const {
-  return std::to_underlying(read_anything::mojom::LetterSpacing::kStandard);
+  return base::to_underlying(read_anything::mojom::LetterSpacing::kStandard);
 }
 
 int ReadAnythingAppController::WideLetterSpacing() const {
-  return std::to_underlying(read_anything::mojom::LetterSpacing::kWide);
+  return base::to_underlying(read_anything::mojom::LetterSpacing::kWide);
 }
 
 int ReadAnythingAppController::VeryWideLetterSpacing() const {
-  return std::to_underlying(read_anything::mojom::LetterSpacing::kVeryWide);
+  return base::to_underlying(read_anything::mojom::LetterSpacing::kVeryWide);
 }
 
 int ReadAnythingAppController::DefaultTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kDefault);
+  return base::to_underlying(read_anything::mojom::Colors::kDefault);
 }
 
 int ReadAnythingAppController::LightTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kLight);
+  return base::to_underlying(read_anything::mojom::Colors::kLight);
 }
 
 int ReadAnythingAppController::DarkTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kDark);
+  return base::to_underlying(read_anything::mojom::Colors::kDark);
 }
 
 int ReadAnythingAppController::YellowTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kYellow);
+  return base::to_underlying(read_anything::mojom::Colors::kYellow);
 }
 
 int ReadAnythingAppController::BlueTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kBlue);
+  return base::to_underlying(read_anything::mojom::Colors::kBlue);
 }
 
 int ReadAnythingAppController::HighContrastTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kHighContrast);
+  return base::to_underlying(read_anything::mojom::Colors::kHighContrast);
 }
 
 int ReadAnythingAppController::LowContrastTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kLowContrast);
+  return base::to_underlying(read_anything::mojom::Colors::kLowContrast);
 }
 
 int ReadAnythingAppController::SepiaLightTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kSepiaLight);
+  return base::to_underlying(read_anything::mojom::Colors::kSepiaLight);
 }
 
 int ReadAnythingAppController::SepiaDarkTheme() const {
-  return std::to_underlying(read_anything::mojom::Colors::kSepiaDark);
+  return base::to_underlying(read_anything::mojom::Colors::kSepiaDark);
 }
 
 bool ReadAnythingAppController::IsHighlightOn() {
@@ -1654,74 +1655,74 @@ int ReadAnythingAppController::NoHighlighting() const {
 }
 
 int ReadAnythingAppController::PauseButtonStopSource() const {
-  return std::to_underlying(ReadAloudAppModel::ReadAloudStopSource::kButton);
+  return base::to_underlying(ReadAloudAppModel::ReadAloudStopSource::kButton);
 }
 
 int ReadAnythingAppController::KeyboardShortcutStopSource() const {
-  return std::to_underlying(
+  return base::to_underlying(
       ReadAloudAppModel::ReadAloudStopSource::kKeyboardShortcut);
 }
 
 int ReadAnythingAppController::EngineInterruptStopSource() const {
-  return std::to_underlying(
+  return base::to_underlying(
       ReadAloudAppModel::ReadAloudStopSource::kEngineInterrupt);
 }
 
 int ReadAnythingAppController::EngineErrorStopSource() const {
-  return std::to_underlying(
+  return base::to_underlying(
       ReadAloudAppModel::ReadAloudStopSource::kEngineError);
 }
 
 int ReadAnythingAppController::ContentFinishedStopSource() const {
-  return std::to_underlying(
+  return base::to_underlying(
       ReadAloudAppModel::ReadAloudStopSource::kFinishContent);
 }
 
 int ReadAnythingAppController::UnexpectedUpdateContentStopSource() const {
-  return std::to_underlying(
+  return base::to_underlying(
       ReadAloudAppModel::ReadAloudStopSource::kUnexpectedUpdateContent);
 }
 
 int ReadAnythingAppController::LineFocusOff() const {
-  return std::to_underlying(read_anything::mojom::LineFocus::kOff);
+  return base::to_underlying(read_anything::mojom::LineFocus::kOff);
 }
 
 int ReadAnythingAppController::LineFocusSmallStaticWindow() const {
-  return std::to_underlying(
+  return base::to_underlying(
       read_anything::mojom::LineFocus::kSmallStaticWindow);
 }
 
 int ReadAnythingAppController::LineFocusMediumStaticWindow() const {
-  return std::to_underlying(
+  return base::to_underlying(
       read_anything::mojom::LineFocus::kMediumStaticWindow);
 }
 
 int ReadAnythingAppController::LineFocusLargeStaticWindow() const {
-  return std::to_underlying(
+  return base::to_underlying(
       read_anything::mojom::LineFocus::kLargeStaticWindow);
 }
 
 int ReadAnythingAppController::LineFocusSmallCursorWindow() const {
-  return std::to_underlying(
+  return base::to_underlying(
       read_anything::mojom::LineFocus::kSmallCursorWindow);
 }
 
 int ReadAnythingAppController::LineFocusMediumCursorWindow() const {
-  return std::to_underlying(
+  return base::to_underlying(
       read_anything::mojom::LineFocus::kMediumCursorWindow);
 }
 
 int ReadAnythingAppController::LineFocusLargeCursorWindow() const {
-  return std::to_underlying(
+  return base::to_underlying(
       read_anything::mojom::LineFocus::kLargeCursorWindow);
 }
 
 int ReadAnythingAppController::LineFocusStaticLine() const {
-  return std::to_underlying(read_anything::mojom::LineFocus::kLineStatic);
+  return base::to_underlying(read_anything::mojom::LineFocus::kLineStatic);
 }
 
 int ReadAnythingAppController::LineFocusCursorLine() const {
-  return std::to_underlying(read_anything::mojom::LineFocus::kLineCursor);
+  return base::to_underlying(read_anything::mojom::LineFocus::kLineCursor);
 }
 
 int ReadAnythingAppController::MaxLineWidth() const {
@@ -1729,12 +1730,12 @@ int ReadAnythingAppController::MaxLineWidth() const {
 }
 
 int ReadAnythingAppController::InSidePanelPresentationState() const {
-  return std::to_underlying(
+  return base::to_underlying(
       read_anything::mojom::ReadAnythingPresentationState::kInSidePanel);
 }
 
 int ReadAnythingAppController::InImmersiveOverlayPresentationState() const {
-  return std::to_underlying(
+  return base::to_underlying(
       read_anything::mojom::ReadAnythingPresentationState::kInImmersiveOverlay);
 }
 
@@ -1755,7 +1756,7 @@ std::vector<ui::AXNodeID> ReadAnythingAppController::GetChildren(
   const std::set<ui::AXNodeID>* node_ids = model_.GetCurrentlyVisibleNodes();
   for (auto it = ax_node->UnignoredChildrenBegin();
        it != ax_node->UnignoredChildrenEnd(); ++it) {
-    if (node_ids->contains(it->id())) {
+    if (base::Contains(*node_ids, it->id())) {
       child_ids.push_back(it->id());
     }
   }
@@ -1968,13 +1969,13 @@ std::vector<std::string> ReadAnythingAppController::GetSupportedFonts() {
 
 std::string ReadAnythingAppController::GetValidatedFontName(
     const std::string& font) const {
-  if (!std::ranges::contains(GetAllFonts(), font)) {
+  if (!base::Contains(GetAllFonts(), font)) {
     return GetAllFonts().front();
   }
   if (font == "Serif" || font == "Sans-serif") {
     return base::ToLowerASCII(font);
   }
-  return font.contains(' ') ? base::StrCat({"\"", font, "\""}) : font;
+  return base::Contains(font, ' ') ? base::StrCat({"\"", font, "\""}) : font;
 }
 
 std::vector<std::string> ReadAnythingAppController::GetAllFonts() const {
diff --git a/chrome/renderer/accessibility/read_anything/read_anything_app_controller_browsertest.cc b/chrome/renderer/accessibility/read_anything/read_anything_app_controller_browsertest.cc
index 3d06d37a7d..331171d942 100644
--- a/chrome/renderer/accessibility/read_anything/read_anything_app_controller_browsertest.cc
+++ b/chrome/renderer/accessibility/read_anything/read_anything_app_controller_browsertest.cc
@@ -17,6 +17,7 @@
 #include "base/strings/string_number_conversions.h"
 #include "base/test/metrics/histogram_tester.h"
 #include "base/test/scoped_feature_list.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "chrome/common/read_anything/read_anything.mojom-shared.h"
 #include "chrome/common/read_anything/read_anything_util.h"
@@ -799,41 +800,41 @@ TEST_F(ReadAnythingAppControllerTest,
 TEST_F(ReadAnythingAppControllerTest, OnLetterSpacingChange_ValidChange) {
   static constexpr auto kLetterSpacing =
       read_anything::mojom::LetterSpacing::kWide;
-  controller().OnLetterSpacingChange(std::to_underlying(kLetterSpacing));
+  controller().OnLetterSpacingChange(base::to_underlying(kLetterSpacing));
   EXPECT_CALL(page_handler_, OnLetterSpaceChange(kLetterSpacing)).Times(1);
-  ASSERT_EQ(controller().LetterSpacing(), std::to_underlying(kLetterSpacing));
+  ASSERT_EQ(controller().LetterSpacing(), base::to_underlying(kLetterSpacing));
 }
 
 TEST_F(ReadAnythingAppControllerTest, OnLetterSpacingChange_InvalidChange) {
   controller().OnLetterSpacingChange(
-      std::to_underlying(read_anything::mojom::LetterSpacing::kMaxValue) + 1);
+      base::to_underlying(read_anything::mojom::LetterSpacing::kMaxValue) + 1);
   EXPECT_CALL(page_handler_, OnLetterSpaceChange).Times(0);
 }
 
 TEST_F(ReadAnythingAppControllerTest, OnLineSpacingChange_ValidChange) {
   static constexpr auto kLineSpacing =
       read_anything::mojom::LineSpacing::kVeryLoose;
-  controller().OnLineSpacingChange(std::to_underlying(kLineSpacing));
+  controller().OnLineSpacingChange(base::to_underlying(kLineSpacing));
   EXPECT_CALL(page_handler_, OnLineSpaceChange(kLineSpacing)).Times(1);
-  ASSERT_EQ(controller().LineSpacing(), std::to_underlying(kLineSpacing));
+  ASSERT_EQ(controller().LineSpacing(), base::to_underlying(kLineSpacing));
 }
 
 TEST_F(ReadAnythingAppControllerTest, OnLineSpacingChange_InvalidChange) {
   controller().OnLineSpacingChange(
-      std::to_underlying(read_anything::mojom::LineSpacing::kMaxValue) + 1);
+      base::to_underlying(read_anything::mojom::LineSpacing::kMaxValue) + 1);
   EXPECT_CALL(page_handler_, OnLineSpaceChange).Times(0);
 }
 
 TEST_F(ReadAnythingAppControllerTest, OnThemeChange_ValidChange) {
   static constexpr auto kColor = read_anything::mojom::Colors::kYellow;
-  controller().OnThemeChange(std::to_underlying(kColor));
+  controller().OnThemeChange(base::to_underlying(kColor));
   EXPECT_CALL(page_handler_, OnColorChange(kColor)).Times(1);
-  ASSERT_EQ(controller().ColorTheme(), std::to_underlying(kColor));
+  ASSERT_EQ(controller().ColorTheme(), base::to_underlying(kColor));
 }
 
 TEST_F(ReadAnythingAppControllerTest, OnThemeChange_InvalidChange) {
   controller().OnThemeChange(
-      std::to_underlying(read_anything::mojom::Colors::kMaxValue) + 1);
+      base::to_underlying(read_anything::mojom::Colors::kMaxValue) + 1);
   EXPECT_CALL(page_handler_, OnColorChange).Times(0);
 }
 
@@ -892,10 +893,10 @@ TEST_F(ReadAnythingAppControllerTest, OnLanguagePrefChange) {
   controller().OnLanguagePrefChange(disabled_lang, false);
 
   EXPECT_CALL(page_handler_, OnLanguagePrefChange).Times(3);
-  ASSERT_TRUE(
-      read_aloud_model().languages_enabled_in_pref().contains(enabled_lang));
-  ASSERT_FALSE(
-      read_aloud_model().languages_enabled_in_pref().contains(disabled_lang));
+  ASSERT_TRUE(base::Contains(read_aloud_model().languages_enabled_in_pref(),
+                             enabled_lang));
+  ASSERT_FALSE(base::Contains(read_aloud_model().languages_enabled_in_pref(),
+                              disabled_lang));
 }
 
 TEST_F(ReadAnythingAppControllerTest, GetStoredVoice_ReturnsLatestVoice) {
@@ -1225,8 +1226,8 @@ TEST_F(ReadAnythingAppControllerTest, OnSettingsRestoredFromPrefs) {
       images_enabled, color, speech_rate, std::move(voices),
       std::move(languages_enabled_in_pref), highlight_granularity, line_focus);
 
-  EXPECT_EQ(std::to_underlying(line_spacing), controller().LineSpacing());
-  EXPECT_EQ(std::to_underlying(letter_spacing), controller().LetterSpacing());
+  EXPECT_EQ(base::to_underlying(line_spacing), controller().LineSpacing());
+  EXPECT_EQ(base::to_underlying(letter_spacing), controller().LetterSpacing());
   EXPECT_EQ(font_name, controller().FontName());
   EXPECT_EQ(font_size, controller().FontSize());
   EXPECT_EQ(links_enabled, controller().LinksEnabled());
@@ -1818,10 +1819,10 @@ TEST_F(ReadAnythingAppControllerTest,
   update.tree_data.sel_is_backward = false;
 
   AccessibilityEventReceived({std::move(update)});
-  EXPECT_TRUE(model().selection_node_ids().contains(1));
-  EXPECT_TRUE(model().selection_node_ids().contains(2));
-  EXPECT_TRUE(model().selection_node_ids().contains(3));
-  EXPECT_TRUE(model().selection_node_ids().contains(4));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 3));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppControllerTest,
@@ -1835,10 +1836,10 @@ TEST_F(ReadAnythingAppControllerTest,
   update.tree_data.sel_focus_offset = 0;
   update.tree_data.sel_is_backward = true;
   AccessibilityEventReceived({std::move(update)});
-  EXPECT_TRUE(model().selection_node_ids().contains(1));
-  EXPECT_TRUE(model().selection_node_ids().contains(2));
-  EXPECT_TRUE(model().selection_node_ids().contains(3));
-  EXPECT_TRUE(model().selection_node_ids().contains(4));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 3));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppControllerTest, DisplayNodeIdsContains_ContentNodes) {
@@ -1854,19 +1855,19 @@ TEST_F(ReadAnythingAppControllerTest, DisplayNodeIdsContains_ContentNodes) {
   ui::AXEvent load_complete(0, ax::mojom::Event::kLoadComplete);
   AccessibilityEventReceived({std::move(update)}, {std::move(load_complete)});
   controller().OnAXTreeDistilled(tree_id_, {3});
-  EXPECT_TRUE(model().display_node_ids().contains(1));
-  EXPECT_FALSE(model().display_node_ids().contains(2));
-  EXPECT_TRUE(model().display_node_ids().contains(3));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 3));
   Mock::VerifyAndClearExpectations(distiller_);
 }
 
 TEST_F(ReadAnythingAppControllerTest,
        DisplayNodeIdsContains_NoSelectionOrContentNodes) {
   controller().OnAXTreeDistilled(tree_id_, {});
-  EXPECT_FALSE(model().display_node_ids().contains(1));
-  EXPECT_FALSE(model().display_node_ids().contains(2));
-  EXPECT_FALSE(model().display_node_ids().contains(3));
-  EXPECT_FALSE(model().display_node_ids().contains(4));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 2));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 3));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppControllerTest, DoesNotCrashIfContentNodeNotFoundInTree) {
@@ -1883,10 +1884,10 @@ TEST_F(ReadAnythingAppControllerTest, Draw_RecomputeDisplayNodes) {
   model().Reset({3, 4});
   controller().Draw(/* recompute_display_nodes= */ true);
 
-  EXPECT_TRUE(model().display_node_ids().contains(1));
-  EXPECT_FALSE(model().display_node_ids().contains(2));
-  EXPECT_TRUE(model().display_node_ids().contains(3));
-  EXPECT_TRUE(model().display_node_ids().contains(4));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 3));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppControllerTest, Draw_DoNotRecomputeDisplayNodesForDocs) {
@@ -1907,9 +1908,9 @@ TEST_F(ReadAnythingAppControllerTest, Draw_DoNotRecomputeDisplayNodesForDocs) {
   controller().OnAXTreeDistilled(tree_id_, {3});
   controller().OnActiveAXTreeIDChanged(id_1, ukm::kInvalidSourceId, false);
   EXPECT_TRUE(controller().IsGoogleDocs());
-  EXPECT_TRUE(model().display_node_ids().contains(1));
-  EXPECT_FALSE(model().display_node_ids().contains(2));
-  EXPECT_TRUE(model().display_node_ids().contains(3));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 3));
   Mock::VerifyAndClearExpectations(distiller_);
 
   ui::AXNodeData node1;
@@ -1920,10 +1921,10 @@ TEST_F(ReadAnythingAppControllerTest, Draw_DoNotRecomputeDisplayNodesForDocs) {
   SendUpdateWithNodes({std::move(node1)});
   model().Reset({3, 4});
   controller().Draw(/* recompute_display_nodes= */ true);
-  EXPECT_FALSE(model().display_node_ids().contains(1));
-  EXPECT_FALSE(model().display_node_ids().contains(2));
-  EXPECT_FALSE(model().display_node_ids().contains(3));
-  EXPECT_FALSE(model().display_node_ids().contains(4));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 2));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 3));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppControllerTest, AccessibilityEventReceived) {
@@ -2874,7 +2875,7 @@ TEST_F(ReadAnythingAppControllerTest, TurnedHighlightOn_SavesHighlightState) {
       .Times(0);
 
   controller().OnHighlightGranularityChanged(
-      std::to_underlying(read_anything::mojom::HighlightGranularity::kOn));
+      base::to_underlying(read_anything::mojom::HighlightGranularity::kOn));
 
   EXPECT_TRUE(controller().IsHighlightOn());
 }
@@ -2890,7 +2891,7 @@ TEST_F(ReadAnythingAppControllerTest, TurnedHighlightOff_SavesHighlightState) {
       .Times(1);
 
   controller().OnHighlightGranularityChanged(
-      std::to_underlying(read_anything::mojom::HighlightGranularity::kOff));
+      base::to_underlying(read_anything::mojom::HighlightGranularity::kOff));
 
   EXPECT_FALSE(controller().IsHighlightOn());
 }
@@ -3001,13 +3002,13 @@ TEST_F(ReadAnythingAppControllerTest, DisplayNodes_WithMultipleTrees) {
 
   // Check the display nodes.
   const auto& display_node_ids = model().display_node_ids();
-  EXPECT_TRUE(display_node_ids.contains(kId1));
-  EXPECT_TRUE(display_node_ids.contains(kId2));
-  EXPECT_TRUE(display_node_ids.contains(kId3));
+  EXPECT_TRUE(base::Contains(display_node_ids, kId1));
+  EXPECT_TRUE(base::Contains(display_node_ids, kId2));
+  EXPECT_TRUE(base::Contains(display_node_ids, kId3));
 
   // The ad content from the child tree should not be in the display nodes.
-  EXPECT_FALSE(display_node_ids.contains(kAdChildNodeId));
-  EXPECT_FALSE(display_node_ids.contains(kAdChildRootId));
+  EXPECT_FALSE(base::Contains(display_node_ids, kAdChildNodeId));
+  EXPECT_FALSE(base::Contains(display_node_ids, kAdChildRootId));
 
   // The text content for the duplicate id returns the actual content, not the
   // ad content.
@@ -4104,10 +4105,10 @@ TEST_F(ReadAnythingAppControllerV8SegmentationTest,
   controller().Draw(/* recompute_display_nodes= */ true);
 
   EXPECT_FALSE(controller().IsSpeechTreeInitialized());
-  EXPECT_TRUE(model().display_node_ids().contains(1));
-  EXPECT_FALSE(model().display_node_ids().contains(2));
-  EXPECT_TRUE(model().display_node_ids().contains(3));
-  EXPECT_TRUE(model().display_node_ids().contains(4));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 3));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppControllerV8SegmentationTest,
diff --git a/chrome/renderer/accessibility/read_anything/read_anything_app_model.cc b/chrome/renderer/accessibility/read_anything/read_anything_app_model.cc
index 89e02348e1..7acfb8fde4 100644
--- a/chrome/renderer/accessibility/read_anything/read_anything_app_model.cc
+++ b/chrome/renderer/accessibility/read_anything/read_anything_app_model.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/stringprintf.h"
@@ -54,7 +55,8 @@ const ui::AXNode* GetUnignoredParentForSelection(const ui::AXNode* node) {
       // displayed as siblings, to avoid misnumbering.
       const std::string_view display =
           node->GetStringAttribute(ax::mojom::StringAttribute::kDisplay);
-      return display.contains("inline") || display.contains("list-item");
+      return base::Contains(display, "inline") ||
+             base::Contains(display, "list-item");
     };
     if (!should_skip(ancestor)) {
       return ancestor;
@@ -361,7 +363,7 @@ void ReadAnythingAppModel::ComputeDisplayNodeIdsForDistilledTree() {
         content_node->GetAncestorsCrossingTreeBoundaryAsQueue();
     while (!ancestors.empty()) {
       ui::AXNodeID ancestor_id = ancestors.front()->id();
-      if (display_node_ids_.contains(ancestor_id)) {
+      if (base::Contains(display_node_ids_, ancestor_id)) {
         break;
       }
       ancestors.pop();
@@ -407,7 +409,7 @@ ui::AXSerializableTree* ReadAnythingAppModel::GetTreeFromId(
 }
 
 bool ReadAnythingAppModel::ContainsTree(const ui::AXTreeID& tree_id) const {
-  return tree_infos_.contains(tree_id);
+  return base::Contains(tree_infos_, tree_id);
 }
 
 bool ReadAnythingAppModel::ContainsActiveTree() const {
@@ -708,7 +710,7 @@ void ReadAnythingAppModel::OnAXTreeDestroyed(const ui::AXTreeID& tree_id) {
 }
 
 ukm::SourceId ReadAnythingAppModel::GetUkmSourceId() const {
-  if (tree_infos_.contains(active_tree_id_)) {
+  if (base::Contains(tree_infos_, active_tree_id_)) {
     ReadAnythingAppModel::AXTreeInfo* tree_info =
         tree_infos_.at(active_tree_id_).get();
     if (tree_info) {
@@ -725,7 +727,7 @@ void ReadAnythingAppModel::SetUkmSourceIdForTree(const ui::AXTreeID& tree,
   // tree_infos_. When this happens, we should keep track of the ukm_source_id,
   // and later, if the tree is added to tree_infos_ while it's still active,
   // we can try again to set the ukm source.
-  if (!tree_infos_.contains(active_tree_id_)) {
+  if (!base::Contains(tree_infos_, active_tree_id_)) {
     pending_ukm_sources_[tree] = ukm_source_id;
     return;
   }
@@ -734,7 +736,7 @@ void ReadAnythingAppModel::SetUkmSourceIdForTree(const ui::AXTreeID& tree,
 }
 
 void ReadAnythingAppModel::SetUkmSourceId(ukm::SourceId ukm_source_id) {
-  if (!tree_infos_.contains(active_tree_id_)) {
+  if (!base::Contains(tree_infos_, active_tree_id_)) {
     return;
   }
   ReadAnythingAppModel::AXTreeInfo* tree_info =
@@ -750,7 +752,7 @@ void ReadAnythingAppModel::SetUkmSourceId(ukm::SourceId ukm_source_id) {
 }
 
 int ReadAnythingAppModel::GetNumSelections() const {
-  if (tree_infos_.contains(active_tree_id_)) {
+  if (base::Contains(tree_infos_, active_tree_id_)) {
     ReadAnythingAppModel::AXTreeInfo* tree_info =
         tree_infos_.at(active_tree_id_).get();
     if (tree_info) {
@@ -761,7 +763,7 @@ int ReadAnythingAppModel::GetNumSelections() const {
 }
 
 void ReadAnythingAppModel::SetNumSelections(int num_selections) {
-  if (!tree_infos_.contains(active_tree_id_)) {
+  if (!base::Contains(tree_infos_, active_tree_id_)) {
     return;
   }
   ReadAnythingAppModel::AXTreeInfo* tree_info =
@@ -782,7 +784,7 @@ ui::AXNode* ReadAnythingAppModel::GetAXNode(
 }
 
 bool ReadAnythingAppModel::NodeIsContentNode(ui::AXNodeID ax_node_id) const {
-  return std::ranges::contains(content_node_ids_, ax_node_id);
+  return base::Contains(content_node_ids_, ax_node_id);
 }
 
 void ReadAnythingAppModel::AdjustTextSize(int increment) {
@@ -1067,7 +1069,7 @@ void ReadAnythingAppModel::ProcessGeneratedEvents(
         break;
       case ui::AXEventGenerator::Event::EXPANDED:
         if (features::IsReadAnythingReadAloudEnabled()) {
-          if (std::ranges::contains(content_node_ids_, event.node_id)) {
+          if (base::Contains(content_node_ids_, event.node_id)) {
             redraw_required_ = true;
           } else {
             requires_distillation_ = true;
diff --git a/chrome/renderer/accessibility/read_anything/read_anything_app_model_browsertest.cc b/chrome/renderer/accessibility/read_anything/read_anything_app_model_browsertest.cc
index 1f70beb535..8c970914e4 100644
--- a/chrome/renderer/accessibility/read_anything/read_anything_app_model_browsertest.cc
+++ b/chrome/renderer/accessibility/read_anything/read_anything_app_model_browsertest.cc
@@ -609,12 +609,12 @@ TEST_F(ReadAnythingAppModelTest, DisplayNodeIdsContains_ContentNodes) {
   // it in AccessibilityEventReceived, it will re-distill the tree.
   ApplyAccessibilityUpdates(tree_id_, {std::move(update)});
   ProcessDisplayNodes({3, 4});
-  EXPECT_TRUE(model().display_node_ids().contains(1));
-  EXPECT_FALSE(model().display_node_ids().contains(2));
-  EXPECT_TRUE(model().display_node_ids().contains(3));
-  EXPECT_TRUE(model().display_node_ids().contains(4));
-  EXPECT_TRUE(model().display_node_ids().contains(5));
-  EXPECT_TRUE(model().display_node_ids().contains(6));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 3));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 4));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 5));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 6));
 }
 
 TEST_F(ReadAnythingAppModelTest,
@@ -629,10 +629,10 @@ TEST_F(ReadAnythingAppModelTest,
   update.nodes[2].AddState(ax::mojom::State::kIgnored);
   ApplyAccessibilityUpdates(tree_id_, {std::move(update)});
   ProcessDisplayNodes({2, 3, 4});
-  EXPECT_TRUE(model().display_node_ids().contains(1));
-  EXPECT_TRUE(model().display_node_ids().contains(2));
-  EXPECT_FALSE(model().display_node_ids().contains(3));
-  EXPECT_FALSE(model().display_node_ids().contains(4));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_TRUE(base::Contains(model().display_node_ids(), 2));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 3));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppModelTest,
@@ -692,10 +692,10 @@ TEST_F(ReadAnythingAppModelTest,
 
   ApplyAccessibilityUpdates(tree_id_, {std::move(update)});
   model().PostProcessSelection();
-  EXPECT_TRUE(model().selection_node_ids().contains(1));
-  EXPECT_TRUE(model().selection_node_ids().contains(2));
-  EXPECT_TRUE(model().selection_node_ids().contains(3));
-  EXPECT_TRUE(model().selection_node_ids().contains(4));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 3));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppModelTest,
@@ -709,10 +709,10 @@ TEST_F(ReadAnythingAppModelTest,
   update.tree_data.sel_is_backward = true;
   ApplyAccessibilityUpdates(tree_id_, {std::move(update)});
   model().PostProcessSelection();
-  EXPECT_TRUE(model().selection_node_ids().contains(1));
-  EXPECT_TRUE(model().selection_node_ids().contains(2));
-  EXPECT_TRUE(model().selection_node_ids().contains(3));
-  EXPECT_TRUE(model().selection_node_ids().contains(4));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 2));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 3));
+  EXPECT_TRUE(base::Contains(model().selection_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppModelTest,
@@ -733,10 +733,10 @@ TEST_F(ReadAnythingAppModelTest,
 
   ApplyAccessibilityUpdates(tree_id_, {std::move(update)});
   model().PostProcessSelection();
-  EXPECT_FALSE(model().display_node_ids().contains(1));
-  EXPECT_FALSE(model().selection_node_ids().contains(2));
-  EXPECT_FALSE(model().selection_node_ids().contains(3));
-  EXPECT_FALSE(model().selection_node_ids().contains(4));
+  EXPECT_FALSE(base::Contains(model().display_node_ids(), 1));
+  EXPECT_FALSE(base::Contains(model().selection_node_ids(), 2));
+  EXPECT_FALSE(base::Contains(model().selection_node_ids(), 3));
+  EXPECT_FALSE(base::Contains(model().selection_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppModelTest, Reset_ResetsState) {
@@ -761,33 +761,33 @@ TEST_F(ReadAnythingAppModelTest, Reset_ResetsState) {
   // Assert initial state before model().Resetting.
   ASSERT_TRUE(model().distillation_in_progress());
 
-  ASSERT_TRUE(model().display_node_ids().contains(1));
-  ASSERT_TRUE(model().display_node_ids().contains(3));
-  ASSERT_TRUE(model().display_node_ids().contains(4));
-  ASSERT_TRUE(model().display_node_ids().contains(5));
-  ASSERT_TRUE(model().display_node_ids().contains(6));
+  ASSERT_TRUE(base::Contains(model().display_node_ids(), 1));
+  ASSERT_TRUE(base::Contains(model().display_node_ids(), 3));
+  ASSERT_TRUE(base::Contains(model().display_node_ids(), 4));
+  ASSERT_TRUE(base::Contains(model().display_node_ids(), 5));
+  ASSERT_TRUE(base::Contains(model().display_node_ids(), 6));
 
   model().Reset({1, 2});
 
   // Assert model().Reset state.
   ASSERT_FALSE(model().distillation_in_progress());
 
-  ASSERT_TRUE(std::ranges::contains(model().content_node_ids(), 1));
-  ASSERT_TRUE(std::ranges::contains(model().content_node_ids(), 2));
+  ASSERT_TRUE(base::Contains(model().content_node_ids(), 1));
+  ASSERT_TRUE(base::Contains(model().content_node_ids(), 2));
 
-  ASSERT_FALSE(model().display_node_ids().contains(1));
-  ASSERT_FALSE(model().display_node_ids().contains(3));
-  ASSERT_FALSE(model().display_node_ids().contains(4));
-  ASSERT_FALSE(model().display_node_ids().contains(5));
-  ASSERT_FALSE(model().display_node_ids().contains(6));
+  ASSERT_FALSE(base::Contains(model().display_node_ids(), 1));
+  ASSERT_FALSE(base::Contains(model().display_node_ids(), 3));
+  ASSERT_FALSE(base::Contains(model().display_node_ids(), 4));
+  ASSERT_FALSE(base::Contains(model().display_node_ids(), 5));
+  ASSERT_FALSE(base::Contains(model().display_node_ids(), 6));
 
   // Calling model().Reset with different content nodes updates the content
   // nodes.
   model().Reset({5, 4});
-  ASSERT_FALSE(std::ranges::contains(model().content_node_ids(), 1));
-  ASSERT_FALSE(std::ranges::contains(model().content_node_ids(), 2));
-  ASSERT_TRUE(std::ranges::contains(model().content_node_ids(), 5));
-  ASSERT_TRUE(std::ranges::contains(model().content_node_ids(), 4));
+  ASSERT_FALSE(base::Contains(model().content_node_ids(), 1));
+  ASSERT_FALSE(base::Contains(model().content_node_ids(), 2));
+  ASSERT_TRUE(base::Contains(model().content_node_ids(), 5));
+  ASSERT_TRUE(base::Contains(model().content_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppModelTest, Reset_ResetsSelectionState) {
@@ -803,9 +803,9 @@ TEST_F(ReadAnythingAppModelTest, Reset_ResetsSelectionState) {
   model().PostProcessSelection();
 
   // Assert initial selection state.
-  ASSERT_TRUE(model().selection_node_ids().contains(1));
-  ASSERT_TRUE(model().selection_node_ids().contains(2));
-  ASSERT_TRUE(model().selection_node_ids().contains(3));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 2));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 3));
 
   ASSERT_TRUE(model().has_selection());
 
@@ -818,9 +818,9 @@ TEST_F(ReadAnythingAppModelTest, Reset_ResetsSelectionState) {
   model().Reset({1, 2});
 
   // Assert model().Reset selection state.
-  ASSERT_FALSE(model().selection_node_ids().contains(1));
-  ASSERT_FALSE(model().selection_node_ids().contains(2));
-  ASSERT_FALSE(model().selection_node_ids().contains(3));
+  ASSERT_FALSE(base::Contains(model().selection_node_ids(), 1));
+  ASSERT_FALSE(base::Contains(model().selection_node_ids(), 2));
+  ASSERT_FALSE(base::Contains(model().selection_node_ids(), 3));
 
   ASSERT_FALSE(model().has_selection());
 
@@ -847,9 +847,9 @@ TEST_F(ReadAnythingAppModelTest, PostProcessSelection_SelectionStateCorrect) {
   ASSERT_FALSE(model().requires_post_process_selection());
   ASSERT_TRUE(model().has_selection());
 
-  ASSERT_TRUE(model().selection_node_ids().contains(1));
-  ASSERT_TRUE(model().selection_node_ids().contains(2));
-  ASSERT_TRUE(model().selection_node_ids().contains(3));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 2));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 3));
 
   ASSERT_EQ(model().start_offset(), 0);
   ASSERT_EQ(model().end_offset(), 0);
@@ -1424,17 +1424,17 @@ TEST_F(ReadAnythingAppModelTest,
   ASSERT_EQ(model().end_node_id(), 5);
 
   // 1 and 3 are ancestors, so they are included as selection nodes..
-  ASSERT_TRUE(model().selection_node_ids().contains(1));
-  ASSERT_TRUE(model().selection_node_ids().contains(3));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 3));
 
-  ASSERT_TRUE(model().selection_node_ids().contains(5));
-  ASSERT_TRUE(model().selection_node_ids().contains(6));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 5));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 6));
 
   // Even though 3 is a generic container with more than one child, its
   // sibling nodes are included in the selection because the start node
   // includes it.
-  ASSERT_TRUE(model().selection_node_ids().contains(2));
-  ASSERT_TRUE(model().selection_node_ids().contains(3));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 2));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 3));
 }
 
 TEST_F(ReadAnythingAppModelTest,
@@ -1473,10 +1473,10 @@ TEST_F(ReadAnythingAppModelTest,
   ASSERT_EQ(model().start_node_id(), 4);
   ASSERT_EQ(model().end_node_id(), 4);
 
-  ASSERT_TRUE(model().selection_node_ids().contains(1));
-  ASSERT_FALSE(model().selection_node_ids().contains(2));
-  ASSERT_TRUE(model().selection_node_ids().contains(3));
-  ASSERT_TRUE(model().selection_node_ids().contains(4));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  ASSERT_FALSE(base::Contains(model().selection_node_ids(), 2));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 3));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppModelTest,
@@ -1515,10 +1515,10 @@ TEST_F(ReadAnythingAppModelTest,
   ASSERT_EQ(model().start_node_id(), 4);
   ASSERT_EQ(model().end_node_id(), 4);
 
-  ASSERT_TRUE(model().selection_node_ids().contains(1));
-  ASSERT_FALSE(model().selection_node_ids().contains(2));
-  ASSERT_TRUE(model().selection_node_ids().contains(3));
-  ASSERT_TRUE(model().selection_node_ids().contains(4));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  ASSERT_FALSE(base::Contains(model().selection_node_ids(), 2));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 3));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 4));
 }
 
 TEST_F(ReadAnythingAppModelTest,
@@ -1555,10 +1555,10 @@ TEST_F(ReadAnythingAppModelTest,
   ASSERT_EQ(model().start_node_id(), 4);
   ASSERT_EQ(model().end_node_id(), 4);
 
-  ASSERT_TRUE(model().selection_node_ids().contains(1));
-  ASSERT_FALSE(model().selection_node_ids().contains(2));
-  ASSERT_TRUE(model().selection_node_ids().contains(3));
-  ASSERT_TRUE(model().selection_node_ids().contains(4));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  ASSERT_FALSE(base::Contains(model().selection_node_ids(), 2));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 3));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 4));
 }
 
 TEST_F(
@@ -1596,14 +1596,14 @@ TEST_F(
   ASSERT_EQ(model().end_node_id(), 5);
 
   // 1 and 3 are ancestors, so they are included as selection nodes..
-  ASSERT_TRUE(model().selection_node_ids().contains(1));
-  ASSERT_TRUE(model().selection_node_ids().contains(3));
-  ASSERT_TRUE(model().selection_node_ids().contains(4));
-  ASSERT_TRUE(model().selection_node_ids().contains(5));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 1));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 3));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 4));
+  ASSERT_TRUE(base::Contains(model().selection_node_ids(), 5));
 
   // Since 3 is a generic container with more than one child, its sibling nodes
   // are not included, so 2 is ignored.
-  ASSERT_FALSE(model().selection_node_ids().contains(2));
+  ASSERT_FALSE(base::Contains(model().selection_node_ids(), 2));
 }
 
 TEST_F(ReadAnythingAppModelTest, ResetTextSize_ReturnsTextSizeToDefault) {
diff --git a/chrome/renderer/actor/page_stability_monitor.cc b/chrome/renderer/actor/page_stability_monitor.cc
index f2f562b240..69f3ff5e16 100644
--- a/chrome/renderer/actor/page_stability_monitor.cc
+++ b/chrome/renderer/actor/page_stability_monitor.cc
@@ -15,6 +15,7 @@
 #include "base/state_transitions.h"
 #include "base/task/sequenced_task_runner.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/common/actor/actor_logging.h"
 #include "chrome/common/actor/journal_details_builder.h"
 #include "chrome/common/chrome_features.h"
diff --git a/chrome/renderer/actor/tool_executor.cc b/chrome/renderer/actor/tool_executor.cc
index 16164ea850..390b57cef6 100644
--- a/chrome/renderer/actor/tool_executor.cc
+++ b/chrome/renderer/actor/tool_executor.cc
@@ -14,6 +14,7 @@
 #include "base/metrics/histogram_functions.h"
 #include "base/notreached.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/common/actor.mojom.h"
 #include "chrome/common/actor/action_result.h"
 #include "chrome/common/actor/journal_details_builder.h"
@@ -204,7 +205,7 @@ void ToolExecutor::ExecuteTool(const actor::TaskId& task_id,
     tool_->MarkAsRevalidation();
     ValidationResult revalidation = tool_->Validate();
     base::UmaHistogramSparse("Actor.Tools.RevalidationResult",
-                             std::to_underlying(revalidation.result->code));
+                             base::to_underlying(revalidation.result->code));
     if (!IsOk(*revalidation.result)) {
       ToolFinished(std::move(revalidation.result));
       return;
diff --git a/chrome/renderer/chrome_content_settings_agent_delegate.cc b/chrome/renderer/chrome_content_settings_agent_delegate.cc
index d967a79e1b..0479803acd 100644
--- a/chrome/renderer/chrome_content_settings_agent_delegate.cc
+++ b/chrome/renderer/chrome_content_settings_agent_delegate.cc
@@ -11,6 +11,7 @@
 #if BUILDFLAG(IS_CHROMEOS)
 #include "ash/webui/file_manager/url_constants.h"
 #endif
+#include "base/containers/contains.h"
 #include "content/public/common/url_constants.h"
 #include "content/public/renderer/render_frame.h"
 #include "third_party/blink/public/platform/web_security_origin.h"
@@ -64,8 +65,8 @@ bool ChromeContentSettingsAgentDelegate::IsPluginTemporarilyAllowed(
     const std::string& identifier) {
   // If the empty string is in here, it means all plugins are allowed.
   // TODO(bauerb): Remove this once we only pass in explicit identifiers.
-  return temporarily_allowed_plugins_.contains(identifier) ||
-         temporarily_allowed_plugins_.contains(std::string());
+  return base::Contains(temporarily_allowed_plugins_, identifier) ||
+         base::Contains(temporarily_allowed_plugins_, std::string());
 }
 
 void ChromeContentSettingsAgentDelegate::AllowPluginTemporarily(
diff --git a/chrome/renderer/controlled_frame/web_url_pattern_natives.cc b/chrome/renderer/controlled_frame/web_url_pattern_natives.cc
index fe76e004d0..ab3731b85e 100644
--- a/chrome/renderer/controlled_frame/web_url_pattern_natives.cc
+++ b/chrome/renderer/controlled_frame/web_url_pattern_natives.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/containers/to_value_list.h"
 #include "extensions/renderer/script_context.h"
 #include "third_party/blink/public/common/safe_url_pattern.h"
@@ -36,7 +37,7 @@ bool WouldBeValidMatchPattern(const blink::SafeUrlPattern& safe_url_pattern) {
       protocol_part.type == liburlpattern::PartType::kFixed;
   const bool protocol_allowed =
       protocol_is_fixed &&
-      std::ranges::contains(kAllowedProtocols, protocol_part.value);
+      base::Contains(kAllowedProtocols, protocol_part.value);
   if (!protocol_is_wildcard && !protocol_allowed) {
     return false;
   }
diff --git a/chrome/renderer/translate/translate_agent_browsertest.cc b/chrome/renderer/translate/translate_agent_browsertest.cc
index bc5e8b9c1f..e98e2f8936 100644
--- a/chrome/renderer/translate/translate_agent_browsertest.cc
+++ b/chrome/renderer/translate/translate_agent_browsertest.cc
@@ -5,7 +5,6 @@
 #include "components/translate/content/renderer/translate_agent.h"
 
 #include <tuple>
-#include <utility>
 
 #include "base/base_paths.h"
 #include "base/files/file.h"
@@ -16,6 +15,7 @@
 #include "base/path_service.h"
 #include "base/run_loop.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/common/chrome_isolated_world_ids.h"
 #include "chrome/test/base/chrome_render_view_test.h"
 #include "components/language_detection/core/constants.h"
@@ -228,7 +228,7 @@ TEST_F(TranslateAgentBrowserTest, TranslateLibNeverReady) {
   EXPECT_CALL(*translate_agent_, GetErrorCode())
       .Times(AtLeast(5))
       .WillRepeatedly(
-          Return(std::to_underlying(translate::TranslateErrors::NONE)));
+          Return(base::to_underlying(translate::TranslateErrors::NONE)));
 
   translate_agent_->TranslatePage("en", "fr", std::string());
   base::RunLoop().RunUntilIdle();
@@ -253,7 +253,7 @@ TEST_F(TranslateAgentBrowserTest, TranslateSuccess) {
       .WillOnce(Return(true));
 
   EXPECT_CALL(*translate_agent_, GetErrorCode())
-      .WillOnce(Return(std::to_underlying(translate::TranslateErrors::NONE)));
+      .WillOnce(Return(base::to_underlying(translate::TranslateErrors::NONE)));
 
   EXPECT_CALL(*translate_agent_, StartTranslation()).WillOnce(Return(true));
 
@@ -309,7 +309,7 @@ TEST_F(TranslateAgentBrowserTest, TranslateFailure) {
 
   EXPECT_CALL(*translate_agent_, GetErrorCode())
       .WillOnce(Return(
-          std::to_underlying(translate::TranslateErrors::TRANSLATION_ERROR)));
+          base::to_underlying(translate::TranslateErrors::TRANSLATION_ERROR)));
 
   // V8 call for performance monitoring should be ignored.
   EXPECT_CALL(*translate_agent_, ExecuteScriptAndGetDoubleResult(_)).Times(2);
diff --git a/chrome/renderer/translate/translate_script_browsertest.cc b/chrome/renderer/translate/translate_script_browsertest.cc
index 2217662a19..34c92a7f2d 100644
--- a/chrome/renderer/translate/translate_script_browsertest.cc
+++ b/chrome/renderer/translate/translate_script_browsertest.cc
@@ -2,10 +2,9 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <utility>
-
 #include "base/strings/stringprintf.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "chrome/test/base/chrome_render_view_test.h"
 #include "components/grit/components_resources.h"
 #include "components/translate/core/common/translate_errors.h"
@@ -145,7 +144,7 @@ TEST_F(TranslateScriptBrowserTest, ElementLoadSuccess) {
   InjectElementLibrary();
   EXPECT_TRUE(IsLibReady());
   EXPECT_FALSE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::NONE),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::NONE),
             GetErrorCode());
 }
 
@@ -158,7 +157,7 @@ TEST_F(TranslateScriptBrowserTest, ElementLoadFailure) {
   EXPECT_FALSE(IsLibReady());
   EXPECT_TRUE(GetError());
   EXPECT_EQ(
-      std::to_underlying(translate::TranslateErrors::INITIALIZATION_ERROR),
+      base::to_underlying(translate::TranslateErrors::INITIALIZATION_ERROR),
       GetErrorCode());
 }
 
@@ -167,13 +166,13 @@ TEST_F(TranslateScriptBrowserTest, TranslateSuccess) {
   InjectElementLibrary();
   EXPECT_TRUE(IsLibReady());
   EXPECT_FALSE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::NONE),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::NONE),
             GetErrorCode());
 
   ExecuteScript(kTranslate);
 
   EXPECT_FALSE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::NONE),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::NONE),
             GetErrorCode());
 }
 
@@ -184,14 +183,14 @@ TEST_F(TranslateScriptBrowserTest, TranslateFail) {
   InjectElementLibrary();
   EXPECT_TRUE(IsLibReady());
   EXPECT_FALSE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::NONE),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::NONE),
             GetErrorCode());
 
   ExecuteScript(kTranslate);
 
   EXPECT_TRUE(GetError());
   EXPECT_EQ(
-      std::to_underlying(translate::TranslateErrors::UNEXPECTED_SCRIPT_ERROR),
+      base::to_underlying(translate::TranslateErrors::UNEXPECTED_SCRIPT_ERROR),
       GetErrorCode());
 }
 
@@ -203,13 +202,13 @@ TEST_F(TranslateScriptBrowserTest, CallbackGetBooleanError) {
   InjectElementLibrary();
   EXPECT_TRUE(IsLibReady());
   EXPECT_FALSE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::NONE),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::NONE),
             GetErrorCode());
 
   ExecuteScript(kTranslate);
 
   EXPECT_TRUE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::TRANSLATION_ERROR),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::TRANSLATION_ERROR),
             GetErrorCode());
 }
 
@@ -222,13 +221,13 @@ TEST_F(TranslateScriptBrowserTest, CallbackGetNumberError1) {
   InjectElementLibrary();
   EXPECT_TRUE(IsLibReady());
   EXPECT_FALSE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::NONE),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::NONE),
             GetErrorCode());
 
   ExecuteScript(kTranslate);
 
   EXPECT_TRUE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::TRANSLATION_ERROR),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::TRANSLATION_ERROR),
             GetErrorCode());
 }
 
@@ -241,14 +240,14 @@ TEST_F(TranslateScriptBrowserTest, CallbackGetNumberError2) {
   InjectElementLibrary();
   EXPECT_TRUE(IsLibReady());
   EXPECT_FALSE(GetError());
-  EXPECT_EQ(std::to_underlying(translate::TranslateErrors::NONE),
+  EXPECT_EQ(base::to_underlying(translate::TranslateErrors::NONE),
             GetErrorCode());
 
   ExecuteScript(kTranslate);
 
   EXPECT_TRUE(GetError());
   EXPECT_EQ(
-      std::to_underlying(translate::TranslateErrors::UNSUPPORTED_LANGUAGE),
+      base::to_underlying(translate::TranslateErrors::UNSUPPORTED_LANGUAGE),
       GetErrorCode());
 }
 
diff --git a/chrome/services/sharing/nearby/nearby_connections.cc b/chrome/services/sharing/nearby/nearby_connections.cc
index 4bdaa6bffc..66351aa98c 100644
--- a/chrome/services/sharing/nearby/nearby_connections.cc
+++ b/chrome/services/sharing/nearby/nearby_connections.cc
@@ -577,7 +577,7 @@ void NearbyConnections::RequestConnectionV3(
           [service_id];
   const std::string& endpoint_id = remote_device->endpoint_id;
 
-  if (endpoint_id_to_presence_device_map.contains(endpoint_id)) {
+  if (base::Contains(endpoint_id_to_presence_device_map, endpoint_id)) {
     CD_LOG(INFO, Feature::NEARBY_INFRA)
         << __func__ << "PresenceDevice already exists in map.";
   } else {
diff --git a/chrome/services/sharing/nearby/nearby_connections_stream_buffer_manager.cc b/chrome/services/sharing/nearby/nearby_connections_stream_buffer_manager.cc
index a3e70eb911..7429018dda 100644
--- a/chrome/services/sharing/nearby/nearby_connections_stream_buffer_manager.cc
+++ b/chrome/services/sharing/nearby/nearby_connections_stream_buffer_manager.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "components/cross_device/logging/logging.h"
 #include "third_party/nearby/src/internal/platform/exception.h"
 #include "third_party/nearby/src/internal/platform/input_stream.h"
@@ -34,7 +35,7 @@ void NearbyConnectionsStreamBufferManager::StartTrackingPayload(
 
 bool NearbyConnectionsStreamBufferManager::IsTrackingPayload(
     int64_t payload_id) const {
-  return id_to_payload_with_buffer_map_.contains(payload_id);
+  return base::Contains(id_to_payload_with_buffer_map_, payload_id);
 }
 
 void NearbyConnectionsStreamBufferManager::StopTrackingFailedPayload(
diff --git a/chrome/services/sharing/nearby/platform/ble_medium.cc b/chrome/services/sharing/nearby/platform/ble_medium.cc
index cae97903e7..84467de3ea 100644
--- a/chrome/services/sharing/nearby/platform/ble_medium.cc
+++ b/chrome/services/sharing/nearby/platform/ble_medium.cc
@@ -5,6 +5,7 @@
 #include "chrome/services/sharing/nearby/platform/ble_medium.h"
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "chrome/services/sharing/nearby/platform/bluetooth_device.h"
 
@@ -121,13 +122,13 @@ bool BleMedium::StartScanning(
 
   // The ID-to-UUID map should always be in sync with the callbacks map, and we
   // assume that the ID-UUID mapping is one-to-one.
-  DCHECK_EQ(
-      discovered_peripheral_callbacks_map_.contains(service_uuid),
-      discovery_service_id_to_fast_advertisement_service_uuid_map_.contains(
-          service_id));
+  DCHECK_EQ(base::Contains(discovered_peripheral_callbacks_map_, service_uuid),
+            base::Contains(
+                discovery_service_id_to_fast_advertisement_service_uuid_map_,
+                service_id));
 
   if (IsScanning() &&
-      discovered_peripheral_callbacks_map_.contains(service_uuid)) {
+      base::Contains(discovered_peripheral_callbacks_map_, service_uuid)) {
     LogStartScanningResult(true);
     return true;
   }
@@ -187,7 +188,7 @@ bool BleMedium::StopScanning(const std::string& service_id) {
           service_id);
   if (it !=
       discovery_service_id_to_fast_advertisement_service_uuid_map_.end()) {
-    DCHECK(discovered_peripheral_callbacks_map_.contains(it->second));
+    DCHECK(base::Contains(discovered_peripheral_callbacks_map_, it->second));
     discovered_peripheral_callbacks_map_.erase(it->second);
     discovery_service_id_to_fast_advertisement_service_uuid_map_.erase(it);
     for (auto& uuid_peripheral_pair : discovered_ble_peripherals_map_) {
diff --git a/chrome/services/sharing/nearby/platform/ble_v2_gatt_server.cc b/chrome/services/sharing/nearby/platform/ble_v2_gatt_server.cc
index 573c2544d5..63ce7214ea 100644
--- a/chrome/services/sharing/nearby/platform/ble_v2_gatt_server.cc
+++ b/chrome/services/sharing/nearby/platform/ble_v2_gatt_server.cc
@@ -6,6 +6,7 @@
 
 #include "base/check.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/notimplemented.h"
diff --git a/chrome/services/sharing/nearby/platform/bluetooth_classic_medium.cc b/chrome/services/sharing/nearby/platform/bluetooth_classic_medium.cc
index 8cd2879b12..a3bbf74a0d 100644
--- a/chrome/services/sharing/nearby/platform/bluetooth_classic_medium.cc
+++ b/chrome/services/sharing/nearby/platform/bluetooth_classic_medium.cc
@@ -5,6 +5,7 @@
 #include "chrome/services/sharing/nearby/platform/bluetooth_classic_medium.h"
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/notimplemented.h"
 #include "chrome/services/sharing/nearby/platform/bluetooth_server_socket.h"
@@ -252,7 +253,7 @@ void BluetoothClassicMedium::DeviceAdded(
   }
 
   const std::string& address = device->address;
-  if (discovered_bluetooth_devices_map_.contains(address)) {
+  if (base::Contains(discovered_bluetooth_devices_map_, address)) {
     auto& bluetooth_device = discovered_bluetooth_devices_map_.at(address);
     bool name_changed = device->name.has_value() &&
                         device->name.value() != bluetooth_device.GetName();
@@ -282,7 +283,7 @@ void BluetoothClassicMedium::DeviceRemoved(
   }
 
   const std::string& address = device->address;
-  if (!discovered_bluetooth_devices_map_.contains(address)) {
+  if (!base::Contains(discovered_bluetooth_devices_map_, address)) {
     return;
   }
 
diff --git a/chrome/services/sharing/nearby/test_support/fake_adapter.cc b/chrome/services/sharing/nearby/test_support/fake_adapter.cc
index 7811ce0c8e..ba2d2abf5a 100644
--- a/chrome/services/sharing/nearby/test_support/fake_adapter.cc
+++ b/chrome/services/sharing/nearby/test_support/fake_adapter.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "testing/gtest/include/gtest/gtest.h"
 
 namespace bluetooth {
@@ -183,8 +184,8 @@ void FakeAdapter::ConnectToServiceInsecurely(
     const device::BluetoothUUID& service_uuid,
     bool should_unbond_on_error,
     ConnectToServiceInsecurelyCallback callback) {
-  if (!allowed_connections_for_address_and_uuid_pair_.contains(
-          std::make_pair(address, service_uuid))) {
+  if (!base::Contains(allowed_connections_for_address_and_uuid_pair_,
+                      std::make_pair(address, service_uuid))) {
     std::move(callback).Run(/*result=*/nullptr);
     return;
   }
@@ -221,8 +222,8 @@ void FakeAdapter::CreateRfcommServiceInsecurely(
     const std::string& service_name,
     const device::BluetoothUUID& service_uuid,
     CreateRfcommServiceInsecurelyCallback callback) {
-  if (!allowed_connections_for_service_name_and_uuid_pair_.contains(
-          std::make_pair(service_name, service_uuid))) {
+  if (!base::Contains(allowed_connections_for_service_name_and_uuid_pair_,
+                      std::make_pair(service_name, service_uuid))) {
     std::move(callback).Run(/*server_socket=*/mojo::NullRemote());
     return;
   }
diff --git a/chrome/services/speech/cros_speech_recognition_recognizer_impl.cc b/chrome/services/speech/cros_speech_recognition_recognizer_impl.cc
index 65028a2f7e..a9b240758e 100644
--- a/chrome/services/speech/cros_speech_recognition_recognizer_impl.cc
+++ b/chrome/services/speech/cros_speech_recognition_recognizer_impl.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/services/speech/cros_speech_recognition_recognizer_impl.h"
 
-#include <algorithm>
 #include <string>
 
 #include "base/containers/flat_map.h"
@@ -88,8 +87,7 @@ CrosSpeechRecognitionRecognizerImpl::AddLiveCaptionLanguagesToConfig(
   for (const auto& config_path : config_paths) {
     if (config_path.first == primary_language_name) {
       continue;
-    } else if (!std::ranges::contains(live_caption_languages,
-                                      config_path.first)) {
+    } else if (!base::Contains(live_caption_languages, config_path.first)) {
       VLOG(1) << "Skipping multilang on captions of " << config_path.first
               << " as it is not listed as a live caption language.";
       continue;
diff --git a/chrome/test/base/ash/extension_js_browser_test.cc b/chrome/test/base/ash/extension_js_browser_test.cc
index b2c94d2f7d..3587e7f3e3 100644
--- a/chrome/test/base/ash/extension_js_browser_test.cc
+++ b/chrome/test/base/ash/extension_js_browser_test.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/callback.h"
 #include "base/json/json_reader.h"
@@ -72,7 +73,7 @@ void ExtensionJSBrowserTest::SetUpOnMainThread() {
         base::BindRepeating([](content::DevToolsAgentHost* host) {
           const auto& ext_ids = GetExtensionIdsToCollectCoverage();
           for (const auto& ext_id : ext_ids) {
-            if (host->GetURL().GetPath().contains(ext_id) &&
+            if (base::Contains(host->GetURL().GetPath(), ext_id) &&
                 host->GetType() == "background_page") {
               return true;
             }
diff --git a/chrome/test/base/ash/interactive/network/shill_service_util.cc b/chrome/test/base/ash/interactive/network/shill_service_util.cc
index 136f260f86..1b939094d5 100644
--- a/chrome/test/base/ash/interactive/network/shill_service_util.cc
+++ b/chrome/test/base/ash/interactive/network/shill_service_util.cc
@@ -4,9 +4,9 @@
 
 #include "chrome/test/base/ash/interactive/network/shill_service_util.h"
 
-#include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/strings/stringprintf.h"
 #include "chromeos/ash/components/dbus/shill/fake_shill_profile_client.h"
@@ -25,7 +25,7 @@ ShillServiceInfo::ShillServiceInfo(unsigned int id, std::string service_type)
   const std::array<std::string, 4> valid_service_types = {
       shill::kTypeWifi, shill::kTypeCellular, shill::kTypeEthernet,
       shill::kTypeVPN};
-  CHECK(std::ranges::contains(valid_service_types, service_type_));
+  CHECK(base::Contains(valid_service_types, service_type_));
 }
 
 ShillServiceInfo::~ShillServiceInfo() = default;
diff --git a/chrome/test/base/ash/web_ui_browser_test.cc b/chrome/test/base/ash/web_ui_browser_test.cc
index bb277e4c10..8053bdf497 100644
--- a/chrome/test/base/ash/web_ui_browser_test.cc
+++ b/chrome/test/base/ash/web_ui_browser_test.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -253,7 +254,7 @@ void BaseWebUIBrowserTest::PreLoadJavascriptLibraries(
   // process.
   auto global_frame_routing_id = preload_frame->GetGlobalId();
   ASSERT_FALSE(
-      libraries_preloaded_for_frames_.contains(global_frame_routing_id));
+      base::Contains(libraries_preloaded_for_frames_, global_frame_routing_id));
 
   RunJavascriptUsingHandler(
       "preloadJavascriptLibraries",
@@ -448,8 +449,8 @@ bool BaseWebUIBrowserTest::RunJavascriptUsingHandler(
   auto* frame_for_libraries = preload_frame
                                   ? preload_frame
                                   : test_handler_->GetRenderFrameHostForTest();
-  if (!libraries_preloaded_for_frames_.contains(
-          frame_for_libraries->GetGlobalId())) {
+  if (!base::Contains(libraries_preloaded_for_frames_,
+                      frame_for_libraries->GetGlobalId())) {
     if (!BuildJavascriptLibraries(&libraries)) {
       ADD_FAILURE() << "Failed to build JavaScript libraries";
       return false;
diff --git a/chrome/test/base/chromeos/crosier/annotations.cc b/chrome/test/base/chromeos/crosier/annotations.cc
index 831f2aad7f..0112d81f15 100644
--- a/chrome/test/base/chromeos/crosier/annotations.cc
+++ b/chrome/test/base/chromeos/crosier/annotations.cc
@@ -5,6 +5,7 @@
 #include "chrome/test/base/chromeos/crosier/annotations.h"
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/logging.h"
@@ -54,7 +55,7 @@ bool BoardHasUseFlag(const char* flag) {
   std::string use_flags;
   CHECK(base::ReadFileToString(
       base::FilePath("/usr/local/etc/tast_use_flags.txt"), &use_flags));
-  return use_flags.contains(flag);
+  return base::Contains(use_flags, flag);
 }
 
 }  // namespace
diff --git a/chrome/test/base/devtools_agent_coverage_observer.cc b/chrome/test/base/devtools_agent_coverage_observer.cc
index 3734ac9d85..f61dd50409 100644
--- a/chrome/test/base/devtools_agent_coverage_observer.cc
+++ b/chrome/test/base/devtools_agent_coverage_observer.cc
@@ -7,6 +7,7 @@
 #include <ostream>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
 #include "content/public/test/browser_test_utils.h"
@@ -91,7 +92,7 @@ bool DevToolsAgentCoverageObserver::ShouldForceDevToolsAgentHostCreation() {
 void DevToolsAgentCoverageObserver::DevToolsAgentHostCreated(
     DevToolsAgentHost* host) {
   VLOG(1) << host << ": Created";
-  CHECK(!devtools_agents_.contains(host));
+  CHECK(!base::Contains(devtools_agents_, host));
 
   if (should_inspect_callback_ && !should_inspect_callback_.Run(host)) {
     VLOG(1) << host << ": Not attaching";
@@ -130,7 +131,7 @@ void DevToolsAgentCoverageObserver::DevToolsAgentHostCrashed(
     DevToolsAgentHost* host,
     base::TerminationStatus status) {
   VLOG(1) << host << ": Crashed";
-  CHECK(!devtools_agents_.contains(host));
+  CHECK(!base::Contains(devtools_agents_, host));
 }
 
 void DevToolsAgentCoverageObserver::DevToolsAgentHostDestroyed(
diff --git a/chrome/test/base/testing_profile.cc b/chrome/test/base/testing_profile.cc
index 46df4c7eb1..5095af99aa 100644
--- a/chrome/test/base/testing_profile.cc
+++ b/chrome/test/base/testing_profile.cc
@@ -12,6 +12,7 @@
 
 #include "base/base_paths.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -677,7 +678,7 @@ void TestingProfile::DestroyOffTheRecordProfile(Profile* otr_profile) {
 
 bool TestingProfile::HasOffTheRecordProfile(
     const OTRProfileID& otr_profile_id) {
-  return otr_profiles_.contains(otr_profile_id);
+  return base::Contains(otr_profiles_, otr_profile_id);
 }
 
 bool TestingProfile::HasAnyOffTheRecordProfile() {
diff --git a/chrome/test/base/ui_test_utils.cc b/chrome/test/base/ui_test_utils.cc
index 089355f204..f4b9f78b87 100644
--- a/chrome/test/base/ui_test_utils.cc
+++ b/chrome/test/base/ui_test_utils.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -121,7 +122,7 @@ BrowserWindowInterface* WaitForBrowserNotInSet(
   if (!new_browser) {
     new_browser = WaitForBrowserToOpen();
     // The new browser should never be in |excluded_browsers|.
-    DCHECK(!excluded_browsers.contains(new_browser));
+    DCHECK(!base::Contains(excluded_browsers, new_browser));
   }
   return new_browser;
 }
diff --git a/chrome/test/chromedriver/capabilities.cc b/chrome/test/chromedriver/capabilities.cc
index 40d3391516..d432922e24 100644
--- a/chrome/test/chromedriver/capabilities.cc
+++ b/chrome/test/chromedriver/capabilities.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -258,7 +259,7 @@ Status ParseMobileEmulation(const base::Value& option,
     std::vector<std::string> supported_platforms =
         MobileDevice::GetReducedUserAgentPlatforms();
     if (!mobile_device.user_agent.has_value() &&
-        !std::ranges::contains(supported_platforms, client_hints.platform)) {
+        !base::Contains(supported_platforms, client_hints.platform)) {
       std::string supported_platforms_str =
           base::JoinString(supported_platforms, ", ");
       return Status(kInvalidArgument,
diff --git a/chrome/test/chromedriver/chrome/chrome_impl.cc b/chrome/test/chromedriver/chrome/chrome_impl.cc
index d33a038823..ae809f82ab 100644
--- a/chrome/test/chromedriver/chrome/chrome_impl.cc
+++ b/chrome/test/chromedriver/chrome/chrome_impl.cc
@@ -12,6 +12,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/threading/platform_thread.h"
 #include "base/time/time.h"
 #include "base/values.h"
@@ -157,7 +158,7 @@ Status ChromeImpl::GetTopLevelWebViewIds(std::list<std::string>* web_view_ids,
 }
 
 bool ChromeImpl::IsBrowserWindow(const WebViewInfo& view) const {
-  return window_types_.contains(view.type);
+  return base::Contains(window_types_, view.type);
 }
 
 Status ChromeImpl::UpdateWebViews(const WebViewsInfo& views_info,
diff --git a/chrome/test/chromedriver/chrome/device_manager.cc b/chrome/test/chromedriver/chrome/device_manager.cc
index 6cb7f3566d..f2fdab5b29 100644
--- a/chrome/test/chromedriver/chrome/device_manager.cc
+++ b/chrome/test/chromedriver/chrome/device_manager.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -260,7 +261,7 @@ Status DeviceManager::AcquireSpecificDevice(const std::string& device_serial,
   if (status.IsError())
     return status;
 
-  if (!std::ranges::contains(devices, device_serial)) {
+  if (!base::Contains(devices, device_serial)) {
     return Status(kUnknownError, "Device " + device_serial + " is not online");
   }
 
@@ -288,5 +289,5 @@ Device* DeviceManager::LockDevice(const std::string& device_serial) {
 }
 
 bool DeviceManager::IsDeviceLocked(const std::string& device_serial) {
-  return std::ranges::contains(active_devices_, device_serial);
+  return base::Contains(active_devices_, device_serial);
 }
diff --git a/chrome/test/chromedriver/commands.cc b/chrome/test/chromedriver/commands.cc
index da7617d894..ebcf13a955 100644
--- a/chrome/test/chromedriver/commands.cc
+++ b/chrome/test/chromedriver/commands.cc
@@ -11,6 +11,7 @@
 #include <list>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -340,7 +341,7 @@ void ExecuteSessionCommandOnSessionThread(
           if (status_tmp.IsError()) {
             status.AddDetails("failed to check if window was closed: " +
                               status_tmp.message());
-          } else if (!std::ranges::contains(tab_view_ids, session->window)) {
+          } else if (!base::Contains(tab_view_ids, session->window)) {
             status = Status(kOk);
           }
         }
diff --git a/chrome/test/chromedriver/logging.cc b/chrome/test/chromedriver/logging.cc
index 9b057686d8..5c55a8a4a9 100644
--- a/chrome/test/chromedriver/logging.cc
+++ b/chrome/test/chromedriver/logging.cc
@@ -15,6 +15,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/json/json_reader.h"
 #include "base/logging.h"
 #include "base/logging/logging_settings.h"
@@ -373,7 +374,8 @@ Status CreateLogs(
         logs.push_back(std::make_unique<WebDriverLog>(type, Log::kAll));
         devtools_listeners.push_back(std::make_unique<PerformanceLogger>(
             logs.back().get(), session, capabilities.perf_logging_prefs,
-            capabilities.window_types.contains(WebViewInfo::kServiceWorker)));
+            base::Contains(capabilities.window_types,
+                           WebViewInfo::kServiceWorker)));
         PerformanceLogger* perf_log =
             static_cast<PerformanceLogger*>(devtools_listeners.back().get());
         // We use a proxy for |perf_log|'s |CommandListener| interface.
diff --git a/chrome/test/chromedriver/server/chromedriver_server.cc b/chrome/test/chromedriver/server/chromedriver_server.cc
index 2eed2aee1b..c0cb3e80cb 100644
--- a/chrome/test/chromedriver/server/chromedriver_server.cc
+++ b/chrome/test/chromedriver/server/chromedriver_server.cc
@@ -18,6 +18,7 @@
 #include "base/at_exit.h"
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -86,7 +87,7 @@ void HandleRequestOnCmdThread(
     const HttpResponseSenderFunc& send_response_func) {
   if (!allowed_ips.empty()) {
     const net::IPAddress& peer_address = request.peer.address();
-    if (!std::ranges::contains(allowed_ips, peer_address)) {
+    if (!base::Contains(allowed_ips, peer_address)) {
       LOG(WARNING) << "unauthorized access from " << request.peer.ToString();
       std::unique_ptr<net::HttpServerResponseInfo> response(
           new net::HttpServerResponseInfo(net::HTTP_UNAUTHORIZED));
diff --git a/chrome/test/media_router/access_code_cast/access_code_cast_integration_browsertest.cc b/chrome/test/media_router/access_code_cast/access_code_cast_integration_browsertest.cc
index ea1c606000..da4c4149a3 100644
--- a/chrome/test/media_router/access_code_cast/access_code_cast_integration_browsertest.cc
+++ b/chrome/test/media_router/access_code_cast/access_code_cast_integration_browsertest.cc
@@ -424,7 +424,7 @@ AccessCodeCastIntegrationBrowserTest::CreateImpl() {
 
   ON_CALL(*cast_media_sink_service_impl, HasSink(_))
       .WillByDefault([this](const MediaSink::Id& sink_id) {
-        return added_sink_ids_.contains(sink_id);
+        return base::Contains(added_sink_ids_, sink_id);
       });
 
   // TODO(b/242777549): Properly delete the cast_media_sink_service_impl instead
diff --git a/chrome/test/media_router/media_router_e2e_browsertest.cc b/chrome/test/media_router/media_router_e2e_browsertest.cc
index e675b8fcfa..100c557869 100644
--- a/chrome/test/media_router/media_router_e2e_browsertest.cc
+++ b/chrome/test/media_router/media_router_e2e_browsertest.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/browser_finder.h"
@@ -114,7 +115,7 @@ void MediaRouterE2EBrowserTest::StopMediaRoute() {
 }
 
 bool MediaRouterE2EBrowserTest::IsSinkDiscovered() const {
-  return observer_->sink_map.contains(receiver_);
+  return base::Contains(observer_->sink_map, receiver_);
 }
 
 bool MediaRouterE2EBrowserTest::IsRouteCreated() const {
diff --git a/chrome/test/media_router/media_router_integration_browsertest.cc b/chrome/test/media_router/media_router_integration_browsertest.cc
index 056f8a45f1..b81b9a3b31 100644
--- a/chrome/test/media_router/media_router_integration_browsertest.cc
+++ b/chrome/test/media_router/media_router_integration_browsertest.cc
@@ -5,7 +5,6 @@
 #include "chrome/test/media_router/media_router_integration_browsertest.h"
 
 #include <memory>
-#include <utility>
 
 #include "base/command_line.h"
 #include "base/files/file_util.h"
@@ -17,6 +16,7 @@
 #include "base/strings/stringprintf.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/threading/thread_restrictions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "chrome/browser/media/router/media_router_feature.h"
 #include "chrome/browser/media/router/mojo/media_router_desktop.h"
@@ -135,7 +135,7 @@ void MediaRouterIntegrationBrowserTest::InitTestUi() {
       test_ui_ = std::make_unique<MediaRouterGmcUiForTest>(web_contents);
       break;
     default:
-      NOTREACHED() << std::to_underlying(test_ui_type_);
+      NOTREACHED() << base::to_underlying(test_ui_type_);
   }
 }
 
diff --git a/chrome/test/user_education/interactive_feature_promo_test_internal.cc b/chrome/test/user_education/interactive_feature_promo_test_internal.cc
index 2663a344e0..f53704d42d 100644
--- a/chrome/test/user_education/interactive_feature_promo_test_internal.cc
+++ b/chrome/test/user_education/interactive_feature_promo_test_internal.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/run_loop.h"
 #include "base/task/single_thread_task_runner.h"
@@ -222,7 +223,7 @@ void InteractiveFeaturePromoTestPrivate::MaybeWaitForTrackerInitialization(
 void InteractiveFeaturePromoTestPrivate::CreateServicesCallback(
     content::BrowserContext* context) {
   auto* const profile = Profile::FromBrowserContext(context);
-  if (profile_data_.contains(profile)) {
+  if (base::Contains(profile_data_, profile)) {
     return;
   }
   profile_data_.emplace(profile, ProfileData());
diff --git a/chrome/updater/enum_traits.h b/chrome/updater/enum_traits.h
index 86fff03751..f37df04edd 100644
--- a/chrome/updater/enum_traits.h
+++ b/chrome/updater/enum_traits.h
@@ -9,7 +9,8 @@
 #include <optional>
 #include <ostream>
 #include <type_traits>
-#include <utility>
+
+#include "base/types/cxx23_to_underlying.h"
 
 namespace updater {
 
@@ -46,8 +47,8 @@ struct EnumTraits {};
 template <typename T>
   requires(std::is_enum_v<T>)
 constexpr std::optional<T> CheckedCastToEnum(std::underlying_type_t<T> v) {
-  return (std::to_underlying(EnumTraits<T>::first_elem) <= v &&
-          v <= std::to_underlying(EnumTraits<T>::last_elem))
+  return (base::to_underlying(EnumTraits<T>::first_elem) <= v &&
+          v <= base::to_underlying(EnumTraits<T>::last_elem))
              ? std::make_optional(static_cast<T>(v))
              : std::nullopt;
 }
diff --git a/chrome/updater/handle_inconsistent_apps_task.cc b/chrome/updater/handle_inconsistent_apps_task.cc
index 8c36c57201..a6dc60c143 100644
--- a/chrome/updater/handle_inconsistent_apps_task.cc
+++ b/chrome/updater/handle_inconsistent_apps_task.cc
@@ -4,11 +4,11 @@
 
 #include "chrome/updater/handle_inconsistent_apps_task.h"
 
-#include <algorithm>
 #include <string>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -96,18 +96,18 @@ void HandleInconsistentAppsTask::FindUnregisteredApps(
     base::OnceClosure callback) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   MigrateLegacyUpdaters(
-      scope_,
-      base::BindRepeating(
-          [](scoped_refptr<PersistedData> persisted_data,
-             const RegistrationRequest& req) {
-            if (!std::ranges::contains(persisted_data->GetAppIds(),
-                                       base::ToLowerASCII(req.app_id)) &&
-                !req.app_id.empty()) {
-              VLOG(1) << "Registering app from legacy updater: " << req.app_id;
-              persisted_data->RegisterApp(req);
-            }
-          },
-          config_->GetUpdaterPersistedData()));
+      scope_, base::BindRepeating(
+                  [](scoped_refptr<PersistedData> persisted_data,
+                     const RegistrationRequest& req) {
+                    if (!base::Contains(persisted_data->GetAppIds(),
+                                        base::ToLowerASCII(req.app_id)) &&
+                        !req.app_id.empty()) {
+                      VLOG(1) << "Registering app from legacy updater: "
+                              << req.app_id;
+                      persisted_data->RegisterApp(req);
+                    }
+                  },
+                  config_->GetUpdaterPersistedData()));
   base::SequencedTaskRunner::GetCurrentDefault()->PostTask(FROM_HERE,
                                                            std::move(callback));
 }
diff --git a/chrome/updater/mac/keystone/agent_main.cc b/chrome/updater/mac/keystone/agent_main.cc
index fb86f8ca1d..410547351b 100644
--- a/chrome/updater/mac/keystone/agent_main.cc
+++ b/chrome/updater/mac/keystone/agent_main.cc
@@ -4,7 +4,6 @@
 
 #include <unistd.h>
 
-#include <algorithm>
 #include <iostream>
 #include <map>
 #include <optional>
@@ -14,6 +13,7 @@
 #include "base/at_exit.h"
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -114,8 +114,8 @@ void KSAgentApp::ChooseServiceForApp(
          base::OnceCallback<void(UpdaterScope)> callback,
          const std::vector<updater::UpdateService::AppState>& states) {
         std::move(callback).Run(
-            std::ranges::contains(states, base::ToLowerASCII(app_id),
-                                  &updater::UpdateService::AppState::app_id)
+            base::Contains(states, base::ToLowerASCII(app_id),
+                           &updater::UpdateService::AppState::app_id)
                 ? UpdaterScope::kSystem
                 : UpdaterScope::kUser);
       },
diff --git a/chrome/updater/policy/service.cc b/chrome/updater/policy/service.cc
index 5df336f093..0c08cde32c 100644
--- a/chrome/updater/policy/service.cc
+++ b/chrome/updater/policy/service.cc
@@ -15,6 +15,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -335,12 +336,11 @@ PolicyStatus<std::string> PolicyService::GetProxyMode() const {
       &PolicyManagerInterface::GetProxyMode,
       base::BindRepeating([](std::optional<std::string> proxy_mode) {
         return (proxy_mode.has_value() &&
-                std::ranges::contains(
-                    std::vector<std::string>(
-                        {kProxyModeDirect, kProxyModeSystem,
-                         kProxyModeFixedServers, kProxyModePacScript,
-                         kProxyModeAutoDetect}),
-                    base::ToLowerASCII(proxy_mode.value())))
+                base::Contains(std::vector<std::string>(
+                                   {kProxyModeDirect, kProxyModeSystem,
+                                    kProxyModeFixedServers, kProxyModePacScript,
+                                    kProxyModeAutoDetect}),
+                               base::ToLowerASCII(proxy_mode.value())))
                    ? proxy_mode
                    : std::nullopt;
       }));
diff --git a/chrome/updater/tag.cc b/chrome/updater/tag.cc
index fb521a61a6..d8eafeb660 100644
--- a/chrome/updater/tag.cc
+++ b/chrome/updater/tag.cc
@@ -15,6 +15,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/files/file.h"
 #include "base/files/file_path.h"
@@ -27,6 +28,7 @@
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected.h"
 #include "base/uuid.h"
 #include "build/build_config.h"
@@ -231,7 +233,7 @@ ErrorCode ParseBrowserType(std::string_view value, TagArgs& args) {
   }
 
   args.browser_type =
-      browser_type < std::to_underlying(TagArgs::BrowserType::kMax)
+      browser_type < base::to_underlying(TagArgs::BrowserType::kMax)
           ? TagArgs::BrowserType(browser_type)
           : TagArgs::BrowserType::kUnknown;
 
@@ -627,7 +629,7 @@ ErrorCode ParseAppInstallerDataArgs(std::string_view app_installer_data_args,
 
 // Checks that |args| does not contain |kDisallowedCharInTag|.
 bool IsValidArgs(std::string_view args) {
-  return !args.contains(kDisallowedCharInTag);
+  return !base::Contains(args, kDisallowedCharInTag);
 }
 
 // Returns a `uint16_t` value as big-endian bytes.
diff --git a/chrome/updater/test/integration_tests_impl.cc b/chrome/updater/test/integration_tests_impl.cc
index f6f76fde4d..9912dddfd9 100644
--- a/chrome/updater/test/integration_tests_impl.cc
+++ b/chrome/updater/test/integration_tests_impl.cc
@@ -17,6 +17,7 @@
 
 #include "base/base_paths.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -855,7 +856,7 @@ void ExpectAppsUpdateSequence(UpdaterScope scope,
   for (const AppUpdateExpectation& app : apps) {
     if ((app.should_update || app.always_serve_crx) &&
         (app.from_version != app.to_version) &&
-        !downloaded_crxes.contains(app.crx_relative_path)) {
+        !base::Contains(downloaded_crxes, app.crx_relative_path)) {
       // Download requests for apps that install/update
       const base::FilePath crx_path = exe_path.Append(app.crx_relative_path);
       ASSERT_TRUE(base::PathExists(crx_path));
@@ -1209,7 +1210,7 @@ void ExpectLogRotated(UpdaterScope scope) {
 }
 
 void ExpectRegistered(UpdaterScope scope, const std::string& app_id) {
-  ASSERT_TRUE(std::ranges::contains(
+  ASSERT_TRUE(base::Contains(
       base::MakeRefCounted<PersistedData>(
           scope, CreateGlobalPrefs(scope)->GetPrefService(), nullptr)
           ->GetAppIds(),
@@ -1217,7 +1218,7 @@ void ExpectRegistered(UpdaterScope scope, const std::string& app_id) {
 }
 
 void ExpectNotRegistered(UpdaterScope scope, const std::string& app_id) {
-  ASSERT_FALSE(std::ranges::contains(
+  ASSERT_FALSE(base::Contains(
       base::MakeRefCounted<PersistedData>(
           scope, CreateGlobalPrefs(scope)->GetPrefService(), nullptr)
           ->GetAppIds(),
@@ -1749,7 +1750,7 @@ bool VersionProcessFilter::Includes(const base::ProcessEntry& entry) const {
     return false;
   }
   const base::Version version(base::UTF16ToUTF8(version_info->file_version()));
-  return version.IsValid() && std::ranges::contains(versions_, version);
+  return version.IsValid() && base::Contains(versions_, version);
 }
 #endif  // BUILDFLAG(IS_WIN)
 
diff --git a/chrome/updater/test/request_matcher.cc b/chrome/updater/test/request_matcher.cc
index 998520afb5..ef1696be07 100644
--- a/chrome/updater/test/request_matcher.cc
+++ b/chrome/updater/test/request_matcher.cc
@@ -4,7 +4,6 @@
 
 #include "chrome/updater/test/request_matcher.h"
 
-#include <algorithm>
 #include <array>
 #include <optional>
 #include <string>
@@ -12,6 +11,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/json/json_reader.h"
 #include "base/logging.h"
@@ -181,7 +181,7 @@ Matcher GetAppPriorityMatcher(const std::string& app_id,
                     dict->FindString("installsource")) {
               static constexpr auto kInstallSources =
                   std::array{"ondemand", "taggedmi", "policy"};
-              return std::ranges::contains(kInstallSources, *install_source) ==
+              return base::Contains(kInstallSources, *install_source) ==
                      (priority == UpdateService::Priority::kForeground);
             }
           }
diff --git a/chrome/updater/tools/updater_util.cc b/chrome/updater/tools/updater_util.cc
index fe92069026..94afccc767 100644
--- a/chrome/updater/tools/updater_util.cc
+++ b/chrome/updater/tools/updater_util.cc
@@ -76,7 +76,7 @@ namespace updater_policy {
 namespace edm = ::wireless_android_enterprise_devicemanagement;
 
 std::ostream& operator<<(std::ostream& os, edm::UpdateValue value) {
-  os << std::to_underlying(value) << " ";
+  os << base::to_underlying(value) << " ";
   switch (value) {
     case edm::UPDATES_DISABLED:
       return os << "(Disabled)";
@@ -91,7 +91,7 @@ std::ostream& operator<<(std::ostream& os, edm::UpdateValue value) {
 }
 
 std::ostream& operator<<(std::ostream& os, edm::InstallDefaultValue value) {
-  os << std::to_underlying(value) << " ";
+  os << base::to_underlying(value) << " ";
   switch (value) {
     case edm::INSTALL_DEFAULT_DISABLED:
       return os << "(Disabled)";
@@ -104,7 +104,7 @@ std::ostream& operator<<(std::ostream& os, edm::InstallDefaultValue value) {
 }
 
 std::ostream& operator<<(std::ostream& os, edm::InstallValue value) {
-  os << std::to_underlying(value) << " ";
+  os << base::to_underlying(value) << " ";
   switch (value) {
     case edm::INSTALL_DISABLED:
       return os << "(Disabled)";
diff --git a/chrome/updater/update_service_impl_impl.cc b/chrome/updater/update_service_impl_impl.cc
index f64aa47855..4a8558fb55 100644
--- a/chrome/updater/update_service_impl_impl.cc
+++ b/chrome/updater/update_service_impl_impl.cc
@@ -17,6 +17,7 @@
 #include "base/barrier_callback.h"
 #include "base/barrier_closure.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/queue.h"
 #include "base/files/file_path.h"
@@ -1143,7 +1144,7 @@ void UpdateServiceImplImpl::UpdateAll(
 
   auto app_ids = config_->GetUpdaterPersistedData()->GetAppIds();
 
-  CHECK(std::ranges::contains(
+  CHECK(base::Contains(
       app_ids, base::ToLowerASCII(kUpdaterAppId),
       static_cast<std::string (*)(std::string_view)>(&base::ToLowerASCII)));
 
diff --git a/chrome/updater/util/util.h b/chrome/updater/util/util.h
index 953e77e846..e7cb07bfca 100644
--- a/chrome/updater/util/util.h
+++ b/chrome/updater/util/util.h
@@ -12,11 +12,11 @@
 #include <ostream>
 #include <string>
 #include <type_traits>
-#include <utility>
 #include <vector>
 
 #include "base/functional/callback_forward.h"
 #include "base/functional/function_ref.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/version.h"
 #include "build/build_config.h"
 #include "chrome/updater/registration_data.h"
@@ -59,7 +59,7 @@ template <std::unsigned_integral T>
 template <typename T>
   requires(std::is_enum_v<T>)
 inline std::ostream& operator<<(std::ostream& os, const T& e) {
-  return os << std::to_underlying(e);
+  return os << base::to_underlying(e);
 }
 
 // Returns the versioned install directory under which the program stores its
diff --git a/chrome/updater/win/installer_api.cc b/chrome/updater/win/installer_api.cc
index bf319fd9ad..21fe6d3e68 100644
--- a/chrome/updater/win/installer_api.cc
+++ b/chrome/updater/win/installer_api.cc
@@ -14,6 +14,7 @@
 
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -274,7 +275,7 @@ bool DeleteInstallerOutput(UpdaterScope updater_scope,
       delete_value(kRegValueInstallerResultUIString),
       delete_value(kRegValueInstallerSuccessLaunchCmdLine),
   };
-  return !std::ranges::contains(results, false);
+  return !base::Contains(results, false);
 }
 
 std::optional<InstallerOutcome> GetInstallerOutcome(UpdaterScope updater_scope,
diff --git a/chrome/updater/win/setup/uninstall.cc b/chrome/updater/win/setup/uninstall.cc
index b832e2c09b..d9641590bd 100644
--- a/chrome/updater/win/setup/uninstall.cc
+++ b/chrome/updater/win/setup/uninstall.cc
@@ -16,6 +16,7 @@
 
 #include "base/base_paths.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/logging.h"
diff --git a/chrome/utility/importer/bookmarks_file_importer.cc b/chrome/utility/importer/bookmarks_file_importer.cc
index 6603970fc5..9bef90dbec 100644
--- a/chrome/utility/importer/bookmarks_file_importer.cc
+++ b/chrome/utility/importer/bookmarks_file_importer.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -62,7 +63,7 @@ bool CanImportURL(const GURL& url) {
       }
     }
 
-    if (std::ranges::contains(chrome::ChromeDebugURLs(), fixed_url.spec())) {
+    if (base::Contains(chrome::ChromeDebugURLs(), fixed_url.spec())) {
       return true;
     }
 
diff --git a/chrome/utility/importer/ie_importer_win.cc b/chrome/utility/importer/ie_importer_win.cc
index 0d7cb23de4..153f462564 100644
--- a/chrome/utility/importer/ie_importer_win.cc
+++ b/chrome/utility/importer/ie_importer_win.cc
@@ -21,6 +21,7 @@
 #include <vector>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -522,8 +523,7 @@ void IEImporter::ImportHistory() {
 
       GURL url(base::AsStringPiece16(url_string));
       // Skips the URLs that are invalid or have other schemes.
-      if (!url.is_valid() ||
-          !std::ranges::contains(kSchemes, url.GetScheme())) {
+      if (!url.is_valid() || !base::Contains(kSchemes, url.GetScheme())) {
         continue;
       }
 
