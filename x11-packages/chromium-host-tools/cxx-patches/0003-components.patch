From 387468cf4b7b10ba262af2dd4256696c2cb7ed42 Mon Sep 17 00:00:00 2001
From: Chongyun Lee <licy183@termux.dev>
Date: Tue, 10 Mar 2026 01:59:06 +0800
Subject: [PATCH 3/8] components

---
 .../chromeos/account_manager_mojo_service.cc  |  9 +++--
 .../browser/affiliation_fetch_throttler.cc    |  2 +-
 .../core/browser/affiliation_service_impl.cc  |  6 ++-
 .../core/browser/sql_table_builder.cc         |  5 ++-
 .../aggregation_coordinator_utils.cc          |  3 +-
 .../browser/android_autofill_manager.cc       |  1 +
 components/app_restore/app_restore_info.cc    |  3 +-
 components/app_restore/arc_read_handler.cc    |  8 ++--
 components/app_restore/arc_save_handler.cc    |  5 ++-
 .../app_restore/full_restore_read_handler.cc  |  4 +-
 .../app_restore/full_restore_save_handler.cc  |  8 ++--
 components/assist_ranker/base_predictor.cc    |  3 +-
 components/attribution_reporting/filters.cc   |  3 +-
 .../autofill/content/browser/bad_message.cc   |  6 +--
 .../content_identity_credential_delegate.cc   |  1 +
 ...dentity_credential_suggestion_generator.cc | 15 +++-----
 .../content/renderer/autofill_agent.cc        |  2 +
 .../content/renderer/form_autofill_util.cc    | 33 ++++++++---------
 .../autofill/content/renderer/form_cache.cc   |  1 +
 .../autofill/content/renderer/form_tracker.cc | 11 +++---
 .../renderer/html_based_username_detector.cc  |  3 +-
 .../renderer/page_passwords_analyser.cc       |  5 ++-
 .../renderer/password_autofill_agent.cc       | 12 +++---
 .../renderer/password_generation_agent.cc     |  5 ++-
 .../autofill/core/browser/autofill_field.cc   | 10 +++--
 .../autofill_crowdsourcing_encoding.cc        |  2 +-
 .../payments/payments_data_manager.cc         | 21 ++++++-----
 .../data_model/addresses/autofill_i18n_api.cc |  9 +++--
 .../data_model/addresses/autofill_profile.cc  |  4 +-
 .../addresses/autofill_structured_address.cc  |  4 +-
 .../data_model/addresses/contact_info.cc      |  4 +-
 .../data_model/autofill_ai/entity_instance.h  |  4 +-
 .../data_model/autofill_ai/entity_type.cc     |  4 +-
 .../data_model/autofill_ai/entity_type.h      |  5 +--
 .../autofill_ai/transpile_entity_schema.py    |  8 ++--
 .../data_model/payments/credit_card.cc        |  8 ++--
 .../data_model/usage_history_information.cc   |  4 +-
 .../addresses/profile_requirement_utils.cc    | 11 +++---
 .../addresses/profile_token_quality.cc        | 23 ++++++------
 .../profile_token_quality_test_api.cc         |  4 +-
 .../data_quality/autofill_data_util.cc        |  5 ++-
 .../core/browser/data_quality/validation.cc   |  1 +
 .../autofill/core/browser/field_types.h       | 18 ++++-----
 ...ave_update_address_profile_delegate_ios.cc |  2 +-
 .../form_import/form_data_importer_utils.cc   | 11 +++---
 .../form_parsing/address_field_parser_ng.cc   |  4 +-
 .../form_parsing/credit_card_field_parser.cc  |  8 ++--
 .../form_processing/label_processing_util.cc  |  2 +-
 .../autofill/core/browser/form_structure.cc   |  3 +-
 .../form_structure_rationalization_engine.cc  |  4 +-
 .../browser/form_structure_rationalizer.cc    |  1 +
 .../core/browser/form_structure_test_api.h    |  1 +
 .../browser/foundations/autofill_driver.cc    |  8 ++--
 .../foundations/autofill_driver_router.cc     | 37 ++++++++++---------
 .../browser/foundations/autofill_manager.cc   |  5 ++-
 .../foundations/browser_autofill_manager.cc   | 30 ++++++++++-----
 .../core/browser/foundations/form_forest.cc   |  5 ++-
 .../foundations/test_autofill_client.h        |  4 +-
 .../foundations/test_autofill_driver.h        |  3 +-
 .../test_autofill_driver_factory.cc           |  6 +--
 .../geo/alternative_state_name_map_updater.cc |  6 +--
 .../core/browser/geo/autofill_country.cc      |  8 ++--
 .../autofill_ai/autofill_ai_labels.cc         |  3 +-
 .../autofill_ai/autofill_ai_manager.cc        |  9 +++--
 .../metrics/autofill_ai_ukm_logger.cc         | 16 ++++----
 .../one_time_tokens/otp_suggestion.cc         |  4 +-
 .../autofill_optimization_guide_decider.cc    | 13 ++++---
 .../core/browser/metrics/autofill_metrics.cc  | 12 +++---
 .../credit_card_form_event_logger.cc          |  4 +-
 .../metrics/form_interactions_ukm_logger.cc   | 11 +++---
 .../browser/metrics/profile_import_metrics.cc | 10 ++---
 .../core/browser/metrics/quality_metrics.cc   |  1 +
 .../field_classification_model_handler.cc     |  4 +-
 .../payments/autofill_offer_manager.cc        | 13 ++++---
 .../core/browser/payments/bnpl_manager.cc     | 16 ++++----
 .../payments_requests/payments_request.cc     |  3 +-
 .../autofill_ai_permission_utils.cc           | 11 +++---
 .../addresses/address_suggestion_generator.cc | 10 ++---
 .../autofill_ai_suggestion_generator.cc       |  4 +-
 .../payments/iban_suggestion_generator.cc     |  6 +--
 .../payments_suggestion_generator_util.cc     | 18 ++++-----
 .../core/browser/suggestions/suggestion.h     |  1 +
 .../ui/addresses/autofill_address_util.cc     |  3 +-
 .../browser/ui/autofill_external_delegate.cc  | 14 +++----
 .../webdata/autofill_ai/entity_table.cc       |  6 +--
 .../webdata/autofill_webdata_backend_impl.cc  |  1 +
 .../payments/payments_autofill_table.cc       |  2 +-
 .../payments/payments_sync_bridge_util.cc     |  9 +++--
 .../valuable_metadata_sync_bridge.cc          |  6 ++-
 .../core/common/autocomplete_parsing_util.cc  | 20 +++++++---
 .../core/common/autofill_data_validation.cc   | 10 ++---
 components/autofill/core/common/dense_set.h   |  4 +-
 .../autofill/core/common/form_field_data.cc   |  6 +--
 .../common/logging/stream_operator_util.h     |  3 +-
 .../ios/browser/autofill_driver_ios.mm        |  7 ++--
 .../background_fetch_delegate_base.cc         |  4 +-
 .../background_sync_controller_impl.cc        | 17 +++++----
 .../bookmarks/browser/bookmark_codec.cc       |  3 +-
 .../bookmarks/browser/bookmark_model.cc       |  1 +
 .../bookmarks/browser/bookmark_utils.cc       | 17 ++++-----
 .../android/website_preference_bridge.cc      |  5 ++-
 .../content/browsing_data_helper.cc           |  4 +-
 .../content/browsing_data_model.cc            |  4 +-
 .../content/local_storage_helper.cc           |  4 +-
 .../content/mock_cookie_helper.cc             |  3 +-
 .../core/browsing_data_policies_utils.cc      |  3 +-
 .../browsing_topics_calculator.cc             |  3 +-
 .../browsing_topics_page_load_data_tracker.cc |  1 +
 .../browsing_topics_redirect_observer.cc      |  4 +-
 components/browsing_topics/epoch_topics.cc    |  5 +--
 .../browser/permissions_manager_impl.cc       |  7 ++--
 .../streaming_receiver_session_client.cc      |  1 +
 components/cdm/common/playready_cdm_common.h  |  4 +-
 .../external_clear_key_key_system_info.cc     |  3 +-
 .../certificate_principal_pattern.cc          |  8 ++--
 .../chrome_require_ct_delegate.cc             |  1 +
 .../cloud_devices/common/description_items.h  |  6 +--
 .../internal/collaboration_finder_impl.cc     |  3 +-
 .../instant_message_processor_impl.cc         |  3 +-
 .../messaging_backend_service_impl.cc         |  1 +
 .../storage/messaging_backend_store_impl.cc   | 11 +++---
 .../tab_group_change_notifier_impl.cc         | 13 ++++---
 .../commerce/core/compare/cluster_manager.cc  |  8 ++--
 .../core/shopping_bookmark_model_observer.cc  |  3 +-
 components/commerce/core/shopping_service.cc  | 10 ++---
 .../core/shopping_service_test_base.cc        |  5 +--
 ...nent_updater_command_line_config_policy.cc | 13 +++----
 components/compose/core/browser/config.cc     |  5 +--
 .../response_header_verifier.cc               |  5 +--
 .../core/browser/content_settings_info.cc     |  5 ++-
 .../content_settings_policy_provider.cc       |  4 +-
 .../browser/content_settings_pref_provider.cc |  5 +--
 .../core/browser/content_settings_registry.cc |  3 +-
 .../core/browser/content_settings_utils.cc    |  4 +-
 .../core/browser/cookie_settings.cc           | 11 +++---
 .../core/browser/host_content_settings_map.cc |  5 ++-
 .../core/browser/permission_settings_info.cc  |  1 +
 .../browser/permission_settings_registry.cc   |  3 +-
 .../core/common/content_settings.cc           |  5 ++-
 .../internal/composite_context_decorator.cc   |  8 ++--
 .../renderer/search_result_extractor_impl.cc  |  9 +++--
 .../browser/child_exit_observer_android.cc    |  1 +
 .../cronet/url_request_context_config.cc      |  3 +-
 .../protocol_handler_registry.cc              |  7 ++--
 .../data_sharing/internal/group_data_model.cc |  3 +-
 components/dbus/menu/menu.cc                  |  5 ++-
 components/dbus/menu/menu_property_list.cc    |  3 +-
 components/dbus/properties/dbus_properties.cc |  1 +
 components/dbus/utils/call_method.h           |  5 ++-
 .../dbus/utils/check_for_service_and_start.cc |  3 +-
 .../desks_storage/core/desk_sync_bridge.cc    | 12 +++---
 .../core/desk_template_conversion.cc          | 10 ++---
 .../core/fake_desk_sync_bridge.cc             |  1 +
 .../core/local_desk_data_manager.cc           |  3 +-
 .../common/discardable_shared_memory_heap.cc  |  4 +-
 .../download_path_reservation_tracker.cc      |  3 +-
 .../common/in_progress_download_manager.cc    |  5 ++-
 components/drive/file_errors.cc               |  4 +-
 .../embedder_support/user_agent_utils.cc      |  3 +-
 .../core/certificate_provisioning_service.cc  |  4 +-
 .../core/realtime_reporting_client_base.cc    |  6 +--
 .../connectors/core/reporting_event_router.cc |  6 +--
 .../connectors/core/reporting_test_utils.cc   |  3 +-
 .../conditions/attributes_condition.cc        |  3 +-
 components/exo/keyboard.cc                    |  1 +
 .../exo/layer_tree_frame_sink_holder.cc       | 21 +++++------
 components/exo/shell_surface_base.cc          |  9 +++--
 components/exo/wayland/output_controller.cc   |  5 ++-
 .../test/integration/buffer_checker_test.cc   | 12 +++---
 .../wayland_dmabuf_feedback_manager.cc        | 25 ++++++-------
 .../exo/wayland/zcr_keyboard_configuration.cc |  3 +-
 components/exo/wayland/zcr_ui_controls.cc     |  2 +-
 components/exo/wayland/zwp_linux_dmabuf.cc    |  3 +-
 .../browser/pix_account_linking_manager.cc    |  5 +--
 .../favicon/core/core_favicon_service.cc      |  5 +--
 components/favicon/core/favicon_backend.cc    |  3 +-
 components/favicon/core/favicon_handler.cc    |  3 +-
 .../favicon_base/select_favicon_frames.cc     |  3 +-
 .../chrome_variations_configuration.cc        |  5 ++-
 .../feature_config_condition_validator.cc     |  5 ++-
 components/feed/core/v2/feed_stream.cc        |  3 +-
 components/feed/core/v2/scheduling.cc         |  7 ++--
 components/feed/feed_feature_list.cc          |  5 +--
 components/gcm_driver/account_tracker.cc      |  9 +++--
 .../public/media_session_item_producer.cc     | 15 ++++----
 .../views/media_item_ui_detailed_view.cc      |  4 +-
 .../public/views/media_item_ui_list_view.cc   |  9 +++--
 .../views/media_item_ui_updated_view.cc       | 20 ++++------
 .../public/views/media_item_ui_view.cc        |  6 ++-
 .../guest_view/browser/guest_view_manager.cc  |  7 ++--
 .../renderer/guest_view_container.cc          |  3 +-
 .../multi_process/test_driver.cc              |  6 +--
 .../history/core/browser/download_database.cc |  5 ++-
 .../history/core/browser/top_sites_impl.cc    |  5 ++-
 ...cluster_similarity_heuristics_processor.cc |  3 +-
 components/history_clusters/core/config.cc    |  9 ++---
 components/history_clusters/core/features.cc  |  1 +
 .../core/history_clusters_db_tasks.cc         |  7 ++--
 .../core/history_clusters_util.cc             |  3 +-
 .../core/keyword_cluster_finalizer.cc         |  1 +
 .../core/on_device_clustering_util.cc         |  4 +-
 components/infobars/core/infobar_container.cc |  3 +-
 .../browser/js_to_browser_messaging.cc        |  5 +--
 .../keyed_service/core/dependency_graph.cc    |  5 +--
 .../core/keyed_service_templated_factory.cc   |  1 +
 .../views/caption_bubble_controller_views.cc  |  5 ++-
 .../lookalikes/core/lookalike_url_util.cc     |  5 ++-
 components/manta/manta_service.cc             |  1 +
 .../media_notification_util.cc                |  4 +-
 .../media_notification_view_impl.cc           |  3 +-
 .../browser/media_sinks_observer.cc           |  5 +--
 ...ller_presentation_service_delegate_impl.cc | 12 +++---
 .../local_presentation_manager.cc             |  3 +-
 ...presentation_service_delegate_observers.cc |  3 +-
 .../providers/cast/cast_media_source.cc       |  3 +-
 .../cast/certificate/cast_cert_validator.cc   |  3 +-
 .../certificate/net_parsed_certificate.cc     |  5 +--
 .../cast/channel/cast_channel_enum.h          |  5 ++-
 .../cast/channel/cast_message_handler.cc      |  5 ++-
 .../metrics/expired_histograms_checker.cc     |  3 +-
 .../metrics/persistent_system_profile.cc      |  1 +
 .../metrics/stability_metrics_helper.cc       |  1 +
 .../test/test_metrics_service_client.cc       |  3 +-
 .../browser/network_session_configurator.cc   |  4 +-
 .../browser/no_state_prefetch_contents.cc     |  4 +-
 .../browser/no_state_prefetch_manager.cc      |  3 +-
 .../offline_pages/core/page_criteria.cc       | 11 +++---
 .../browser/autocomplete_grouper_groups.cc    |  3 +-
 .../browser/autocomplete_grouper_sections.cc  |  1 +
 .../omnibox/browser/autocomplete_result.cc    |  3 +-
 .../enterprise_search_aggregator_provider.cc  | 12 +++---
 .../omnibox/browser/history_fuzzy_provider.cc |  1 +
 .../omnibox/browser/keyword_provider.cc       |  1 +
 .../browser/on_device_head_provider.cc        |  3 +-
 .../browser/on_device_tail_model_executor.cc  | 12 +++---
 .../browser/search_suggestion_parser.cc       |  7 ++--
 .../translation_manager_util.cc               |  3 +-
 .../browser/page_content_metadata_observer.cc |  3 +-
 .../core/delivery/model_info.cc               |  1 +
 .../core/delivery/model_provider_registry.cc  |  3 +-
 .../core/delivery/prediction_manager.cc       |  1 +
 .../model_execution_features.cc               |  1 +
 .../model_execution_features_controller.cc    |  4 +-
 .../on_device_model_adaptation_loader.cc      |  5 +--
 .../on_device_model_component.cc              |  4 +-
 .../on_device_model_metadata.cc               |  1 +
 .../on_device_model_service_controller.cc     |  4 +-
 .../core/model_execution/performance_class.cc |  9 +++--
 .../core/model_execution/safety_config.cc     |  4 +-
 .../core/model_execution/session_impl.cc      |  1 +
 .../core/optimization_guide_features.cc       |  3 +-
 .../origin_trials/browser/origin_trials.cc    |  3 +-
 .../async/browser/test_secret_portal.cc       |  3 +-
 .../core/page_content_annotations_features.cc |  7 ++--
 .../core/page_content_annotations_service.cc  |  5 ++-
 .../core/page_visibility_model_handler.cc     |  1 +
 .../android/page_info_controller_android.cc   |  4 +-
 components/page_info/core/features.cc         |  3 +-
 .../browser/paint_preview_client.cc           |  7 ++--
 .../common/paint_preview_tracker.cc           |  6 ++-
 .../paint_preview/common/redaction_params.cc  |  5 ++-
 .../core/browser/fake_form_fetcher.cc         |  3 +-
 .../core/browser/form_fetcher_impl.cc         |  3 +-
 .../browser/form_parsing/form_data_parser.cc  |  3 +-
 .../core/browser/http_credentials_cleaner.cc  |  3 +-
 .../browser/password_form_metrics_recorder.cc | 13 +++++--
 .../core/browser/password_manager_util.cc     | 10 ++---
 .../browser/password_manual_fallback_flow.cc  |  7 ++--
 .../browser/password_reuse_detector_impl.cc   |  3 +-
 ...ogins_with_affiliations_request_handler.cc |  7 ++--
 .../browser/ui/saved_passwords_presenter.cc   |  1 +
 .../core/browser/votes_uploader.cc            |  1 +
 .../core/common/password_manager_util.cc      |  4 +-
 .../payments/content/manifest_verifier.cc     |  3 +-
 components/payments/content/payment_app.cc    |  3 +-
 .../payments/content/payment_request.cc       | 16 ++++----
 .../payments/content/payment_request_state.cc |  5 ++-
 ...secure_payment_confirmation_app_factory.cc |  5 ++-
 .../content/service_worker_payment_app.cc     | 12 +++---
 .../service_worker_payment_app_factory.cc     |  4 +-
 .../service_worker_payment_app_finder.cc      | 11 +++---
 .../core/payment_manifest_downloader.cc       |  4 +-
 .../core/payment_request_data_util.cc         |  3 +-
 components/pdf/browser/pdf_document_helper.cc |  5 ++-
 components/pdf/common/pdf_util.cc             |  5 +--
 .../pdf/renderer/pdf_accessibility_tree.cc    |  1 +
 .../browser_child_process_watcher.cc          |  7 +++-
 .../closing_page_voter.cc                     |  5 ++-
 .../force_foreground_voter.cc                 |  1 +
 .../freezing/freezing_policy.cc               | 12 +++---
 .../graph/frame_node_impl.cc                  |  1 +
 .../performance_manager/graph/graph_impl.cc   |  3 +-
 .../graph/process_node_impl.cc                |  5 ++-
 .../performance_manager/owned_objects.h       |  1 +
 .../performance_manager_tab_helper.cc         |  3 +-
 .../site_data/site_data_cache_factory.cc      |  9 +++--
 .../site_data/site_data_cache_impl.cc         |  3 +-
 .../context_collection.cc                     |  3 +-
 .../cpu_measurement_monitor.cc                |  2 +-
 .../memory_measurement_provider.cc            |  1 +
 .../scenarios/loading_scenario_data.cc        |  3 +-
 .../performance_manager_test_harness.cc       |  3 +-
 .../performance_manager/user_tuning/prefs.cc  |  1 +
 .../v8_memory/v8_detailed_memory_decorator.cc |  4 +-
 .../performance_manager/worker_watcher.cc     | 10 +++--
 .../object_permission_context_base.cc         |  3 +-
 .../permissions/permission_request_manager.cc |  5 ++-
 components/permissions/permission_util.cc     |  9 ++---
 components/permissions/request_type.cc        |  3 +-
 .../test/mock_permission_prompt_factory.cc    |  5 ++-
 .../metrics/plus_address_submission_logger.cc |  7 ++--
 .../policy/core/browser/policy_error_map.cc   |  9 +++--
 .../core/browser/policy_pref_mapping_test.cc  | 11 +++---
 .../core/common/cloud/cloud_policy_client.cc  |  1 +
 .../cloud/dmserver_job_configurations.cc      |  8 ++--
 .../encrypted_reporting_job_configuration.cc  |  3 +-
 .../core/common/config_dir_policy_loader.cc   |  3 +-
 .../core/common/local_test_policy_loader.cc   |  1 +
 components/policy/core/common/policy_map.cc   |  4 +-
 .../policy/core/common/policy_service_impl.cc |  3 +-
 .../remote_commands_service.cc                |  3 +-
 components/policy/core/common/schema.cc       |  7 ++--
 .../policy/test_support/client_storage.cc     |  4 +-
 .../request_handler_for_policy.cc             |  3 +-
 components/prefs/pref_registry.cc             |  1 +
 components/prefs/scoped_user_pref_update.cc   |  6 +--
 components/prefs/segregated_pref_store.cc     |  9 +++--
 .../browser/print_composite_client.cc         |  5 ++-
 .../common/cloud_print_cdd_conversion.cc      |  3 +-
 .../privacy_sandbox_attestations.cc           |  5 +--
 .../privacy_sandbox_settings_impl.cc          |  5 ++-
 .../proto_extras/proto_extras_plugin.cc       | 12 +++---
 .../proto_extras/proto_test_extras_plugin.cc  |  1 +
 .../regional_capabilities/program_settings.cc |  7 ++--
 .../regional_capabilities_service.cc          | 10 ++---
 .../metrics/fakes/fake_reporting_settings.cc  | 20 +++++-----
 .../safe_browsing_api_handler_bridge.cc       |  1 +
 .../content/browser/async_check_tracker.cc    |  8 ++--
 .../content/browser/base_ui_manager.cc        |  8 ++--
 .../browser/client_side_detection_host.cc     |  7 ++--
 .../browser/client_side_detection_service.cc  |  3 +-
 .../mock_safe_browsing_database_manager.cc    |  9 +++--
 ...fe_browsing_navigation_observer_manager.cc |  9 +++--
 .../content/browser/threat_details.cc         | 11 +++---
 .../browser/triggers/trigger_manager.cc       |  7 ++--
 .../browser/triggers/trigger_throttler.cc     |  3 +-
 .../db/v4_get_hash_protocol_manager.cc        | 11 +++---
 .../browser/db/v4_local_database_manager.cc   |  5 ++-
 .../hash_realtime_service.cc                  | 10 ++---
 .../core/browser/realtime/policy_engine.cc    |  5 +--
 .../core/browser/verdict_cache_manager.cc     |  8 ++--
 .../hash_realtime_utils.cc                    | 13 +++----
 .../internal/tab_group_sync_service_impl.cc   |  8 ++--
 .../search_engine_choice_utils.cc             |  8 ++--
 components/search_engines/template_url.cc     |  5 ++-
 .../security_interstitial_tab_helper.cc       |  7 ++--
 .../stateful_ssl_host_state_delegate.cc       | 12 +++---
 .../core/https_only_mode_allowlist.cc         |  5 ++-
 .../core/https_only_mode_enforcelist.cc       |  5 ++-
 .../home_modules_card_registry.cc             |  1 +
 .../training_data_collector_impl.cc           |  4 +-
 .../internal/database/signal_sample_view.cc   |  4 +-
 .../database/signal_storage_config.cc         |  6 ++-
 .../database/test_segment_info_database.cc    |  5 ++-
 .../internal/execution/mock_model_provider.cc |  3 +-
 .../selection/segment_selector_impl.cc        |  3 +-
 .../signals/histogram_signal_handler.cc       |  5 ++-
 .../public/cpp/app_registry_cache.cc          |  7 ++--
 .../public/cpp/app_registry_cache.h           |  1 +
 .../public/cpp/instance_registry.cc           |  3 +-
 .../public/cpp/preferred_apps_impl.cc         |  3 +-
 .../cpp/shortcut/shortcut_registry_cache.cc   |  3 +-
 .../cpp/shortcut/shortcut_registry_cache.h    |  1 +
 .../public/cpp/stub_icon_loader.cc            |  3 +-
 .../print_compositor/print_compositor_impl.cc |  7 ++--
 .../leveldb/local_storage_leveldb.cc          |  3 +-
 .../leveldb/session_storage_leveldb.cc        |  4 +-
 .../storage/dom_storage/local_storage_impl.cc |  1 +
 .../dom_storage/session_storage_impl.cc       |  5 ++-
 .../dom_storage/session_storage_metadata.cc   |  8 ++--
 .../sqlite/local_storage_sqlite.cc            |  3 +-
 .../sqlite/session_storage_sqlite.cc          |  3 +-
 .../public/cpp/filesystem/filesystem_impl.cc  |  3 +-
 .../service_worker_disk_cache.cc              |  7 ++--
 .../service_worker/service_worker_storage.cc  | 19 +++++-----
 .../service_worker_storage_control_impl.cc    |  5 ++-
 .../shared_storage_test_utils.cc              |  4 +-
 .../session_proto_db/session_proto_db.h       |  6 +--
 .../signin/core/browser/account_reconcilor.cc |  3 +-
 .../browser/account_reconcilor_delegate.cc    |  3 +-
 .../dice_account_reconcilor_delegate.cc       | 13 ++++---
 .../mirror_account_reconcilor_delegate.cc     |  1 +
 .../core/browser/signin_header_helper.cc      |  5 ++-
 .../identity_manager/account_info_util.cc     |  4 +-
 .../account_tracker_service.cc                | 25 +++++++------
 .../gaia_cookie_manager_service.cc            |  9 +++--
 .../oauth_multilogin_token_fetcher.cc         |  6 +--
 ...e_oauth2_token_service_delegate_android.cc |  7 ++--
 ..._oauth2_token_service_delegate_chromeos.cc |  8 ++--
 .../identity_manager/token_binding_helper.cc  |  3 +-
 components/signin/public/base/signin_prefs.cc |  3 +-
 .../account_capabilities_test_mutator.cc      |  4 +-
 .../site_isolation/site_isolation_policy.cc   |  1 +
 components/soda/soda_installer.cc             | 20 +++++-----
 .../soda/soda_installer_impl_chromeos.cc      |  1 +
 .../spellcheck/common/spellcheck_common.cc    |  4 +-
 .../mtp_manager_client_chromeos.cc            |  3 +-
 components/storage_monitor/storage_monitor.cc |  3 +-
 .../storage_monitor_chromeos.cc               |  9 +++--
 .../storage_monitor/storage_monitor_linux.cc  |  6 ++-
 .../storage_monitor/transient_device_ids.cc   |  5 ++-
 ...ent_subresource_filter_throttle_manager.cc | 33 +++++++++--------
 .../subresource_filter_observer_test_utils.cc |  3 +-
 .../browser/subresource_filter_features.cc    |  3 +-
 .../sync/service/data_type_manager_impl.cc    |  9 +++--
 .../sync/service/device_statistics_tracker.cc |  4 +-
 components/sync/service/sync_prefs.cc         |  1 +
 .../sync/test/mock_connection_manager.cc      |  4 +-
 components/sync/test/test_sync_service.cc     |  1 +
 .../sync_bookmarks/bookmark_model_merger.cc   |  1 +
 .../bookmark_specifics_conversions.cc         |  4 +-
 .../sync_preferences/pref_model_associator.cc |  1 +
 .../preferences_merge_helper.cc               |  5 +--
 .../tab_groups/tab_group_visual_data.cc       |  3 +-
 .../content/android/translate_message.cc      |  6 +--
 .../translate/core/browser/translate_prefs.cc |  9 +++--
 .../translate_internals_handler.cc            |  3 +-
 .../test/fake_trusted_vault_client.cc         |  4 +-
 .../views/ui_element_with_metadata.cc         |  6 +--
 components/ui_devtools/views/view_element.cc  | 10 ++---
 .../observers/ukm_consent_state_observer.cc   |  5 ++-
 components/ukm/ukm_recorder_impl.cc           | 11 +++---
 components/ukm/ukm_test_helper.cc             |  6 ++-
 .../net/url_loader_post_interceptor.cc        |  7 ++--
 components/update_client/update_client.cc     |  5 ++-
 components/update_client/utils.cc             |  3 +-
 .../spoof_checks/idn_spoof_checker.cc         |  6 +--
 components/url_matcher/url_matcher.cc         | 13 ++++---
 .../url_pattern_index/url_pattern_index.cc    |  3 +-
 .../feature_promo/feature_promo_controller.cc |  3 +-
 .../feature_promo/feature_promo_controller.h  |  6 +--
 .../feature_promo/feature_promo_lifecycle.cc  |  1 +
 .../feature_promo/feature_promo_registry.cc   |  1 +
 .../feature_promo/feature_promo_registry.h    |  1 +
 .../common/ntp_promo/ntp_promo_controller.cc  |  5 +--
 .../common/ntp_promo/ntp_promo_order.cc       |  3 +-
 .../common/ntp_promo/ntp_promo_registry.cc    |  1 +
 .../common/product_messaging_controller.cc    |  6 +--
 .../common/tutorial/tutorial_registry.cc      |  5 ++-
 .../webui/help_bubble_handler.cc              |  3 +-
 .../webui/whats_new_registry.cc               |  1 +
 .../value_store/test_value_store_factory.cc   |  3 +-
 components/variations/active_field_trials.cc  |  3 +-
 .../child_process_field_trial_syncer.cc       |  3 +-
 .../variations/field_trial_internals_utils.cc |  3 +-
 components/variations/processed_study.cc      |  3 +-
 .../service/limited_entropy_randomization.cc  |  5 +--
 components/variations/study_filtering.cc      | 29 +++++++--------
 .../variations/synthetic_trial_registry.cc    | 11 +++---
 .../variations/variations_ids_provider.cc     |  7 ++--
 .../history_url_visit_data_fetcher.cc         |  3 +-
 .../session_url_visit_data_fetcher.cc         |  6 +--
 .../url_grouping/group_suggestions_manager.cc |  7 ++--
 .../url_grouping/grouping_heuristics.cc       |  8 ++--
 .../viz/client/frame_eviction_manager.cc      |  5 ++-
 components/viz/client/frame_evictor.cc        |  4 +-
 components/viz/common/features.cc             |  2 +-
 .../common/frame_sinks/begin_frame_source.cc  | 15 ++++----
 .../viz/common/performance_hint_utils.cc      |  4 +-
 .../common/quads/aggregated_render_pass.cc    |  4 +-
 components/viz/common/resources/resource_id.h |  1 +
 .../viz/host/host_frame_sink_manager.cc       |  4 +-
 .../display/display_resource_provider_skia.cc |  6 +--
 .../display/overlay_candidate_factory.cc      |  4 +-
 .../display_embedder/output_device_backing.cc |  3 +-
 .../compositor_frame_sink_support.cc          |  3 +-
 .../viz/service/frame_sinks/frame_counter.cc  |  3 +-
 .../frame_sinks/frame_sink_manager_impl.cc    | 28 +++++++-------
 .../viz/service/input/mock_input_manager.cc   |  2 +-
 .../service/performance_hint/hint_session.cc  |  1 +
 components/viz/service/surfaces/surface.cc    |  3 +-
 .../transferable_resource_tracker.cc          |  7 ++--
 components/viz/test/test_gles2_interface.cc   |  7 ++--
 components/viz/test/test_gles2_interface.h    |  1 +
 .../browser/walletable_permission_utils.cc    |  3 +-
 components/web_package/input_reader.cc        | 13 +++----
 .../attribute_map_parser.cc                   |  3 +-
 .../browser/banners/app_banner_manager.cc     |  1 +
 .../installable/installable_evaluator.cc      |  2 -
 .../installable/installable_manager.cc        |  1 +
 .../browser/installable/installable_task.cc   |  5 +--
 .../reading/signed_web_bundle_reader.cc       |  5 ++-
 .../core/browser/passkey_sync_bridge.cc       |  5 ++-
 components/webui/flags/flags_state.cc         | 19 +++++-----
 components/webui/flags/flags_test_helpers.cc  |  4 +-
 495 files changed, 1566 insertions(+), 1272 deletions(-)

diff --git a/components/account_manager_core/chromeos/account_manager_mojo_service.cc b/components/account_manager_core/chromeos/account_manager_mojo_service.cc
index 3d5802883c..64531a642d 100644
--- a/components/account_manager_core/chromeos/account_manager_mojo_service.cc
+++ b/components/account_manager_core/chromeos/account_manager_mojo_service.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
@@ -278,10 +279,10 @@ void AccountManagerMojoService::MaybeNotifyAuthErrorObservers(
     const account_manager::AccountKey& account_key,
     const GoogleServiceAuthError& error,
     const std::vector<account_manager::Account>& known_accounts) {
-  if (!std::ranges::contains(known_accounts, account_key,
-                             [](const account_manager::Account& account) {
-                               return account.key;
-                             })) {
+  if (!base::Contains(known_accounts, account_key,
+                      [](const account_manager::Account& account) {
+                        return account.key;
+                      })) {
     // Ignore if the account is not known.
     return;
   }
diff --git a/components/affiliations/core/browser/affiliation_fetch_throttler.cc b/components/affiliations/core/browser/affiliation_fetch_throttler.cc
index 73e4331dcb..c3629e19c3 100644
--- a/components/affiliations/core/browser/affiliation_fetch_throttler.cc
+++ b/components/affiliations/core/browser/affiliation_fetch_throttler.cc
@@ -34,7 +34,7 @@ constexpr auto kRetryableHttpErrors =
 bool IsHttpErrorNonRetryable(
     std::optional<net::HttpStatusCode> response_code_optional) {
   return response_code_optional.has_value() &&
-         !kRetryableHttpErrors.contains(response_code_optional.value());
+         !base::Contains(kRetryableHttpErrors, response_code_optional.value());
 }
 }  // namespace
 // static
diff --git a/components/affiliations/core/browser/affiliation_service_impl.cc b/components/affiliations/core/browser/affiliation_service_impl.cc
index 40761a40a0..8e0ae5731d 100644
--- a/components/affiliations/core/browser/affiliation_service_impl.cc
+++ b/components/affiliations/core/browser/affiliation_service_impl.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -200,7 +201,8 @@ void AffiliationServiceImpl::PrefetchChangePasswordURL(
     const GURL& url,
     base::OnceClosure callback) {
   FacetURI facet_uri = ConvertGURLToFacet(url);
-  if (!facet_uri.is_valid() || change_password_urls_.contains(facet_uri)) {
+  if (!facet_uri.is_valid() ||
+      base::Contains(change_password_urls_, facet_uri)) {
     base::SequencedTaskRunner::GetCurrentDefault()->PostTask(
         FROM_HERE, std::move(callback));
     return;
@@ -224,7 +226,7 @@ GURL AffiliationServiceImpl::GetChangePasswordURL(const GURL& url) const {
     return it->second.change_password_url;
   }
   auto requested_facet_uris = fetcher_manager_->GetRequestedFacetURIs();
-  if (std::ranges::contains(requested_facet_uris, uri)) {
+  if (base::Contains(requested_facet_uris, uri)) {
     LogFetchResult(GetChangePasswordUrlMetric::kNotFetchedYet);
   } else {
     LogFetchResult(GetChangePasswordUrlMetric::kNoUrlOverrideAvailable);
diff --git a/components/affiliations/core/browser/sql_table_builder.cc b/components/affiliations/core/browser/sql_table_builder.cc
index 97b5ab2e4f..37afa986e2 100644
--- a/components/affiliations/core/browser/sql_table_builder.cc
+++ b/components/affiliations/core/browser/sql_table_builder.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/strings/strcat.h"
@@ -152,7 +153,7 @@ void SQLTableBuilder::RenameColumn(const std::string& old_name,
   // Check there is no index in the current version that references |old_name|.
   DCHECK(std::ranges::none_of(indices_, [&old_name](const Index& index) {
     return index.max_version == kInvalidVersion &&
-           std::ranges::contains(index.columns, old_name);
+           base::Contains(index.columns, old_name);
   }));
   // Migrating a foreign key can be supported but the index on it is to be
   // updated.
@@ -188,7 +189,7 @@ void SQLTableBuilder::DropColumn(const std::string& name) {
   // Check there is no index in the current version that references |old_name|.
   DCHECK(std::ranges::none_of(indices_, [&name](const Index& index) {
     return index.max_version == kInvalidVersion &&
-           std::ranges::contains(index.columns, name);
+           base::Contains(index.columns, name);
   }));
   if (sealed_version_ != kInvalidVersion &&
       column->min_version <= sealed_version_) {
diff --git a/components/aggregation_service/aggregation_coordinator_utils.cc b/components/aggregation_service/aggregation_coordinator_utils.cc
index 28789e9a46..a4cd26bef3 100644
--- a/components/aggregation_service/aggregation_coordinator_utils.cc
+++ b/components/aggregation_service/aggregation_coordinator_utils.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_split.h"
 #include "components/aggregation_service/features.h"
@@ -67,7 +68,7 @@ class CoordinatorOrigins {
 
   bool contains(const url::Origin& origin) const {
     CHECK(IsValid());
-    return std::ranges::contains(origins_, origin);
+    return base::Contains(origins_, origin);
   }
 
   const url::Origin& default_origin() const {
diff --git a/components/android_autofill/browser/android_autofill_manager.cc b/components/android_autofill/browser/android_autofill_manager.cc
index 30808390d0..6602c67f5a 100644
--- a/components/android_autofill/browser/android_autofill_manager.cc
+++ b/components/android_autofill/browser/android_autofill_manager.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/notreached.h"
 #include "components/android_autofill/browser/android_form_event_logger.h"
diff --git a/components/app_restore/app_restore_info.cc b/components/app_restore/app_restore_info.cc
index 9a7148f539..772e9beed9 100644
--- a/components/app_restore/app_restore_info.cc
+++ b/components/app_restore/app_restore_info.cc
@@ -4,6 +4,7 @@
 
 #include "components/app_restore/app_restore_info.h"
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "components/account_id/account_id.h"
 
@@ -28,7 +29,7 @@ void AppRestoreInfo::RemoveObserver(Observer* observer) {
 }
 
 bool AppRestoreInfo::CanPerformRestore(const AccountId& account_id) {
-  return restore_prefs_.contains(account_id);
+  return base::Contains(restore_prefs_, account_id);
 }
 
 void AppRestoreInfo::SetRestorePref(const AccountId& account_id,
diff --git a/components/app_restore/arc_read_handler.cc b/components/app_restore/arc_read_handler.cc
index 4a62954dc2..51a412fc83 100644
--- a/components/app_restore/arc_read_handler.cc
+++ b/components/app_restore/arc_read_handler.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "components/app_restore/app_launch_info.h"
 #include "components/app_restore/app_restore_info.h"
 #include "components/app_restore/app_restore_utils.h"
@@ -38,7 +39,8 @@ void ArcReadHandler::AddRestoreData(const std::string& app_id,
 }
 
 void ArcReadHandler::AddArcWindowCandidate(aura::Window* window) {
-  if (!task_id_to_window_id_.contains(window->GetProperty(kWindowIdKey))) {
+  if (!base::Contains(task_id_to_window_id_,
+                      window->GetProperty(kWindowIdKey))) {
     // Check `session_id` to see whether this is a ghost window.
     int32_t session_id = window->GetProperty(kGhostWindowSessionIdKey);
     if (session_id >= kArcSessionIdOffsetForRestoredLaunching)
@@ -97,7 +99,7 @@ void ArcReadHandler::OnTaskDestroyed(int32_t task_id) {
 }
 
 bool ArcReadHandler::HasRestoreData(int32_t window_id) {
-  return window_id_to_app_id_.contains(window_id);
+  return base::Contains(window_id_to_app_id_, window_id);
 }
 
 std::unique_ptr<AppLaunchInfo> ArcReadHandler::GetArcAppLaunchInfo(
@@ -151,7 +153,7 @@ int32_t ArcReadHandler::GetArcRestoreWindowIdForTaskId(int32_t task_id) {
   // launched. If `not_restored_task_ids_` has `task_id`, that means the ARC app
   // window is not restored.
   if (session_id_to_window_id_.empty() ||
-      not_restored_task_ids_.contains(task_id)) {
+      base::Contains(not_restored_task_ids_, task_id)) {
     return 0;
   }
 
diff --git a/components/app_restore/arc_save_handler.cc b/components/app_restore/arc_save_handler.cc
index 0b588795d3..5c97da22f8 100644
--- a/components/app_restore/arc_save_handler.cc
+++ b/components/app_restore/arc_save_handler.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "components/app_restore/app_launch_info.h"
 #include "components/app_restore/app_restore_info.h"
 #include "components/app_restore/app_restore_utils.h"
@@ -38,7 +39,7 @@ void ArcSaveHandler::SaveAppLaunchInfo(AppLaunchInfoPtr app_launch_info) {
 
   // If the ghost window has been created for `session_id`, and the launch info
   // hasn't been added yet, add `app_launch_info` to the restore data.
-  if (ghost_window_session_id_to_app_id_.contains(session_id)) {
+  if (base::Contains(ghost_window_session_id_to_app_id_, session_id)) {
     if (!FullRestoreSaveHandler::GetInstance()->HasAppRestoreData(
             profile_path_, app_launch_info->app_id, session_id)) {
       app_launch_info->window_id = session_id;
@@ -90,7 +91,7 @@ void ArcSaveHandler::ModifyWindowInfo(
 
 void ArcSaveHandler::OnWindowInitialized(aura::Window* window) {
   int32_t task_id = window->GetProperty(app_restore::kWindowIdKey);
-  if (!task_id_to_app_id_.contains(task_id)) {
+  if (!base::Contains(task_id_to_app_id_, task_id)) {
     // Check `session_id` to see whether this is a ghost window.
     int32_t session_id =
         window->GetProperty(app_restore::kGhostWindowSessionIdKey);
diff --git a/components/app_restore/full_restore_read_handler.cc b/components/app_restore/full_restore_read_handler.cc
index 20b5b07fa2..067e8b8f8c 100644
--- a/components/app_restore/full_restore_read_handler.cc
+++ b/components/app_restore/full_restore_read_handler.cc
@@ -223,7 +223,7 @@ bool FullRestoreReadHandler::HasBrowser(const base::FilePath& profile_path) {
 
 bool FullRestoreReadHandler::HasWindowInfo(int32_t restore_window_id) {
   if (!SessionID::IsValidValue(restore_window_id) ||
-      !should_check_restore_data_.contains(active_profile_path_)) {
+      !base::Contains(should_check_restore_data_, active_profile_path_)) {
     return false;
   }
 
@@ -254,7 +254,7 @@ std::unique_ptr<app_restore::WindowInfo> FullRestoreReadHandler::GetWindowInfo(
 std::unique_ptr<app_restore::WindowInfo>
 FullRestoreReadHandler::GetWindowInfoForActiveProfile(
     int32_t restore_window_id) {
-  if (!should_check_restore_data_.contains(active_profile_path_))
+  if (!base::Contains(should_check_restore_data_, active_profile_path_))
     return nullptr;
   return GetWindowInfo(restore_window_id);
 }
diff --git a/components/app_restore/full_restore_save_handler.cc b/components/app_restore/full_restore_save_handler.cc
index ddb4b016d9..edcaded4ba 100644
--- a/components/app_restore/full_restore_save_handler.cc
+++ b/components/app_restore/full_restore_save_handler.cc
@@ -86,7 +86,7 @@ void FullRestoreSaveHandler::AllowSave() {
     wait_timer_.Stop();
 
   if (!is_shut_down_ &&
-      pending_save_profile_paths_.contains(active_profile_path_)) {
+      base::Contains(pending_save_profile_paths_, active_profile_path_)) {
     MaybeStartSaveTimer(active_profile_path_);
   }
 }
@@ -165,7 +165,7 @@ void FullRestoreSaveHandler::OnWindowInitialized(aura::Window* window) {
         }
       }
 
-      if (ignore_applications_ids_.contains(app_launch_info->app_id)) {
+      if (base::Contains(ignore_applications_ids_, app_launch_info->app_id)) {
         return;
       }
     }
@@ -563,7 +563,7 @@ void FullRestoreSaveHandler::ClearForTesting() {
 
 void FullRestoreSaveHandler::MaybeStartSaveTimer(
     const base::FilePath& profile_path) {
-  if (!been_read_profile_paths_.contains(profile_path)) {
+  if (!base::Contains(been_read_profile_paths_, profile_path)) {
     // FullRestoreSaveHandler might be called to save the help app before
     // FullRestoreAppLaunchHandler reads the full restore data from the full
     // restore file during the system startup phase, e.g. when a new user login.
@@ -592,7 +592,7 @@ void FullRestoreSaveHandler::MaybeStartSaveTimer(
 
 void FullRestoreSaveHandler::Save() {
   if (is_shut_down_ ||
-      !pending_save_profile_paths_.contains(active_profile_path_)) {
+      !base::Contains(pending_save_profile_paths_, active_profile_path_)) {
     return;
   }
 
diff --git a/components/assist_ranker/base_predictor.cc b/components/assist_ranker/base_predictor.cc
index 7de8a1623d..86eee2ad57 100644
--- a/components/assist_ranker/base_predictor.cc
+++ b/components/assist_ranker/base_predictor.cc
@@ -4,6 +4,7 @@
 
 #include "components/assist_ranker/base_predictor.h"
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "components/assist_ranker/proto/ranker_example.pb.h"
 #include "components/assist_ranker/proto/ranker_model.pb.h"
@@ -58,7 +59,7 @@ void BasePredictor::LogFeatureToUkm(const std::string& feature_name,
                                     ukm::UkmEntryBuilder* ukm_builder) {
   DCHECK(ukm_builder);
 
-  if (!config_.feature_allowlist->contains(feature_name)) {
+  if (!base::Contains(*config_.feature_allowlist, feature_name)) {
     DVLOG(1) << "Feature not allowed: " << feature_name;
     return;
   }
diff --git a/components/attribution_reporting/filters.cc b/components/attribution_reporting/filters.cc
index 1c54ac4dba..dc856b5ae6 100644
--- a/components/attribution_reporting/filters.cc
+++ b/components/attribution_reporting/filters.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/notreached.h"
 #include "base/strings/string_util.h"
@@ -272,7 +273,7 @@ bool FilterData::Matches(mojom::SourceType source_type,
 
           bool has_intersection = std::ranges::any_of(
               trigger_filter.second, [&](const std::string& value) {
-                return std::ranges::contains(source_filter->second, value);
+                return base::Contains(source_filter->second, value);
               });
           // Negating filters are considered matched if the intersection of
           // the filter values is empty.
diff --git a/components/autofill/content/browser/bad_message.cc b/components/autofill/content/browser/bad_message.cc
index d42aac6262..474ddfce1b 100644
--- a/components/autofill/content/browser/bad_message.cc
+++ b/components/autofill/content/browser/bad_message.cc
@@ -4,8 +4,7 @@
 
 #include "components/autofill/content/browser/bad_message.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "components/autofill/core/common/aliases.h"
 #include "components/autofill/core/common/form_data.h"
 #include "components/autofill/core/common/form_field_data.h"
@@ -28,8 +27,7 @@ bool CheckSingleValidTriggerSource(
 }
 
 bool CheckFieldInForm(const FormData& form, FieldRendererId field_id) {
-  if (!std::ranges::contains(form.fields(), field_id,
-                             &FormFieldData::renderer_id)) {
+  if (!base::Contains(form.fields(), field_id, &FormFieldData::renderer_id)) {
     mojo::ReportBadMessage("Unexpected FormData/FieldRendererId pair received");
     return false;
   }
diff --git a/components/autofill/content/browser/content_identity_credential_delegate.cc b/components/autofill/content/browser/content_identity_credential_delegate.cc
index ddbccee59e..861710ae03 100644
--- a/components/autofill/content/browser/content_identity_credential_delegate.cc
+++ b/components/autofill/content/browser/content_identity_credential_delegate.cc
@@ -4,6 +4,7 @@
 
 #include "components/autofill/content/browser/content_identity_credential_delegate.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
diff --git a/components/autofill/content/browser/suggestions/identity_credential_suggestion_generator.cc b/components/autofill/content/browser/suggestions/identity_credential_suggestion_generator.cc
index 7e2b40a165..4a154c7c9b 100644
--- a/components/autofill/content/browser/suggestions/identity_credential_suggestion_generator.cc
+++ b/components/autofill/content/browser/suggestions/identity_credential_suggestion_generator.cc
@@ -4,8 +4,7 @@
 
 #include "components/autofill/content/browser/suggestions/identity_credential_suggestion_generator.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/notreached.h"
 #include "base/strings/utf_string_conversions.h"
@@ -28,21 +27,19 @@ std::map<FieldType, std::u16string> CreateFederatedProfileFields(
   std::map<FieldType, std::u16string> fields;
 
   if (!account->email.empty() &&
-      std::ranges::contains(
-          account->identity_provider->disclosure_fields,
-          content::IdentityRequestDialogDisclosureField::kEmail)) {
+      base::Contains(account->identity_provider->disclosure_fields,
+                     content::IdentityRequestDialogDisclosureField::kEmail)) {
     fields[EMAIL_ADDRESS] = base::UTF8ToUTF16(account->email);
   }
 
   if (!account->name.empty() &&
-      std::ranges::contains(
-          account->identity_provider->disclosure_fields,
-          content::IdentityRequestDialogDisclosureField::kName)) {
+      base::Contains(account->identity_provider->disclosure_fields,
+                     content::IdentityRequestDialogDisclosureField::kName)) {
     fields[NAME_FULL] = base::UTF8ToUTF16(account->name);
   }
 
   if (!account->phone.empty() &&
-      std::ranges::contains(
+      base::Contains(
           account->identity_provider->disclosure_fields,
           content::IdentityRequestDialogDisclosureField::kPhoneNumber)) {
     fields[PHONE_HOME_WHOLE_NUMBER] = base::UTF8ToUTF16(account->phone);
diff --git a/components/autofill/content/renderer/autofill_agent.cc b/components/autofill/content/renderer/autofill_agent.cc
index a04b40115b..15940072f7 100644
--- a/components/autofill/content/renderer/autofill_agent.cc
+++ b/components/autofill/content/renderer/autofill_agent.cc
@@ -19,6 +19,7 @@
 #include <vector>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/to_vector.h"
@@ -40,6 +41,7 @@
 #include "base/task/single_thread_task_runner.h"
 #include "base/time/time.h"
 #include "base/timer/timer.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/optional_ref.h"
 #include "base/types/zip.h"
 #include "build/build_config.h"
diff --git a/components/autofill/content/renderer/form_autofill_util.cc b/components/autofill/content/renderer/form_autofill_util.cc
index 5ec7230d17..1c7218faef 100644
--- a/components/autofill/content/renderer/form_autofill_util.cc
+++ b/components/autofill/content/renderer/form_autofill_util.cc
@@ -19,6 +19,7 @@
 #include "base/check_deref.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
@@ -425,7 +426,7 @@ std::u16string FindChildTextInner(const WebNode& node,
   if (node.IsElementNode()) {
     const WebElement element = node.To<WebElement>();
     if (HasTagName<kOption>(element) ||
-        (HasTagName<kDiv>(element) && divs_to_skip.contains(node)) ||
+        (HasTagName<kDiv>(element) && base::Contains(divs_to_skip, node)) ||
         IsAutofillableElement(element.DynamicTo<WebFormControlElement>())) {
       return std::u16string();
     }
@@ -1030,7 +1031,7 @@ std::optional<InferredLabel> InferLabelFromAncestors(
       continue;
 
     std::string tag_name = parent.To<WebElement>().TagName().Utf8();
-    if (seen_tag_names.contains(tag_name))
+    if (base::Contains(seen_tag_names, tag_name))
       continue;
     seen_tag_names.insert(tag_name);
 
@@ -1435,17 +1436,15 @@ FormFieldData* SearchForFormControlByName(
   if (it == end ||
       std::ranges::find(it + 1, end, field_name, get_field_name) != end) {
     auto ShadowHostHasTargetName = [&](const auto& p) {
-      return std::ranges::contains(p.second.shadow_host_name_attributes,
-                                   field_name) ||
-             std::ranges::contains(p.second.shadow_host_id_attributes,
-                                   field_name);
+      return base::Contains(p.second.shadow_host_name_attributes, field_name) ||
+             base::Contains(p.second.shadow_host_id_attributes, field_name);
     };
     it = std::ranges::find_if(fields, ShadowHostHasTargetName);
     if (it != end) {
-      label_source = std::ranges::contains(
-                         it->second.shadow_host_name_attributes, field_name)
-                         ? LabelSource::kForShadowHostName
-                         : LabelSource::kForShadowHostId;
+      label_source =
+          base::Contains(it->second.shadow_host_name_attributes, field_name)
+              ? LabelSource::kForShadowHostName
+              : LabelSource::kForShadowHostId;
     }
   } else {
     label_source = LabelSource::kForName;
@@ -2219,8 +2218,8 @@ std::optional<InferredLabel> InferredLabel::BuildIfValid(std::u16string label,
     // U+2014: "—"  (Em Dash)
     // U+2212: "−"  (Minus Sign)
     // U+FF0D: "－" (Fullwidth Hyphen-Minus)
-    return !kInvalidChars.contains(c) &&
-           !(std::u16string_view(base::kWhitespaceUTF16)).contains(c);
+    return !base::Contains(kInvalidChars, c) &&
+           !base::Contains(std::u16string_view(base::kWhitespaceUTF16), c);
   };
   auto is_from_extended_hyphen_like_list = [](char16_t c) {
     return c == u'\u2014' || c == u'\u2212' || c == u'\uFF0D';
@@ -2456,8 +2455,8 @@ FindFormAndFieldForFormControlElement(
 
   // This is not reachable if the following holds:
   // ```
-  // std::ranges::contains(
-  //   GetOwnedFormControls(element.GetOwningFormForAutofill()), element)
+  // base::Contains(GetOwnedFormControls(element.GetOwningFormForAutofill()),
+  //                element)
   // ```
   // It's not clear if that condition is true. See crbug.com/347059988 for the
   // ongoing debugging.
@@ -2500,7 +2499,7 @@ FindFormAndFieldForFormControlElement(
   WebFormElement assoc_form_element = element.Form();  // nocheck
 
   // clang-format off
-  SCOPED_CRASH_KEY_BOOL("Autofill", "invariant", std::ranges::contains(GetOwnedFormControls(element.GetDocument(), element.GetOwningFormForAutofill()), element));
+  SCOPED_CRASH_KEY_BOOL("Autofill", "invariant", base::Contains(GetOwnedFormControls(element.GetDocument(), element.GetOwningFormForAutofill()), element));
   SCOPED_CRASH_KEY_STRING256("Autofill", "url", url.spec());
   SCOPED_CRASH_KEY_BOOL("Autofill", "ExtractFormData_succeeded", extract_form_data_succeeded);
   SCOPED_CRASH_KEY_NUMBER("Autofill", "extracted_form_size", form->fields().size());
@@ -2508,7 +2507,7 @@ FindFormAndFieldForFormControlElement(
   SCOPED_CRASH_KEY_STRING64("Autofill", "elem_tag_name", element.TagName().Utf8());
   SCOPED_CRASH_KEY_STRING64("Autofill", "elem_id", get_id(element));
   SCOPED_CRASH_KEY_STRING64("Autofill", "elem_form_attr", element.GetAttribute("form").Utf8());
-  SCOPED_CRASH_KEY_NUMBER("Autofill", "elem_form_control_type", std::to_underlying(element.FormControlType()));  // nocheck
+  SCOPED_CRASH_KEY_NUMBER("Autofill", "elem_form_control_type", base::to_underlying(element.FormControlType()));  // nocheck
 
   SCOPED_CRASH_KEY_BOOL("Autofill", "elem_autofillable", IsAutofillableElement(element));
   SCOPED_CRASH_KEY_BOOL("Autofill", "elem_document", !!document);
@@ -2518,7 +2517,7 @@ FindFormAndFieldForFormControlElement(
 #define SCOPED_CRASH_KEYS_FOR_FORM(prefix, f)                                                                                  \
   SCOPED_CRASH_KEY_BOOL("Autofill", #prefix "_form_non_null", !!f);                                                            \
   SCOPED_CRASH_KEY_BOOL("Autofill", #prefix "_form_connected", f && is_connected(f));                                          \
-  SCOPED_CRASH_KEY_BOOL("Autofill", #prefix "_form_owns_element", f && std::ranges::contains(get_form_control_elements(f), element)); \
+  SCOPED_CRASH_KEY_BOOL("Autofill", #prefix "_form_owns_element", f && base::Contains(get_form_control_elements(f), element)); \
   SCOPED_CRASH_KEY_BOOL("Autofill", #prefix "_form_in_shadow_dom", f && !!f.OwnerShadowHost());                                \
   SCOPED_CRASH_KEY_BOOL("Autofill", #prefix "_form_in_same_dom", f && element.OwnerShadowHost() == f.OwnerShadowHost());       \
   SCOPED_CRASH_KEY_BOOL("Autofill", #prefix "_form_is_top_level", is_top_level(f));                                            \
diff --git a/components/autofill/content/renderer/form_cache.cc b/components/autofill/content/renderer/form_cache.cc
index 4ae7576032..8950bea528 100644
--- a/components/autofill/content/renderer/form_cache.cc
+++ b/components/autofill/content/renderer/form_cache.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "components/autofill/content/renderer/autofill_agent.h"
 #include "components/autofill/content/renderer/form_autofill_util.h"
diff --git a/components/autofill/content/renderer/form_tracker.cc b/components/autofill/content/renderer/form_tracker.cc
index b266ab640c..20680ac3e4 100644
--- a/components/autofill/content/renderer/form_tracker.cc
+++ b/components/autofill/content/renderer/form_tracker.cc
@@ -5,7 +5,6 @@
 #include "components/autofill/content/renderer/form_tracker.h"
 
 #include <optional>
-#include <utility>
 #include <variant>
 
 #include "base/check.h"
@@ -95,16 +94,16 @@ void LogSubmittedFormMetric(mojom::SubmissionSource source,
     kMaxValue = kTotal_Cached
   };
   static_assert(
-      std::to_underlying(SubmittedFormTypeBySource::kMaxValue) + 1 ==
-          3 * (std::to_underlying(mojom::SubmissionSource::kMaxValue) + 2),
+      base::to_underlying(SubmittedFormTypeBySource::kMaxValue) + 1 ==
+          3 * (base::to_underlying(mojom::SubmissionSource::kMaxValue) + 2),
       "SubmittedFormTypeBySource should have three values for each value of "
       "SubmissionSource in addition to three `Total` values");
 
   using underlying_type = std::underlying_type_t<SubmittedFormTypeBySource>;
-  underlying_type source_bucket = std::to_underlying(source) * 3;
+  underlying_type source_bucket = base::to_underlying(source) * 3;
   underlying_type total_bucket =
-      std::to_underlying(SubmittedFormTypeBySource::kTotal_Null);
-  underlying_type offset = std::to_underlying(type);
+      base::to_underlying(SubmittedFormTypeBySource::kTotal_Null);
+  underlying_type offset = base::to_underlying(type);
   base::UmaHistogramEnumeration(
       "Autofill.SubmissionDetection.SubmittedFormType",
       static_cast<SubmittedFormTypeBySource>(source_bucket + offset));
diff --git a/components/autofill/content/renderer/html_based_username_detector.cc b/components/autofill/content/renderer/html_based_username_detector.cc
index ee7da583cb..de8cb12e64 100644
--- a/components/autofill/content/renderer/html_based_username_detector.cc
+++ b/components/autofill/content/renderer/html_based_username_detector.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
 #include "base/i18n/case_conversion.h"
@@ -201,7 +202,7 @@ void FindWordsFromCategoryInForm(
   }
 
   if (fields_found > 0 && fields_found <= 2)
-    if (!std::ranges::contains(*username_predictions, chosen_field_renderer_id))
+    if (!base::Contains(*username_predictions, chosen_field_renderer_id))
       username_predictions->push_back(chosen_field_renderer_id);
 }
 
diff --git a/components/autofill/content/renderer/page_passwords_analyser.cc b/components/autofill/content/renderer/page_passwords_analyser.cc
index c6661ba167..03a16a1c81 100644
--- a/components/autofill/content/renderer/page_passwords_analyser.cc
+++ b/components/autofill/content/renderer/page_passwords_analyser.cc
@@ -10,6 +10,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/lazy_instance.h"
 #include "base/memory/raw_ptr.h"
 #include "base/strings/strcat.h"
@@ -209,8 +210,8 @@ std::vector<FormInputCollection> ExtractFormsForAnalysis(
       // to be username or password fields.
       if (input.TagName() == "INPUT" &&
           (!input.HasAttribute("type") ||
-           std::ranges::contains(kTypeAttributes,
-                                 input.GetAttribute("type").Utf8()))) {
+           base::Contains(kTypeAttributes,
+                          input.GetAttribute("type").Utf8()))) {
         form_input_collections.back().AddInput(input);
         inputs_with_forms.insert(input);
       }
diff --git a/components/autofill/content/renderer/password_autofill_agent.cc b/components/autofill/content/renderer/password_autofill_agent.cc
index 3177e6b7fc..8cba62f2fb 100644
--- a/components/autofill/content/renderer/password_autofill_agent.cc
+++ b/components/autofill/content/renderer/password_autofill_agent.cc
@@ -16,6 +16,7 @@
 
 #include "base/check.h"
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/i18n/case_conversion.h"
@@ -351,9 +352,8 @@ void AnnotateFieldsWithSignatures(
 // fields, both of which are cached in the Document.
 bool HasPasswordField(const WebLocalFrame& frame) {
   auto ContainsPasswordField = [&](const auto& fields) {
-    return std::ranges::contains(
-        fields, blink::mojom::FormControlType::kInputPassword,
-        &WebFormControlElement::FormControlTypeForAutofill);
+    return base::Contains(fields, blink::mojom::FormControlType::kInputPassword,
+                          &WebFormControlElement::FormControlTypeForAutofill);
   };
 
   WebDocument doc = frame.GetDocument();
@@ -832,8 +832,8 @@ void PasswordAutofillAgent::NotifyPasswordManagerAboutUserFieldModification(
 
   GetPasswordManagerDriver().UserModifiedNonPasswordField(
       GetFieldRendererId(element), element_value,
-      autocomplete_attribute.contains(
-          password_manager::constants::kAutocompleteUsername),
+      base::Contains(autocomplete_attribute,
+                     password_manager::constants::kAutocompleteUsername),
       is_likely_otp);
 }
 
@@ -1565,7 +1565,7 @@ bool PasswordAutofillAgent::IsUsernameInputField(
     const WebInputElement& input_element) const {
   return input_element &&
          input_element.FormControlTypeForAutofill() != kInputPassword &&
-         web_input_to_password_info_.contains(FieldRef(input_element));
+         base::Contains(web_input_to_password_info_, FieldRef(input_element));
 }
 
 void PasswordAutofillAgent::ReadyToCommitNavigation(
diff --git a/components/autofill/content/renderer/password_generation_agent.cc b/components/autofill/content/renderer/password_generation_agent.cc
index 6a7fa075b0..7488e6875c 100644
--- a/components/autofill/content/renderer/password_generation_agent.cc
+++ b/components/autofill/content/renderer/password_generation_agent.cc
@@ -12,6 +12,7 @@
 #include "base/auto_reset.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/task/single_thread_task_runner.h"
@@ -376,8 +377,8 @@ void PasswordGenerationAgent::GeneratedPasswordAccepted(
     }
     password_agent_->TrackAutofilledElement(password_element);
   }
-  CHECK(std::ranges::contains(current_generation_item_->password_elements_,
-                              current_generation_item_->generation_element_));
+  CHECK(base::Contains(current_generation_item_->password_elements_,
+                       current_generation_item_->generation_element_));
 
   std::optional<FormData> presaved_form_data =
       CreateFormDataToPresave(/*form_cache=*/{});
diff --git a/components/autofill/core/browser/autofill_field.cc b/components/autofill/core/browser/autofill_field.cc
index 8dc4e1278f..1de3a0b162 100644
--- a/components/autofill/core/browser/autofill_field.cc
+++ b/components/autofill/core/browser/autofill_field.cc
@@ -11,6 +11,7 @@
 #include <ranges>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/no_destructor.h"
@@ -19,6 +20,7 @@
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/data_model/data_model_utils.h"
 #include "components/autofill/core/browser/field_type_utils.h"
 #include "components/autofill/core/browser/field_types.h"
@@ -176,8 +178,8 @@ bool PreferHeuristicOverHtml(FieldType heuristic_type,
     return true;
   }
 
-  return kAutofillHeuristicsVsHtmlOverrides.contains(
-      std::make_pair(heuristic_type, html_type));
+  return base::Contains(kAutofillHeuristicsVsHtmlOverrides,
+                        std::make_pair(heuristic_type, html_type));
 }
 
 // Returns whether the `heuristic_type` should be preferred over the
@@ -199,8 +201,8 @@ bool PreferHeuristicOverServer(FieldType heuristic_type,
     return true;
   }
 
-  if (kAutofillHeuristicsVsServerOverrides.contains(
-          std::make_pair(heuristic_type, server_type))) {
+  if (base::Contains(kAutofillHeuristicsVsServerOverrides,
+                     std::make_pair(heuristic_type, server_type))) {
     return true;
   }
 
diff --git a/components/autofill/core/browser/crowdsourcing/autofill_crowdsourcing_encoding.cc b/components/autofill/core/browser/crowdsourcing/autofill_crowdsourcing_encoding.cc
index 2846c0aba1..892aff5963 100644
--- a/components/autofill/core/browser/crowdsourcing/autofill_crowdsourcing_encoding.cc
+++ b/components/autofill/core/browser/crowdsourcing/autofill_crowdsourcing_encoding.cc
@@ -1057,7 +1057,7 @@ EncodeAutofillPageQueryRequest(const std::vector<FormData>& forms) {
   // the original form signature.
   std::set<FormSignature> processed_forms;
   for (const FormData& form : forms) {
-    if (processed_forms.contains(CalculateFormSignature(form))) {
+    if (base::Contains(processed_forms, CalculateFormSignature(form))) {
       continue;
     }
     UMA_HISTOGRAM_COUNTS_1000("Autofill.FieldCount", form.fields().size());
diff --git a/components/autofill/core/browser/data_manager/payments/payments_data_manager.cc b/components/autofill/core/browser/data_manager/payments/payments_data_manager.cc
index 7a01f991e4..b9ca27e1b8 100644
--- a/components/autofill/core/browser/data_manager/payments/payments_data_manager.cc
+++ b/components/autofill/core/browser/data_manager/payments/payments_data_manager.cc
@@ -9,6 +9,7 @@
 #include <variant>
 
 #include "base/android/device_info.h"
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/containers/span.h"
 #include "base/containers/to_vector.h"
@@ -983,8 +984,11 @@ std::vector<BnplIssuer> PaymentsDataManager::GetBnplIssuers() const {
 
   std::vector<BnplIssuer> result;
   result.reserve(linked_bnpl_issuers_.size() + unlinked_bnpl_issuers_.size());
-  base::Extend(result, linked_bnpl_issuers_);
-  base::Extend(result, unlinked_bnpl_issuers_);
+  result.insert(result.end(), linked_bnpl_issuers_.begin(),
+                linked_bnpl_issuers_.end());
+  result.insert(result.end(), unlinked_bnpl_issuers_.begin(),
+                unlinked_bnpl_issuers_.end());
+
   return result;
 }
 
@@ -2283,10 +2287,9 @@ void PaymentsDataManager::CacheIfLinkedBnplPaymentInstrument(
   // Ensures the server does not return any duplicate issuers. Should never
   // happen, but servers should never be trusted and responses must be handled
   // gracefully.
-  if (std::ranges::contains(
-          linked_bnpl_issuers_,
-          ConvertToBnplIssuerIdEnum(bnpl_issuer_details.issuer_id()),
-          &BnplIssuer::issuer_id)) {
+  if (base::Contains(linked_bnpl_issuers_,
+                     ConvertToBnplIssuerIdEnum(bnpl_issuer_details.issuer_id()),
+                     &BnplIssuer::issuer_id)) {
     return;
   }
 
@@ -2402,9 +2405,9 @@ void PaymentsDataManager::CacheIfBnplPaymentInstrumentCreationOption(
   // Ensures the server does not return any duplicate issuers. Should never
   // happen, but servers should never be trusted and responses must be handled
   // gracefully.
-  if (std::ranges::contains(unlinked_bnpl_issuers_,
-                            ConvertToBnplIssuerIdEnum(bnpl_issuer.issuer_id()),
-                            &BnplIssuer::issuer_id)) {
+  if (base::Contains(unlinked_bnpl_issuers_,
+                     ConvertToBnplIssuerIdEnum(bnpl_issuer.issuer_id()),
+                     &BnplIssuer::issuer_id)) {
     return;
   }
 
diff --git a/components/autofill/core/browser/data_model/addresses/autofill_i18n_api.cc b/components/autofill/core/browser/data_model/addresses/autofill_i18n_api.cc
index 79748b438f..7d1c86bd11 100644
--- a/components/autofill/core/browser/data_model/addresses/autofill_i18n_api.cc
+++ b/components/autofill/core/browser/data_model/addresses/autofill_i18n_api.cc
@@ -4,10 +4,10 @@
 
 #include "components/autofill/core/browser/data_model/addresses/autofill_i18n_api.h"
 
-#include <algorithm>
 #include <memory>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/feature_list.h"
 #include "base/notreached.h"
@@ -55,7 +55,7 @@ std::u16string GetFormattingExpressionOverrides(
            "SG", "LK", "TH", "GB", "US", "VN", "ZA"});
 
   if (field_type == ADDRESS_HOME_STREET_LOCATION) {
-    if (kHouseNumberFirstCountriesSet.contains(country_code.value())) {
+    if (base::Contains(kHouseNumberFirstCountriesSet, country_code.value())) {
       return u"${ADDRESS_HOME_HOUSE_NUMBER;;} ${ADDRESS_HOME_STREET_NAME;;}";
     }
   }
@@ -76,7 +76,8 @@ std::u16string GetFormattingExpressionOverrides(
 
   if (field_type == ADDRESS_HOME_ZIP &&
       base::FeatureList::IsEnabled(features::kAutofillSupportSplitZipCode)) {
-    if (kSpaceZipCodeSeparatorCountriesSet.contains(country_code.value())) {
+    if (base::Contains(kSpaceZipCodeSeparatorCountriesSet,
+                       country_code.value())) {
       return u"${ADDRESS_HOME_ZIP_PREFIX;;} ${ADDRESS_HOME_ZIP_SUFFIX;;}";
     }
   }
@@ -466,7 +467,7 @@ bool IsTypeEnabledForCountry(FieldType field_type,
   return std::ranges::any_of(
       it->second, [field_type](const FieldTypeDescription& description) {
         return description.field_type == field_type ||
-               std::ranges::contains(description.children, field_type);
+               base::Contains(description.children, field_type);
       });
 }
 
diff --git a/components/autofill/core/browser/data_model/addresses/autofill_profile.cc b/components/autofill/core/browser/data_model/addresses/autofill_profile.cc
index 83053b75fa..1d719c6458 100644
--- a/components/autofill/core/browser/data_model/addresses/autofill_profile.cc
+++ b/components/autofill/core/browser/data_model/addresses/autofill_profile.cc
@@ -123,8 +123,8 @@ void GetFieldsForDistinguishingProfiles(
     std::vector<FieldType>* distinguishing_fields) {
   std::vector<FieldType> default_fields;
   if (!suggested_fields) {
-    default_fields.append_range(
-        AutofillProfile::kDefaultDistinguishingFieldsForLabels);
+    auto range = AutofillProfile::kDefaultDistinguishingFieldsForLabels;
+    default_fields.insert(default_fields.end(), range.begin(), range.end());
     if (excluded_fields.empty()) {
       distinguishing_fields->swap(default_fields);
       return;
diff --git a/components/autofill/core/browser/data_model/addresses/autofill_structured_address.cc b/components/autofill/core/browser/data_model/addresses/autofill_structured_address.cc
index e53522550c..93cd02867d 100644
--- a/components/autofill/core/browser/data_model/addresses/autofill_structured_address.cc
+++ b/components/autofill/core/browser/data_model/addresses/autofill_structured_address.cc
@@ -4,10 +4,10 @@
 
 #include "components/autofill/core/browser/data_model/addresses/autofill_structured_address.h"
 
-#include <algorithm>
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/strings/string_split.h"
@@ -208,7 +208,7 @@ void StreetAddressNode::CalculateAddressLines() {
 }
 
 bool StreetAddressNode::IsValueValid() const {
-  return !std::ranges::contains(address_lines_, std::u16string());
+  return !base::Contains(address_lines_, std::u16string());
 }
 
 std::u16string StreetAddressNode::GetValueForOtherSupportedType(
diff --git a/components/autofill/core/browser/data_model/addresses/contact_info.cc b/components/autofill/core/browser/data_model/addresses/contact_info.cc
index 762188fe28..f0ce7e04e8 100644
--- a/components/autofill/core/browser/data_model/addresses/contact_info.cc
+++ b/components/autofill/core/browser/data_model/addresses/contact_info.cc
@@ -13,7 +13,6 @@
 #include <utility>
 
 #include "base/check_op.h"
-#include "base/containers/adapters.h"
 #include "base/i18n/char_iterator.h"
 #include "base/notreached.h"
 #include "base/strings/strcat.h"
@@ -120,14 +119,13 @@ std::set<std::u16string> GetNamePartVariants(std::u16string_view name_part) {
       continue;
     }
     std::vector<std::u16string> new_variants;
-    new_variants.reserve(variants.size() * 2);
     for (const std::u16string& variant : variants) {
       new_variants.push_back(base::CollapseWhitespace(
           base::JoinString({variant, sub_name}, kSpace), true));
       new_variants.push_back(base::CollapseWhitespace(
           base::JoinString({variant, sub_name.substr(0, 1)}, kSpace), true));
     }
-    variants.insert_range(base::RangeAsRvalues(std::move(new_variants)));
+    variants.insert(new_variants.begin(), new_variants.end());
   }
 
   // As a common case, also add the variant that just concatenates all of the
diff --git a/components/autofill/core/browser/data_model/autofill_ai/entity_instance.h b/components/autofill/core/browser/data_model/autofill_ai/entity_instance.h
index fb27f1d69d..39702d498a 100644
--- a/components/autofill/core/browser/data_model/autofill_ai/entity_instance.h
+++ b/components/autofill/core/browser/data_model/autofill_ai/entity_instance.h
@@ -8,13 +8,13 @@
 #include <ostream>
 #include <string>
 #include <string_view>
-#include <utility>
 #include <variant>
 
 #include "base/compiler_specific.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/optional_ref.h"
 #include "base/types/pass_key.h"
 #include "base/types/strong_alias.h"
@@ -201,7 +201,7 @@ struct AttributeInstance::CompareByType {
 
  private:
   bool lt(AttributeTypeName lhs, AttributeTypeName rhs) const {
-    return std::to_underlying(lhs) < std::to_underlying(rhs);
+    return base::to_underlying(lhs) < base::to_underlying(rhs);
   }
 };
 
diff --git a/components/autofill/core/browser/data_model/autofill_ai/entity_type.cc b/components/autofill/core/browser/data_model/autofill_ai/entity_type.cc
index 286db42630..1fd77fe3cf 100644
--- a/components/autofill/core/browser/data_model/autofill_ai/entity_type.cc
+++ b/components/autofill/core/browser/data_model/autofill_ai/entity_type.cc
@@ -5,9 +5,9 @@
 #include "components/autofill/core/browser/data_model/autofill_ai/entity_type.h"
 
 #include <optional>
-#include <utility>
 
 #include "base/feature_list.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/data_model/addresses/contact_info.h"
 #include "components/autofill/core/browser/field_types.h"
 #include "components/autofill/core/common/autofill_features.h"
@@ -183,7 +183,7 @@ std::optional<EntityTypeName> ToSafeEntityTypeName(
   // auto-generated and its value range is contiguous. If it were not, this
   // check would not be sufficient.
   if (raw_value < 0 ||
-      raw_value > std::to_underlying(EntityTypeName::kMaxValue)) {
+      raw_value > base::to_underlying(EntityTypeName::kMaxValue)) {
     return std::nullopt;
   }
   return EntityTypeName(raw_value);
diff --git a/components/autofill/core/browser/data_model/autofill_ai/entity_type.h b/components/autofill/core/browser/data_model/autofill_ai/entity_type.h
index e635311de6..0b0563f827 100644
--- a/components/autofill/core/browser/data_model/autofill_ai/entity_type.h
+++ b/components/autofill/core/browser/data_model/autofill_ai/entity_type.h
@@ -8,7 +8,6 @@
 #include <ostream>
 #include <string_view>
 #include <type_traits>
-#include <utility>
 
 #include "base/containers/span.h"
 #include "base/notreached.h"
@@ -267,7 +266,7 @@ struct DenseSetTraits<AttributeType> {
     return T(static_cast<N>(x));
   }
   static constexpr UnderlyingType to_underlying(T x) {
-    return std::to_underlying(x.name());
+    return base::to_underlying(x.name());
   }
   static constexpr bool is_valid(T x) { return true; }
 
@@ -433,7 +432,7 @@ struct DenseSetTraits<EntityType> {
     return T(static_cast<N>(x));
   }
   static constexpr UnderlyingType to_underlying(T x) {
-    return std::to_underlying(x.name());
+    return base::to_underlying(x.name());
   }
   static constexpr bool is_valid(T x) { return true; }
 
diff --git a/components/autofill/core/browser/data_model/autofill_ai/transpile_entity_schema.py b/components/autofill/core/browser/data_model/autofill_ai/transpile_entity_schema.py
index ef96b292f9..886b951a94 100755
--- a/components/autofill/core/browser/data_model/autofill_ai/transpile_entity_schema.py
+++ b/components/autofill/core/browser/data_model/autofill_ai/transpile_entity_schema.py
@@ -246,16 +246,16 @@ def generate_cpp_functions(schema):
   yield 'bool AttributeType::DisambiguationOrder(const AttributeType& lhs, const AttributeType& rhs) {'
   yield '  constexpr auto rank = [](const AttributeType& a) {'
   yield '    static constexpr auto ranks = [] {'
-  yield '      std::array<int, std::to_underlying(AttributeTypeName::kMaxValue) + 1> ranks{};'
+  yield '      std::array<int, base::to_underlying(AttributeTypeName::kMaxValue) + 1> ranks{};'
   yield '      for (int& rank : ranks) {'
   yield '        rank = std::numeric_limits<int>::max();'
   yield '      }'
   for entity, order in ((entity['name'], entity['disambiguation order']) for entity in schema):
     for rank, attribute in enumerate(order):
-      yield f'      ranks[std::to_underlying({attribute_name(entity, attribute)})] = {rank+1};'
+      yield f'      ranks[base::to_underlying({attribute_name(entity, attribute)})] = {rank+1};'
   yield '      return ranks;'
   yield '    }();'
-  yield '    return ranks[std::to_underlying(a.name())];'
+  yield '    return ranks[base::to_underlying(a.name())];'
   yield '  };'
   yield '  return rank(lhs) < rank(rhs);'
   yield '}'
@@ -284,12 +284,12 @@ def generate_cpp_functions_header(schema, include_guard):
 #include <array>
 #include <limits>
 #include <string>
-#include <utility>
 
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/span.h"
 #include "base/notreached.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/optional_ref.h"
 #include "base/types/pass_key.h"
 #include "components/autofill/core/browser/country_type.h"
diff --git a/components/autofill/core/browser/data_model/payments/credit_card.cc b/components/autofill/core/browser/data_model/payments/credit_card.cc
index a5c85a618d..b2d93cda0a 100644
--- a/components/autofill/core/browser/data_model/payments/credit_card.cc
+++ b/components/autofill/core/browser/data_model/payments/credit_card.cc
@@ -1317,7 +1317,7 @@ std::ostream& operator<<(std::ostream& os, const CreditCard& credit_card) {
             << base::UTF16ToUTF8(
                    credit_card.GetRawInfo(CREDIT_CARD_EXP_4_DIGIT_YEAR))
             << " " << credit_card.bank_name() << " "
-            << std::to_underlying(credit_card.record_type()) << " "
+            << base::to_underlying(credit_card.record_type()) << " "
             << credit_card.usage_history().use_count() << " "
             << credit_card.usage_history().use_date() << " "
             << credit_card.billing_address_id() << " " << credit_card.nickname()
@@ -1327,14 +1327,14 @@ std::ostream& operator<<(std::ostream& os, const CreditCard& credit_card) {
                    credit_card.card_issuer())
             << " " << credit_card.issuer_id() << " "
             << credit_card.instrument_id() << " "
-            << std::to_underlying(credit_card.virtual_card_enrollment_state())
+            << base::to_underlying(credit_card.virtual_card_enrollment_state())
             << " " << credit_card.card_art_url().spec() << " "
             << base::UTF16ToUTF8(credit_card.product_description()) << " "
             << credit_card.product_terms_url().spec() << " "
             << credit_card.benefit_source() << " " << credit_card.cvc() << " "
-            << std::to_underlying(
+            << base::to_underlying(
                    credit_card.card_info_retrieval_enrollment_state())
-            << " " << std::to_underlying(credit_card.card_creation_source());
+            << " " << base::to_underlying(credit_card.card_creation_source());
 }
 
 void CreditCard::SetNameOnCardFromSeparateParts() {
diff --git a/components/autofill/core/browser/data_model/usage_history_information.cc b/components/autofill/core/browser/data_model/usage_history_information.cc
index 18786c4107..15e276556d 100644
--- a/components/autofill/core/browser/data_model/usage_history_information.cc
+++ b/components/autofill/core/browser/data_model/usage_history_information.cc
@@ -8,7 +8,6 @@
 #include <cmath>
 
 #include "base/check_op.h"
-#include "base/containers/extend.h"
 #include "base/feature_list.h"
 #include "components/autofill/core/common/autofill_clock.h"
 #include "components/autofill/core/common/autofill_features.h"
@@ -69,7 +68,8 @@ void UsageHistoryInformation::MergeUsageHistories(
   set_use_count(std::max(use_count(), other.use_count()));
 
   // Take the `usage_history_size()` latest use dates (nullopts go last).
-  base::Extend(use_dates_, other.use_dates_);
+  use_dates_.insert(use_dates_.end(), other.use_dates_.begin(),
+                    other.use_dates_.end());
   std::ranges::sort(use_dates_, std::greater<>());
   use_dates_.resize(usage_history_size());
 }
diff --git a/components/autofill/core/browser/data_quality/addresses/profile_requirement_utils.cc b/components/autofill/core/browser/data_quality/addresses/profile_requirement_utils.cc
index 0b4f316ac0..adbf9f322f 100644
--- a/components/autofill/core/browser/data_quality/addresses/profile_requirement_utils.cc
+++ b/components/autofill/core/browser/data_quality/addresses/profile_requirement_utils.cc
@@ -4,10 +4,10 @@
 
 #include "components/autofill/core/browser/data_quality/addresses/profile_requirement_utils.h"
 
-#include <algorithm>
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
@@ -188,9 +188,8 @@ bool ValidateNonEmptyValues(const AutofillProfile& profile,
 bool IsMinimumAddress(const AutofillProfile& profile, LogBuffer* log_buffer) {
   const std::vector<std::string>& country_codes =
       CountryDataMap::GetInstance()->country_codes();
-  if (!std::ranges::contains(
-          country_codes,
-          base::UTF16ToUTF8(profile.GetRawInfo(ADDRESS_HOME_COUNTRY)))) {
+  if (!base::Contains(country_codes, base::UTF16ToUTF8(profile.GetRawInfo(
+                                         ADDRESS_HOME_COUNTRY)))) {
     return false;
   }
   std::vector<AddressImportRequirement> address_requirements =
@@ -198,8 +197,8 @@ bool IsMinimumAddress(const AutofillProfile& profile, LogBuffer* log_buffer) {
   return std::ranges::none_of(
       kMinimumAddressRequirementViolations,
       [&](AddressImportRequirement address_requirement_violation) {
-        return std::ranges::contains(address_requirements,
-                                     address_requirement_violation);
+        return base::Contains(address_requirements,
+                              address_requirement_violation);
       });
 }
 
diff --git a/components/autofill/core/browser/data_quality/addresses/profile_token_quality.cc b/components/autofill/core/browser/data_quality/addresses/profile_token_quality.cc
index 4a82c552e9..ed886c3f25 100644
--- a/components/autofill/core/browser/data_quality/addresses/profile_token_quality.cc
+++ b/components/autofill/core/browser/data_quality/addresses/profile_token_quality.cc
@@ -6,15 +6,16 @@
 
 #include <algorithm>
 #include <set>
-#include <utility>
 
 #include "base/check.h"
 #include "base/check_deref.h"
 #include "base/containers/circular_deque.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/rand_util.h"
 #include "base/strings/levenshtein_distance.h"
 #include "base/strings/string_util.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/data_manager/addresses/address_data_manager.h"
 #include "components/autofill/core/browser/data_manager/personal_data_manager.h"
 #include "components/autofill/core/browser/data_model/addresses/autofill_profile.h"
@@ -153,9 +154,8 @@ bool ProfileTokenQuality::AddObservationsForFilledForm(
         GetFormSignatureHash(form_structure.form_signature());
     if (auto observations = observations_.find(stored_type);
         observations != observations_.end() &&
-        std::ranges::contains(
-            observations->second, hash,
-            [](const Observation& o) { return o.form_hash; })) {
+        base::Contains(observations->second, hash,
+                       [](const Observation& o) { return o.form_hash; })) {
       // An observation for the `stored_type` and `hash` was already collected.
       continue;
     }
@@ -169,7 +169,7 @@ bool ProfileTokenQuality::AddObservationsForFilledForm(
         selected_option ? selected_option->text : form_data.fields()[i].value();
     possible_observations.emplace_back(
         stored_type,
-        Observation{.type = std::to_underlying(GetObservationTypeFromField(
+        Observation{.type = base::to_underlying(GetObservationTypeFromField(
                         field, value, other_profiles, adm.app_locale())),
                     .form_hash = hash});
   }
@@ -213,7 +213,7 @@ ProfileTokenQuality::GetObservationTypesForFieldType(FieldType type) const {
   std::vector<ObservationType> types;
   types.reserve(it->second.size());
   for (const Observation& observation : it->second) {
-    if (observation.type <= std::to_underlying(ObservationType::kMaxValue)) {
+    if (observation.type <= base::to_underlying(ObservationType::kMaxValue)) {
       types.push_back(static_cast<ObservationType>(observation.type));
     } else {
       // This is possible if the `observation.type` was synced from a newer
@@ -227,7 +227,7 @@ ProfileTokenQuality::GetObservationTypesForFieldType(FieldType type) const {
 
 void ProfileTokenQuality::AddObservation(FieldType type,
                                          Observation observation) {
-  CHECK_NE(observation.type, std::to_underlying(ObservationType::kUnknown));
+  CHECK_NE(observation.type, base::to_underlying(ObservationType::kUnknown));
   base::circular_deque<Observation>& observations =
       observations_[profile_->GetStorableTypeOf(type)];
   CHECK_LE(observations.size(), kMaxObservationsPerToken);
@@ -263,9 +263,8 @@ ObservationType ProfileTokenQuality::GetObservationTypeFromField(
     const std::vector<const AutofillProfile*>& other_profiles,
     const std::string& app_locale) const {
   CHECK(field.autofill_source_profile_guid() == profile_->guid());
-  DCHECK(!std::ranges::contains(
-      other_profiles, profile_->guid(),
-      [](const AutofillProfile* p) { return p->guid(); }));
+  DCHECK(!base::Contains(other_profiles, profile_->guid(),
+                         [](const AutofillProfile* p) { return p->guid(); }));
 
   const FieldType type = field.Type().GetAddressType();
   if (field.is_autofilled()) {
@@ -307,9 +306,9 @@ void ProfileTokenQuality::LoadSerializedObservationsForStoredType(
   for (size_t i = 0; i + 1 < serialized_data.size() &&
                      observations_.size() < kMaxObservationsPerToken;
        i += 2) {
-    static_assert(std::to_underlying(ObservationType::kUnknown) == 0);
+    static_assert(base::to_underlying(ObservationType::kUnknown) == 0);
     if (serialized_data[i] == 0 ||
-        serialized_data[i] > std::to_underlying(ObservationType::kMaxValue)) {
+        serialized_data[i] > base::to_underlying(ObservationType::kMaxValue)) {
       // Invalid data read from disk.
       continue;
     }
diff --git a/components/autofill/core/browser/data_quality/addresses/profile_token_quality_test_api.cc b/components/autofill/core/browser/data_quality/addresses/profile_token_quality_test_api.cc
index 3c29c7e884..6d95467cf3 100644
--- a/components/autofill/core/browser/data_quality/addresses/profile_token_quality_test_api.cc
+++ b/components/autofill/core/browser/data_quality/addresses/profile_token_quality_test_api.cc
@@ -4,10 +4,10 @@
 
 #include "components/autofill/core/browser/data_quality/addresses/profile_token_quality_test_api.h"
 
-#include <utility>
 #include <vector>
 
 #include "base/check.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/data_model/addresses/autofill_profile.h"
 #include "components/autofill/core/browser/data_quality/addresses/profile_token_quality.h"
 #include "components/autofill/core/browser/field_type_utils.h"
@@ -32,7 +32,7 @@ void ProfileTokenQualityTestApi::AddObservation(
   quality_->AddObservation(
       field_type,
       ProfileTokenQuality::Observation{
-          .type = std::to_underlying(observation_type), .form_hash = hash});
+          .type = base::to_underlying(observation_type), .form_hash = hash});
 }
 
 std::vector<ProfileTokenQualityTestApi::FormSignatureHash>
diff --git a/components/autofill/core/browser/data_quality/autofill_data_util.cc b/components/autofill/core/browser/data_quality/autofill_data_util.cc
index 2fd637f49a..13404a8625 100644
--- a/components/autofill/core/browser/data_quality/autofill_data_util.cc
+++ b/components/autofill/core/browser/data_quality/autofill_data_util.cc
@@ -10,6 +10,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/i18n/char_iterator.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_split.h"
@@ -510,8 +511,8 @@ const char* GetIssuerNetworkForBasicCardIssuerNetwork(
 }
 
 bool IsValidBasicCardIssuerNetwork(std::string_view basic_card_issuer_network) {
-  return std::ranges::contains(kPaymentRequestData, basic_card_issuer_network,
-                               &PaymentRequestData::basic_card_issuer_network);
+  return base::Contains(kPaymentRequestData, basic_card_issuer_network,
+                        &PaymentRequestData::basic_card_issuer_network);
 }
 
 bool IsValidCountryCode(std::string_view country_code) {
diff --git a/components/autofill/core/browser/data_quality/validation.cc b/components/autofill/core/browser/data_quality/validation.cc
index 3e24020a16..413809c565 100644
--- a/components/autofill/core/browser/data_quality/validation.cc
+++ b/components/autofill/core/browser/data_quality/validation.cc
@@ -11,6 +11,7 @@
 
 #include "base/check.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/notreached.h"
diff --git a/components/autofill/core/browser/field_types.h b/components/autofill/core/browser/field_types.h
index 4dfdfa0e18..455d546022 100644
--- a/components/autofill/core/browser/field_types.h
+++ b/components/autofill/core/browser/field_types.h
@@ -6,8 +6,8 @@
 #define COMPONENTS_AUTOFILL_CORE_BROWSER_FIELD_TYPES_H_
 
 #include <type_traits>
-#include <utility>
 
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/common/dense_set.h"
 #include "components/autofill/core/common/html_field_types.h"
 
@@ -602,7 +602,7 @@ struct DenseSetTraits<FieldType>
     : EnumDenseSetTraits<FieldType, NO_SERVER_DATA, MAX_VALID_FIELD_TYPE> {
   static constexpr bool is_valid(FieldType x) {
     return x == NO_SERVER_DATA ||
-           ToSafeFieldType(std::to_underlying(x), NO_SERVER_DATA) !=
+           ToSafeFieldType(base::to_underlying(x), NO_SERVER_DATA) !=
                NO_SERVER_DATA;
   }
 };
@@ -614,7 +614,7 @@ struct DenseSetTraits<HtmlFieldType>
                          HtmlFieldType::kMaxValue> {
   static constexpr bool is_valid(HtmlFieldType x) {
     return x == HtmlFieldType::kUnrecognized ||
-           ToSafeHtmlFieldType(std::to_underlying(x),
+           ToSafeHtmlFieldType(base::to_underlying(x),
                                HtmlFieldType::kUnrecognized) !=
                HtmlFieldType::kUnrecognized;
   }
@@ -711,8 +711,8 @@ constexpr HtmlFieldType ToSafeHtmlFieldType(
     std::underlying_type_t<HtmlFieldType> raw_value,
     HtmlFieldType fallback_value) {
   auto is_invalid = [](std::underlying_type_t<HtmlFieldType> t) {
-    return t < std::to_underlying(HtmlFieldType::kMinValue) ||
-           t > std::to_underlying(HtmlFieldType::kMaxValue) ||
+    return t < base::to_underlying(HtmlFieldType::kMinValue) ||
+           t > base::to_underlying(HtmlFieldType::kMaxValue) ||
            // Full address is deprecated.
            t == 17 ||
            // UPI is deprecated.
@@ -896,12 +896,12 @@ constexpr FieldTypeGroup GroupTypeOfFieldType(FieldType field_type) {
 
 namespace internal {
 consteval std::array<FieldTypeSet,
-                     std::to_underlying(FieldTypeGroup::kMaxValue) + 1>
+                     base::to_underlying(FieldTypeGroup::kMaxValue) + 1>
 FieldTypesByGroupHelper() {
-  constexpr auto kMaxValue = std::to_underlying(FieldTypeGroup::kMaxValue);
+  constexpr auto kMaxValue = base::to_underlying(FieldTypeGroup::kMaxValue);
   std::array<FieldTypeSet, kMaxValue + 1> map{};
   for (FieldType field_type : FieldTypeSet::all()) {
-    auto index = std::to_underlying(GroupTypeOfFieldType(field_type));
+    auto index = base::to_underlying(GroupTypeOfFieldType(field_type));
     map[index].insert(field_type);
   }
   return map;
@@ -909,7 +909,7 @@ FieldTypesByGroupHelper() {
 }  // namespace internal
 
 constexpr FieldTypeSet FieldTypesOfGroup(FieldTypeGroup group) {
-  return internal::FieldTypesByGroupHelper()[std::to_underlying(group)];
+  return internal::FieldTypesByGroupHelper()[base::to_underlying(group)];
 }
 
 }  // namespace autofill
diff --git a/components/autofill/core/browser/form_import/addresses/autofill_save_update_address_profile_delegate_ios.cc b/components/autofill/core/browser/form_import/addresses/autofill_save_update_address_profile_delegate_ios.cc
index 63c860e45f..b897caf8fe 100644
--- a/components/autofill/core/browser/form_import/addresses/autofill_save_update_address_profile_delegate_ios.cc
+++ b/components/autofill/core/browser/form_import/addresses/autofill_save_update_address_profile_delegate_ios.cc
@@ -4,9 +4,9 @@
 
 #include "components/autofill/core/browser/form_import/addresses/autofill_save_update_address_profile_delegate_ios.h"
 
-#include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/types/optional_util.h"
diff --git a/components/autofill/core/browser/form_import/form_data_importer_utils.cc b/components/autofill/core/browser/form_import/form_data_importer_utils.cc
index 337fc64c67..3f5ead1857 100644
--- a/components/autofill/core/browser/form_import/form_data_importer_utils.cc
+++ b/components/autofill/core/browser/form_import/form_data_importer_utils.cc
@@ -4,8 +4,7 @@
 
 #include "components/autofill/core/browser/form_import/form_data_importer_utils.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/strings/utf_string_conversions.h"
 #include "components/autofill/core/browser/data_manager/addresses/address_data_manager.h"
 #include "components/autofill/core/browser/data_quality/addresses/profile_requirement_utils.h"
@@ -23,10 +22,10 @@ bool IsOriginPartOfDeletionInfo(const std::optional<url::Origin>& origin,
     return false;
   }
   return deletion_info.IsAllHistory() ||
-         std::ranges::contains(deletion_info.deleted_rows(), *origin,
-                               [](const history::URLRow& url_row) {
-                                 return url::Origin::Create(url_row.url());
-                               });
+         base::Contains(deletion_info.deleted_rows(), *origin,
+                        [](const history::URLRow& url_row) {
+                          return url::Origin::Create(url_row.url());
+                        });
 }
 
 }  // anonymous namespace
diff --git a/components/autofill/core/browser/form_parsing/address_field_parser_ng.cc b/components/autofill/core/browser/form_parsing/address_field_parser_ng.cc
index 6df081cb8d..c11efb0777 100644
--- a/components/autofill/core/browser/form_parsing/address_field_parser_ng.cc
+++ b/components/autofill/core/browser/form_parsing/address_field_parser_ng.cc
@@ -7,9 +7,9 @@
 #include <initializer_list>
 #include <ostream>
 #include <string_view>
-#include <utility>
 
 #include "base/strings/string_number_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/autofill_field.h"
 #include "components/autofill/core/browser/data_model/addresses/autofill_i18n_api.h"
 #include "components/autofill/core/browser/field_types.h"
@@ -244,7 +244,7 @@ void AddressFieldParserNG::FieldTypeInformation::InitializeIncompatibilities() {
     for (FieldType c2 : supported_field_types_) {
       // Comparing the underlying integer values of c1 and c2 is a speed
       // optimization to avoid redundant work that would happen due to symmetry.
-      if (std::to_underlying(c1) > std::to_underlying(c2)) {
+      if (base::to_underlying(c1) > base::to_underlying(c2)) {
         continue;
       }
       // Note: Don't inline the following line because two operator[]() calls
diff --git a/components/autofill/core/browser/form_parsing/credit_card_field_parser.cc b/components/autofill/core/browser/form_parsing/credit_card_field_parser.cc
index d58b424327..d40208fa4b 100644
--- a/components/autofill/core/browser/form_parsing/credit_card_field_parser.cc
+++ b/components/autofill/core/browser/form_parsing/credit_card_field_parser.cc
@@ -6,13 +6,13 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <memory>
 #include <string>
 #include <string_view>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
@@ -336,7 +336,7 @@ bool CreditCardFieldParser::LikelyCardYearSelectField(
     // While 23 is a valid expiration year, the selector is not a expiration
     // year selector. In case we find a single-digit entry, we reject this as
     // an expiration year selector.
-    if (std::ranges::contains(field.options(), u"2", option_projection)) {
+    if (base::Contains(field.options(), u"2", option_projection)) {
       return false;
     }
     auto is_substring = [](std::u16string_view option,
@@ -627,7 +627,7 @@ FieldType CreditCardFieldParser::DetermineExpirationYearType(
     // While 23 is a valid expiration year, the selector is not a expiration
     // year selector. In case we find a single-digit entry, we reject this as
     // an expiration year selector.
-    if (std::ranges::contains(field.options(), u"2", option_projection)) {
+    if (base::Contains(field.options(), u"2", option_projection)) {
       return false;
     }
     auto is_substring = [&year_needle](std::u16string_view option) {
@@ -759,7 +759,7 @@ CreditCardFieldParser::DetermineExpirationDateFormat(
   }
   // Add generic fallbacks.
   for (const char16_t* fallback : {u"/", u""}) {
-    if (!std::ranges::contains(separator_candidates, fallback)) {
+    if (!base::Contains(separator_candidates, fallback)) {
       separator_candidates.emplace_back(fallback);
     }
   }
diff --git a/components/autofill/core/browser/form_processing/label_processing_util.cc b/components/autofill/core/browser/form_processing/label_processing_util.cc
index f2261c7600..8ff7db547e 100644
--- a/components/autofill/core/browser/form_processing/label_processing_util.cc
+++ b/components/autofill/core/browser/form_processing/label_processing_util.cc
@@ -39,7 +39,7 @@ std::vector<std::u16string_view> SplitBySeparators(std::u16string_view label) {
           base::SplitStringPieceUsingSubstr(component, separator,
                                             base::TRIM_WHITESPACE,
                                             base::SPLIT_WANT_NONEMPTY);
-      new_components.append_range(subcomponents);
+      new_components.insert(new_components.end(), subcomponents.begin(), subcomponents.end());
     }
     components = std::move(new_components);
   }
diff --git a/components/autofill/core/browser/form_structure.cc b/components/autofill/core/browser/form_structure.cc
index 22d11a8618..1ca11c66b5 100644
--- a/components/autofill/core/browser/form_structure.cc
+++ b/components/autofill/core/browser/form_structure.cc
@@ -20,6 +20,7 @@
 #include "base/check_deref.h"
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/map_util.h"
 #include "base/containers/to_vector.h"
@@ -219,7 +220,7 @@ void FormStructure::RationalizeAndAssignSections(
   // The sections are mapped to consecutive natural numbers starting at 1.
   std::map<Section, size_t> section_id_map;
   for (const auto& field : fields_) {
-    if (!section_id_map.contains(field->section())) {
+    if (!base::Contains(section_id_map, field->section())) {
       size_t next_section_id = section_id_map.size() + 1;
       section_id_map[field->section()] = next_section_id;
     }
diff --git a/components/autofill/core/browser/form_structure_rationalization_engine.cc b/components/autofill/core/browser/form_structure_rationalization_engine.cc
index d7492b0084..a6e9192a01 100644
--- a/components/autofill/core/browser/form_structure_rationalization_engine.cc
+++ b/components/autofill/core/browser/form_structure_rationalization_engine.cc
@@ -4,9 +4,9 @@
 
 #include "components/autofill/core/browser/form_structure_rationalization_engine.h"
 
-#include <algorithm>
 #include <array>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/feature_list.h"
 #include "base/no_destructor.h"
@@ -146,7 +146,7 @@ namespace internal {
 bool IsEnvironmentConditionFulfilled(ParsingContext& context,
                                      const EnvironmentCondition& env) {
   if (!env.country_list.empty() &&
-      !std::ranges::contains(env.country_list, context.client_country)) {
+      !base::Contains(env.country_list, context.client_country)) {
     return false;
   }
 
diff --git a/components/autofill/core/browser/form_structure_rationalizer.cc b/components/autofill/core/browser/form_structure_rationalizer.cc
index 5749cbea90..c30f10468f 100644
--- a/components/autofill/core/browser/form_structure_rationalizer.cc
+++ b/components/autofill/core/browser/form_structure_rationalizer.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "components/autofill/core/browser/autofill_field.h"
 #include "components/autofill/core/browser/data_model/data_model_utils.h"
diff --git a/components/autofill/core/browser/form_structure_test_api.h b/components/autofill/core/browser/form_structure_test_api.h
index aaf81992cb..f28952355f 100644
--- a/components/autofill/core/browser/form_structure_test_api.h
+++ b/components/autofill/core/browser/form_structure_test_api.h
@@ -8,6 +8,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/raw_ref.h"
 #include "components/autofill/core/browser/form_structure.h"
diff --git a/components/autofill/core/browser/foundations/autofill_driver.cc b/components/autofill/core/browser/foundations/autofill_driver.cc
index dd5cdb220d..884d5722a5 100644
--- a/components/autofill/core/browser/foundations/autofill_driver.cc
+++ b/components/autofill/core/browser/foundations/autofill_driver.cc
@@ -4,8 +4,6 @@
 
 #include "components/autofill/core/browser/foundations/autofill_driver.h"
 
-#include <utility>
-
 #include "base/check.h"
 
 namespace autofill {
@@ -52,9 +50,9 @@ void AutofillDriver::SetLifecycleState(
   DCHECK(
       IsValidTransition(previous_lifecycle_state_, lifecycle_state_, new_state))
       << "Invalid AutofillDriver::LifecycleState change "
-      << std::to_underlying(previous_lifecycle_state_) << " -> "
-      << std::to_underlying(lifecycle_state_) << " -> "
-      << std::to_underlying(new_state);
+      << base::to_underlying(previous_lifecycle_state_) << " -> "
+      << base::to_underlying(lifecycle_state_) << " -> "
+      << base::to_underlying(new_state);
   previous_lifecycle_state_ = lifecycle_state_;
 #endif
 
diff --git a/components/autofill/core/browser/foundations/autofill_driver_router.cc b/components/autofill/core/browser/foundations/autofill_driver_router.cc
index 6720f1242e..32e3b1f522 100644
--- a/components/autofill/core/browser/foundations/autofill_driver_router.cc
+++ b/components/autofill/core/browser/foundations/autofill_driver_router.cc
@@ -8,6 +8,7 @@
 #include <functional>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/debug/crash_logging.h"
 #include "base/metrics/histogram_functions.h"
@@ -139,8 +140,8 @@ void AutofillDriverRouter::FormsSeen(
   for (FormGlobalId renderer_form_id : renderer_form_ids) {
     const FormData& browser_form =
         form_forest_.GetBrowserForm(renderer_form_id);
-    if (!std::ranges::contains(browser_forms, browser_form.global_id(),
-                               &FormData::global_id)) {
+    if (!base::Contains(browser_forms, browser_form.global_id(),
+                        &FormData::global_id)) {
       browser_forms.push_back(browser_form);
     }
   }
@@ -149,8 +150,8 @@ void AutofillDriverRouter::FormsSeen(
 
   for (const FormGlobalId form_id : forms_with_removed_fields) {
     const FormData& browser_form = form_forest_.GetBrowserForm(form_id);
-    if (!std::ranges::contains(browser_forms, browser_form.global_id(),
-                               &FormData::global_id)) {
+    if (!base::Contains(browser_forms, browser_form.global_id(),
+                        &FormData::global_id)) {
       browser_forms.push_back(browser_form);
     }
   }
@@ -192,8 +193,8 @@ void AutofillDriverRouter::CaretMovedInFormField(
   form_forest_.UpdateTreeOfRendererForm(std::move(form), source);
 
   const FormData& browser_form = form_forest_.GetBrowserForm(form_id);
-  if (!std::ranges::contains(browser_form.fields(), field_id,
-                             &FormFieldData::global_id)) {
+  if (!base::Contains(browser_form.fields(), field_id,
+                      &FormFieldData::global_id)) {
     // To avoid very large flattened forms, UpdateTreeOfRendererForm() may have
     // cut the tree into two and, as a result, may have lost some fields. We
     // drop such events.
@@ -217,8 +218,8 @@ void AutofillDriverRouter::TextFieldValueChanged(
   TriggerFormExtractionExcept(source);
 
   const FormData& browser_form = form_forest_.GetBrowserForm(form_id);
-  if (!std::ranges::contains(browser_form.fields(), field_id,
-                             &FormFieldData::global_id)) {
+  if (!base::Contains(browser_form.fields(), field_id,
+                      &FormFieldData::global_id)) {
     // To avoid very large flattened forms, UpdateTreeOfRendererForm() may have
     // cut the tree into two and, as a result, may have lost some fields. We
     // drop such events.
@@ -240,8 +241,8 @@ void AutofillDriverRouter::TextFieldDidScroll(
   TriggerFormExtractionExcept(source);
 
   const FormData& browser_form = form_forest_.GetBrowserForm(form_id);
-  if (!std::ranges::contains(browser_form.fields(), field_id,
-                             &FormFieldData::global_id)) {
+  if (!base::Contains(browser_form.fields(), field_id,
+                      &FormFieldData::global_id)) {
     // To avoid very large flattened forms, UpdateTreeOfRendererForm() may have
     // cut the tree into two and, as a result, may have lost some fields. We
     // drop such events.
@@ -263,8 +264,8 @@ void AutofillDriverRouter::SelectControlSelectionChanged(
   TriggerFormExtractionExcept(source);
 
   const FormData& browser_form = form_forest_.GetBrowserForm(form_id);
-  if (!std::ranges::contains(browser_form.fields(), field_id,
-                             &FormFieldData::global_id)) {
+  if (!base::Contains(browser_form.fields(), field_id,
+                      &FormFieldData::global_id)) {
     // To avoid very large flattened forms, UpdateTreeOfRendererForm() may have
     // cut the tree into two and, as a result, may have lost some fields. We
     // drop such events.
@@ -293,8 +294,8 @@ void AutofillDriverRouter::AskForValuesToFill(
   TriggerFormExtractionExcept(source);
 
   const FormData& browser_form = form_forest_.GetBrowserForm(form_id);
-  if (!std::ranges::contains(browser_form.fields(), field_id,
-                             &FormFieldData::global_id)) {
+  if (!base::Contains(browser_form.fields(), field_id,
+                      &FormFieldData::global_id)) {
     // To avoid very large flattened forms, UpdateTreeOfRendererForm() may have
     // cut the tree into two and, as a result, may have lost some fields. We
     // drop such events.
@@ -378,8 +379,8 @@ void AutofillDriverRouter::FocusOnFormField(
   TriggerFormExtractionExcept(source);
 
   const FormData& browser_form = form_forest_.GetBrowserForm(form_id);
-  if (!std::ranges::contains(browser_form.fields(), field_id,
-                             &FormFieldData::global_id)) {
+  if (!base::Contains(browser_form.fields(), field_id,
+                      &FormFieldData::global_id)) {
     // To avoid very large flattened forms, UpdateTreeOfRendererForm() may have
     // cut the tree into two and, as a result, may have lost some fields. We
     // drop such events.
@@ -443,8 +444,8 @@ void AutofillDriverRouter::JavaScriptChangedAutofilledValue(
   TriggerFormExtractionExcept(source);
 
   const FormData& browser_form = form_forest_.GetBrowserForm(form_id);
-  if (!std::ranges::contains(browser_form.fields(), field_id,
-                             &FormFieldData::global_id)) {
+  if (!base::Contains(browser_form.fields(), field_id,
+                      &FormFieldData::global_id)) {
     // To avoid very large flattened forms, UpdateTreeOfRendererForm() may have
     // cut the tree into two and, as a result, may have lost some fields. We
     // drop such events.
diff --git a/components/autofill/core/browser/foundations/autofill_manager.cc b/components/autofill/core/browser/foundations/autofill_manager.cc
index f4f551f1fd..4619c8066c 100644
--- a/components/autofill/core/browser/foundations/autofill_manager.cc
+++ b/components/autofill/core/browser/foundations/autofill_manager.cc
@@ -13,6 +13,7 @@
 #include "base/check_deref.h"
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/containers/to_vector.h"
 #include "base/feature_list.h"
@@ -621,7 +622,7 @@ void AutofillManager::ParseFormsAsync(
   std::vector<FormData> parseable_forms;
   parseable_forms.reserve(forms.size());
   for (const FormData& form : forms) {
-    bool is_new_form = !form_structures_.contains(form.global_id());
+    bool is_new_form = !base::Contains(form_structures_, form.global_id());
     if (num_managed_forms + is_new_form > kAutofillManagerMaxFormCacheSize) {
       LOG_AF(log_manager()) << LoggingScope::kAbortParsing
                             << LogMessage::kAbortParsingTooManyForms << form;
@@ -647,7 +648,7 @@ void AutofillManager::ParseFormAsync(
     base::OnceCallback<void(AutofillManager&, const FormData&)> callback) {
   SCOPED_UMA_HISTOGRAM_TIMER("Autofill.Timing.ParseFormAsync");
 
-  bool is_new_form = !form_structures_.contains(form.global_id());
+  bool is_new_form = !base::Contains(form_structures_, form.global_id());
   if (form_structures_.size() + is_new_form >
       kAutofillManagerMaxFormCacheSize) {
     LOG_AF(log_manager()) << LoggingScope::kAbortParsing
diff --git a/components/autofill/core/browser/foundations/browser_autofill_manager.cc b/components/autofill/core/browser/foundations/browser_autofill_manager.cc
index 0d6c17ce71..1560e2c5f1 100644
--- a/components/autofill/core/browser/foundations/browser_autofill_manager.cc
+++ b/components/autofill/core/browser/foundations/browser_autofill_manager.cc
@@ -30,6 +30,7 @@
 #include "base/check_op.h"
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/map_util.h"
@@ -57,6 +58,7 @@
 #include "base/task/thread_pool.h"
 #include "base/threading/thread_restrictions.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/zip.h"
 #include "base/uuid.h"
 #include "build/build_config.h"
@@ -827,7 +829,7 @@ void ReorderWebauthnFallbackToFooter(std::vector<Suggestion>& suggestions) {
 
   // Without "Manage" suggestions, ensure a separator for the footer exists.
   if (!has_other_management_items &&
-      !std::ranges::contains(suggestions, SuggestionType::kSeparator,
+      !base::Contains(suggestions, SuggestionType::kSeparator,
                              &Suggestion::type)) {
     suggestions.emplace(new_pos, SuggestionType::kSeparator);
   }
@@ -1347,8 +1349,10 @@ void BrowserAutofillManager::OnIndividualSuggestionsGenerated(
   // TODO(crbug.com/409962888): Add logic to discard/merge
   // `returned_suggestions` into a single list.
   std::vector<Suggestion> suggestions;
-  for (auto& [filling_product, filling_suggestions] : returned_suggestions) {
-    base::Extend(suggestions, std::move(filling_suggestions));
+  for (const auto& [filling_product, filling_suggestions] :
+       returned_suggestions) {
+    suggestions.insert(suggestions.end(), filling_suggestions.begin(),
+                       filling_suggestions.end());
   }
 
   OnGenerateSuggestionsComplete(form_id, field_id, trigger_source, context,
@@ -1690,8 +1694,12 @@ void BrowserAutofillManager::
   suggestions.reserve(plus_address_suggestions.size() +
                       single_field_suggestions.size());
   // Prioritize plus address over single field form fill suggestions.
-  base::Extend(suggestions, plus_address_suggestions);
-  base::Extend(suggestions, std::move(single_field_suggestions));
+  suggestions.insert(suggestions.cend(),
+                     std::make_move_iterator(plus_address_suggestions.begin()),
+                     std::make_move_iterator(plus_address_suggestions.end()));
+  suggestions.insert(suggestions.cend(),
+                     std::make_move_iterator(single_field_suggestions.begin()),
+                     std::make_move_iterator(single_field_suggestions.end()));
 
   if (suggestions.empty()) {
     // Note the check below is the same done for regular autocomplete
@@ -1822,7 +1830,11 @@ void BrowserAutofillManager::MixPlusAddressAndAddressSuggestions(
         client().GetPlusAddressDelegate()->GetManagePlusAddressSuggestion());
   }
   // Mix both types of suggestions.
-  base::Extend(plus_address_suggestions, std::move(address_suggestions));
+  plus_address_suggestions.insert(
+      plus_address_suggestions.cend(),
+      std::make_move_iterator(address_suggestions.begin()),
+      std::make_move_iterator(address_suggestions.end()));
+
   std::move(callback).Run(/*show_suggestions=*/true,
                           std::move(plus_address_suggestions));
 }
@@ -2328,7 +2340,7 @@ void BrowserAutofillManager::DidShowSuggestions(
         base::JoinString(base::ToVector(shown_suggestion_types,
                                         [](SuggestionType type) {
                                           return base::NumberToString(
-                                              std::to_underlying(type));
+                                              base::to_underlying(type));
                                         }),
                          ","));
 
@@ -2346,8 +2358,8 @@ void BrowserAutofillManager::DidShowSuggestions(
     return;
   }
 
-  if (std::ranges::contains(shown_suggestion_types, FillingProduct::kCreditCard,
-                            GetFillingProductFromSuggestionType) &&
+  if (base::Contains(shown_suggestion_types, FillingProduct::kCreditCard,
+                     GetFillingProductFromSuggestionType) &&
       IsCreditCardFidoAuthenticationEnabled()) {
     GetCreditCardAccessManager()->PrepareToFetchCreditCard();
   }
diff --git a/components/autofill/core/browser/foundations/form_forest.cc b/components/autofill/core/browser/foundations/form_forest.cc
index f0140dd047..be91686996 100644
--- a/components/autofill/core/browser/foundations/form_forest.cc
+++ b/components/autofill/core/browser/foundations/form_forest.cc
@@ -14,6 +14,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/stack.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -193,8 +194,8 @@ void FormForest::UpdateTreeOfRendererForm(FormData* form,
     *old_form = std::move(*form);
     form = old_form;
   } else {
-    DCHECK(!std::ranges::contains(frame->child_forms, form->renderer_id(),
-                                  &FormData::renderer_id));
+    DCHECK(!base::Contains(frame->child_forms, form->renderer_id(),
+                           &FormData::renderer_id));
     form->set_fields({});
     child_frames_changed = false;
     frame->child_forms.push_back(std::move(*form));
diff --git a/components/autofill/core/browser/foundations/test_autofill_client.h b/components/autofill/core/browser/foundations/test_autofill_client.h
index f95aa3003a..465e2d1682 100644
--- a/components/autofill/core/browser/foundations/test_autofill_client.h
+++ b/components/autofill/core/browser/foundations/test_autofill_client.h
@@ -504,8 +504,8 @@ class TestAutofillClientTemplate : public T {
     GetPrefs()->registry()->RegisterIntegerPref(
         optimization_guide::prefs::
             kAutofillPredictionImprovementsEnterprisePolicyAllowed,
-        std::to_underlying(optimization_guide::model_execution::prefs::
-                               ModelExecutionEnterprisePolicyValue::kAllow),
+        base::to_underlying(optimization_guide::model_execution::prefs::
+                                ModelExecutionEnterprisePolicyValue::kAllow),
         PrefRegistry::LOSSY_PREF);
 
     identity_test_environment().MakePrimaryAccountAvailable(
diff --git a/components/autofill/core/browser/foundations/test_autofill_driver.h b/components/autofill/core/browser/foundations/test_autofill_driver.h
index bb7c5a5a23..e278e5e5ed 100644
--- a/components/autofill/core/browser/foundations/test_autofill_driver.h
+++ b/components/autofill/core/browser/foundations/test_autofill_driver.h
@@ -5,7 +5,6 @@
 #ifndef COMPONENTS_AUTOFILL_CORE_BROWSER_FOUNDATIONS_TEST_AUTOFILL_DRIVER_H_
 #define COMPONENTS_AUTOFILL_CORE_BROWSER_FOUNDATIONS_TEST_AUTOFILL_DRIVER_H_
 
-#include <algorithm>
 #include <concepts>
 #include <map>
 #include <string>
@@ -139,7 +138,7 @@ class TestAutofillDriverTemplate : public T {
     for (const auto& [id, type] : field_type_map) {
       if ((!field_type_map_filter_ ||
            field_type_map_filter_.Run(triggered_origin, id, type)) &&
-          std::ranges::contains(fields, id, &FormFieldData::global_id)) {
+          base::Contains(fields, id, &FormFieldData::global_id)) {
         result.push_back(id);
       }
     }
diff --git a/components/autofill/core/browser/foundations/test_autofill_driver_factory.cc b/components/autofill/core/browser/foundations/test_autofill_driver_factory.cc
index 1520935717..bc0ece179f 100644
--- a/components/autofill/core/browser/foundations/test_autofill_driver_factory.cc
+++ b/components/autofill/core/browser/foundations/test_autofill_driver_factory.cc
@@ -4,9 +4,8 @@
 
 #include "components/autofill/core/browser/foundations/test_autofill_driver_factory.h"
 
-#include <algorithm>
-
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "components/autofill/core/browser/foundations/test_autofill_driver.h"
 
@@ -31,8 +30,7 @@ TestAutofillDriverFactory::~TestAutofillDriverFactory() {
 }
 
 bool TestAutofillDriverFactory::IsOwned(TestAutofillDriver& driver) const {
-  return std::ranges::contains(drivers_, &driver,
-                               &ToPointer<TestAutofillDriver>);
+  return base::Contains(drivers_, &driver, &ToPointer<TestAutofillDriver>);
 }
 
 TestAutofillDriver& TestAutofillDriverFactory::TakeOwnership(
diff --git a/components/autofill/core/browser/geo/alternative_state_name_map_updater.cc b/components/autofill/core/browser/geo/alternative_state_name_map_updater.cc
index 80e6931f23..d9db7e6bba 100644
--- a/components/autofill/core/browser/geo/alternative_state_name_map_updater.cc
+++ b/components/autofill/core/browser/geo/alternative_state_name_map_updater.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -135,8 +136,7 @@ void AlternativeStateNameMapUpdater::LoadStatesData(
   std::erase_if(country_to_state_names_map,
                 [&country_codes](
                     const CountryToStateNamesListMapping::value_type& entry) {
-                  return !std::ranges::contains(country_codes,
-                                                entry.first.value());
+                  return !base::Contains(country_codes, entry.first.value());
                 });
 
   // If there is no valid country to be processed, return early.
@@ -153,7 +153,7 @@ void AlternativeStateNameMapUpdater::LoadStatesData(
   for (const auto& [country_code, states] : country_to_state_names_map) {
     // This is a security check to ensure that we only attempt to read files
     // that match to known countries.
-    if (!std::ranges::contains(country_codes, country_code.value())) {
+    if (!base::Contains(country_codes, country_code.value())) {
       continue;
     }
 
diff --git a/components/autofill/core/browser/geo/autofill_country.cc b/components/autofill/core/browser/geo/autofill_country.cc
index 66030d13ed..27d1bee8c4 100644
--- a/components/autofill/core/browser/geo/autofill_country.cc
+++ b/components/autofill/core/browser/geo/autofill_country.cc
@@ -6,12 +6,12 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <array>
 #include <optional>
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
@@ -84,8 +84,8 @@ std::string AutofillCountry::CountryCodeForLocale(std::string_view locale) {
   std::string country_code = icu::Locale(likely_locale.data()).getCountry();
 
   // Default to the United States if we have no better guess.
-  if (!std::ranges::contains(CountryDataMap::GetInstance()->country_codes(),
-                             country_code)) {
+  if (!base::Contains(CountryDataMap::GetInstance()->country_codes(),
+                      country_code)) {
     return "US";
   }
 
@@ -194,7 +194,7 @@ bool AutofillCountry::IsAddressFieldSettingAccessible(
   // or part of the Autofill's address extensions.
   return (is_valid_field && ::i18n::addressinput::IsFieldUsed(
                                 libaddressinput_field, country_code_)) ||
-         std::ranges::contains(
+         base::Contains(
              address_format_extensions(), field_type,
              [](const AddressFormatExtension& rule) { return rule.type; });
 }
diff --git a/components/autofill/core/browser/integrators/autofill_ai/autofill_ai_labels.cc b/components/autofill/core/browser/integrators/autofill_ai/autofill_ai_labels.cc
index db73506e6f..7a68c068ac 100644
--- a/components/autofill/core/browser/integrators/autofill_ai/autofill_ai_labels.cc
+++ b/components/autofill/core/browser/integrators/autofill_ai/autofill_ai_labels.cc
@@ -12,6 +12,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
@@ -83,7 +84,7 @@ std::pair<std::u16string, DenseSet<AttributeType>> GetValueAndTypesForLabel(
   static constexpr std::array kAirports = {
       AttributeType(kFlightReservationDepartureAirport),
       AttributeType(kFlightReservationArrivalAirport)};
-  if (std::ranges::contains(kAirports, type)) {
+  if (base::Contains(kAirports, type)) {
     // The label for flight airport information should be:
     // - Empty if no airport information is available.
     // - "DEPARTURE–ARRIVAL" if both the departure and arrival airports are
diff --git a/components/autofill/core/browser/integrators/autofill_ai/autofill_ai_manager.cc b/components/autofill/core/browser/integrators/autofill_ai/autofill_ai_manager.cc
index 2bfce5968e..d06ee3e34d 100644
--- a/components/autofill/core/browser/integrators/autofill_ai/autofill_ai_manager.cc
+++ b/components/autofill/core/browser/integrators/autofill_ai/autofill_ai_manager.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/flat_set.h"
@@ -458,10 +459,10 @@ bool AutofillAiManager::ShouldDisplayIph(const FormStructure& form,
   std::map<EntityType, DenseSet<AttributeType>> attributes_in_form;
   for (auto [entity, fields_and_types] : RationalizeAndDetermineAttributeTypes(
            form.fields(), focused_field->section())) {
-    if (std::ranges::contains(fields_and_types, focused_field->global_id(),
-                              [](const AutofillFieldWithAttributeType& f) {
-                                return f.field->global_id();
-                              }) &&
+    if (base::Contains(fields_and_types, focused_field->global_id(),
+                       [](const AutofillFieldWithAttributeType& f) {
+                         return f.field->global_id();
+                       }) &&
         MayPerformAutofillAiAction(*client_, AutofillAiAction::kIphForOptIn,
                                    entity)) {
       attributes_in_form[entity].insert_all(
diff --git a/components/autofill/core/browser/integrators/autofill_ai/metrics/autofill_ai_ukm_logger.cc b/components/autofill/core/browser/integrators/autofill_ai/metrics/autofill_ai_ukm_logger.cc
index 3875e4c09e..2a1c742a4b 100644
--- a/components/autofill/core/browser/integrators/autofill_ai/metrics/autofill_ai_ukm_logger.cc
+++ b/components/autofill/core/browser/integrators/autofill_ai/metrics/autofill_ai_ukm_logger.cc
@@ -7,10 +7,10 @@
 #include <algorithm>
 #include <cstdint>
 #include <type_traits>
-#include <utility>
 
 #include "base/check_deref.h"
 #include "base/notreached.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/autofill_field.h"
 #include "components/autofill/core/browser/data_model/autofill_ai/entity_instance.h"
 #include "components/autofill/core/browser/field_types.h"
@@ -256,7 +256,7 @@ void AutofillAiUkmLogger::LogKeyMetrics(ukm::SourceId ukm_source_id,
       .SetOptInStatus(opt_in_status)
       .SetAutofillFilledFieldCount(autofill_filled_field_count)
       .SetAutofillAiFilledFieldCount(autofill_ai_filled_field_count)
-      .SetEntityType(std::to_underlying(entity_type.name()));
+      .SetEntityType(base::to_underlying(entity_type.name()));
   if (suggestions_shown) {
     builder.SetFillingAcceptance(suggestion_filled);
   }
@@ -313,7 +313,7 @@ void AutofillAiUkmLogger::LogImportPromptResult(
   ukm::builders::AutofillAi_UserPromptMetrics(ukm_source_id)
       .SetFormSessionIdentifier(form_session_id)
       .SetFormSignature(CalculateFormSignature(form).value())
-      .SetEntityType(std::to_underlying(entity_type.name()))
+      .SetEntityType(base::to_underlying(entity_type.name()))
       .SetStorageType(GetStorageType(record_type))
       .SetPromptType(GetPromptType(prompt_type))
       .SetResult(GetUserDecision(result))
@@ -364,9 +364,9 @@ void AutofillAiUkmLogger::LogFieldEvent(ukm::SourceId ukm_source_id,
     mqls_field_event->set_field_rank(field.rank());
     mqls_field_event->set_field_rank_in_signature_group(
         field.rank_in_signature_group());
-    mqls_field_event->set_field_type(std::to_underlying(
+    mqls_field_event->set_field_type(base::to_underlying(
         !field_types.empty() ? *field_types.begin() : UNKNOWN_TYPE));
-    mqls_field_event->set_ai_field_type(std::to_underlying(
+    mqls_field_event->set_ai_field_type(base::to_underlying(
         !ai_field_types.empty() ? *ai_field_types.begin() : UNKNOWN_TYPE));
     for (FieldType field_type : field_types) {
       mqls_field_event->add_field_types(field_type);
@@ -409,7 +409,7 @@ void AutofillAiUkmLogger::LogFieldEvent(ukm::SourceId ukm_source_id,
   e.SetFormSessionEventOrder(form_event_order);
   e.SetFieldSignature(HashFieldSignature(field.GetFieldSignature()));
   e.SetFieldSessionIdentifier(field_session_identifier);
-  e.SetFormatStringSource(std::to_underlying(field.format_string_source()));
+  e.SetFormatStringSource(base::to_underlying(field.format_string_source()));
   maybe_set(e, &UkmEvent::SetFieldType, next_field_type());
   maybe_set(e, &UkmEvent::SetFieldType2, next_field_type());
   maybe_set(e, &UkmEvent::SetFieldType3, next_field_type());
@@ -418,8 +418,8 @@ void AutofillAiUkmLogger::LogFieldEvent(ukm::SourceId ukm_source_id,
   maybe_set(e, &UkmEvent::SetAiFieldType2, next_ai_field_type());
   maybe_set(e, &UkmEvent::SetAiFieldType3, next_ai_field_type());
   maybe_set(e, &UkmEvent::SetAiFieldType4, next_ai_field_type());
-  e.SetEventType(std::to_underlying(event_type));
-  e.SetEntityType(std::to_underlying(entity_type.name()));
+  e.SetEventType(base::to_underlying(event_type));
+  e.SetEntityType(base::to_underlying(entity_type.name()));
   e.Record(client_->GetUkmRecorder());
 }
 
diff --git a/components/autofill/core/browser/integrators/one_time_tokens/otp_suggestion.cc b/components/autofill/core/browser/integrators/one_time_tokens/otp_suggestion.cc
index adf86c39f4..887cfdc89d 100644
--- a/components/autofill/core/browser/integrators/one_time_tokens/otp_suggestion.cc
+++ b/components/autofill/core/browser/integrators/one_time_tokens/otp_suggestion.cc
@@ -4,9 +4,9 @@
 
 #include "components/autofill/core/browser/integrators/one_time_tokens/otp_suggestion.h"
 
-#include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "components/autofill/core/browser/autofill_field.h"
 #include "components/autofill/core/browser/field_types.h"
 #include "components/autofill/core/browser/form_structure.h"
@@ -28,7 +28,7 @@ OtpFillData CreateFillDataForOtpSuggestion(const FormStructure& form,
   const FieldGlobalId& field_id = trigger_field.global_id();
 
   // Check if the triggering field is not classified as OTP field anymore.
-  if (!std::ranges::contains(otp_field_ids, field_id)) {
+  if (!base::Contains(otp_field_ids, field_id)) {
     // The only way this could happen is if the form has changed between the
     // field focus and the filling moment. Fill into the current field, since
     // the user requested that and otherwise it would be weird.
diff --git a/components/autofill/core/browser/integrators/optimization_guide/autofill_optimization_guide_decider.cc b/components/autofill/core/browser/integrators/optimization_guide/autofill_optimization_guide_decider.cc
index b50836cfde..cbcf70de47 100644
--- a/components/autofill/core/browser/integrators/optimization_guide/autofill_optimization_guide_decider.cc
+++ b/components/autofill/core/browser/integrators/optimization_guide/autofill_optimization_guide_decider.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "components/autofill/core/browser/autofill_field.h"
 #include "components/autofill/core/browser/data_manager/payments/payments_data_manager.h"
@@ -133,9 +134,12 @@ void AddCreditCardOptimizationTypes(
     // optimizations.
     if (base::FeatureList::IsEnabled(
             features::kAutofillEnableCardBenefitsSync)) {
-      optimization_types.insert_range(
-          base::RangeAsRvalues(GetCardBenefitsOptimizationTypesForCard(
-              *card, payments_data_manager)));
+      auto benefits_optimization_types =
+          GetCardBenefitsOptimizationTypesForCard(*card, payments_data_manager);
+      if (!benefits_optimization_types.empty()) {
+        optimization_types.insert(benefits_optimization_types.begin(),
+                                  benefits_optimization_types.end());
+      }
     }
   }
 }
@@ -210,8 +214,7 @@ void AddOptimizationTypesForBnplIssuers(
     BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID)
   auto bnpl_issuer_allowlist_can_be_loaded =
       [&bnpl_issuers](BnplIssuer::IssuerId issuer_id) {
-        return std::ranges::contains(bnpl_issuers, issuer_id,
-                                     &BnplIssuer::issuer_id);
+        return base::Contains(bnpl_issuers, issuer_id, &BnplIssuer::issuer_id);
       };
 
   if (bnpl_issuer_allowlist_can_be_loaded(BnplIssuer::IssuerId::kBnplAffirm)) {
diff --git a/components/autofill/core/browser/metrics/autofill_metrics.cc b/components/autofill/core/browser/metrics/autofill_metrics.cc
index cb61bc9dcb..b794538c3e 100644
--- a/components/autofill/core/browser/metrics/autofill_metrics.cc
+++ b/components/autofill/core/browser/metrics/autofill_metrics.cc
@@ -9,6 +9,7 @@
 #include <variant>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -17,6 +18,7 @@
 #include "base/strings/strcat.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/autofill_field.h"
 #include "components/autofill/core/browser/autofill_type.h"
 #include "components/autofill/core/browser/data_model/addresses/autofill_profile.h"
@@ -105,7 +107,7 @@ const std::string GetImageTypeString(
       return "ValuableImage";
   }
   NOTREACHED() << "Unhandled AutofillImageFetcherBase::ImageType "
-               << std::to_underlying(image_type);
+               << base::to_underlying(image_type);
 }
 
 }  // namespace
@@ -645,16 +647,16 @@ void AutofillMetrics::LogFormFillDurationFromInteraction(
     parent_metric = "Autofill.FillDuration.FromInteraction.WithoutAutofill";
   }
   LogFormFillDuration(parent_metric, duration);
-  if (form_types.contains(FormType::kCreditCardForm)) {
+  if (base::Contains(form_types, FormType::kCreditCardForm)) {
     LogFormFillDuration(parent_metric + ".CreditCard", duration);
   }
-  if (form_types.contains(FormType::kAddressForm)) {
+  if (base::Contains(form_types, FormType::kAddressForm)) {
     LogFormFillDuration(parent_metric + ".Address", duration);
   }
-  if (form_types.contains(FormType::kPasswordForm)) {
+  if (base::Contains(form_types, FormType::kPasswordForm)) {
     LogFormFillDuration(parent_metric + ".Password", duration);
   }
-  if (form_types.contains(FormType::kUnknownFormType)) {
+  if (base::Contains(form_types, FormType::kUnknownFormType)) {
     LogFormFillDuration(parent_metric + ".Unknown", duration);
   }
 }
diff --git a/components/autofill/core/browser/metrics/form_events/credit_card_form_event_logger.cc b/components/autofill/core/browser/metrics/form_events/credit_card_form_event_logger.cc
index 277732cda6..f8579f6187 100644
--- a/components/autofill/core/browser/metrics/form_events/credit_card_form_event_logger.cc
+++ b/components/autofill/core/browser/metrics/form_events/credit_card_form_event_logger.cc
@@ -7,11 +7,13 @@
 #include <algorithm>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/user_metrics.h"
 #include "base/metrics/user_metrics_action.h"
 #include "base/strings/strcat.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/data_manager/payments/payments_data_manager.h"
 #include "components/autofill/core/browser/form_import/form_data_importer.h"
 #include "components/autofill/core/browser/foundations/browser_autofill_manager.h"
@@ -835,7 +837,7 @@ bool CreditCardFormEventLogger::DoesCardHaveOffer(
 
   auto card_linked_offer_map = offer_manager->GetCardLinkedOffersMap(
       client().GetLastCommittedPrimaryMainFrameURL());
-  return card_linked_offer_map.contains(credit_card.guid());
+  return base::Contains(card_linked_offer_map, credit_card.guid());
 }
 
 bool CreditCardFormEventLogger::DoSuggestionsIncludeVirtualCard() {
diff --git a/components/autofill/core/browser/metrics/form_interactions_ukm_logger.cc b/components/autofill/core/browser/metrics/form_interactions_ukm_logger.cc
index 909e01a673..1f8959f157 100644
--- a/components/autofill/core/browser/metrics/form_interactions_ukm_logger.cc
+++ b/components/autofill/core/browser/metrics/form_interactions_ukm_logger.cc
@@ -4,7 +4,6 @@
 
 #include "components/autofill/core/browser/metrics/form_interactions_ukm_logger.h"
 
-#include <utility>
 #include <variant>
 
 #include "base/check_deref.h"
@@ -111,7 +110,7 @@ void FormInteractionsUkmLogger::LogDidFillSuggestion(
 
   auto metric = ukm::builders::Autofill_SuggestionFilled(ukm_source_id);
   if (record_type) {
-    metric.SetRecordType(std::to_underlying(*record_type));
+    metric.SetRecordType(base::to_underlying(*record_type));
   }
   metric.SetIsForCreditCard(record_type.has_value())
       .SetMillisecondsSinceFormParsed(MillisecondsSinceFormParsed(
@@ -441,8 +440,8 @@ void FormInteractionsUkmLogger::LogAutofillFieldInfoAtFormRemove(
   builder.SetFormSessionIdentifier(FormGlobalIdToHash64Bit(form.global_id()))
       .SetFieldSessionIdentifier(FieldGlobalIdToHash64Bit(field.global_id()))
       .SetFieldSignature(HashFieldSignature(field.GetFieldSignature()))
-      .SetFormControlType2(std::to_underlying(field.form_control_type()))
-      .SetAutocompleteState(std::to_underlying(autocomplete_state))
+      .SetFormControlType2(base::to_underlying(field.form_control_type()))
+      .SetAutocompleteState(base::to_underlying(autocomplete_state))
       .SetFieldLogEventCount(field_log_events.size());
 
   SetStatusVector(AutofillStatus::kIsFocusable, field.is_focusable());
@@ -512,8 +511,8 @@ void FormInteractionsUkmLogger::LogAutofillFieldInfoAtFormRemove(
   }
 
   if (had_html_type) {
-    builder.SetHtmlFieldType(std::to_underlying(html_type))
-        .SetHtmlFieldMode(std::to_underlying(html_mode));
+    builder.SetHtmlFieldType(base::to_underlying(html_type))
+        .SetHtmlFieldMode(base::to_underlying(html_mode));
   }
 
   if (had_server_type) {
diff --git a/components/autofill/core/browser/metrics/profile_import_metrics.cc b/components/autofill/core/browser/metrics/profile_import_metrics.cc
index ba35510689..2981b2408b 100644
--- a/components/autofill/core/browser/metrics/profile_import_metrics.cc
+++ b/components/autofill/core/browser/metrics/profile_import_metrics.cc
@@ -4,9 +4,9 @@
 
 #include "components/autofill/core/browser/metrics/profile_import_metrics.h"
 
-#include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/i18n/char_iterator.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/strcat.h"
@@ -176,16 +176,16 @@ void LogAddressFormImportRequirementMetric(const AutofillProfile& profile) {
     LogAddressFormImportRequirementMetric(requirement);
   }
 
-  bool is_zip_missing = std::ranges::contains(
+  bool is_zip_missing = base::Contains(
       requirements,
       AddressProfileImportRequirementMetric::kZipRequirementViolated);
-  bool is_state_missing = std::ranges::contains(
+  bool is_state_missing = base::Contains(
       requirements,
       AddressProfileImportRequirementMetric::kStateRequirementViolated);
-  bool is_city_missing = std::ranges::contains(
+  bool is_city_missing = base::Contains(
       requirements,
       AddressProfileImportRequirementMetric::kCityRequirementViolated);
-  bool is_line1_missing = std::ranges::contains(
+  bool is_line1_missing = base::Contains(
       requirements,
       AddressProfileImportRequirementMetric::kLine1RequirementViolated);
   const auto metric =
diff --git a/components/autofill/core/browser/metrics/quality_metrics.cc b/components/autofill/core/browser/metrics/quality_metrics.cc
index 5101653ec7..a60906ef68 100644
--- a/components/autofill/core/browser/metrics/quality_metrics.cc
+++ b/components/autofill/core/browser/metrics/quality_metrics.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/span.h"
 #include "base/i18n/char_iterator.h"
diff --git a/components/autofill/core/browser/ml_model/field_classification_model_handler.cc b/components/autofill/core/browser/ml_model/field_classification_model_handler.cc
index 00689ee8a2..b17c88ffe7 100644
--- a/components/autofill/core/browser/ml_model/field_classification_model_handler.cc
+++ b/components/autofill/core/browser/ml_model/field_classification_model_handler.cc
@@ -9,7 +9,6 @@
 #include <vector>
 
 #include "base/barrier_callback.h"
-#include "base/containers/extend.h"
 #include "base/containers/span.h"
 #include "base/containers/to_vector.h"
 #include "base/functional/bind.h"
@@ -518,7 +517,8 @@ FieldClassificationModelHandler::CalculateModelInputHash(
   flattened_data.reserve(flattened_data_size);
   for (const std::vector<FieldClassificationModelEncoder::TokenId>&
            field_tokens : input) {
-    base::Extend(flattened_data, field_tokens);
+    flattened_data.insert(flattened_data.end(), field_tokens.begin(),
+                          field_tokens.end());
   }
 
   return ModelInputHash(base::FastHash(base::as_byte_span(flattened_data)));
diff --git a/components/autofill/core/browser/payments/autofill_offer_manager.cc b/components/autofill/core/browser/payments/autofill_offer_manager.cc
index f7717a62c5..3d42addea2 100644
--- a/components/autofill/core/browser/payments/autofill_offer_manager.cc
+++ b/components/autofill/core/browser/payments/autofill_offer_manager.cc
@@ -4,10 +4,10 @@
 
 #include "components/autofill/core/browser/payments/autofill_offer_manager.h"
 
-#include <algorithm>
 #include <ranges>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "components/autofill/core/browser/data_manager/payments/payments_data_manager.h"
 #include "components/autofill/core/browser/data_manager/personal_data_manager.h"
@@ -45,8 +45,8 @@ AutofillOfferManager::GetCardLinkedOffersMap(
   GURL last_committed_primary_main_frame_origin =
       last_committed_primary_main_frame_url.DeprecatedGetOriginAsURL();
 
-  if (!eligible_merchant_domains_.contains(
-          last_committed_primary_main_frame_origin)) {
+  if (!base::Contains(eligible_merchant_domains_,
+                      last_committed_primary_main_frame_origin)) {
     return {};
   }
 
@@ -70,8 +70,8 @@ AutofillOfferManager::GetCardLinkedOffersMap(
     for (const CreditCard* card : cards) {
       // If card has an offer, add the card's guid id to the map. There is
       // currently a one-to-one mapping between cards and offer data.
-      if (std::ranges::contains(offer->GetEligibleInstrumentIds(),
-                                card->instrument_id())) {
+      if (base::Contains(offer->GetEligibleInstrumentIds(),
+                         card->instrument_id())) {
         card_linked_offers_map[card->guid()] = offer;
       }
     }
@@ -82,7 +82,8 @@ AutofillOfferManager::GetCardLinkedOffersMap(
 
 bool AutofillOfferManager::IsUrlEligible(
     const GURL& last_committed_primary_main_frame_url) {
-  return eligible_merchant_domains_.contains(
+  return base::Contains(
+      eligible_merchant_domains_,
       last_committed_primary_main_frame_url.DeprecatedGetOriginAsURL());
 }
 
diff --git a/components/autofill/core/browser/payments/bnpl_manager.cc b/components/autofill/core/browser/payments/bnpl_manager.cc
index a887d6a7e5..3a950e4aa9 100644
--- a/components/autofill/core/browser/payments/bnpl_manager.cc
+++ b/components/autofill/core/browser/payments/bnpl_manager.cc
@@ -4,7 +4,6 @@
 
 #include "components/autofill/core/browser/payments/bnpl_manager.h"
 
-#include <algorithm>
 #include <cstdint>
 #include <optional>
 #include <string>
@@ -14,6 +13,7 @@
 
 #include "base/barrier_callback.h"
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -196,8 +196,8 @@ void BnplManager::NotifyOfSuggestionGeneration(
 void BnplManager::OnSuggestionsShown(
     base::span<const Suggestion> suggestions,
     UpdateSuggestionsCallback update_suggestions_callback) {
-  if (std::ranges::contains(suggestions, SuggestionType::kBnplEntry,
-                            &Suggestion::type) &&
+  if (base::Contains(suggestions, SuggestionType::kBnplEntry,
+                     &Suggestion::type) &&
       base::FeatureList::IsEnabled(
           features::kAutofillEnableAiBasedAmountExtraction)) {
     payments_autofill_client()
@@ -217,8 +217,8 @@ void BnplManager::OnSuggestionsShown(
     case kNotifyUpdateCallbackOfSuggestionsShownResponse:
       // The update suggestions callback attempts to add a BNPL entry to the
       // list of suggestions if no BNPL entry exists in the list.
-      if (!std::ranges::contains(suggestions, SuggestionType::kBnplEntry,
-                                 &Suggestion::type)) {
+      if (!base::Contains(suggestions, SuggestionType::kBnplEntry,
+                          &Suggestion::type)) {
         update_suggestions_barrier_callback_->Run(SuggestionsShownResponse(
             std::vector<Suggestion>(std::begin(suggestions),
                                     std::end(suggestions)),
@@ -346,9 +346,9 @@ void BnplManager::OnAmountExtractionReturnedFromAi(
 
 bool BnplManager::AcceptTosActionRequired() const {
   return ongoing_flow_state_->issuer->payment_instrument().has_value() &&
-         ongoing_flow_state_->issuer->payment_instrument()
-             ->action_required()
-             .contains(PaymentInstrument::ActionRequired::kAcceptTos);
+         base::Contains(ongoing_flow_state_->issuer->payment_instrument()
+                            ->action_required(),
+                        PaymentInstrument::ActionRequired::kAcceptTos);
 }
 
 bool BnplManager::HasSeenAmountExtractionAiTerms() const {
diff --git a/components/autofill/core/browser/payments/payments_requests/payments_request.cc b/components/autofill/core/browser/payments/payments_requests/payments_request.cc
index 8b54aee9b7..ce6fd73a44 100644
--- a/components/autofill/core/browser/payments/payments_requests/payments_request.cc
+++ b/components/autofill/core/browser/payments/payments_requests/payments_request.cc
@@ -9,6 +9,7 @@
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "build/build_config.h"
 #include "components/autofill/core/browser/data_model/form_group.h"
@@ -90,7 +91,7 @@ base::DictValue PaymentsRequest::BuildChromeUserContext(
   if (!client_behavior_signals.empty()) {
     base::ListValue active_client_signals;
     for (ClientBehaviorConstants signal : client_behavior_signals) {
-      active_client_signals.Append(std::to_underlying(signal));
+      active_client_signals.Append(base::to_underlying(signal));
     }
     std::ranges::sort(active_client_signals);
     chrome_user_context.Set("client_behavior_signals",
diff --git a/components/autofill/core/browser/permissions/autofill_ai/autofill_ai_permission_utils.cc b/components/autofill/core/browser/permissions/autofill_ai/autofill_ai_permission_utils.cc
index bf0f08a6a6..e596e2c7aa 100644
--- a/components/autofill/core/browser/permissions/autofill_ai/autofill_ai_permission_utils.cc
+++ b/components/autofill/core/browser/permissions/autofill_ai/autofill_ai_permission_utils.cc
@@ -6,13 +6,14 @@
 
 #include <algorithm>
 #include <string_view>
-#include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/notreached.h"
 #include "base/strings/string_split.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/country_type.h"
 #include "components/autofill/core/browser/data_manager/autofill_ai/entity_data_manager.h"
 #include "components/autofill/core/browser/data_model/autofill_ai/entity_type.h"
@@ -109,7 +110,7 @@ void MaybeOutputReason(std::string* out, std::string_view message) {
   // Parses `parameter` and returns whether any of the country codes is contains
   // match `country_code`.
   auto contains_geo_ip = [&country_code](std::string_view parameter) {
-    return std::ranges::contains(
+    return base::Contains(
         base::SplitStringPiece(parameter, ",",
                                base::WhitespaceHandling::TRIM_WHITESPACE,
                                base::SplitResult::SPLIT_WANT_NONEMPTY),
@@ -307,12 +308,12 @@ void MaybeOutputReason(std::string* out, std::string_view message) {
 
   // State of the AutofillAI-specific enterprise policy pref.
   constexpr int kAutofillPredictionSettingsAllowWithoutLogging =
-      std::to_underlying(
+      base::to_underlying(
           optimization_guide::model_execution::prefs::
               ModelExecutionEnterprisePolicyValue::kAllowWithoutLogging);
   constexpr int kAutofillPredictionSettingsDisabled =
-      std::to_underlying(optimization_guide::model_execution::prefs::
-                             ModelExecutionEnterprisePolicyValue::kDisable);
+      base::to_underlying(optimization_guide::model_execution::prefs::
+                              ModelExecutionEnterprisePolicyValue::kDisable);
   static_assert(kAutofillPredictionSettingsAllowWithoutLogging == 1);
   static_assert(kAutofillPredictionSettingsDisabled == 2);
 
diff --git a/components/autofill/core/browser/suggestions/addresses/address_suggestion_generator.cc b/components/autofill/core/browser/suggestions/addresses/address_suggestion_generator.cc
index a2f2d308b7..2280fe9915 100644
--- a/components/autofill/core/browser/suggestions/addresses/address_suggestion_generator.cc
+++ b/components/autofill/core/browser/suggestions/addresses/address_suggestion_generator.cc
@@ -8,7 +8,6 @@
 #include <functional>
 #include <optional>
 #include <string>
-#include <utility>
 #include <vector>
 
 #include "base/check_deref.h"
@@ -177,10 +176,11 @@ std::u16string GetFormattedPhoneNumber(const AutofillProfile& profile,
 std::u16string GetFullSuggestionText(const Suggestion& suggestion) {
   std::vector<Suggestion::Text> all_text_parts;
   all_text_parts.push_back(suggestion.main_text);
-  base::Extend(all_text_parts, suggestion.minor_texts);
+  all_text_parts.insert(all_text_parts.end(), suggestion.minor_texts.begin(),
+                        suggestion.minor_texts.end());
 
   for (const std::vector<Suggestion::Text>& label : suggestion.labels) {
-    base::Extend(all_text_parts, label);
+    all_text_parts.insert(all_text_parts.end(), label.begin(), label.end());
   }
 
   return base::CollapseWhitespace(
@@ -533,14 +533,14 @@ std::vector<AutofillProfile> GetProfilesToSuggest(
       SCOPED_CRASH_KEY_NUMBER("Autofill", "field_types_contains",
                               field_types.contains(trigger_field_type));
       SCOPED_CRASH_KEY_NUMBER("Autofill", "trigger_field_type",
-                              std::to_underlying(trigger_field_type));
+                              base::to_underlying(trigger_field_type));
       SCOPED_CRASH_KEY_NUMBER("Autofill", "size_before_filter",
                               size_before_filter);
       SCOPED_CRASH_KEY_NUMBER("Autofill", "trigger_field_value_size",
                               trigger_field.value().size());
       SCOPED_CRASH_KEY_NUMBER(
           "Autofill", "trigger_field_form_ctrl_type",
-          std::to_underlying(trigger_field.form_control_type()));
+          base::to_underlying(trigger_field.form_control_type()));
       base::debug::DumpWithoutCrashing();
     }
   }
diff --git a/components/autofill/core/browser/suggestions/autofill_ai/autofill_ai_suggestion_generator.cc b/components/autofill/core/browser/suggestions/autofill_ai/autofill_ai_suggestion_generator.cc
index 5c4a2e185f..2a97a66c52 100644
--- a/components/autofill/core/browser/suggestions/autofill_ai/autofill_ai_suggestion_generator.cc
+++ b/components/autofill/core/browser/suggestions/autofill_ai/autofill_ai_suggestion_generator.cc
@@ -60,9 +60,9 @@ namespace {
 // Note that an AutofillField may have multiple AttributeTypes of distinct
 // EntityTypes assigned. That is, it may happen that both of the following are
 // true:
-//   std::ranges::contains(assignment.Find(EntityType(kVehicle)),
+//   base::Contains(assignment.Find(EntityType(kVehicle)),
 //                  {field, AttributeType(kVehicleOwner));
-//   std::ranges::contains(assignment.Find(EntityType(kDriversLicense)),
+//   base::Contains(assignment.Find(EntityType(kDriversLicense)),
 //                  {field, AttributeType(kDriversLicenseName));
 class AttributeTypeAssignment {
  public:
diff --git a/components/autofill/core/browser/suggestions/payments/iban_suggestion_generator.cc b/components/autofill/core/browser/suggestions/payments/iban_suggestion_generator.cc
index a829e1f6d9..d935ad2d30 100644
--- a/components/autofill/core/browser/suggestions/payments/iban_suggestion_generator.cc
+++ b/components/autofill/core/browser/suggestions/payments/iban_suggestion_generator.cc
@@ -4,8 +4,7 @@
 
 #include "components/autofill/core/browser/suggestions/payments/iban_suggestion_generator.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/functional/function_ref.h"
 #include "base/strings/string_util.h"
@@ -176,8 +175,7 @@ void IbanSuggestionGenerator::FetchSuggestionData(
   // assume the IBAN has been filled, and don't show any suggestions.
   if (!trigger_autofill_field ||
       (!trigger_autofill_field->value().empty() &&
-       std::ranges::contains(ibans, trigger_autofill_field->value(),
-                             &Iban::value))) {
+       base::Contains(ibans, trigger_autofill_field->value(), &Iban::value))) {
     callback({SuggestionDataSource::kIban, {}});
     return;
   }
diff --git a/components/autofill/core/browser/suggestions/payments/payments_suggestion_generator_util.cc b/components/autofill/core/browser/suggestions/payments/payments_suggestion_generator_util.cc
index 60617634d3..ec980e7949 100644
--- a/components/autofill/core/browser/suggestions/payments/payments_suggestion_generator_util.cc
+++ b/components/autofill/core/browser/suggestions/payments/payments_suggestion_generator_util.cc
@@ -13,6 +13,7 @@
 #include <vector>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
@@ -746,8 +747,9 @@ BnplSuggestionUpdateResult MaybeUpdateDesktopSuggestionsWithBnpl(
       }
       suggestion_update_result.suggestions.push_back(CreateBnplSuggestion(
           std::move(bnpl_issuers), extracted_amount_in_micros));
-      base::Extend(suggestion_update_result.suggestions,
-                   current_suggestions.subspan(index));
+      suggestion_update_result.suggestions.insert(
+          suggestion_update_result.suggestions.end(),
+          current_suggestions.begin() + index, current_suggestions.end());
       suggestion_update_result.is_bnpl_suggestion_added = true;
       return suggestion_update_result;
     }
@@ -820,8 +822,7 @@ Suggestion CreateBnplSuggestion(
 #if !BUILDFLAG(IS_ANDROID)
   using IssuerId = BnplIssuer::IssuerId;
   auto issuer_present = [&bnpl_issuers](IssuerId issuer_id) {
-    return std::ranges::contains(bnpl_issuers, issuer_id,
-                                 &BnplIssuer::issuer_id);
+    return base::Contains(bnpl_issuers, issuer_id, &BnplIssuer::issuer_id);
   };
   bool affirm_present = issuer_present(IssuerId::kBnplAffirm);
   bool zip_present = issuer_present(IssuerId::kBnplZip);
@@ -1203,8 +1204,8 @@ std::vector<CreditCard> GetOrderedCardsToSuggest(
     std::ranges::stable_sort(
         available_cards,
         [&card_linked_offers_map](const CreditCard* a, const CreditCard* b) {
-          return card_linked_offers_map.contains(a->guid()) &&
-                 !card_linked_offers_map.contains(b->guid());
+          return base::Contains(card_linked_offers_map, a->guid()) &&
+                 !base::Contains(card_linked_offers_map, b->guid());
         });
   }
   // Suppress disused credit cards when triggered from an empty field.
@@ -1470,9 +1471,8 @@ GetVirtualCreditCardsForStandaloneCvcField(
         it != usage_data.end()) {
       VirtualCardUsageData::VirtualCardLastFour virtual_card_last_four =
           it->virtual_card_last_four();
-      if (std::ranges::contains(
-              four_digit_combinations_in_dom,
-              base::UTF16ToUTF8(virtual_card_last_four.value()))) {
+      if (base::Contains(four_digit_combinations_in_dom,
+                         base::UTF16ToUTF8(virtual_card_last_four.value()))) {
         // Card has usage data on webpage and last four is present in DOM.
         virtual_card_guid_to_last_four_map[credit_card->guid()] =
             virtual_card_last_four;
diff --git a/components/autofill/core/browser/suggestions/suggestion.h b/components/autofill/core/browser/suggestions/suggestion.h
index a75be0503c..fd3c82dedc 100644
--- a/components/autofill/core/browser/suggestions/suggestion.h
+++ b/components/autofill/core/browser/suggestions/suggestion.h
@@ -15,6 +15,7 @@
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/memory/raw_ptr.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/strong_alias.h"
 #include "build/build_config.h"
 #include "components/autofill/core/browser/data_model/autofill_ai/entity_instance.h"
diff --git a/components/autofill/core/browser/ui/addresses/autofill_address_util.cc b/components/autofill/core/browser/ui/addresses/autofill_address_util.cc
index 55ffa9bd4a..78eb32356a 100644
--- a/components/autofill/core/browser/ui/addresses/autofill_address_util.cc
+++ b/components/autofill/core/browser/ui/addresses/autofill_address_util.cc
@@ -11,6 +11,7 @@
 
 #include "autofill_address_util.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/memory/ptr_util.h"
 #include "base/not_fatal_until.h"
@@ -300,7 +301,7 @@ std::vector<ProfileValueDifference> GetProfileDifferenceForUi(
                      EMAIL_ADDRESS, PHONE_HOME_WHOLE_NUMBER});
 
   std::erase_if(differences_for_ui, [](const ProfileValueDifference& diff) {
-    return !std::ranges::contains(kPriorityOrder, diff.type);
+    return !base::Contains(kPriorityOrder, diff.type);
   });
 
   auto get_priority = [](FieldType type) -> size_t {
diff --git a/components/autofill/core/browser/ui/autofill_external_delegate.cc b/components/autofill/core/browser/ui/autofill_external_delegate.cc
index d11a703269..50ce297f4b 100644
--- a/components/autofill/core/browser/ui/autofill_external_delegate.cc
+++ b/components/autofill/core/browser/ui/autofill_external_delegate.cc
@@ -6,7 +6,6 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <functional>
 #include <iterator>
 #include <optional>
@@ -16,6 +15,7 @@
 #include "base/check.h"
 #include "base/check_deref.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/to_vector.h"
 #include "base/feature_list.h"
@@ -112,8 +112,8 @@ const Suggestion* FindTestSuggestion(AutofillClient& client,
     base::span<const AutofillProfile> test_addresses =
         client.GetTestAddresses();
 
-    return guid && std::ranges::contains(test_addresses, guid->value(),
-                                         &AutofillProfile::guid);
+    return guid && base::Contains(test_addresses, guid->value(),
+                                  &AutofillProfile::guid);
   };
   for (const Suggestion& suggestion : suggestions) {
     if (is_test_suggestion(suggestion) && index-- == 0) {
@@ -1179,8 +1179,8 @@ void AutofillExternalDelegate::DidAcceptAddressSuggestion(
           autofill_trigger_field &&
           autofill_trigger_field->Type().GetGroups().contains(
               FieldTypeGroup::kEmail) &&
-          std::ranges::contains(shown_suggestion_types_,
-                                SuggestionType::kFillExistingPlusAddress);
+          base::Contains(shown_suggestion_types_,
+                         SuggestionType::kFillExistingPlusAddress);
       if (const AutofillPlusAddressDelegate* plus_address_delegate =
               manager_->client().GetPlusAddressDelegate();
           plus_address_delegate && email_and_plus_address_shown) {
@@ -1346,8 +1346,8 @@ void AutofillExternalDelegate::DidAcceptPaymentsSuggestion(
     default:
       NOTREACHED();  // Should be handled elsewhere
   }
-  if (std::ranges::contains(shown_suggestion_types_,
-                            SuggestionType::kScanCreditCard)) {
+  if (base::Contains(shown_suggestion_types_,
+                     SuggestionType::kScanCreditCard)) {
     AutofillMetrics::LogScanCreditCardPromptMetric(
         suggestion.type == SuggestionType::kScanCreditCard
             ? AutofillMetrics::SCAN_CARD_ITEM_SELECTED
diff --git a/components/autofill/core/browser/webdata/autofill_ai/entity_table.cc b/components/autofill/core/browser/webdata/autofill_ai/entity_table.cc
index dcdd0ee5ce..136c3bc427 100644
--- a/components/autofill/core/browser/webdata/autofill_ai/entity_table.cc
+++ b/components/autofill/core/browser/webdata/autofill_ai/entity_table.cc
@@ -10,7 +10,6 @@
 #include <ranges>
 #include <string>
 #include <type_traits>
-#include <utility>
 
 #include "base/command_line.h"
 #include "base/feature_list.h"
@@ -21,6 +20,7 @@
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/uuid.h"
 #include "components/autofill/core/browser/autofill_field.h"
 #include "components/autofill/core/browser/data_model/addresses/autofill_structured_address_component.h"
@@ -410,7 +410,7 @@ bool EntityTable::AddEntityInstance(const EntityInstance& entity) {
   s.BindString(0, *entity.guid());
   s.BindString(1, entity.type().name_as_string());
   s.BindString(2, entity.nickname());
-  s.BindInt(3, std::to_underlying(entity.record_type()));
+  s.BindInt(3, base::to_underlying(entity.record_type()));
   s.BindBool(4, entity.are_attributes_read_only().value());
   s.BindString(5, entity.frecency_override(/*pass_key=*/{}));
 
@@ -655,7 +655,7 @@ std::vector<EntityInstance> EntityTable::GetEntityInstances(
       record_type.has_value()
           ? base::StrCat(
                 {"WHERE ", entities::kRecordType, "= ",
-                 base::NumberToString(std::to_underlying(*record_type))})
+                 base::NumberToString(base::to_underlying(*record_type))})
           : "";
   // Collects all entities and populates them with the attributes from the
   // previous query.
diff --git a/components/autofill/core/browser/webdata/autofill_webdata_backend_impl.cc b/components/autofill/core/browser/webdata/autofill_webdata_backend_impl.cc
index 34596a89fa..6f97cf753b 100644
--- a/components/autofill/core/browser/webdata/autofill_webdata_backend_impl.cc
+++ b/components/autofill/core/browser/webdata/autofill_webdata_backend_impl.cc
@@ -17,6 +17,7 @@
 #include "base/observer_list.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/task/sequenced_task_runner.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/uuid.h"
 #include "components/autofill/core/browser/data_model/autofill_ai/entity_instance.h"
 #include "components/autofill/core/browser/data_model/payments/autofill_offer_data.h"
diff --git a/components/autofill/core/browser/webdata/payments/payments_autofill_table.cc b/components/autofill/core/browser/webdata/payments/payments_autofill_table.cc
index 3506dcf239..976ead7052 100644
--- a/components/autofill/core/browser/webdata/payments/payments_autofill_table.cc
+++ b/components/autofill/core/browser/webdata/payments/payments_autofill_table.cc
@@ -1679,7 +1679,7 @@ bool PaymentsAutofillTable::SetCreditCardBenefits(
                              *benefit_base.linked_card_instrument_id());
     insert_benefit.BindInt(index++, benefit_type);
     insert_benefit.BindInt(
-        index++, std::to_underlying(std::visit(
+        index++, base::to_underlying(std::visit(
                      absl::Overload{
                          [](const CreditCardCategoryBenefit& a) {
                            return a.benefit_category();
diff --git a/components/autofill/core/browser/webdata/payments/payments_sync_bridge_util.cc b/components/autofill/core/browser/webdata/payments/payments_sync_bridge_util.cc
index 15f3c5a680..fc0fafb828 100644
--- a/components/autofill/core/browser/webdata/payments/payments_sync_bridge_util.cc
+++ b/components/autofill/core/browser/webdata/payments/payments_sync_bridge_util.cc
@@ -9,7 +9,6 @@
 
 #include "base/base64.h"
 #include "base/check.h"
-#include "base/containers/extend.h"
 #include "base/pickle.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
@@ -1012,8 +1011,12 @@ void PopulateWalletTypesFromSyncData(
       case sync_pb::AutofillWalletSpecifics::MASKED_CREDIT_CARD: {
         wallet_cards.push_back(
             CardFromSpecifics(autofill_specifics.masked_card()));
-        base::Extend(benefits, CreditCardBenefitsFromCardSpecifics(
-                                   autofill_specifics.masked_card()));
+
+        std::vector<CreditCardBenefit> benefits_from_specifics =
+            CreditCardBenefitsFromCardSpecifics(
+                autofill_specifics.masked_card());
+        benefits.insert(benefits.end(), benefits_from_specifics.begin(),
+                        benefits_from_specifics.end());
         break;
       }
       case sync_pb::AutofillWalletSpecifics::POSTAL_ADDRESS:
diff --git a/components/autofill/core/browser/webdata/valuables/valuable_metadata_sync_bridge.cc b/components/autofill/core/browser/webdata/valuables/valuable_metadata_sync_bridge.cc
index 2a1478da5e..7ce10969ce 100644
--- a/components/autofill/core/browser/webdata/valuables/valuable_metadata_sync_bridge.cc
+++ b/components/autofill/core/browser/webdata/valuables/valuable_metadata_sync_bridge.cc
@@ -503,7 +503,11 @@ EntityTable* ValuableMetadataSyncBridge::GetEntityTable() {
 std::optional<sync_pb::AutofillValuableMetadataSpecifics::PassType>
 ValuableMetadataSyncBridge::GetPassTypeForEntityId(
     const EntityInstance::EntityId& guid) {
-  return GetEntityTable()->GetEntityType(guid).and_then(EntityTypeToPassType);
+  auto v = GetEntityTable()->GetEntityType(guid);
+  if (v.has_value()) {
+    return EntityTypeToPassType(v.value());
+  }
+  return std::nullopt;
 }
 
 const EntityTable* ValuableMetadataSyncBridge::GetEntityTable() const {
diff --git a/components/autofill/core/common/autocomplete_parsing_util.cc b/components/autofill/core/common/autocomplete_parsing_util.cc
index 40fc788237..bc5d234ea0 100644
--- a/components/autofill/core/common/autocomplete_parsing_util.cc
+++ b/components/autofill/core/common/autocomplete_parsing_util.cc
@@ -185,12 +185,20 @@ HtmlFieldType FieldTypeFromAutocompleteAttributeValue(std::string value) {
     base::ReplaceFirstSubstringAfterOffset(&value, 0, "phone", "tel");
   }
 
-  return ParseStandardizedAutocompleteAttribute(value)
-      .or_else([&] { return ParseProposedAutocompleteAttribute(value); })
-      .or_else([&] { return ParseNonStandarizedAutocompleteAttribute(value); })
-      // `value` cannot be mapped to any HtmlFieldType. By classifying the field
-      // as HtmlFieldType::kUnrecognized Autofill is effectively disabled.
-      .value_or(HtmlFieldType::kUnrecognized);
+  std::optional<HtmlFieldType> type =
+      ParseStandardizedAutocompleteAttribute(value);
+  if (!type.has_value()) {
+    type = ParseProposedAutocompleteAttribute(value);
+    if (!type.has_value())
+      type = ParseNonStandarizedAutocompleteAttribute(value);
+  }
+
+  if (type.has_value())
+    return *type;
+
+  // `value` cannot be mapped to any HtmlFieldType. By classifying the field
+  // as HtmlFieldType::kUnrecognized Autofill is effectively disabled.
+  return HtmlFieldType::kUnrecognized;
 }
 
 std::optional<AutocompleteParsingResult> ParseAutocompleteAttribute(
diff --git a/components/autofill/core/common/autofill_data_validation.cc b/components/autofill/core/common/autofill_data_validation.cc
index 2bd82619c9..00850da4f4 100644
--- a/components/autofill/core/common/autofill_data_validation.cc
+++ b/components/autofill/core/common/autofill_data_validation.cc
@@ -5,8 +5,8 @@
 #include "components/autofill/core/common/autofill_data_validation.h"
 
 #include <algorithm>
-#include <utility>
 
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/common/autofill_constants.h"
 #include "components/autofill/core/common/autofill_util.h"
 #include "components/autofill/core/common/form_data.h"
@@ -35,10 +35,10 @@ bool IsValidOption(const SelectOption& option) {
 bool IsValidFormFieldData(const FormFieldData& field) {
   return IsValidString16(field.label()) && IsValidString16(field.name()) &&
          IsValidString16(field.value()) &&
-         std::to_underlying(field.form_control_type()) >=
-             std::to_underlying(FormControlType::kMinValue) &&
-         std::to_underlying(field.form_control_type()) <=
-             std::to_underlying(FormControlType::kMaxValue) &&
+         base::to_underlying(field.form_control_type()) >=
+             base::to_underlying(FormControlType::kMinValue) &&
+         base::to_underlying(field.form_control_type()) <=
+             base::to_underlying(FormControlType::kMaxValue) &&
          IsValidString(field.autocomplete_attribute()) &&
          IsValidOptionVector(field.options());
 }
diff --git a/components/autofill/core/common/dense_set.h b/components/autofill/core/common/dense_set.h
index 70c9889aeb..763db4dc0d 100644
--- a/components/autofill/core/common/dense_set.h
+++ b/components/autofill/core/common/dense_set.h
@@ -12,13 +12,13 @@
 #include <iterator>
 #include <ranges>
 #include <type_traits>
-#include <utility>
 
 #include "base/check.h"
 #include "base/check_op.h"
 #include "base/containers/span.h"
 #include "base/memory/raw_ptr.h"
 #include "base/numerics/safe_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 
 namespace autofill {
 
@@ -276,7 +276,7 @@ struct EnumDenseSetTraits {
     return static_cast<T>(x);
   }
   static constexpr UnderlyingType to_underlying(T x) {
-    return std::to_underlying(x);
+    return base::to_underlying(x);
   }
   static constexpr bool is_valid(T x) { return true; }
 
diff --git a/components/autofill/core/common/form_field_data.cc b/components/autofill/core/common/form_field_data.cc
index fa594af0f2..66422e9801 100644
--- a/components/autofill/core/common/form_field_data.cc
+++ b/components/autofill/core/common/form_field_data.cc
@@ -7,7 +7,6 @@
 #include <algorithm>
 #include <optional>
 #include <tuple>
-#include <utility>
 #include <variant>
 
 #include "base/i18n/rtl.h"
@@ -17,6 +16,7 @@
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/zip.h"
 #include "build/build_config.h"
 #include "components/autofill/core/common/autocomplete_parsing_util.h"
@@ -452,8 +452,8 @@ std::string_view FormControlTypeToString(FormControlType type) {
 
 std::optional<FormControlType> StringToFormControlTypeDiscouraged(
     std::string_view type_string) {
-  for (auto i = std::to_underlying(FormControlType::kMinValue);
-       i <= std::to_underlying(FormControlType::kMaxValue); ++i) {
+  for (auto i = base::to_underlying(FormControlType::kMinValue);
+       i <= base::to_underlying(FormControlType::kMaxValue); ++i) {
     FormControlType type = static_cast<FormControlType>(i);
     if (mojom::IsKnownEnumValue(type) &&
         type_string == FormControlTypeToString(type) &&
diff --git a/components/autofill/core/common/logging/stream_operator_util.h b/components/autofill/core/common/logging/stream_operator_util.h
index 4795bcca8c..1d24730bd1 100644
--- a/components/autofill/core/common/logging/stream_operator_util.h
+++ b/components/autofill/core/common/logging/stream_operator_util.h
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/memory/stack_allocated.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "url/origin.h"
 
 namespace autofill {
@@ -48,7 +49,7 @@ std::ostream& operator<<(std::ostream& os, PrintWrapper<T> pw) {
     return os << PrintWrapper(pw.x.ToString());
   }
   if constexpr (std::is_enum_v<T>) {
-    return os << std::to_underlying(pw.x);
+    return os << base::to_underlying(pw.x);
   }
 }
 
diff --git a/components/autofill/ios/browser/autofill_driver_ios.mm b/components/autofill/ios/browser/autofill_driver_ios.mm
index d3393e193e..fca9bd3f73 100644
--- a/components/autofill/ios/browser/autofill_driver_ios.mm
+++ b/components/autofill/ios/browser/autofill_driver_ios.mm
@@ -4,7 +4,6 @@
 
 #import "components/autofill/ios/browser/autofill_driver_ios.h"
 
-#include <algorithm>
 #import <concepts>
 #import <functional>
 #import <optional>
@@ -13,6 +12,7 @@
 #import <variant>
 
 #import "base/check_deref.h"
+#import "base/containers/contains.h"
 #import "base/containers/to_vector.h"
 #import "base/feature_list.h"
 #import "base/functional/bind.h"
@@ -350,9 +350,8 @@ void AutofillDriverIOS::ExtractFormWithField(
                 }
                 auto it =
                     std::ranges::find_if(*forms, [&](const FormData& form) {
-                      return std::ranges::contains(form.fields(),
-                                                   field_renderer_id,
-                                                   &FormFieldData::renderer_id);
+                      return base::Contains(form.fields(), field_renderer_id,
+                                            &FormFieldData::renderer_id);
                     });
                 std::move(renderer_form_handler)
                     .Run(it == forms->end() ? std::nullopt
diff --git a/components/background_fetch/background_fetch_delegate_base.cc b/components/background_fetch/background_fetch_delegate_base.cc
index e3dc34f277..c7df459830 100644
--- a/components/background_fetch/background_fetch_delegate_base.cc
+++ b/components/background_fetch/background_fetch_delegate_base.cc
@@ -4,10 +4,10 @@
 
 #include "components/background_fetch/background_fetch_delegate_base.h"
 
-#include <algorithm>
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/notreached.h"
@@ -431,7 +431,7 @@ bool BackgroundFetchDelegateBase::IsGuidOutstanding(
     return false;
   }
 
-  return std::ranges::contains(
+  return base::Contains(
       job_details_iter->second.fetch_description->outstanding_guids, guid);
 }
 
diff --git a/components/background_sync/background_sync_controller_impl.cc b/components/background_sync/background_sync_controller_impl.cc
index 57403ab75c..119300731d 100644
--- a/components/background_sync/background_sync_controller_impl.cc
+++ b/components/background_sync/background_sync_controller_impl.cc
@@ -4,6 +4,7 @@
 
 #include "components/background_sync/background_sync_controller_impl.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
@@ -129,8 +130,8 @@ void BackgroundSyncControllerImpl::GetParameterOverrides(
     parameters->skip_permissions_check_for_testing = true;
   }
 
-  if (field_params.contains(
-          kMaxAttemptsWithNotificationPermissionParameterName)) {
+  if (base::Contains(field_params,
+                     kMaxAttemptsWithNotificationPermissionParameterName)) {
     int max_attempts;
     if (base::StringToInt(
             field_params[kMaxAttemptsWithNotificationPermissionParameterName],
@@ -139,7 +140,7 @@ void BackgroundSyncControllerImpl::GetParameterOverrides(
     }
   }
 
-  if (field_params.contains(kMaxAttemptsParameterName)) {
+  if (base::Contains(field_params, kMaxAttemptsParameterName)) {
     int max_attempts;
     if (base::StringToInt(field_params[kMaxAttemptsParameterName],
                           &max_attempts)) {
@@ -147,7 +148,7 @@ void BackgroundSyncControllerImpl::GetParameterOverrides(
     }
   }
 
-  if (field_params.contains(kInitialRetryParameterName)) {
+  if (base::Contains(field_params, kInitialRetryParameterName)) {
     int initial_retry_delay_sec;
     if (base::StringToInt(field_params[kInitialRetryParameterName],
                           &initial_retry_delay_sec)) {
@@ -155,7 +156,7 @@ void BackgroundSyncControllerImpl::GetParameterOverrides(
     }
   }
 
-  if (field_params.contains(kRetryDelayFactorParameterName)) {
+  if (base::Contains(field_params, kRetryDelayFactorParameterName)) {
     int retry_delay_factor;
     if (base::StringToInt(field_params[kRetryDelayFactorParameterName],
                           &retry_delay_factor)) {
@@ -163,7 +164,7 @@ void BackgroundSyncControllerImpl::GetParameterOverrides(
     }
   }
 
-  if (field_params.contains(kMinSyncRecoveryTimeName)) {
+  if (base::Contains(field_params, kMinSyncRecoveryTimeName)) {
     int min_sync_recovery_time_sec;
     if (base::StringToInt(field_params[kMinSyncRecoveryTimeName],
                           &min_sync_recovery_time_sec)) {
@@ -172,7 +173,7 @@ void BackgroundSyncControllerImpl::GetParameterOverrides(
     }
   }
 
-  if (field_params.contains(kMaxSyncEventDurationName)) {
+  if (base::Contains(field_params, kMaxSyncEventDurationName)) {
     int max_sync_event_duration_sec;
     if (base::StringToInt(field_params[kMaxSyncEventDurationName],
                           &max_sync_event_duration_sec)) {
@@ -181,7 +182,7 @@ void BackgroundSyncControllerImpl::GetParameterOverrides(
     }
   }
 
-  if (field_params.contains(kMinPeriodicSyncEventsInterval)) {
+  if (base::Contains(field_params, kMinPeriodicSyncEventsInterval)) {
     int min_periodic_sync_events_interval_sec;
     if (base::StringToInt(field_params[kMinPeriodicSyncEventsInterval],
                           &min_periodic_sync_events_interval_sec)) {
diff --git a/components/bookmarks/browser/bookmark_codec.cc b/components/bookmarks/browser/bookmark_codec.cc
index 0d6b3de9c7..5d0baeaf1a 100644
--- a/components/bookmarks/browser/bookmark_codec.cc
+++ b/components/bookmarks/browser/bookmark_codec.cc
@@ -12,6 +12,7 @@
 #include <utility>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/json/json_string_value_serializer.h"
 #include "base/logging.h"
@@ -318,7 +319,7 @@ void BookmarkCodec::DecodeNode(const base::DictValue& value,
 
     // Guard against UUID collisions, which would violate BookmarkModel's
     // invariant that each UUID is unique.
-    if (uuids_.contains(uuid)) {
+    if (base::Contains(uuids_, uuid)) {
       uuid = base::Uuid::GenerateRandomV4();
       uuids_reassigned_ = true;
     }
diff --git a/components/bookmarks/browser/bookmark_model.cc b/components/bookmarks/browser/bookmark_model.cc
index 75dc0cd413..2b4e06b91a 100644
--- a/components/bookmarks/browser/bookmark_model.cc
+++ b/components/bookmarks/browser/bookmark_model.cc
@@ -13,6 +13,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
diff --git a/components/bookmarks/browser/bookmark_utils.cc b/components/bookmarks/browser/bookmark_utils.cc
index d5beb2ca11..abac9e464d 100644
--- a/components/bookmarks/browser/bookmark_utils.cc
+++ b/components/bookmarks/browser/bookmark_utils.cc
@@ -12,6 +12,7 @@
 #include <unordered_set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/i18n/case_conversion.h"
@@ -211,15 +212,13 @@ std::vector<const BookmarkNode*> GetMostRecentlyModifiedUserFolders(
   auto more_recently_modified = [account_permanent_nodes_possibly_null,
                                  default_node](const BookmarkNode* n1,
                                                const BookmarkNode* n2) {
-    base::Time t1 =
-        std::ranges::contains(account_permanent_nodes_possibly_null, n1)
-            ? std::max(n1->date_folder_modified(), n1->date_added())
-            : n1->date_folder_modified();
-
-    base::Time t2 =
-        std::ranges::contains(account_permanent_nodes_possibly_null, n2)
-            ? std::max(n2->date_folder_modified(), n2->date_added())
-            : n2->date_folder_modified();
+    base::Time t1 = base::Contains(account_permanent_nodes_possibly_null, n1)
+                        ? std::max(n1->date_folder_modified(), n1->date_added())
+                        : n1->date_folder_modified();
+
+    base::Time t2 = base::Contains(account_permanent_nodes_possibly_null, n2)
+                        ? std::max(n2->date_folder_modified(), n2->date_added())
+                        : n2->date_folder_modified();
 
     // If no node has been modified more recently, choose a default folder.
     return t1 == t2 ? (n1 == default_node || n2 != default_node) : (t1 > t2);
diff --git a/components/browser_ui/site_settings/android/website_preference_bridge.cc b/components/browser_ui/site_settings/android/website_preference_bridge.cc
index 47985c345d..f33a05f148 100644
--- a/components/browser_ui/site_settings/android/website_preference_bridge.cc
+++ b/components/browser_ui/site_settings/android/website_preference_bridge.cc
@@ -16,6 +16,7 @@
 #include "base/android/jni_string.h"
 #include "base/android/scoped_java_ref.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/json/json_reader.h"
@@ -231,7 +232,7 @@ void GetOrigins(JNIEnv* env,
 
   for (const auto& settings_it : embargo_settings) {
     const std::string origin = settings_it.primary_pattern.ToString();
-    if (std::ranges::contains(seen_origins, origin)) {
+    if (base::Contains(seen_origins, origin)) {
       // This origin has already been added to the list, so don't add it again.
       continue;
     }
@@ -1211,7 +1212,7 @@ static void JNI_WebsitePreferenceBridge_GetContentSettingsExceptions(
               static_cast<ContentSettingsType>(content_settings_type));
   ScopedJavaLocalRef<jstring> jembedder;
   for (const GURL& embargoed_origin : embargoed_origins) {
-    if (std::ranges::contains(seen_origins, embargoed_origin.spec())) {
+    if (base::Contains(seen_origins, embargoed_origin.spec())) {
       continue;
     }
     std::string embargoed_origin_pattern =
diff --git a/components/browsing_data/content/browsing_data_helper.cc b/components/browsing_data/content/browsing_data_helper.cc
index f6c1554bdb..4202c95ad2 100644
--- a/components/browsing_data/content/browsing_data_helper.cc
+++ b/components/browsing_data/content/browsing_data_helper.cc
@@ -4,9 +4,9 @@
 
 #include "components/browsing_data/content/browsing_data_helper.h"
 
-#include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/strings/utf_string_conversions.h"
 #include "build/build_config.h"
 #include "components/content_settings/core/browser/content_settings_registry.h"
@@ -67,7 +67,7 @@ bool IsSameHost(const std::string& host, const std::string& top_frame_host) {
 
 bool IsWebScheme(const std::string& scheme) {
   const std::vector<std::string>& schemes = url::GetWebStorageSchemes();
-  return std::ranges::contains(schemes, scheme);
+  return base::Contains(schemes, scheme);
 }
 
 bool HasWebScheme(const GURL& origin) {
diff --git a/components/browsing_data/content/browsing_data_model.cc b/components/browsing_data/content/browsing_data_model.cc
index dd737dcb96..fe15cea461 100644
--- a/components/browsing_data/content/browsing_data_model.cc
+++ b/components/browsing_data/content/browsing_data_model.cc
@@ -4,13 +4,13 @@
 
 #include "components/browsing_data/content/browsing_data_model.h"
 
-#include <algorithm>
 #include <set>
 #include <string>
 #include <variant>
 
 #include "base/barrier_closure.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -438,7 +438,7 @@ void StorageRemoverHelper::BackendFinished() {
 
 // Only websafe state is considered browsing data.
 bool HasStorageScheme(const url::Origin& origin) {
-  return std::ranges::contains(url::GetWebStorageSchemes(), origin.scheme());
+  return base::Contains(url::GetWebStorageSchemes(), origin.scheme());
 }
 
 void OnTrustTokenIssuanceInfoLoaded(
diff --git a/components/browsing_data/content/local_storage_helper.cc b/components/browsing_data/content/local_storage_helper.cc
index e2d8604654..c16f799a76 100644
--- a/components/browsing_data/content/local_storage_helper.cc
+++ b/components/browsing_data/content/local_storage_helper.cc
@@ -4,10 +4,10 @@
 
 #include "components/browsing_data/content/local_storage_helper.h"
 
-#include <algorithm>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "content/public/browser/browser_context.h"
@@ -27,7 +27,7 @@ namespace {
 
 // Only websafe state is considered browsing data.
 bool HasStorageScheme(const url::Origin& origin) {
-  return std::ranges::contains(url::GetWebStorageSchemes(), origin.scheme());
+  return base::Contains(url::GetWebStorageSchemes(), origin.scheme());
 }
 
 void GetUsageInfoCallback(LocalStorageHelper::FetchCallback callback,
diff --git a/components/browsing_data/content/mock_cookie_helper.cc b/components/browsing_data/content/mock_cookie_helper.cc
index 219edd5b2f..97e90f5977 100644
--- a/components/browsing_data/content/mock_cookie_helper.cc
+++ b/components/browsing_data/content/mock_cookie_helper.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/time/time.h"
 #include "net/cookies/cookie_options.h"
@@ -26,7 +27,7 @@ void MockCookieHelper::StartFetching(FetchCallback callback) {
 }
 
 void MockCookieHelper::DeleteCookie(const net::CanonicalCookie& cookie) {
-  ASSERT_TRUE(cookies_.contains(cookie));
+  ASSERT_TRUE(base::Contains(cookies_, cookie));
   cookies_[cookie] = false;
 }
 
diff --git a/components/browsing_data/core/browsing_data_policies_utils.cc b/components/browsing_data/core/browsing_data_policies_utils.cc
index 200ef0ccf4..23148f1e6d 100644
--- a/components/browsing_data/core/browsing_data_policies_utils.cc
+++ b/components/browsing_data/core/browsing_data_policies_utils.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <forward_list>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/span.h"
 #include "base/strings/stringprintf.h"
@@ -149,7 +150,7 @@ base::flat_set<std::string> GetBrowsingDataLifetimePlatformUnsupportedTypes(
     std::for_each(data_types.begin(), data_types.end(),
                   [&unsupported_types](const base::Value& type) {
                     const std::string& type_string = type.GetString();
-                    if (std::ranges::contains(kUnsupportedTypes, type_string)) {
+                    if (base::Contains(kUnsupportedTypes, type_string)) {
                       unsupported_types.push_front(type_string);
                     }
                   });
diff --git a/components/browsing_topics/browsing_topics_calculator.cc b/components/browsing_topics/browsing_topics_calculator.cc
index 4a3b0d7d44..afe9a2ac62 100644
--- a/components/browsing_topics/browsing_topics_calculator.cc
+++ b/components/browsing_topics/browsing_topics_calculator.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/rand_util.h"
 #include "base/task/sequenced_task_runner.h"
@@ -325,7 +326,7 @@ void BrowsingTopicsCalculator::DeriveTopTopics(
       uint64_t padded_topic_index_decision = GenerateRandUint64();
       padded_topic = semantic_tree.GetRandomTopic(taxonomy_version,
                                                   padded_topic_index_decision);
-    } while (std::ranges::contains(top_topics, padded_topic));
+    } while (base::Contains(top_topics, padded_topic));
 
     top_topics.emplace_back(std::move(padded_topic));
   }
diff --git a/components/browsing_topics/browsing_topics_page_load_data_tracker.cc b/components/browsing_topics/browsing_topics_page_load_data_tracker.cc
index e1dfa7397b..61e5d26be4 100644
--- a/components/browsing_topics/browsing_topics_page_load_data_tracker.cc
+++ b/components/browsing_topics/browsing_topics_page_load_data_tracker.cc
@@ -4,6 +4,7 @@
 
 #include "components/browsing_topics/browsing_topics_page_load_data_tracker.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "components/browsing_topics/util.h"
 #include "components/history/content/browser/history_context_helper.h"
diff --git a/components/browsing_topics/browsing_topics_redirect_observer.cc b/components/browsing_topics/browsing_topics_redirect_observer.cc
index 5de4b979bd..e9592fa27c 100644
--- a/components/browsing_topics/browsing_topics_redirect_observer.cc
+++ b/components/browsing_topics/browsing_topics_redirect_observer.cc
@@ -4,6 +4,7 @@
 
 #include "components/browsing_topics/browsing_topics_redirect_observer.h"
 
+#include "base/containers/contains.h"
 #include "components/browsing_topics/browsing_topics_page_load_data_tracker.h"
 #include "content/public/browser/navigation_controller.h"
 #include "content/public/browser/navigation_handle.h"
@@ -61,7 +62,8 @@ BrowsingTopicsRedirectObserver::PendingNavigationRedirectState::operator=(
 
 void BrowsingTopicsRedirectObserver::ReadyToCommitNavigation(
     content::NavigationHandle* navigation_handle) {
-  CHECK(!pending_navigations_redirect_state_.contains(navigation_handle));
+  CHECK(
+      !base::Contains(pending_navigations_redirect_state_, navigation_handle));
 
   if (!navigation_handle->IsInPrimaryMainFrame()) {
     return;
diff --git a/components/browsing_topics/epoch_topics.cc b/components/browsing_topics/epoch_topics.cc
index 705f490eb2..c72b9f5122 100644
--- a/components/browsing_topics/epoch_topics.cc
+++ b/components/browsing_topics/epoch_topics.cc
@@ -4,8 +4,7 @@
 
 #include "components/browsing_topics/epoch_topics.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/hash/legacy_hash.h"
 #include "base/json/values_util.h"
 #include "base/logging.h"
@@ -237,7 +236,7 @@ void EpochTopics::ClearTopic(Topic topic) {
     SemanticTree semantic_tree;
     std::vector<Topic> top_topic_ancestors =
         semantic_tree.GetAncestorTopics(top_topic_and_domains.topic());
-    if (std::ranges::contains(top_topic_ancestors, topic)) {
+    if (base::Contains(top_topic_ancestors, topic)) {
       top_topic_and_domains = TopicAndDomains();
     }
   }
diff --git a/components/cast_receiver/browser/permissions_manager_impl.cc b/components/cast_receiver/browser/permissions_manager_impl.cc
index 445c7725ad..e0f8f2f6d6 100644
--- a/components/cast_receiver/browser/permissions_manager_impl.cc
+++ b/components/cast_receiver/browser/permissions_manager_impl.cc
@@ -4,8 +4,7 @@
 
 #include "components/cast_receiver/browser/permissions_manager_impl.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "content/public/browser/web_contents.h"
 
 namespace cast_receiver {
@@ -64,7 +63,7 @@ const std::string& PermissionsManagerImpl::GetAppId() const {
 blink::mojom::PermissionStatus PermissionsManagerImpl::GetPermissionStatus(
     blink::PermissionType permission,
     const GURL& url) const {
-  if (!std::ranges::contains(permissions_, permission)) {
+  if (!base::Contains(permissions_, permission)) {
     return blink::mojom::PermissionStatus::DENIED;
   }
 
@@ -73,7 +72,7 @@ blink::mojom::PermissionStatus PermissionsManagerImpl::GetPermissionStatus(
     return blink::mojom::PermissionStatus::GRANTED;
   }
 
-  if (std::ranges::contains(additional_origins_, url_origin)) {
+  if (base::Contains(additional_origins_, url_origin)) {
     return blink::mojom::PermissionStatus::GRANTED;
   }
 
diff --git a/components/cast_receiver/browser/streaming_receiver_session_client.cc b/components/cast_receiver/browser/streaming_receiver_session_client.cc
index fd196f1ce6..bda4f5e4f8 100644
--- a/components/cast_receiver/browser/streaming_receiver_session_client.cc
+++ b/components/cast_receiver/browser/streaming_receiver_session_client.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/strings/string_util.h"
 #include "base/task/sequenced_task_runner.h"
diff --git a/components/cdm/common/playready_cdm_common.h b/components/cdm/common/playready_cdm_common.h
index 77c22f1d62..2c55b78df5 100644
--- a/components/cdm/common/playready_cdm_common.h
+++ b/components/cdm/common/playready_cdm_common.h
@@ -5,10 +5,10 @@
 #ifndef COMPONENTS_CDM_COMMON_PLAYREADY_CDM_COMMON_H_
 #define COMPONENTS_CDM_COMMON_PLAYREADY_CDM_COMMON_H_
 
-#include <algorithm>
 #include <array>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/stl_util.h"
 #include "base/token.h"
 #include "media/cdm/cdm_type.h"
@@ -46,7 +46,7 @@ inline bool IsPlayReadyHwSecureKeySystem(const std::string& key_system) {
 }
 
 inline bool IsPlayReadyKeySystem(const std::string& name) {
-  return std::ranges::contains(kPlayReadyKeySystems, name);
+  return base::Contains(kPlayReadyKeySystems, name);
 }
 
 #endif  // COMPONENTS_CDM_COMMON_PLAYREADY_CDM_COMMON_H_
diff --git a/components/cdm/renderer/external_clear_key_key_system_info.cc b/components/cdm/renderer/external_clear_key_key_system_info.cc
index 14cb336290..5efc9ed630 100644
--- a/components/cdm/renderer/external_clear_key_key_system_info.cc
+++ b/components/cdm/renderer/external_clear_key_key_system_info.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "media/base/eme_constants.h"
 #include "media/base/key_system_names.h"
@@ -56,7 +57,7 @@ bool ExternalClearKeyKeySystemInfo::IsSupportedKeySystem(
     const std::string& key_system) const {
   return (key_system == key_system_ ||
           media::IsSubKeySystemOf(key_system, key_system_)) &&
-         !std::ranges::contains(excluded_key_systems_, key_system);
+         !base::Contains(excluded_key_systems_, key_system);
 }
 
 bool ExternalClearKeyKeySystemInfo::IsSupportedInitDataType(
diff --git a/components/certificate_matching/certificate_principal_pattern.cc b/components/certificate_matching/certificate_principal_pattern.cc
index cb36f10456..55c5d1a91a 100644
--- a/components/certificate_matching/certificate_principal_pattern.cc
+++ b/components/certificate_matching/certificate_principal_pattern.cc
@@ -4,10 +4,10 @@
 
 #include "components/certificate_matching/certificate_principal_pattern.h"
 
-#include <algorithm>
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/values.h"
 #include "net/cert/x509_cert_types.h"
 #include "net/cert/x509_certificate.h"
@@ -64,14 +64,14 @@ bool CertificatePrincipalPattern::Matches(
   }
 
   if (!organization_.empty()) {
-    if (!std::ranges::contains(principal.organization_names, organization_)) {
+    if (!base::Contains(principal.organization_names, organization_)) {
       return false;
     }
   }
 
   if (!organization_unit_.empty()) {
-    if (!std::ranges::contains(principal.organization_unit_names,
-                               organization_unit_)) {
+    if (!base::Contains(principal.organization_unit_names,
+                        organization_unit_)) {
       return false;
     }
   }
diff --git a/components/certificate_transparency/chrome_require_ct_delegate.cc b/components/certificate_transparency/chrome_require_ct_delegate.cc
index 00a46a83ec..699da7286d 100644
--- a/components/certificate_transparency/chrome_require_ct_delegate.cc
+++ b/components/certificate_transparency/chrome_require_ct_delegate.cc
@@ -13,6 +13,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/location.h"
diff --git a/components/cloud_devices/common/description_items.h b/components/cloud_devices/common/description_items.h
index d4640c71cc..20ba14974b 100644
--- a/components/cloud_devices/common/description_items.h
+++ b/components/cloud_devices/common/description_items.h
@@ -10,12 +10,12 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <optional>
 #include <utility>
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "components/cloud_devices/common/cloud_device_description.h"
 
 namespace cloud_devices {
@@ -66,7 +66,7 @@ class ListCapability {
   const Option& operator[](size_t i) const { return options_[i]; }
 
   bool Contains(const Option& option) const {
-    return std::ranges::contains(options_, option);
+    return base::Contains(options_, option);
   }
 
   void AddOption(Option&& option) { options_.emplace_back(std::move(option)); }
@@ -135,7 +135,7 @@ class SelectionCapability {
   const Option& operator[](size_t i) const { return options_[i]; }
 
   bool Contains(const Option& option) const {
-    return std::ranges::contains(options_, option);
+    return base::Contains(options_, option);
   }
 
   const Option& GetDefault() const {
diff --git a/components/collaboration/internal/collaboration_finder_impl.cc b/components/collaboration/internal/collaboration_finder_impl.cc
index 9a07f9550b..d704e34535 100644
--- a/components/collaboration/internal/collaboration_finder_impl.cc
+++ b/components/collaboration/internal/collaboration_finder_impl.cc
@@ -4,6 +4,7 @@
 
 #include "components/collaboration/internal/collaboration_finder_impl.h"
 
+#include "base/containers/contains.h"
 #include "components/data_sharing/public/data_sharing_service.h"
 #include "components/data_sharing/public/group_data.h"
 #include "components/sync/base/collaboration_id.h"
@@ -31,7 +32,7 @@ void CollaborationFinderImpl::SetClient(Client* client) {
 
 bool CollaborationFinderImpl::IsCollaborationAvailable(
     const syncer::CollaborationId& collaboration_id) {
-  if (collaborations_available_for_testing_.contains(collaboration_id)) {
+  if (base::Contains(collaborations_available_for_testing_, collaboration_id)) {
     return true;
   }
 
diff --git a/components/collaboration/internal/messaging/instant_message_processor_impl.cc b/components/collaboration/internal/messaging/instant_message_processor_impl.cc
index fb0aba6c31..24128dd955 100644
--- a/components/collaboration/internal/messaging/instant_message_processor_impl.cc
+++ b/components/collaboration/internal/messaging/instant_message_processor_impl.cc
@@ -9,6 +9,7 @@
 #include <unordered_map>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/hash/hash.h"
 #include "base/strings/to_string.h"
@@ -130,7 +131,7 @@ void InstantMessageProcessorImpl::HideInstantMessage(
     if (!current_message_id.has_value()) {
       return false;
     }
-    return message_ids.contains(*current_message_id);
+    return base::Contains(message_ids, *current_message_id);
   });
 
   // In case the message was displayed previously, we still tell the UI about
diff --git a/components/collaboration/internal/messaging/messaging_backend_service_impl.cc b/components/collaboration/internal/messaging/messaging_backend_service_impl.cc
index a6739e47e0..9b143cdb42 100644
--- a/components/collaboration/internal/messaging/messaging_backend_service_impl.cc
+++ b/components/collaboration/internal/messaging/messaging_backend_service_impl.cc
@@ -13,6 +13,7 @@
 
 #include "base/check.h"
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
 #include "base/i18n/break_iterator.h"
diff --git a/components/collaboration/internal/messaging/storage/messaging_backend_store_impl.cc b/components/collaboration/internal/messaging/storage/messaging_backend_store_impl.cc
index 0381857605..d26c4248f4 100644
--- a/components/collaboration/internal/messaging/storage/messaging_backend_store_impl.cc
+++ b/components/collaboration/internal/messaging/storage/messaging_backend_store_impl.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/task/single_thread_task_runner.h"
 #include "components/collaboration/internal/messaging/storage/collaboration_message_util.h"
 #include "components/collaboration/internal/messaging/storage/messaging_backend_database_impl.h"
@@ -315,7 +316,7 @@ void MessagingBackendStoreImpl::AddMessage(
       data_sharing::GroupId(message.collaboration_id());
   CHECK(!collaboration_id->empty());
 
-  if (!messages_.contains(collaboration_id)) {
+  if (!base::Contains(messages_, collaboration_id)) {
     messages_.insert({collaboration_id, std::make_unique<MessagesPerGroup>()});
   }
 
@@ -376,7 +377,7 @@ void MessagingBackendStoreImpl::RemoveMessages(
     auto& tab_messages = messages_per_group->tab_messages;
     for (auto it = tab_messages.begin(); it != tab_messages.end();) {
       std::string message_uuid = it->second.uuid();
-      if (message_ids.contains(message_uuid)) {
+      if (base::Contains(message_ids, message_uuid)) {
         it = tab_messages.erase(it);
       } else {
         ++it;
@@ -387,7 +388,7 @@ void MessagingBackendStoreImpl::RemoveMessages(
     for (auto it = tab_group_messages.begin();
          it != tab_group_messages.end();) {
       std::string message_uuid = it->second.uuid();
-      if (message_ids.contains(message_uuid)) {
+      if (base::Contains(message_ids, message_uuid)) {
         it = tab_group_messages.erase(it);
       } else {
         ++it;
@@ -397,7 +398,7 @@ void MessagingBackendStoreImpl::RemoveMessages(
     auto& collab_messages = messages_per_group->collaboration_messages;
     for (auto it = collab_messages.begin(); it != collab_messages.end();) {
       std::string message_uuid = it->uuid();
-      if (message_ids.contains(message_uuid)) {
+      if (base::Contains(message_ids, message_uuid)) {
         it = collab_messages.erase(it);
       } else {
         ++it;
@@ -409,7 +410,7 @@ void MessagingBackendStoreImpl::RemoveMessages(
   for (auto it = ungrouped_messages_.begin();
        it != ungrouped_messages_.end();) {
     std::string message_uuid = it->uuid();
-    if (message_ids.contains(message_uuid)) {
+    if (base::Contains(message_ids, message_uuid)) {
       it = ungrouped_messages_.erase(it);
     } else {
       ++it;
diff --git a/components/collaboration/internal/messaging/tab_group_change_notifier_impl.cc b/components/collaboration/internal/messaging/tab_group_change_notifier_impl.cc
index 9e191a0842..33cfee491a 100644
--- a/components/collaboration/internal/messaging/tab_group_change_notifier_impl.cc
+++ b/components/collaboration/internal/messaging/tab_group_change_notifier_impl.cc
@@ -6,6 +6,7 @@
 
 #include <unordered_map>
 
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/uuid.h"
@@ -298,12 +299,12 @@ void TabGroupChangeNotifierImpl::OnTabSelected(
   std::set<tab_groups::LocalTabID> selection_removed;
   std::set<tab_groups::LocalTabID> selection_added;
   for (const auto& tab : last_selected_tabs_) {
-    if (!selected_tabs.contains(tab)) {
+    if (!base::Contains(selected_tabs, tab)) {
       selection_removed.insert(tab);
     }
   }
   for (const auto& tab : selected_tabs) {
-    if (!last_selected_tabs_.contains(tab)) {
+    if (!base::Contains(last_selected_tabs_, tab)) {
       selection_added.insert(tab);
     }
   }
@@ -494,7 +495,7 @@ void TabGroupChangeNotifierImpl::ProcessTabGroupUpdates(
       for (auto& tab : removed_tabs) {
         bool is_selected =
             tab.local_tab_id().has_value() &&
-            last_selected_tabs_.contains(tab.local_tab_id().value());
+            base::Contains(last_selected_tabs_, tab.local_tab_id().value());
         observer.OnTabRemoved(tab, source, is_selected);
       }
     }
@@ -506,9 +507,9 @@ void TabGroupChangeNotifierImpl::ProcessTabGroupUpdates(
   if (!updated_tab_pairs.empty()) {
     for (auto& observer : observers_) {
       for (auto& [before_tab, after_tab] : updated_tab_pairs) {
-        bool is_selected =
-            after_tab.local_tab_id().has_value() &&
-            last_selected_tabs_.contains(after_tab.local_tab_id().value());
+        bool is_selected = after_tab.local_tab_id().has_value() &&
+                           base::Contains(last_selected_tabs_,
+                                          after_tab.local_tab_id().value());
         observer.OnTabUpdated(before_tab, after_tab, source, is_selected);
       }
     }
diff --git a/components/commerce/core/compare/cluster_manager.cc b/components/commerce/core/compare/cluster_manager.cc
index be323bc7d3..c55e195a3b 100644
--- a/components/commerce/core/compare/cluster_manager.cc
+++ b/components/commerce/core/compare/cluster_manager.cc
@@ -4,13 +4,13 @@
 
 #include "components/commerce/core/compare/cluster_manager.h"
 
-#include <algorithm>
 #include <optional>
 #include <set>
 #include <string>
 #include <vector>
 
 #include "base/barrier_callback.h"
+#include "base/containers/contains.h"
 #include "components/commerce/core/commerce_feature_list.h"
 #include "components/commerce/core/commerce_utils.h"
 #include "components/commerce/core/compare/candidate_product.h"
@@ -83,7 +83,7 @@ std::string FindTitleForSimilarProducts(
       if (bottom_label) {
         std::string bottom_label_string = bottom_label.value();
         bottom_labels.insert(bottom_label_string);
-        if (!counted_labels.contains(bottom_label_string)) {
+        if (!base::Contains(counted_labels, bottom_label_string)) {
           counted_labels.insert(bottom_label_string);
           label_count[bottom_label_string]++;
         }
@@ -93,7 +93,7 @@ std::string FindTitleForSimilarProducts(
       if (second_to_bottom_label) {
         std::string second_to_bottom_label_string =
             second_to_bottom_label.value();
-        if (!counted_labels.contains(second_to_bottom_label)) {
+        if (!base::Contains(counted_labels, second_to_bottom_label)) {
           counted_labels.insert(second_to_bottom_label_string);
           label_count[second_to_bottom_label_string]++;
         }
@@ -546,7 +546,7 @@ void ClusterManager::OnGetComparableProducts(
   const std::vector<UrlInfo> url_infos = get_open_url_infos_cb_.Run();
   for (const auto& kv : entry_point_info.similar_candidate_products) {
     // If the product Id cannot be clustered, skip it.
-    if (!std::ranges::contains(cluster_product_ids, kv.second)) {
+    if (!base::Contains(cluster_product_ids, kv.second)) {
       continue;
     }
 
diff --git a/components/commerce/core/shopping_bookmark_model_observer.cc b/components/commerce/core/shopping_bookmark_model_observer.cc
index ae2d87c744..9b7e56c819 100644
--- a/components/commerce/core/shopping_bookmark_model_observer.cc
+++ b/components/commerce/core/shopping_bookmark_model_observer.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback.h"
 #include "base/metrics/user_metrics.h"
@@ -215,7 +216,7 @@ void ShoppingBookmarkModelObserver::HandleNodeDeletion(
   // Determine the number of duplicates that are in the folder being deleted.
   // Skip any previously unsubscribed IDs.
   if (folder_being_deleted &&
-      !unsubscribed_ids->contains(specifics->product_cluster_id())) {
+      !base::Contains(*unsubscribed_ids, specifics->product_cluster_id())) {
     size_t duplicates_in_deleted_folder = 0;
     for (const bookmarks::BookmarkNode* duplicate : bookmarks_with_cluster) {
       if (bookmarks::IsDescendantOf(duplicate, folder_being_deleted)) {
diff --git a/components/commerce/core/shopping_service.cc b/components/commerce/core/shopping_service.cc
index 5d65f4726e..66e71fbc9a 100644
--- a/components/commerce/core/shopping_service.cc
+++ b/components/commerce/core/shopping_service.cc
@@ -4,11 +4,11 @@
 
 #include "components/commerce/core/shopping_service.h"
 
-#include <algorithm>
 #include <vector>
 
 #include "base/barrier_callback.h"
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
@@ -122,14 +122,14 @@ class ProductSpecificationsUrlObserver
     // First remove any references to URLs that are no longer in the product
     // spec set.
     for (const auto& url : before.urls()) {
-      if (!std::ranges::contains(after.urls(), url)) {
+      if (!base::Contains(after.urls(), url)) {
         cache_->RemoveRef(url);
       }
     }
 
     // Now add any URLs that weren't previously referenced.
     for (const auto& url : after.urls()) {
-      if (!std::ranges::contains(before.urls(), url)) {
+      if (!base::Contains(before.urls(), url)) {
         cache_->AddRef(url);
       }
     }
@@ -1105,7 +1105,7 @@ void ShoppingService::HandleOptGuideProductInfoResponse(
     if (attempt_on_demand && commerce_info_cache_.IsUrlReferenced(url) &&
         entry) {
       if (entry->run_product_info_on_demand) {
-        DCHECK(!on_demand_product_info_callbacks_.contains(url));
+        DCHECK(!base::Contains(on_demand_product_info_callbacks_, url));
         entry->run_product_info_on_demand = false;
         on_demand_product_info_callbacks_[url].push_back(std::move(callback));
 
@@ -1132,7 +1132,7 @@ void ShoppingService::HandleOptGuideProductInfoResponse(
                 AsWeakPtr(),
                 base::BindRepeating(&ShoppingService::OnGetOnDemandProductInfo,
                                     AsWeakPtr())));
-      } else if (on_demand_product_info_callbacks_.contains(url)) {
+      } else if (base::Contains(on_demand_product_info_callbacks_, url)) {
         // If there is a on demand call running, add callback to the queue.
         on_demand_product_info_callbacks_[url].push_back(std::move(callback));
       } else {
diff --git a/components/commerce/core/shopping_service_test_base.cc b/components/commerce/core/shopping_service_test_base.cc
index 7871b8178c..c872e428f6 100644
--- a/components/commerce/core/shopping_service_test_base.cc
+++ b/components/commerce/core/shopping_service_test_base.cc
@@ -4,10 +4,10 @@
 
 #include "components/commerce/core/shopping_service_test_base.h"
 
-#include <algorithm>
 #include <optional>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/memory/ptr_util.h"
 #include "base/memory/ref_counted.h"
@@ -322,8 +322,7 @@ OptimizationMetadata MockOptGuideDecider::BuildDiscountsResponse(
 
   std::vector<DiscountClusterType> checked_cluster_types;
   for (const auto& info_to_check : infos) {
-    if (std::ranges::contains(checked_cluster_types,
-                              info_to_check.cluster_type)) {
+    if (base::Contains(checked_cluster_types, info_to_check.cluster_type)) {
       continue;
     }
     checked_cluster_types.push_back(info_to_check.cluster_type);
diff --git a/components/component_updater/component_updater_command_line_config_policy.cc b/components/component_updater/component_updater_command_line_config_policy.cc
index 4d598f6872..c3908014a1 100644
--- a/components/component_updater/component_updater_command_line_config_policy.cc
+++ b/components/component_updater/component_updater_command_line_config_policy.cc
@@ -4,11 +4,11 @@
 
 #include "components/component_updater/component_updater_command_line_config_policy.h"
 
-#include <algorithm>
 #include <string>
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_split.h"
 #include "base/strings/sys_string_conversions.h"
@@ -77,16 +77,15 @@ ComponentUpdaterCommandLineConfigPolicy::
 
 #if BUILDFLAG(IS_MAC) || BUILDFLAG(IS_WIN)
   background_downloads_enabled_ =
-      !std::ranges::contains(switch_values, kSwitchDisableBackgroundDownloads);
+      !base::Contains(switch_values, kSwitchDisableBackgroundDownloads);
 #else
   background_downloads_enabled_ = false;
 #endif
 
-  deltas_enabled_ =
-      !std::ranges::contains(switch_values, kSwitchDisableDeltaUpdates);
-  fast_update_ = std::ranges::contains(switch_values, kSwitchFastUpdate);
-  pings_enabled_ = !std::ranges::contains(switch_values, kSwitchDisablePings);
-  test_request_ = std::ranges::contains(switch_values, kSwitchTestRequestParam);
+  deltas_enabled_ = !base::Contains(switch_values, kSwitchDisableDeltaUpdates);
+  fast_update_ = base::Contains(switch_values, kSwitchFastUpdate);
+  pings_enabled_ = !base::Contains(switch_values, kSwitchDisablePings);
+  test_request_ = base::Contains(switch_values, kSwitchTestRequestParam);
 
   const std::string switch_url_source =
       GetSwitchArgument(switch_values, kSwitchUrlSource);
diff --git a/components/compose/core/browser/config.cc b/components/compose/core/browser/config.cc
index 242aaeb4e5..7a4d1907da 100644
--- a/components/compose/core/browser/config.cc
+++ b/components/compose/core/browser/config.cc
@@ -4,8 +4,6 @@
 
 #include "components/compose/core/browser/config.h"
 
-#include <utility>
-
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
@@ -13,6 +11,7 @@
 #include "base/no_destructor.h"
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/compose/core/browser/compose_features.h"
 #include "components/segmentation_platform/public/features.h"
 
@@ -189,7 +188,7 @@ Config::Config() {
   positioning_strategy = static_cast<DialogFallbackPositioningStrategy>(
       base::GetFieldTrialParamByFeatureAsInt(
           features::kComposeUiParams, "positioning_strategy",
-          std::to_underlying(positioning_strategy)));
+          base::to_underlying(positioning_strategy)));
 
   request_latency_timeout =
       base::Seconds(base::GetFieldTrialParamByFeatureAsInt(
diff --git a/components/content_relationship_verification/response_header_verifier.cc b/components/content_relationship_verification/response_header_verifier.cc
index 84f8cadae5..2b31a46c38 100644
--- a/components/content_relationship_verification/response_header_verifier.cc
+++ b/components/content_relationship_verification/response_header_verifier.cc
@@ -6,8 +6,7 @@
 
 #include <stdio.h>
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/strings/string_split.h"
 
 namespace {
@@ -44,7 +43,7 @@ ResponseHeaderVerificationResult ResponseHeaderVerifier::Verify(
       SplitString(embedder_ancestors_header_value, kNormalizedHeaderDelimiter,
                   base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
 
-  if (std::ranges::contains(allowed_package_names, package_name)) {
+  if (base::Contains(allowed_package_names, package_name)) {
     return ResponseHeaderVerificationResult::kAllow;
   }
 
diff --git a/components/content_settings/core/browser/content_settings_info.cc b/components/content_settings/core/browser/content_settings_info.cc
index f725182847..10712a4818 100644
--- a/components/content_settings/core/browser/content_settings_info.cc
+++ b/components/content_settings/core/browser/content_settings_info.cc
@@ -7,6 +7,7 @@
 #include <optional>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "components/content_settings/core/browser/content_settings_utils.h"
 #include "components/content_settings/core/browser/permission_settings_info.h"
 #include "components/content_settings/core/browser/website_settings_info.h"
@@ -40,7 +41,7 @@ ContentSetting ContentSettingsInfo::GetInitialDefaultSetting() const {
 }
 
 bool ContentSettingsInfo::IsSettingValid(ContentSetting setting) const {
-  return valid_settings_.contains(setting);
+  return base::Contains(valid_settings_, setting);
 }
 
 // TODO(raymes): Find a better way to deal with the special-casing in
@@ -61,7 +62,7 @@ bool ContentSettingsInfo::IsDefaultSettingValid(ContentSetting setting) const {
     return false;
   }
 
-  return valid_settings_.contains(setting);
+  return base::Contains(valid_settings_, setting);
 }
 
 bool ContentSettingsInfo::Delegate::IsValid(
diff --git a/components/content_settings/core/browser/content_settings_policy_provider.cc b/components/content_settings/core/browser/content_settings_policy_provider.cc
index 06db486e22..2f02614079 100644
--- a/components/content_settings/core/browser/content_settings_policy_provider.cc
+++ b/components/content_settings/core/browser/content_settings_policy_provider.cc
@@ -6,10 +6,10 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -752,7 +752,7 @@ void PolicyProvider::OnPreferenceChanged(const std::string& name) {
     }
   }
 
-  if (std::ranges::contains(kManagedPrefs, name)) {
+  if (base::Contains(kManagedPrefs, name)) {
     ReadManagedContentSettings(true);
     ReadManagedDefaultSettings();
   }
diff --git a/components/content_settings/core/browser/content_settings_pref_provider.cc b/components/content_settings/core/browser/content_settings_pref_provider.cc
index 74500441b3..6fe8fa1237 100644
--- a/components/content_settings/core/browser/content_settings_pref_provider.cc
+++ b/components/content_settings/core/browser/content_settings_pref_provider.cc
@@ -6,12 +6,12 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <map>
 #include <memory>
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
@@ -261,8 +261,7 @@ bool PrefProvider::SetWebsiteSetting(
   // permission has been set by the One Time Provider, therefore we reset a
   // potentially existing Allow Always setting.
   if (constraints.session_model() == mojom::SessionModel::ONE_TIME) {
-    DCHECK(std::ranges::contains(GetTypesWithTemporaryGrantsInHcsm(),
-                                 content_type));
+    DCHECK(base::Contains(GetTypesWithTemporaryGrantsInHcsm(), content_type));
     in_value = base::Value();
   }
 
diff --git a/components/content_settings/core/browser/content_settings_registry.cc b/components/content_settings/core/browser/content_settings_registry.cc
index 164baa1e1d..ff4e7c7500 100644
--- a/components/content_settings/core/browser/content_settings_registry.cc
+++ b/components/content_settings/core/browser/content_settings_registry.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/values.h"
 #include "build/blink_buildflags.h"
@@ -857,7 +858,7 @@ void ContentSettingsRegistry::Register(
   // Ensure that nothing has been registered yet for the given type.
   DCHECK(!permission_settings_registry_->Get(type)) << type;
   DCHECK(!website_settings_registry_->Get(type)) << type;
-  DCHECK(!content_settings_info_.contains(type)) << type;
+  DCHECK(!base::Contains(content_settings_info_, type)) << type;
 
   auto delegate = std::make_unique<ContentSettingsInfo::Delegate>();
   auto* delegate_ptr = delegate.get();
diff --git a/components/content_settings/core/browser/content_settings_utils.cc b/components/content_settings/core/browser/content_settings_utils.cc
index 8757878569..67ab0bc01e 100644
--- a/components/content_settings/core/browser/content_settings_utils.cc
+++ b/components/content_settings/core/browser/content_settings_utils.cc
@@ -6,10 +6,10 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <array>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "base/strings/string_split.h"
 #include "base/values.h"
@@ -314,7 +314,7 @@ const std::vector<ContentSettingsType>& GetTypesWithTemporaryGrantsInHcsm() {
 
 bool ShouldTypeExpireActively(ContentSettingsType type) {
   return base::FeatureList::IsEnabled(features::kActiveContentSettingExpiry) &&
-         std::ranges::contains(GetTypesWithTemporaryGrantsInHcsm(), type);
+         base::Contains(GetTypesWithTemporaryGrantsInHcsm(), type);
 }
 
 PermissionSetting ValueToPermissionSetting(const PermissionSettingsInfo* info,
diff --git a/components/content_settings/core/browser/cookie_settings.cc b/components/content_settings/core/browser/cookie_settings.cc
index 6c12fac586..d304323638 100644
--- a/components/content_settings/core/browser/cookie_settings.cc
+++ b/components/content_settings/core/browser/cookie_settings.cc
@@ -4,10 +4,10 @@
 
 #include "components/content_settings/core/browser/cookie_settings.h"
 
-#include <algorithm>
 #include <memory>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
@@ -317,11 +317,10 @@ ContentSetting CookieSettings::GetContentSetting(
 
 bool CookieSettings::IsThirdPartyCookiesAllowedScheme(
     std::string_view scheme) const {
-  return std::ranges::contains(
-      ContentSettingsRegistry::GetInstance()
-          ->Get(ContentSettingsType::COOKIES)
-          ->third_party_cookie_allowed_secondary_schemes(),
-      scheme);
+  return base::Contains(ContentSettingsRegistry::GetInstance()
+                            ->Get(ContentSettingsType::COOKIES)
+                            ->third_party_cookie_allowed_secondary_schemes(),
+                        scheme);
 }
 
 CookieSettings::~CookieSettings() = default;
diff --git a/components/content_settings/core/browser/host_content_settings_map.cc b/components/content_settings/core/browser/host_content_settings_map.cc
index 7bdf1b1471..b815690940 100644
--- a/components/content_settings/core/browser/host_content_settings_map.cc
+++ b/components/content_settings/core/browser/host_content_settings_map.cc
@@ -16,6 +16,7 @@
 #include "base/barrier_closure.h"
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/flat_map.h"
 #include "base/debug/crash_logging.h"
@@ -1342,7 +1343,7 @@ void HostContentSettingsMap::UpdateExpiryEnforcementTimer(
 
   base::TimeDelta next_run = base::TimeDelta::Max();
 
-  if (!expiration_enforcement_timers_.contains(content_type)) {
+  if (!base::Contains(expiration_enforcement_timers_, content_type)) {
     expiration_enforcement_timers_[content_type] =
         std::make_unique<base::OneShotTimer>();
   }
@@ -1414,7 +1415,7 @@ void HostContentSettingsMap::DeleteNearlyExpiredSettingsAndMaybeScheduleNextRun(
     const base::TimeDelta next_run = std::max(
         base::Seconds(0), next_expiry - clock_->Now() - kEagerExpiryBuffer);
 
-    if (!expiration_enforcement_timers_.contains(content_setting_type)) {
+    if (!base::Contains(expiration_enforcement_timers_, content_setting_type)) {
       expiration_enforcement_timers_[content_setting_type] =
           std::make_unique<base::OneShotTimer>();
     }
diff --git a/components/content_settings/core/browser/permission_settings_info.cc b/components/content_settings/core/browser/permission_settings_info.cc
index 6120eb7775..24df2ed468 100644
--- a/components/content_settings/core/browser/permission_settings_info.cc
+++ b/components/content_settings/core/browser/permission_settings_info.cc
@@ -4,6 +4,7 @@
 
 #include "components/content_settings/core/browser/permission_settings_info.h"
 
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "components/content_settings/core/browser/website_settings_info.h"
 #include "components/content_settings/core/common/content_settings.h"
diff --git a/components/content_settings/core/browser/permission_settings_registry.cc b/components/content_settings/core/browser/permission_settings_registry.cc
index 224e88590c..be746d6503 100644
--- a/components/content_settings/core/browser/permission_settings_registry.cc
+++ b/components/content_settings/core/browser/permission_settings_registry.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/values.h"
 #include "components/content_settings/core/browser/content_settings_utils.h"
 #include "components/content_settings/core/browser/geolocation_setting_delegate.h"
@@ -112,7 +113,7 @@ const PermissionSettingsInfo* PermissionSettingsRegistry::Register(
     return nullptr;
   }
 
-  DCHECK(!permission_settings_info_.contains(type));
+  DCHECK(!base::Contains(permission_settings_info_, type));
   auto& info = permission_settings_info_[type] =
       std::make_unique<PermissionSettingsInfo>(
           website_settings_info, allowlisted_primary_schemes,
diff --git a/components/content_settings/core/common/content_settings.cc b/components/content_settings/core/common/content_settings.cc
index a6660fb35b..313973f227 100644
--- a/components/content_settings/core/common/content_settings.cc
+++ b/components/content_settings/core/common/content_settings.cc
@@ -13,6 +13,7 @@
 #include "base/check_op.h"
 #include "base/notreached.h"
 #include "base/strings/strcat.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "components/content_settings/core/common/content_settings_metadata.h"
 #include "components/content_settings/core/common/content_settings_types.h"
@@ -152,8 +153,8 @@ content_settings::SettingInfo content_settings::SettingInfo::Clone() const {
 
 std::ostream& operator<<(std::ostream& os, const GeolocationSetting& it) {
   return os << "GeolocationSetting{approximate: "
-            << std::to_underlying(it.approximate)
-            << ", precise: " << std::to_underlying(it.precise) << "}";
+            << base::to_underlying(it.approximate)
+            << ", precise: " << base::to_underlying(it.precise) << "}";
 }
 
 std::ostream& operator<<(std::ostream& os, const PermissionSetting& it) {
diff --git a/components/contextual_tasks/internal/composite_context_decorator.cc b/components/contextual_tasks/internal/composite_context_decorator.cc
index 3b01eb7123..ffcc03d24a 100644
--- a/components/contextual_tasks/internal/composite_context_decorator.cc
+++ b/components/contextual_tasks/internal/composite_context_decorator.cc
@@ -4,11 +4,11 @@
 
 #include "components/contextual_tasks/internal/composite_context_decorator.h"
 
-#include <algorithm>
 #include <map>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/task/single_thread_task_runner.h"
 #include "components/contextual_tasks/internal/fallback_title_context_decorator.h"
@@ -26,7 +26,7 @@ constexpr contextual_tasks::ContextualTaskContextSource kEarlyDecorators[] = {
 };
 
 bool IsEarlyDecorator(contextual_tasks::ContextualTaskContextSource source) {
-  return std::ranges::contains(kEarlyDecorators, source);
+  return base::Contains(kEarlyDecorators, source);
 }
 }  // namespace
 
@@ -76,7 +76,7 @@ void CompositeContextDecorator::DecorateContext(
   for (const auto& source : kEarlyDecorators) {
     // Check if we should run this decorator (either all are requested, or this
     // specific one is).
-    if (sources.empty() || sources.contains(source)) {
+    if (sources.empty() || base::Contains(sources, source)) {
       auto it = decorators_.find(source);
       if (it != decorators_.end()) {
         decorators_to_run.push_back(it->second.get());
@@ -93,7 +93,7 @@ void CompositeContextDecorator::DecorateContext(
       continue;
     }
 
-    if (sources.empty() || sources.contains(source)) {
+    if (sources.empty() || base::Contains(sources, source)) {
       decorators_to_run.push_back(pair.second.get());
     }
   }
diff --git a/components/continuous_search/renderer/search_result_extractor_impl.cc b/components/continuous_search/renderer/search_result_extractor_impl.cc
index bbd6d50a86..c566910936 100644
--- a/components/continuous_search/renderer/search_result_extractor_impl.cc
+++ b/components/continuous_search/renderer/search_result_extractor_impl.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/string_util.h"
 #include "base/task/single_thread_task_runner.h"
@@ -138,8 +139,8 @@ bool ExtractRelatedSearches(blink::WebDocument document,
   for (blink::WebElement anchor = anchors.FirstItem(); !anchor.IsNull();
        anchor = anchors.NextItem()) {
     if (!anchor.HasAttribute("class") ||
-        !anchor.GetAttribute("class").Utf8().contains(
-            GetConfig().related_searches_anchor_classname)) {
+        !base::Contains(anchor.GetAttribute("class").Utf8(),
+                        GetConfig().related_searches_anchor_classname)) {
       continue;
     }
 
@@ -162,8 +163,8 @@ bool ExtractRelatedSearches(blink::WebDocument document,
     for (blink::WebElement inner_div = inner_divs.FirstItem();
          !inner_div.IsNull(); inner_div = inner_divs.NextItem()) {
       if (!inner_div.HasAttribute("class") ||
-          !inner_div.GetAttribute("class").Utf8().contains(
-              GetConfig().related_searches_title_classname)) {
+          !base::Contains(inner_div.GetAttribute("class").Utf8(),
+                          GetConfig().related_searches_title_classname)) {
         continue;
       }
       title = inner_div.TextContent().Utf16();
diff --git a/components/crash/content/browser/child_exit_observer_android.cc b/components/crash/content/browser/child_exit_observer_android.cc
index 54ec97f5da..fa06bed3e6 100644
--- a/components/crash/content/browser/child_exit_observer_android.cc
+++ b/components/crash/content/browser/child_exit_observer_android.cc
@@ -7,6 +7,7 @@
 #include <unistd.h>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "components/crash/content/browser/crash_memory_metrics_collector_android.h"
 #include "content/public/browser/browser_thread.h"
diff --git a/components/cronet/url_request_context_config.cc b/components/cronet/url_request_context_config.cc
index 2b8d013528..a2e9e318a2 100644
--- a/components/cronet/url_request_context_config.cc
+++ b/components/cronet/url_request_context_config.cc
@@ -9,6 +9,7 @@
 #include <type_traits>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/json/json_reader.h"
 #include "base/json/json_writer.h"
@@ -464,7 +465,7 @@ void URLRequestContextConfig::SetContextBuilderExperimentalOptions(
         quic::ParsedQuicVersionVector obsolete_versions =
             net::ObsoleteQuicVersions();
         for (const quic::ParsedQuicVersion& version : supported_versions) {
-          if (!std::ranges::contains(obsolete_versions, version)) {
+          if (!base::Contains(obsolete_versions, version)) {
             filtered_versions.push_back(version);
           }
         }
diff --git a/components/custom_handlers/protocol_handler_registry.cc b/components/custom_handlers/protocol_handler_registry.cc
index b5e8268849..ea7d9d9cca 100644
--- a/components/custom_handlers/protocol_handler_registry.cc
+++ b/components/custom_handlers/protocol_handler_registry.cc
@@ -13,6 +13,7 @@
 #include "base/check_is_test.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ref_counted.h"
 #include "base/notreached.h"
@@ -268,7 +269,7 @@ ProtocolHandlerRegistry::GetUserDefinedHandlers(base::Time begin,
   ProtocolHandlerRegistry::ProtocolHandlerList result;
   for (const auto& [protocol, handlers_list] : user_protocol_handlers_) {
     for (const ProtocolHandler& handler : handlers_list) {
-      if (std::ranges::contains(predefined_protocol_handlers_, handler))
+      if (base::Contains(predefined_protocol_handlers_, handler))
         continue;
       if (begin <= handler.last_modified() && handler.last_modified() < end)
         result.push_back(handler);
@@ -347,7 +348,7 @@ bool ProtocolHandlerRegistry::IsRegistered(
   if (!handlers) {
     return false;
   }
-  return std::ranges::contains(*handlers, handler);
+  return base::Contains(*handlers, handler);
 }
 
 bool ProtocolHandlerRegistry::IsRegisteredByUser(
@@ -780,7 +781,7 @@ bool ProtocolHandlerRegistry::HandlerExists(const ProtocolHandler& handler,
 
 bool ProtocolHandlerRegistry::HandlerExists(const ProtocolHandler& handler,
                                             const ProtocolHandlerList& list) {
-  return std::ranges::contains(list, handler);
+  return base::Contains(list, handler);
 }
 
 void ProtocolHandlerRegistry::EraseHandler(const ProtocolHandler& handler,
diff --git a/components/data_sharing/internal/group_data_model.cc b/components/data_sharing/internal/group_data_model.cc
index 81ed1e4c41..436a94de03 100644
--- a/components/data_sharing/internal/group_data_model.cc
+++ b/components/data_sharing/internal/group_data_model.cc
@@ -7,6 +7,7 @@
 #include <iterator>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
@@ -399,7 +400,7 @@ void GroupDataModel::NotifyObserversAboutChangedMembers(
 
   std::vector<std::pair<GaiaId, base::Time>> added_members;
   for (const auto& new_pair : new_members) {
-    if (!old_members_gaia_ids.contains(new_pair.first)) {
+    if (!base::Contains(old_members_gaia_ids, new_pair.first)) {
       added_members.push_back(new_pair);
     }
   }
diff --git a/components/dbus/menu/menu.cc b/components/dbus/menu/menu.cc
index df7fd32ac3..d615fa291f 100644
--- a/components/dbus/menu/menu.cc
+++ b/components/dbus/menu/menu.cc
@@ -9,6 +9,7 @@
 #include <memory>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/i18n/rtl.h"
@@ -392,7 +393,7 @@ void DbusMenu::OnGetGroupProperties(ScopedMethodResponse* response) {
     struct_writer.OpenArray("{sv}", &property_writer);
     for (const auto& property_pair : item.properties) {
       if (!property_filter.empty() &&
-          !property_filter.contains(property_pair.first)) {
+          !base::Contains(property_filter, property_pair.first)) {
         continue;
       }
       dbus::MessageWriter dict_entry_writer(nullptr);
@@ -556,7 +557,7 @@ void DbusMenu::WriteMenuItem(const MenuItem* item,
   struct_writer.OpenArray("{sv}", &properties_writer);
   for (const auto& property : item->properties) {
     if (property_filter.empty() ||
-        std::ranges::contains(property_filter, property.first)) {
+        base::Contains(property_filter, property.first)) {
       dbus::MessageWriter dict_entry_writer(nullptr);
       properties_writer.OpenDictEntry(&dict_entry_writer);
       dict_entry_writer.AppendString(property.first);
diff --git a/components/dbus/menu/menu_property_list.cc b/components/dbus/menu/menu_property_list.cc
index 5c870649bc..4c18ea912c 100644
--- a/components/dbus/menu/menu_property_list.cc
+++ b/components/dbus/menu/menu_property_list.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/memory/ref_counted_memory.h"
 #include "base/notimplemented.h"
@@ -149,7 +150,7 @@ void ComputeMenuPropertyChanges(const MenuItemProperties& old_properties,
   // Compute added properties.
   for (const auto& pair : new_properties) {
     const std::string& key = pair.first;
-    if (!old_properties.contains(key)) {
+    if (!base::Contains(old_properties, key)) {
       item_updated_props->push_back(key);
     }
   }
diff --git a/components/dbus/properties/dbus_properties.cc b/components/dbus/properties/dbus_properties.cc
index debc7571d1..d4c8c9d20b 100644
--- a/components/dbus/properties/dbus_properties.cc
+++ b/components/dbus/properties/dbus_properties.cc
@@ -6,6 +6,7 @@
 
 #include <dbus/dbus-shared.h>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/notimplemented.h"
 #include "components/dbus/properties/success_barrier_callback.h"
diff --git a/components/dbus/utils/call_method.h b/components/dbus/utils/call_method.h
index ea3322ab91..9c136f8ac7 100644
--- a/components/dbus/utils/call_method.h
+++ b/components/dbus/utils/call_method.h
@@ -16,6 +16,7 @@
 #include "base/functional/callback.h"
 #include "base/numerics/clamped_math.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected.h"
 #include "components/dbus/utils/read_message.h"
 #include "components/dbus/utils/read_value.h"
@@ -36,9 +37,9 @@ enum class CallMethodErrorStatus {
 };
 
 constexpr base::TimeDelta kTimeoutDefault = base::Milliseconds(
-    std::to_underlying(dbus::ObjectProxy::TIMEOUT_USE_DEFAULT));
+    base::to_underlying(dbus::ObjectProxy::TIMEOUT_USE_DEFAULT));
 constexpr base::TimeDelta kTimeoutMax =
-    base::Milliseconds(std::to_underlying(dbus::ObjectProxy::TIMEOUT_MAX));
+    base::Milliseconds(base::to_underlying(dbus::ObjectProxy::TIMEOUT_MAX));
 
 class COMPONENT_EXPORT(COMPONENTS_DBUS) CallMethodError {
  public:
diff --git a/components/dbus/utils/check_for_service_and_start.cc b/components/dbus/utils/check_for_service_and_start.cc
index 69d11c3f63..40626a1843 100644
--- a/components/dbus/utils/check_for_service_and_start.cc
+++ b/components/dbus/utils/check_for_service_and_start.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
 #include "components/dbus/utils/name_has_owner.h"
@@ -77,7 +78,7 @@ void OnListActivatableNamesResponse(scoped_refptr<dbus::Bus> bus,
     return;
   }
 
-  if (std::ranges::contains(activatable_names, name)) {
+  if (base::Contains(activatable_names, name)) {
     StartServiceByName(bus, name, std::move(callback));
   } else {
     // The service is not activatable
diff --git a/components/desks_storage/core/desk_sync_bridge.cc b/components/desks_storage/core/desk_sync_bridge.cc
index 58a774abe7..c7da838e58 100644
--- a/components/desks_storage/core/desk_sync_bridge.cc
+++ b/components/desks_storage/core/desk_sync_bridge.cc
@@ -4,13 +4,13 @@
 
 #include "components/desks_storage/core/desk_sync_bridge.h"
 
-#include <algorithm>
 #include <optional>
 #include <string>
 
 #include "ash/constants/ash_features.h"
 #include "ash/public/cpp/desk_template.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/json/json_writer.h"
@@ -656,14 +656,14 @@ void DeskSyncBridge::UploadLocalOnlyData(
 }
 
 bool DeskSyncBridge::HasUserTemplateWithName(const std::u16string& name) {
-  return std::ranges::contains(desk_template_entries_, name,
-                               [](const DeskEntries::value_type& entry) {
-                                 return entry.second->template_name();
-                               });
+  return base::Contains(desk_template_entries_, name,
+                        [](const DeskEntries::value_type& entry) {
+                          return entry.second->template_name();
+                        });
 }
 
 bool DeskSyncBridge::HasUuid(const base::Uuid& uuid) const {
-  return uuid.is_valid() && desk_template_entries_.contains(uuid);
+  return uuid.is_valid() && base::Contains(desk_template_entries_, uuid);
 }
 
 std::string DeskSyncBridge::GetCacheGuid() {
diff --git a/components/desks_storage/core/desk_template_conversion.cc b/components/desks_storage/core/desk_template_conversion.cc
index 3eb3117929..30b4b4eefc 100644
--- a/components/desks_storage/core/desk_template_conversion.cc
+++ b/components/desks_storage/core/desk_template_conversion.cc
@@ -274,7 +274,7 @@ base::DictValue ConvertCoralTabAppEntitiesToDict(
 }
 
 bool IsValidGroupColor(const std::string& group_color) {
-  return kValidTabGroupColors.contains(group_color);
+  return base::Contains(kValidTabGroupColors, group_color);
 }
 
 GroupColor ConvertGroupColorStringToGroupColor(const std::string& group_color) {
@@ -361,7 +361,7 @@ std::optional<tab_groups::TabGroupInfo> MakeTabGroupInfoFromDict(
 
 // Returns true if launch container string value is valid.
 bool IsValidLaunchContainer(const std::string& launch_container) {
-  return kValidLaunchContainers.contains(launch_container);
+  return base::Contains(kValidLaunchContainers, launch_container);
 }
 
 // Returns a casted apps::LaunchContainer to be set as an app restore data's
@@ -387,7 +387,7 @@ int32_t StringToLaunchContainer(const std::string& launch_container) {
 
 // Returns true if the disposition is a valid value.
 bool IsValidWindowOpenDisposition(const std::string& disposition) {
-  return kValidWindowOpenDispositions.contains(disposition);
+  return base::Contains(kValidWindowOpenDispositions, disposition);
 }
 
 // Returns a casted WindowOpenDisposition to be set in the app restore data.
@@ -542,7 +542,7 @@ std::vector<coral::mojom::EntityPtr> ConvertDictToCoralTabAppEntities(
 }
 
 bool IsValidWindowState(const std::string& window_state) {
-  return kValidWindowStates.contains(window_state);
+  return base::Contains(kValidWindowStates, window_state);
 }
 
 // Convert JSON string WindowState `state` to ui::mojom::WindowShowState used by
@@ -1041,7 +1041,7 @@ std::string SerializeDeskTypeAsString(ash::DeskTemplateType desk_type) {
 }
 
 bool IsValidDeskTemplateType(const std::string& desk_template_type) {
-  return kValidDeskTypes.contains(desk_template_type);
+  return base::Contains(kValidDeskTypes, desk_template_type);
 }
 
 // TODO(b/258692868): Currently parse any invalid value for this field as
diff --git a/components/desks_storage/core/fake_desk_sync_bridge.cc b/components/desks_storage/core/fake_desk_sync_bridge.cc
index ca9aa11c55..d23ae3fd01 100644
--- a/components/desks_storage/core/fake_desk_sync_bridge.cc
+++ b/components/desks_storage/core/fake_desk_sync_bridge.cc
@@ -5,6 +5,7 @@
 #include "components/desks_storage/core/fake_desk_sync_bridge.h"
 
 #include "ash/public/cpp/desk_template.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/ptr_util.h"
diff --git a/components/desks_storage/core/local_desk_data_manager.cc b/components/desks_storage/core/local_desk_data_manager.cc
index 957eab8b6c..28a1834b92 100644
--- a/components/desks_storage/core/local_desk_data_manager.cc
+++ b/components/desks_storage/core/local_desk_data_manager.cc
@@ -8,6 +8,7 @@
 
 #include "ash/public/cpp/desk_template.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/files/dir_reader_posix.h"
 #include "base/files/file_util.h"
@@ -646,7 +647,7 @@ void LocalDeskDataManager::OnDeleteEntry(
 ash::DeskTemplateType LocalDeskDataManager::GetDeskTypeOfUuid(
     const base::Uuid uuid) const {
   for (const auto& [desk_type, saved_desk] : saved_desks_list_) {
-    if (saved_desk.contains(uuid))
+    if (base::Contains(saved_desk, uuid))
       return desk_type;
   }
   return ash::DeskTemplateType::kUnknown;
diff --git a/components/discardable_memory/common/discardable_shared_memory_heap.cc b/components/discardable_memory/common/discardable_shared_memory_heap.cc
index adcc55b6b2..3700314902 100644
--- a/components/discardable_memory/common/discardable_shared_memory_heap.cc
+++ b/components/discardable_memory/common/discardable_shared_memory_heap.cc
@@ -12,6 +12,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/format_macros.h"
 #include "base/memory/aligned_memory.h"
 #include "base/memory/discardable_shared_memory.h"
@@ -114,8 +115,7 @@ DiscardableSharedMemoryHeap::~DiscardableSharedMemoryHeap() {
   memory_segments_.clear();
   DCHECK_EQ(num_blocks_, 0u);
   DCHECK_EQ(num_free_blocks_, 0u);
-  DCHECK(!std::ranges::contains(free_spans_, false,
-                                &base::LinkedList<Span>::empty));
+  DCHECK(!base::Contains(free_spans_, false, &base::LinkedList<Span>::empty));
 }
 
 std::unique_ptr<DiscardableSharedMemoryHeap::Span>
diff --git a/components/download/internal/common/download_path_reservation_tracker.cc b/components/download/internal/common/download_path_reservation_tracker.cc
index 6cab959a01..bb3a2fb423 100644
--- a/components/download/internal/common/download_path_reservation_tracker.cc
+++ b/components/download/internal/common/download_path_reservation_tracker.cc
@@ -10,6 +10,7 @@
 #include <map>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -396,7 +397,7 @@ void UpdateReservation(ReservationKey key, const base::FilePath& new_path) {
 // |key|.
 void RevokeReservation(ReservationKey key) {
   DCHECK(g_reservation_map != nullptr);
-  DCHECK(g_reservation_map->contains(key));
+  DCHECK(base::Contains(*g_reservation_map, key));
   g_reservation_map->erase(key);
   if (g_reservation_map->size() == 0) {
     // No more reservations. Delete map.
diff --git a/components/download/internal/common/in_progress_download_manager.cc b/components/download/internal/common/in_progress_download_manager.cc
index 32e2ae4ac8..c31c6bf3df 100644
--- a/components/download/internal/common/in_progress_download_manager.cc
+++ b/components/download/internal/common/in_progress_download_manager.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/string_util.h"
@@ -569,7 +570,7 @@ void InProgressDownloadManager::StartDownloadWithItem(
   if (info->is_new_download && !should_persist_new_download)
     non_persistent_download_guids_.insert(download->GetGuid());
   // If the download is not persisted, don't notify |download_db_cache_|.
-  if (!non_persistent_download_guids_.contains(download->GetGuid())) {
+  if (!base::Contains(non_persistent_download_guids_, download->GetGuid())) {
     download_db_cache_->AddOrReplaceEntry(
         CreateDownloadDBEntryFromItem(*download));
     download->RemoveObserver(download_db_cache_.get());
@@ -637,7 +638,7 @@ void InProgressDownloadManager::OnDownloadNamesRetrieved(
     uint32_t download_id = item->GetId();
     // Remove entries with duplicate ids.
     if (download_id != DownloadItem::kInvalidId &&
-        download_ids.contains(download_id)) {
+        base::Contains(download_ids, download_id)) {
       RemoveInProgressDownload(item->GetGuid());
       continue;
     }
diff --git a/components/drive/file_errors.cc b/components/drive/file_errors.cc
index 58c64d4e33..c79b3fcbb8 100644
--- a/components/drive/file_errors.cc
+++ b/components/drive/file_errors.cc
@@ -5,9 +5,9 @@
 #include "components/drive/file_errors.h"
 
 #include <type_traits>
-#include <utility>
 
 #include "base/notreached.h"
+#include "base/types/cxx23_to_underlying.h"
 
 namespace drive {
 
@@ -79,7 +79,7 @@ bool IsFileErrorOk(FileError error) {
       return false;
   }
 
-  NOTREACHED() << "Unexpected FileError " << std::to_underlying(error);
+  NOTREACHED() << "Unexpected FileError " << base::to_underlying(error);
 }
 
 base::File::Error FileErrorToBaseFileError(FileError error) {
diff --git a/components/embedder_support/user_agent_utils.cc b/components/embedder_support/user_agent_utils.cc
index 557eadb182..3d8c8e3125 100644
--- a/components/embedder_support/user_agent_utils.cc
+++ b/components/embedder_support/user_agent_utils.cc
@@ -15,6 +15,7 @@
 #include "base/android/device_info.h"
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/no_destructor.h"
@@ -776,7 +777,7 @@ std::string GetCpuBitness() {
   }
   return std::string();
 #elif BUILDFLAG(IS_POSIX)
-  return BuildCpuInfo().contains("64") ? "64" : "32";
+  return base::Contains(BuildCpuInfo(), "64") ? "64" : "32";
 #else
 #error Unsupported platform
 #endif
diff --git a/components/enterprise/client_certificates/core/certificate_provisioning_service.cc b/components/enterprise/client_certificates/core/certificate_provisioning_service.cc
index 2220256c4b..4357f91832 100644
--- a/components/enterprise/client_certificates/core/certificate_provisioning_service.cc
+++ b/components/enterprise/client_certificates/core/certificate_provisioning_service.cc
@@ -4,12 +4,12 @@
 
 #include "components/enterprise/client_certificates/core/certificate_provisioning_service.h"
 
-#include <algorithm>
 #include <optional>
 #include <utility>
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/raw_ptr.h"
@@ -499,7 +499,7 @@ void CertificateProvisioningServiceImpl::OnIdentitiesDeleted(
       << "Identities successfully deleted from store.";
 
   if (cached_identity_ &&
-      std::ranges::contains(identity_names, cached_identity_->name)) {
+      base::Contains(identity_names, cached_identity_->name)) {
     if (cached_identity_->certificate) {
       context_delegate_->OnClientCertificateDeleted(
           cached_identity_->certificate);
diff --git a/components/enterprise/connectors/core/realtime_reporting_client_base.cc b/components/enterprise/connectors/core/realtime_reporting_client_base.cc
index 868c51dd63..4161411854 100644
--- a/components/enterprise/connectors/core/realtime_reporting_client_base.cc
+++ b/components/enterprise/connectors/core/realtime_reporting_client_base.cc
@@ -4,9 +4,9 @@
 
 #include "components/enterprise/connectors/core/realtime_reporting_client_base.h"
 
-#include <algorithm>
 #include <ctime>
 
+#include "base/containers/contains.h"
 #include "base/containers/to_value_list.h"
 #include "base/i18n/time_formatting.h"
 #include "base/logging.h"
@@ -222,8 +222,8 @@ void RealtimeReportingClientBase::ReportEventWithTimestampDeprecated(
 #ifndef NDEBUG
   // Make sure the event is included in the kAllReportingEnabledEvents or the
   // kAllReportingOptInEvents array.
-  bool found = std::ranges::contains(kAllReportingEnabledEvents, name) ||
-               std::ranges::contains(kAllReportingOptInEvents, name);
+  bool found = base::Contains(kAllReportingEnabledEvents, name) ||
+               base::Contains(kAllReportingOptInEvents, name);
   DCHECK(found);
 #endif
 
diff --git a/components/enterprise/connectors/core/reporting_event_router.cc b/components/enterprise/connectors/core/reporting_event_router.cc
index 24c31b2256..c19e04ba0a 100644
--- a/components/enterprise/connectors/core/reporting_event_router.cc
+++ b/components/enterprise/connectors/core/reporting_event_router.cc
@@ -4,9 +4,9 @@
 
 #include "components/enterprise/connectors/core/reporting_event_router.h"
 
-#include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/json/values_util.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/utf_string_conversions.h"
@@ -32,10 +32,10 @@ bool IsEventInReportingSettings(const std::string& event,
   if (!settings.has_value()) {
     return false;
   }
-  if (std::ranges::contains(kAllReportingEnabledEvents, event)) {
+  if (base::Contains(kAllReportingEnabledEvents, event)) {
     return settings->enabled_event_names.count(event) > 0;
   }
-  if (std::ranges::contains(kAllReportingOptInEvents, event)) {
+  if (base::Contains(kAllReportingOptInEvents, event)) {
     return settings->enabled_opt_in_events.count(event) > 0;
   }
   return false;
diff --git a/components/enterprise/connectors/core/reporting_test_utils.cc b/components/enterprise/connectors/core/reporting_test_utils.cc
index 50019c08cd..8ce45f5522 100644
--- a/components/enterprise/connectors/core/reporting_test_utils.cc
+++ b/components/enterprise/connectors/core/reporting_test_utils.cc
@@ -6,6 +6,7 @@
 
 #include <cstddef>
 
+#include "base/containers/contains.h"
 #include "base/json/json_writer.h"
 #include "base/json/values_util.h"
 #include "base/strings/utf_string_conversions.h"
@@ -864,7 +865,7 @@ void EventReportValidatorBase::ValidateMimeType(
     const std::set<std::string>* expected_mimetypes) {
   const std::string* type = value->FindString(kKeyContentType);
   if (expected_mimetypes) {
-    EXPECT_TRUE(expected_mimetypes->contains(*type))
+    EXPECT_TRUE(base::Contains(*expected_mimetypes, *type))
         << *type << " is not an expected mimetype";
   } else {
     EXPECT_EQ(nullptr, type);
diff --git a/components/enterprise/data_controls/core/browser/conditions/attributes_condition.cc b/components/enterprise/data_controls/core/browser/conditions/attributes_condition.cc
index 4538c544ef..3c702a90b6 100644
--- a/components/enterprise/data_controls/core/browser/conditions/attributes_condition.cc
+++ b/components/enterprise/data_controls/core/browser/conditions/attributes_condition.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "components/url_matcher/url_util.h"
 
@@ -91,7 +92,7 @@ bool AttributesCondition::ComponentMatches(Component component) const {
 
   // With components to match, `component` needs to be in the set to pass the
   // condition.
-  return components_.contains(component);
+  return base::Contains(components_, component);
 }
 #endif  // BUILDFLAG(IS_CHROMEOS)
 
diff --git a/components/exo/keyboard.cc b/components/exo/keyboard.cc
index efa6c2a66d..6fdf397107 100644
--- a/components/exo/keyboard.cc
+++ b/components/exo/keyboard.cc
@@ -16,6 +16,7 @@
 #include "ash/public/cpp/keyboard/keyboard_controller.h"
 #include "ash/shell.h"
 #include "ash/wm/window_state.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/flat_tree.h"
 #include "base/containers/span.h"
diff --git a/components/exo/layer_tree_frame_sink_holder.cc b/components/exo/layer_tree_frame_sink_holder.cc
index ee8e5435ba..465f22addd 100644
--- a/components/exo/layer_tree_frame_sink_holder.cc
+++ b/components/exo/layer_tree_frame_sink_holder.cc
@@ -4,8 +4,7 @@
 
 #include "components/exo/layer_tree_frame_sink_holder.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/trace_event/typed_macros.h"
 #include "cc/mojo_embedder/async_layer_tree_frame_sink.h"
@@ -163,17 +162,17 @@ void LayerTreeFrameSinkHolder::ReclaimResources(
     // TODO(crbug.com/40269434): if viz reclaims the resources b/c the
     // viz::Surface never gets embedded, this prevents clients from receiving
     // release callbacks. This needs to be addressed.
-    if (std::ranges::contains(last_frame_resources_, resource.id)) {
+    if (base::Contains(last_frame_resources_, resource.id)) {
       continue;
     }
     in_use_resources_.erase(resource.id);
 
     // Skip resources that are also in the cached frame.
     if (cached_frame_ &&
-        std::ranges::contains(cached_frame_->resource_list, resource.id,
-                              [](const viz::TransferableResource& resource) {
-                                return resource.id;
-                              })) {
+        base::Contains(cached_frame_->resource_list, resource.id,
+                       [](const viz::TransferableResource& resource) {
+                         return resource.id;
+                       })) {
       continue;
     }
 
@@ -344,10 +343,10 @@ void LayerTreeFrameSinkHolder::DiscardCachedFrame(
 
     // Skip if the resource is also in `new_frame`.
     if (new_frame &&
-        std::ranges::contains(new_frame->resource_list, resource.id,
-                              [](const viz::TransferableResource& resource) {
-                                return resource.id;
-                              })) {
+        base::Contains(new_frame->resource_list, resource.id,
+                       [](const viz::TransferableResource& resource) {
+                         return resource.id;
+                       })) {
       continue;
     }
     resource_manager_.ReclaimResource(resource.ToReturnedResource());
diff --git a/components/exo/shell_surface_base.cc b/components/exo/shell_surface_base.cc
index 2d8084c2a0..073e0f3954 100644
--- a/components/exo/shell_surface_base.cc
+++ b/components/exo/shell_surface_base.cc
@@ -21,6 +21,7 @@
 #include "ash/wm/window_util.h"
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/logging.h"
 #include "base/memory/raw_ptr.h"
@@ -1024,14 +1025,14 @@ void ShellSurfaceBase::RebindRootSurface(Surface* root_surface,
     // Int properties.
     for (auto* const key :
          {aura::client::kSkipImeProcessing, chromeos::kFrameRestoreLookKey}) {
-      if (window->GetAllPropertyKeys().contains(key)) {
+      if (base::Contains(window->GetAllPropertyKeys(), key)) {
         OnWindowPropertyChanged(window, key,
                                 /*old_value(unused)=*/0);
       }
     }
     // Boolean property.
-    if (window->GetAllPropertyKeys().contains(
-            aura::client::kWindowWorkspaceKey)) {
+    if (base::Contains(window->GetAllPropertyKeys(),
+                       aura::client::kWindowWorkspaceKey)) {
       OnWindowPropertyChanged(window, aura::client::kWindowWorkspaceKey,
                               /*old_value(unused)=*/0);
     }
@@ -1486,7 +1487,7 @@ void ShellSurfaceBase::OnCaptureChanged(aura::Window* lost_capture,
   auto is_close_candidate_with_popup_grab =
       [&gained_capture_ancestors](aura::Window* window) {
         return IsPopupWithGrab(window) &&
-               !gained_capture_ancestors.contains(window);
+               !base::Contains(gained_capture_ancestors, window);
       };
 
   aura::Window* root = wm::GetTransientRoot(lost_capture);
diff --git a/components/exo/wayland/output_controller.cc b/components/exo/wayland/output_controller.cc
index 0f0b0ce99f..6da17855ef 100644
--- a/components/exo/wayland/output_controller.cc
+++ b/components/exo/wayland/output_controller.cc
@@ -12,6 +12,7 @@
 #include <algorithm>
 
 #include "ash/shell.h"
+#include "base/containers/contains.h"
 #include "components/exo/wayland/output_configuration_change.h"
 #include "components/exo/wayland/wayland_display_output.h"
 #include "components/exo/wayland/wl_output.h"
@@ -156,8 +157,8 @@ bool OutputController::ProcessDisplayChangesForAuraOutputManager(
        configuration_change.display_metrics_changes) {
     // Added displays may appear in both added and changed lists, ensure added
     // outputs are not represented in both added and changed lists.
-    if (!std::ranges::contains(configuration_change.added_displays,
-                               change.display.get())) {
+    if (!base::Contains(configuration_change.added_displays,
+                        change.display.get())) {
       output_config_change.changed_outputs.emplace_back(
           GetWaylandDisplayOutput(change.display->id()),
           change.changed_metrics);
diff --git a/components/exo/wayland/test/integration/buffer_checker_test.cc b/components/exo/wayland/test/integration/buffer_checker_test.cc
index 4bc5add7ce..20d1ab9ec8 100644
--- a/components/exo/wayland/test/integration/buffer_checker_test.cc
+++ b/components/exo/wayland/test/integration/buffer_checker_test.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/queue.h"
 #include "base/logging.h"
@@ -255,7 +256,7 @@ class BufferCheckerTestClient : public ::exo::wayland::clients::ClientBase {
     reported_format_modifier_map.clear();
     for (DmabufFeedbackTranche tranche : current_feedback_.tranches) {
       for (const auto& [format, modifiers] : tranche.format_modifier_map) {
-        if (!std::ranges::contains(reported_formats, format)) {
+        if (!base::Contains(reported_formats, format)) {
           reported_formats.push_back(format);
         }
         if (!reported_format_modifier_map.contains(format)) {
@@ -263,8 +264,7 @@ class BufferCheckerTestClient : public ::exo::wayland::clients::ClientBase {
         }
 
         for (uint64_t modifier : modifiers) {
-          if (!std::ranges::contains(reported_format_modifier_map[format],
-                                     modifier)) {
+          if (!base::Contains(reported_format_modifier_map[format], modifier)) {
             reported_format_modifier_map[format].push_back(modifier);
           }
         }
@@ -509,7 +509,7 @@ TEST_F(BufferCheckerClientTest, CanUseAnyReportedBufferModifiersLegacy) {
       }
     }
 
-    if (std::ranges::contains(modifiers, DRM_FORMAT_MOD_INVALID)) {
+    if (base::Contains(modifiers, DRM_FORMAT_MOD_INVALID)) {
       int res = client.GetNumSupportedFormatsAndModifier(
           format, std::vector<uint64_t>({DRM_FORMAT_MOD_INVALID}));
       EXPECT_TRUE(res != -1);
@@ -577,7 +577,7 @@ TEST_F(BufferCheckerClientTest,
       }
     }
 
-    if (std::ranges::contains(modifiers, DRM_FORMAT_MOD_INVALID)) {
+    if (base::Contains(modifiers, DRM_FORMAT_MOD_INVALID)) {
       int res = client.GetNumSupportedFormatsAndModifier(
           format, std::vector<uint64_t>({DRM_FORMAT_MOD_INVALID}));
       EXPECT_TRUE(res != -1);
@@ -645,7 +645,7 @@ TEST_F(BufferCheckerClientTest,
       }
     }
 
-    if (std::ranges::contains(modifiers, DRM_FORMAT_MOD_INVALID)) {
+    if (base::Contains(modifiers, DRM_FORMAT_MOD_INVALID)) {
       int res = client.GetNumSupportedFormatsAndModifier(
           format, std::vector<uint64_t>({DRM_FORMAT_MOD_INVALID}));
       EXPECT_TRUE(res != -1);
diff --git a/components/exo/wayland/wayland_dmabuf_feedback_manager.cc b/components/exo/wayland/wayland_dmabuf_feedback_manager.cc
index 435b612261..44f6541a4f 100644
--- a/components/exo/wayland/wayland_dmabuf_feedback_manager.cc
+++ b/components/exo/wayland/wayland_dmabuf_feedback_manager.cc
@@ -9,11 +9,10 @@
 #include <linux-dmabuf-unstable-v1-server-protocol.h>
 #include <sys/stat.h>
 
-#include <algorithm>
-
 #include "ash/constants/ash_features.h"
 #include "ash/shell.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "components/exo/buffer.h"
@@ -148,7 +147,7 @@ class WaylandDmabufFeedback {
 
     for (const auto& [format, modifier_entries] :
          default_tranche_->GetFormatsAndModifiers()) {
-      if (display_formats_and_modifiers.contains(format)) {
+      if (base::Contains(display_formats_and_modifiers, format)) {
         base::flat_map<size_t, uint64_t> scanout_modifier_entries;
 
         if (modifier_entries.size() == 1) {
@@ -157,8 +156,8 @@ class WaylandDmabufFeedback {
           scanout_modifier_entries.emplace(it->first, it->second);
         } else {
           for (const auto& [table_index, modifier] : modifier_entries) {
-            if (std::ranges::contains(display_formats_and_modifiers.at(format),
-                                      modifier)) {
+            if (base::Contains(display_formats_and_modifiers.at(format),
+                               modifier)) {
               scanout_modifier_entries.emplace(table_index, modifier);
             }
           }
@@ -252,7 +251,7 @@ class WaylandDmabufSurfaceFeedback : public SurfaceObserver {
   }
   void OnSurfaceFeedbackRefDestroyed(
       WaylandDmabufSurfaceFeedbackResourceWrapper* surface_feedback_ref) {
-    DCHECK(surface_feedback_refs_.contains(surface_feedback_ref));
+    DCHECK(base::Contains(surface_feedback_refs_, surface_feedback_ref));
     surface_feedback_refs_.erase(surface_feedback_ref);
     if (surface_feedback_refs_.empty())
       feedback_manager_->RemoveSurfaceFeedback(surface_);
@@ -388,7 +387,7 @@ WaylandDmabufFeedbackManager::WaylandDmabufFeedbackManager(Display* display)
 WaylandDmabufFeedbackManager::~WaylandDmabufFeedbackManager() = default;
 
 bool WaylandDmabufFeedbackManager::IsFormatSupported(uint32_t format) const {
-  return drm_formats_and_modifiers_.contains(format);
+  return base::Contains(drm_formats_and_modifiers_, format);
 }
 
 void WaylandDmabufFeedbackManager::SendFormatsAndModifiers(
@@ -462,14 +461,14 @@ void WaylandDmabufFeedbackManager::GetSurfaceFeedback(
                     std::move(surface_feedback_ref));
 
   auto* feedback = surface_feedback->GetFeedback();
-  if (scanout_candidates_.contains(surface))
+  if (base::Contains(scanout_candidates_, surface))
     feedback->MaybeAddScanoutTranche(surface);
 
   SendFeedback(feedback, feedback_resource);
 }
 
 void WaylandDmabufFeedbackManager::RemoveSurfaceFeedback(Surface* surface) {
-  DCHECK(surface_feedbacks_.contains(surface));
+  DCHECK(base::Contains(surface_feedbacks_, surface));
   surface_feedbacks_.erase(surface);
 }
 
@@ -485,7 +484,7 @@ void WaylandDmabufFeedbackManager::AddSurfaceToScanoutCandidates(
 
   scanout_candidates_.emplace(surface, reason);
 
-  if (!surface_feedbacks_.contains(surface)) {
+  if (!base::Contains(surface_feedbacks_, surface)) {
     return;
   }
 
@@ -523,7 +522,7 @@ void WaylandDmabufFeedbackManager::RemoveSurfaceFromScanoutCandidates(
     return;
   }
 
-  if (!surface_feedbacks_.contains(surface)) {
+  if (!base::Contains(surface_feedbacks_, surface)) {
     return;
   }
 
@@ -541,8 +540,8 @@ void WaylandDmabufFeedbackManager::RemoveSurfaceFromScanoutCandidates(
 }
 
 void WaylandDmabufFeedbackManager::MaybeResendFeedback(Surface* surface) {
-  if (!scanout_candidates_.contains(surface) ||
-      !surface_feedbacks_.contains(surface)) {
+  if (!base::Contains(scanout_candidates_, surface) ||
+      !base::Contains(surface_feedbacks_, surface)) {
     return;
   }
 
diff --git a/components/exo/wayland/zcr_keyboard_configuration.cc b/components/exo/wayland/zcr_keyboard_configuration.cc
index b295348416..293b0418a7 100644
--- a/components/exo/wayland/zcr_keyboard_configuration.cc
+++ b/components/exo/wayland/zcr_keyboard_configuration.cc
@@ -14,6 +14,7 @@
 #include "ash/ime/ime_controller_impl.h"
 #include "ash/shell.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/memory/free_deleter.h"
 #include "base/memory/raw_ptr.h"
@@ -131,7 +132,7 @@ class WaylandKeyboardDeviceConfigurationDelegate
 
     for (const auto& descriptor : enabled_ime_descriptors) {
       const std::string& keyboard_layout = descriptor.keyboard_layout();
-      if (!installed_keyboard_layouts_.contains(keyboard_layout)) {
+      if (!base::Contains(installed_keyboard_layouts_, keyboard_layout)) {
         sequenced_task_runner_->PostTaskAndReplyWithResult(
             FROM_HERE,
             base::BindOnce(
diff --git a/components/exo/wayland/zcr_ui_controls.cc b/components/exo/wayland/zcr_ui_controls.cc
index 6d59a4fecf..8234ffee96 100644
--- a/components/exo/wayland/zcr_ui_controls.cc
+++ b/components/exo/wayland/zcr_ui_controls.cc
@@ -75,7 +75,7 @@ base::OnceClosure UpdateStateAndBindEmitProcessed(struct wl_resource* resource,
 
   return base::BindLambdaForTesting([=]() {
     // Ensure |resource| hasn't been destroyed in the meantime.
-    if (state->pending_request_ids_.contains(resource)) {
+    if (base::Contains(state->pending_request_ids_, resource)) {
       zcr_ui_controls_v1_send_request_processed(resource, id);
       state->pending_request_ids_[resource].erase(id);
 
diff --git a/components/exo/wayland/zwp_linux_dmabuf.cc b/components/exo/wayland/zwp_linux_dmabuf.cc
index 3eeac4e93d..3b446a0e6c 100644
--- a/components/exo/wayland/zwp_linux_dmabuf.cc
+++ b/components/exo/wayland/zwp_linux_dmabuf.cc
@@ -8,6 +8,7 @@
 #include <drm_fourcc.h>
 #include <linux-dmabuf-unstable-v1-server-protocol.h>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "components/exo/buffer.h"
 #include "components/exo/display.h"
@@ -109,7 +110,7 @@ bool ValidateLinuxBufferParams(wl_resource* resource,
 
   // Validate that we have planes 0..num_planes-1
   for (uint32_t i = 0; i < num_planes; ++i) {
-    if (!linux_buffer_params->planes.contains(i)) {
+    if (!base::Contains(linux_buffer_params->planes, i)) {
       wl_resource_post_error(resource,
                              ZWP_LINUX_BUFFER_PARAMS_V1_ERROR_INCOMPLETE,
                              "missing a plane");
diff --git a/components/facilitated_payments/core/browser/pix_account_linking_manager.cc b/components/facilitated_payments/core/browser/pix_account_linking_manager.cc
index ebd4fdd5d3..130e01d472 100644
--- a/components/facilitated_payments/core/browser/pix_account_linking_manager.cc
+++ b/components/facilitated_payments/core/browser/pix_account_linking_manager.cc
@@ -4,8 +4,6 @@
 
 #include "components/facilitated_payments/core/browser/pix_account_linking_manager.h"
 
-#include <utility>
-
 #include "base/check_deref.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/bind.h"
@@ -211,7 +209,8 @@ void PixAccountLinkingManager::OnUiScreenEvent(UiEvent ui_event_type) {
       break;
     }
     default:
-      NOTREACHED() << "Unhandled UiEvent " << std::to_underlying(ui_event_type);
+      NOTREACHED() << "Unhandled UiEvent "
+                   << base::to_underlying(ui_event_type);
   }
 }
 
diff --git a/components/favicon/core/core_favicon_service.cc b/components/favicon/core/core_favicon_service.cc
index d07723e949..b591bdba06 100644
--- a/components/favicon/core/core_favicon_service.cc
+++ b/components/favicon/core/core_favicon_service.cc
@@ -4,8 +4,7 @@
 
 #include "components/favicon/core/core_favicon_service.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "components/favicon_base/favicon_util.h"
 #include "third_party/skia/include/core/SkBitmap.h"
 #include "ui/gfx/image/image.h"
@@ -35,7 +34,7 @@ std::vector<SkBitmap> CoreFaviconService::ExtractSkBitmapsToStore(
   const std::vector<float> favicon_scales = favicon_base::GetFaviconScales();
   for (const gfx::ImageSkiaRep& rep : image_skia.image_reps()) {
     // Only save images with a supported sale.
-    if (std::ranges::contains(favicon_scales, rep.scale())) {
+    if (base::Contains(favicon_scales, rep.scale())) {
       bitmaps.push_back(rep.GetBitmap());
     }
   }
diff --git a/components/favicon/core/favicon_backend.cc b/components/favicon/core/favicon_backend.cc
index 998935cbc0..8cfda438a8 100644
--- a/components/favicon/core/favicon_backend.cc
+++ b/components/favicon/core/favicon_backend.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/memory/ptr_util.h"
@@ -406,7 +407,7 @@ MergeFaviconResult FaviconBackend::MergeFavicon(
       // a favicon bitmap mapped to `icon_url`. The one there is more correct
       // and having multiple equally sized favicon bitmaps for `page_url` is
       // ambiguous in terms of GetFaviconsForURL().
-      if (std::ranges::contains(favicon_sizes, bitmaps_to_copy[j].pixel_size))
+      if (base::Contains(favicon_sizes, bitmaps_to_copy[j].pixel_size))
         continue;
 
       // Add the favicon bitmap as expired as it is not consistent with the
diff --git a/components/favicon/core/favicon_handler.cc b/components/favicon/core/favicon_handler.cc
index b579f5521f..538d6c06e3 100644
--- a/components/favicon/core/favicon_handler.cc
+++ b/components/favicon/core/favicon_handler.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
@@ -68,7 +69,7 @@ bool HasExpiredOrIncompleteResult(
   for (float favicon_scale : favicon_scales) {
     int edge_size_in_pixel = std::ceil(desired_size_in_dip * favicon_scale);
     gfx::Size value(edge_size_in_pixel, edge_size_in_pixel);
-    if (!std::ranges::contains(favicon_sizes, value)) {
+    if (!base::Contains(favicon_sizes, value)) {
       return true;
     }
   }
diff --git a/components/favicon_base/select_favicon_frames.cc b/components/favicon_base/select_favicon_frames.cc
index ff09c8d861..9a61ec9de8 100644
--- a/components/favicon_base/select_favicon_frames.cc
+++ b/components/favicon_base/select_favicon_frames.cc
@@ -12,6 +12,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "components/favicon_base/favicon_util.h"
 #include "skia/ext/image_operations.h"
 #include "third_party/skia/include/core/SkCanvas.h"
@@ -127,7 +128,7 @@ void GetCandidateIndicesWithBestScores(
     return;
   }
 
-  if (std::ranges::contains(desired_sizes, 0)) {
+  if (base::Contains(desired_sizes, 0)) {
     // Just return the biggest image available.
     SelectionResult result;
     result.index = BiggestCandidate(candidate_sizes);
diff --git a/components/feature_engagement/internal/chrome_variations_configuration.cc b/components/feature_engagement/internal/chrome_variations_configuration.cc
index be2452f25e..ec2bf68c7d 100644
--- a/components/feature_engagement/internal/chrome_variations_configuration.cc
+++ b/components/feature_engagement/internal/chrome_variations_configuration.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "build/build_config.h"
@@ -117,7 +118,7 @@ void ChromeVariationsConfiguration::LoadFeatureConfig(
     const ConfigurationProviderList& configuration_providers,
     const FeatureVector& all_features,
     const GroupVector& all_groups) {
-  DCHECK(!configs_.contains(feature.name));
+  DCHECK(!base::Contains(configs_, feature.name));
 
   DVLOG(3) << "Loading feature config for " << feature.name;
   bool loaded = false;
@@ -143,7 +144,7 @@ void ChromeVariationsConfiguration::LoadFeatureConfig(
 void ChromeVariationsConfiguration::LoadGroupConfig(
     const base::Feature& group,
     const ConfigurationProviderList& configuration_providers) {
-  DCHECK(!group_configs_.contains(group.name));
+  DCHECK(!base::Contains(group_configs_, group.name));
 
   DVLOG(3) << "Parsing group config for " << group.name;
 
diff --git a/components/feature_engagement/internal/feature_config_condition_validator.cc b/components/feature_engagement/internal/feature_config_condition_validator.cc
index 77aef62be0..bbc6e22d65 100644
--- a/components/feature_engagement/internal/feature_config_condition_validator.cc
+++ b/components/feature_engagement/internal/feature_config_condition_validator.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/notreached.h"
 #include "components/feature_engagement/internal/availability_model.h"
@@ -123,7 +124,7 @@ void FeatureConfigConditionValidator::NotifyIsShowing(
       DCHECK(config.session_rate_impact.affected_features.has_value());
       for (const std::string& feature_name :
            config.session_rate_impact.affected_features.value()) {
-        DCHECK(std::ranges::contains(all_feature_names, feature_name));
+        DCHECK(base::Contains(all_feature_names, feature_name));
         ++times_shown_for_feature_[feature_name];
       }
       break;
@@ -222,7 +223,7 @@ bool FeatureConfigConditionValidator::IsBlocked(
     case BlockedBy::Type::EXPLICIT:
       for (const std::string& feature_name :
            *config.blocked_by.affected_features) {
-        if (currently_showing_features_.contains(feature_name)) {
+        if (base::Contains(currently_showing_features_, feature_name)) {
           return true;
         }
       }
diff --git a/components/feed/core/v2/feed_stream.cc b/components/feed/core/v2/feed_stream.cc
index 8b3713b2e7..0a539ef1ae 100644
--- a/components/feed/core/v2/feed_stream.cc
+++ b/components/feed/core/v2/feed_stream.cc
@@ -11,6 +11,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -865,7 +866,7 @@ void FeedStream::UploadActionsComplete(UploadActionsTask::Result result) {
 }
 
 bool FeedStream::WasUrlRecentlyNavigatedFromFeed(const GURL& url) {
-  return std::ranges::contains(recent_feed_navigations_, url);
+  return base::Contains(recent_feed_navigations_, url);
 }
 
 void FeedStream::InvalidateContentCacheFor(StreamKind stream_kind) {
diff --git a/components/feed/core/v2/scheduling.cc b/components/feed/core/v2/scheduling.cc
index 5a2aa9c0a3..180a224634 100644
--- a/components/feed/core/v2/scheduling.cc
+++ b/components/feed/core/v2/scheduling.cc
@@ -4,10 +4,9 @@
 
 #include "components/feed/core/v2/scheduling.h"
 
-#include <utility>
-
 #include "base/json/values_util.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "components/feed/core/v2/config.h"
 #include "components/feed/core/v2/feedstore_util.h"
@@ -48,7 +47,7 @@ RequestSchedule::Type GetScheduleType(const base::Value* value) {
   if (value && value->is_int()) {
     int int_value = value->GetInt();
     if (int_value >= 0 &&
-        int_value <= std::to_underlying(RequestSchedule::Type::kMaxValue)) {
+        int_value <= base::to_underlying(RequestSchedule::Type::kMaxValue)) {
       return static_cast<RequestSchedule::Type>(int_value);
     }
   }
@@ -68,7 +67,7 @@ base::DictValue RequestScheduleToDict(const RequestSchedule& schedule) {
   base::DictValue result;
   result.Set("anchor", base::TimeToValue(schedule.anchor_time));
   result.Set("offsets", VectorToList(schedule.refresh_offsets));
-  result.Set("type", std::to_underlying(schedule.type));
+  result.Set("type", base::to_underlying(schedule.type));
   return result;
 }
 
diff --git a/components/feed/feed_feature_list.cc b/components/feed/feed_feature_list.cc
index b11f3a2bf3..91aee6f381 100644
--- a/components/feed/feed_feature_list.cc
+++ b/components/feed/feed_feature_list.cc
@@ -4,8 +4,7 @@
 
 #include "components/feed/feed_feature_list.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/time/time.h"
@@ -82,7 +81,7 @@ BASE_FEATURE(kAndroidOpenIncognitoAsWindow, base::FEATURE_DISABLED_BY_DEFAULT);
 bool IsWebFeedEnabledForLocale(const std::string& country) {
   const std::vector<std::string> launched_countries = {"AU", "CA", "GB",
                                                        "NZ", "US", "ZA"};
-  return std::ranges::contains(launched_countries, country) &&
+  return base::Contains(launched_countries, country) &&
          !base::FeatureList::IsEnabled(kWebFeedKillSwitch);
 }
 
diff --git a/components/gcm_driver/account_tracker.cc b/components/gcm_driver/account_tracker.cc
index ea2de2de5d..459942dce8 100644
--- a/components/gcm_driver/account_tracker.cc
+++ b/components/gcm_driver/account_tracker.cc
@@ -4,6 +4,7 @@
 
 #include "components/gcm_driver/account_tracker.h"
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/observer_list.h"
 #include "base/trace_event/trace_event.h"
@@ -119,10 +120,10 @@ void AccountTracker::OnPrimaryAccountChanged(
 
 void AccountTracker::UpdateSignInState(const CoreAccountId& account_id,
                                        bool is_signed_in) {
-  if (!is_signed_in && !accounts_.contains(account_id))
+  if (!is_signed_in && !base::Contains(accounts_, account_id))
     return;
 
-  DCHECK(accounts_.contains(account_id));
+  DCHECK(base::Contains(accounts_, account_id));
   AccountState& account = accounts_[account_id];
   if (account.is_signed_in == is_signed_in)
     return;
@@ -133,7 +134,7 @@ void AccountTracker::UpdateSignInState(const CoreAccountId& account_id,
 }
 
 void AccountTracker::StartTrackingAccount(const CoreAccountInfo& account) {
-  if (accounts_.contains(account.account_id))
+  if (base::Contains(accounts_, account.account_id))
     return;
 
   DVLOG(1) << "StartTracking " << account.account_id;
@@ -145,7 +146,7 @@ void AccountTracker::StartTrackingAccount(const CoreAccountInfo& account) {
 
 void AccountTracker::StopTrackingAccount(const CoreAccountId account_id) {
   DVLOG(1) << "StopTracking " << account_id;
-  if (accounts_.contains(account_id)) {
+  if (base::Contains(accounts_, account_id)) {
     UpdateSignInState(account_id, /*is_signed_in=*/false);
     accounts_.erase(account_id);
   }
diff --git a/components/global_media_controls/public/media_session_item_producer.cc b/components/global_media_controls/public/media_session_item_producer.cc
index 68ee306053..5cae7177f8 100644
--- a/components/global_media_controls/public/media_session_item_producer.cc
+++ b/components/global_media_controls/public/media_session_item_producer.cc
@@ -4,6 +4,7 @@
 
 #include "components/global_media_controls/public/media_session_item_producer.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/observer_list.h"
 #include "components/global_media_controls/public/media_item_manager.h"
@@ -292,8 +293,8 @@ void MediaSessionItemProducer::OnFocusLost(
     return;
 
   // If we're not currently showing this item, then we can just remove it.
-  if (!active_controllable_session_ids_.contains(id) &&
-      !frozen_session_ids_.contains(id)) {
+  if (!base::Contains(active_controllable_session_ids_, id) &&
+      !base::Contains(frozen_session_ids_, id)) {
     RemoveItem(id);
     return;
   }
@@ -368,7 +369,7 @@ bool MediaSessionItemProducer::IsItemActivelyPlaying(const std::string& id) {
 
 void MediaSessionItemProducer::ActivateItem(const std::string& id) {
   DCHECK(HasSession(id));
-  if (inactive_session_ids_.contains(id))
+  if (base::Contains(inactive_session_ids_, id))
     return;
 
   active_controllable_session_ids_.insert(id);
@@ -392,14 +393,14 @@ void MediaSessionItemProducer::RemoveItem(const std::string& id) {
 
 void MediaSessionItemProducer::RefreshItem(const std::string& id) {
   DCHECK(HasSession(id));
-  if (inactive_session_ids_.contains(id))
+  if (base::Contains(inactive_session_ids_, id))
     return;
 
   item_manager_->RefreshItem(id);
 }
 
 bool MediaSessionItemProducer::HasSession(const std::string& id) const {
-  return sessions_.contains(id);
+  return base::Contains(sessions_, id);
 }
 
 void MediaSessionItemProducer::LogMediaSessionActionButtonPressed(
@@ -454,7 +455,7 @@ MediaSessionItemProducer::Session* MediaSessionItemProducer::GetSession(
 }
 
 void MediaSessionItemProducer::OnSessionBecameActive(const std::string& id) {
-  DCHECK(inactive_session_ids_.contains(id));
+  DCHECK(base::Contains(inactive_session_ids_, id));
 
   auto it = sessions_.find(id);
   CHECK(it != sessions_.end());
@@ -471,7 +472,7 @@ void MediaSessionItemProducer::OnSessionBecameActive(const std::string& id) {
 
 void MediaSessionItemProducer::OnSessionBecameInactive(const std::string& id) {
   // If this session is already marked inactive, then there's nothing to do.
-  if (inactive_session_ids_.contains(id))
+  if (base::Contains(inactive_session_ids_, id))
     return;
 
   inactive_session_ids_.insert(id);
diff --git a/components/global_media_controls/public/views/media_item_ui_detailed_view.cc b/components/global_media_controls/public/views/media_item_ui_detailed_view.cc
index 27ceb059c1..fd6407a3f8 100644
--- a/components/global_media_controls/public/views/media_item_ui_detailed_view.cc
+++ b/components/global_media_controls/public/views/media_item_ui_detailed_view.cc
@@ -592,8 +592,8 @@ void MediaItemUIDetailedView::UpdateActionButtonsVisibility() {
   bool should_invalidate_layout = false;
 
   for (views::Button* button : action_buttons_) {
-    bool should_show = enabled_actions_.contains(
-        static_cast<MediaSessionAction>(button->GetID()));
+    bool should_show = base::Contains(
+        enabled_actions_, static_cast<MediaSessionAction>(button->GetID()));
 
     if (button == picture_in_picture_button_) {
       // Force the picture-in-picture button to be visible if the media is
diff --git a/components/global_media_controls/public/views/media_item_ui_list_view.cc b/components/global_media_controls/public/views/media_item_ui_list_view.cc
index 6d5b313fc3..452601840e 100644
--- a/components/global_media_controls/public/views/media_item_ui_list_view.cc
+++ b/components/global_media_controls/public/views/media_item_ui_list_view.cc
@@ -4,6 +4,7 @@
 
 #include "components/global_media_controls/public/views/media_item_ui_list_view.h"
 
+#include "base/containers/contains.h"
 #include "components/global_media_controls/public/views/media_item_ui_updated_view.h"
 #include "components/global_media_controls/public/views/media_item_ui_view.h"
 #include "ui/base/metadata/metadata_impl_macros.h"
@@ -61,7 +62,7 @@ MediaItemUIListView::~MediaItemUIListView() = default;
 
 void MediaItemUIListView::ShowItem(const std::string& id,
                                    std::unique_ptr<MediaItemUIView> item) {
-  DCHECK(!items_.contains(id));
+  DCHECK(!base::Contains(items_, id));
   DCHECK_NE(nullptr, item.get());
 
   item->SetScrollView(this);
@@ -72,7 +73,7 @@ void MediaItemUIListView::ShowItem(const std::string& id,
 }
 
 void MediaItemUIListView::HideItem(const std::string& id) {
-  if (!items_.contains(id))
+  if (!base::Contains(items_, id))
     return;
 
   // If we're removing the topmost item and there are others, then we need to
@@ -96,7 +97,7 @@ MediaItemUIView* MediaItemUIListView::GetItem(const std::string& id) {
 void MediaItemUIListView::ShowUpdatedItem(
     const std::string& id,
     std::unique_ptr<MediaItemUIUpdatedView> item) {
-  CHECK(!updated_items_.contains(id));
+  CHECK(!base::Contains(updated_items_, id));
   CHECK(item.get());
 
   updated_items_[id] = contents()->AddChildView(std::move(item));
@@ -106,7 +107,7 @@ void MediaItemUIListView::ShowUpdatedItem(
 }
 
 void MediaItemUIListView::HideUpdatedItem(const std::string& id) {
-  if (!updated_items_.contains(id)) {
+  if (!base::Contains(updated_items_, id)) {
     return;
   }
 
diff --git a/components/global_media_controls/public/views/media_item_ui_updated_view.cc b/components/global_media_controls/public/views/media_item_ui_updated_view.cc
index 9edbce7c86..e805c62218 100644
--- a/components/global_media_controls/public/views/media_item_ui_updated_view.cc
+++ b/components/global_media_controls/public/views/media_item_ui_updated_view.cc
@@ -4,8 +4,6 @@
 
 #include "components/global_media_controls/public/views/media_item_ui_updated_view.h"
 
-#include <algorithm>
-
 #include "base/metrics/histogram_functions.h"
 #include "components/global_media_controls/media_view_utils.h"
 #include "components/global_media_controls/public/format_duration.h"
@@ -603,8 +601,8 @@ void MediaItemUIUpdatedView::MediaActionButtonPressed(views::Button* button) {
 
   // Make the screen reader announce the button text for accessibility since
   // there are only visual changes outside these buttons when they are clicked.
-  if (std::ranges::contains(kProgressRowMediaActions,
-                            static_cast<MediaSessionAction>(button->GetID()))) {
+  if (base::Contains(kProgressRowMediaActions,
+                     static_cast<MediaSessionAction>(button->GetID()))) {
     GetViewAccessibility().AnnouncePolitely(button->GetTooltipText());
   }
 
@@ -626,22 +624,20 @@ void MediaItemUIUpdatedView::UpdateMediaActionButtonsVisibility() {
   bool should_invalidate_layout = false;
 
   for (views::Button* button : media_action_buttons_) {
-    bool should_show = media_actions_.contains(
-        static_cast<MediaSessionAction>(button->GetID()));
+    bool should_show = base::Contains(
+        media_actions_, static_cast<MediaSessionAction>(button->GetID()));
     // Do not show the picture-in-picture button for a casting media item.
     if (button == picture_in_picture_button_ && footer_view_) {
       should_show = false;
     }
 
-    if (std::ranges::contains(
-            kProgressRowMediaActions,
-            static_cast<MediaSessionAction>(button->GetID()))) {
+    if (base::Contains(kProgressRowMediaActions,
+                       static_cast<MediaSessionAction>(button->GetID()))) {
       if (is_live_) {
         // For live media, the seek progress buttons are always hidden and the
         // other buttons are visible if their media actions are supported.
-        if (std::ranges::contains(
-                kProgressRowSeekMediaActions,
-                static_cast<MediaSessionAction>(button->GetID()))) {
+        if (base::Contains(kProgressRowSeekMediaActions,
+                           static_cast<MediaSessionAction>(button->GetID()))) {
           should_show = false;
         }
       } else {
diff --git a/components/global_media_controls/public/views/media_item_ui_view.cc b/components/global_media_controls/public/views/media_item_ui_view.cc
index 2f4e105ff4..f464b2f9f1 100644
--- a/components/global_media_controls/public/views/media_item_ui_view.cc
+++ b/components/global_media_controls/public/views/media_item_ui_view.cc
@@ -248,9 +248,11 @@ void MediaItemUIView::OnVisibleActionsChanged(
     const base::flat_set<media_session::mojom::MediaSessionAction>& actions) {
   has_many_actions_ =
       (actions.size() >= kMinVisibleActionsForExpanding ||
-       actions.contains(
+       base::Contains(
+           actions,
            media_session::mojom::MediaSessionAction::kEnterPictureInPicture) ||
-       actions.contains(
+       base::Contains(
+           actions,
            media_session::mojom::MediaSessionAction::kExitPictureInPicture));
   ForceExpandedState();
 
diff --git a/components/guest_view/browser/guest_view_manager.cc b/components/guest_view/browser/guest_view_manager.cc
index 67524944e8..5e6cf13ad6 100644
--- a/components/guest_view/browser/guest_view_manager.cc
+++ b/components/guest_view/browser/guest_view_manager.cc
@@ -7,6 +7,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "build/build_config.h"
 #include "components/crash/core/common/crash_key.h"
@@ -140,7 +141,7 @@ void GuestViewManager::AttachGuest(content::ChildProcessId embedder_process_id,
 
   // If there is an existing guest attached to the element, then the embedder is
   // misbehaving.
-  if (instance_id_map_.contains(key)) {
+  if (base::Contains(instance_id_map_, key)) {
     bad_message::ReceivedBadMessage(embedder_process_id,
                                     bad_message::GVM_INVALID_ATTACH);
     return;
@@ -567,7 +568,7 @@ void GuestViewManager::RegisterGuestViewType(
   // if it was registered elsewhere, then we do not want to overwrite it. Note
   // that it is possible for tests to have special test factory methods
   // registered here.
-  if (guest_view_registry_.contains(type))
+  if (base::Contains(guest_view_registry_, type))
     return;
 
   guest_view_registry_.insert({type, {create_function, cleanup_function}});
@@ -635,7 +636,7 @@ bool GuestViewManager::CanEmbedderAccessInstanceIDMaybeKill(
 bool GuestViewManager::CanUseGuestInstanceID(int guest_instance_id) {
   if (guest_instance_id <= last_instance_id_removed_)
     return false;
-  return !removed_instance_ids_.contains(guest_instance_id);
+  return !base::Contains(removed_instance_ids_, guest_instance_id);
 }
 
 bool GuestViewManager::CanEmbedderAccessInstanceID(
diff --git a/components/guest_view/renderer/guest_view_container.cc b/components/guest_view/renderer/guest_view_container.cc
index 39b17657c5..28176740b6 100644
--- a/components/guest_view/renderer/guest_view_container.cc
+++ b/components/guest_view/renderer/guest_view_container.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/no_destructor.h"
@@ -63,7 +64,7 @@ GuestViewContainer::GuestViewContainer(content::RenderFrame* render_frame,
     : element_instance_id_(element_instance_id),
       render_frame_lifetime_observer_(
           std::make_unique<RenderFrameLifetimeObserver>(this, render_frame)) {
-  DCHECK(!GetContainerMap().contains(element_instance_id));
+  DCHECK(!base::Contains(GetContainerMap(), element_instance_id));
   GetContainerMap().insert(std::make_pair(element_instance_id, this));
 }
 
diff --git a/components/heap_profiling/multi_process/test_driver.cc b/components/heap_profiling/multi_process/test_driver.cc
index cd1ee1fbac..3fb45094d4 100644
--- a/components/heap_profiling/multi_process/test_driver.cc
+++ b/components/heap_profiling/multi_process/test_driver.cc
@@ -4,12 +4,12 @@
 
 #include "components/heap_profiling/multi_process/test_driver.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <string>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/json/json_reader.h"
@@ -76,7 +76,7 @@ bool RenderersAreBeingProfiled(
         base::kNullProcessHandle)
       continue;
     base::ProcessId pid = iter.GetCurrentValue()->GetProcess().Pid();
-    if (std::ranges::contains(profiled_pids, pid)) {
+    if (base::Contains(profiled_pids, pid)) {
       return true;
     }
   }
@@ -810,7 +810,7 @@ void TestDriver::WaitForProfilingToStartForBrowserUIThread() {
     Supervisor::GetInstance()->GetProfiledPids(std::move(callback));
     run_loop.Run();
 
-    if (std::ranges::contains(profiled_pids, base::GetCurrentProcId())) {
+    if (base::Contains(profiled_pids, base::GetCurrentProcId())) {
       break;
     }
   }
diff --git a/components/history/core/browser/download_database.cc b/components/history/core/browser/download_database.cc
index 8b2ff73677..bb5af3e945 100644
--- a/components/history/core/browser/download_database.cc
+++ b/components/history/core/browser/download_database.cc
@@ -11,6 +11,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/files/file_path.h"
 #include "base/metrics/histogram_macros.h"
@@ -508,7 +509,7 @@ void DownloadDatabase::QueryDownloads(std::vector<DownloadRow>* results) {
       dropped_reason = DROPPED_REASON_BAD_DANGER_TYPE;
     }
     if (dropped_reason == DROPPED_REASON_MAX) {
-      DCHECK(!info_map.contains(info->id));
+      DCHECK(!base::Contains(info_map, info->id));
       uint32_t id = info->id;
       info_map[id] = info.release();
     }
@@ -532,7 +533,7 @@ void DownloadDatabase::QueryDownloads(std::vector<DownloadRow>* results) {
 
     // Confirm the id has already been seen--if it hasn't, discard the
     // record.
-    if (!info_map.contains(id)) {
+    if (!base::Contains(info_map, id)) {
       continue;
     }
 
diff --git a/components/history/core/browser/top_sites_impl.cc b/components/history/core/browser/top_sites_impl.cc
index a9569ab555..71493000d2 100644
--- a/components/history/core/browser/top_sites_impl.cc
+++ b/components/history/core/browser/top_sites_impl.cc
@@ -13,6 +13,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/bind.h"
@@ -363,8 +364,8 @@ bool TopSitesImpl::AddPrepopulatedPages(MostVisitedURLList* urls) const {
   for (const auto& prepopulated_page : prepopulated_pages_) {
     if (urls->size() >= kTopSitesNumber)
       break;
-    if (!std::ranges::contains(*urls, prepopulated_page.most_visited.url,
-                               &MostVisitedURL::url)) {
+    if (!base::Contains(*urls, prepopulated_page.most_visited.url,
+                        &MostVisitedURL::url)) {
       urls->push_back(prepopulated_page.most_visited);
       added = true;
     }
diff --git a/components/history_clusters/core/cluster_similarity_heuristics_processor.cc b/components/history_clusters/core/cluster_similarity_heuristics_processor.cc
index dd29644f75..d790f1b8a4 100644
--- a/components/history_clusters/core/cluster_similarity_heuristics_processor.cc
+++ b/components/history_clusters/core/cluster_similarity_heuristics_processor.cc
@@ -8,6 +8,7 @@
 #include <unordered_set>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "components/history_clusters/core/on_device_clustering_util.h"
 #include "components/history_clusters/core/similar_visit.h"
@@ -79,7 +80,7 @@ void ClusterSimilarityHeuristicsProcessor::ProcessClusters(
 
         bool has_full_set_membership = true;
         for (const auto& visit : smaller_cluster_visits) {
-          if (!larger_cluster_visits.contains(SimilarVisit(visit))) {
+          if (!base::Contains(larger_cluster_visits, SimilarVisit(visit))) {
             has_full_set_membership = false;
             break;
           }
diff --git a/components/history_clusters/core/config.cc b/components/history_clusters/core/config.cc
index d1c06fdef0..e99d7a530b 100644
--- a/components/history_clusters/core/config.cc
+++ b/components/history_clusters/core/config.cc
@@ -4,9 +4,8 @@
 
 #include "components/history_clusters/core/config.h"
 
-#include <algorithm>
-
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/no_destructor.h"
@@ -280,10 +279,8 @@ bool IsApplicationLocaleSupportedByJourneys(
   // Allow any exact locale matches, and also allow any users where the
   // primary language subtag, e.g. "en" from "en-US" to match any element of
   // the list.
-  return allowlist.empty() ||
-         std::ranges::contains(allowlist, application_locale) ||
-         std::ranges::contains(allowlist,
-                               l10n_util::GetLanguage(application_locale));
+  return allowlist.empty() || base::Contains(allowlist, application_locale) ||
+         base::Contains(allowlist, l10n_util::GetLanguage(application_locale));
 }
 
 const Config& GetConfig() {
diff --git a/components/history_clusters/core/features.cc b/components/history_clusters/core/features.cc
index 8124cbe075..b9339141a3 100644
--- a/components/history_clusters/core/features.cc
+++ b/components/history_clusters/core/features.cc
@@ -4,6 +4,7 @@
 
 #include "components/history_clusters/core/features.h"
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/strings/string_split.h"
diff --git a/components/history_clusters/core/history_clusters_db_tasks.cc b/components/history_clusters/core/history_clusters_db_tasks.cc
index a88ef998e5..cc2ea73956 100644
--- a/components/history_clusters/core/history_clusters_db_tasks.cc
+++ b/components/history_clusters/core/history_clusters_db_tasks.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/time/time.h"
 #include "base/timer/elapsed_timer.h"
@@ -212,9 +213,9 @@ void GetAnnotatedVisitsToCluster::AddIncompleteVisits(
     // https://crbug.com/1252047.
     history::VisitID visit_id =
         incomplete_visit_context_annotations.visit_row.visit_id;
-    if (std::ranges::contains(
-            annotated_visits_, visit_id,
-            [](const auto& visit) { return visit.visit_row.visit_id; })) {
+    if (base::Contains(annotated_visits_, visit_id, [](const auto& visit) {
+          return visit.visit_row.visit_id;
+        })) {
       continue;
     }
 
diff --git a/components/history_clusters/core/history_clusters_util.cc b/components/history_clusters/core/history_clusters_util.cc
index 88afca7189..388105324d 100644
--- a/components/history_clusters/core/history_clusters_util.cc
+++ b/components/history_clusters/core/history_clusters_util.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/i18n/case_conversion.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/string_util.h"
@@ -360,7 +361,7 @@ void CoalesceRelatedSearches(std::vector<history::Cluster>& clusters) {
           return;
         }
 
-        if (!std::ranges::contains(cluster.related_searches, search_query)) {
+        if (!base::Contains(cluster.related_searches, search_query)) {
           cluster.related_searches.push_back(search_query);
         }
       }
diff --git a/components/history_clusters/core/keyword_cluster_finalizer.cc b/components/history_clusters/core/keyword_cluster_finalizer.cc
index b84f11d17f..adcc47c27a 100644
--- a/components/history_clusters/core/keyword_cluster_finalizer.cc
+++ b/components/history_clusters/core/keyword_cluster_finalizer.cc
@@ -6,6 +6,7 @@
 
 #include <queue>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
diff --git a/components/history_clusters/core/on_device_clustering_util.cc b/components/history_clusters/core/on_device_clustering_util.cc
index 53196fb453..b7d0c2427a 100644
--- a/components/history_clusters/core/on_device_clustering_util.cc
+++ b/components/history_clusters/core/on_device_clustering_util.cc
@@ -4,9 +4,9 @@
 
 #include "components/history_clusters/core/on_device_clustering_util.h"
 
-#include <algorithm>
 #include <iterator>
 
+#include "base/containers/contains.h"
 #include "base/time/time.h"
 #include "components/history_clusters/core/config.h"
 #include "components/history_clusters/core/history_clusters_util.h"
@@ -43,7 +43,7 @@ void MergeDuplicateVisitIntoCanonicalVisit(
     // This is an n^2 algorithm, but in practice the list of related
     // searches should be on the order of 10 elements long at maximum.
     // If that's not true we should replace this with a set structure.
-    if (!std::ranges::contains(canonical_searches, query)) {
+    if (!base::Contains(canonical_searches, query)) {
       canonical_searches.push_back(query);
     }
   }
diff --git a/components/infobars/core/infobar_container.cc b/components/infobars/core/infobar_container.cc
index 876df137b1..487aa96c3a 100644
--- a/components/infobars/core/infobar_container.cc
+++ b/components/infobars/core/infobar_container.cc
@@ -8,6 +8,7 @@
 
 #include "base/auto_reset.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_base.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/metrics_hashes.h"
@@ -124,7 +125,7 @@ void InfoBarContainer::OnManagerWillBeDestroyed(
 void InfoBarContainer::AddInfoBar(InfoBar* infobar,
                                   size_t position,
                                   bool animate) {
-  DCHECK(!std::ranges::contains(infobars_, infobar));
+  DCHECK(!base::Contains(infobars_, infobar));
   DCHECK_LE(position, infobars_.size());
   infobars_.insert(infobars_.begin() + position, infobar);
   PlatformSpecificAddInfoBar(infobar, position);
diff --git a/components/js_injection/browser/js_to_browser_messaging.cc b/components/js_injection/browser/js_to_browser_messaging.cc
index e81e6d6597..7e501b7994 100644
--- a/components/js_injection/browser/js_to_browser_messaging.cc
+++ b/components/js_injection/browser/js_to_browser_messaging.cc
@@ -4,8 +4,7 @@
 
 #include "components/js_injection/browser/js_to_browser_messaging.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "components/back_forward_cache/back_forward_cache_disable.h"
 #include "components/js_injection/browser/web_message.h"
@@ -37,7 +36,7 @@ namespace {
 // this case we want to let developer to know that local files are not safe,
 // so we still pass "null".
 std::string GetOriginString(const url::Origin& source_origin) {
-  return std::ranges::contains(url::GetLocalSchemes(), source_origin.scheme())
+  return base::Contains(url::GetLocalSchemes(), source_origin.scheme())
              ? "null"
              : source_origin.Serialize();
 }
diff --git a/components/keyed_service/core/dependency_graph.cc b/components/keyed_service/core/dependency_graph.cc
index f7e9b71e54..779d50dc00 100644
--- a/components/keyed_service/core/dependency_graph.cc
+++ b/components/keyed_service/core/dependency_graph.cc
@@ -9,7 +9,6 @@
 #include <algorithm>
 #include <iterator>
 #include <map>
-#include <ranges>
 #include <string_view>
 #include <vector>
 
@@ -86,7 +85,7 @@ bool DependencyGraph::GetDestructionOrder(
 
 bool DependencyGraph::BuildConstructionOrder() {
   // Step 1: Build a set of nodes with no incoming edges.
-  base::circular_deque<DependencyNode*> queue(std::from_range, all_nodes_);
+  base::circular_deque<DependencyNode*> queue(base::from_range, all_nodes_);
   for (const auto& pair : edges_)
     base::Erase(queue, pair.second);
 
@@ -132,7 +131,7 @@ std::string DependencyGraph::DumpAsGraphviz(
   std::string escaped_toplevel_name = Escape(toplevel_name);
 
   // Make a copy of all nodes.
-  base::circular_deque<DependencyNode*> nodes(std::from_range, all_nodes_);
+  base::circular_deque<DependencyNode*> nodes(base::from_range, all_nodes_);
 
   // State all dependencies and remove |second| so we don't generate an
   // implicit dependency on the top level node.
diff --git a/components/keyed_service/core/keyed_service_templated_factory.cc b/components/keyed_service/core/keyed_service_templated_factory.cc
index b7a1a3ae06..957c06ff7b 100644
--- a/components/keyed_service/core/keyed_service_templated_factory.cc
+++ b/components/keyed_service/core/keyed_service_templated_factory.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/callback.h"
 #include "base/memory/ref_counted.h"
diff --git a/components/live_caption/views/caption_bubble_controller_views.cc b/components/live_caption/views/caption_bubble_controller_views.cc
index 3eec982bde..42a2a32c6d 100644
--- a/components/live_caption/views/caption_bubble_controller_views.cc
+++ b/components/live_caption/views/caption_bubble_controller_views.cc
@@ -147,7 +147,8 @@ void CaptionBubbleControllerViews::SetActiveModel(
             // owns |caption_bubble_model|.
             base::Unretained(this)));
 
-    if (closed_sessions_.contains(caption_bubble_context->GetSessionId())) {
+    if (base::Contains(closed_sessions_,
+                       caption_bubble_context->GetSessionId())) {
       caption_bubble_model->Close();
     }
 
@@ -193,7 +194,7 @@ void CaptionBubbleControllerViews::OnSessionEnded(
 
 void CaptionBubbleControllerViews::OnSessionReset(
     const std::string& session_id) {
-  if (closed_sessions_.contains(session_id)) {
+  if (base::Contains(closed_sessions_, session_id)) {
     closed_sessions_.erase(session_id);
   }
 
diff --git a/components/lookalikes/core/lookalike_url_util.cc b/components/lookalikes/core/lookalike_url_util.cc
index feeee65cf1..a136c5d0b8 100644
--- a/components/lookalikes/core/lookalike_url_util.cc
+++ b/components/lookalikes/core/lookalike_url_util.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/hash/sha1.h"
 #include "base/i18n/char_iterator.h"
@@ -185,7 +186,7 @@ bool SkeletonsMatch(const url_formatter::Skeletons& skeletons1,
   DCHECK(!skeletons1.empty());
   DCHECK(!skeletons2.empty());
   for (const std::string& skeleton1 : skeletons1) {
-    if (skeletons2.contains(skeleton1)) {
+    if (base::Contains(skeletons2, skeleton1)) {
       return true;
     }
   }
@@ -441,7 +442,7 @@ bool DoesETLDPlus1MatchTopDomainOrEngagedSite(
       // consider engaged sites that are bare eTLD+1s (or a trivial subdomain)
       // and are a skeleton match.
       if (IsETLDPlusOneOrTrivialSubdomain(engaged_site) &&
-          engaged_site.skeletons.contains(skeleton)) {
+          base::Contains(engaged_site.skeletons, skeleton)) {
         *embedded_target = engaged_site.domain_and_registry;
         return true;
       }
diff --git a/components/manta/manta_service.cc b/components/manta/manta_service.cc
index 0cbd416859..8be646d074 100644
--- a/components/manta/manta_service.cc
+++ b/components/manta/manta_service.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/memory/scoped_refptr.h"
 #include "components/account_id/account_id.h"
diff --git a/components/media_message_center/media_notification_util.cc b/components/media_message_center/media_notification_util.cc
index cb466ec69d..36c95b9745 100644
--- a/components/media_message_center/media_notification_util.cc
+++ b/components/media_message_center/media_notification_util.cc
@@ -4,6 +4,7 @@
 
 #include "components/media_message_center/media_notification_util.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
@@ -83,7 +84,8 @@ base::flat_set<MediaSessionAction> GetTopVisibleActions(
     if (visible_actions.size() >= max_actions)
       break;
 
-    if (!enabled_actions.contains(action) || ignored_actions.contains(action))
+    if (!base::Contains(enabled_actions, action) ||
+        base::Contains(ignored_actions, action))
       continue;
 
     visible_actions.insert(action);
diff --git a/components/media_message_center/media_notification_view_impl.cc b/components/media_message_center/media_notification_view_impl.cc
index 023bfaa6a2..d7d1d6fb02 100644
--- a/components/media_message_center/media_notification_view_impl.cc
+++ b/components/media_message_center/media_notification_view_impl.cc
@@ -7,6 +7,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_macros.h"
 #include "components/media_message_center/media_notification_background_ash_impl.h"
@@ -503,7 +504,7 @@ void MediaNotificationViewImpl::UpdateActionButtonsVisibility() {
   for (views::View* view : GetButtons()) {
     views::Button* action_button = views::Button::AsButton(view);
     bool should_show =
-        visible_actions.contains(GetActionFromButtonTag(*action_button));
+        base::Contains(visible_actions, GetActionFromButtonTag(*action_button));
     bool should_invalidate = should_show != action_button->GetVisible();
 
     action_button->SetVisible(should_show);
diff --git a/components/media_router/browser/media_sinks_observer.cc b/components/media_router/browser/media_sinks_observer.cc
index b4d1a0f973..29f6ec9010 100644
--- a/components/media_router/browser/media_sinks_observer.cc
+++ b/components/media_router/browser/media_sinks_observer.cc
@@ -4,9 +4,8 @@
 
 #include "components/media_router/browser/media_sinks_observer.h"
 
-#include <algorithm>
-
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "components/media_router/browser/media_router.h"
 
 #if DCHECK_IS_ON()
@@ -53,7 +52,7 @@ void MediaSinksObserver::OnSinksUpdated(
   base::AutoReset<bool> reset_in_on_sinks_updated(&in_on_sinks_updated_, true);
 #endif
 
-  if (origins.empty() || std::ranges::contains(origins, origin_)) {
+  if (origins.empty() || base::Contains(origins, origin_)) {
     OnSinksReceived(sinks);
   } else {
     OnSinksReceived(std::vector<MediaSink>());
diff --git a/components/media_router/browser/presentation/controller_presentation_service_delegate_impl.cc b/components/media_router/browser/presentation/controller_presentation_service_delegate_impl.cc
index 3cf3b468f2..44a05b2ac3 100644
--- a/components/media_router/browser/presentation/controller_presentation_service_delegate_impl.cc
+++ b/components/media_router/browser/presentation/controller_presentation_service_delegate_impl.cc
@@ -12,6 +12,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/small_map.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
@@ -263,7 +264,7 @@ void PresentationFrame::ConnectToPresentation(
         std::move(receiver_connection_receiver), pid_route_it->second);
   } else {
     MediaRoute::Id route_id = pid_route_it->second.media_route_id();
-    if (browser_connection_proxies_.contains(route_id)) {
+    if (base::Contains(browser_connection_proxies_, route_id)) {
       return;
     }
 
@@ -517,8 +518,8 @@ void ControllerPresentationServiceDelegateImpl::ReconnectPresentation(
   if (local_presentation_manager->IsLocalPresentation(presentation_id)) {
     auto* route = local_presentation_manager->GetRoute(presentation_id);
 
-    if (!route || !std::ranges::contains(presentation_urls,
-                                         route->media_source().url())) {
+    if (!route ||
+        !base::Contains(presentation_urls, route->media_source().url())) {
       return;
     }
 
@@ -627,9 +628,8 @@ void ControllerPresentationServiceDelegateImpl::OnPresentationResponse(
     const content::PresentationRequest& presentation_request,
     mojom::RoutePresentationConnectionPtr connection,
     const RouteRequestResult& result) {
-  if (!result.route() ||
-      !std::ranges::contains(presentation_request.presentation_urls,
-                             result.presentation_url())) {
+  if (!result.route() || !base::Contains(presentation_request.presentation_urls,
+                                         result.presentation_url())) {
     return;
   }
 
diff --git a/components/media_router/browser/presentation/local_presentation_manager.cc b/components/media_router/browser/presentation/local_presentation_manager.cc
index 029da7e037..3738d172ee 100644
--- a/components/media_router/browser/presentation/local_presentation_manager.cc
+++ b/components/media_router/browser/presentation/local_presentation_manager.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "content/public/browser/render_frame_host.h"
 #include "content/public/browser/render_process_host.h"
 #include "content/public/browser/web_contents.h"
@@ -83,7 +84,7 @@ void LocalPresentationManager::OnLocalPresentationReceiverTerminated(
 
 bool LocalPresentationManager::IsLocalPresentation(
     const std::string& presentation_id) {
-  return local_presentations_.contains(presentation_id);
+  return base::Contains(local_presentations_, presentation_id);
 }
 
 bool LocalPresentationManager::IsLocalPresentation(
diff --git a/components/media_router/browser/presentation/presentation_service_delegate_observers.cc b/components/media_router/browser/presentation/presentation_service_delegate_observers.cc
index 040937bac9..be04e8d6d9 100644
--- a/components/media_router/browser/presentation/presentation_service_delegate_observers.cc
+++ b/components/media_router/browser/presentation/presentation_service_delegate_observers.cc
@@ -4,6 +4,7 @@
 
 #include "components/media_router/browser/presentation/presentation_service_delegate_observers.h"
 
+#include "base/containers/contains.h"
 
 namespace media_router {
 
@@ -23,7 +24,7 @@ void PresentationServiceDelegateObservers::AddObserver(
   DCHECK(observer);
 
   content::GlobalRenderFrameHostId rfh_id(render_process_id, render_frame_id);
-  DCHECK(!observers_.contains(rfh_id));
+  DCHECK(!base::Contains(observers_, rfh_id));
   observers_[rfh_id] = observer;
 }
 
diff --git a/components/media_router/common/providers/cast/cast_media_source.cc b/components/media_router/common/providers/cast/cast_media_source.cc
index a60f4bf4e9..16ccc85792 100644
--- a/components/media_router/common/providers/cast/cast_media_source.cc
+++ b/components/media_router/common/providers/cast/cast_media_source.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/logging.h"
 #include "base/strings/escape.h"
@@ -518,7 +519,7 @@ bool CastMediaSource::ProvidesStreamingAudioCapture() const {
 void CastMediaSource::set_supported_app_types(
     const std::vector<ReceiverAppType>& types) {
   DCHECK(!types.empty());
-  DCHECK(std::ranges::contains(types, ReceiverAppType::kWeb));
+  DCHECK(base::Contains(types, ReceiverAppType::kWeb));
   supported_app_types_ = types;
 }
 
diff --git a/components/media_router/common/providers/cast/certificate/cast_cert_validator.cc b/components/media_router/common/providers/cast/certificate/cast_cert_validator.cc
index bfb27a2147..5cc7087c5f 100644
--- a/components/media_router/common/providers/cast/certificate/cast_cert_validator.cc
+++ b/components/media_router/common/providers/cast/certificate/cast_cert_validator.cc
@@ -13,6 +13,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/logging.h"
 #include "base/path_service.h"
@@ -165,7 +166,7 @@ void DetermineDeviceCertificatePolicy(
   for (const auto& cert : result_path->certs) {
     if (cert->has_policy_oids()) {
       const std::vector<bssl::der::Input>& policies = cert->policy_oids();
-      if (std::ranges::contains(policies, AudioOnlyPolicyOid())) {
+      if (base::Contains(policies, AudioOnlyPolicyOid())) {
         audio_only = true;
         break;
       }
diff --git a/components/media_router/common/providers/cast/certificate/net_parsed_certificate.cc b/components/media_router/common/providers/cast/certificate/net_parsed_certificate.cc
index 8e4a41081f..a6c8050a8e 100644
--- a/components/media_router/common/providers/cast/certificate/net_parsed_certificate.cc
+++ b/components/media_router/common/providers/cast/certificate/net_parsed_certificate.cc
@@ -4,9 +4,8 @@
 
 #include "components/media_router/common/providers/cast/certificate/net_parsed_certificate.h"
 
-#include <algorithm>
-
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_view_util.h"
 #include "crypto/evp.h"
 #include "net/cert/time_conversions.h"
@@ -192,7 +191,7 @@ bool NetParsedCertificate::HasPolicyOid(const openscreen::ByteView& oid) const {
     return false;
   }
   const std::vector<bssl::der::Input>& policies = cert_->policy_oids();
-  return std::ranges::contains(policies, bssl::der::Input(oid));
+  return base::Contains(policies, bssl::der::Input(oid));
 }
 
 void NetParsedCertificate::SetNotBeforeTimeForTesting(time_t not_before) {
diff --git a/components/media_router/common/providers/cast/channel/cast_channel_enum.h b/components/media_router/common/providers/cast/channel/cast_channel_enum.h
index 45da34ff2a..603441464e 100644
--- a/components/media_router/common/providers/cast/channel/cast_channel_enum.h
+++ b/components/media_router/common/providers/cast/channel/cast_channel_enum.h
@@ -7,7 +7,8 @@
 
 #include <cstdint>
 #include <string>
-#include <utility>
+
+#include "base/types/cxx23_to_underlying.h"
 
 namespace cast_channel {
 
@@ -15,7 +16,7 @@ namespace cast_channel {
 // with ostreams.
 template <typename Enumeration>
 auto AsInteger(Enumeration const value) {
-  return std::to_underlying(value);
+  return base::to_underlying(value);
 }
 
 enum class ReadyState {
diff --git a/components/media_router/common/providers/cast/channel/cast_message_handler.cc b/components/media_router/common/providers/cast/channel/cast_message_handler.cc
index e20ffa0397..285b6002de 100644
--- a/components/media_router/common/providers/cast/channel/cast_message_handler.cc
+++ b/components/media_router/common/providers/cast/channel/cast_message_handler.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/observer_list.h"
 #include "base/rand_util.h"
@@ -517,8 +518,8 @@ bool CastMessageHandler::PendingRequests::AddAppAvailabilityRequest(
           base::Unretained(this), request_id));
 
   // Look for a request with the given app ID.
-  bool found = std::ranges::contains(pending_app_availability_requests_, app_id,
-                                     &GetAppAvailabilityRequest::app_id);
+  bool found = base::Contains(pending_app_availability_requests_, app_id,
+                              &GetAppAvailabilityRequest::app_id);
   pending_app_availability_requests_.emplace_back(std::move(request));
   return !found;
 }
diff --git a/components/metrics/expired_histograms_checker.cc b/components/metrics/expired_histograms_checker.cc
index aed85f0a55..699107b779 100644
--- a/components/metrics/expired_histograms_checker.cc
+++ b/components/metrics/expired_histograms_checker.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/metrics_hashes.h"
 #include "base/metrics/statistics_recorder.h"
@@ -26,7 +27,7 @@ ExpiredHistogramsChecker::~ExpiredHistogramsChecker() = default;
 
 bool ExpiredHistogramsChecker::ShouldRecord(uint32_t histogram_hash) const {
   // If histogram is explicitly allowed then it should always be recorded.
-  if (allowlist_.contains(histogram_hash)) {
+  if (base::Contains(allowlist_, histogram_hash)) {
     return true;
   }
   return !std::binary_search(std::begin(expired_histogram_hashes_),
diff --git a/components/metrics/persistent_system_profile.cc b/components/metrics/persistent_system_profile.cc
index 6ed2e876a5..7a86e0b208 100644
--- a/components/metrics/persistent_system_profile.cc
+++ b/components/metrics/persistent_system_profile.cc
@@ -11,6 +11,7 @@
 #include "base/atomicops.h"
 #include "base/bits.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/debug/crash_logging.h"
 #include "base/memory/singleton.h"
diff --git a/components/metrics/stability_metrics_helper.cc b/components/metrics/stability_metrics_helper.cc
index b2d8452483..a4a896ee84 100644
--- a/components/metrics/stability_metrics_helper.cc
+++ b/components/metrics/stability_metrics_helper.cc
@@ -9,6 +9,7 @@
 #include <string>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
diff --git a/components/metrics/test/test_metrics_service_client.cc b/components/metrics/test/test_metrics_service_client.cc
index abe4d42315..c5597ff2c0 100644
--- a/components/metrics/test/test_metrics_service_client.cc
+++ b/components/metrics/test/test_metrics_service_client.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "components/metrics/metrics_log_uploader.h"
 #include "components/regional_capabilities/regional_capabilities_country_id.h"
@@ -36,7 +37,7 @@ void TestMetricsServiceClient::SetMetricsClientId(
 }
 
 bool TestMetricsServiceClient::ShouldUploadMetricsForUserId(uint64_t user_id) {
-  return allowed_user_ids_.contains(user_id);
+  return base::Contains(allowed_user_ids_, user_id);
 }
 
 int32_t TestMetricsServiceClient::GetProduct() {
diff --git a/components/network_session_configurator/browser/network_session_configurator.cc b/components/network_session_configurator/browser/network_session_configurator.cc
index d8d017c78c..cf00378dac 100644
--- a/components/network_session_configurator/browser/network_session_configurator.cc
+++ b/components/network_session_configurator/browser/network_session_configurator.cc
@@ -4,7 +4,6 @@
 
 #include "components/network_session_configurator/browser/network_session_configurator.h"
 
-#include <algorithm>
 #include <limits>
 #include <map>
 #include <string_view>
@@ -13,6 +12,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial.h"
 #include "base/metrics/field_trial_params.h"
@@ -541,7 +541,7 @@ quic::ParsedQuicVersionVector GetQuicVersions(
   quic::ParsedQuicVersionVector obsolete_versions = net::ObsoleteQuicVersions();
   bool found_obsolete_version = false;
   for (const quic::ParsedQuicVersion& version : trial_versions) {
-    if (!std::ranges::contains(obsolete_versions, version)) {
+    if (!base::Contains(obsolete_versions, version)) {
       filtered_versions.push_back(version);
     } else {
       found_obsolete_version = true;
diff --git a/components/no_state_prefetch/browser/no_state_prefetch_contents.cc b/components/no_state_prefetch/browser/no_state_prefetch_contents.cc
index a7317c88c4..ecbcc8aaea 100644
--- a/components/no_state_prefetch/browser/no_state_prefetch_contents.cc
+++ b/components/no_state_prefetch/browser/no_state_prefetch_contents.cc
@@ -6,11 +6,11 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <functional>
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/raw_ptr.h"
@@ -414,7 +414,7 @@ bool NoStatePrefetchContents::Matches(
       session_storage_namespace_id_ != session_storage_namespace->id()) {
     return false;
   }
-  return std::ranges::contains(alias_urls_, url);
+  return base::Contains(alias_urls_, url);
 }
 
 void NoStatePrefetchContents::PrimaryMainFrameRenderProcessGone(
diff --git a/components/no_state_prefetch/browser/no_state_prefetch_manager.cc b/components/no_state_prefetch/browser/no_state_prefetch_manager.cc
index d15445ec14..0db82a4776 100644
--- a/components/no_state_prefetch/browser/no_state_prefetch_manager.cc
+++ b/components/no_state_prefetch/browser/no_state_prefetch_manager.cc
@@ -16,6 +16,7 @@
 #include "base/check_op.h"
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -982,7 +983,7 @@ bool NoStatePrefetchManager::MayReuseProcessHost(
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   // Isolate prefetch processes to make the resource monitoring check more
   // accurate.
-  return !prerender_process_hosts_.contains(process_host);
+  return !base::Contains(prerender_process_hosts_, process_host);
 }
 
 void NoStatePrefetchManager::RenderProcessHostDestroyed(
diff --git a/components/offline_pages/core/page_criteria.cc b/components/offline_pages/core/page_criteria.cc
index 108aa493cb..4b440c5b3a 100644
--- a/components/offline_pages/core/page_criteria.cc
+++ b/components/offline_pages/core/page_criteria.cc
@@ -4,8 +4,7 @@
 
 #include "components/offline_pages/core/page_criteria.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "components/offline_pages/core/offline_page_client_policy.h"
 #include "components/offline_pages/core/offline_page_item.h"
@@ -20,12 +19,12 @@ PageCriteria::PageCriteria(PageCriteria&&) = default;
 
 bool MeetsCriteria(const PageCriteria& criteria, const ClientId& client_id) {
   if (criteria.client_ids &&
-      !std::ranges::contains(criteria.client_ids.value(), client_id)) {
+      !base::Contains(criteria.client_ids.value(), client_id)) {
     return false;
   }
   if (criteria.client_namespaces &&
-      !std::ranges::contains(criteria.client_namespaces.value(),
-                             client_id.name_space)) {
+      !base::Contains(criteria.client_namespaces.value(),
+                      client_id.name_space)) {
     return false;
   }
   if (!criteria.guid.empty() && client_id.id != criteria.guid) {
@@ -84,7 +83,7 @@ bool MeetsCriteria(const PageCriteria& criteria, const OfflinePageItem& item) {
   }
 
   if (criteria.offline_ids &&
-      !std::ranges::contains(criteria.offline_ids.value(), item.offline_id)) {
+      !base::Contains(criteria.offline_ids.value(), item.offline_id)) {
     return false;
   }
 
diff --git a/components/omnibox/browser/autocomplete_grouper_groups.cc b/components/omnibox/browser/autocomplete_grouper_groups.cc
index 0924752ca2..f0b167a06e 100644
--- a/components/omnibox/browser/autocomplete_grouper_groups.cc
+++ b/components/omnibox/browser/autocomplete_grouper_groups.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "components/omnibox/browser/autocomplete_match.h"
 #include "third_party/omnibox_proto/groups.pb.h"
@@ -75,7 +76,7 @@ void Group::GroupMatchesByGroupId() {
   size_t position = 0;
   for (const auto& match : matches_) {
     auto group_id = match->suggestion_group_id.value();
-    if (!group_id_position.contains(group_id)) {
+    if (!base::Contains(group_id_position, group_id)) {
       group_id_position[group_id] = position++;
     }
   }
diff --git a/components/omnibox/browser/autocomplete_grouper_sections.cc b/components/omnibox/browser/autocomplete_grouper_sections.cc
index 0200eb35b5..652023930e 100644
--- a/components/omnibox/browser/autocomplete_grouper_sections.cc
+++ b/components/omnibox/browser/autocomplete_grouper_sections.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/notreached.h"
 #include "base/strings/string_number_conversions.h"
diff --git a/components/omnibox/browser/autocomplete_result.cc b/components/omnibox/browser/autocomplete_result.cc
index 1a6968997e..86f7d6f286 100644
--- a/components/omnibox/browser/autocomplete_result.cc
+++ b/components/omnibox/browser/autocomplete_result.cc
@@ -19,6 +19,7 @@
 #include "base/check_op.h"
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/not_fatal_until.h"
@@ -823,7 +824,7 @@ void AutocompleteResult::GroupAndDemoteMatchesInGroups() {
     }
 
     const omnibox::GroupId group_id = match.suggestion_group_id.value();
-    if (!suggestion_groups_map().contains(group_id)) {
+    if (!base::Contains(suggestion_groups_map(), group_id)) {
       // Strip group IDs from the matches for which there is no suggestion
       // group information. These matches should instead be treated as
       // ordinary matches with no group IDs.
diff --git a/components/omnibox/browser/enterprise_search_aggregator_provider.cc b/components/omnibox/browser/enterprise_search_aggregator_provider.cc
index 929369b140..90b2512e8b 100644
--- a/components/omnibox/browser/enterprise_search_aggregator_provider.cc
+++ b/components/omnibox/browser/enterprise_search_aggregator_provider.cc
@@ -18,6 +18,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/i18n/case_conversion.h"
 #include "base/json/json_reader.h"
@@ -496,8 +497,7 @@ EnterpriseSearchAggregatorProvider::Request::Request(Request&&) = default;
 bool EnterpriseSearchAggregatorProvider::Request::Allowed(
     bool in_keyword_mode) const {
   // Query requests are only allowed in keyword mode.
-  return !std::ranges::contains(types_, SuggestionType::QUERY) ||
-         in_keyword_mode;
+  return !base::Contains(types_, SuggestionType::QUERY) || in_keyword_mode;
 }
 
 void EnterpriseSearchAggregatorProvider::Request::Reset(
@@ -896,17 +896,17 @@ EnterpriseSearchAggregatorProvider::
   const base::ListValue* contentResults =
       root_val.FindList("contentSuggestions");
   RequestParsed parsed{};
-  if (std::ranges::contains(suggestion_types, SuggestionType::QUERY)) {
+  if (base::Contains(suggestion_types, SuggestionType::QUERY)) {
     parsed.Append(ParseResultList(input_words, queryResults,
                                   /*suggestion_type=*/SuggestionType::QUERY,
                                   /*is_navigation=*/false));
   }
-  if (std::ranges::contains(suggestion_types, SuggestionType::PEOPLE)) {
+  if (base::Contains(suggestion_types, SuggestionType::PEOPLE)) {
     parsed.Append(ParseResultList(input_words, peopleResults,
                                   /*suggestion_type=*/SuggestionType::PEOPLE,
                                   /*is_navigation=*/true));
   }
-  if (std::ranges::contains(suggestion_types, SuggestionType::CONTENT)) {
+  if (base::Contains(suggestion_types, SuggestionType::CONTENT)) {
     parsed.Append(ParseResultList(input_words, contentResults,
                                   /*suggestion_type=*/SuggestionType::CONTENT,
                                   /*is_navigation=*/true));
@@ -957,7 +957,7 @@ EnterpriseSearchAggregatorProvider::ParseResultList(
         // Check for existing size parameters (e.g., -s128, =w256, -h64).
         RE2 size_regex("=(?:[swh]\\d+|[^=]*?-[swh]\\d+)");
         if (!RE2::PartialMatch(image_url, size_regex)) {
-          image_url += image_url.contains("=") ? "-s64" : "=s64";
+          image_url += base::Contains(image_url, "=") ? "-s64" : "=s64";
         }
       }
       icon_url = template_url_->favicon_url().spec();
diff --git a/components/omnibox/browser/history_fuzzy_provider.cc b/components/omnibox/browser/history_fuzzy_provider.cc
index 94842c5e30..1071d82010 100644
--- a/components/omnibox/browser/history_fuzzy_provider.cc
+++ b/components/omnibox/browser/history_fuzzy_provider.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/metrics/histogram_functions.h"
diff --git a/components/omnibox/browser/keyword_provider.cc b/components/omnibox/browser/keyword_provider.cc
index 1d16c42b9e..8c289d2979 100644
--- a/components/omnibox/browser/keyword_provider.cc
+++ b/components/omnibox/browser/keyword_provider.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/strings/escape.h"
 #include "base/strings/string_util.h"
diff --git a/components/omnibox/browser/on_device_head_provider.cc b/components/omnibox/browser/on_device_head_provider.cc
index 45e2a59314..0715bce7af 100644
--- a/components/omnibox/browser/on_device_head_provider.cc
+++ b/components/omnibox/browser/on_device_head_provider.cc
@@ -6,6 +6,7 @@
 
 #include <limits>
 
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/i18n/case_conversion.h"
 #include "base/memory/ptr_util.h"
@@ -368,7 +369,7 @@ bool OnDeviceHeadProvider::ShouldFetchTailSuggestions(
     // Determines if the prefix contains multiple words by checking if it has
     // whitespaces; Note this does not work when the prefix is not using
     // whitespace as delimiter, e.g. CJK languages.
-    bool is_single_word_prefix = !sanitized_input.contains(" ");
+    bool is_single_word_prefix = !base::Contains(sanitized_input, " ");
     if (is_single_word_prefix) {
       return false;
     }
diff --git a/components/omnibox/browser/on_device_tail_model_executor.cc b/components/omnibox/browser/on_device_tail_model_executor.cc
index 29e27c26ba..669a180a6b 100644
--- a/components/omnibox/browser/on_device_tail_model_executor.cc
+++ b/components/omnibox/browser/on_device_tail_model_executor.cc
@@ -11,6 +11,7 @@
 
 #include "base/base64.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/hash/hash.h"
 #include "base/logging.h"
@@ -227,11 +228,12 @@ bool OnDeviceTailModelExecutor::Init(
     if (!file_path.empty()) {
       std::string file_path_str =
           optimization_guide::FilePathToString(file_path);
-      if (file_path_str.contains(kVocabFileNameKeyword)) {
+      if (base::Contains(file_path_str, kVocabFileNameKeyword)) {
         vocab_filepath = file_path;
-      } else if (file_path_str.contains(kBadwordHashesFileNameKeyword)) {
+      } else if (base::Contains(file_path_str, kBadwordHashesFileNameKeyword)) {
         badword_hashes_filepath = file_path;
-      } else if (file_path_str.contains(kBadSubstringDenyListFileNameKeyword)) {
+      } else if (base::Contains(file_path_str,
+                                kBadSubstringDenyListFileNameKeyword)) {
         bad_substrings_filepath = file_path;
       }
     }
@@ -424,7 +426,7 @@ bool OnDeviceTailModelExecutor::IsSuggestionBad(const std::string& suggestion) {
   }
 
   for (const std::string& substring : bad_substrings_) {
-    if (suggestion.contains(substring)) {
+    if (base::Contains(suggestion, substring)) {
       return true;
     }
   }
@@ -436,7 +438,7 @@ bool OnDeviceTailModelExecutor::IsSuggestionBad(const std::string& suggestion) {
 
     for (const std::string& word : words) {
       auto hash_value = base::PersistentHash(word);
-      if (badword_hashes_.contains(hash_value)) {
+      if (base::Contains(badword_hashes_, hash_value)) {
         return true;
       }
     }
diff --git a/components/omnibox/browser/search_suggestion_parser.cc b/components/omnibox/browser/search_suggestion_parser.cc
index e436e6e1b5..51a07e6bbc 100644
--- a/components/omnibox/browser/search_suggestion_parser.cc
+++ b/components/omnibox/browser/search_suggestion_parser.cc
@@ -14,6 +14,7 @@
 
 #include "base/base64.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/feature_list.h"
 #include "base/i18n/icu_string_conversions.h"
@@ -192,7 +193,7 @@ constexpr auto kReservedReservedGroupSectionsMap =
 // section known to Chrome.
 omnibox::GroupSection ChromeGroupSectionForRemoteGroupIndex(
     const int group_index) {
-  if (kReservedReservedGroupSectionsMap.contains(group_index)) {
+  if (base::Contains(kReservedReservedGroupSectionsMap, group_index)) {
     return kReservedReservedGroupSectionsMap.at(group_index);
   } else {
     // Return a default section if we don't have any reserved sections left.
@@ -1060,7 +1061,7 @@ bool SearchSuggestionParser::ParseSuggestResults(
     }
 
     // There is nothing to do if the group config has been added before.
-    if (results->suggestion_groups_map.contains(suggestion_group_id)) {
+    if (base::Contains(results->suggestion_groups_map, suggestion_group_id)) {
       return true;
     }
 
@@ -1083,7 +1084,7 @@ bool SearchSuggestionParser::ParseSuggestResults(
     // Add the group config associated with the suggestion, if the suggestion
     // has a valid group ID and a corresponding group config is found in the
     // response.
-    if (!groups_info.group_configs().contains(suggestion_group_id) ||
+    if (!base::Contains(groups_info.group_configs(), suggestion_group_id) ||
         !add_group_config(suggestion_group_id, groups_info.group_configs().at(
                                                    suggestion_group_id))) {
       continue;
diff --git a/components/on_device_translation/translation_manager_util.cc b/components/on_device_translation/translation_manager_util.cc
index 5795374680..87f9eb2d2c 100644
--- a/components/on_device_translation/translation_manager_util.cc
+++ b/components/on_device_translation/translation_manager_util.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/rand_util.h"
 #include "base/strings/string_split.h"
 #include "components/language/core/browser/pref_names.h"
@@ -34,7 +35,7 @@ const std::vector<std::string_view> GetAcceptLanguages(
 
 bool IsInAcceptLanguage(const std::vector<std::string_view>& accept_languages,
                         const std::string_view lang) {
-  return std::ranges::contains(accept_languages, l10n_util::GetLanguage(lang));
+  return base::Contains(accept_languages, l10n_util::GetLanguage(lang));
 }
 
 bool IsTranslatorAllowed(content::BrowserContext* browser_context) {
diff --git a/components/optimization_guide/content/browser/page_content_metadata_observer.cc b/components/optimization_guide/content/browser/page_content_metadata_observer.cc
index b4fa7c7ef3..485818d91c 100644
--- a/components/optimization_guide/content/browser/page_content_metadata_observer.cc
+++ b/components/optimization_guide/content/browser/page_content_metadata_observer.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "components/optimization_guide/content/browser/media_transcript_provider.h"
 #include "components/optimization_guide/content/browser/page_content_proto_util.h"
 #include "content/public/browser/navigation_handle.h"
@@ -104,7 +105,7 @@ void PageContentMetadataObserver::RenderFrameCreated(
 
   if (base::FeatureList::IsEnabled(
           media::kMediaTrasncriptsFlagInPageMetadata)) {
-    if (std::ranges::contains(names_, kHasMediaTranscripts)) {
+    if (base::Contains(names_, kHasMediaTranscripts)) {
       MediaTranscriptObserver::GetOrCreateForCurrentDocument(render_frame_host)
           ->AddOwner(weak_ptr_factory_.GetWeakPtr());
     }
diff --git a/components/optimization_guide/core/delivery/model_info.cc b/components/optimization_guide/core/delivery/model_info.cc
index 1fd35201ad..44f808bc6a 100644
--- a/components/optimization_guide/core/delivery/model_info.cc
+++ b/components/optimization_guide/core/delivery/model_info.cc
@@ -4,6 +4,7 @@
 
 #include "components/optimization_guide/core/delivery/model_info.h"
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/notreached.h"
 #include "base/strings/utf_string_conversions.h"
diff --git a/components/optimization_guide/core/delivery/model_provider_registry.cc b/components/optimization_guide/core/delivery/model_provider_registry.cc
index 5412e1f128..7add0c1120 100644
--- a/components/optimization_guide/core/delivery/model_provider_registry.cc
+++ b/components/optimization_guide/core/delivery/model_provider_registry.cc
@@ -4,6 +4,7 @@
 
 #include "components/optimization_guide/core/delivery/model_provider_registry.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/task/single_thread_task_runner.h"
 #include "components/optimization_guide/core/delivery/model_util.h"
@@ -48,7 +49,7 @@ bool IsModelMetadataTypeOnServerAllowlist(const proto::Any& model_metadata) {
       "google.privacy.webpermissionpredictions.aiv3.v1."
       "PermissionsAiv3ModelMetadata"};
 
-  return kAllowList->contains(model_metadata.type_url());
+  return base::Contains(*kAllowList, model_metadata.type_url());
 }
 
 }  // namespace
diff --git a/components/optimization_guide/core/delivery/prediction_manager.cc b/components/optimization_guide/core/delivery/prediction_manager.cc
index d8fdecdea6..4d9ff24e0e 100644
--- a/components/optimization_guide/core/delivery/prediction_manager.cc
+++ b/components/optimization_guide/core/delivery/prediction_manager.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/flat_tree.h"
diff --git a/components/optimization_guide/core/model_execution/model_execution_features.cc b/components/optimization_guide/core/model_execution/model_execution_features.cc
index 09446e40ed..234f680728 100644
--- a/components/optimization_guide/core/model_execution/model_execution_features.cc
+++ b/components/optimization_guide/core/model_execution/model_execution_features.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/notreached.h"
diff --git a/components/optimization_guide/core/model_execution/model_execution_features_controller.cc b/components/optimization_guide/core/model_execution/model_execution_features_controller.cc
index 0c0c039fd4..1b82a44ca8 100644
--- a/components/optimization_guide/core/model_execution/model_execution_features_controller.cc
+++ b/components/optimization_guide/core/model_execution/model_execution_features_controller.cc
@@ -5,6 +5,7 @@
 #include "components/optimization_guide/core/model_execution/model_execution_features_controller.h"
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
@@ -194,7 +195,8 @@ ModelExecutionFeaturesController::GetCurrentUserValidityResult(
     UserVisibleFeatureKey feature,
     bool skip_enterprise_check) const {
   DCHECK_CALLED_ON_VALID_THREAD(thread_checker_);
-  bool require_account = !features_allowed_for_unsigned_user_.contains(feature);
+  bool require_account =
+      !base::Contains(features_allowed_for_unsigned_user_, feature);
 
   if (require_account) {
     UserValidityResult signin_checks_result = PerformSigninChecks();
diff --git a/components/optimization_guide/core/model_execution/on_device_model_adaptation_loader.cc b/components/optimization_guide/core/model_execution/on_device_model_adaptation_loader.cc
index 0957e06e4a..462fbce155 100644
--- a/components/optimization_guide/core/model_execution/on_device_model_adaptation_loader.cc
+++ b/components/optimization_guide/core/model_execution/on_device_model_adaptation_loader.cc
@@ -94,9 +94,8 @@ bool ArePerformanceHintsCompatible(
     return true;
   }
   // Check if the adaptation model supports any of the base model's hints.
-  return std::ranges::contains(
-      adaptation_metadata.supported_performance_hints(),
-      base_spec.selected_performance_hint);
+  return base::Contains(adaptation_metadata.supported_performance_hints(),
+                        base_spec.selected_performance_hint);
 }
 
 std::optional<OnDeviceModelAdaptationAvailability>
diff --git a/components/optimization_guide/core/model_execution/on_device_model_component.cc b/components/optimization_guide/core/model_execution/on_device_model_component.cc
index 6fe6f7ceb7..54dee0848a 100644
--- a/components/optimization_guide/core/model_execution/on_device_model_component.cc
+++ b/components/optimization_guide/core/model_execution/on_device_model_component.cc
@@ -5,7 +5,6 @@
 #include "components/optimization_guide/core/model_execution/on_device_model_component.h"
 
 #include <optional>
-#include <utility>
 
 #include "base/check.h"
 #include "base/files/file_path.h"
@@ -21,6 +20,7 @@
 #include "base/strings/strcat.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected.h"
 #include "components/optimization_guide/core/delivery/model_util.h"
 #include "components/optimization_guide/core/model_execution/model_execution_prefs.h"
@@ -112,7 +112,7 @@ GetBestPerformanceHintForDevice(
     }
   }
   for (auto hint : prioritized_hints) {
-    if (supported_hints.contains(std::to_underlying(hint))) {
+    if (supported_hints.contains(base::to_underlying(hint))) {
       return hint;
     }
   }
diff --git a/components/optimization_guide/core/model_execution/on_device_model_metadata.cc b/components/optimization_guide/core/model_execution/on_device_model_metadata.cc
index 845cb8330f..79d06ec14e 100644
--- a/components/optimization_guide/core/model_execution/on_device_model_metadata.cc
+++ b/components/optimization_guide/core/model_execution/on_device_model_metadata.cc
@@ -7,6 +7,7 @@
 #include <cstddef>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/ptr_util.h"
 #include "base/memory/scoped_refptr.h"
diff --git a/components/optimization_guide/core/model_execution/on_device_model_service_controller.cc b/components/optimization_guide/core/model_execution/on_device_model_service_controller.cc
index 05eeb4f46f..439e581cf9 100644
--- a/components/optimization_guide/core/model_execution/on_device_model_service_controller.cc
+++ b/components/optimization_guide/core/model_execution/on_device_model_service_controller.cc
@@ -4,12 +4,12 @@
 
 #include "components/optimization_guide/core/model_execution/on_device_model_service_controller.h"
 
-#include <algorithm>
 #include <cstddef>
 #include <cstdint>
 #include <memory>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -422,7 +422,7 @@ void OnDeviceModelServiceController::BaseModelController::RequireAdaptationRank(
     // Older configs may not specify rank, and should be covered by defaults.
     return;
   }
-  if (std::ranges::contains(supported_adaptation_ranks_, required_rank)) {
+  if (base::Contains(supported_adaptation_ranks_, required_rank)) {
     return;
   }
   // Add the rank and reset all remotes to force a reload.
diff --git a/components/optimization_guide/core/model_execution/performance_class.cc b/components/optimization_guide/core/model_execution/performance_class.cc
index 405980b01a..edf48a27b7 100644
--- a/components/optimization_guide/core/model_execution/performance_class.cc
+++ b/components/optimization_guide/core/model_execution/performance_class.cc
@@ -5,14 +5,15 @@
 #include "components/optimization_guide/core/model_execution/performance_class.h"
 
 #include <algorithm>
-#include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_split.h"
 #include "base/strings/to_string.h"
 #include "base/trace_event/trace_event.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/version_info/version_info.h"
 #include "components/optimization_guide/core/model_execution/model_execution_prefs.h"
 #include "components/optimization_guide/core/optimization_guide_enums.h"
@@ -147,8 +148,8 @@ bool IsPerformanceClassCompatible(
   std::vector<std::string_view> perf_classes_list = base::SplitStringPiece(
       perf_classes_string, ",", base::WhitespaceHandling::TRIM_WHITESPACE,
       base::SplitResult::SPLIT_WANT_NONEMPTY);
-  return std::ranges::contains(
-      perf_classes_list, base::ToString(static_cast<int>(performance_class)));
+  return base::Contains(perf_classes_list,
+                        base::ToString(static_cast<int>(performance_class)));
 }
 
 OnDeviceModelPerformanceClass PerformanceClassFromPref(
@@ -168,7 +169,7 @@ void UpdatePerformanceClassPref(
   // TODO(crbug.com/437807121): Check performance info before setting prefs.
   local_state->SetInteger(
       model_execution::prefs::localstate::kOnDevicePerformanceClass,
-      std::to_underlying(performance_class));
+      base::to_underlying(performance_class));
   local_state->SetString(
       model_execution::prefs::localstate::kOnDevicePerformanceClassVersion,
       version_info::GetVersionNumber());
diff --git a/components/optimization_guide/core/model_execution/safety_config.cc b/components/optimization_guide/core/model_execution/safety_config.cc
index 6c17985b4c..0eb2405cee 100644
--- a/components/optimization_guide/core/model_execution/safety_config.cc
+++ b/components/optimization_guide/core/model_execution/safety_config.cc
@@ -4,13 +4,13 @@
 
 #include "components/optimization_guide/core/model_execution/safety_config.h"
 
-#include <algorithm>
 #include <cstddef>
 #include <iterator>
 #include <optional>
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "components/optimization_guide/core/model_execution/multimodal_message.h"
 #include "components/optimization_guide/core/model_execution/substitution.h"
@@ -156,7 +156,7 @@ bool SafetyConfig::IsTextInUnsupportedOrUndeterminedLanguage(
     return true;
   }
 
-  if (!std::ranges::contains(allowed_languages, safety_info->language->code)) {
+  if (!base::Contains(allowed_languages, safety_info->language->code)) {
     // Unsupported language.
     return true;
   }
diff --git a/components/optimization_guide/core/model_execution/session_impl.cc b/components/optimization_guide/core/model_execution/session_impl.cc
index 8302706a4d..75aa7f35ee 100644
--- a/components/optimization_guide/core/model_execution/session_impl.cc
+++ b/components/optimization_guide/core/model_execution/session_impl.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/metrics/histogram_functions.h"
diff --git a/components/optimization_guide/core/optimization_guide_features.cc b/components/optimization_guide/core/optimization_guide_features.cc
index 356ccbe874..189018c28c 100644
--- a/components/optimization_guide/core/optimization_guide_features.cc
+++ b/components/optimization_guide/core/optimization_guide_features.cc
@@ -10,6 +10,7 @@
 
 #include "base/byte_count.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
@@ -167,7 +168,7 @@ bool IsOnDeviceModelBackgroundDownloadEnabledForFeature(
   std::vector<std::string_view> allowed_features =
       base::SplitStringPiece(allowed_features_string, ",",
                              base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-  return std::ranges::contains(allowed_features, GetVariantName(feature));
+  return base::Contains(allowed_features, GetVariantName(feature));
 }
 
 BASE_FEATURE(kOptimizationGuideIconView, base::FEATURE_DISABLED_BY_DEFAULT);
diff --git a/components/origin_trials/browser/origin_trials.cc b/components/origin_trials/browser/origin_trials.cc
index e09d0c391f..9b6685e7f3 100644
--- a/components/origin_trials/browser/origin_trials.cc
+++ b/components/origin_trials/browser/origin_trials.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "components/origin_trials/common/persisted_trial_token.h"
@@ -210,7 +211,7 @@ base::flat_set<std::string> OriginTrials::GetPersistedTrialsForOriginWithMatch(
       if (trial_feature_match &&
           // TODO(crbug.com/40189223): FeaturesEnabledByTrial should be part of
           // general validation logic.
-          !std::ranges::contains(
+          !base::Contains(
               trial_token_validator_->FeaturesEnabledByTrial(token.trial_name),
               trial_feature_match.value())) {
         continue;
diff --git a/components/os_crypt/async/browser/test_secret_portal.cc b/components/os_crypt/async/browser/test_secret_portal.cc
index 738a0ee13e..67b39423c9 100644
--- a/components/os_crypt/async/browser/test_secret_portal.cc
+++ b/components/os_crypt/async/browser/test_secret_portal.cc
@@ -7,6 +7,7 @@
 #include <map>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/nix/xdg_util.h"
 #include "components/dbus/utils/read_value.h"
@@ -65,7 +66,7 @@ void TestSecretPortal::RetrieveSecret(
       method_call->GetSender(), *handle_token));
   auto* exported_response = bus_->GetExportedObject(response_path);
 
-  EXPECT_TRUE(!pre_test_ || !options->contains("token"));
+  EXPECT_TRUE(!pre_test_ || !base::Contains(*options, "token"));
 
   auto response = dbus::Response::FromMethodCall(method_call);
   dbus::MessageWriter writer(response.get());
diff --git a/components/page_content_annotations/core/page_content_annotations_features.cc b/components/page_content_annotations/core/page_content_annotations_features.cc
index e54b1b884e..fc78b4a64a 100644
--- a/components/page_content_annotations/core/page_content_annotations_features.cc
+++ b/components/page_content_annotations/core/page_content_annotations_features.cc
@@ -4,8 +4,7 @@
 
 #include "components/page_content_annotations/core/page_content_annotations_features.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/strings/string_split.h"
@@ -69,8 +68,8 @@ bool IsSupportedLocale(const std::string& locale,
 
   // Otherwise, the locale or the primary language subtag must match an element
   // of the allowlist.
-  return std::ranges::contains(supported, locale) ||
-         std::ranges::contains(supported, l10n_util::GetLanguage(locale));
+  return base::Contains(supported, locale) ||
+         base::Contains(supported, l10n_util::GetLanguage(locale));
 }
 
 bool IsSupportedCountry(const std::string& country_code,
diff --git a/components/page_content_annotations/core/page_content_annotations_service.cc b/components/page_content_annotations/core/page_content_annotations_service.cc
index c5a75e41c9..ed3b5baf1e 100644
--- a/components/page_content_annotations/core/page_content_annotations_service.cc
+++ b/components/page_content_annotations/core/page_content_annotations_service.cc
@@ -10,6 +10,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/i18n/case_conversion.h"
 #include "base/metrics/histogram_functions.h"
@@ -555,8 +556,8 @@ void PageContentAnnotationsService::OnZeroSuggestResponseUpdated(
   std::vector<std::string> related_searches;
   for (const auto& result : suggest_results) {
     // Suggestions with HIVEMIND subtype are considered "related searches".
-    if (std::ranges::contains(result.subtypes,
-                              omnibox::SuggestSubtype::SUBTYPE_HIVEMIND)) {
+    if (base::Contains(result.subtypes,
+                       omnibox::SuggestSubtype::SUBTYPE_HIVEMIND)) {
       related_searches.push_back(
           base::UTF16ToUTF8(base::CollapseWhitespace(result.suggestion, true)));
     }
diff --git a/components/page_content_annotations/core/page_visibility_model_handler.cc b/components/page_content_annotations/core/page_visibility_model_handler.cc
index a0315c11eb..707027e6ae 100644
--- a/components/page_content_annotations/core/page_visibility_model_handler.cc
+++ b/components/page_content_annotations/core/page_visibility_model_handler.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/task/sequenced_task_runner.h"
 #include "components/optimization_guide/core/delivery/optimization_guide_model_provider.h"
 #include "components/optimization_guide/core/optimization_guide_features.h"
diff --git a/components/page_info/android/page_info_controller_android.cc b/components/page_info/android/page_info_controller_android.cc
index ed66a66885..6520002eb7 100644
--- a/components/page_info/android/page_info_controller_android.cc
+++ b/components/page_info/android/page_info_controller_android.cc
@@ -4,13 +4,13 @@
 
 #include "components/page_info/android/page_info_controller_android.h"
 
-#include <algorithm>
 #include <string>
 
 #include "base/android/jni_android.h"
 #include "base/android/jni_array.h"
 #include "base/android/jni_string.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/notimplemented.h"
 #include "components/content_settings/core/browser/host_content_settings_map.h"
@@ -220,7 +220,7 @@ void PageInfoControllerAndroid::SetPermissionInfo(
   bool requested_notifications = false;
 
   for (const auto& permission : permission_info_list) {
-    if (std::ranges::contains(permissions_to_display, permission.type)) {
+    if (base::Contains(permissions_to_display, permission.type)) {
       std::optional<PermissionSetting> setting_to_display =
           GetSettingToDisplay(permission);
       if (setting_to_display) {
diff --git a/components/page_info/core/features.cc b/components/page_info/core/features.cc
index 2e67fd7709..293603645c 100644
--- a/components/page_info/core/features.cc
+++ b/components/page_info/core/features.cc
@@ -6,6 +6,7 @@
 
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
@@ -22,7 +23,7 @@ constexpr auto kDefaultLangs = base::MakeFixedFlatSet<std::string_view>({
 });
 
 extern bool IsAboutThisSiteFeatureEnabled(const std::string& locale) {
-  if (kDefaultLangs.contains(l10n_util::GetLanguage(locale))) {
+  if (base::Contains(kDefaultLangs, l10n_util::GetLanguage(locale))) {
     return base::FeatureList::IsEnabled(kPageInfoAboutThisSite);
   }
   return base::FeatureList::IsEnabled(kPageInfoAboutThisSiteMoreLangs);
diff --git a/components/paint_preview/browser/paint_preview_client.cc b/components/paint_preview/browser/paint_preview_client.cc
index e495171c3f..6219a64d6b 100644
--- a/components/paint_preview/browser/paint_preview_client.cc
+++ b/components/paint_preview/browser/paint_preview_client.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -331,17 +332,17 @@ PaintPreviewClient::InProgressDocumentCaptureState::
 
 bool PaintPreviewClient::InProgressDocumentCaptureState::IsAllowedToCapture(
     const base::UnguessableToken& frame_token) const {
-  return accepted_tokens.contains(frame_token);
+  return base::Contains(accepted_tokens, frame_token);
 }
 
 bool PaintPreviewClient::InProgressDocumentCaptureState::IsFinishedCapturing(
     const base::UnguessableToken& frame_token) const {
-  return finished_subframes.contains(frame_token);
+  return base::Contains(finished_subframes, frame_token);
 }
 
 bool PaintPreviewClient::InProgressDocumentCaptureState::IsCaptureInProgress(
     const base::UnguessableToken& frame_token) const {
-  return awaiting_subframes.contains(frame_token);
+  return base::Contains(awaiting_subframes, frame_token);
 }
 
 base::FilePath
diff --git a/components/paint_preview/common/paint_preview_tracker.cc b/components/paint_preview/common/paint_preview_tracker.cc
index 2dd4cd0fa6..d9e5dc3dd8 100644
--- a/components/paint_preview/common/paint_preview_tracker.cc
+++ b/components/paint_preview/common/paint_preview_tracker.cc
@@ -11,6 +11,7 @@
 
 #include "base/check.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "components/paint_preview/common/glyph_usage.h"
 #include "components/paint_preview/common/mojom/paint_preview_recorder.mojom.h"
 #include "third_party/skia/include/core/SkCanvas.h"
@@ -97,7 +98,8 @@ uint32_t PaintPreviewTracker::CreateContentForRemoteFrame(
   sk_sp<SkPicture> pic = SkPicture::MakePlaceholder(
       SkRect::MakeXYWH(rect.x(), rect.y(), rect.width(), rect.height()));
   const uint32_t content_id = pic->uniqueID();
-  DCHECK(!picture_context_.content_id_to_embedding_token.contains(content_id));
+  DCHECK(!base::Contains(picture_context_.content_id_to_embedding_token,
+                         content_id));
   picture_context_.content_id_to_embedding_token[content_id] = embedding_token;
   subframe_pics_[content_id] = pic;
   return content_id;
@@ -112,7 +114,7 @@ void PaintPreviewTracker::AddGlyphs(const SkTextBlob* blob) {
     // Fail fast if the number of glyphs is undetermined or 0.
     if (typeface->countGlyphs() <= 0)
       continue;
-    if (!typeface_glyph_usage_.contains(typeface->uniqueID())) {
+    if (!base::Contains(typeface_glyph_usage_, typeface->uniqueID())) {
       if (ShouldUseDenseGlyphUsage(typeface)) {
         typeface_glyph_usage_.insert(
             {typeface->uniqueID(),
diff --git a/components/paint_preview/common/redaction_params.cc b/components/paint_preview/common/redaction_params.cc
index fae241f0bc..d061edd4b7 100644
--- a/components/paint_preview/common/redaction_params.cc
+++ b/components/paint_preview/common/redaction_params.cc
@@ -4,6 +4,7 @@
 
 #include "components/paint_preview/common/redaction_params.h"
 
+#include "base/containers/contains.h"
 #include "net/base/schemeful_site.h"
 
 namespace paint_preview {
@@ -47,13 +48,13 @@ bool RedactionParams::ShouldRedactSubframe(
 
 bool RedactionParams::State::AllowlistContainsOrigin(
     const url::Origin& origin) const {
-  return allowed_origins.contains(origin);
+  return base::Contains(allowed_origins, origin);
 }
 
 bool RedactionParams::State::AllowlistContainsSite(
     const url::Origin& origin) const {
   return !allowed_sites.empty() &&
-         allowed_sites.contains(net::SchemefulSite(origin));
+         base::Contains(allowed_sites, net::SchemefulSite(origin));
 }
 
 }  // namespace paint_preview
diff --git a/components/password_manager/core/browser/fake_form_fetcher.cc b/components/password_manager/core/browser/fake_form_fetcher.cc
index 1a94b58f7f..85e98db68b 100644
--- a/components/password_manager/core/browser/fake_form_fetcher.cc
+++ b/components/password_manager/core/browser/fake_form_fetcher.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "components/password_manager/core/browser/password_form.h"
 #include "components/password_manager/core/browser/password_manager_util.h"
@@ -75,7 +76,7 @@ bool FakeFormFetcher::IsMovingBlocked(const signin::GaiaIdHash& destination,
       if (form.username_value != username) {
         continue;
       }
-      if (std::ranges::contains(form.moving_blocked_for_list, destination)) {
+      if (base::Contains(form.moving_blocked_for_list, destination)) {
         return true;
       }
     }
diff --git a/components/password_manager/core/browser/form_fetcher_impl.cc b/components/password_manager/core/browser/form_fetcher_impl.cc
index 604bbdfc3b..08ee67508e 100644
--- a/components/password_manager/core/browser/form_fetcher_impl.cc
+++ b/components/password_manager/core/browser/form_fetcher_impl.cc
@@ -13,6 +13,7 @@
 
 #include "base/check_deref.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/notimplemented.h"
 #include "base/notreached.h"
@@ -210,7 +211,7 @@ bool FormFetcherImpl::IsMovingBlocked(const signin::GaiaIdHash& destination,
       if (form.username_value != username) {
         continue;
       }
-      if (std::ranges::contains(form.moving_blocked_for_list, destination)) {
+      if (base::Contains(form.moving_blocked_for_list, destination)) {
         return true;
       }
     }
diff --git a/components/password_manager/core/browser/form_parsing/form_data_parser.cc b/components/password_manager/core/browser/form_parsing/form_data_parser.cc
index 416cdf6ba9..549799176f 100644
--- a/components/password_manager/core/browser/form_parsing/form_data_parser.cc
+++ b/components/password_manager/core/browser/form_parsing/form_data_parser.cc
@@ -15,6 +15,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/i18n/case_conversion.h"
 #include "base/memory/raw_ptr.h"
@@ -865,7 +866,7 @@ void ParseUsingBaseHeuristics(
     for (auto it = processed_fields.begin(); it != processed_fields.end();
          ++it) {
       if ((it->is_password || it->is_predicted_as_password) &&
-          std::ranges::contains(password_ids, it->field->renderer_id())) {
+          base::Contains(password_ids, it->field->renderer_id())) {
         first_relevant_password = it;
         break;
       }
diff --git a/components/password_manager/core/browser/http_credentials_cleaner.cc b/components/password_manager/core/browser/http_credentials_cleaner.cc
index cdb96abb36..0a0f0aa0c8 100644
--- a/components/password_manager/core/browser/http_credentials_cleaner.cc
+++ b/components/password_manager/core/browser/http_credentials_cleaner.cc
@@ -4,6 +4,7 @@
 
 #include "components/password_manager/core/browser/http_credentials_cleaner.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "components/password_manager/core/browser/http_password_store_migrator.h"
@@ -95,7 +96,7 @@ void HttpCredentialCleaner::OnHSTSQueryResult(
     return;
   }
 
-  if (user_it->second.contains(form->password_value)) {
+  if (base::Contains(user_it->second, form->password_value)) {
     // The password store contains the same credentials (signon_realm, scheme,
     // username and password) on HTTPS version of the form.
     base::UmaHistogramEnumeration(
diff --git a/components/password_manager/core/browser/password_form_metrics_recorder.cc b/components/password_manager/core/browser/password_form_metrics_recorder.cc
index 0de94554fb..57c57898bd 100644
--- a/components/password_manager/core/browser/password_form_metrics_recorder.cc
+++ b/components/password_manager/core/browser/password_form_metrics_recorder.cc
@@ -11,6 +11,7 @@
 
 #include "base/check_deref.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/metrics/user_metrics.h"
@@ -140,17 +141,21 @@ UsernamePasswordsState CalculateUsernamePasswordsState(
     // or both. In the last case we use the control type of the form as a
     // tie-break, if this is `password`, the user likely typed a password,
     // otherwise a username.
-    bool is_possibly_saved_username_in_profile_store = saved_usernames.contains(
+    bool is_possibly_saved_username_in_profile_store = base::Contains(
+        saved_usernames,
         std::make_pair(value, PasswordForm::Store::kProfileStore));
-    bool is_possibly_saved_username_in_account_store = saved_usernames.contains(
+    bool is_possibly_saved_username_in_account_store = base::Contains(
+        saved_usernames,
         std::make_pair(value, PasswordForm::Store::kAccountStore));
     bool is_possibly_saved_username =
         is_possibly_saved_username_in_profile_store ||
         is_possibly_saved_username_in_account_store;
 
-    bool is_possibly_saved_password_in_profile_store = saved_passwords.contains(
+    bool is_possibly_saved_password_in_profile_store = base::Contains(
+        saved_passwords,
         std::make_pair(value, PasswordForm::Store::kProfileStore));
-    bool is_possibly_saved_password_in_account_store = saved_passwords.contains(
+    bool is_possibly_saved_password_in_account_store = base::Contains(
+        saved_passwords,
         std::make_pair(value, PasswordForm::Store::kAccountStore));
     bool is_possibly_saved_password =
         is_possibly_saved_password_in_profile_store ||
diff --git a/components/password_manager/core/browser/password_manager_util.cc b/components/password_manager/core/browser/password_manager_util.cc
index 5ecad79c22..4c622773c1 100644
--- a/components/password_manager/core/browser/password_manager_util.cc
+++ b/components/password_manager/core/browser/password_manager_util.cc
@@ -11,6 +11,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_functions.h"
@@ -189,9 +190,8 @@ std::vector<PasswordForm> FindBestMatches(base::span<PasswordForm> matches) {
     } else {
       // Insert another credential only if the store is different as well as the
       // password value.
-      if (std::ranges::contains(
-              it->second, match.in_store,
-              [](const auto& form) { return form.in_store; })) {
+      if (base::Contains(it->second, match.in_store,
+                         [](const auto& form) { return form.in_store; })) {
         continue;
       };
       // If 2 credential have the same password and the same username, update
@@ -427,9 +427,9 @@ bool IsUppercaseLetter(char16_t c) {
 bool IsSpecialSymbol(char16_t c) {
   // The static assert is intended to ensure that the underlying type of
   // `kSpecialSymbols` does not become a char. If that happened, the call to
-  // `std::ranges::contains` would lead to (silent) overflow.
+  // `base::Contains` would lead to (silent) overflow.
   static_assert(sizeof(decltype(kSpecialSymbols)::value_type) == sizeof(c));
-  return kSpecialSymbols.contains(c);
+  return base::Contains(kSpecialSymbols, c);
 }
 
 bool IsSingleUsernameType(autofill::FieldType type) {
diff --git a/components/password_manager/core/browser/password_manual_fallback_flow.cc b/components/password_manager/core/browser/password_manual_fallback_flow.cc
index d65be8e82a..6e70c6e968 100644
--- a/components/password_manager/core/browser/password_manual_fallback_flow.cc
+++ b/components/password_manager/core/browser/password_manual_fallback_flow.cc
@@ -10,6 +10,7 @@
 
 #include "base/check.h"
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_functions.h"
@@ -62,9 +63,9 @@ std::optional<password_manager::PasswordForm> GetCorrespondingPasswordForm(
       credential_ui_entries, [&payload](const CredentialUIEntry& ui_entry) {
         return ui_entry.username == payload.username &&
                ui_entry.password == payload.password &&
-               std::ranges::contains(
-                   ui_entry.GetAffiliatedDomains(), payload.signon_realm,
-                   &CredentialUIEntry::DomainInfo::signon_realm);
+               base::Contains(ui_entry.GetAffiliatedDomains(),
+                              payload.signon_realm,
+                              &CredentialUIEntry::DomainInfo::signon_realm);
       });
 
   if (found_credential_it == credential_ui_entries.end()) {
diff --git a/components/password_manager/core/browser/password_reuse_detector_impl.cc b/components/password_manager/core/browser/password_reuse_detector_impl.cc
index 7ee967e822..535769e626 100644
--- a/components/password_manager/core/browser/password_reuse_detector_impl.cc
+++ b/components/password_manager/core/browser/password_reuse_detector_impl.cc
@@ -9,6 +9,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "components/password_manager/core/browser/hash_password_manager.h"
 #include "components/password_manager/core/browser/password_form.h"
 #include "components/password_manager/core/browser/password_hash_data.h"
@@ -278,7 +279,7 @@ PasswordReuseDetectorImpl::CheckSavedPasswordReuseBasedOnHash(
     }
     // If the page's URL matches a saved domain for this password,
     // this isn't password-reuse.
-    if (domains.contains(registry_controlled_domain)) {
+    if (base::Contains(domains, registry_controlled_domain)) {
       continue;
     }
 
diff --git a/components/password_manager/core/browser/password_store/get_logins_with_affiliations_request_handler.cc b/components/password_manager/core/browser/password_store/get_logins_with_affiliations_request_handler.cc
index b2620646e4..2964bdd7c5 100644
--- a/components/password_manager/core/browser/password_store/get_logins_with_affiliations_request_handler.cc
+++ b/components/password_manager/core/browser/password_store/get_logins_with_affiliations_request_handler.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/barrier_callback.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -235,7 +236,7 @@ void GetLoginsHelper::HandleAffiliationsAndGroupsReceived(
     // The PSL forms are requested in the main request, ignore affiliated
     // matches too.
     if (!IsPublicSuffixDomainMatch(realm, requested_digest_.signon_realm) &&
-        !affiliations_.contains(realm)) {
+        !base::Contains(affiliations_, realm)) {
       digests_to_request.emplace_back(requested_digest_.scheme, realm,
                                       GURL(realm));
     }
@@ -274,10 +275,10 @@ LoginsResultOrError GetLoginsHelper::MergeResults(
             !affiliations::IsValidAndroidFacetURI(form.signon_realm)) {
           signon_realm = url::Origin::Create(form.url).GetURL().spec();
         }
-        if (affiliations_.contains(signon_realm)) {
+        if (base::Contains(affiliations_, signon_realm)) {
           form.match_type |= PasswordForm::MatchType::kAffiliated;
         }
-        if (group_.contains(signon_realm)) {
+        if (base::Contains(group_, signon_realm)) {
           form.match_type |= PasswordForm::MatchType::kGrouped;
         }
         break;
diff --git a/components/password_manager/core/browser/ui/saved_passwords_presenter.cc b/components/password_manager/core/browser/ui/saved_passwords_presenter.cc
index 87030e82ac..59dbd6b5c3 100644
--- a/components/password_manager/core/browser/ui/saved_passwords_presenter.cc
+++ b/components/password_manager/core/browser/ui/saved_passwords_presenter.cc
@@ -14,6 +14,7 @@
 #include "base/barrier_callback.h"
 #include "base/barrier_closure.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
diff --git a/components/password_manager/core/browser/votes_uploader.cc b/components/password_manager/core/browser/votes_uploader.cc
index cf0093395c..95d45f5479 100644
--- a/components/password_manager/core/browser/votes_uploader.cc
+++ b/components/password_manager/core/browser/votes_uploader.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/hash/hash.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_macros.h"
diff --git a/components/password_manager/core/common/password_manager_util.cc b/components/password_manager/core/common/password_manager_util.cc
index cd5de9b0ac..d22af9fbed 100644
--- a/components/password_manager/core/common/password_manager_util.cc
+++ b/components/password_manager/core/common/password_manager_util.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "components/autofill/core/common/autofill_regexes.h"
 #include "components/autofill/core/common/form_data.h"
@@ -80,7 +81,8 @@ bool CanValueBeConsideredAsSingleUsername(const std::u16string& value) {
 bool IsLikelyOtp(std::u16string_view name,
                  std::u16string_view id,
                  std::string_view autocomplete) {
-  return autocomplete.contains(
+  return base::Contains(
+             autocomplete,
              password_manager::constants::kAutocompleteOneTimePassword) ||
          autofill::MatchesRegex<password_manager::constants::kOneTimePwdRe>(
              name) ||
diff --git a/components/payments/content/manifest_verifier.cc b/components/payments/content/manifest_verifier.cc
index 046759da70..260d91e28d 100644
--- a/components/payments/content/manifest_verifier.cc
+++ b/components/payments/content/manifest_verifier.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/string_util.h"
@@ -39,7 +40,7 @@ void EnableMethodManifestUrlForSupportedApps(
     std::map<GURL, std::set<GURL>>* prohibited_payment_methods) {
   for (auto app_id : app_ids) {
     auto* app = (*apps)[app_id].get();
-    app->has_explicitly_verified_methods = std::ranges::contains(
+    app->has_explicitly_verified_methods = base::Contains(
         supported_origin_strings,
         url::Origin::Create(app->scope.DeprecatedGetOriginAsURL()).Serialize());
     if (app->has_explicitly_verified_methods) {
diff --git a/components/payments/content/payment_app.cc b/components/payments/content/payment_app.cc
index e1d0d30c84..995df04a9e 100644
--- a/components/payments/content/payment_app.cc
+++ b/components/payments/content/payment_app.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 
 namespace payments {
@@ -67,7 +68,7 @@ std::set<std::string> PaymentApp::GetApplicationIdentifiersThatHideThisApp()
 void PaymentApp::IsValidForPaymentMethodIdentifier(
     const std::string& payment_method_identifier,
     bool* is_valid) const {
-  *is_valid = app_method_names_.contains(payment_method_identifier);
+  *is_valid = base::Contains(app_method_names_, payment_method_identifier);
 }
 
 const std::set<std::string>& PaymentApp::GetAppMethodNames() const {
diff --git a/components/payments/content/payment_request.cc b/components/payments/content/payment_request.cc
index c5786816da..43c646c64e 100644
--- a/components/payments/content/payment_request.cc
+++ b/components/payments/content/payment_request.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
@@ -214,19 +215,18 @@ void PaymentRequest::Init(
   GURL android_pay_url(methods::kAndroidPay);
   GURL google_play_billing_url(methods::kGooglePlayBilling);
   std::vector<JourneyLogger::PaymentMethodCategory> method_categories;
-  if (std::ranges::contains(spec_->url_payment_method_identifiers(),
-                            google_pay_url) ||
-      std::ranges::contains(spec_->url_payment_method_identifiers(),
-                            android_pay_url)) {
+  if (base::Contains(spec_->url_payment_method_identifiers(), google_pay_url) ||
+      base::Contains(spec_->url_payment_method_identifiers(),
+                     android_pay_url)) {
     method_categories.push_back(JourneyLogger::PaymentMethodCategory::kGoogle);
   }
-  if (std::ranges::contains(spec_->url_payment_method_identifiers(),
-                            google_pay_authentication_url)) {
+  if (base::Contains(spec_->url_payment_method_identifiers(),
+                     google_pay_authentication_url)) {
     method_categories.push_back(
         JourneyLogger::PaymentMethodCategory::kGooglePayAuthentication);
   }
-  if (std::ranges::contains(spec_->url_payment_method_identifiers(),
-                            google_play_billing_url)) {
+  if (base::Contains(spec_->url_payment_method_identifiers(),
+                     google_play_billing_url)) {
     method_categories.push_back(
         JourneyLogger::PaymentMethodCategory::kPlayBilling);
   }
diff --git a/components/payments/content/payment_request_state.cc b/components/payments/content/payment_request_state.cc
index 4b0379f084..1a5d923f56 100644
--- a/components/payments/content/payment_request_state.cc
+++ b/components/payments/content/payment_request_state.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -350,8 +351,8 @@ void PaymentRequestState::CheckRequestedMethodsSupported(
 
   if (!are_requested_methods_supported_ &&
       get_all_payment_apps_error_.empty() &&
-      spec_->payment_method_identifiers_set().contains(
-          methods::kGooglePlayBilling) &&
+      base::Contains(spec_->payment_method_identifiers_set(),
+                     methods::kGooglePlayBilling) &&
       !IsInTwa()) {
     get_all_payment_apps_error_ = errors::kAppStoreMethodOnlySupportedInTwa;
   }
diff --git a/components/payments/content/secure_payment_confirmation_app_factory.cc b/components/payments/content/secure_payment_confirmation_app_factory.cc
index 3907070457..c79d954d3e 100644
--- a/components/payments/content/secure_payment_confirmation_app_factory.cc
+++ b/components/payments/content/secure_payment_confirmation_app_factory.cc
@@ -15,6 +15,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/strings/string_util.h"
@@ -289,8 +290,8 @@ void SecurePaymentConfirmationAppFactory::Create(
   DCHECK(delegate);
 
   base::WeakPtr<PaymentRequestSpec> spec = delegate->GetSpec();
-  if (!spec || !spec->payment_method_identifiers_set().contains(
-                   methods::kSecurePaymentConfirmation)) {
+  if (!spec || !base::Contains(spec->payment_method_identifiers_set(),
+                               methods::kSecurePaymentConfirmation)) {
     delegate->OnDoneCreatingPaymentApps();
     return;
   }
diff --git a/components/payments/content/service_worker_payment_app.cc b/components/payments/content/service_worker_payment_app.cc
index 56c18d97a9..edb57cf06f 100644
--- a/components/payments/content/service_worker_payment_app.cc
+++ b/components/payments/content/service_worker_payment_app.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -188,14 +189,14 @@ ServiceWorkerPaymentApp::CreateCanMakePaymentEventData() {
 
   DCHECK(spec_->details().modifiers);
   for (const auto& modifier : *spec_->details().modifiers) {
-    if (supported_url_methods.contains(
-            modifier->method_data->supported_method)) {
+    if (base::Contains(supported_url_methods,
+                       modifier->method_data->supported_method)) {
       event_data->modifiers.emplace_back(modifier.Clone());
     }
   }
 
   for (const auto& data : spec_->method_data()) {
-    if (supported_url_methods.contains(data->supported_method)) {
+    if (base::Contains(supported_url_methods, data->supported_method)) {
       event_data->method_data.push_back(data.Clone());
     }
   }
@@ -299,13 +300,14 @@ ServiceWorkerPaymentApp::CreatePaymentRequestEventData() {
 
   DCHECK(spec_->details().modifiers);
   for (const auto& modifier : *spec_->details().modifiers) {
-    if (GetAppMethodNames().contains(modifier->method_data->supported_method)) {
+    if (base::Contains(GetAppMethodNames(),
+                       modifier->method_data->supported_method)) {
       event_data->modifiers.emplace_back(modifier.Clone());
     }
   }
 
   for (const auto& data : spec_->method_data()) {
-    if (GetAppMethodNames().contains(data->supported_method)) {
+    if (base::Contains(GetAppMethodNames(), data->supported_method)) {
       event_data->method_data.push_back(data.Clone());
     }
   }
diff --git a/components/payments/content/service_worker_payment_app_factory.cc b/components/payments/content/service_worker_payment_app_factory.cc
index e1bebe422c..69b3fed369 100644
--- a/components/payments/content/service_worker_payment_app_factory.cc
+++ b/components/payments/content/service_worker_payment_app_factory.cc
@@ -4,11 +4,11 @@
 
 #include "components/payments/content/service_worker_payment_app_factory.h"
 
-#include <algorithm>
 #include <map>
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
@@ -59,7 +59,7 @@ class ServiceWorkerPaymentAppCreator {
       std::vector<std::string> enabled_methods =
           installed_app.second->enabled_methods;
       bool has_app_store_billing_method =
-          std::ranges::contains(enabled_methods, methods::kGooglePlayBilling);
+          base::Contains(enabled_methods, methods::kGooglePlayBilling);
       if (ShouldSkipAppForPartialDelegation(
               installed_app.second->supported_delegations, delegate_,
               has_app_store_billing_method)) {
diff --git a/components/payments/content/service_worker_payment_app_finder.cc b/components/payments/content/service_worker_payment_app_finder.cc
index dd8f846c32..dda0f437ca 100644
--- a/components/payments/content/service_worker_payment_app_finder.cc
+++ b/components/payments/content/service_worker_payment_app_finder.cc
@@ -9,6 +9,7 @@
 
 #include "base/base64.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/raw_ptr.h"
@@ -436,11 +437,11 @@ void ServiceWorkerPaymentAppFinder::GetAllPaymentApps(
     return;
 
   // Do not look up payment handlers for ignored payment methods.
-  std::erase_if(
-      requested_method_data,
-      [&](const mojom::PaymentMethodDataPtr& method_data) {
-        return ignored_methods_.contains(method_data->supported_method);
-      });
+  std::erase_if(requested_method_data,
+                [&](const mojom::PaymentMethodDataPtr& method_data) {
+                  return base::Contains(ignored_methods_,
+                                        method_data->supported_method);
+                });
   if (requested_method_data.empty()) {
     std::move(callback).Run(
         content::InstalledPaymentAppsFinder::PaymentApps(),
diff --git a/components/payments/core/payment_manifest_downloader.cc b/components/payments/core/payment_manifest_downloader.cc
index d6fa6b518b..9a213601fa 100644
--- a/components/payments/core/payment_manifest_downloader.cc
+++ b/components/payments/core/payment_manifest_downloader.cc
@@ -4,13 +4,13 @@
 
 #include "components/payments/core/payment_manifest_downloader.h"
 
-#include <algorithm>
 #include <optional>
 #include <string_view>
 #include <unordered_map>
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/strings/string_number_conversions.h"
@@ -312,7 +312,7 @@ void PaymentManifestDownloader::OnURLLoaderCompleteInternal(
     std::vector<std::string> rel_parts =
         base::SplitString(rel->second.value_or(""), HTTP_LWS,
                           base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-    if (std::ranges::contains(rel_parts, "payment-method-manifest")) {
+    if (base::Contains(rel_parts, "payment-method-manifest")) {
       GURL payment_method_manifest_url = final_url.Resolve(*link_url);
 
       if (!IsValidManifestUrl(payment_method_manifest_url, *log_,
diff --git a/components/payments/core/payment_request_data_util.cc b/components/payments/core/payment_request_data_util.cc
index 8a58a28abf..d48dc2155d 100644
--- a/components/payments/core/payment_request_data_util.cc
+++ b/components/payments/core/payment_request_data_util.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
@@ -99,7 +100,7 @@ FilterStringifiedMethodData(
   auto result =
       std::make_unique<std::map<std::string, std::set<std::string>>>();
   for (const auto& pair : stringified_method_data) {
-    if (supported_payment_method_names.contains(pair.first)) {
+    if (base::Contains(supported_payment_method_names, pair.first)) {
       result->insert({pair.first, pair.second});
     }
   }
diff --git a/components/pdf/browser/pdf_document_helper.cc b/components/pdf/browser/pdf_document_helper.cc
index efb01a1bfc..12235d8f74 100644
--- a/components/pdf/browser/pdf_document_helper.cc
+++ b/components/pdf/browser/pdf_document_helper.cc
@@ -7,6 +7,7 @@
 #include <utility>
 
 #include "base/notreached.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/pdf/browser/pdf_document_helper_client.h"
 #include "components/pdf/browser/pdf_frame_util.h"
 #include "content/public/browser/document_user_data.h"
@@ -318,7 +319,7 @@ bool PDFDocumentHelper::IsCommandIdEnabled(int command_id) const {
   bool readable = true;
 
   switch (command_id) {
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
       return readable && has_selection_;
       // TODO(wjmaclean): add logic for cut/paste as the information required
       // from PDFium becomes available.
@@ -330,7 +331,7 @@ void PDFDocumentHelper::ExecuteCommand(int command_id, int event_flags) {
   // TODO(wjmaclean, dsinclair): Need to communicate to PDFium to accept
   // cut/paste commands.
   switch (command_id) {
-    case std::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
+    case base::to_underlying(ui::TouchEditable::MenuCommands::kCopy):
       GetWebContents().Copy();
       break;
   }
diff --git a/components/pdf/common/pdf_util.cc b/components/pdf/common/pdf_util.cc
index 357f0bd791..e0a8c35b87 100644
--- a/components/pdf/common/pdf_util.cc
+++ b/components/pdf/common/pdf_util.cc
@@ -4,8 +4,7 @@
 
 #include "components/pdf/common/pdf_util.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "content/public/common/url_utils.h"
 #include "extensions/buildflags/buildflags.h"
@@ -47,7 +46,7 @@ bool IsPdfInternalPluginAllowedOrigin(
   // allowlisted. See also https://crbug.com/520422 and
   // https://crbug.com/1027173.
   return IsPdfExtensionOrigin(origin) ||
-         std::ranges::contains(additional_allowed_origins, origin);
+         base::Contains(additional_allowed_origins, origin);
 }
 
 SkColor GetPdfBackgroundColor() {
diff --git a/components/pdf/renderer/pdf_accessibility_tree.cc b/components/pdf/renderer/pdf_accessibility_tree.cc
index 72faac2ec7..146544ecdc 100644
--- a/components/pdf/renderer/pdf_accessibility_tree.cc
+++ b/components/pdf/renderer/pdf_accessibility_tree.cc
@@ -47,6 +47,7 @@
 #include "ui/gfx/geometry/transform.h"
 
 #if BUILDFLAG(ENABLE_SCREEN_AI_SERVICE)
+#include "base/containers/contains.h"
 #include "ui/strings/grit/auto_image_annotation_strings.h"
 #endif  // BUILDFLAG(ENABLE_SCREEN_AI_SERVICE)
 
diff --git a/components/performance_manager/browser_child_process_watcher.cc b/components/performance_manager/browser_child_process_watcher.cc
index 8b38ac570b..16132166ad 100644
--- a/components/performance_manager/browser_child_process_watcher.cc
+++ b/components/performance_manager/browser_child_process_watcher.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
 #include "base/process/process.h"
@@ -64,13 +65,15 @@ ProcessNodeImpl* BrowserChildProcessWatcher::GetChildProcessNode(
 
 void BrowserChildProcessWatcher::CreateChildProcessNodeForTesting(
     const content::ChildProcessData& data) {
-  CHECK(!tracked_process_nodes_.contains(BrowserChildProcessHostId(data.id)));
+  CHECK(!base::Contains(tracked_process_nodes_,
+                        BrowserChildProcessHostId(data.id)));
   BrowserChildProcessLaunchedAndConnected(data);
 }
 
 void BrowserChildProcessWatcher::DeleteChildProcessNodeForTesting(
     const content::ChildProcessData& data) {
-  CHECK(tracked_process_nodes_.contains(BrowserChildProcessHostId(data.id)));
+  CHECK(base::Contains(tracked_process_nodes_,
+                       BrowserChildProcessHostId(data.id)));
   BrowserChildProcessHostDisconnected(data);
 }
 
diff --git a/components/performance_manager/execution_context_priority/closing_page_voter.cc b/components/performance_manager/execution_context_priority/closing_page_voter.cc
index 1b2f363b04..2f3a22ebb2 100644
--- a/components/performance_manager/execution_context_priority/closing_page_voter.cc
+++ b/components/performance_manager/execution_context_priority/closing_page_voter.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/not_fatal_until.h"
 #include "components/performance_manager/public/execution_context/execution_context_registry.h"
 #include "components/performance_manager/public/graph/graph.h"
@@ -75,7 +76,7 @@ void ClosingPageVoter::OnBeforeFrameNodeAdded(
     const PageNode* pending_page_node,
     const ProcessNode* pending_process_node,
     const FrameNode* pending_parent_or_outer_document_or_embedder) {
-  if (closing_pages_.contains(pending_page_node)) {
+  if (base::Contains(closing_pages_, pending_page_node)) {
     // A frame is added to a closing page. Adjust the vote.
     AdjustVotesForSubtree(frame_node, /*is_closing=*/true);
   }
@@ -83,7 +84,7 @@ void ClosingPageVoter::OnBeforeFrameNodeAdded(
 
 void ClosingPageVoter::OnBeforeFrameNodeRemoved(const FrameNode* frame_node) {
   // Invalidate vote on frame removal.
-  if (closing_pages_.contains(frame_node->GetPageNode())) {
+  if (base::Contains(closing_pages_, frame_node->GetPageNode())) {
     AdjustVotesForSubtree(frame_node, /*is_closing=*/false);
   }
 }
diff --git a/components/performance_manager/execution_context_priority/force_foreground_voter.cc b/components/performance_manager/execution_context_priority/force_foreground_voter.cc
index 226c6010e0..c49aea3674 100644
--- a/components/performance_manager/execution_context_priority/force_foreground_voter.cc
+++ b/components/performance_manager/execution_context_priority/force_foreground_voter.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/not_fatal_until.h"
 #include "components/performance_manager/public/execution_context/execution_context.h"
 #include "components/performance_manager/public/graph/graph.h"
diff --git a/components/performance_manager/freezing/freezing_policy.cc b/components/performance_manager/freezing/freezing_policy.cc
index ca1adb17e5..e72cdbeaf4 100644
--- a/components/performance_manager/freezing/freezing_policy.cc
+++ b/components/performance_manager/freezing/freezing_policy.cc
@@ -11,6 +11,7 @@
 
 #include "base/byte_size.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -373,7 +374,7 @@ base::flat_set<raw_ptr<const PageNode>> FreezingPolicy::GetConnectedPages(
       CHECK(it != browsing_instance_states_.end());
       const BrowsingInstanceState& browsing_instance_state = it->second;
       for (auto* browsing_instance_page : browsing_instance_state.pages) {
-        if (!connected_pages.contains(browsing_instance_page)) {
+        if (!base::Contains(connected_pages, browsing_instance_page)) {
           pages_to_visit.insert(browsing_instance_page);
         }
       }
@@ -615,7 +616,7 @@ void FreezingPolicy::OnBeforePageNodeRemoved(const PageNode* page_node) {
   }
 
   CHECK(page_node->GetMainFrameNodes().empty());
-  CHECK(!std::ranges::contains(most_recently_used_, page_node),
+  CHECK(!base::Contains(most_recently_used_, page_node),
         base::NotFatalUntil::M140);
   CheckMostRecentlyUsedListSize();
 }
@@ -666,7 +667,7 @@ void FreezingPolicy::OnIsVisibleChanged(const PageNode* page_node) {
   } else {
     // Page becomes hidden.
     if (page_node->GetType() == PageType::kTab) {
-      CHECK(!std::ranges::contains(most_recently_used_, page_node),
+      CHECK(!base::Contains(most_recently_used_, page_node),
             base::NotFatalUntil::M140);
       CHECK_GT(num_visible_tabs_, 0, base::NotFatalUntil::M140);
       --num_visible_tabs_;
@@ -1062,7 +1063,8 @@ void FreezingPolicy::DiscardFrozenPagesWithGrowingMemoryOnMemoryMeasurement(
 
     const base::ByteSize current =
         result.memory_summary_result->private_footprint;
-    if (browsing_instance_states_without_initial_measurement.contains(id)) {
+    if (base::Contains(browsing_instance_states_without_initial_measurement,
+                       id)) {
       // Store the first PMF measurement after being frozen.
       state.per_origin_pmf_after_freezing[origin_in_browsing_instance_context
                                               .GetOrigin()] = current;
@@ -1442,7 +1444,7 @@ void FreezingPolicy::UpdateAllPagesFrozenState() {
 
   base::flat_set<raw_ptr<const PageNode>> visited_pages;
   for (auto& [id, state] : browsing_instance_states_) {
-    if (!visited_pages.contains(*state.pages.begin())) {
+    if (!base::Contains(visited_pages, *state.pages.begin())) {
       UpdateFrozenState(*state.pages.begin(), now, &visited_pages);
     }
   }
diff --git a/components/performance_manager/graph/frame_node_impl.cc b/components/performance_manager/graph/frame_node_impl.cc
index 8013a653b4..c405b8246e 100644
--- a/components/performance_manager/graph/frame_node_impl.cc
+++ b/components/performance_manager/graph/frame_node_impl.cc
@@ -7,6 +7,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
diff --git a/components/performance_manager/graph/graph_impl.cc b/components/performance_manager/graph/graph_impl.cc
index 5ceec9cd80..c5d59eb8d5 100644
--- a/components/performance_manager/graph/graph_impl.cc
+++ b/components/performance_manager/graph/graph_impl.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/map_util.h"
@@ -322,7 +323,7 @@ GraphImpl* GraphImpl::FromGraph(const Graph* graph) {
 bool GraphImpl::NodeInGraph(const NodeBase* node) const {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   const NodeSet& nodes = GetNodesOfType(node->GetNodeType());
-  return nodes.contains(node->ToNode());
+  return base::Contains(nodes, node->ToNode());
 }
 
 bool GraphImpl::NodeEdgesArePublic(const NodeBase* node) const {
diff --git a/components/performance_manager/graph/process_node_impl.cc b/components/performance_manager/graph/process_node_impl.cc
index 34eb6e0278..ce6ddcd453 100644
--- a/components/performance_manager/graph/process_node_impl.cc
+++ b/components/performance_manager/graph/process_node_impl.cc
@@ -11,6 +11,7 @@
 #include "base/byte_size.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/read_only_shared_memory_region.h"
 #include "base/trace_event/named_trigger.h"
@@ -378,7 +379,7 @@ void ProcessNodeImpl::AddFrame(FrameNodeImpl* frame_node) {
 void ProcessNodeImpl::RemoveFrame(FrameNodeImpl* frame_node) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   DCHECK_EQ(process_type_, content::PROCESS_TYPE_RENDERER);
-  DCHECK(frame_nodes_.contains(frame_node));
+  DCHECK(base::Contains(frame_nodes_, frame_node));
   frame_nodes_.erase(frame_node);
 }
 
@@ -392,7 +393,7 @@ void ProcessNodeImpl::AddWorker(WorkerNodeImpl* worker_node) {
 void ProcessNodeImpl::RemoveWorker(WorkerNodeImpl* worker_node) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   DCHECK_EQ(process_type_, content::PROCESS_TYPE_RENDERER);
-  DCHECK(worker_nodes_.contains(worker_node));
+  DCHECK(base::Contains(worker_nodes_, worker_node));
   worker_nodes_.erase(worker_node);
 }
 
diff --git a/components/performance_manager/owned_objects.h b/components/performance_manager/owned_objects.h
index f80c5882f1..0db9b2f89c 100644
--- a/components/performance_manager/owned_objects.h
+++ b/components/performance_manager/owned_objects.h
@@ -8,6 +8,7 @@
 #include <memory>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/unique_ptr_adapters.h"
 
diff --git a/components/performance_manager/performance_manager_tab_helper.cc b/components/performance_manager/performance_manager_tab_helper.cc
index 0ee6814087..7486d0b09b 100644
--- a/components/performance_manager/performance_manager_tab_helper.cc
+++ b/components/performance_manager/performance_manager_tab_helper.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/weak_ptr.h"
 #include "base/observer_list.h"
@@ -191,7 +192,7 @@ void PerformanceManagerTabHelper::RenderFrameCreated(
     content::RenderFrameHost* render_frame_host) {
   DCHECK_NE(nullptr, render_frame_host);
   // This must not exist in the map yet.
-  DCHECK(!frames_.contains(render_frame_host));
+  DCHECK(!base::Contains(frames_, render_frame_host));
 
   content::RenderFrameHost* parent = render_frame_host->GetParent();
   FrameNodeImpl* parent_frame_node = nullptr;
diff --git a/components/performance_manager/persistence/site_data/site_data_cache_factory.cc b/components/performance_manager/persistence/site_data/site_data_cache_factory.cc
index 6b4d8e8726..3813e46e04 100644
--- a/components/performance_manager/persistence/site_data/site_data_cache_factory.cc
+++ b/components/performance_manager/persistence/site_data/site_data_cache_factory.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/task/sequenced_task_runner.h"
 #include "components/performance_manager/performance_manager_impl.h"
@@ -95,7 +96,7 @@ void SiteDataCacheFactory::SetCacheInspectorForTesting(
     const std::string& browser_context_id,
     SiteDataCacheInspector* inspector) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  DCHECK(!data_cache_inspector_map_.contains(browser_context_id));
+  DCHECK(!base::Contains(data_cache_inspector_map_, browser_context_id));
   data_cache_inspector_map_.emplace(browser_context_id, inspector);
 }
 
@@ -105,13 +106,13 @@ void SiteDataCacheFactory::OnBrowserContextCreated(
     std::optional<std::string> parent_context_id) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
 
-  DCHECK(!data_cache_map_.contains(browser_context_id));
+  DCHECK(!base::Contains(data_cache_map_, browser_context_id));
 
   if (parent_context_id) {
     SiteDataCacheInspector* parent_debug =
         GetInspectorForBrowserContext(parent_context_id.value());
     DCHECK(parent_debug);
-    DCHECK(data_cache_map_.contains(parent_context_id.value()));
+    DCHECK(base::Contains(data_cache_map_, parent_context_id.value()));
     SiteDataCache* data_cache_for_readers =
         data_cache_map_[parent_context_id.value()].get();
     DCHECK(data_cache_for_readers);
@@ -129,7 +130,7 @@ void SiteDataCacheFactory::OnBrowserContextCreated(
 void SiteDataCacheFactory::OnBrowserContextDestroyed(
     const std::string& browser_context_id) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  DCHECK(data_cache_map_.contains(browser_context_id));
+  DCHECK(base::Contains(data_cache_map_, browser_context_id));
   data_cache_map_.erase(browser_context_id);
 }
 
diff --git a/components/performance_manager/persistence/site_data/site_data_cache_impl.cc b/components/performance_manager/persistence/site_data/site_data_cache_impl.cc
index aec73083fd..481eca4ae7 100644
--- a/components/performance_manager/persistence/site_data/site_data_cache_impl.cc
+++ b/components/performance_manager/persistence/site_data/site_data_cache_impl.cc
@@ -7,6 +7,7 @@
 #include <set>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback.h"
 #include "base/memory/ptr_util.h"
@@ -138,7 +139,7 @@ internal::SiteDataImpl* SiteDataCacheImpl::GetOrCreateFeatureImpl(
 void SiteDataCacheImpl::OnSiteDataImplDestroyed(internal::SiteDataImpl* impl) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   DCHECK(impl);
-  DCHECK(origin_data_map_.contains(impl->origin()));
+  DCHECK(base::Contains(origin_data_map_, impl->origin()));
   // Remove the entry for this origin as this is about to get destroyed.
   auto num_erased = origin_data_map_.erase(impl->origin());
   DCHECK_EQ(1U, num_erased);
diff --git a/components/performance_manager/resource_attribution/context_collection.cc b/components/performance_manager/resource_attribution/context_collection.cc
index ff14c81856..e17aaad4eb 100644
--- a/components/performance_manager/resource_attribution/context_collection.cc
+++ b/components/performance_manager/resource_attribution/context_collection.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 
 namespace resource_attribution {
 
@@ -34,7 +35,7 @@ bool ContextCollection::IsEmpty() const {
 
 bool ContextCollection::ContainsContext(const ResourceContext& context) const {
   return all_context_types_.test(ResourceContextTypeId(context).value()) ||
-         resource_contexts_.contains(context);
+         base::Contains(resource_contexts_, context);
 }
 
 // static
diff --git a/components/performance_manager/resource_attribution/cpu_measurement_monitor.cc b/components/performance_manager/resource_attribution/cpu_measurement_monitor.cc
index df6d37781d..4867fb17e8 100644
--- a/components/performance_manager/resource_attribution/cpu_measurement_monitor.cc
+++ b/components/performance_manager/resource_attribution/cpu_measurement_monitor.cc
@@ -293,7 +293,7 @@ QueryResultMap CPUMeasurementMonitor::UpdateAndGetCPUMeasurements(
     const OriginInBrowsingInstanceContext& origin_context = it->first;
     scoped_refptr<ScopedCPUTimeResult>& result_ptr = it->second;
     CHECK(result_ptr);
-    if (live_origin_contexts.contains(origin_context)) {
+    if (base::Contains(live_origin_contexts, origin_context)) {
       save_result(ResourceContext(origin_context), result_ptr);
       ++it;
     } else {
diff --git a/components/performance_manager/resource_attribution/memory_measurement_provider.cc b/components/performance_manager/resource_attribution/memory_measurement_provider.cc
index a8a5158453..5558b2446c 100644
--- a/components/performance_manager/resource_attribution/memory_measurement_provider.cc
+++ b/components/performance_manager/resource_attribution/memory_measurement_provider.cc
@@ -7,6 +7,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/metrics/histogram_functions.h"
diff --git a/components/performance_manager/scenarios/loading_scenario_data.cc b/components/performance_manager/scenarios/loading_scenario_data.cc
index af04fa037f..669c406dbc 100644
--- a/components/performance_manager/scenarios/loading_scenario_data.cc
+++ b/components/performance_manager/scenarios/loading_scenario_data.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/numerics/checked_math.h"
 #include "components/performance_manager/public/graph/process_node.h"
 
@@ -83,7 +84,7 @@ size_t LoadingScenarioPageFrameCounts::DecrementFrameCountForProcess(
 
 bool LoadingScenarioPageFrameCounts::ProcessHasFramesInPage(
     const ProcessNode* process_node) const {
-  return process_frame_counts_.contains(process_node);
+  return base::Contains(process_frame_counts_, process_node);
 }
 
 std::vector<const ProcessNode*>
diff --git a/components/performance_manager/test_support/performance_manager_test_harness.cc b/components/performance_manager/test_support/performance_manager_test_harness.cc
index a6d2004371..908a603f17 100644
--- a/components/performance_manager/test_support/performance_manager_test_harness.cc
+++ b/components/performance_manager/test_support/performance_manager_test_harness.cc
@@ -9,6 +9,7 @@
 #include <string>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/stringprintf.h"
 #include "base/test/bind.h"
@@ -391,7 +392,7 @@ void PerformanceManagerTestHarness::ServiceWorkerFactory::
   CHECK(it != service_worker_infos_.end());
   ServiceWorkerInfo& info = it->second;
 
-  DCHECK(info.clients.contains(client_uuid));
+  DCHECK(base::Contains(info.clients, client_uuid));
 
   observer_->OnControlleeNavigationCommitted(version_id, client_uuid,
                                              render_frame_host_id);
diff --git a/components/performance_manager/user_tuning/prefs.cc b/components/performance_manager/user_tuning/prefs.cc
index 889c528f61..05826f9391 100644
--- a/components/performance_manager/user_tuning/prefs.cc
+++ b/components/performance_manager/user_tuning/prefs.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/json/values_util.h"
 #include "base/values.h"
 #include "components/pref_registry/pref_registry_syncable.h"
diff --git a/components/performance_manager/v8_memory/v8_detailed_memory_decorator.cc b/components/performance_manager/v8_memory/v8_detailed_memory_decorator.cc
index 6b6aa3cadf..60a5d3ad95 100644
--- a/components/performance_manager/v8_memory/v8_detailed_memory_decorator.cc
+++ b/components/performance_manager/v8_memory/v8_detailed_memory_decorator.cc
@@ -4,12 +4,12 @@
 
 #include "components/performance_manager/v8_memory/v8_detailed_memory_decorator.h"
 
-#include <algorithm>
 #include <utility>
 #include <vector>
 
 #include "base/byte_count.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -877,7 +877,7 @@ void V8DetailedMemoryRequestQueue::AddMeasurementRequest(
       measurement_requests =
           IsMeasurementBounded(request->mode()) ? bounded_measurement_requests_
                                                 : lazy_measurement_requests_;
-  DCHECK(!std::ranges::contains(measurement_requests, request))
+  DCHECK(!base::Contains(measurement_requests, request))
       << "V8DetailedMemoryRequest object added twice";
   // Each user of the decorator is expected to issue a single
   // V8DetailedMemoryRequest, so the size of measurement_requests is too low
diff --git a/components/performance_manager/worker_watcher.cc b/components/performance_manager/worker_watcher.cc
index 4cb2dfa57d..51b7bc7a86 100644
--- a/components/performance_manager/worker_watcher.cc
+++ b/components/performance_manager/worker_watcher.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/notreached.h"
@@ -236,7 +237,7 @@ void WorkerWatcher::OnBeforeWorkerDestroyed(
   }
 
 #if DCHECK_IS_ON()
-  DCHECK(!detached_frame_count_per_worker_.contains(worker_node.get()));
+  DCHECK(!base::Contains(detached_frame_count_per_worker_, worker_node.get()));
 #endif  // DCHECK_IS_ON()
   PerformanceManagerImpl::DeleteNode(std::move(worker_node));
 
@@ -301,7 +302,7 @@ void WorkerWatcher::OnBeforeWorkerDestroyed(
   }
 
 #if DCHECK_IS_ON()
-  DCHECK(!detached_frame_count_per_worker_.contains(worker_node.get()));
+  DCHECK(!base::Contains(detached_frame_count_per_worker_, worker_node.get()));
 #endif  // DCHECK_IS_ON()
   PerformanceManagerImpl::DeleteNode(std::move(worker_node));
 
@@ -373,7 +374,8 @@ void WorkerWatcher::OnVersionStoppedRunning(int64_t version_id) {
   DisconnectAllServiceWorkerClients(service_worker_node.get(), version_id);
 
 #if DCHECK_IS_ON()
-  DCHECK(!detached_frame_count_per_worker_.contains(service_worker_node.get()));
+  DCHECK(!base::Contains(detached_frame_count_per_worker_,
+                         service_worker_node.get()));
 #endif  // DCHECK_IS_ON()
   PerformanceManagerImpl::DeleteNode(std::move(service_worker_node));
 
@@ -441,7 +443,7 @@ void WorkerWatcher::OnControlleeRemoved(int64_t version_id,
     // |client_uuid| should not be part of this service worker's clients.
     auto it = service_worker_clients_.find(version_id);
     if (it != service_worker_clients_.end())
-      DCHECK(!it->second.contains(client_uuid));
+      DCHECK(!base::Contains(it->second, client_uuid));
 #endif  // DCHECK_IS_ON()
     return;
   }
diff --git a/components/permissions/object_permission_context_base.cc b/components/permissions/object_permission_context_base.cc
index f2b5da831b..42f978e79b 100644
--- a/components/permissions/object_permission_context_base.cc
+++ b/components/permissions/object_permission_context_base.cc
@@ -7,6 +7,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/observer_list.h"
@@ -189,7 +190,7 @@ void ObjectPermissionContextBase::UpdateObjectPermission(
 
   origin_objects_it->second.erase(object_it);
   key = GetKeyForObject(new_object);
-  DCHECK(!origin_objects_it->second.contains(key));
+  DCHECK(!base::Contains(origin_objects_it->second, key));
 
   GrantObjectPermission(origin, std::move(new_object));
 }
diff --git a/components/permissions/permission_request_manager.cc b/components/permissions/permission_request_manager.cc
index 76a5facb99..3c4d750c45 100644
--- a/components/permissions/permission_request_manager.cc
+++ b/components/permissions/permission_request_manager.cc
@@ -13,6 +13,7 @@
 #include "base/auto_reset.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
@@ -711,8 +712,8 @@ void PermissionRequestManager::Deny(const PromptOptions& prompt_options) {
   // trapped in request loops where the website automatically navigates
   // cross-origin (e.g. to another subdomain) to be able to prompt again after
   // a rejection.
-  if (std::ranges::contains(requests_, ContentSettingsType::NOTIFICATIONS,
-                            &PermissionRequest::GetContentSettingsType)) {
+  if (base::Contains(requests_, ContentSettingsType::NOTIFICATIONS,
+                     &PermissionRequest::GetContentSettingsType)) {
     is_notification_prompt_cooldown_active_ = true;
   }
 
diff --git a/components/permissions/permission_util.cc b/components/permissions/permission_util.cc
index 1e0b4bfd8b..f15c86c4f0 100644
--- a/components/permissions/permission_util.cc
+++ b/components/permissions/permission_util.cc
@@ -4,10 +4,10 @@
 
 #include "components/permissions/permission_util.h"
 
-#include <algorithm>
 #include <memory>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
@@ -425,13 +425,12 @@ bool PermissionUtil::IsGuardContentSetting(ContentSettingsType type) {
 }
 
 bool PermissionUtil::DoesSupportTemporaryGrants(ContentSettingsType type) {
-  return std::ranges::contains(content_settings::GetTypesWithTemporaryGrants(),
-                               type);
+  return base::Contains(content_settings::GetTypesWithTemporaryGrants(), type);
 }
 
 bool PermissionUtil::DoesStoreTemporaryGrantsInHcsm(ContentSettingsType type) {
-  return std::ranges::contains(
-      content_settings::GetTypesWithTemporaryGrantsInHcsm(), type);
+  return base::Contains(content_settings::GetTypesWithTemporaryGrantsInHcsm(),
+                        type);
 }
 
 // Due to dependency issues, this method is duplicated in
diff --git a/components/permissions/request_type.cc b/components/permissions/request_type.cc
index 39bad3ef7f..9be3073669 100644
--- a/components/permissions/request_type.cc
+++ b/components/permissions/request_type.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/notreached.h"
@@ -387,7 +388,7 @@ bool IsConfirmationChipSupported(RequestType for_request_type) {
           RequestType::kMicStream,
           // clang-format on
       });
-  return kRequestsWithChip.contains(for_request_type);
+  return base::Contains(kRequestsWithChip, for_request_type);
 }
 
 #if !BUILDFLAG(IS_IOS)
diff --git a/components/permissions/test/mock_permission_prompt_factory.cc b/components/permissions/test/mock_permission_prompt_factory.cc
index 510971644c..282c9fc4db 100644
--- a/components/permissions/test/mock_permission_prompt_factory.cc
+++ b/components/permissions/test/mock_permission_prompt_factory.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/ptr_util.h"
@@ -80,11 +81,11 @@ int MockPermissionPromptFactory::TotalRequestCount() {
 }
 
 bool MockPermissionPromptFactory::RequestTypeSeen(RequestType type) {
-  return std::ranges::contains(request_types_seen_, type);
+  return base::Contains(request_types_seen_, type);
 }
 
 bool MockPermissionPromptFactory::RequestOriginSeen(const GURL& origin) {
-  return std::ranges::contains(request_origins_seen_, origin);
+  return base::Contains(request_origins_seen_, origin);
 }
 
 void MockPermissionPromptFactory::WaitForPermissionBubble() {
diff --git a/components/plus_addresses/core/browser/metrics/plus_address_submission_logger.cc b/components/plus_addresses/core/browser/metrics/plus_address_submission_logger.cc
index ffac2831cf..adfaf95e1f 100644
--- a/components/plus_addresses/core/browser/metrics/plus_address_submission_logger.cc
+++ b/components/plus_addresses/core/browser/metrics/plus_address_submission_logger.cc
@@ -14,6 +14,7 @@
 #include "base/scoped_multi_source_observation.h"
 #include "base/strings/strcat.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/autofill/core/browser/autofill_field.h"
 #include "components/autofill/core/browser/data_model/transliterator.h"
 #include "components/autofill/core/browser/form_structure.h"
@@ -140,10 +141,10 @@ void PlusAddressSubmissionLogger::OnPlusAddressSuggestionShown(
       .SetFieldCountRendererForm(ukm::GetExponentialBucketMinForCounts1000(
           field_count_in_renderer_form))
       .SetManagedProfile(account_info.IsManaged() == signin::Tribool::kTrue)
-      .SetPasswordFormType(std::to_underlying(form_type))
+      .SetPasswordFormType(base::to_underlying(form_type))
       .SetPlusAddressCount(
-          std::to_underlying(ToPlusAddressCountBucket(plus_address_count)))
-      .SetSuggestionContext(std::to_underlying(suggestion_context));
+          base::to_underlying(ToPlusAddressCountBucket(plus_address_count)))
+      .SetSuggestionContext(base::to_underlying(suggestion_context));
   records_[&manager].insert_or_assign(field, std::move(record));
 }
 
diff --git a/components/policy/core/browser/policy_error_map.cc b/components/policy/core/browser/policy_error_map.cc
index ca29dc6ad1..48ca0e13e6 100644
--- a/components/policy/core/browser/policy_error_map.cc
+++ b/components/policy/core/browser/policy_error_map.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
 #include "components/policy/core/common/schema.h"
@@ -137,9 +138,9 @@ void PolicyErrorMap::AddError(const std::string& policy,
 bool PolicyErrorMap::HasError(const std::string& policy) {
   if (IsReady()) {
     CheckReadyAndConvert();
-    return map_.contains(policy);
+    return base::Contains(map_, policy);
   }
-  return std::ranges::contains(pending_, policy, &PendingError::policy_name);
+  return base::Contains(pending_, policy, &PendingError::policy_name);
 }
 
 bool PolicyErrorMap::HasFatalError(const std::string& policy) {
@@ -147,12 +148,12 @@ bool PolicyErrorMap::HasFatalError(const std::string& policy) {
       std::make_pair(policy, PolicyMap::MessageType::kError);
   if (IsReady()) {
     CheckReadyAndConvert();
-    return std::ranges::contains(
+    return base::Contains(
         map_, fatal_error, [](const std::pair<std::string, Data>& entry) {
           return std::make_pair(entry.first, entry.second.level);
         });
   }
-  return std::ranges::contains(pending_, fatal_error, [](const auto& entry) {
+  return base::Contains(pending_, fatal_error, [](const auto& entry) {
     return std::make_pair(entry->policy_name(), entry->level());
   });
 }
diff --git a/components/policy/core/browser/policy_pref_mapping_test.cc b/components/policy/core/browser/policy_pref_mapping_test.cc
index 6b660bad04..d258b9f82c 100644
--- a/components/policy/core/browser/policy_pref_mapping_test.cc
+++ b/components/policy/core/browser/policy_pref_mapping_test.cc
@@ -4,7 +4,6 @@
 
 #include "components/policy/core/browser/policy_pref_mapping_test.h"
 
-#include <algorithm>
 #include <map>
 #include <memory>
 #include <optional>
@@ -13,6 +12,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
@@ -443,15 +443,15 @@ class PolicyTestCase {
 #else
 #error "Unknown platform"
 #endif
-    return std::ranges::contains(supported_os_, os);
+    return base::Contains(supported_os_, os);
   }
 
   bool IsOsCovered() const {
 #if BUILDFLAG(IS_ANDROID)
     // Android policies that apply to desktop Android are covered as part of the
     // desktop Android build because they may invoke desktop-only code.
-    return std::ranges::contains(supported_os_, "android") ||
-           std::ranges::contains(supported_os_, "desktop_android");
+    return base::Contains(supported_os_, "android") ||
+           base::Contains(supported_os_, "desktop_android");
 #else
     return IsOsSupported();
 #endif
@@ -700,7 +700,8 @@ void VerifyPolicyToPrefMappings(const base::FilePath& test_case_dir,
       }
     }
 
-    if (test_filter.has_value() && !test_filter.value().contains(policy_name)) {
+    if (test_filter.has_value() &&
+        !base::Contains(test_filter.value(), policy_name)) {
       // Skip policy based on the filter.
       continue;
     }
diff --git a/components/policy/core/common/cloud/cloud_policy_client.cc b/components/policy/core/common/cloud/cloud_policy_client.cc
index cc34fa402b..abf46219f2 100644
--- a/components/policy/core/common/cloud/cloud_policy_client.cc
+++ b/components/policy/core/common/cloud/cloud_policy_client.cc
@@ -9,6 +9,7 @@
 #include <variant>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
diff --git a/components/policy/core/common/cloud/dmserver_job_configurations.cc b/components/policy/core/common/cloud/dmserver_job_configurations.cc
index 02a3af4640..261bfb7851 100644
--- a/components/policy/core/common/cloud/dmserver_job_configurations.cc
+++ b/components/policy/core/common/cloud/dmserver_job_configurations.cc
@@ -4,8 +4,7 @@
 
 #include "components/policy/core/common/cloud/dmserver_job_configurations.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/to_string.h"
 #include "components/policy/core/common/cloud/cloud_policy_client.h"
@@ -279,9 +278,8 @@ DMServerJobConfiguration::MapNetErrorAndResponseToDMStatus(
       // statuses depending on the contents of the response body.
       em::DeviceManagementResponse response;
       if (response.ParseFromString(response_body) &&
-          std::ranges::contains(
-              response.error_detail(),
-              em::CBCM_DELETION_POLICY_PREFERENCE_DELETE_TOKEN)) {
+          base::Contains(response.error_detail(),
+                         em::CBCM_DELETION_POLICY_PREFERENCE_DELETE_TOKEN)) {
         return DM_STATUS_SERVICE_DEVICE_NEEDS_RESET;
       }
 #endif  // !BUILDFLAG(IS_CHROMEOS)
diff --git a/components/policy/core/common/cloud/encrypted_reporting_job_configuration.cc b/components/policy/core/common/cloud/encrypted_reporting_job_configuration.cc
index e0c8550412..cf4a758995 100644
--- a/components/policy/core/common/cloud/encrypted_reporting_job_configuration.cc
+++ b/components/policy/core/common/cloud/encrypted_reporting_job_configuration.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_number_conversions.h"
@@ -74,7 +75,7 @@ EncryptedReportingJobConfiguration::~EncryptedReportingJobConfiguration() {
 void EncryptedReportingJobConfiguration::UpdatePayloadBeforeGetInternal() {
   for (auto it = payload_.begin(); it != payload_.end();) {
     const auto& [key, value] = *it;
-    if (!GetTopLevelKeyAllowList().contains(key)) {
+    if (!base::Contains(GetTopLevelKeyAllowList(), key)) {
       it = payload_.erase(it);
       continue;
     }
diff --git a/components/policy/core/common/config_dir_policy_loader.cc b/components/policy/core/common/config_dir_policy_loader.cc
index 9d22409a61..0baaa6aaed 100644
--- a/components/policy/core/common/config_dir_policy_loader.cc
+++ b/components/policy/core/common/config_dir_policy_loader.cc
@@ -12,6 +12,7 @@
 #include <string>
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -186,7 +187,7 @@ void ConfigDirPolicyLoader::Merge3rdPartyPolicy(const base::Value* policies,
                                         : POLICY_DOMAIN_EXTENSIONS;
 
   for (auto domains_it : *domains_dictionary) {
-    if (!supported_domains.contains(domains_it.first)) {
+    if (!base::Contains(supported_domains, domains_it.first)) {
       SYSLOG(WARNING) << "Unsupported 3rd party policy domain: "
                       << domains_it.first;
       continue;
diff --git a/components/policy/core/common/local_test_policy_loader.cc b/components/policy/core/common/local_test_policy_loader.cc
index ca75330649..82a7870f0a 100644
--- a/components/policy/core/common/local_test_policy_loader.cc
+++ b/components/policy/core/common/local_test_policy_loader.cc
@@ -7,6 +7,7 @@
 #include <map>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/json/json_reader.h"
 #include "base/logging.h"
 #include "base/strings/stringprintf.h"
diff --git a/components/policy/core/common/policy_map.cc b/components/policy/core/common/policy_map.cc
index b95ebcb904..22f4527048 100644
--- a/components/policy/core/common/policy_map.cc
+++ b/components/policy/core/common/policy_map.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/strings/strcat.h"
@@ -532,8 +533,7 @@ void PolicyMap::MergeFrom(const PolicyMap& other) {
 #if !BUILDFLAG(IS_CHROMEOS)
     // Skip precedence metapolicies since they have already been merged into the
     // current PolicyMap.
-    if (std::ranges::contains(metapolicy::kPrecedence,
-                              policy_and_entry.first)) {
+    if (base::Contains(metapolicy::kPrecedence, policy_and_entry.first)) {
       continue;
     }
 #endif
diff --git a/components/policy/core/common/policy_service_impl.cc b/components/policy/core/common/policy_service_impl.cc
index c3c7c5796f..8c6774577f 100644
--- a/components/policy/core/common/policy_service_impl.cc
+++ b/components/policy/core/common/policy_service_impl.cc
@@ -12,6 +12,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -239,7 +240,7 @@ void PolicyServiceImpl::RemoveProviderUpdateObserver(
 bool PolicyServiceImpl::HasProvider(
     ConfigurationPolicyProvider* provider) const {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  return std::ranges::contains(providers_, provider);
+  return base::Contains(providers_, provider);
 }
 
 const PolicyMap& PolicyServiceImpl::GetPolicies(
diff --git a/components/policy/core/common/remote_commands/remote_commands_service.cc b/components/policy/core/common/remote_commands/remote_commands_service.cc
index d6e5f7a7fe..3888d45cc3 100644
--- a/components/policy/core/common/remote_commands/remote_commands_service.cc
+++ b/components/policy/core/common/remote_commands/remote_commands_service.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -335,7 +336,7 @@ void RemoteCommandsService::EnqueueCommand(
   }
 
   // If the command is already fetched, ignore it.
-  if (std::ranges::contains(fetched_command_ids_, command.command_id())) {
+  if (base::Contains(fetched_command_ids_, command.command_id())) {
     RecordReceivedRemoteCommand(MetricReceivedRemoteCommand::kDuplicated);
     return;
   }
diff --git a/components/policy/core/common/schema.cc b/components/policy/core/common/schema.cc
index ae74576e22..d06c6ce2d1 100644
--- a/components/policy/core/common/schema.cc
+++ b/components/policy/core/common/schema.cc
@@ -20,6 +20,7 @@
 
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/json/json_reader.h"
 #include "base/memory/ptr_util.h"
@@ -858,7 +859,7 @@ base::expected<void, std::string> Schema::InternalStorage::Parse(
   const std::string* id = schema.FindString(schema::kId);
   if (id) {
     auto& id_map = references_and_ids->id_map;
-    if (id_map.contains(*id)) {
+    if (base::Contains(id_map, *id)) {
       return base::unexpected("Duplicated id: " + *id);
     }
     id_map[*id] = *index;
@@ -1247,7 +1248,7 @@ bool Schema::Validate(const base::Value& value,
     }
 
     for (const auto& required_property : GetRequiredProperties()) {
-      if (present_properties.contains(required_property)) {
+      if (base::Contains(present_properties, required_property)) {
         continue;
       }
 
@@ -1354,7 +1355,7 @@ bool Schema::Normalize(base::Value* value,
     }
 
     for (const auto& required_property : GetRequiredProperties()) {
-      if (present_properties.contains(required_property)) {
+      if (base::Contains(present_properties, required_property)) {
         continue;
       }
 
diff --git a/components/policy/test_support/client_storage.cc b/components/policy/test_support/client_storage.cc
index 970dae4ffd..c7051d4b54 100644
--- a/components/policy/test_support/client_storage.cc
+++ b/components/policy/test_support/client_storage.cc
@@ -4,10 +4,10 @@
 
 #include "components/policy/test_support/client_storage.h"
 
-#include <algorithm>
 #include <array>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_view_util.h"
 #include "crypto/hash.h"
 
@@ -66,7 +66,7 @@ const ClientStorage::ClientInfo* ClientStorage::GetClientOrNull(
 const ClientStorage::ClientInfo* ClientStorage::LookupByStateKey(
     const std::string& state_key) const {
   for (auto const& [device_id, client_info] : clients_) {
-    if (std::ranges::contains(client_info.state_keys, state_key)) {
+    if (base::Contains(client_info.state_keys, state_key)) {
       return &client_info;
     }
   }
diff --git a/components/policy/test_support/request_handler_for_policy.cc b/components/policy/test_support/request_handler_for_policy.cc
index 837902c0c1..b0b9c9099e 100644
--- a/components/policy/test_support/request_handler_for_policy.cc
+++ b/components/policy/test_support/request_handler_for_policy.cc
@@ -4,6 +4,7 @@
 
 #include "components/policy/test_support/request_handler_for_policy.h"
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/logging.h"
 #include "base/strings/stringprintf.h"
@@ -169,7 +170,7 @@ std::unique_ptr<HttpResponse> RequestHandlerForPolicy::HandleRequest(
               &error_msg)) {
         return CreateHttpResponse(net::HTTP_BAD_REQUEST, error_msg);
       }
-    } else if (kExtensionPolicyTypes.contains(policy_type)) {
+    } else if (base::Contains(kExtensionPolicyTypes, policy_type)) {
       if (!ProcessCloudPolicyForExtensions(
               fetch_request, *client_info,
               device_management_response.mutable_policy_response(),
diff --git a/components/prefs/pref_registry.cc b/components/prefs/pref_registry.cc
index e64137fb8d..e20d6ced01 100644
--- a/components/prefs/pref_registry.cc
+++ b/components/prefs/pref_registry.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/values.h"
 #include "components/prefs/default_pref_store.h"
 #include "components/prefs/pref_store.h"
diff --git a/components/prefs/scoped_user_pref_update.cc b/components/prefs/scoped_user_pref_update.cc
index 5ac4477c05..dce33396a6 100644
--- a/components/prefs/scoped_user_pref_update.cc
+++ b/components/prefs/scoped_user_pref_update.cc
@@ -5,7 +5,6 @@
 #include "components/prefs/scoped_user_pref_update.h"
 
 #include <string_view>
-#include <utility>
 
 #include "base/check_deref.h"
 #include "base/check_op.h"
@@ -16,6 +15,7 @@
 // the problem.
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
+#include "base/types/cxx23_to_underlying.h"
 
 namespace subtle {
 
@@ -45,11 +45,11 @@ base::Value* ScopedUserPrefUpdateBase::GetValueOfType(base::Value::Type type) {
     const PrefService::Preference* pref = service_->FindPreference(path_);
     SCOPED_CRASH_KEY_NUMBER(
         "ScopedUserPrefUpdate", "PrevServiceStatus",
-        std::to_underlying(service_->GetInitializationStatus()));
+        base::to_underlying(service_->GetInitializationStatus()));
     SCOPED_CRASH_KEY_STRING32("ScopedUserPrefUpdate", "FindPreference",
                               pref ? "Yes" : "No");
     SCOPED_CRASH_KEY_NUMBER("ScopedUserPrefUpdate", "Type",
-                            pref ? std::to_underlying(pref->GetType()) : -1);
+                            pref ? base::to_underlying(pref->GetType()) : -1);
     base::debug::DumpWithoutCrashing();
   }
   return value_;
diff --git a/components/prefs/segregated_pref_store.cc b/components/prefs/segregated_pref_store.cc
index abf4f865b8..54cf971481 100644
--- a/components/prefs/segregated_pref_store.cc
+++ b/components/prefs/segregated_pref_store.cc
@@ -9,6 +9,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "base/observer_list.h"
 #include "base/values.h"
@@ -220,15 +221,15 @@ SegregatedPrefStore::~SegregatedPrefStore() {
 }
 
 PersistentPrefStore* SegregatedPrefStore::StoreForKey(std::string_view key) {
-  return (selected_preference_names_.contains(key) ? selected_pref_store_
-                                                   : default_pref_store_)
+  return (base::Contains(selected_preference_names_, key) ? selected_pref_store_
+                                                          : default_pref_store_)
       .get();
 }
 
 const PersistentPrefStore* SegregatedPrefStore::StoreForKey(
     std::string_view key) const {
-  return (selected_preference_names_.contains(key) ? selected_pref_store_
-                                                   : default_pref_store_)
+  return (base::Contains(selected_preference_names_, key) ? selected_pref_store_
+                                                          : default_pref_store_)
       .get();
 }
 
diff --git a/components/printing/browser/print_composite_client.cc b/components/printing/browser/print_composite_client.cc
index 6a451ed97a..a9fff5e4fa 100644
--- a/components/printing/browser/print_composite_client.cc
+++ b/components/printing/browser/print_composite_client.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/read_only_shared_memory_region.h"
 #include "base/strings/utf_string_conversions.h"
@@ -197,8 +198,8 @@ void PrintCompositeClient::PrintCrossProcessSubframe(
   }
 
   // If this frame is already printed, no need to print again.
-  if (pending_subframes_.contains(subframe_host) ||
-      printed_subframes_.contains(subframe_host)) {
+  if (base::Contains(pending_subframes_, subframe_host) ||
+      base::Contains(printed_subframes_, subframe_host)) {
     return;
   }
 
diff --git a/components/printing/common/cloud_print_cdd_conversion.cc b/components/printing/common/cloud_print_cdd_conversion.cc
index 51ab695bec..49ae083067 100644
--- a/components/printing/common/cloud_print_cdd_conversion.cc
+++ b/components/printing/common/cloud_print_cdd_conversion.cc
@@ -6,7 +6,6 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 
@@ -312,7 +311,7 @@ void StoreMarginsUnique(const printing::PaperMargins& margins_um,
   printer::Margins printer_margins(
       margins_um.top_margin_um, margins_um.right_margin_um,
       margins_um.bottom_margin_um, margins_um.left_margin_um);
-  if (!std::ranges::contains(margins_storage, printer_margins)) {
+  if (!base::Contains(margins_storage, printer_margins)) {
     margins_storage.emplace_back(std::move(printer_margins));
   }
 }
diff --git a/components/privacy_sandbox/privacy_sandbox_attestations/privacy_sandbox_attestations.cc b/components/privacy_sandbox/privacy_sandbox_attestations/privacy_sandbox_attestations.cc
index e3aa04addb..b132c2cc46 100644
--- a/components/privacy_sandbox/privacy_sandbox_attestations/privacy_sandbox_attestations.cc
+++ b/components/privacy_sandbox/privacy_sandbox_attestations/privacy_sandbox_attestations.cc
@@ -4,7 +4,6 @@
 
 #include "components/privacy_sandbox/privacy_sandbox_attestations/privacy_sandbox_attestations.h"
 
-#include <algorithm>
 #include <ios>
 #include <memory>
 #include <optional>
@@ -15,6 +14,7 @@
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -390,8 +390,7 @@ void PrivacySandboxAttestations::AddOverride(const net::SchemefulSite& site) {
 
 bool PrivacySandboxAttestations::IsOverridden(
     const net::SchemefulSite& site) const {
-  return IsOverriddenByFlags(site) ||
-         std::ranges::contains(overridden_sites_, site);
+  return IsOverriddenByFlags(site) || base::Contains(overridden_sites_, site);
 }
 
 void PrivacySandboxAttestations::SetAllPrivacySandboxAttestedForTesting(
diff --git a/components/privacy_sandbox/privacy_sandbox_settings_impl.cc b/components/privacy_sandbox/privacy_sandbox_settings_impl.cc
index 10b65eef66..4806d5d7c2 100644
--- a/components/privacy_sandbox/privacy_sandbox_settings_impl.cc
+++ b/components/privacy_sandbox/privacy_sandbox_settings_impl.cc
@@ -9,6 +9,7 @@
 #include <cstdint>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/json/values_util.h"
 #include "base/metrics/field_trial_params.h"
@@ -292,14 +293,14 @@ bool PrivacySandboxSettingsImpl::IsTopicAllowed(const CanonicalTopic& topic) {
     }
 
     if ((topic.topic_id() == blocked_topic->topic_id()) ||
-        (std::ranges::contains(ancestor_topics, blocked_topic->topic_id()))) {
+        (base::Contains(ancestor_topics, blocked_topic->topic_id()))) {
       return false;
     }
   }
 
   for (browsing_topics::Topic blocked_topic_id : GetFinchDisabledTopics()) {
     if ((topic.topic_id() == blocked_topic_id) ||
-        (std::ranges::contains(ancestor_topics, blocked_topic_id))) {
+        (base::Contains(ancestor_topics, blocked_topic_id))) {
       return false;
     }
   }
diff --git a/components/proto_extras/proto_extras_plugin.cc b/components/proto_extras/proto_extras_plugin.cc
index bd8729ec64..8b0e5a77b8 100644
--- a/components/proto_extras/proto_extras_plugin.cc
+++ b/components/proto_extras/proto_extras_plugin.cc
@@ -5,6 +5,7 @@
 #include <string>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/files/file_path.h"
 #include "base/logging.h"
@@ -403,13 +404,14 @@ class ProtoExtrasGenerator : public google::protobuf::compiler::CodeGenerator {
     CHECK(file);
 
     ProtoExtrasGeneratorOptions generator_options{
-        .generate_to_value_serialization =
-            command_line_options.contains("generate_to_value_serialization"),
+        .generate_to_value_serialization = base::Contains(
+            command_line_options, "generate_to_value_serialization"),
         .generate_stream_operator =
-            command_line_options.contains("generate_stream_operator"),
-        .generate_equality = command_line_options.contains("generate_equality"),
+            base::Contains(command_line_options, "generate_stream_operator"),
+        .generate_equality =
+            base::Contains(command_line_options, "generate_equality"),
         .protobuf_full_support =
-            command_line_options.contains("protobuf_full_support"),
+            base::Contains(command_line_options, "protobuf_full_support"),
     };
     // The current design of this library assumes that only one of the
     // serialization options is enabled.
diff --git a/components/proto_extras/proto_test_extras_plugin.cc b/components/proto_extras/proto_test_extras_plugin.cc
index 0b1b2ad9d6..97279c4042 100644
--- a/components/proto_extras/proto_test_extras_plugin.cc
+++ b/components/proto_extras/proto_test_extras_plugin.cc
@@ -6,6 +6,7 @@
 #include <string>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/logging.h"
 #include "base/memory/raw_ptr.h"
diff --git a/components/regional_capabilities/program_settings.cc b/components/regional_capabilities/program_settings.cc
index 7d49e2a8dd..3aa393c618 100644
--- a/components/regional_capabilities/program_settings.cc
+++ b/components/regional_capabilities/program_settings.cc
@@ -4,8 +4,7 @@
 
 #include "components/regional_capabilities/program_settings.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/notreached.h"
 #include "components/country_codes/country_codes.h"
@@ -110,8 +109,8 @@ bool IsInProgramRegion(Program program,
     return true;  // All countries can be associated with the default program.
   }
 
-  return std::ranges::contains(
-      GetSettingsForProgram(program).associated_countries, country_id);
+  return base::Contains(GetSettingsForProgram(program).associated_countries,
+                        country_id);
 }
 
 bool IsClientCompatibleWithProgram(Program program) {
diff --git a/components/regional_capabilities/regional_capabilities_service.cc b/components/regional_capabilities/regional_capabilities_service.cc
index a259c9419f..26ebc85c3c 100644
--- a/components/regional_capabilities/regional_capabilities_service.cc
+++ b/components/regional_capabilities/regional_capabilities_service.cc
@@ -4,7 +4,6 @@
 
 #include "components/regional_capabilities/regional_capabilities_service.h"
 
-#include <algorithm>
 #include <optional>
 #include <utility>
 #include <variant>
@@ -13,6 +12,7 @@
 #include "base/callback_list.h"
 #include "base/check_deref.h"
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -376,8 +376,8 @@ bool RegionalCapabilitiesService::
     return true;
   }
 
-  if (!std::ranges::contains(GetActiveProgramSettings().associated_countries,
-                             client_->GetVariationsLatestCountryId())) {
+  if (!base::Contains(GetActiveProgramSettings().associated_countries,
+                      client_->GetVariationsLatestCountryId())) {
     return false;
   }
 
@@ -391,8 +391,8 @@ bool RegionalCapabilitiesService::
 
 bool RegionalCapabilitiesService::CanRecordDisplayStateForCountry(
     CountryId display_state_country_id) {
-  if (!std::ranges::contains(GetActiveProgramSettings().associated_countries,
-                             display_state_country_id)) {
+  if (!base::Contains(GetActiveProgramSettings().associated_countries,
+                      display_state_country_id)) {
     // Choice screen completions happen in context of a given regional program.
     // Based on the client state, the active program might change across
     // sessions. Since the metrics upload get tagged with the active program
diff --git a/components/reporting/metrics/fakes/fake_reporting_settings.cc b/components/reporting/metrics/fakes/fake_reporting_settings.cc
index ad865008b4..405c9daeba 100644
--- a/components/reporting/metrics/fakes/fake_reporting_settings.cc
+++ b/components/reporting/metrics/fakes/fake_reporting_settings.cc
@@ -4,6 +4,7 @@
 
 #include "components/reporting/metrics/fakes/fake_reporting_settings.h"
 
+#include "base/containers/contains.h"
 #include "base/run_loop.h"
 #include "base/task/sequenced_task_runner.h"
 #include "base/values.h"
@@ -17,7 +18,7 @@ FakeReportingSettings::~FakeReportingSettings() = default;
 base::CallbackListSubscription FakeReportingSettings::AddSettingsObserver(
     const std::string& path,
     base::RepeatingClosure callback) {
-  if (!settings_callbacks_map_.contains(path)) {
+  if (!base::Contains(settings_callbacks_map_, path)) {
     settings_callbacks_map_[path] =
         std::make_unique<base::RepeatingClosureList>();
   }
@@ -33,17 +34,16 @@ bool FakeReportingSettings::PrepareTrustedValues(base::OnceClosure callback) {
 
 bool FakeReportingSettings::GetBoolean(const std::string& path,
                                        bool* out_value) const {
-  const auto iter = bool_map_.find(path);
-  if (iter == bool_map_.end()) {
+  if (!base::Contains(bool_map_, path)) {
     return false;
   }
-  *out_value = iter->second;
+  *out_value = bool_map_.at(path);
   return true;
 }
 
 bool FakeReportingSettings::GetInteger(const std::string& path,
                                        int* out_value) const {
-  if (!int_map_.contains(path)) {
+  if (!base::Contains(int_map_, path)) {
     return false;
   }
   *out_value = int_map_.at(path);
@@ -61,7 +61,7 @@ bool FakeReportingSettings::GetList(const std::string& path,
 
 bool FakeReportingSettings::GetReportingEnabled(const std::string& path,
                                                 bool* out_value) const {
-  if (!reporting_enabled_map_.contains(path)) {
+  if (!base::Contains(reporting_enabled_map_, path)) {
     return false;
   }
   *out_value = reporting_enabled_map_.at(path);
@@ -71,14 +71,14 @@ bool FakeReportingSettings::GetReportingEnabled(const std::string& path,
 void FakeReportingSettings::SetBoolean(const std::string& path,
                                        bool bool_value) {
   bool_map_[path] = bool_value;
-  if (settings_callbacks_map_.contains(path)) {
+  if (base::Contains(settings_callbacks_map_, path)) {
     settings_callbacks_map_.at(path)->Notify();
   }
 }
 
 void FakeReportingSettings::SetInteger(const std::string& path, int int_value) {
   int_map_[path] = int_value;
-  if (settings_callbacks_map_.contains(path)) {
+  if (base::Contains(settings_callbacks_map_, path)) {
     settings_callbacks_map_.at(path)->Notify();
   }
 }
@@ -86,7 +86,7 @@ void FakeReportingSettings::SetInteger(const std::string& path, int int_value) {
 void FakeReportingSettings::SetList(const std::string& path,
                                     const base::ListValue& list_value) {
   list_map_.insert_or_assign(path, list_value.Clone());
-  if (settings_callbacks_map_.contains(path)) {
+  if (base::Contains(settings_callbacks_map_, path)) {
     settings_callbacks_map_.at(path)->Notify();
   }
 }
@@ -94,7 +94,7 @@ void FakeReportingSettings::SetList(const std::string& path,
 void FakeReportingSettings::SetReportingEnabled(const std::string& path,
                                                 bool enabled_value) {
   reporting_enabled_map_[path] = enabled_value;
-  if (settings_callbacks_map_.contains(path)) {
+  if (base::Contains(settings_callbacks_map_, path)) {
     settings_callbacks_map_.at(path)->Notify();
   }
 }
diff --git a/components/safe_browsing/android/safe_browsing_api_handler_bridge.cc b/components/safe_browsing/android/safe_browsing_api_handler_bridge.cc
index 10e2042bbb..bfde8daac8 100644
--- a/components/safe_browsing/android/safe_browsing_api_handler_bridge.cc
+++ b/components/safe_browsing/android/safe_browsing_api_handler_bridge.cc
@@ -12,6 +12,7 @@
 #include "base/android/jni_string.h"
 #include "base/callback_list.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/heap_array.h"
 #include "base/feature_list.h"
diff --git a/components/safe_browsing/content/browser/async_check_tracker.cc b/components/safe_browsing/content/browser/async_check_tracker.cc
index 5e8d5f9adc..61d9b7c40e 100644
--- a/components/safe_browsing/content/browser/async_check_tracker.cc
+++ b/components/safe_browsing/content/browser/async_check_tracker.cc
@@ -124,7 +124,7 @@ void AsyncCheckTracker::PendingCheckerCompleted(
            << " proceed: " << result.proceed
            << " has_post_commit_interstitial_skipped: "
            << result.has_post_commit_interstitial_skipped;
-  if (!pending_checkers_.contains(navigation_id)) {
+  if (!base::Contains(pending_checkers_, navigation_id)) {
     return;
   }
   if (!result.proceed) {
@@ -157,12 +157,12 @@ void AsyncCheckTracker::PendingCheckerCompleted(
 }
 
 bool AsyncCheckTracker::IsNavigationPending(int64_t navigation_id) {
-  return !committed_navigation_timestamps_.contains(navigation_id);
+  return !base::Contains(committed_navigation_timestamps_, navigation_id);
 }
 
 std::optional<base::TimeTicks>
 AsyncCheckTracker::GetNavigationCommittedTimestamp(int64_t navigation_id) {
-  if (!committed_navigation_timestamps_.contains(navigation_id)) {
+  if (!base::Contains(committed_navigation_timestamps_, navigation_id)) {
     return std::nullopt;
   }
   return committed_navigation_timestamps_[navigation_id];
@@ -250,7 +250,7 @@ void AsyncCheckTracker::DisplayBlockingPage(UnsafeResource resource) {
 }
 
 void AsyncCheckTracker::MaybeDeleteChecker(int64_t navigation_id) {
-  if (!pending_checkers_.contains(navigation_id)) {
+  if (!base::Contains(pending_checkers_, navigation_id)) {
     return;
   }
   pending_checkers_[navigation_id].reset();
diff --git a/components/safe_browsing/content/browser/base_ui_manager.cc b/components/safe_browsing/content/browser/base_ui_manager.cc
index 42a4a68a7c..71d0924dd9 100644
--- a/components/safe_browsing/content/browser/base_ui_manager.cc
+++ b/components/safe_browsing/content/browser/base_ui_manager.cc
@@ -143,7 +143,7 @@ class AllowlistUrlSet : public content::WebContentsUserData<AllowlistUrlSet> {
       return;
     }
     if (navigation_id.has_value()) {
-      if (pending_navigation_ids_.contains(navigation_id.value())) {
+      if (base::Contains(pending_navigation_ids_, navigation_id.value())) {
         // Do not add URL entry for the same navigation id in
         // |pending_allowlisted_entries_| more than once. Otherwise, the
         // security indicator may not be cleared properly when navigating away.
@@ -380,9 +380,9 @@ void BaseUIManager::DisplayBlockingPage(const UnsafeResource& resource) {
   //    blocking page at the same time, or
   // 2. The async check finishes between WillProcessResponse and
   //    DidFinishNavigation.
-  bool already_reported =
-      resource.navigation_id.has_value() &&
-      report_sent_navigation_ids_.contains(resource.navigation_id.value());
+  bool already_reported = resource.navigation_id.has_value() &&
+                          base::Contains(report_sent_navigation_ids_,
+                                         resource.navigation_id.value());
   if (resource.threat_type != SB_THREAT_TYPE_SAFE &&
       resource.threat_type != SB_THREAT_TYPE_BILLING &&
       resource.threat_type != SB_THREAT_TYPE_MANAGED_POLICY_BLOCK &&
diff --git a/components/safe_browsing/content/browser/client_side_detection_host.cc b/components/safe_browsing/content/browser/client_side_detection_host.cc
index cb97c340cd..192fa28959 100644
--- a/components/safe_browsing/content/browser/client_side_detection_host.cc
+++ b/components/safe_browsing/content/browser/client_side_detection_host.cc
@@ -4,7 +4,6 @@
 
 #include "components/safe_browsing/content/browser/client_side_detection_host.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <utility>
@@ -945,9 +944,9 @@ void ClientSideDetectionHost::OnPromptAdded() {
       permissions::PermissionRequestManager::FromWebContents(web_contents());
   CHECK(permission_request_manager);
 
-  if (std::ranges::contains(permission_request_manager->Requests(),
-                            permissions::RequestType::kNotifications,
-                            &permissions::PermissionRequest::request_type)) {
+  if (base::Contains(permission_request_manager->Requests(),
+                     permissions::RequestType::kNotifications,
+                     &permissions::PermissionRequest::request_type)) {
     MaybeStartPreClassification(
         ClientSideDetectionType::NOTIFICATION_PERMISSION_PROMPT);
   }
diff --git a/components/safe_browsing/content/browser/client_side_detection_service.cc b/components/safe_browsing/content/browser/client_side_detection_service.cc
index 23b084f30e..285fa4e911 100644
--- a/components/safe_browsing/content/browser/client_side_detection_service.cc
+++ b/components/safe_browsing/content/browser/client_side_detection_service.cc
@@ -9,6 +9,7 @@
 
 #include "base/callback_list.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/queue.h"
 #include "base/files/file.h"
 #include "base/files/file_path.h"
@@ -222,7 +223,7 @@ void ClientSideDetectionService::OnURLLoaderComplete(
       "SBClientPhishing.NetworkResult2", url_loader->NetError(),
       response_code.has_value() ? response_code.value() : 0);
 
-  DCHECK(client_phishing_reports_.contains(url_loader));
+  DCHECK(base::Contains(client_phishing_reports_, url_loader));
   HandlePhishingVerdict(url_loader, url_loader->GetFinalURL(),
                         url_loader->NetError(), response_code,
                         std::move(response_body).value_or(""));
diff --git a/components/safe_browsing/content/browser/notification_content_detection/mock_safe_browsing_database_manager.cc b/components/safe_browsing/content/browser/notification_content_detection/mock_safe_browsing_database_manager.cc
index 0011212c45..e9934a6ae2 100644
--- a/components/safe_browsing/content/browser/notification_content_detection/mock_safe_browsing_database_manager.cc
+++ b/components/safe_browsing/content/browser/notification_content_detection/mock_safe_browsing_database_manager.cc
@@ -4,6 +4,7 @@
 
 #include "components/safe_browsing/content/browser/notification_content_detection/mock_safe_browsing_database_manager.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 
 namespace safe_browsing {
@@ -17,8 +18,8 @@ void MockSafeBrowsingDatabaseManager::CheckUrlForHighConfidenceAllowlist(
     const GURL& gurl,
     CheckUrlForHighConfidenceAllowlistCallback callback) {
   std::string url = gurl.spec();
-  DCHECK(urls_allowlist_match_.contains(url));
-  if (delayed_url_callbacks_.contains(url)) {
+  DCHECK(base::Contains(urls_allowlist_match_, url));
+  if (base::Contains(delayed_url_callbacks_, url)) {
     delayed_url_callbacks_[url] = std::move(callback);
   } else {
     std::move(callback).Run(urls_allowlist_match_[url],
@@ -40,8 +41,8 @@ void MockSafeBrowsingDatabaseManager::SetCallbackToDelayed(const GURL& gurl) {
 
 void MockSafeBrowsingDatabaseManager::RestartDelayedCallback(const GURL& gurl) {
   std::string url = gurl.spec();
-  DCHECK(urls_allowlist_match_.contains(url));
-  DCHECK(delayed_url_callbacks_.contains(url));
+  DCHECK(base::Contains(urls_allowlist_match_, url));
+  DCHECK(base::Contains(delayed_url_callbacks_, url));
   DCHECK(delayed_url_callbacks_[url]);
   std::move(delayed_url_callbacks_[url])
       .Run(urls_allowlist_match_[url], /*logging_details=*/std::nullopt);
diff --git a/components/safe_browsing/content/browser/safe_browsing_navigation_observer_manager.cc b/components/safe_browsing/content/browser/safe_browsing_navigation_observer_manager.cc
index ac8110a000..c541b3c8f1 100644
--- a/components/safe_browsing/content/browser/safe_browsing_navigation_observer_manager.cc
+++ b/components/safe_browsing/content/browser/safe_browsing_navigation_observer_manager.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/metrics/histogram_functions.h"
@@ -305,7 +306,7 @@ void NavigationEventList::RecordPendingNavigationEvent(
 void NavigationEventList::AddRedirectUrlToPendingNavigationEvent(
     content::NavigationHandle* navigation_handle,
     const GURL& server_redirect_url) {
-  if (!pending_navigation_events_.contains(navigation_handle)) {
+  if (!base::Contains(pending_navigation_events_, navigation_handle)) {
     return;
   }
   pending_navigation_events_[navigation_handle]->server_redirect_urls.push_back(
@@ -314,7 +315,7 @@ void NavigationEventList::AddRedirectUrlToPendingNavigationEvent(
 
 void NavigationEventList::RemovePendingNavigationEvent(
     content::NavigationHandle* navigation_handle) {
-  if (pending_navigation_events_.contains(navigation_handle)) {
+  if (base::Contains(pending_navigation_events_, navigation_handle)) {
     pending_navigation_events_.erase(navigation_handle);
   }
 }
@@ -929,8 +930,8 @@ void SafeBrowsingNavigationObserverManager::MaybeAddToReferrerChain(
     }
   }
   // Update the referrer entry if we got here from a Push notification.
-  if (notification_navigation_events_.contains(
-          nav_event->original_request_url) &&
+  if (base::Contains(notification_navigation_events_,
+                     nav_event->original_request_url) &&
       (nav_event->navigation_initiation ==
            ReferrerChainEntry::RENDERER_INITIATED_WITHOUT_USER_GESTURE ||
        nav_event->navigation_initiation ==
diff --git a/components/safe_browsing/content/browser/threat_details.cc b/components/safe_browsing/content/browser/threat_details.cc
index b88f4a6911..b0d6932883 100644
--- a/components/safe_browsing/content/browser/threat_details.cc
+++ b/components/safe_browsing/content/browser/threat_details.cc
@@ -14,6 +14,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -182,7 +183,7 @@ void TrimElements(const std::set<int> target_ids,
     // Otherwise, insert the parent ID into the list of ids to keep. This will
     // capture the parent and siblings of the target element, as well as each of
     // their children.
-    if (!std::ranges::contains(element_ids_to_keep, parent_id)) {
+    if (!base::Contains(element_ids_to_keep, parent_id)) {
       element_ids_to_keep.push_back(parent_id);
 
       // Check if this element has a resource. If so, remember to also keep the
@@ -225,12 +226,12 @@ void TrimElements(const std::set<int> target_ids,
     const HTMLElement& element = *element_iter->second;
 
     // Delete any elements that we do not want to keep.
-    if (!std::ranges::contains(element_ids_to_keep, element.id())) {
+    if (!base::Contains(element_ids_to_keep, element.id())) {
       // If this element has a resource then maybe delete the resouce too. Some
       // resources may be shared between kept and trimmed elements, and those
       // ones should not be deleted.
       if (element.has_resource_id() &&
-          !std::ranges::contains(kept_resource_ids, element.resource_id())) {
+          !base::Contains(kept_resource_ids, element.resource_id())) {
         const std::string& resource_url =
             resource_id_to_url[element.resource_id()];
         resources->erase(resource_url);
@@ -800,7 +801,7 @@ bool ThreatDetails::ShouldFillReferrerChain() {
       base::MakeFixedFlatSet<ClientSafeBrowsingReportRequest::ReportType>(
           {ClientSafeBrowsingReportRequest::URL_SUSPICIOUS,
            ClientSafeBrowsingReportRequest::APK_DOWNLOAD});
-  return valid_report_types.contains(report_->type());
+  return base::Contains(valid_report_types, report_->type());
 }
 
 void ThreatDetails::FillReferrerChain(
@@ -821,7 +822,7 @@ bool ThreatDetails::ShouldFillInterstitialInteractions() {
       base::MakeFixedFlatSet<ClientSafeBrowsingReportRequest::ReportType>(
           {ClientSafeBrowsingReportRequest::URL_PHISHING,
            ClientSafeBrowsingReportRequest::URL_CLIENT_SIDE_PHISHING});
-  return valid_report_types.contains(report_->type());
+  return base::Contains(valid_report_types, report_->type());
 }
 
 void ThreatDetails::MaybeAttachThreatDetailsAndLaunchSurvey() {
diff --git a/components/safe_browsing/content/browser/triggers/trigger_manager.cc b/components/safe_browsing/content/browser/triggers/trigger_manager.cc
index 33700c7457..7847c1d77e 100644
--- a/components/safe_browsing/content/browser/triggers/trigger_manager.cc
+++ b/components/safe_browsing/content/browser/triggers/trigger_manager.cc
@@ -4,6 +4,7 @@
 
 #include "components/safe_browsing/content/browser/triggers/trigger_manager.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
 #include "base/metrics/histogram_functions.h"
@@ -249,7 +250,7 @@ TriggerManager::FinishCollectingThreatDetails(
   bool should_send_report =
       CanSendReport(data_collection_permissions, trigger_type);
   bool has_threat_details_in_map =
-      data_collectors_map_.contains(web_contents_key);
+      base::Contains(data_collectors_map_, web_contents_key);
 
   if (should_send_report &&
       trigger_type == TriggerType::SECURITY_INTERSTITIAL) {
@@ -313,7 +314,7 @@ TriggerManager::FinishCollectingThreatDetails(
 void TriggerManager::ThreatDetailsDone(WebContentsKey web_contents_key) {
   DCHECK(content::BrowserThread::CurrentlyOn(content::BrowserThread::UI));
   // Clean up the ThreatDetailsdata collector on the specified tab.
-  if (!data_collectors_map_.contains(web_contents_key)) {
+  if (!base::Contains(data_collectors_map_, web_contents_key)) {
     return;
   }
 
@@ -324,7 +325,7 @@ void TriggerManager::ThreatDetailsDone(WebContentsKey web_contents_key) {
 void TriggerManager::WebContentsDestroyed(content::WebContents* web_contents) {
   DCHECK(content::BrowserThread::CurrentlyOn(content::BrowserThread::UI));
   WebContentsKey key = GetWebContentsKey(web_contents);
-  if (!data_collectors_map_.contains(key)) {
+  if (!base::Contains(data_collectors_map_, key)) {
     return;
   }
   data_collectors_map_.erase(key);
diff --git a/components/safe_browsing/content/browser/triggers/trigger_throttler.cc b/components/safe_browsing/content/browser/triggers/trigger_throttler.cc
index c8c95750fc..9dbbd421bc 100644
--- a/components/safe_browsing/content/browser/triggers/trigger_throttler.cc
+++ b/components/safe_browsing/content/browser/triggers/trigger_throttler.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_split.h"
@@ -85,7 +86,7 @@ bool TriggerThrottler::TriggerCanFire(const TriggerType trigger_type) const {
 
   // Other triggers are capped, see how many times this trigger has already
   // fired.
-  if (!trigger_events_.contains(trigger_type)) {
+  if (!base::Contains(trigger_events_, trigger_type)) {
     return true;
   }
 
diff --git a/components/safe_browsing/core/browser/db/v4_get_hash_protocol_manager.cc b/components/safe_browsing/core/browser/db/v4_get_hash_protocol_manager.cc
index 561e4cd3ab..5c43467c0a 100644
--- a/components/safe_browsing/core/browser/db/v4_get_hash_protocol_manager.cc
+++ b/components/safe_browsing/core/browser/db/v4_get_hash_protocol_manager.cc
@@ -4,13 +4,13 @@
 
 #include "components/safe_browsing/core/browser/db/v4_get_hash_protocol_manager.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <string>
 #include <utility>
 
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
 #include "base/metrics/histogram_functions.h"
@@ -537,7 +537,7 @@ void V4GetHashProtocolManager::OnFullHashForApi(
   ThreatMetadata md;
   for (const FullHashInfo& full_hash_info : full_hash_infos) {
     DCHECK_EQ(GetChromeUrlApiId(), full_hash_info.list_id);
-    DCHECK(std::ranges::contains(full_hashes, full_hash_info.full_hash));
+    DCHECK(base::Contains(full_hashes, full_hash_info.full_hash));
     md.api_permissions.insert(full_hash_info.metadata.api_permissions.begin(),
                               full_hash_info.metadata.api_permissions.end());
   }
@@ -591,10 +591,9 @@ bool V4GetHashProtocolManager::ParseHashResponse(
 
     ListIdentifier list_id(match.platform_type(), match.threat_entry_type(),
                            match.threat_type());
-    if (!std::ranges::contains(platform_types_, list_id.platform_type()) ||
-        !std::ranges::contains(threat_entry_types_,
-                               list_id.threat_entry_type()) ||
-        !std::ranges::contains(threat_types_, list_id.threat_type())) {
+    if (!base::Contains(platform_types_, list_id.platform_type()) ||
+        !base::Contains(threat_entry_types_, list_id.threat_entry_type()) ||
+        !base::Contains(threat_types_, list_id.threat_type())) {
       // The server may send a ThreatMatch response for lists that we didn't ask
       // for so ignore those ThreatMatch responses.
       continue;
diff --git a/components/safe_browsing/core/browser/db/v4_local_database_manager.cc b/components/safe_browsing/core/browser/db/v4_local_database_manager.cc
index f4f60dd65a..2f5fa0954d 100644
--- a/components/safe_browsing/core/browser/db/v4_local_database_manager.cc
+++ b/components/safe_browsing/core/browser/db/v4_local_database_manager.cc
@@ -10,6 +10,7 @@
 
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
@@ -705,8 +706,8 @@ void V4LocalDatabaseManager::GetArtificialPrefixMatches(
           artificial_store_and_hash_prefix.hash_prefix;
       DCHECK_EQ(crypto::kSHA256Length, artificial_full_hash.size());
       if (artificial_full_hash == full_hash &&
-          check->stores_to_check.contains(
-              artificial_store_and_hash_prefix.list_id)) {
+          base::Contains(check->stores_to_check,
+                         artificial_store_and_hash_prefix.list_id)) {
         (check->artificial_full_hash_to_store_and_hash_prefixes)[full_hash] = {
             artificial_store_and_hash_prefix};
       }
diff --git a/components/safe_browsing/core/browser/hashprefix_realtime/hash_realtime_service.cc b/components/safe_browsing/core/browser/hashprefix_realtime/hash_realtime_service.cc
index 5622b30243..6b5a50d0c4 100644
--- a/components/safe_browsing/core/browser/hashprefix_realtime/hash_realtime_service.cc
+++ b/components/safe_browsing/core/browser/hashprefix_realtime/hash_realtime_service.cc
@@ -4,9 +4,8 @@
 
 #include "components/safe_browsing/core/browser/hashprefix_realtime/hash_realtime_service.h"
 
-#include <algorithm>
-
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/escape.h"
@@ -50,7 +49,7 @@ SBThreatType MapFullHashDetailToSbThreatType(
   // Note that for hash-prefix real-time checks, there is no need to use
   // the FRAME_ONLY enum in the attributes field, because all the checks are for
   // frame URLs.
-  if (std::ranges::contains(detail.attributes(), V5::ThreatAttribute::CANARY)) {
+  if (base::Contains(detail.attributes(), V5::ThreatAttribute::CANARY)) {
     return SBThreatType::SB_THREAT_TYPE_SUSPICIOUS_SITE;
   }
   switch (detail.threat_type()) {
@@ -204,7 +203,7 @@ int HashRealTimeService::GetThreatSeverity(
     const V5::FullHash::FullHashDetail& detail) {
   // These values should be consistent with the ones in GetThreatSeverity in
   // v4_local_database_manager.cc.
-  if (std::ranges::contains(detail.attributes(), V5::ThreatAttribute::CANARY)) {
+  if (base::Contains(detail.attributes(), V5::ThreatAttribute::CANARY)) {
     // ThreatAttribute::CANARY should be equivalent to SUSPICIOUS.
     return 4;
   }
@@ -517,7 +516,8 @@ void HashRealTimeService::RemoveUnmatchedFullHashes(
       std::remove_if(
           mutable_full_hashes->begin(), mutable_full_hashes->end(),
           [requested_hash_prefixes_set](const V5::FullHash& full_hash) {
-            return !requested_hash_prefixes_set.contains(
+            return !base::Contains(
+                requested_hash_prefixes_set,
                 hash_realtime_utils::GetHashPrefix(full_hash.full_hash()));
           }),
       mutable_full_hashes->end());
diff --git a/components/safe_browsing/core/browser/realtime/policy_engine.cc b/components/safe_browsing/core/browser/realtime/policy_engine.cc
index e941538cfa..985f922935 100644
--- a/components/safe_browsing/core/browser/realtime/policy_engine.cc
+++ b/components/safe_browsing/core/browser/realtime/policy_engine.cc
@@ -4,8 +4,7 @@
 
 #include "components/safe_browsing/core/browser/realtime/policy_engine.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "build/build_config.h"
@@ -46,7 +45,7 @@ namespace safe_browsing {
 // static
 bool RealTimePolicyEngine::IsInExcludedCountry(
     const std::string& country_code) {
-  return std::ranges::contains(GetExcludedCountries(), country_code);
+  return base::Contains(GetExcludedCountries(), country_code);
 }
 
 // static
diff --git a/components/safe_browsing/core/browser/verdict_cache_manager.cc b/components/safe_browsing/core/browser/verdict_cache_manager.cc
index b3dfca951b..ccfa14d4a0 100644
--- a/components/safe_browsing/core/browser/verdict_cache_manager.cc
+++ b/components/safe_browsing/core/browser/verdict_cache_manager.cc
@@ -4,12 +4,12 @@
 
 #include "components/safe_browsing/core/browser/verdict_cache_manager.h"
 
-#include <algorithm>
 #include <optional>
 #include <string_view>
 
 #include "base/base64.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/notreached.h"
@@ -209,7 +209,7 @@ size_t GetHostDepth(const std::string& hostname) {
 bool PathVariantsMatchCacheExpression(
     const std::vector<std::string>& generated_paths,
     const std::string& cache_expression_path) {
-  return std::ranges::contains(generated_paths, cache_expression_path);
+  return base::Contains(generated_paths, cache_expression_path);
 }
 
 bool IsCacheExpired(int cache_creation_time, int cache_duration) {
@@ -648,7 +648,7 @@ void VerdictCacheManager::CacheRealTimeUrlVerdict(
     // For the same cache_expression, threat_info is in decreasing order of
     // severity. To avoid lower severity threat being overridden by higher one,
     // only store threat info that is first seen for a cache expression.
-    if (std::ranges::contains(visited_cache_expressions, cache_expression)) {
+    if (base::Contains(visited_cache_expressions, cache_expression)) {
       continue;
     }
 
@@ -788,7 +788,7 @@ ChromeUserPopulation::PageLoadToken VerdictCacheManager::CreatePageLoadToken(
 ChromeUserPopulation::PageLoadToken VerdictCacheManager::GetPageLoadToken(
     const GURL& url) {
   std::string hostname = url.GetHost();
-  if (!page_load_token_map_.contains(hostname)) {
+  if (!base::Contains(page_load_token_map_, hostname)) {
     return ChromeUserPopulation::PageLoadToken();
   }
 
diff --git a/components/safe_browsing/core/common/hashprefix_realtime/hash_realtime_utils.cc b/components/safe_browsing/core/common/hashprefix_realtime/hash_realtime_utils.cc
index 19b654187d..ffa1ebf038 100644
--- a/components/safe_browsing/core/common/hashprefix_realtime/hash_realtime_utils.cc
+++ b/components/safe_browsing/core/common/hashprefix_realtime/hash_realtime_utils.cc
@@ -4,9 +4,8 @@
 
 #include "components/safe_browsing/core/common/hashprefix_realtime/hash_realtime_utils.h"
 
-#include <algorithm>
-
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/strcat.h"
 #include "build/branding_buildflags.h"
@@ -40,7 +39,7 @@ bool CanCheckUrl(const GURL& url) {
 }
 
 bool IsHashDetailRelevant(const V5::FullHash::FullHashDetail& detail) {
-  if (std::ranges::contains(detail.attributes(), V5::ThreatAttribute::CANARY)) {
+  if (base::Contains(detail.attributes(), V5::ThreatAttribute::CANARY)) {
 #if BUILDFLAG(IS_IOS)
     // iOS doesn't support CANARY threat attribute.
     return false;
@@ -52,8 +51,7 @@ bool IsHashDetailRelevant(const V5::FullHash::FullHashDetail& detail) {
       // are not frame URLs), so only checking SOCIAL_ENGINEERING here.
       return false;
     }
-    if (std::ranges::contains(detail.attributes(),
-                              V5::ThreatAttribute::FRAME_ONLY)) {
+    if (base::Contains(detail.attributes(), V5::ThreatAttribute::FRAME_ONLY)) {
       // CANARY and FRAME_ONLY should not be set at the same time.
       return false;
     }
@@ -82,9 +80,8 @@ bool IsHashRealTimeLookupEligibleInSession() {
 }
 bool IsHashRealTimeLookupEligibleInLocation(
     std::optional<std::string> latest_country) {
-  return (
-      !latest_country.has_value() ||
-      !std::ranges::contains(GetExcludedCountries(), latest_country.value()));
+  return (!latest_country.has_value() ||
+          !base::Contains(GetExcludedCountries(), latest_country.value()));
 }
 bool IsHashRealTimeLookupEligibleInSessionAndLocation(
     std::optional<std::string> latest_country) {
diff --git a/components/saved_tab_groups/internal/tab_group_sync_service_impl.cc b/components/saved_tab_groups/internal/tab_group_sync_service_impl.cc
index c61d51afa5..5ee091015c 100644
--- a/components/saved_tab_groups/internal/tab_group_sync_service_impl.cc
+++ b/components/saved_tab_groups/internal/tab_group_sync_service_impl.cc
@@ -4,11 +4,11 @@
 
 #include "components/saved_tab_groups/internal/tab_group_sync_service_impl.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/observer_list.h"
@@ -917,9 +917,9 @@ bool TabGroupSyncServiceImpl::ShouldExposeSavedTabGroupInList(
     }
   }
 
-  if (std::ranges::contains(
-          shared_tab_groups_waiting_for_collaboration_, group.saved_guid(),
-          [](const auto& entry) { return std::get<1>(entry); })) {
+  if (base::Contains(shared_tab_groups_waiting_for_collaboration_,
+                     group.saved_guid(),
+                     [](const auto& entry) { return std::get<1>(entry); })) {
     // The shared tab group should not be returned while its collaboration is
     // not available.
     return false;
diff --git a/components/search_engines/search_engine_choice/search_engine_choice_utils.cc b/components/search_engines/search_engine_choice/search_engine_choice_utils.cc
index 50ada3a492..b388252d25 100644
--- a/components/search_engines/search_engine_choice/search_engine_choice_utils.cc
+++ b/components/search_engines/search_engine_choice/search_engine_choice_utils.cc
@@ -4,7 +4,6 @@
 
 #include "components/search_engines/search_engine_choice/search_engine_choice_utils.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <string>
@@ -13,6 +12,7 @@
 #include "base/check_is_test.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/to_vector.h"
 #include "base/feature_list.h"
@@ -180,9 +180,9 @@ ChoiceScreenData::ChoiceScreenData(
           current_default_to_highlight != nullptr,
           /*includes_non_regional_set_engine=*/
           current_default_to_highlight != nullptr &&
-              !std::ranges::contains(search_engines_,
-                                     current_default_to_highlight,
-                                     &std::unique_ptr<TemplateURL>::get))),
+              !base::Contains(search_engines_,
+                              current_default_to_highlight,
+                              &std::unique_ptr<TemplateURL>::get))),
       current_default_to_highlight_(current_default_to_highlight) {}
 
 ChoiceScreenData::~ChoiceScreenData() = default;
diff --git a/components/search_engines/template_url.cc b/components/search_engines/template_url.cc
index c8eaa35cc4..8ac853df38 100644
--- a/components/search_engines/template_url.cc
+++ b/components/search_engines/template_url.cc
@@ -16,6 +16,7 @@
 #include "base/check_op.h"
 #include "base/command_line.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
 #include "base/format_macros.h"
@@ -2053,7 +2054,7 @@ bool TemplateURL::KeepSearchTermsInURL(const GURL& url,
   std::vector<std::string> query_params;
   if (keep_search_intent_params && !data().search_intent_params.empty()) {
     for (net::QueryIterator it(url); !it.IsAtEnd(); it.Advance()) {
-      if (!std::ranges::contains(data().search_intent_params, it.GetKey())) {
+      if (!base::Contains(data().search_intent_params, it.GetKey())) {
         continue;
       }
       query_params.push_back(base::StrCat({it.GetKey(), "=", it.GetValue()}));
@@ -2131,7 +2132,7 @@ void TemplateURL::EncodeSearchTerms(
     std::u16string* encoded_terms,
     std::u16string* encoded_original_query) const {
   std::vector<std::string> encodings(input_encodings());
-  if (!std::ranges::contains(encodings, "UTF-8")) {
+  if (!base::Contains(encodings, "UTF-8")) {
     encodings.push_back("UTF-8");
   }
   for (auto i = encodings.begin(); i != encodings.end(); ++i) {
diff --git a/components/security_interstitials/content/security_interstitial_tab_helper.cc b/components/security_interstitials/content/security_interstitial_tab_helper.cc
index 429d96ef3d..421219f026 100644
--- a/components/security_interstitials/content/security_interstitial_tab_helper.cc
+++ b/components/security_interstitials/content/security_interstitial_tab_helper.cc
@@ -143,7 +143,8 @@ void SecurityInterstitialTabHelper::BindInterstitialCommands(
 
 bool SecurityInterstitialTabHelper::IsInterstitialPendingForNavigation(
     int64_t navigation_id) const {
-  return blocking_documents_for_pending_navigations_.contains(navigation_id);
+  return base::Contains(blocking_documents_for_pending_navigations_,
+                        navigation_id);
 }
 
 bool SecurityInterstitialTabHelper::ShouldDisplayURL() const {
@@ -164,8 +165,8 @@ bool SecurityInterstitialTabHelper::HasPendingOrActiveInterstitial() const {
 
 bool SecurityInterstitialTabHelper::IsInterstitialCommittedForFrame(
     content::FrameTreeNodeId frame_tree_node_id) const {
-  return blocking_documents_for_committed_navigations_.contains(
-      frame_tree_node_id);
+  return base::Contains(blocking_documents_for_committed_navigations_,
+                        frame_tree_node_id);
 }
 
 security_interstitials::SecurityInterstitialPage*
diff --git a/components/security_interstitials/content/stateful_ssl_host_state_delegate.cc b/components/security_interstitials/content/stateful_ssl_host_state_delegate.cc
index 66c6e6ad3f..33243959e9 100644
--- a/components/security_interstitials/content/stateful_ssl_host_state_delegate.cc
+++ b/components/security_interstitials/content/stateful_ssl_host_state_delegate.cc
@@ -13,6 +13,7 @@
 #include <utility>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -180,8 +181,8 @@ StatefulSSLHostStateDelegate::QueryPolicy(
     }
     AllowedCert allowed_cert =
         AllowedCert(GetKey(cert, error), storage_partition->GetPath());
-    if (allowed_certs_for_non_default_storage_partitions_[host].contains(
-            allowed_cert)) {
+    if (base::Contains(allowed_certs_for_non_default_storage_partitions_[host],
+                       allowed_cert)) {
       return ALLOWED;
     }
     return DENIED;
@@ -232,9 +233,9 @@ bool StatefulSSLHostStateDelegate::DidHostRunInsecureContent(
     InsecureContentType content_type) {
   switch (content_type) {
     case MIXED_CONTENT:
-      return ran_mixed_content_hosts_.contains(host);
+      return base::Contains(ran_mixed_content_hosts_, host);
     case CERT_ERRORS_CONTENT:
-      return ran_content_with_cert_errors_hosts_.contains(host);
+      return base::Contains(ran_content_with_cert_errors_hosts_, host);
   }
   NOTREACHED();
 }
@@ -399,7 +400,8 @@ bool StatefulSSLHostStateDelegate::HasCertAllowException(
     content::StoragePartition* storage_partition) {
   if (!storage_partition ||
       storage_partition != browser_context_->GetDefaultStoragePartition()) {
-    return allowed_certs_for_non_default_storage_partitions_.contains(host);
+    return base::Contains(allowed_certs_for_non_default_storage_partitions_,
+                          host);
   }
 
   GURL url = GetSecureGURLForHost(host);
diff --git a/components/security_interstitials/core/https_only_mode_allowlist.cc b/components/security_interstitials/core/https_only_mode_allowlist.cc
index 66597eba3c..0f60bdece2 100644
--- a/components/security_interstitials/core/https_only_mode_allowlist.cc
+++ b/components/security_interstitials/core/https_only_mode_allowlist.cc
@@ -4,6 +4,7 @@
 
 #include "components/security_interstitials/core/https_only_mode_allowlist.h"
 
+#include "base/containers/contains.h"
 #include "base/json/values_util.h"
 #include "base/time/clock.h"
 #include "base/values.h"
@@ -75,8 +76,8 @@ bool HttpsOnlyModeAllowlist::IsHttpAllowedForHost(
     const std::string& host,
     bool is_nondefault_storage) const {
   if (is_nondefault_storage) {
-    return allowed_http_hosts_for_non_default_storage_partitions_.contains(
-        host);
+    return base::Contains(
+        allowed_http_hosts_for_non_default_storage_partitions_, host);
   }
 
   GURL url = GetSecureGURLForHost(host);
diff --git a/components/security_interstitials/core/https_only_mode_enforcelist.cc b/components/security_interstitials/core/https_only_mode_enforcelist.cc
index 2d237f92d9..2ba2d0f215 100644
--- a/components/security_interstitials/core/https_only_mode_enforcelist.cc
+++ b/components/security_interstitials/core/https_only_mode_enforcelist.cc
@@ -4,6 +4,7 @@
 
 #include "components/security_interstitials/core/https_only_mode_enforcelist.h"
 
+#include "base/containers/contains.h"
 #include "base/json/values_util.h"
 #include "base/time/clock.h"
 #include "base/values.h"
@@ -111,8 +112,8 @@ bool HttpsOnlyModeEnforcelist::IsEnforcedForUrl(
     return false;
   }
   if (is_nondefault_storage) {
-    return enforce_https_hosts_for_non_default_storage_partitions_.contains(
-        url.GetHost());
+    return base::Contains(
+        enforce_https_hosts_for_non_default_storage_partitions_, url.GetHost());
   }
 
   GURL secure_url = GetSecureGURLForHost(url.GetHost());
diff --git a/components/segmentation_platform/embedder/home_modules/home_modules_card_registry.cc b/components/segmentation_platform/embedder/home_modules/home_modules_card_registry.cc
index f0c23b38c6..8e2ded24e7 100644
--- a/components/segmentation_platform/embedder/home_modules/home_modules_card_registry.cc
+++ b/components/segmentation_platform/embedder/home_modules/home_modules_card_registry.cc
@@ -12,6 +12,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/strings/strcat.h"
 #include "base/strings/string_split.h"
diff --git a/components/segmentation_platform/internal/data_collection/training_data_collector_impl.cc b/components/segmentation_platform/internal/data_collection/training_data_collector_impl.cc
index 10480298b2..67cbcb7946 100644
--- a/components/segmentation_platform/internal/data_collection/training_data_collector_impl.cc
+++ b/components/segmentation_platform/internal/data_collection/training_data_collector_impl.cc
@@ -4,9 +4,9 @@
 
 #include "components/segmentation_platform/internal/data_collection/training_data_collector_impl.h"
 
-#include <algorithm>
 #include <cstdint>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
 #include "base/metrics/field_trial_params.h"
@@ -261,7 +261,7 @@ void TrainingDataCollectorImpl::OnHistogramSignalUpdated(
       // Process both enum histograms with their corresponding accepted enum ids
       // and value histograms with no enum ids.
       if (accepted_enum_ids.empty() ||
-          std::ranges::contains(accepted_enum_ids, sample)) {
+          base::Contains(accepted_enum_ids, sample)) {
         const SegmentInfo* info = segment_info_database_->GetCachedSegmentInfo(
             segment_id, model_source);
         OnUmaUpdatedReportForSegmentInfo(param, info);
diff --git a/components/segmentation_platform/internal/database/signal_sample_view.cc b/components/segmentation_platform/internal/database/signal_sample_view.cc
index bcc2f7bc17..f3aab88e31 100644
--- a/components/segmentation_platform/internal/database/signal_sample_view.cc
+++ b/components/segmentation_platform/internal/database/signal_sample_view.cc
@@ -4,7 +4,7 @@
 
 #include "components/segmentation_platform/internal/database/signal_sample_view.h"
 
-#include <algorithm>
+#include "base/containers/contains.h"
 
 namespace segmentation_platform {
 
@@ -12,7 +12,7 @@ bool SignalSampleView::Query::IsFeatureMatching(
     const SignalDatabase::DbEntry& sample) const {
   return sample.type == type && sample.name_hash == metric_hash &&
          start <= sample.time && end >= sample.time &&
-         (enum_ids.empty() || std::ranges::contains(enum_ids, sample.value));
+         (enum_ids.empty() || base::Contains(enum_ids, sample.value));
 }
 
 SignalSampleView::Query::Query(proto::SignalType type,
diff --git a/components/segmentation_platform/internal/database/signal_storage_config.cc b/components/segmentation_platform/internal/database/signal_storage_config.cc
index 59e0572562..e3da84bd8f 100644
--- a/components/segmentation_platform/internal/database/signal_storage_config.cc
+++ b/components/segmentation_platform/internal/database/signal_storage_config.cc
@@ -4,6 +4,7 @@
 
 #include "components/segmentation_platform/internal/database/signal_storage_config.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "components/segmentation_platform/internal/metadata/metadata_utils.h"
 #include "components/segmentation_platform/public/proto/model_metadata.pb.h"
@@ -285,8 +286,9 @@ void SignalStorageConfig::GetSignalsForCleanup(
     // include signals when not needed. For UMA signals, skip adding signals
     // that are used by any models.
     // TODO(ssid) : Handle this for UKM signals.
-    if (known_signals.contains(std::make_pair(signal_config.name_hash(),
-                                              signal_config.signal_type())) ||
+    if (base::Contains(known_signals,
+                       std::make_pair(signal_config.name_hash(),
+                                      signal_config.signal_type())) ||
         signal_config.signal_type() == proto::SignalType::UKM_EVENT) {
       continue;
     }
diff --git a/components/segmentation_platform/internal/database/test_segment_info_database.cc b/components/segmentation_platform/internal/database/test_segment_info_database.cc
index 1a9746ffaf..0c5b74f06b 100644
--- a/components/segmentation_platform/internal/database/test_segment_info_database.cc
+++ b/components/segmentation_platform/internal/database/test_segment_info_database.cc
@@ -7,6 +7,7 @@
 #include <optional>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "components/segmentation_platform/internal/metadata/metadata_writer.h"
 #include "components/segmentation_platform/internal/proto/model_prediction.pb.h"
 #include "components/segmentation_platform/public/proto/types.pb.h"
@@ -28,7 +29,7 @@ void TestSegmentInfoDatabase::GetSegmentInfoForSegments(
   auto result = std::make_unique<SegmentInfoDatabase::SegmentInfoList>();
   for (const auto& pair : segment_infos_) {
     if (pair.second.model_source() != ModelSource::DEFAULT_MODEL_SOURCE &&
-        segment_ids.contains(pair.first)) {
+        base::Contains(segment_ids, pair.first)) {
       result->emplace_back(pair.first, &pair.second);
     }
   }
@@ -40,7 +41,7 @@ TestSegmentInfoDatabase::GetSegmentInfoForBothModels(
     const base::flat_set<SegmentId>& segment_ids) {
   auto result = std::make_unique<SegmentInfoDatabase::SegmentInfoList>();
   for (const auto& pair : segment_infos_) {
-    if (segment_ids.contains(pair.first)) {
+    if (base::Contains(segment_ids, pair.first)) {
       result->emplace_back(pair.first, &pair.second);
     }
   }
diff --git a/components/segmentation_platform/internal/execution/mock_model_provider.cc b/components/segmentation_platform/internal/execution/mock_model_provider.cc
index 524ad2655a..c4a5ce1fe5 100644
--- a/components/segmentation_platform/internal/execution/mock_model_provider.cc
+++ b/components/segmentation_platform/internal/execution/mock_model_provider.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
 #include "components/segmentation_platform/public/model_provider.h"
@@ -64,7 +65,7 @@ std::unique_ptr<ModelProvider> TestModelProviderFactory::CreateProvider(
 
 std::unique_ptr<DefaultModelProvider>
 TestModelProviderFactory::CreateDefaultProvider(proto::SegmentId segment_id) {
-  if (!data_->segments_supporting_default_model.contains(segment_id)) {
+  if (!base::Contains(data_->segments_supporting_default_model, segment_id)) {
     return nullptr;
   }
 
diff --git a/components/segmentation_platform/internal/selection/segment_selector_impl.cc b/components/segmentation_platform/internal/selection/segment_selector_impl.cc
index bc85f6aa01..8dbf49f73b 100644
--- a/components/segmentation_platform/internal/selection/segment_selector_impl.cc
+++ b/components/segmentation_platform/internal/selection/segment_selector_impl.cc
@@ -4,6 +4,7 @@
 
 #include "components/segmentation_platform/internal/selection/segment_selector_impl.h"
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/logging.h"
 #include "base/task/single_thread_task_runner.h"
@@ -173,7 +174,7 @@ void SegmentSelectorImpl::OnModelExecutionCompleted(SegmentId segment_id) {
   DCHECK(segment_result_provider_);
 
   // If the |segment_id| is not in config, then skip any updates early.
-  if (!config_->segments.contains(segment_id))
+  if (!base::Contains(config_->segments, segment_id))
     return;
 
   if (!IsPreviousSelectionInvalid())
diff --git a/components/segmentation_platform/internal/signals/histogram_signal_handler.cc b/components/segmentation_platform/internal/signals/histogram_signal_handler.cc
index 590a793a3a..86198c600e 100644
--- a/components/segmentation_platform/internal/signals/histogram_signal_handler.cc
+++ b/components/segmentation_platform/internal/signals/histogram_signal_handler.cc
@@ -6,6 +6,7 @@
 
 #include <cstdint>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/metrics_hashes.h"
@@ -31,14 +32,14 @@ void HistogramSignalHandler::SetRelevantHistograms(
     const RelevantHistograms& histograms) {
   auto it = histogram_observers_.begin();
   while (it != histogram_observers_.end()) {
-    if (!histograms.contains(it->first)) {
+    if (!base::Contains(histograms, it->first)) {
       it = histogram_observers_.erase(it);
     } else {
       ++it;
     }
   }
   for (const auto& pair : histograms) {
-    if (histogram_observers_.contains(pair)) {
+    if (base::Contains(histogram_observers_, pair)) {
       continue;
     }
     const auto& histogram_name = pair.first;
diff --git a/components/services/app_service/public/cpp/app_registry_cache.cc b/components/services/app_service/public/cpp/app_registry_cache.cc
index 172928dece..277255af3e 100644
--- a/components/services/app_service/public/cpp/app_registry_cache.cc
+++ b/components/services/app_service/public/cpp/app_registry_cache.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "base/sequence_checker.h"
 #include "components/services/app_service/public/cpp/app.h"
@@ -68,7 +69,7 @@ std::vector<AppPtr> AppRegistryCache::GetAllApps() {
     AppUpdate::Merge(apps[apps.size() - 1].get(), delta);
   }
   for (const auto& d_iter : deltas_in_progress_) {
-    if (!states_.contains(d_iter.first)) {
+    if (!base::Contains(states_, d_iter.first)) {
       // Call AppUpdate::Merge to set the init value for the icon key's
       // `update_version` to keep the consistency as AppRegistryCache's
       // `states_`.
@@ -90,11 +91,11 @@ const std::set<AppType>& AppRegistryCache::InitializedAppTypes() const {
 }
 
 bool AppRegistryCache::IsAppTypeInitialized(apps::AppType app_type) const {
-  return initialized_app_types_.contains(app_type);
+  return base::Contains(initialized_app_types_, app_type);
 }
 
 bool AppRegistryCache::IsAppTypePublished(apps::AppType app_type) const {
-  return published_app_types_.contains(app_type);
+  return base::Contains(published_app_types_, app_type);
 }
 
 bool AppRegistryCache::IsAppInstalled(std::string_view app_id) const {
diff --git a/components/services/app_service/public/cpp/app_registry_cache.h b/components/services/app_service/public/cpp/app_registry_cache.h
index 57849fdd8a..01ab49d2fe 100644
--- a/components/services/app_service/public/cpp/app_registry_cache.h
+++ b/components/services/app_service/public/cpp/app_registry_cache.h
@@ -15,6 +15,7 @@
 
 #include "base/compiler_specific.h"
 #include "base/component_export.h"
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/observer_list.h"
 #include "base/observer_list_types.h"
diff --git a/components/services/app_service/public/cpp/instance_registry.cc b/components/services/app_service/public/cpp/instance_registry.cc
index 8343e73f9c..5c30e5ab77 100644
--- a/components/services/app_service/public/cpp/instance_registry.cc
+++ b/components/services/app_service/public/cpp/instance_registry.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/unguessable_token.h"
 #include "components/services/app_service/public/cpp/instance.h"
@@ -138,7 +139,7 @@ bool InstanceRegistry::Exists(const aura::Window* window) const {
 }
 
 bool InstanceRegistry::ContainsAppId(const std::string& app_id) const {
-  return app_id_to_instances_.contains(app_id);
+  return base::Contains(app_id_to_instances_, app_id);
 }
 
 void InstanceRegistry::DoOnInstance(InstancePtr delta) {
diff --git a/components/services/app_service/public/cpp/preferred_apps_impl.cc b/components/services/app_service/public/cpp/preferred_apps_impl.cc
index 111b858035..9028c5ab27 100644
--- a/components/services/app_service/public/cpp/preferred_apps_impl.cc
+++ b/components/services/app_service/public/cpp/preferred_apps_impl.cc
@@ -8,6 +8,7 @@
 #include <iterator>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -258,7 +259,7 @@ void PreferredAppsImpl::SetSupportedLinksPreferenceImpl(
     // can be notified correctly.
     for (auto& replaced_app_and_filters : replaced_apps) {
       const std::string& removed_app_id = replaced_app_and_filters.first;
-      bool first_removal_for_app = !removed.contains(app_id);
+      bool first_removal_for_app = !base::Contains(removed, app_id);
       bool did_replace_supported_link = std::ranges::any_of(
           replaced_app_and_filters.second,
           [&removed_app_id](const auto& filter) {
diff --git a/components/services/app_service/public/cpp/shortcut/shortcut_registry_cache.cc b/components/services/app_service/public/cpp/shortcut/shortcut_registry_cache.cc
index 6daf206271..7eacd05464 100644
--- a/components/services/app_service/public/cpp/shortcut/shortcut_registry_cache.cc
+++ b/components/services/app_service/public/cpp/shortcut/shortcut_registry_cache.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "components/services/app_service/public/cpp/shortcut/shortcut_update.h"
 
@@ -82,7 +83,7 @@ ShortcutView ShortcutRegistryCache::GetShortcut(const ShortcutId& shortcut_id) {
 
 bool ShortcutRegistryCache::HasShortcut(const ShortcutId& shortcut_id) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  return states_.contains(shortcut_id);
+  return base::Contains(states_, shortcut_id);
 }
 
 std::vector<ShortcutView> ShortcutRegistryCache::GetAllShortcuts() {
diff --git a/components/services/app_service/public/cpp/shortcut/shortcut_registry_cache.h b/components/services/app_service/public/cpp/shortcut/shortcut_registry_cache.h
index cf00906a1f..a06a4cfa19 100644
--- a/components/services/app_service/public/cpp/shortcut/shortcut_registry_cache.h
+++ b/components/services/app_service/public/cpp/shortcut/shortcut_registry_cache.h
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/component_export.h"
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "base/observer_list_types.h"
 #include "base/sequence_checker.h"
diff --git a/components/services/app_service/public/cpp/stub_icon_loader.cc b/components/services/app_service/public/cpp/stub_icon_loader.cc
index cd98c773f9..93c5128eb8 100644
--- a/components/services/app_service/public/cpp/stub_icon_loader.cc
+++ b/components/services/app_service/public/cpp/stub_icon_loader.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "ui/gfx/image/image_skia.h"
 #include "ui/gfx/image/image_skia_rep.h"
 
@@ -34,7 +35,7 @@ std::unique_ptr<IconLoader::Releaser> StubIconLoader::LoadIconFromIconKey(
     bool allow_placeholder_icon,
     apps::LoadIconCallback callback) {
   num_load_calls_++;
-  if (update_version_by_app_id_.contains(id)) {
+  if (base::Contains(update_version_by_app_id_, id)) {
     auto icon_value = std::make_unique<IconValue>();
     icon_value->icon_type = icon_type;
     icon_value->uncompressed =
diff --git a/components/services/print_compositor/print_compositor_impl.cc b/components/services/print_compositor/print_compositor_impl.cc
index 13f8478c3b..3305b7b509 100644
--- a/components/services/print_compositor/print_compositor_impl.cc
+++ b/components/services/print_compositor/print_compositor_impl.cc
@@ -8,6 +8,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/discardable_memory.h"
 #include "base/task/single_thread_task_runner.h"
@@ -157,7 +158,7 @@ PrintCompositorImpl::~PrintCompositorImpl() {
 
 void PrintCompositorImpl::NotifyUnavailableSubframe(uint64_t frame_guid) {
   // Add this frame into the map.
-  DCHECK(!frame_info_map_.contains(frame_guid));
+  DCHECK(!base::Contains(frame_info_map_, frame_guid));
   auto& frame_info =
       frame_info_map_.emplace(frame_guid, std::make_unique<FrameInfo>())
           .first->second;
@@ -181,7 +182,7 @@ void PrintCompositorImpl::AddSubframeContent(
   }
 
   // Add this frame and its serialized content.
-  DCHECK(!frame_info_map_.contains(frame_guid));
+  DCHECK(!base::Contains(frame_info_map_, frame_guid));
   frame_info_map_.emplace(frame_guid, std::make_unique<FrameInfo>(
                                           mapping.GetMemoryAsSpan<uint8_t>(),
                                           subframe_content_map));
@@ -197,7 +198,7 @@ void PrintCompositorImpl::AddSubframeContent(
   std::vector<uint64_t> pending_subframes;
   for (auto& subframe_content : subframe_content_map) {
     auto subframe_guid = subframe_content.second;
-    if (!frame_info_map_.contains(subframe_guid))
+    if (!base::Contains(frame_info_map_, subframe_guid))
       pending_subframes.push_back(subframe_guid);
   }
 
diff --git a/components/services/storage/dom_storage/leveldb/local_storage_leveldb.cc b/components/services/storage/dom_storage/leveldb/local_storage_leveldb.cc
index f5558b6805..3abd067350 100644
--- a/components/services/storage/dom_storage/leveldb/local_storage_leveldb.cc
+++ b/components/services/storage/dom_storage/leveldb/local_storage_leveldb.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_view_util.h"
 #include "base/types/expected_macros.h"
@@ -328,7 +329,7 @@ DbStatus LocalStorageLevelDB::DeleteStorageKeysFromSession(
     // A valid `map` must be in `storage_keys` and `kLocalStorageSessionId`.
     CHECK_EQ(map.session_ids().size(), 1u);
     CHECK_EQ(map.session_ids()[0], kLocalStorageSessionId);
-    DCHECK(std::ranges::contains(metadata_to_delete, map.storage_key()));
+    DCHECK(base::Contains(metadata_to_delete, map.storage_key()));
 
     DB_RETURN_IF_ERROR(batch->DeletePrefixed(GetMapPrefix(map.storage_key())));
   }
diff --git a/components/services/storage/dom_storage/leveldb/session_storage_leveldb.cc b/components/services/storage/dom_storage/leveldb/session_storage_leveldb.cc
index dccd106470..6a4b628a2e 100644
--- a/components/services/storage/dom_storage/leveldb/session_storage_leveldb.cc
+++ b/components/services/storage/dom_storage/leveldb/session_storage_leveldb.cc
@@ -4,12 +4,12 @@
 
 #include "components/services/storage/dom_storage/leveldb/session_storage_leveldb.h"
 
-#include <algorithm>
 #include <optional>
 #include <string>
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "base/strings/string_view_util.h"
@@ -252,7 +252,7 @@ DbStatus SessionStorageLevelDB::DeleteStorageKeysFromSession(
   for (const DomStorageDatabase::MapLocator& map : maps_to_delete) {
     // A valid `map` must be in `storage_keys` and `session_id`.
     CHECK(map.session_ids().empty());
-    DCHECK(std::ranges::contains(metadata_to_delete, map.storage_key()));
+    DCHECK(base::Contains(metadata_to_delete, map.storage_key()));
 
     DB_RETURN_IF_ERROR(
         batch->DeletePrefixed(GetMapPrefix(map.map_id().value())));
diff --git a/components/services/storage/dom_storage/local_storage_impl.cc b/components/services/storage/dom_storage/local_storage_impl.cc
index 0e959b6e8c..49cd454f01 100644
--- a/components/services/storage/dom_storage/local_storage_impl.cc
+++ b/components/services/storage/dom_storage/local_storage_impl.cc
@@ -15,6 +15,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/byte_size.h"
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
diff --git a/components/services/storage/dom_storage/session_storage_impl.cc b/components/services/storage/dom_storage/session_storage_impl.cc
index 9b0fa91647..746872f974 100644
--- a/components/services/storage/dom_storage/session_storage_impl.cc
+++ b/components/services/storage/dom_storage/session_storage_impl.cc
@@ -10,6 +10,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/ptr_util.h"
@@ -222,8 +223,8 @@ void SessionStorageImpl::CloneNamespace(
             clone_from_namespace_id);
         clone_from_ns->second->AddChildNamespaceWaitingForClone(
             clone_to_namespace_id);
-      } else if (metadata_.namespace_storage_key_map().contains(
-                     clone_from_namespace_id)) {
+      } else if (base::Contains(metadata_.namespace_storage_key_map(),
+                                clone_from_namespace_id)) {
         CHECK_EQ(connection_state_, CONNECTION_FINISHED,
                  base::NotFatalUntil::M146);
         // The namespace exists on disk but is not in-use, so do the appropriate
diff --git a/components/services/storage/dom_storage/session_storage_metadata.cc b/components/services/storage/dom_storage/session_storage_metadata.cc
index e6a3026695..ac16bab96b 100644
--- a/components/services/storage/dom_storage/session_storage_metadata.cc
+++ b/components/services/storage/dom_storage/session_storage_metadata.cc
@@ -4,10 +4,10 @@
 
 #include "components/services/storage/dom_storage/session_storage_metadata.h"
 
-#include <algorithm>
 #include <string_view>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/strings/string_number_conversions.h"
 #include "components/services/storage/dom_storage/async_dom_storage_database.h"
@@ -90,7 +90,7 @@ SessionStorageMetadata::RegisterNewMap(const std::string& namespace_id,
     CHECK_GT(old_map_locator.session_ids().size(), 1u);
 
     // Remove `namespace_id` from the source clone's sessions.
-    DCHECK(std::ranges::contains(old_map_locator.session_ids(), namespace_id));
+    DCHECK(base::Contains(old_map_locator.session_ids(), namespace_id));
     old_map_locator.RemoveSession(namespace_id);
 
     namespace_it->second = new_map_locator;
@@ -136,7 +136,7 @@ SessionStorageMetadata::TakeNamespace(const std::string& namespace_id) {
   for (const auto& storage_key_map_pair : storage_keys) {
     DomStorageDatabase::SharedMapLocator* map_locator =
         storage_key_map_pair.second.get();
-    DCHECK(std::ranges::contains(map_locator->session_ids(), namespace_id));
+    DCHECK(base::Contains(map_locator->session_ids(), namespace_id));
     map_locator->RemoveSession(namespace_id);
   }
   namespace_storage_key_map_.erase(it);
@@ -158,7 +158,7 @@ SessionStorageMetadata::TakeExistingMap(const std::string& namespace_id,
 
   scoped_refptr<DomStorageDatabase::SharedMapLocator> map_locator =
       storage_key_map_it->second.get();
-  DCHECK(std::ranges::contains(map_locator->session_ids(), namespace_id));
+  DCHECK(base::Contains(map_locator->session_ids(), namespace_id));
   map_locator->RemoveSession(namespace_id);
 
   ns_entry->second.erase(storage_key_map_it);
diff --git a/components/services/storage/dom_storage/sqlite/local_storage_sqlite.cc b/components/services/storage/dom_storage/sqlite/local_storage_sqlite.cc
index 8e9d28a827..df9a0c1723 100644
--- a/components/services/storage/dom_storage/sqlite/local_storage_sqlite.cc
+++ b/components/services/storage/dom_storage/sqlite/local_storage_sqlite.cc
@@ -5,6 +5,7 @@
 #include "components/services/storage/dom_storage/sqlite/local_storage_sqlite.h"
 
 #include "base/byte_size.h"
+#include "base/containers/contains.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/types/expected_macros.h"
 #include "components/services/storage/dom_storage/dom_storage_constants.h"
@@ -289,7 +290,7 @@ DbStatus LocalStorageSqlite::DeleteStorageKeysFromSession(
     // `kLocalStorageSessionId`.
     CHECK_EQ(map.session_ids().size(), 1u);
     CHECK_EQ(map.session_ids()[0], kLocalStorageSessionId);
-    DCHECK(std::ranges::contains(metadata_to_delete, map.storage_key()));
+    DCHECK(base::Contains(metadata_to_delete, map.storage_key()));
 
     ASSIGN_OR_RETURN(std::optional<int64_t> map_id,
                      FindMapId(map.storage_key()));
diff --git a/components/services/storage/dom_storage/sqlite/session_storage_sqlite.cc b/components/services/storage/dom_storage/sqlite/session_storage_sqlite.cc
index 45302ec679..ddd683a8a5 100644
--- a/components/services/storage/dom_storage/sqlite/session_storage_sqlite.cc
+++ b/components/services/storage/dom_storage/sqlite/session_storage_sqlite.cc
@@ -4,6 +4,7 @@
 
 #include "components/services/storage/dom_storage/sqlite/session_storage_sqlite.h"
 
+#include "base/containers/contains.h"
 #include "base/types/expected_macros.h"
 #include "components/services/storage/dom_storage/sqlite/map_entries_table.h"
 #include "components/services/storage/dom_storage/sqlite/sqlite_database_macros.h"
@@ -205,7 +206,7 @@ DbStatus SessionStorageSqlite::DeleteStorageKeysFromSession(
   for (const MapLocator& map_locator : maps_to_delete) {
     // The map's storage key must be in `metadata_to_delete`.
     DCHECK(
-        std::ranges::contains(metadata_to_delete, map_locator.storage_key()));
+        base::Contains(metadata_to_delete, map_locator.storage_key()));
 
     // The map must be unreferenced with no sessions remaining.
     CHECK(map_locator.session_ids().empty());
diff --git a/components/services/storage/public/cpp/filesystem/filesystem_impl.cc b/components/services/storage/public/cpp/filesystem/filesystem_impl.cc
index e0b6fa1a6e..2632816a1f 100644
--- a/components/services/storage/public/cpp/filesystem/filesystem_impl.cc
+++ b/components/services/storage/public/cpp/filesystem/filesystem_impl.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/files/file.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_util.h"
@@ -46,7 +47,7 @@ class LockTable {
   void RemoveLock(const base::FilePath& path) {
     const base::FilePath normalized_path = path.NormalizePathSeparators();
     base::AutoLock lock(lock_);
-    DCHECK(lock_paths_.contains(normalized_path));
+    DCHECK(base::Contains(lock_paths_, normalized_path));
     lock_paths_.erase(normalized_path);
   }
 
diff --git a/components/services/storage/service_worker/service_worker_disk_cache.cc b/components/services/storage/service_worker/service_worker_disk_cache.cc
index 4b6afc4ca0..60ee001571 100644
--- a/components/services/storage/service_worker/service_worker_disk_cache.cc
+++ b/components/services/storage/service_worker/service_worker_disk_cache.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -169,7 +170,7 @@ void ServiceWorkerDiskCache::CreateEntry(int64_t key, EntryCallback callback) {
   }
 
   uint64_t call_id = GetNextCallId();
-  DCHECK(!active_entry_calls_.contains(call_id));
+  DCHECK(!base::Contains(active_entry_calls_, call_id));
   active_entry_calls_.emplace(call_id, std::move(callback));
 
   disk_cache::EntryResult result = disk_cache_->CreateEntry(
@@ -204,7 +205,7 @@ void ServiceWorkerDiskCache::OpenEntry(int64_t key, EntryCallback callback) {
   }
 
   uint64_t call_id = GetNextCallId();
-  DCHECK(!active_entry_calls_.contains(call_id));
+  DCHECK(!base::Contains(active_entry_calls_, call_id));
   active_entry_calls_.emplace(call_id, std::move(callback));
 
   disk_cache::EntryResult result = disk_cache_->OpenEntry(
@@ -240,7 +241,7 @@ void ServiceWorkerDiskCache::DoomEntry(int64_t key,
   }
 
   uint64_t call_id = GetNextCallId();
-  DCHECK(!active_doom_calls_.contains(call_id));
+  DCHECK(!base::Contains(active_doom_calls_, call_id));
   active_doom_calls_.emplace(call_id, std::move(callback));
 
   net::Error net_error = disk_cache_->DoomEntry(
diff --git a/components/services/storage/service_worker/service_worker_storage.cc b/components/services/storage/service_worker/service_worker_storage.cc
index e859fc2293..17b3738e50 100644
--- a/components/services/storage/service_worker/service_worker_storage.cc
+++ b/components/services/storage/service_worker/service_worker_storage.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/files/file_util.h"
 #include "base/functional/callback_helpers.h"
@@ -262,7 +263,7 @@ void ServiceWorkerStorage::FindRegistrationForClientUrl(
   }
 
   // Bypass database lookup when there is no stored registration.
-  if (!registered_keys_.contains(key)) {
+  if (!base::Contains(registered_keys_, key)) {
     std::optional<std::vector<GURL>> scopes = std::vector<GURL>();
     storage_shared_buffer().PutRegistrationScopes(key, *scopes);
     std::move(callback).Run(
@@ -299,7 +300,7 @@ void ServiceWorkerStorage::FindRegistrationForScope(
   }
 
   // Bypass database lookup when there is no stored registration.
-  if (!registered_keys_.contains(key)) {
+  if (!base::Contains(registered_keys_, key)) {
     RunSoon(FROM_HERE,
             base::BindOnce(std::move(callback),
                            /*data=*/nullptr, /*resources=*/nullptr,
@@ -333,7 +334,7 @@ void ServiceWorkerStorage::FindRegistrationForId(
   }
 
   // Bypass database lookup when there is no stored registration.
-  if (!registered_keys_.contains(key)) {
+  if (!base::Contains(registered_keys_, key)) {
     std::move(callback).Run(
         /*data=*/nullptr, /*resources=*/nullptr,
         ServiceWorkerDatabase::Status::kErrorNotFound);
@@ -726,7 +727,7 @@ void ServiceWorkerStorage::CreateResourceReader(
   }
 
   uint64_t resource_operation_id = GetNextResourceOperationId();
-  DCHECK(!resource_readers_.contains(resource_operation_id));
+  DCHECK(!base::Contains(resource_readers_, resource_operation_id));
   std::optional<const net::SHA256HashValue> checksum_copy;
   if (sha256_checksum) {
     checksum_copy.emplace(*sha256_checksum);
@@ -757,7 +758,7 @@ void ServiceWorkerStorage::CreateResourceWriter(
   }
 
   uint64_t resource_operation_id = GetNextResourceOperationId();
-  DCHECK(!resource_writers_.contains(resource_operation_id));
+  DCHECK(!base::Contains(resource_writers_, resource_operation_id));
   resource_writers_[resource_operation_id] =
       std::make_unique<ServiceWorkerResourceWriterImpl>(
           resource_id, disk_cache()->GetWeakPtr(), std::move(receiver),
@@ -784,7 +785,7 @@ void ServiceWorkerStorage::CreateResourceMetadataWriter(
   }
 
   uint64_t resource_operation_id = GetNextResourceOperationId();
-  DCHECK(!resource_metadata_writers_.contains(resource_operation_id));
+  DCHECK(!base::Contains(resource_metadata_writers_, resource_operation_id));
   resource_metadata_writers_[resource_operation_id] =
       std::make_unique<ServiceWorkerResourceMetadataWriterImpl>(
           resource_id, disk_cache()->GetWeakPtr(), std::move(receiver),
@@ -1609,19 +1610,19 @@ void ServiceWorkerStorage::ClearSessionOnlyOrigins() {
 
 void ServiceWorkerStorage::OnResourceReaderDisconnected(
     uint64_t resource_operation_id) {
-  DCHECK(resource_readers_.contains(resource_operation_id));
+  DCHECK(base::Contains(resource_readers_, resource_operation_id));
   resource_readers_.erase(resource_operation_id);
 }
 
 void ServiceWorkerStorage::OnResourceWriterDisconnected(
     uint64_t resource_operation_id) {
-  DCHECK(resource_writers_.contains(resource_operation_id));
+  DCHECK(base::Contains(resource_writers_, resource_operation_id));
   resource_writers_.erase(resource_operation_id);
 }
 
 void ServiceWorkerStorage::OnResourceMetadataWriterDisconnected(
     uint64_t resource_operation_id) {
-  DCHECK(resource_metadata_writers_.contains(resource_operation_id));
+  DCHECK(base::Contains(resource_metadata_writers_, resource_operation_id));
   resource_metadata_writers_.erase(resource_operation_id);
 }
 
diff --git a/components/services/storage/service_worker/service_worker_storage_control_impl.cc b/components/services/storage/service_worker/service_worker_storage_control_impl.cc
index cb67b1d58d..494ae218f5 100644
--- a/components/services/storage/service_worker/service_worker_storage_control_impl.cc
+++ b/components/services/storage/service_worker/service_worker_storage_control_impl.cc
@@ -4,6 +4,7 @@
 
 #include "components/services/storage/service_worker/service_worker_storage_control_impl.h"
 
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/task/sequenced_task_runner.h"
 #include "components/services/storage/service_worker/service_worker_resource_ops.h"
@@ -139,7 +140,7 @@ void ServiceWorkerStorageControlImpl::Recover(
     std::vector<mojom::ServiceWorkerLiveVersionInfoPtr> versions,
     RecoverCallback callback) {
   for (auto& version : versions) {
-    DCHECK(!live_versions_.contains(version->id));
+    DCHECK(!base::Contains(live_versions_, version->id));
     auto reference = std::make_unique<ServiceWorkerLiveVersionRefImpl>(
         weak_ptr_factory_.GetWeakPtr(), version->id);
     reference->Add(std::move(version->reference));
@@ -605,7 +606,7 @@ void ServiceWorkerStorageControlImpl::MaybePurgeResources(
     return;
   }
 
-  if (live_versions_.contains(version_id)) {
+  if (base::Contains(live_versions_, version_id)) {
     live_versions_[version_id]->set_purgeable_resources(
         std::move(purgeable_resources));
   } else {
diff --git a/components/services/storage/shared_storage/shared_storage_test_utils.cc b/components/services/storage/shared_storage/shared_storage_test_utils.cc
index 3f7621390a..535e0ca7aa 100644
--- a/components/services/storage/shared_storage/shared_storage_test_utils.cc
+++ b/components/services/storage/shared_storage/shared_storage_test_utils.cc
@@ -4,7 +4,6 @@
 
 #include "components/services/storage/shared_storage/shared_storage_test_utils.h"
 
-#include <algorithm>
 #include <deque>
 #include <iterator>
 #include <queue>
@@ -12,6 +11,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/path_service.h"
 #include "base/strings/strcat.h"
@@ -426,7 +426,7 @@ StorageKeyPolicyMatcherFunctionUtility::MakeMatcherFunction(
   return base::BindRepeating(
       [](std::vector<url::Origin> origins_to_match,
          const blink::StorageKey& storage_key, SpecialStoragePolicy* policy) {
-        return std::ranges::contains(origins_to_match, storage_key.origin());
+        return base::Contains(origins_to_match, storage_key.origin());
       },
       origins_to_match);
 }
diff --git a/components/session_proto_db/session_proto_db.h b/components/session_proto_db/session_proto_db.h
index e3906436ad..baa93d9cfd 100644
--- a/components/session_proto_db/session_proto_db.h
+++ b/components/session_proto_db/session_proto_db.h
@@ -5,13 +5,13 @@
 #ifndef COMPONENTS_SESSION_PROTO_DB_SESSION_PROTO_DB_H_
 #define COMPONENTS_SESSION_PROTO_DB_SESSION_PROTO_DB_H_
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <string>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/raw_ptr.h"
@@ -273,8 +273,8 @@ void SessionProtoDB<T>::PerformMaintenance(
               // Return all keys which where key_substring_to_match is a
               // substring of said keys and hasn't been explicitly marked
               // not to be removed in keys_to_keep.
-              return key.contains(key_substring_to_match) &&
-                     !std::ranges::contains(keys_to_keep, key);
+              return base::Contains(key, key_substring_to_match) &&
+                     !base::Contains(keys_to_keep, key);
             },
             keys_to_keep, key_substring_to_match),
         base::BindOnce(&SessionProtoDB::OnPerformMaintenance,
diff --git a/components/signin/core/browser/account_reconcilor.cc b/components/signin/core/browser/account_reconcilor.cc
index 1c149a4762..59d5850253 100644
--- a/components/signin/core/browser/account_reconcilor.cc
+++ b/components/signin/core/browser/account_reconcilor.cc
@@ -11,6 +11,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -606,7 +607,7 @@ void AccountReconcilor::OnAccountsInCookieUpdated(
 
   if (!primary_account.empty() &&
       delegate_->ShouldAbortReconcileIfPrimaryHasError() &&
-      !std::ranges::contains(chrome_accounts, primary_account)) {
+      !base::Contains(chrome_accounts, primary_account)) {
     VLOG(1) << "Primary account has error, abort.";
     DCHECK(is_reconcile_started_);
     AbortReconcile();
diff --git a/components/signin/core/browser/account_reconcilor_delegate.cc b/components/signin/core/browser/account_reconcilor_delegate.cc
index 8a93e3a01b..33634dac92 100644
--- a/components/signin/core/browser/account_reconcilor_delegate.cc
+++ b/components/signin/core/browser/account_reconcilor_delegate.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/time/time.h"
 #include "google_apis/gaia/google_service_auth_error.h"
@@ -74,7 +75,7 @@ AccountReconcilorDelegate::ReorderChromeAccountsForReconcile(
   // which go above this count will fail.
   const int kMaxGaiaAccounts = 10;
   DCHECK(first_account.empty() ||
-         std::ranges::contains(chrome_accounts, first_account));
+         base::Contains(chrome_accounts, first_account));
 
   // Ordered list of accounts, this is the result of this function.
   std::vector<CoreAccountId> ordered_accounts;
diff --git a/components/signin/core/browser/dice_account_reconcilor_delegate.cc b/components/signin/core/browser/dice_account_reconcilor_delegate.cc
index 3440df4d90..5a5fe84c93 100644
--- a/components/signin/core/browser/dice_account_reconcilor_delegate.cc
+++ b/components/signin/core/browser/dice_account_reconcilor_delegate.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_macros.h"
@@ -130,9 +131,9 @@ DiceAccountReconcilorDelegate::GetInconsistencyReason(
   bool primary_account_has_token = false;
   if (!primary_account.empty()) {
     primary_account_has_token =
-        std::ranges::contains(chrome_accounts, primary_account);
+        base::Contains(chrome_accounts, primary_account);
     bool primary_account_has_cookie =
-        std::ranges::contains(valid_gaia_accounts_ids, primary_account);
+        base::Contains(valid_gaia_accounts_ids, primary_account);
     if (primary_account_has_token && !primary_account_has_cookie) {
       return InconsistencyReason::kMissingSyncCookie;
     }
@@ -145,7 +146,7 @@ DiceAccountReconcilorDelegate::GetInconsistencyReason(
   bool missing_first_web_account_token =
       primary_account.empty() && !gaia_accounts.empty() &&
       gaia_accounts[0].valid &&
-      !std::ranges::contains(chrome_accounts, gaia_accounts[0].id);
+      !base::Contains(chrome_accounts, gaia_accounts[0].id);
 
   if (missing_first_web_account_token) {
     return InconsistencyReason::kMissingFirstWebAccountToken;
@@ -190,7 +191,7 @@ bool DiceAccountReconcilorDelegate::ShouldDeleteAccountsFromGaia(
   // account.
   for (const gaia::ListedAccount& gaia_account : gaia_accounts) {
     if (gaia_account.valid &&
-        !std::ranges::contains(chrome_accounts, gaia_account.id)) {
+        !base::Contains(chrome_accounts, gaia_account.id)) {
       return true;
     }
   }
@@ -296,7 +297,7 @@ bool DiceAccountReconcilorDelegate::ShouldRevokeTokensBeforeMultilogin(
   }
 
   // The default gaia account doesn't have token.
-  if (!std::ranges::contains(chrome_accounts, gaia_accounts[0].id)) {
+  if (!base::Contains(chrome_accounts, gaia_accounts[0].id)) {
     if (IsCookieBasedConsistencyMode()) {
       // Logout only if the default cookie account is valid.
       return gaia_accounts[0].valid;
@@ -326,7 +327,7 @@ CoreAccountId DiceAccountReconcilorDelegate::GetFirstGaiaAccountForMultilogin(
   // should try to put cached first account first since Gaia has no information
   // about it.
   if (gaia_accounts.empty() && !last_known_first_account_.empty() &&
-      std::ranges::contains(chrome_accounts, last_known_first_account_)) {
+      base::Contains(chrome_accounts, last_known_first_account_)) {
     // last_known_account_ is always empty on first execution.
     DCHECK(!first_execution);
     return last_known_first_account_;
diff --git a/components/signin/core/browser/mirror_account_reconcilor_delegate.cc b/components/signin/core/browser/mirror_account_reconcilor_delegate.cc
index ec624eab00..e666113545 100644
--- a/components/signin/core/browser/mirror_account_reconcilor_delegate.cc
+++ b/components/signin/core/browser/mirror_account_reconcilor_delegate.cc
@@ -4,6 +4,7 @@
 
 #include "components/signin/core/browser/mirror_account_reconcilor_delegate.h"
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "build/build_config.h"
 #include "components/signin/core/browser/account_reconcilor.h"
diff --git a/components/signin/core/browser/signin_header_helper.cc b/components/signin/core/browser/signin_header_helper.cc
index 58f77dd620..9aa0761a91 100644
--- a/components/signin/core/browser/signin_header_helper.cc
+++ b/components/signin/core/browser/signin_header_helper.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/escape.h"
@@ -87,11 +88,11 @@ const GURL& RequestAdapter::GetUrl() {
 bool RequestAdapter::HasHeader(const std::string& name) {
   return (original_headers_->HasHeader(name) ||
           modified_headers_->HasHeader(name)) &&
-         !std::ranges::contains(*headers_to_remove_, name);
+         !base::Contains(*headers_to_remove_, name);
 }
 
 void RequestAdapter::RemoveRequestHeaderByName(const std::string& name) {
-  if (!std::ranges::contains(*headers_to_remove_, name)) {
+  if (!base::Contains(*headers_to_remove_, name)) {
     headers_to_remove_->push_back(name);
   }
 }
diff --git a/components/signin/internal/identity_manager/account_info_util.cc b/components/signin/internal/identity_manager/account_info_util.cc
index 267a7490d9..4db2e33cf6 100644
--- a/components/signin/internal/identity_manager/account_info_util.cc
+++ b/components/signin/internal/identity_manager/account_info_util.cc
@@ -4,12 +4,12 @@
 
 #include "components/signin/internal/identity_manager/account_info_util.h"
 
-#include <algorithm>
 #include <map>
 #include <optional>
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/logging.h"
 #include "base/values.h"
@@ -173,7 +173,7 @@ std::optional<AccountCapabilities> AccountCapabilitiesFromServerResponse(
     }
 
     // Add the capability to the map if it's supported in Chrome.
-    if (std::ranges::contains(
+    if (base::Contains(
             AccountCapabilities::GetSupportedAccountCapabilityNames(), *name)) {
       capabilities_map.insert({*name, *boolean_value});
     }
diff --git a/components/signin/internal/identity_manager/account_tracker_service.cc b/components/signin/internal/identity_manager/account_tracker_service.cc
index b1d8cb16de..ea82fe0e30 100644
--- a/components/signin/internal/identity_manager/account_tracker_service.cc
+++ b/components/signin/internal/identity_manager/account_tracker_service.cc
@@ -13,6 +13,7 @@
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -230,7 +231,7 @@ void AccountTrackerService::StartTrackingAccount(
   // TODO(crbug.com/40283610): Change into a CHECK once there are no crash
   // reports for tracking empty account ids.
   DUMP_WILL_BE_CHECK(!account_id.empty());
-  if (!accounts_.contains(account_id)) {
+  if (!base::Contains(accounts_, account_id)) {
     DVLOG(1) << "StartTracking " << account_id;
     AccountInfo account_info;
     account_info.account_id = account_id;
@@ -239,13 +240,13 @@ void AccountTrackerService::StartTrackingAccount(
 }
 
 bool AccountTrackerService::IsTrackingAccount(const CoreAccountId& account_id) {
-  return accounts_.contains(account_id);
+  return base::Contains(accounts_, account_id);
 }
 
 void AccountTrackerService::StopTrackingAccount(
     const CoreAccountId& account_id) {
   DVLOG(1) << "StopTracking " << account_id;
-  if (accounts_.contains(account_id)) {
+  if (base::Contains(accounts_, account_id)) {
     AccountInfo account_info = std::move(accounts_[account_id]);
     RemoveFromPrefs(account_info);
     RemoveAccountImageFromDisk(account_id);
@@ -326,7 +327,7 @@ void AccountTrackerService::SetAccountImage(
 void AccountTrackerService::SetAccountCapabilities(
     const CoreAccountId& account_id,
     const AccountCapabilities& account_capabilities) {
-  DCHECK(accounts_.contains(account_id));
+  DCHECK(base::Contains(accounts_, account_id));
   AccountInfo& account_info = accounts_[account_id];
 
   bool modified = account_info.capabilities.UpdateWith(account_capabilities);
@@ -352,7 +353,7 @@ void AccountTrackerService::SetAccountCapabilities(
 
 void AccountTrackerService::SetIsChildAccount(const CoreAccountId& account_id,
                                               bool is_child_account) {
-  DCHECK(accounts_.contains(account_id)) << account_id.ToString();
+  DCHECK(base::Contains(accounts_, account_id)) << account_id.ToString();
   AccountInfo& account_info = accounts_[account_id];
   bool modified = UpdateAccountInfoChildStatus(account_info, is_child_account);
   if (!modified) {
@@ -367,7 +368,7 @@ void AccountTrackerService::SetIsChildAccount(const CoreAccountId& account_id,
 void AccountTrackerService::SetIsAdvancedProtectionAccount(
     const CoreAccountId& account_id,
     bool is_under_advanced_protection) {
-  DCHECK(accounts_.contains(account_id)) << account_id.ToString();
+  DCHECK(base::Contains(accounts_, account_id)) << account_id.ToString();
   AccountInfo& account_info = accounts_[account_id];
   if (account_info.is_under_advanced_protection ==
       is_under_advanced_protection) {
@@ -421,7 +422,7 @@ void AccountTrackerService::MigrateToGaiaId() {
     // If there is already an account keyed to the current account's gaia id,
     // assume this is the result of a partial migration and skip the account
     // that is currently inspected.
-    if (accounts_.contains(new_account_id)) {
+    if (base::Contains(accounts_, new_account_id)) {
       continue;
     }
 
@@ -443,7 +444,7 @@ void AccountTrackerService::MigrateToGaiaId() {
 
   // Remove any obsolete account.
   for (const auto& account_id : to_remove) {
-    DCHECK(accounts_.contains(account_id));
+    DCHECK(base::Contains(accounts_, account_id));
     AccountInfo& account_info = accounts_[account_id];
     RemoveAccountImageFromDisk(account_id);
     RemoveFromPrefs(account_info);
@@ -780,7 +781,7 @@ CoreAccountId AccountTrackerService::SeedAccountInfo(AccountInfo info) {
     return CoreAccountId();
   }
 
-  const bool already_exists = accounts_.contains(info.account_id);
+  const bool already_exists = base::Contains(accounts_, info.account_id);
   StartTrackingAccount(info.account_id);
   AccountInfo& account_info = accounts_[info.account_id];
   DCHECK(!already_exists || account_info.gaia.empty() ||
@@ -815,7 +816,7 @@ void AccountTrackerService::SeedAccountsInfo(
 
   if (primary_account_id) {
     // The primary account must be present in the account list.
-    CHECK(std::ranges::contains(accounts, *primary_account_id,
+    CHECK(base::Contains(accounts, *primary_account_id,
                                 &AccountInfo::GetAccountId),
           base::NotFatalUntil::M148);
   }
@@ -826,8 +827,8 @@ void AccountTrackerService::SeedAccountsInfo(
     for (const auto& account : GetAccounts()) {
       CoreAccountId curr_account_id = account.account_id;
       if (curr_account_id != primary_account_id &&
-          !std::ranges::contains(accounts, curr_account_id,
-                                 &AccountInfo::account_id)) {
+          !base::Contains(accounts, curr_account_id,
+                          &AccountInfo::account_id)) {
         RemoveAccount(curr_account_id);
       }
     }
diff --git a/components/signin/internal/identity_manager/gaia_cookie_manager_service.cc b/components/signin/internal/identity_manager/gaia_cookie_manager_service.cc
index 54655574a9..7bd9b721d9 100644
--- a/components/signin/internal/identity_manager/gaia_cookie_manager_service.cc
+++ b/components/signin/internal/identity_manager/gaia_cookie_manager_service.cc
@@ -14,6 +14,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -508,8 +509,8 @@ signin::AccountsInCookieJarInfo GaiaCookieManagerService::ListAccounts() {
     // `ListAccounts()` doesn't mean a change has happened that requires adding
     // a new /ListAccounts request even if there is one in-flight.
     // Only trigger a request, if none is ongoing.
-    if (!std::ranges::contains(requests_, LIST_ACCOUNTS,
-                               &GaiaCookieRequest::request_type)) {
+    if (!base::Contains(requests_, LIST_ACCOUNTS,
+                        &GaiaCookieRequest::request_type)) {
       TriggerListAccounts();
     }
   }
@@ -555,8 +556,8 @@ void GaiaCookieManagerService::LogOutAllAccounts(
   DCHECK(completion_callback);
 
   // Verify a LOG_OUT isn't already queued.
-  if (std::ranges::contains(requests_, GaiaCookieRequestType::LOG_OUT,
-                            &GaiaCookieRequest::request_type)) {
+  if (base::Contains(requests_, GaiaCookieRequestType::LOG_OUT,
+                     &GaiaCookieRequest::request_type)) {
     std::move(completion_callback)
         .Run(GoogleServiceAuthError(GoogleServiceAuthError::REQUEST_CANCELED));
     return;
diff --git a/components/signin/internal/identity_manager/oauth_multilogin_token_fetcher.cc b/components/signin/internal/identity_manager/oauth_multilogin_token_fetcher.cc
index a12579478a..fadfa88354 100644
--- a/components/signin/internal/identity_manager/oauth_multilogin_token_fetcher.cc
+++ b/components/signin/internal/identity_manager/oauth_multilogin_token_fetcher.cc
@@ -4,10 +4,10 @@
 
 #include "components/signin/internal/identity_manager/oauth_multilogin_token_fetcher.h"
 
-#include <algorithm>
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
@@ -87,8 +87,8 @@ void OAuthMultiloginTokenFetcher::OnTokenRequestComplete(
     const OAuthMultiloginTokenRequest* request,
     OAuthMultiloginTokenRequest::Result result) {
   CoreAccountId account_id = request->account_id();
-  CHECK(std::ranges::contains(account_params_, account_id,
-                              &AccountParams::account_id));
+  CHECK(
+      base::Contains(account_params_, account_id, &AccountParams::account_id));
   size_t num_erased = std::erase_if(
       token_requests_,
       [request](const auto& element) { return element.get() == request; });
diff --git a/components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_android.cc b/components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_android.cc
index 49550a09be..4815e475ea 100644
--- a/components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_android.cc
+++ b/components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_android.cc
@@ -4,11 +4,10 @@
 
 #include "components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_android.h"
 
-#include <algorithm>
-
 #include "base/android/jni_android.h"
 #include "base/android/jni_array.h"
 #include "base/android/jni_string.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
@@ -180,7 +179,7 @@ bool ProfileOAuth2TokenServiceDelegateAndroid::RefreshTokenIsAvailable(
       << "ProfileOAuth2TokenServiceDelegateAndroid::RefreshTokenIsAvailable"
       << " account= " << account_id;
   std::vector<CoreAccountId> accounts = GetValidAccounts();
-  return std::ranges::contains(accounts, account_id);
+  return base::Contains(accounts, account_id);
 }
 
 std::vector<CoreAccountId>
@@ -326,7 +325,7 @@ void ProfileOAuth2TokenServiceDelegateAndroid::UpdateAccountList(
     if (signed_in_id.has_value() && prev_id == *signed_in_id) {
       continue;
     }
-    if (!std::ranges::contains(curr_ids, prev_id)) {
+    if (!base::Contains(curr_ids, prev_id)) {
       DVLOG(1) << "ProfileOAuth2TokenServiceDelegateAndroid::UpdateAccountList:"
                << "revoked=" << prev_id;
       revoked_ids->push_back(prev_id);
diff --git a/components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_chromeos.cc b/components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_chromeos.cc
index 4240e6dd5f..b39f91da09 100644
--- a/components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_chromeos.cc
+++ b/components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_chromeos.cc
@@ -4,12 +4,12 @@
 
 #include "components/signin/internal/identity_manager/profile_oauth2_token_service_delegate_chromeos.h"
 
-#include <algorithm>
 #include <string>
 #include <utility>
 #include <vector>
 
 #include "base/check_deref.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "build/build_config.h"
@@ -220,9 +220,9 @@ bool ProfileOAuth2TokenServiceDelegateChromeOS::RefreshTokenIsAvailable(
 
   // We intentionally do NOT check if the refresh token associated with
   // |account_id| is valid or not. See crbug.com/919793 for details.
-  return std::ranges::contains(GetOAuthAccountIdsFromAccountKeys(
-                                   account_keys_, account_tracker_service_),
-                               account_id);
+  return base::Contains(GetOAuthAccountIdsFromAccountKeys(
+                            account_keys_, account_tracker_service_),
+                        account_id);
 }
 
 // Note: This method should use the same logic for filtering accounts as
diff --git a/components/signin/internal/identity_manager/token_binding_helper.cc b/components/signin/internal/identity_manager/token_binding_helper.cc
index 5615feb0ac..3acdc3c6b0 100644
--- a/components/signin/internal/identity_manager/token_binding_helper.cc
+++ b/components/signin/internal/identity_manager/token_binding_helper.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/barrier_callback.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
@@ -98,7 +99,7 @@ void TokenBindingHelper::SetBindingKey(
 }
 
 bool TokenBindingHelper::HasBindingKey(const CoreAccountId& account_id) const {
-  return binding_keys_.contains(account_id);
+  return base::Contains(binding_keys_, account_id);
 }
 
 void TokenBindingHelper::ClearAllKeys() {
diff --git a/components/signin/public/base/signin_prefs.cc b/components/signin/public/base/signin_prefs.cc
index 831f4f564a..b2eea5b960 100644
--- a/components/signin/public/base/signin_prefs.cc
+++ b/components/signin/public/base/signin_prefs.cc
@@ -6,6 +6,7 @@
 
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/json/values_util.h"
 #include "base/values.h"
 #include "components/prefs/pref_change_registrar.h"
@@ -193,7 +194,7 @@ size_t SigninPrefs::RemoveAllAccountPrefsExcept(
   for (const std::pair<const std::string&, const base::Value&> account_prefs :
        pref_service_->GetDict(kSigninAccountPrefs)) {
     GaiaId gaia_id(account_prefs.first);
-    if (!gaia_ids_to_keep.contains(gaia_id)) {
+    if (!base::Contains(gaia_ids_to_keep, gaia_id)) {
       accounts_prefs_to_remove.push_back(std::move(gaia_id));
     }
   }
diff --git a/components/signin/public/identity_manager/account_capabilities_test_mutator.cc b/components/signin/public/identity_manager/account_capabilities_test_mutator.cc
index 9ca6456d7b..1cc7399e60 100644
--- a/components/signin/public/identity_manager/account_capabilities_test_mutator.cc
+++ b/components/signin/public/identity_manager/account_capabilities_test_mutator.cc
@@ -4,10 +4,10 @@
 
 #include "components/signin/public/identity_manager/account_capabilities_test_mutator.h"
 
-#include <algorithm>
 #include <ostream>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "build/build_config.h"
 #include "components/signin/internal/identity_manager/account_capabilities_constants.h"
 
@@ -174,7 +174,7 @@ void AccountCapabilitiesTestMutator::SetCapability(const std::string& name,
                                                    bool value) {
   base::span<const std::string_view> capability_names =
       AccountCapabilities::GetSupportedAccountCapabilityNames();
-  CHECK(std::ranges::contains(capability_names, name))
+  CHECK(base::Contains(capability_names, name))
       << "Invalid capability name: " << name;
   capabilities_->capabilities_map_[name] = value;
 }
diff --git a/components/site_isolation/site_isolation_policy.cc b/components/site_isolation/site_isolation_policy.cc
index d54bd02374..9213f95d47 100644
--- a/components/site_isolation/site_isolation_policy.cc
+++ b/components/site_isolation/site_isolation_policy.cc
@@ -4,6 +4,7 @@
 
 #include "components/site_isolation/site_isolation_policy.h"
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/json/values_util.h"
 #include "base/metrics/field_trial_params.h"
diff --git a/components/soda/soda_installer.cc b/components/soda/soda_installer.cc
index 7ef6766be7..be575797f8 100644
--- a/components/soda/soda_installer.cc
+++ b/components/soda/soda_installer.cc
@@ -4,10 +4,10 @@
 
 #include "components/soda/soda_installer.h"
 
-#include <algorithm>
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/observer_list.h"
 #include "base/strings/string_split.h"
@@ -152,7 +152,7 @@ const std::set<LanguageCode> SodaInstaller::InstalledLanguages() const {
 }
 
 bool SodaInstaller::IsLanguageInstalled(LanguageCode language_code) const {
-  return installed_languages_.contains(language_code);
+  return base::Contains(installed_languages_, language_code);
 }
 
 void SodaInstaller::AddObserver(Observer* observer) {
@@ -180,7 +180,7 @@ void SodaInstaller::NotifySodaInstalledForTesting(LanguageCode language_code) {
 
   // Otherwise, this means a language pack installed.
   installed_languages_.insert(language_code);
-  if (language_pack_progress_.contains(language_code)) {
+  if (base::Contains(language_pack_progress_, language_code)) {
     language_pack_progress_.erase(language_code);
   }
   if (soda_binary_installed_) {
@@ -199,7 +199,7 @@ void SodaInstaller::NotifySodaErrorForTesting(LanguageCode language_code,
     language_pack_progress_.clear();
   } else {
     // Error with the language pack download.
-    if (language_pack_progress_.contains(language_code)) {
+    if (base::Contains(language_pack_progress_, language_code)) {
       language_pack_progress_.erase(language_code);
     }
   }
@@ -224,7 +224,7 @@ void SodaInstaller::NotifySodaProgressForTesting(int progress,
     is_soda_downloading_ = true;
   } else {
     // Language pack download progress.
-    if (language_pack_progress_.contains(language_code)) {
+    if (base::Contains(language_pack_progress_, language_code)) {
       language_pack_progress_.insert({language_code, progress});
     } else {
       language_pack_progress_[language_code] = progress;
@@ -272,7 +272,7 @@ void SodaInstaller::RegisterLanguage(std::string_view language,
                                      PrefService* global_prefs) {
   ScopedListPrefUpdate update(global_prefs,
                               prefs::kSodaRegisteredLanguagePacks);
-  if (!update->contains(language)) {
+  if (!base::Contains(*update, base::Value(language))) {
     update->Append(language);
   }
 
@@ -283,7 +283,7 @@ void SodaInstaller::UnregisterLanguage(std::string_view language,
                                        PrefService* global_prefs) {
   ScopedListPrefUpdate update(global_prefs,
                               prefs::kSodaRegisteredLanguagePacks);
-  if (update->contains(language)) {
+  if (base::Contains(*update, base::Value(language))) {
     update->EraseValue(base::Value(language));
   }
 }
@@ -295,18 +295,18 @@ void SodaInstaller::UnregisterLanguages(PrefService* global_prefs) {
 }
 
 bool SodaInstaller::IsLanguageEnabled(std::string_view language) {
-  return std::ranges::contains(GetLiveCaptionEnabledLanguages(), language);
+  return base::Contains(GetLiveCaptionEnabledLanguages(), language);
 }
 
 bool SodaInstaller::IsSodaLanguageDownloading(
     LanguageCode language_code) const {
   return (is_soda_downloading_ && IsLanguageInstalled(language_code)) ||
-         language_pack_progress_.contains(language_code);
+         base::Contains(language_pack_progress_, language_code);
 }
 
 bool SodaInstaller::IsSodaDownloading(LanguageCode language_code) const {
   return is_soda_downloading_ ||
-         language_pack_progress_.contains(language_code);
+         base::Contains(language_pack_progress_, language_code);
 }
 
 std::optional<SodaInstaller::ErrorCode> SodaInstaller::GetSodaInstallErrorCode(
diff --git a/components/soda/soda_installer_impl_chromeos.cc b/components/soda/soda_installer_impl_chromeos.cc
index 99416a8a94..b7d074d4d9 100644
--- a/components/soda/soda_installer_impl_chromeos.cc
+++ b/components/soda/soda_installer_impl_chromeos.cc
@@ -9,6 +9,7 @@
 
 #include "ash/constants/ash_features.h"
 #include "ash/constants/ash_pref_names.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
diff --git a/components/spellcheck/common/spellcheck_common.cc b/components/spellcheck/common/spellcheck_common.cc
index 2e4007facd..3d13fcfbae 100644
--- a/components/spellcheck/common/spellcheck_common.cc
+++ b/components/spellcheck/common/spellcheck_common.cc
@@ -4,11 +4,11 @@
 
 #include "components/spellcheck/common/spellcheck_common.h"
 
-#include <algorithm>
 #include <string_view>
 
 #include "base/check.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/metrics/field_trial.h"
 #include "base/strings/string_util.h"
@@ -231,7 +231,7 @@ void FillSuggestions(
 
     const std::u16string& suggestion = suggestions_list[language][index];
     // Only add the suggestion if it's unique.
-    if (!std::ranges::contains(*optional_suggestions, suggestion)) {
+    if (!base::Contains(*optional_suggestions, suggestion)) {
       optional_suggestions->push_back(suggestion);
     }
     if (optional_suggestions->size() >= kMaxSuggestions) {
diff --git a/components/storage_monitor/mtp_manager_client_chromeos.cc b/components/storage_monitor/mtp_manager_client_chromeos.cc
index 155ace29d9..63d69b6c6e 100644
--- a/components/storage_monitor/mtp_manager_client_chromeos.cc
+++ b/components/storage_monitor/mtp_manager_client_chromeos.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/utf_string_conversions.h"
 #include "components/storage_monitor/storage_info.h"
@@ -83,7 +84,7 @@ void MtpManagerClientChromeOS::StorageAttached(
   if (device_id.empty() || storage_label.empty())
     return;
 
-  DCHECK(!storage_map_.contains(location));
+  DCHECK(!base::Contains(storage_map_, location));
 
   StorageInfo storage_info(device_id, location, storage_label, vendor_name,
                            product_name, 0);
diff --git a/components/storage_monitor/storage_monitor.cc b/components/storage_monitor/storage_monitor.cc
index bfcd0f84ee..59000e6412 100644
--- a/components/storage_monitor/storage_monitor.cc
+++ b/components/storage_monitor/storage_monitor.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/raw_ptr.h"
 #include "components/storage_monitor/removable_storage_observer.h"
@@ -158,7 +159,7 @@ void StorageMonitor::MarkInitialized() {
 void StorageMonitor::ProcessAttach(const StorageInfo& info) {
   {
     base::AutoLock lock(storage_lock_);
-    if (storage_map_.contains(info.device_id())) {
+    if (base::Contains(storage_map_, info.device_id())) {
       // This can happen if our unique id scheme fails. Ignore the incoming
       // non-unique attachment.
       return;
diff --git a/components/storage_monitor/storage_monitor_chromeos.cc b/components/storage_monitor/storage_monitor_chromeos.cc
index cc65ee4b54..ad313f1e28 100644
--- a/components/storage_monitor/storage_monitor_chromeos.cc
+++ b/components/storage_monitor/storage_monitor_chromeos.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -203,7 +204,7 @@ void StorageMonitorCros::OnMountEvent(
 
   switch (event) {
     case DiskMountManager::MOUNTING: {
-      if (mount_map_.contains(mount_info.mount_path)) {
+      if (base::Contains(mount_map_, mount_info.mount_path)) {
         return;
       }
 
@@ -245,7 +246,7 @@ bool StorageMonitorCros::GetStorageInfoForPath(
     return false;
 
   base::FilePath current = path;
-  while (!mount_map_.contains(current.value()) &&
+  while (!base::Contains(mount_map_, current.value()) &&
          current != current.DirName()) {
     current = current.DirName();
   }
@@ -315,7 +316,7 @@ void StorageMonitorCros::AddMountedPath(
     bool has_dcim) {
   DCHECK_CURRENTLY_ON(content::BrowserThread::UI);
 
-  if (mount_map_.contains(mount_info.mount_path)) {
+  if (base::Contains(mount_map_, mount_info.mount_path)) {
     // CheckExistingMountPoints() added the mount point information in the map
     // before the device attached handler is called. Therefore, an entry for
     // the device already exists in the map.
@@ -343,7 +344,7 @@ void StorageMonitorCros::AddFixedStorageDisk(const Disk& disk) {
   if (!GetFixedStorageInfo(disk, &info))
     return;
 
-  if (mount_map_.contains(disk.mount_path()))
+  if (base::Contains(mount_map_, disk.mount_path()))
     return;
 
   mount_map_.insert(std::make_pair(disk.mount_path(), info));
diff --git a/components/storage_monitor/storage_monitor_linux.cc b/components/storage_monitor/storage_monitor_linux.cc
index 75f5629589..e12a055657 100644
--- a/components/storage_monitor/storage_monitor_linux.cc
+++ b/components/storage_monitor/storage_monitor_linux.cc
@@ -17,6 +17,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/process/kill.h"
@@ -247,7 +248,8 @@ bool StorageMonitorLinux::GetStorageInfoForPath(
     return false;
 
   base::FilePath current = path;
-  while (!mount_info_map_.contains(current) && current != current.DirName())
+  while (!base::Contains(mount_info_map_, current) &&
+         current != current.DirName())
     current = current.DirName();
 
   auto mount_info = mount_info_map_.find(current);
@@ -408,7 +410,7 @@ void StorageMonitorLinux::UpdateMtab(const MountPointDeviceMap& new_mtab) {
 bool StorageMonitorLinux::IsDeviceAlreadyMounted(
     const base::FilePath& mount_device) const {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  return mount_priority_map_.contains(mount_device);
+  return base::Contains(mount_priority_map_, mount_device);
 }
 
 void StorageMonitorLinux::HandleDeviceMountedMultipleTimes(
diff --git a/components/storage_monitor/transient_device_ids.cc b/components/storage_monitor/transient_device_ids.cc
index cfabc42533..02e5676449 100644
--- a/components/storage_monitor/transient_device_ids.cc
+++ b/components/storage_monitor/transient_device_ids.cc
@@ -7,6 +7,7 @@
 #include "components/storage_monitor/transient_device_ids.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/uuid.h"
 #include "components/storage_monitor/storage_info.h"
 
@@ -20,11 +21,11 @@ std::string TransientDeviceIds::GetTransientIdForDeviceId(
     const std::string& device_id) {
   DCHECK(thread_checker_.CalledOnValidThread());
 
-  if (!device_id_map_.contains(device_id)) {
+  if (!base::Contains(device_id_map_, device_id)) {
     std::string transient_id;
     do {
       transient_id = base::Uuid::GenerateRandomV4().AsLowercaseString();
-    } while (transient_id_map_.contains(transient_id));
+    } while (base::Contains(transient_id_map_, transient_id));
 
     device_id_map_[device_id] = transient_id;
     transient_id_map_[transient_id] = device_id;
diff --git a/components/subresource_filter/content/browser/content_subresource_filter_throttle_manager.cc b/components/subresource_filter/content/browser/content_subresource_filter_throttle_manager.cc
index 114d786cae..8532201a7e 100644
--- a/components/subresource_filter/content/browser/content_subresource_filter_throttle_manager.cc
+++ b/components/subresource_filter/content/browser/content_subresource_filter_throttle_manager.cc
@@ -8,6 +8,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -156,7 +157,8 @@ void ContentSubresourceFilterThrottleManager::ReadyToCommitInFrameNavigation(
     // CHECK.
     DCHECK_EQ(
         ad_evidence.parent_is_ad(),
-        ad_frames_.contains(
+        base::Contains(
+            ad_frames_,
             frame_host->GetParentOrOuterDocument()->GetFrameTreeNodeId()));
     ad_evidence.set_is_complete();
     ad_evidence_for_navigation = ad_evidence;
@@ -291,7 +293,7 @@ void ContentSubresourceFilterThrottleManager::DidFinishInFrameNavigation(
       !(navigation_handle->HasCommitted() &&
         !navigation_handle->GetURL().IsAboutBlank()) &&
       !navigation_handle->IsWaitingToCommit() &&
-      !ad_frames_.contains(frame_tree_node_id)) {
+      !base::Contains(ad_frames_, frame_tree_node_id)) {
     EnsureFrameAdEvidence(navigation_handle).set_is_complete();
 
     // TODO(crbug.com/342351452): Remove these temporary crash keys once rare
@@ -544,10 +546,10 @@ void ContentSubresourceFilterThrottleManager::OnChildFrameNavigationEvaluated(
 
   // TODO(crbug.com/347625215): This is rarely hit. After fixing, upgrade to a
   // CHECK.
-  DCHECK_EQ(
-      ad_evidence.parent_is_ad(),
-      ad_frames_.contains(navigation_handle->GetParentFrameOrOuterDocument()
-                              ->GetFrameTreeNodeId()));
+  DCHECK_EQ(ad_evidence.parent_is_ad(),
+            base::Contains(ad_frames_,
+                           navigation_handle->GetParentFrameOrOuterDocument()
+                               ->GetFrameTreeNodeId()));
 
   ad_evidence.UpdateFilterListResult(
       InterpretLoadPolicyAsEvidence(load_policy));
@@ -570,8 +572,8 @@ void ContentSubresourceFilterThrottleManager::
   }
   MaybeCreateAndAddChildNavigationThrottle(registry);
 
-  CHECK(!ongoing_activation_throttles_.contains(
-      navigation_handle.GetNavigationId()));
+  CHECK(!base::Contains(ongoing_activation_throttles_,
+                        navigation_handle.GetNavigationId()));
   if (auto activation_throttle =
           MaybeCreateActivationStateComputingThrottle(registry)) {
     ongoing_activation_throttles_[navigation_handle.GetNavigationId()] =
@@ -582,7 +584,7 @@ void ContentSubresourceFilterThrottleManager::
 
 bool ContentSubresourceFilterThrottleManager::IsFrameTaggedAsAd(
     content::FrameTreeNodeId frame_tree_node_id) const {
-  return ad_frames_.contains(frame_tree_node_id);
+  return base::Contains(ad_frames_, frame_tree_node_id);
 }
 
 bool ContentSubresourceFilterThrottleManager::IsRenderFrameHostTaggedAsAd(
@@ -761,7 +763,7 @@ void ContentSubresourceFilterThrottleManager::SetIsAdFrame(
     bool is_ad_frame) {
   content::FrameTreeNodeId frame_tree_node_id =
       render_frame_host->GetFrameTreeNodeId();
-  CHECK(tracked_ad_evidence_.contains(frame_tree_node_id));
+  CHECK(base::Contains(tracked_ad_evidence_, frame_tree_node_id));
 
   // TODO(crbug.com/373985560): This is rarely hit. After fixing, upgrade to a
   // CHECK.
@@ -770,7 +772,7 @@ void ContentSubresourceFilterThrottleManager::SetIsAdFrame(
   CHECK(render_frame_host->GetParentOrOuterDocument());
 
   // `ad_frames_` does not need updating.
-  if (is_ad_frame == ad_frames_.contains(frame_tree_node_id)) {
+  if (is_ad_frame == base::Contains(ad_frames_, frame_tree_node_id)) {
     return;
   }
 
@@ -794,7 +796,7 @@ void ContentSubresourceFilterThrottleManager::SetIsAdFrameForTesting(
     bool is_ad_frame) {
   CHECK(render_frame_host->GetParentOrOuterDocument());
   if (is_ad_frame ==
-      ad_frames_.contains(render_frame_host->GetFrameTreeNodeId())) {
+      base::Contains(ad_frames_, render_frame_host->GetFrameTreeNodeId())) {
     return;
   }
 
@@ -910,8 +912,8 @@ void ContentSubresourceFilterThrottleManager::AdScriptDidCreateFencedFrame(
     return;
   }
 
-  CHECK(
-      !tracked_ad_evidence_.contains(fenced_frame_root->GetFrameTreeNodeId()));
+  CHECK(!base::Contains(tracked_ad_evidence_,
+                        fenced_frame_root->GetFrameTreeNodeId()));
   OnChildFrameWasCreatedByAdScript(fenced_frame_root);
 }
 
@@ -953,7 +955,8 @@ ContentSubresourceFilterThrottleManager::EnsureFrameAdEvidence(
   CHECK(parent_frame_tree_node_id);
   return tracked_ad_evidence_
       .emplace(frame_tree_node_id,
-               /*parent_is_ad=*/ad_frames_.contains(parent_frame_tree_node_id))
+               /*parent_is_ad=*/base::Contains(ad_frames_,
+                                               parent_frame_tree_node_id))
       .first->second;
 }
 
diff --git a/components/subresource_filter/content/browser/subresource_filter_observer_test_utils.cc b/components/subresource_filter/content/browser/subresource_filter_observer_test_utils.cc
index 1aa1146726..a397acde9d 100644
--- a/components/subresource_filter/content/browser/subresource_filter_observer_test_utils.cc
+++ b/components/subresource_filter/content/browser/subresource_filter_observer_test_utils.cc
@@ -5,6 +5,7 @@
 #include "components/subresource_filter/content/browser/subresource_filter_observer_test_utils.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/types/optional_util.h"
 #include "components/subresource_filter/core/mojom/subresource_filter.mojom.h"
@@ -83,7 +84,7 @@ TestSubresourceFilterObserver::GetPageActivation(const GURL& url) const {
 
 bool TestSubresourceFilterObserver::GetIsAdFrame(
     content::FrameTreeNodeId frame_tree_node_id) const {
-  return ad_frames_.contains(frame_tree_node_id);
+  return base::Contains(ad_frames_, frame_tree_node_id);
 }
 
 std::optional<LoadPolicy>
diff --git a/components/subresource_filter/core/browser/subresource_filter_features.cc b/components/subresource_filter/core/browser/subresource_filter_features.cc
index b617af5716..3386229935 100644
--- a/components/subresource_filter/core/browser/subresource_filter_features.cc
+++ b/components/subresource_filter/core/browser/subresource_filter_features.cc
@@ -12,6 +12,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/no_destructor.h"
 #include "base/rand_util.h"
@@ -422,7 +423,7 @@ scoped_refptr<ConfigurationList> GetEnabledConfigurations() {
 }
 
 bool HasEnabledConfiguration(const Configuration& config) {
-  return std::ranges::contains(
+  return base::Contains(
       GetEnabledConfigurations()->configs_by_decreasing_priority(), config);
 }
 
diff --git a/components/sync/service/data_type_manager_impl.cc b/components/sync/service/data_type_manager_impl.cc
index beee645e8f..7a6cf754c3 100644
--- a/components/sync/service/data_type_manager_impl.cc
+++ b/components/sync/service/data_type_manager_impl.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/barrier_callback.h"
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -364,7 +365,7 @@ TypeStatusMapForDebugging DataTypeManagerImpl::GetTypeStatusMapForDebugging(
     TypeStatusForDebugging& type_status = result[type];
     type_status.state = DataTypeController::StateToString(controller->state());
 
-    if (data_type_error_map.contains(type)) {
+    if (base::Contains(data_type_error_map, type)) {
       const SyncError& error = data_type_error_map.at(type);
       switch (error.error_type()) {
         case SyncError::MODEL_ERROR:
@@ -445,7 +446,7 @@ void DataTypeManagerImpl::GetAllNodesForDebugging(
       continue;
     }
 
-    CHECK(controllers_.contains(type));
+    CHECK(base::Contains(controllers_, type));
     const std::unique_ptr<DataTypeController>& controller =
         controllers_.at(type);
 
@@ -467,7 +468,7 @@ void DataTypeManagerImpl::GetEntityCountsForDebugging(
 }
 
 DataTypeController* DataTypeManagerImpl::GetControllerForTest(DataType type) {
-  CHECK(controllers_.contains(type));
+  CHECK(base::Contains(controllers_, type));
   return controllers_.at(type).get();
 }
 
@@ -1028,7 +1029,7 @@ void DataTypeManagerImpl::RecordMemoryUsageAndCountsHistograms() {
       continue;
     }
 
-    CHECK(controllers_.contains(type));
+    CHECK(base::Contains(controllers_, type));
     controllers_.at(type)->RecordMemoryUsageAndCountsHistograms();
   }
 }
diff --git a/components/sync/service/device_statistics_tracker.cc b/components/sync/service/device_statistics_tracker.cc
index 9c382582c9..4b9c43262f 100644
--- a/components/sync/service/device_statistics_tracker.cc
+++ b/components/sync/service/device_statistics_tracker.cc
@@ -4,10 +4,10 @@
 
 #include "components/sync/service/device_statistics_tracker.h"
 
-#include <algorithm>
 #include <utility>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/rand_util.h"
 #include "base/task/sequenced_task_runner.h"
@@ -120,7 +120,7 @@ void DeviceStatisticsTracker::Start(base::OnceClosure callback) {
       identity_manager_->GetAccountsWithRefreshTokens();
 
   if (!primary_account_.IsEmpty() &&
-      !std::ranges::contains(accounts, primary_account_)) {
+      !base::Contains(accounts, primary_account_)) {
     // The primary account must have been removed between the constructor and
     // now, or something's wrong with the IdentityManager.
     base::SequencedTaskRunner::GetCurrentDefault()->PostTask(
diff --git a/components/sync/service/sync_prefs.cc b/components/sync/service/sync_prefs.cc
index 3010811c86..366b289a95 100644
--- a/components/sync/service/sync_prefs.cc
+++ b/components/sync/service/sync_prefs.cc
@@ -9,6 +9,7 @@
 #include "base/auto_reset.h"
 #include "base/base64.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/feature_list.h"
 #include "base/files/file_path.h"
diff --git a/components/sync/test/mock_connection_manager.cc b/components/sync/test/mock_connection_manager.cc
index d6e27a108f..913a3e6958 100644
--- a/components/sync/test/mock_connection_manager.cc
+++ b/components/sync/test/mock_connection_manager.cc
@@ -4,10 +4,10 @@
 
 #include "components/sync/test/mock_connection_manager.h"
 
-#include <algorithm>
 #include <map>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/location.h"
 #include "base/strings/stringprintf.h"
 #include "base/uuid.h"
@@ -511,7 +511,7 @@ bool MockConnectionManager::ShouldConflictThisCommit() {
 }
 
 bool MockConnectionManager::ShouldTransientErrorThisId(const std::string& id) {
-  return std::ranges::contains(transient_error_ids_, id);
+  return base::Contains(transient_error_ids_, id);
 }
 
 bool MockConnectionManager::ProcessCommit(
diff --git a/components/sync/test/test_sync_service.cc b/components/sync/test/test_sync_service.cc
index c5f2c9178c..972a974700 100644
--- a/components/sync/test/test_sync_service.cc
+++ b/components/sync/test/test_sync_service.cc
@@ -7,6 +7,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/time/time.h"
 #include "base/values.h"
 #include "build/build_config.h"
diff --git a/components/sync_bookmarks/bookmark_model_merger.cc b/components/sync_bookmarks/bookmark_model_merger.cc
index ef43d5a700..a482f437e4 100644
--- a/components/sync_bookmarks/bookmark_model_merger.cc
+++ b/components/sync_bookmarks/bookmark_model_merger.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/string_util.h"
diff --git a/components/sync_bookmarks/bookmark_specifics_conversions.cc b/components/sync_bookmarks/bookmark_specifics_conversions.cc
index 9d7af25225..009323df64 100644
--- a/components/sync_bookmarks/bookmark_specifics_conversions.cc
+++ b/components/sync_bookmarks/bookmark_specifics_conversions.cc
@@ -4,13 +4,13 @@
 
 #include "components/sync_bookmarks/bookmark_specifics_conversions.h"
 
-#include <algorithm>
 #include <string>
 #include <string_view>
 #include <unordered_set>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/hash/sha1.h"
@@ -195,7 +195,7 @@ std::string InferGuidForLegacyBookmark(
 }
 
 bool IsForbiddenTitleWithMaybeTrailingSpaces(const std::string& title) {
-  return std::ranges::contains(
+  return base::Contains(
       kForbiddenTitles,
       base::TrimWhitespaceASCII(title, base::TrimPositions::TRIM_TRAILING));
 }
diff --git a/components/sync_preferences/pref_model_associator.cc b/components/sync_preferences/pref_model_associator.cc
index 9746f5cd68..c183a9072f 100644
--- a/components/sync_preferences/pref_model_associator.cc
+++ b/components/sync_preferences/pref_model_associator.cc
@@ -12,6 +12,7 @@
 
 #include "base/auto_reset.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/json/json_reader.h"
 #include "base/json/json_string_value_serializer.h"
diff --git a/components/sync_preferences/preferences_merge_helper.cc b/components/sync_preferences/preferences_merge_helper.cc
index bad4b110ba..5ccaa9eb50 100644
--- a/components/sync_preferences/preferences_merge_helper.cc
+++ b/components/sync_preferences/preferences_merge_helper.cc
@@ -4,9 +4,8 @@
 
 #include "components/sync_preferences/preferences_merge_helper.h"
 
-#include <algorithm>
-
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "components/sync_preferences/pref_model_associator_client.h"
 #include "components/sync_preferences/syncable_prefs_database.h"
@@ -29,7 +28,7 @@ base::ListValue MergeListValues(const base::ListValue& local_value,
                                 const base::ListValue& server_value) {
   base::ListValue result = server_value.Clone();
   for (const auto& value : local_value) {
-    if (!std::ranges::contains(result, value)) {
+    if (!base::Contains(result, value)) {
       result.Append(value.Clone());
     }
   }
diff --git a/components/tab_groups/tab_group_visual_data.cc b/components/tab_groups/tab_group_visual_data.cc
index 3cc086d317..e8381e66e0 100644
--- a/components/tab_groups/tab_group_visual_data.cc
+++ b/components/tab_groups/tab_group_visual_data.cc
@@ -6,6 +6,7 @@
 
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/strings/utf_string_conversions.h"
 #include "components/tab_groups/tab_group_color.h"
 
@@ -26,7 +27,7 @@ TabGroupVisualData::TabGroupVisualData(std::u16string title,
       color_(TabGroupColorId::kGrey),
       is_collapsed_(is_collapsed) {
   auto color_id = static_cast<tab_groups::TabGroupColorId>(color_int);
-  if (tab_groups::GetTabGroupColorLabelMap().contains(color_id)) {
+  if (base::Contains(tab_groups::GetTabGroupColorLabelMap(), color_id)) {
     color_ = color_id;
   }
 }
diff --git a/components/translate/content/android/translate_message.cc b/components/translate/content/android/translate_message.cc
index 708e5cc050..7ba474ff97 100644
--- a/components/translate/content/android/translate_message.cc
+++ b/components/translate/content/android/translate_message.cc
@@ -7,7 +7,6 @@
 #include <stddef.h>
 #include <stdint.h>
 
-#include <algorithm>
 #include <type_traits>
 #include <utility>
 #include <vector>
@@ -15,6 +14,7 @@
 #include "base/android/jni_android.h"
 #include "base/android/jni_array.h"
 #include "base/android/jni_string.h"
+#include "base/containers/contains.h"
 #include "base/containers/heap_array.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/logging.h"
@@ -648,7 +648,7 @@ TranslateMessage::ConstructLanguagePickerMenu(
 
   // Add the content languages to the menu.
   for (const std::string& content_language_code : content_language_codes) {
-    if (std::ranges::contains(skip_language_codes, content_language_code)) {
+    if (base::Contains(skip_language_codes, content_language_code)) {
       continue;
     }
 
@@ -676,7 +676,7 @@ TranslateMessage::ConstructLanguagePickerMenu(
   // Add the full list of languages to the menu.
   for (size_t i = 0U; i < ui_languages_manager_->GetNumberOfLanguages(); ++i) {
     std::string code = ui_languages_manager_->GetLanguageCodeAt(i);
-    if (std::ranges::contains(skip_language_codes, code)) {
+    if (base::Contains(skip_language_codes, code)) {
       continue;
     }
 
diff --git a/components/translate/core/browser/translate_prefs.cc b/components/translate/core/browser/translate_prefs.cc
index 30a5aa0d91..e0ff762155 100644
--- a/components/translate/core/browser/translate_prefs.cc
+++ b/components/translate/core/browser/translate_prefs.cc
@@ -13,6 +13,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/i18n/rtl.h"
 #include "base/json/values_util.h"
@@ -94,8 +95,8 @@ void MigrateObsoleteAlwaysTranslateLanguagesPref(PrefService* prefs) {
     // If the old pair's source language matches any of the never-translate
     // languages, it probably means that this source language was set to never
     // be translated after the old pref was deprecated, so avoid this conflict.
-    if (prefs->GetList(prefs::kBlockedLanguages)
-            .contains(old_language_pair.first)) {
+    if (base::Contains(prefs->GetList(prefs::kBlockedLanguages),
+                       old_language_pair.first)) {
       continue;
     }
 
@@ -242,7 +243,7 @@ bool TranslatePrefs::IsBlockedLanguage(std::string_view input_language) const {
   language::ToTranslateLanguageSynonym(&canonical_lang);
   const base::ListValue& blocked =
       prefs_->GetList(translate::prefs::kBlockedLanguages);
-  return blocked.contains(canonical_lang);
+  return base::Contains(blocked, base::Value(std::move(canonical_lang)));
 }
 
 void TranslatePrefs::BlockLanguage(std::string_view input_language) {
@@ -339,7 +340,7 @@ void TranslatePrefs::AddToLanguageList(std::string_view input_language,
   }
 
   // Add the language to the list.
-  if (!std::ranges::contains(languages, chrome_language)) {
+  if (!base::Contains(languages, chrome_language)) {
     user_selected_languages.push_back(chrome_language);
     language_prefs_->SetUserSelectedLanguagesList(user_selected_languages);
   }
diff --git a/components/translate/translate_internals/translate_internals_handler.cc b/components/translate/translate_internals/translate_internals_handler.cc
index ba5f8783e1..e5c3636821 100644
--- a/components/translate/translate_internals/translate_internals_handler.cc
+++ b/components/translate/translate_internals/translate_internals_handler.cc
@@ -12,6 +12,7 @@
 
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "components/language/core/browser/pref_names.h"
 #include "components/prefs/pref_service.h"
@@ -110,7 +111,7 @@ void TranslateInternalsHandler::OnTranslateError(
   base::DictValue dict;
   dict.Set("time", details.time.InMillisecondsFSinceUnixEpoch());
   dict.Set("url", details.url.spec());
-  dict.Set("error", std::to_underlying(details.error));
+  dict.Set("error", base::to_underlying(details.error));
   SendMessageToJs("translateErrorDetailsAdded", dict);
 }
 
diff --git a/components/trusted_vault/test/fake_trusted_vault_client.cc b/components/trusted_vault/test/fake_trusted_vault_client.cc
index c2fb7a61a8..b2d4ffd4e0 100644
--- a/components/trusted_vault/test/fake_trusted_vault_client.cc
+++ b/components/trusted_vault/test/fake_trusted_vault_client.cc
@@ -4,13 +4,13 @@
 
 #include "components/trusted_vault/test/fake_trusted_vault_client.h"
 
-#include <algorithm>
 #include <list>
 #include <map>
 #include <string>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/location.h"
@@ -65,7 +65,7 @@ FakeTrustedVaultClient::FakeServer::RequestRotatedKeysFromServer(
   }
 
   const std::vector<std::vector<uint8_t>>& latest_keys = it->second;
-  if (!std::ranges::contains(latest_keys, key_known_by_client)) {
+  if (!base::Contains(latest_keys, key_known_by_client)) {
     // |key_known_by_client| is invalid or too old: cannot be used to follow
     // key rotation.
     return {};
diff --git a/components/ui_devtools/views/ui_element_with_metadata.cc b/components/ui_devtools/views/ui_element_with_metadata.cc
index 4cadacc10d..eeed9907c9 100644
--- a/components/ui_devtools/views/ui_element_with_metadata.cc
+++ b/components/ui_devtools/views/ui_element_with_metadata.cc
@@ -4,8 +4,7 @@
 
 #include "components/ui_devtools/views/ui_element_with_metadata.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/notreached.h"
 #include "base/strings/strcat.h"
@@ -113,8 +112,7 @@ bool UIElementWithMetaData::SetPropertiesFromString(const std::string& text) {
     // based on the allowed values specified in the metadata.
     auto valid_values = member->GetValidValues();
     if (!valid_values.empty() &&
-        !std::ranges::contains(valid_values,
-                               base::UTF8ToUTF16(property_value))) {
+        !base::Contains(valid_values, base::UTF8ToUTF16(property_value))) {
       // Ignore the value.
       continue;
     }
diff --git a/components/ui_devtools/views/view_element.cc b/components/ui_devtools/views/view_element.cc
index 0be6ec98c7..454179b20f 100644
--- a/components/ui_devtools/views/view_element.cc
+++ b/components/ui_devtools/views/view_element.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
@@ -105,7 +106,7 @@ void ViewElement::OnChildViewRemoved(views::View* parent, views::View* view) {
 
 void ViewElement::OnChildViewAdded(views::View* parent, views::View* view) {
   DCHECK_EQ(parent, view_);
-  if (std::ranges::contains(children(), view, [](UIElement* child) {
+  if (base::Contains(children(), view, [](UIElement* child) {
         return UIElement::GetBackingElement<views::View, ViewElement>(child);
       })) {
     RebuildTree();
@@ -184,10 +185,9 @@ void ViewElement::PaintRect() const {
 
 bool ViewElement::FindMatchByElementID(
     const ui::ElementIdentifier& identifier) {
-  return std::ranges::contains(
-      views::ElementTrackerViews::GetInstance()
-          ->GetAllMatchingViewsInAnyContext(identifier),
-      view_);
+  return base::Contains(views::ElementTrackerViews::GetInstance()
+                            ->GetAllMatchingViewsInAnyContext(identifier),
+                        view_);
 }
 
 bool ViewElement::DispatchMouseEvent(protocol::DOM::MouseEvent* event) {
diff --git a/components/ukm/observers/ukm_consent_state_observer.cc b/components/ukm/observers/ukm_consent_state_observer.cc
index 068be359c9..11fbdbfe0a 100644
--- a/components/ukm/observers/ukm_consent_state_observer.cc
+++ b/components/ukm/observers/ukm_consent_state_observer.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram_functions.h"
 #include "components/metrics/metrics_switches.h"
@@ -207,7 +208,7 @@ void UkmConsentStateObserver::OnUrlKeyedDataCollectionConsentStateChanged(
 void UkmConsentStateObserver::UpdateProfileState(
     syncer::SyncService* sync,
     UrlKeyedDataCollectionConsentHelper* consent_helper) {
-  DCHECK(previous_states_.contains(sync));
+  DCHECK(base::Contains(previous_states_, sync));
   const ProfileState& previous_state = previous_states_[sync];
   DCHECK(consent_helper);
   ProfileState state = GetProfileState(sync, consent_helper);
@@ -223,7 +224,7 @@ void UkmConsentStateObserver::UpdateProfileState(
 }
 
 void UkmConsentStateObserver::OnSyncShutdown(syncer::SyncService* sync) {
-  DCHECK(previous_states_.contains(sync));
+  DCHECK(base::Contains(previous_states_, sync));
   auto found = consent_helpers_.find(sync);
   if (found != consent_helpers_.end()) {
     found->second->RemoveObserver(this);
diff --git a/components/ukm/ukm_recorder_impl.cc b/components/ukm/ukm_recorder_impl.cc
index d26787fcfc..e4f5207585 100644
--- a/components/ukm/ukm_recorder_impl.cc
+++ b/components/ukm/ukm_recorder_impl.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/component_export.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
@@ -440,7 +441,7 @@ void UkmRecorderImpl::StoreRecordingsInReport(Report* report) {
         continue;
       }
       // Omit entryless sources from the report.
-      if (!source_ids_seen.contains(kv.first)) {
+      if (!base::Contains(source_ids_seen, kv.first)) {
         continue;
       }
 
@@ -562,7 +563,7 @@ void UkmRecorderImpl::StoreRecordingsInReport(Report* report) {
   // having any events in this reporting cycle.
   int num_sources_entryless = 0;
   for (const auto& kv : recordings_.sources) {
-    if (!source_ids_seen.contains(kv.first)) {
+    if (!base::Contains(source_ids_seen, kv.first)) {
       num_sources_entryless++;
     }
   }
@@ -584,7 +585,7 @@ int UkmRecorderImpl::PruneData(std::set<SourceId>& source_ids_seen) {
   // existing sources that were seen in this report.
   auto it = source_ids_seen.begin();
   while (it != source_ids_seen.end()) {
-    if (!recordings_.sources.contains(*it)) {
+    if (!base::Contains(recordings_.sources, *it)) {
       it = source_ids_seen.erase(it);
     } else {
       it++;
@@ -690,7 +691,7 @@ void UkmRecorderImpl::UpdateSourceURL(SourceId source_id,
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   DCHECK(GetSourceIdType(source_id) != SourceIdType::NO_URL_ID);
 
-  if (recordings_.sources.contains(source_id))
+  if (base::Contains(recordings_.sources, source_id))
     return;
 
   const GURL sanitized_url = SanitizeURL(unsanitized_url);
@@ -728,7 +729,7 @@ void UkmRecorderImpl::RecordNavigation(
     SourceId source_id,
     const UkmSource::NavigationData& unsanitized_navigation_data) {
   DCHECK(GetSourceIdType(source_id) == SourceIdType::NAVIGATION_ID);
-  DCHECK(!recordings_.sources.contains(source_id));
+  DCHECK(!base::Contains(recordings_.sources, source_id));
   // TODO(csharrison): Consider changing this behavior so the Source isn't even
   // recorded at all if the final URL in |unsanitized_navigation_data| should
   // not be recorded.
diff --git a/components/ukm/ukm_test_helper.cc b/components/ukm/ukm_test_helper.cc
index 28416ac704..a832c44f21 100644
--- a/components/ukm/ukm_test_helper.cc
+++ b/components/ukm/ukm_test_helper.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/run_loop.h"
 #include "components/metrics/log_decoder.h"
@@ -72,12 +73,13 @@ UkmSource* UkmTestHelper::GetSource(SourceId source_id) {
 }
 
 bool UkmTestHelper::HasSource(SourceId source_id) {
-  return ukm_service_ && ukm_service_->sources().contains(source_id);
+  return ukm_service_ && base::Contains(ukm_service_->sources(), source_id);
 }
 
 bool UkmTestHelper::IsSourceObsolete(SourceId source_id) {
   return ukm_service_ &&
-         ukm_service_->recordings_.obsolete_source_ids.contains(source_id);
+         base::Contains(ukm_service_->recordings_.obsolete_source_ids,
+                        source_id);
 }
 
 void UkmTestHelper::RecordSourceForTesting(SourceId source_id) {
diff --git a/components/update_client/net/url_loader_post_interceptor.cc b/components/update_client/net/url_loader_post_interceptor.cc
index ca265871d3..2fcd9270c8 100644
--- a/components/update_client/net/url_loader_post_interceptor.cc
+++ b/components/update_client/net/url_loader_post_interceptor.cc
@@ -4,9 +4,8 @@
 
 #include "components/update_client/net/url_loader_post_interceptor.h"
 
-#include <algorithm>
-
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/run_loop.h"
@@ -166,7 +165,7 @@ void URLLoaderPostInterceptor::InitializeWithInterceptor() {
           replacements.ClearQuery();
           url = url.ReplaceComponents(replacements);
         }
-        if (!std::ranges::contains(filtered_urls_, url)) {
+        if (!base::Contains(filtered_urls_, url)) {
           return;
         }
 
@@ -218,7 +217,7 @@ URLLoaderPostInterceptor::RequestHandler(
     replacements.ClearQuery();
     url = url.ReplaceComponents(replacements);
   }
-  if (!std::ranges::contains(filtered_urls_, url)) {
+  if (!base::Contains(filtered_urls_, url)) {
     return nullptr;
   }
 
diff --git a/components/update_client/update_client.cc b/components/update_client/update_client.cc
index 78077ed8c8..a39e52a691 100644
--- a/components/update_client/update_client.cc
+++ b/components/update_client/update_client.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/location.h"
@@ -209,14 +210,14 @@ bool UpdateClientImpl::IsUpdating(const std::string& id) const {
 
   for (const auto& task : tasks_) {
     const auto ids = task->GetIds();
-    if (std::ranges::contains(ids, id)) {
+    if (base::Contains(ids, id)) {
       return true;
     }
   }
 
   for (const auto& task : task_queue_) {
     const auto ids = task->GetIds();
-    if (std::ranges::contains(ids, id)) {
+    if (base::Contains(ids, id)) {
       return true;
     }
   }
diff --git a/components/update_client/utils.cc b/components/update_client/utils.cc
index f49ea3fd19..7e163c1645 100644
--- a/components/update_client/utils.cc
+++ b/components/update_client/utils.cc
@@ -16,6 +16,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/heap_array.h"
 #include "base/containers/span.h"
 #include "base/files/file.h"
@@ -134,7 +135,7 @@ bool IsValidInstallerAttributePart(const std::string& part,
   return part.size() >= min_length && part.size() <= max_length &&
          std::ranges::all_of(part, [&special_chars](char ch) {
            return base::IsAsciiAlpha(ch) || base::IsAsciiDigit(ch) ||
-                  special_chars.contains(ch);
+                  base::Contains(special_chars, ch);
          });
 }
 
diff --git a/components/url_formatter/spoof_checks/idn_spoof_checker.cc b/components/url_formatter/spoof_checks/idn_spoof_checker.cc
index 83b114ea27..1156d8d4ef 100644
--- a/components/url_formatter/spoof_checks/idn_spoof_checker.cc
+++ b/components/url_formatter/spoof_checks/idn_spoof_checker.cc
@@ -4,12 +4,12 @@
 
 #include "components/url_formatter/spoof_checks/idn_spoof_checker.h"
 
-#include <algorithm>
 #include <bit>
 #include <cstdint>
 #include <string_view>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/no_destructor.h"
 #include "base/numerics/safe_conversions.h"
@@ -429,7 +429,7 @@ IDNSpoofCheckerResult IDNSpoofChecker::SafeToDisplayAsUnicode(
       if (IsLabelWholeScriptConfusableForScript(*script, label_string) &&
           !IsWholeScriptConfusableAllowedForTLD(*script, top_level_domain,
                                                 top_level_domain_unicode) &&
-          !std::ranges::contains(kAllowedWholeScriptConfusableWords, label)) {
+          !base::Contains(kAllowedWholeScriptConfusableWords, label)) {
         return IDNSpoofCheckerResult::kWholeScriptConfusable;
       }
     }
@@ -803,7 +803,7 @@ bool IDNSpoofChecker::IsWholeScriptConfusableAllowedForTLD(
   if (script.all_letters->containsSome(tld_string)) {
     return true;
   }
-  return std::ranges::contains(script.allowed_tlds, tld);
+  return base::Contains(script.allowed_tlds, tld);
 }
 
 // static
diff --git a/components/url_matcher/url_matcher.cc b/components/url_matcher/url_matcher.cc
index 6ae8ec5fa0..25d3f96d21 100644
--- a/components/url_matcher/url_matcher.cc
+++ b/components/url_matcher/url_matcher.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/notreached.h"
 #include "url/gurl.h"
@@ -224,7 +225,7 @@ bool URLMatcherCondition::IsMatch(
     const std::set<MatcherStringPattern::ID>& matching_patterns,
     const GURL& url) const {
   DCHECK(string_pattern_);
-  if (!matching_patterns.contains(string_pattern_->id())) {
+  if (!base::Contains(matching_patterns, string_pattern_->id())) {
     return false;
   }
   // The criteria HOST_CONTAINS, PATH_CONTAINS, QUERY_CONTAINS are based on
@@ -467,7 +468,7 @@ void URLMatcherConditionFactory::ForgetUnusedPatterns(
     const std::set<MatcherStringPattern::ID>& used_patterns) {
   auto i = substring_pattern_singletons_.begin();
   while (i != substring_pattern_singletons_.end()) {
-    if (used_patterns.contains(i->first->id())) {
+    if (base::Contains(used_patterns, i->first->id())) {
       ++i;
     } else {
       substring_pattern_singletons_.erase(i++);
@@ -476,7 +477,7 @@ void URLMatcherConditionFactory::ForgetUnusedPatterns(
 
   i = regex_pattern_singletons_.begin();
   while (i != regex_pattern_singletons_.end()) {
-    if (used_patterns.contains(i->first->id())) {
+    if (base::Contains(used_patterns, i->first->id())) {
       ++i;
     } else {
       regex_pattern_singletons_.erase(i++);
@@ -485,7 +486,7 @@ void URLMatcherConditionFactory::ForgetUnusedPatterns(
 
   i = origin_and_path_regex_pattern_singletons_.begin();
   while (i != origin_and_path_regex_pattern_singletons_.end()) {
-    if (used_patterns.contains(i->first->id())) {
+    if (base::Contains(used_patterns, i->first->id())) {
       ++i;
     } else {
       origin_and_path_regex_pattern_singletons_.erase(i++);
@@ -714,7 +715,7 @@ URLMatcherSchemeFilter::URLMatcherSchemeFilter(
 URLMatcherSchemeFilter::~URLMatcherSchemeFilter() = default;
 
 bool URLMatcherSchemeFilter::IsMatch(const GURL& url) const {
-  return std::ranges::contains(filters_, url.GetScheme());
+  return base::Contains(filters_, url.GetScheme());
 }
 
 //
@@ -854,7 +855,7 @@ bool URLMatcherConditionSet::IsMatch(
   // elements are found, no need to verify match that is expected to take more
   // cycles.
   for (auto i = query_conditions_.begin(); i != query_conditions_.end(); ++i) {
-    if (!matching_patterns.contains(i->string_pattern()->id())) {
+    if (!base::Contains(matching_patterns, i->string_pattern()->id())) {
       return false;
     }
   }
diff --git a/components/url_pattern_index/url_pattern_index.cc b/components/url_pattern_index/url_pattern_index.cc
index d9abf238b3..1e6e48718f 100644
--- a/components/url_pattern_index/url_pattern_index.cc
+++ b/components/url_pattern_index/url_pattern_index.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/callback.h"
 #include "base/memory/raw_ref.h"
@@ -674,7 +675,7 @@ const flat::UrlRule* FindMatchAmongCandidates(
     if (!DoesURLMatchRequestDomainList(url, *rule))
       continue;
 
-    if (disabled_rule_ids.contains(rule->id()))
+    if (base::Contains(disabled_rule_ids, rule->id()))
       continue;
 
     if (matched_rules)
diff --git a/components/user_education/common/feature_promo/feature_promo_controller.cc b/components/user_education/common/feature_promo/feature_promo_controller.cc
index 6bd6aa7396..5165da62b8 100644
--- a/components/user_education/common/feature_promo/feature_promo_controller.cc
+++ b/components/user_education/common/feature_promo/feature_promo_controller.cc
@@ -9,6 +9,7 @@
 
 #include "base/auto_reset.h"
 #include "base/callback_list.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -179,7 +180,7 @@ bool FeaturePromoControllerCommon::HasPromoBeenDismissed(
       if (params.key.empty()) {
         return false;
       }
-      return data->shown_for_keys.contains(params.key);
+      return base::Contains(data->shown_for_keys, params.key);
   }
 }
 
diff --git a/components/user_education/common/feature_promo/feature_promo_controller.h b/components/user_education/common/feature_promo/feature_promo_controller.h
index ed9448b7cf..c22a39783a 100644
--- a/components/user_education/common/feature_promo/feature_promo_controller.h
+++ b/components/user_education/common/feature_promo/feature_promo_controller.h
@@ -5,7 +5,6 @@
 #ifndef COMPONENTS_USER_EDUCATION_COMMON_FEATURE_PROMO_FEATURE_PROMO_CONTROLLER_H_
 #define COMPONENTS_USER_EDUCATION_COMMON_FEATURE_PROMO_FEATURE_PROMO_CONTROLLER_H_
 
-#include <algorithm>
 #include <initializer_list>
 #include <memory>
 #include <optional>
@@ -14,6 +13,7 @@
 
 #include "base/auto_reset.h"
 #include "base/callback_list.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_forward.h"
@@ -165,9 +165,9 @@ class FeaturePromoController {
                      Args... additional_status) const {
     const FeaturePromoStatus actual = GetPromoStatus(iph_feature);
     const std::initializer_list<FeaturePromoStatus> list{additional_status...};
-    DCHECK(!std::ranges::contains(list, FeaturePromoStatus::kNotRunning));
+    DCHECK(!base::Contains(list, FeaturePromoStatus::kNotRunning));
     return actual == FeaturePromoStatus::kBubbleShowing ||
-           std::ranges::contains(list, actual);
+           base::Contains(list, actual);
   }
 
   // Starts a promo with the settings for skipping any logging or filtering
diff --git a/components/user_education/common/feature_promo/feature_promo_lifecycle.cc b/components/user_education/common/feature_promo/feature_promo_lifecycle.cc
index 52197aca24..1a41bdb994 100644
--- a/components/user_education/common/feature_promo/feature_promo_lifecycle.cc
+++ b/components/user_education/common/feature_promo/feature_promo_lifecycle.cc
@@ -7,6 +7,7 @@
 #include <optional>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
diff --git a/components/user_education/common/feature_promo/feature_promo_registry.cc b/components/user_education/common/feature_promo/feature_promo_registry.cc
index 3925a2f3d3..c1a13f481a 100644
--- a/components/user_education/common/feature_promo/feature_promo_registry.cc
+++ b/components/user_education/common/feature_promo/feature_promo_registry.cc
@@ -4,6 +4,7 @@
 
 #include "components/user_education/common/feature_promo/feature_promo_registry.h"
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/feature_list.h"
 #include "components/user_education/common/feature_promo/feature_promo_specification.h"
diff --git a/components/user_education/common/feature_promo/feature_promo_registry.h b/components/user_education/common/feature_promo/feature_promo_registry.h
index 83b024e67a..81d1362c8b 100644
--- a/components/user_education/common/feature_promo/feature_promo_registry.h
+++ b/components/user_education/common/feature_promo/feature_promo_registry.h
@@ -7,6 +7,7 @@
 
 #include <map>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/feature_list.h"
 #include "components/user_education/common/feature_promo/feature_promo_specification.h"
diff --git a/components/user_education/common/ntp_promo/ntp_promo_controller.cc b/components/user_education/common/ntp_promo/ntp_promo_controller.cc
index d3c31f30d3..5cee9d7563 100644
--- a/components/user_education/common/ntp_promo/ntp_promo_controller.cc
+++ b/components/user_education/common/ntp_promo/ntp_promo_controller.cc
@@ -4,8 +4,7 @@
 
 #include "components/user_education/common/ntp_promo/ntp_promo_controller.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/strcat.h"
@@ -300,7 +299,7 @@ bool NtpPromoController::ShouldShowPromo(const NtpPromoIdentifier& id,
   }
 
   // If the promo is suppressed via Finch, don't show it (ie. a kill switch).
-  if (std::ranges::contains(params_.suppress_list, id)) {
+  if (base::Contains(params_.suppress_list, id)) {
     return false;
   }
 
diff --git a/components/user_education/common/ntp_promo/ntp_promo_order.cc b/components/user_education/common/ntp_promo/ntp_promo_order.cc
index e022c4b1ce..552e0aa6dd 100644
--- a/components/user_education/common/ntp_promo/ntp_promo_order.cc
+++ b/components/user_education/common/ntp_promo/ntp_promo_order.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "components/user_education/common/ntp_promo/ntp_promo_identifier.h"
 #include "components/user_education/common/ntp_promo/ntp_promo_registry.h"
@@ -76,7 +77,7 @@ std::vector<NtpPromoIdentifier> NtpPromoOrderPolicy::OrderPendingPromos(
     const auto* spec = registry_->GetNtpPromoSpecification(id);
     CHECK(spec);
     for (const auto& after : spec->show_after()) {
-      if (std::ranges::contains(ids, after)) {
+      if (base::Contains(ids, after)) {
         show_after_map[id].insert(after);
       }
     }
diff --git a/components/user_education/common/ntp_promo/ntp_promo_registry.cc b/components/user_education/common/ntp_promo/ntp_promo_registry.cc
index 89fbafce28..b9b4801a29 100644
--- a/components/user_education/common/ntp_promo/ntp_promo_registry.cc
+++ b/components/user_education/common/ntp_promo/ntp_promo_registry.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "components/user_education/common/ntp_promo/ntp_promo_specification.h"
 
 namespace user_education {
diff --git a/components/user_education/common/product_messaging_controller.cc b/components/user_education/common/product_messaging_controller.cc
index 51d634c14f..3d47335ada 100644
--- a/components/user_education/common/product_messaging_controller.cc
+++ b/components/user_education/common/product_messaging_controller.cc
@@ -4,11 +4,11 @@
 
 #include "components/user_education/common/product_messaging_controller.h"
 
-#include <algorithm>
 #include <sstream>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
 #include "base/notreached.h"
@@ -114,7 +114,7 @@ void ProductMessagingController::Init(
 
 bool ProductMessagingController::IsNoticeQueued(
     RequiredNoticeId notice_id) const {
-  return pending_notices_.contains(notice_id);
+  return base::Contains(pending_notices_, notice_id);
 }
 
 void ProductMessagingController::QueueRequiredNotice(
@@ -124,7 +124,7 @@ void ProductMessagingController::QueueRequiredNotice(
     std::initializer_list<RequiredNoticeId> blocked_by) {
   CHECK(notice_id);
   CHECK(!ready_to_start_callback.is_null());
-  CHECK(!std::ranges::contains(blocked_by, internal::kShowAfterAllNotices));
+  CHECK(!base::Contains(blocked_by, internal::kShowAfterAllNotices));
 
   // Cannot re-queue the current notice.
   if (current_notice_ == notice_id) {
diff --git a/components/user_education/common/tutorial/tutorial_registry.cc b/components/user_education/common/tutorial/tutorial_registry.cc
index dd2c2201c5..0c55790f76 100644
--- a/components/user_education/common/tutorial/tutorial_registry.cc
+++ b/components/user_education/common/tutorial/tutorial_registry.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "components/user_education/common/tutorial/tutorial.h"
 #include "components/user_education/common/tutorial/tutorial_description.h"
@@ -20,7 +21,7 @@ TutorialRegistry::TutorialRegistry() = default;
 TutorialRegistry::~TutorialRegistry() = default;
 
 bool TutorialRegistry::IsTutorialRegistered(TutorialIdentifier id) const {
-  return tutorial_registry_.contains(id);
+  return base::Contains(tutorial_registry_, id);
 }
 
 const TutorialDescription* TutorialRegistry::GetTutorialDescription(
@@ -44,7 +45,7 @@ const std::vector<TutorialIdentifier> TutorialRegistry::GetTutorialIdentifiers()
 
 void TutorialRegistry::AddTutorial(TutorialIdentifier id,
                                    TutorialDescription description) {
-  if (tutorial_registry_.contains(id)) {
+  if (base::Contains(tutorial_registry_, id)) {
     return;
   }
 
diff --git a/components/user_education/webui/help_bubble_handler.cc b/components/user_education/webui/help_bubble_handler.cc
index 5b64ad7431..9deecd11a7 100644
--- a/components/user_education/webui/help_bubble_handler.cc
+++ b/components/user_education/webui/help_bubble_handler.cc
@@ -10,6 +10,7 @@
 
 #include "base/callback_list.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/memory/ptr_util.h"
 #include "base/memory/raw_ptr.h"
@@ -495,7 +496,7 @@ void HelpBubbleHandlerBase::BindTrackedElementHandler(
 
 bool HelpBubbleHandlerBase::ToggleHelpBubbleFocusForAccessibility(
     ui::ElementIdentifier anchor_id) {
-  if (element_data_.contains(anchor_id)) {
+  if (base::Contains(element_data_, anchor_id)) {
     GetClient()->ToggleFocusForAccessibility(anchor_id.GetName());
     return true;
   }
diff --git a/components/user_education/webui/whats_new_registry.cc b/components/user_education/webui/whats_new_registry.cc
index fc2f4b1cf3..5e812e717e 100644
--- a/components/user_education/webui/whats_new_registry.cc
+++ b/components/user_education/webui/whats_new_registry.cc
@@ -4,6 +4,7 @@
 
 #include "components/user_education/webui/whats_new_registry.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/values.h"
 #include "ui/webui/resources/js/browser_command/browser_command.mojom.h"
diff --git a/components/value_store/test_value_store_factory.cc b/components/value_store/test_value_store_factory.cc
index 40a15b61c3..b7b0b0eb85 100644
--- a/components/value_store/test_value_store_factory.cc
+++ b/components/value_store/test_value_store_factory.cc
@@ -4,6 +4,7 @@
 
 #include "components/value_store/test_value_store_factory.h"
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "components/value_store/leveldb_value_store.h"
 #include "components/value_store/testing_value_store.h"
@@ -43,7 +44,7 @@ void TestValueStoreFactory::DeleteValueStore(const base::FilePath& directory) {
 }
 
 bool TestValueStoreFactory::HasValueStore(const base::FilePath& directory) {
-  return value_store_map_.contains(directory);
+  return base::Contains(value_store_map_, directory);
 }
 
 ValueStore* TestValueStoreFactory::GetExisting(
diff --git a/components/variations/active_field_trials.cc b/components/variations/active_field_trials.cc
index df1fd220b7..ceb189c9a6 100644
--- a/components/variations/active_field_trials.cc
+++ b/components/variations/active_field_trials.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial.h"
 #include "base/no_destructor.h"
 #include "base/process/launch.h"
@@ -157,7 +158,7 @@ bool IsInSyntheticTrialGroup(std::string_view trial_name,
                              std::string_view trial_group) {
   std::vector<std::string> synthetic_trials;
   GetSyntheticTrialGroupIdsAsString(&synthetic_trials);
-  return std::ranges::contains(
+  return base::Contains(
       synthetic_trials,
       base::StringPrintf("%x-%x", HashName(trial_name), HashName(trial_group)));
 }
diff --git a/components/variations/child_process_field_trial_syncer.cc b/components/variations/child_process_field_trial_syncer.cc
index c66236851d..2e09785c24 100644
--- a/components/variations/child_process_field_trial_syncer.cc
+++ b/components/variations/child_process_field_trial_syncer.cc
@@ -9,6 +9,7 @@
 #include "base/auto_reset.h"
 #include "base/base_switches.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_list_including_low_anonymity.h"
 #include "components/variations/variations_crash_keys.h"
 
@@ -76,7 +77,7 @@ void ChildProcessFieldTrialSyncer::Init(
   base::FieldTrialListIncludingLowAnonymity::GetActiveFieldTrialGroups(
       &current_active_trials);
   for (const auto& trial : current_active_trials) {
-    if (!initially_active_trials.contains(trial.trial_name)) {
+    if (!base::Contains(initially_active_trials, trial.trial_name)) {
       activated_callback_.Run(trial.trial_name);
     }
   }
diff --git a/components/variations/field_trial_internals_utils.cc b/components/variations/field_trial_internals_utils.cc
index be19da338a..3733354f4e 100644
--- a/components/variations/field_trial_internals_utils.cc
+++ b/components/variations/field_trial_internals_utils.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/field_trial.h"
 #include "base/metrics/histogram_functions.h"
@@ -33,7 +34,7 @@ bool ContainsExperiment(const std::vector<variations::StudyGroupNames>& studies,
                         std::string_view experiment_name) {
   for (const auto& study : studies) {
     if (study.name == study_name) {
-      if (std::ranges::contains(study.groups, experiment_name)) {
+      if (base::Contains(study.groups, experiment_name)) {
         return true;
       }
     }
diff --git a/components/variations/processed_study.cc b/components/variations/processed_study.cc
index aa66fdea53..37350a0ea0 100644
--- a/components/variations/processed_study.cc
+++ b/components/variations/processed_study.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
@@ -94,7 +95,7 @@ bool ValidateExperimentNames(const Study& study) {
   // Specifying a default experiment is optional, so finding it in the
   // experiment list is only required when it is specified.
   if (!study.default_experiment_name().empty() &&
-      !experiment_names.contains(study.default_experiment_name())) {
+      !base::Contains(experiment_names, study.default_experiment_name())) {
     LogInvalidReason(InvalidStudyReason::kMissingDefaultExperimentInList);
     DVLOG(1) << study.name() << " is missing default experiment ("
              << study.default_experiment_name() << ") in its experiment list";
diff --git a/components/variations/service/limited_entropy_randomization.cc b/components/variations/service/limited_entropy_randomization.cc
index ca0c2e1e44..6e73b3c93e 100644
--- a/components/variations/service/limited_entropy_randomization.cc
+++ b/components/variations/service/limited_entropy_randomization.cc
@@ -6,11 +6,11 @@
 
 #include <math.h>
 
-#include <algorithm>
 #include <cstdint>
 #include <limits>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/logging.h"
@@ -111,8 +111,7 @@ const Layer* FindLayerForStudy(const LayerByIdMap& layer_by_id_map,
   }
   const Layer* layer = iter->second;
   for (const uint32_t member_id : layer_member_ids) {
-    if (!std::ranges::contains(layer->members(), member_id,
-                               &Layer::LayerMember::id)) {
+    if (!base::Contains(layer->members(), member_id, &Layer::LayerMember::id)) {
       SCOPED_CRASH_KEY_NUMBER(SR_CRASH_KEY, "ref_member_id", member_id);
       LogSeedRejectionReason(
           SeedRejectionReason::kDanglingLayerMemberReference);
diff --git a/components/variations/study_filtering.cc b/components/variations/study_filtering.cc
index 645c948001..fc721bf70f 100644
--- a/components/variations/study_filtering.cc
+++ b/components/variations/study_filtering.cc
@@ -7,12 +7,12 @@
 #include <stddef.h>
 #include <stdint.h>
 
-#include <algorithm>
 #include <cstdint>
 #include <functional>
 #include <set>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/strings/string_util.h"
 #include "components/variations/variations_layers.h"
@@ -26,7 +26,7 @@ base::Time ConvertStudyDateToBaseTime(int64_t date_time) {
   return base::Time::UnixEpoch() + base::Seconds(date_time);
 }
 
-// Similar to std::ranges::contains(), but specifically for ASCII strings and
+// Similar to base::Contains(), but specifically for ASCII strings and
 // case-insensitive comparison.
 template <typename Collection>
 bool ContainsStringIgnoreCaseASCII(const Collection& collection,
@@ -46,7 +46,7 @@ bool CheckStudyChannel(const Study::Filter& filter, Study::Channel channel) {
     return true;
   }
 
-  return std::ranges::contains(filter.channel(), channel);
+  return base::Contains(filter.channel(), channel);
 }
 
 bool CheckStudyFormFactor(const Study::Filter& filter,
@@ -61,11 +61,11 @@ bool CheckStudyFormFactor(const Study::Filter& filter,
   // Note if both are specified, the excludelist is ignored. We do not expect
   // both to be present for Chrome due to server-side checks.
   if (filter.form_factor_size() > 0) {
-    return std::ranges::contains(filter.form_factor(), form_factor);
+    return base::Contains(filter.form_factor(), form_factor);
   }
 
   // Omit if there is a matching excludelist entry.
-  return !std::ranges::contains(filter.exclude_form_factor(), form_factor);
+  return !base::Contains(filter.exclude_form_factor(), form_factor);
 }
 
 bool CheckStudyCpuArchitecture(const Study::Filter& filter,
@@ -80,12 +80,11 @@ bool CheckStudyCpuArchitecture(const Study::Filter& filter,
   // Note if both are specified, the excludelist is ignored. We do not expect
   // both to be present for Chrome due to server-side checks.
   if (filter.cpu_architecture_size() > 0) {
-    return std::ranges::contains(filter.cpu_architecture(), cpu_architecture);
+    return base::Contains(filter.cpu_architecture(), cpu_architecture);
   }
 
   // Omit if there is a matching excludelist entry.
-  return !std::ranges::contains(filter.exclude_cpu_architecture(),
-                                cpu_architecture);
+  return !base::Contains(filter.exclude_cpu_architecture(), cpu_architecture);
 }
 
 bool CheckStudyHardwareClass(const Study::Filter& filter,
@@ -123,11 +122,11 @@ bool CheckStudyLocale(const Study::Filter& filter, const std::string& locale) {
   // Note if both are specified, the excludelist is ignored. We do not expect
   // both to be present for Chrome due to server-side checks.
   if (filter.locale_size() > 0) {
-    return std::ranges::contains(filter.locale(), locale);
+    return base::Contains(filter.locale(), locale);
   }
 
   // Omit if there is a matching excludelist entry.
-  return !std::ranges::contains(filter.exclude_locale(), locale);
+  return !base::Contains(filter.exclude_locale(), locale);
 }
 
 bool CheckStudyCountry(const Study::Filter& filter,
@@ -141,15 +140,15 @@ bool CheckStudyCountry(const Study::Filter& filter,
   // Note if both are specified, the excludelist is ignored. We do not expect
   // both to be present for Chrome due to server-side checks.
   if (filter.country_size() > 0) {
-    return std::ranges::contains(filter.country(), country);
+    return base::Contains(filter.country(), country);
   }
 
   // Omit if there is a matching excludelist entry.
-  return !std::ranges::contains(filter.exclude_country(), country);
+  return !base::Contains(filter.exclude_country(), country);
 }
 
 bool CheckStudyPlatform(const Study::Filter& filter, Study::Platform platform) {
-  return std::ranges::contains(filter.platform(), platform);
+  return base::Contains(filter.platform(), platform);
 }
 
 bool CheckStudyLowEndDevice(const Study::Filter& filter,
@@ -253,7 +252,7 @@ bool CheckStudyGoogleGroup(const Study::Filter& filter,
   if (filter.google_group_size() > 0) {
     if (std::ranges::none_of(filter.google_group(),
                              [&client_groups](int64_t group) {
-                               return client_groups.contains(group);
+                               return base::Contains(client_groups, group);
                              })) {
       // A google_group filter was specified, and the client is not a member of
       // any of the groups.
@@ -264,7 +263,7 @@ bool CheckStudyGoogleGroup(const Study::Filter& filter,
   if (filter.exclude_google_group_size() > 0) {
     if (std::ranges::any_of(filter.exclude_google_group(),
                             [&client_groups](int64_t group) {
-                              return client_groups.contains(group);
+                              return base::Contains(client_groups, group);
                             })) {
       // An exclude_google_group filter was specified, and the client is a
       // member of at least one of the groups.
diff --git a/components/variations/synthetic_trial_registry.cc b/components/variations/synthetic_trial_registry.cc
index 70263fba63..dc265ef651 100644
--- a/components/variations/synthetic_trial_registry.cc
+++ b/components/variations/synthetic_trial_registry.cc
@@ -4,9 +4,8 @@
 
 #include "components/variations/synthetic_trial_registry.h"
 
-#include <algorithm>
-
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/observer_list.h"
 #include "base/strings/string_number_conversions.h"
@@ -107,10 +106,10 @@ void SyntheticTrialRegistry::RegisterExternalExperimentsInternal(
     // If existing ids shouldn't be overridden, skip entries whose study names
     // are already registered.
     if (mode == kDoNotOverrideExistingIds) {
-      if (std::ranges::contains(synthetic_trial_groups_, trial_hash,
-                                [](const SyntheticTrialGroup& group) {
-                                  return group.id().name;
-                                })) {
+      if (base::Contains(synthetic_trial_groups_, trial_hash,
+                         [](const SyntheticTrialGroup& group) {
+                           return group.id().name;
+                         })) {
         continue;
       }
     }
diff --git a/components/variations/variations_ids_provider.cc b/components/variations/variations_ids_provider.cc
index 8a320dad14..4f5f6a81ba 100644
--- a/components/variations/variations_ids_provider.cc
+++ b/components/variations/variations_ids_provider.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/no_destructor.h"
@@ -218,7 +219,7 @@ bool VariationsIdsProvider::ForceDisableVariationIds(
 
 void VariationsIdsProvider::AddObserver(Observer* observer) {
   base::AutoLock scoped_lock(lock_);
-  CHECK(!std::ranges::contains(observer_list_, observer));
+  CHECK(!base::Contains(observer_list_, observer));
   observer_list_.push_back(observer);
 }
 
@@ -624,8 +625,8 @@ bool VariationsIdsProvider::IsDuplicateId(VariationID id) {
     }
 
     const VariationIDEntry entry(id, key);
-    if (force_enabled_ids_set_.contains(entry) ||
-        synthetic_variation_ids_set_.contains(entry)) {
+    if (base::Contains(force_enabled_ids_set_, entry) ||
+        base::Contains(synthetic_variation_ids_set_, entry)) {
       return true;
     }
   }
diff --git a/components/visited_url_ranking/internal/history_url_visit_data_fetcher.cc b/components/visited_url_ranking/internal/history_url_visit_data_fetcher.cc
index ae042ca8c5..e67d913217 100644
--- a/components/visited_url_ranking/internal/history_url_visit_data_fetcher.cc
+++ b/components/visited_url_ranking/internal/history_url_visit_data_fetcher.cc
@@ -12,6 +12,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/time/time.h"
@@ -206,7 +207,7 @@ void HistoryURLVisitDataFetcher::OnGotAnnotatedVisits(
   for (auto& annotated_visit : annotated_visits) {
     Source current_visit_source = GetVisitSource(
         sync_device_info, annotated_visit, device_info_sync_service_);
-    if (!std::ranges::contains(requested_fetch_sources, current_visit_source)) {
+    if (!base::Contains(requested_fetch_sources, current_visit_source)) {
       continue;
     }
 
diff --git a/components/visited_url_ranking/internal/session_url_visit_data_fetcher.cc b/components/visited_url_ranking/internal/session_url_visit_data_fetcher.cc
index 411a2e4494..dc91ef61a7 100644
--- a/components/visited_url_ranking/internal/session_url_visit_data_fetcher.cc
+++ b/components/visited_url_ranking/internal/session_url_visit_data_fetcher.cc
@@ -4,13 +4,13 @@
 
 #include "components/visited_url_ranking/internal/session_url_visit_data_fetcher.h"
 
-#include <algorithm>
 #include <map>
 #include <optional>
 #include <utility>
 #include <variant>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/time/time.h"
 #include "components/sessions/core/session_types.h"
 #include "components/sync_sessions/open_tabs_ui_delegate.h"
@@ -125,13 +125,13 @@ void SessionURLVisitDataFetcher::FetchURLVisitData(
   for (const sync_sessions::SyncedSession* session : sessions) {
     if (local_session &&
         local_session->GetSessionName() == session->GetSessionName()) {
-      if (!std::ranges::contains(fetcher_sources, Source::kLocal)) {
+      if (!base::Contains(fetcher_sources, Source::kLocal)) {
         continue;
       }
       AddAggregateVisitDataFromSession(
           local_session, Source::kLocal, options.begin_time,
           url_visit_tab_data_map, config.deduplication_helper);
-    } else if (std::ranges::contains(fetcher_sources, Source::kForeign)) {
+    } else if (base::Contains(fetcher_sources, Source::kForeign)) {
       AddAggregateVisitDataFromSession(
           session, Source::kForeign, options.begin_time, url_visit_tab_data_map,
           config.deduplication_helper);
diff --git a/components/visited_url_ranking/internal/url_grouping/group_suggestions_manager.cc b/components/visited_url_ranking/internal/url_grouping/group_suggestions_manager.cc
index 789ead3738..790a5268e7 100644
--- a/components/visited_url_ranking/internal/url_grouping/group_suggestions_manager.cc
+++ b/components/visited_url_ranking/internal/url_grouping/group_suggestions_manager.cc
@@ -4,8 +4,7 @@
 
 #include "components/visited_url_ranking/internal/url_grouping/group_suggestions_manager.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
 #include "base/memory/weak_ptr.h"
@@ -93,7 +92,7 @@ void RecordSuggestionUKM(
         input->GetMetadataArgument(tab_recent_foreground_count_input);
     std::optional<ProcessedValue> ukm_source_id =
         input->GetMetadataArgument(ukm_source_id_input);
-    if (!std::ranges::contains(shown_suggestion.tab_ids, tab_id->float_val) ||
+    if (!base::Contains(shown_suggestion.tab_ids, tab_id->float_val) ||
         ukm_source_id->int64_val == ukm::kInvalidSourceId) {
       continue;
     }
@@ -131,7 +130,7 @@ void RecordTabIndexMetrics(
         input->GetMetadataArgument(tab_index_input);
     std::optional<ProcessedValue> is_last_tab =
         input->GetMetadataArgument(is_last_tab_input);
-    if (!std::ranges::contains(shown_suggestion.tab_ids, tab_id->float_val)) {
+    if (!base::Contains(shown_suggestion.tab_ids, tab_id->float_val)) {
       continue;
     }
     if (is_last_tab->float_val) {
diff --git a/components/visited_url_ranking/internal/url_grouping/grouping_heuristics.cc b/components/visited_url_ranking/internal/url_grouping/grouping_heuristics.cc
index faa88b7e84..113c39d593 100644
--- a/components/visited_url_ranking/internal/url_grouping/grouping_heuristics.cc
+++ b/components/visited_url_ranking/internal/url_grouping/grouping_heuristics.cc
@@ -4,10 +4,10 @@
 
 #include "components/visited_url_ranking/internal/url_grouping/grouping_heuristics.h"
 
-#include <algorithm>
 #include <unordered_map>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/flat_map.h"
 #include "base/json/json_writer.h"
@@ -183,7 +183,7 @@ class SimilarSourceHeuristic : public GroupingHeuristics::Heuristic {
       for (const auto& pair : tab_id_to_parent_id_map) {
         float tab_id = pair.first;
         float parent_id = pair.second;
-        if (tab_id_to_parent_id_map.contains(parent_id) &&
+        if (base::Contains(tab_id_to_parent_id_map, parent_id) &&
             tab_id_to_parent_id_map[parent_id] != parent_id) {
           new_tab_id_to_parent_id_map[tab_id] =
               tab_id_to_parent_id_map[parent_id];
@@ -288,7 +288,7 @@ bool IsGroupVisible(const GroupSuggestion& suggestion,
     }
 
     int tab_id = tab_data->last_active_tab.id;
-    if (!std::ranges::contains(suggestion.tab_ids, tab_id)) {
+    if (!base::Contains(suggestion.tab_ids, tab_id)) {
       continue;
     }
 
@@ -472,7 +472,7 @@ void GroupingHeuristics::GetSuggestions(
     signals.push_back(AsInputContext(kSuggestionsPredictionSchema, candidate));
   }
   for (const auto type : heuristics_priority) {
-    if (!heuristics_.contains(type)) {
+    if (!base::Contains(heuristics_, type)) {
       continue;
     }
     auto& heuristic = heuristics_[type];
diff --git a/components/viz/client/frame_eviction_manager.cc b/components/viz/client/frame_eviction_manager.cc
index 230155797a..e25065f09d 100644
--- a/components/viz/client/frame_eviction_manager.cc
+++ b/components/viz/client/frame_eviction_manager.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/system/sys_info.h"
@@ -49,8 +50,8 @@ void FrameEvictionManager::RemoveFrame(FrameEvictionManagerClient* frame) {
 }
 
 void FrameEvictionManager::LockFrame(FrameEvictionManagerClient* frame) {
-  if (std::ranges::contains(unlocked_frames_, frame,
-                            [](const auto& p) { return p.first; })) {
+  if (base::Contains(unlocked_frames_, frame,
+                     [](const auto& p) { return p.first; })) {
     DCHECK(locked_frames_.find(frame) == locked_frames_.end());
     unlocked_frames_.remove_if([&](const auto& p) { return p.first == frame; });
     locked_frames_[frame] = 1;
diff --git a/components/viz/client/frame_evictor.cc b/components/viz/client/frame_evictor.cc
index 3edc6435bc..99e6c2253d 100644
--- a/components/viz/client/frame_evictor.cc
+++ b/components/viz/client/frame_evictor.cc
@@ -4,9 +4,9 @@
 
 #include "components/viz/client/frame_evictor.h"
 
-#include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "build/buildflag.h"
 #include "components/viz/common/features.h"
@@ -55,7 +55,7 @@ std::vector<SurfaceId> FrameEvictor::CollectSurfaceIdsForEviction() const {
   std::vector<SurfaceId> output_ids = std::move(ids.embedded_ids);
   auto current = client_->GetCurrentSurfaceId();
   DCHECK(output_ids.empty() || !current.is_valid() ||
-         std::ranges::contains(output_ids, current));
+         base::Contains(output_ids, current));
 
   if (output_ids.empty() && current.is_valid()) {
     output_ids.push_back(current);
diff --git a/components/viz/common/features.cc b/components/viz/common/features.cc
index 0cb7ed2521..a770afaf50 100644
--- a/components/viz/common/features.cc
+++ b/components/viz/common/features.cc
@@ -539,7 +539,7 @@ bool ShouldUseAdpfForSoc(std::string_view soc_allowlist,
                          std::string_view soc) {
   std::vector<std::string_view> allowlist = base::SplitStringPiece(
       soc_allowlist, "|", base::TRIM_WHITESPACE, base::SPLIT_WANT_NONEMPTY);
-  return std::ranges::contains(allowlist, soc);
+  return base::Contains(allowlist, soc);
 }
 #endif  // BUILDFLAG(IS_ANDROID)
 
diff --git a/components/viz/common/frame_sinks/begin_frame_source.cc b/components/viz/common/frame_sinks/begin_frame_source.cc
index b33a3cfe52..a8ef04a969 100644
--- a/components/viz/common/frame_sinks/begin_frame_source.cc
+++ b/components/viz/common/frame_sinks/begin_frame_source.cc
@@ -12,6 +12,7 @@
 
 #include "base/atomic_sequence_num.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/location.h"
 #include "base/memory/raw_ptr.h"
@@ -268,7 +269,7 @@ BackToBackBeginFrameSource::~BackToBackBeginFrameSource() = default;
 
 void BackToBackBeginFrameSource::AddObserver(BeginFrameObserver* obs) {
   DCHECK(obs);
-  DCHECK(!observers_.contains(obs));
+  DCHECK(!base::Contains(observers_, obs));
   observers_.insert(obs);
   pending_begin_frame_observers_.insert(obs);
   obs->OnBeginFrameSourcePausedChanged(false);
@@ -277,7 +278,7 @@ void BackToBackBeginFrameSource::AddObserver(BeginFrameObserver* obs) {
 
 void BackToBackBeginFrameSource::RemoveObserver(BeginFrameObserver* obs) {
   DCHECK(obs);
-  DCHECK(observers_.contains(obs));
+  DCHECK(base::Contains(observers_, obs));
   observers_.erase(obs);
   pending_begin_frame_observers_.erase(obs);
   if (pending_begin_frame_observers_.empty()) {
@@ -286,7 +287,7 @@ void BackToBackBeginFrameSource::RemoveObserver(BeginFrameObserver* obs) {
 }
 
 void BackToBackBeginFrameSource::DidFinishFrame(BeginFrameObserver* obs) {
-  if (observers_.contains(obs)) {
+  if (base::Contains(observers_, obs)) {
     pending_begin_frame_observers_.insert(obs);
     time_source_->SetActive(true);
   }
@@ -381,7 +382,7 @@ BeginFrameArgs DelayBasedBeginFrameSource::CreateBeginFrameArgs(
 
 void DelayBasedBeginFrameSource::AddObserver(BeginFrameObserver* obs) {
   DCHECK(obs);
-  DCHECK(!observers_.contains(obs));
+  DCHECK(!base::Contains(observers_, obs));
 
   observers_.insert(obs);
   obs->OnBeginFrameSourcePausedChanged(false);
@@ -414,7 +415,7 @@ void DelayBasedBeginFrameSource::AddObserver(BeginFrameObserver* obs) {
 
 void DelayBasedBeginFrameSource::RemoveObserver(BeginFrameObserver* obs) {
   DCHECK(obs);
-  DCHECK(observers_.contains(obs));
+  DCHECK(base::Contains(observers_, obs));
 
   observers_.erase(obs);
   if (observers_.empty())
@@ -524,7 +525,7 @@ void ExternalBeginFrameSource::AsProtozeroInto(
 
 void ExternalBeginFrameSource::AddObserver(BeginFrameObserver* obs) {
   DCHECK(obs);
-  DCHECK(!observers_.contains(obs));
+  DCHECK(!base::Contains(observers_, obs));
 
   if (observers_.empty()) {
     client_->OnNeedsBeginFrames(true);
@@ -546,7 +547,7 @@ void ExternalBeginFrameSource::AddObserver(BeginFrameObserver* obs) {
 
 void ExternalBeginFrameSource::RemoveObserver(BeginFrameObserver* obs) {
   DCHECK(obs);
-  DCHECK(observers_.contains(obs));
+  DCHECK(base::Contains(observers_, obs));
 
   observers_.erase(obs);
   if (observers_.empty()) {
diff --git a/components/viz/common/performance_hint_utils.cc b/components/viz/common/performance_hint_utils.cc
index 79024b015c..b76d8ea564 100644
--- a/components/viz/common/performance_hint_utils.cc
+++ b/components/viz/common/performance_hint_utils.cc
@@ -4,9 +4,9 @@
 
 #include "components/viz/common/performance_hint_utils.h"
 
-#include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "build/build_config.h"
 
@@ -30,7 +30,7 @@ bool CheckThreadIdsDoNotBelongToCurrentProcess(
   }
   static_assert(std::is_same_v<pid_t, base::PlatformThreadId::UnderlyingType>);
   for (const auto& tid : thread_ids_from_sandboxed_process) {
-    if (std::ranges::contains(privileged_thread_ids, tid.raw())) {
+    if (base::Contains(privileged_thread_ids, tid.raw())) {
       return false;
     }
   }
diff --git a/components/viz/common/quads/aggregated_render_pass.cc b/components/viz/common/quads/aggregated_render_pass.cc
index f2fe52c4f0..6813966fb1 100644
--- a/components/viz/common/quads/aggregated_render_pass.cc
+++ b/components/viz/common/quads/aggregated_render_pass.cc
@@ -5,12 +5,12 @@
 #include "components/viz/common/quads/aggregated_render_pass.h"
 
 #include <unordered_map>
-#include <utility>
 
 #include "base/memory/ptr_util.h"
 #include "base/notreached.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/trace_event/traced_value.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "cc/base/math_util.h"
 #include "components/viz/common/frame_sinks/copy_output_request.h"
@@ -225,7 +225,7 @@ void AggregatedRenderPass::AsValueInto(
   RenderPassInternal::AsValueInto(value, resource_id_to_index_map);
 
   value->SetInteger("content_color_usage",
-                    std::to_underlying(content_color_usage));
+                    base::to_underlying(content_color_usage));
   value->SetBoolean("is_from_surface_root_pass", is_from_surface_root_pass);
 #if BUILDFLAG(IS_WIN)
   value->SetBoolean("will_backing_be_read_by_viz", will_backing_be_read_by_viz);
diff --git a/components/viz/common/resources/resource_id.h b/components/viz/common/resources/resource_id.h
index 4a2b8a53d1..6ded665b19 100644
--- a/components/viz/common/resources/resource_id.h
+++ b/components/viz/common/resources/resource_id.h
@@ -12,6 +12,7 @@
 #include <map>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/types/id_type.h"
 #include "components/viz/common/viz_common_export.h"
diff --git a/components/viz/host/host_frame_sink_manager.cc b/components/viz/host/host_frame_sink_manager.cc
index e575f2b937..ec9c82731e 100644
--- a/components/viz/host/host_frame_sink_manager.cc
+++ b/components/viz/host/host_frame_sink_manager.cc
@@ -4,12 +4,12 @@
 
 #include "components/viz/host/host_frame_sink_manager.h"
 
-#include <algorithm>
 #include <cstddef>
 #include <optional>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -297,7 +297,7 @@ bool HostFrameSinkManager::RegisterFrameSinkHierarchy(
   }
 
   FrameSinkData& parent_data = iter->second;
-  CHECK(!std::ranges::contains(parent_data.children, child_frame_sink_id));
+  CHECK(!base::Contains(parent_data.children, child_frame_sink_id));
   parent_data.children.push_back(child_frame_sink_id);
 
   // Register and store the parent.
diff --git a/components/viz/service/display/display_resource_provider_skia.cc b/components/viz/service/display/display_resource_provider_skia.cc
index 6c317ccf9e..aedcaa37b0 100644
--- a/components/viz/service/display/display_resource_provider_skia.cc
+++ b/components/viz/service/display/display_resource_provider_skia.cc
@@ -4,11 +4,11 @@
 
 #include "components/viz/service/display/display_resource_provider_skia.h"
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "build/build_config.h"
 #include "components/viz/service/display/resource_fence.h"
@@ -147,7 +147,7 @@ DisplayResourceProviderSkia::LockSetForExternalUse::LockResource(
   DCHECK(resource.is_gpu_resource_type());
 
   if (!resource.locked_for_external_use) {
-    DCHECK(!std::ranges::contains(resources_, std::make_pair(id, &resource)));
+    DCHECK(!base::Contains(resources_, std::make_pair(id, &resource)));
     resources_.emplace_back(id, &resource);
 
     if (!resource.image_context) {
@@ -178,7 +178,7 @@ DisplayResourceProviderSkia::LockSetForExternalUse::LockResource(
     }
   }
 
-  DCHECK(std::ranges::contains(resources_, std::make_pair(id, &resource)));
+  DCHECK(base::Contains(resources_, std::make_pair(id, &resource)));
   return resource.image_context.get();
 }
 
diff --git a/components/viz/service/display/overlay_candidate_factory.cc b/components/viz/service/display/overlay_candidate_factory.cc
index 87d76ff3c0..9b45da07e3 100644
--- a/components/viz/service/display/overlay_candidate_factory.cc
+++ b/components/viz/service/display/overlay_candidate_factory.cc
@@ -4,9 +4,9 @@
 
 #include "components/viz/service/display/overlay_candidate_factory.h"
 
-#include <algorithm>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "build/build_config.h"
 #include "cc/base/math_util.h"
 #include "components/viz/common/quads/aggregated_render_pass_draw_quad.h"
@@ -356,7 +356,7 @@ OverlayCandidate::CandidateStatus OverlayCandidateFactory::FromDrawQuadResource(
         resource_provider_->IsLowLatencyRendering(resource_id);
 
     if (!context_.is_delegated_context &&
-        !std::ranges::contains(kOverlayFormats, candidate.format)) {
+        !base::Contains(kOverlayFormats, candidate.format)) {
       return CandidateStatus::kFailFormat;
     }
   }
diff --git a/components/viz/service/display_embedder/output_device_backing.cc b/components/viz/service/display_embedder/output_device_backing.cc
index a15e4669ba..9ca1aa110a 100644
--- a/components/viz/service/display_embedder/output_device_backing.cc
+++ b/components/viz/service/display_embedder/output_device_backing.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/logging.h"
 #include "base/memory/unsafe_shared_memory_region.h"
@@ -74,7 +75,7 @@ void OutputDeviceBacking::RegisterClient(Client* client) {
 }
 
 void OutputDeviceBacking::UnregisterClient(Client* client) {
-  DCHECK(std::ranges::contains(clients_, client));
+  DCHECK(base::Contains(clients_, client));
   std::erase(clients_, client);
   ClientResized();
 }
diff --git a/components/viz/service/frame_sinks/compositor_frame_sink_support.cc b/components/viz/service/frame_sinks/compositor_frame_sink_support.cc
index 69ea4c7d78..ce60fd08ae 100644
--- a/components/viz/service/frame_sinks/compositor_frame_sink_support.cc
+++ b/components/viz/service/frame_sinks/compositor_frame_sink_support.cc
@@ -11,6 +11,7 @@
 #include <variant>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/stack_trace.h"
@@ -1314,7 +1315,7 @@ const FrameSinkId& CompositorFrameSinkSupport::GetFrameSinkId() const {
 
 void CompositorFrameSinkSupport::AttachCaptureClient(
     CapturableFrameSink::Client* client) {
-  DCHECK(!std::ranges::contains(capture_clients_, client));
+  DCHECK(!base::Contains(capture_clients_, client));
   capture_clients_.push_back(client);
   if (client->IsVideoCaptureStarted()) {
     OnClientCaptureStarted();
diff --git a/components/viz/service/frame_sinks/frame_counter.cc b/components/viz/service/frame_sinks/frame_counter.cc
index 6c71898fc4..18f5ecbb21 100644
--- a/components/viz/service/frame_sinks/frame_counter.cc
+++ b/components/viz/service/frame_sinks/frame_counter.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/time/time.h"
 #include "services/viz/privileged/mojom/compositing/frame_sink_manager.mojom.h"
 #include "services/viz/privileged/mojom/compositing/frame_sinks_metrics_recorder.mojom.h"
@@ -33,7 +34,7 @@ FrameCounter::~FrameCounter() = default;
 void FrameCounter::AddFrameSink(const FrameSinkId& frame_sink_id,
                                 bool is_root,
                                 std::string_view debug_label) {
-  DCHECK(!frame_sink_data_.contains(frame_sink_id));
+  DCHECK(!base::Contains(frame_sink_data_, frame_sink_id));
 
   auto per_sink_data = mojom::FrameCountingPerSinkData::New(
       is_root, static_cast<std::string>(debug_label), 0,
diff --git a/components/viz/service/frame_sinks/frame_sink_manager_impl.cc b/components/viz/service/frame_sinks/frame_sink_manager_impl.cc
index 08fcdcfa47..431ab5536c 100644
--- a/components/viz/service/frame_sinks/frame_sink_manager_impl.cc
+++ b/components/viz/service/frame_sinks/frame_sink_manager_impl.cc
@@ -203,7 +203,7 @@ void FrameSinkManagerImpl::SetInputManagerForTesting(
 void FrameSinkManagerImpl::RegisterFrameSinkId(const FrameSinkId& frame_sink_id,
                                                bool report_activation) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  DCHECK(!frame_sink_data_.contains(frame_sink_id));
+  DCHECK(!base::Contains(frame_sink_data_, frame_sink_id));
 
   frame_sink_data_.emplace(std::make_pair(frame_sink_id, report_activation));
 
@@ -249,7 +249,7 @@ void FrameSinkManagerImpl::SetFrameSinkDebugLabel(
 void FrameSinkManagerImpl::CreateRootCompositorFrameSink(
     mojom::RootCompositorFrameSinkParamsPtr params) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  DCHECK(!root_sink_map_.contains(params->frame_sink_id));
+  DCHECK(!base::Contains(root_sink_map_, params->frame_sink_id));
   DCHECK(output_surface_provider_);
 
   // We are transferring ownership of |params| so remember FrameSinkId here.
@@ -314,7 +314,7 @@ void FrameSinkManagerImpl::CreateFrameSinkBundle(
     mojo::PendingReceiver<mojom::FrameSinkBundle> receiver,
     mojo::PendingRemote<mojom::FrameSinkBundleClient> client) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  if (bundle_map_.contains(bundle_id)) {
+  if (base::Contains(bundle_map_, bundle_id)) {
     uint32_t client_id = bundle_id.client_id();
     uint32_t bundle_id_value = bundle_id.bundle_id();
     std::visit(
@@ -340,7 +340,7 @@ void FrameSinkManagerImpl::CreateCompositorFrameSink(
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   TRACE_EVENT("viz", "FrameSinkManagerImpl::CreateCompositorFrameSink",
               "frame_sink_id", frame_sink_id);
-  if (sink_map_.contains(frame_sink_id)) {
+  if (base::Contains(sink_map_, frame_sink_id)) {
     std::visit(
         [](auto& receiver) {
           receiver.ReportBadMessage("Duplicate FrameSinkId");
@@ -387,7 +387,7 @@ void FrameSinkManagerImpl::RegisterFrameSinkHierarchy(
   CHECK(!ChildContains(child_frame_sink_id, parent_frame_sink_id));
 
   auto& children = frame_sink_source_map_[parent_frame_sink_id].children;
-  DCHECK(!children.contains(child_frame_sink_id));
+  DCHECK(!base::Contains(children, child_frame_sink_id));
   children.insert(child_frame_sink_id);
 
   // Add `parent_frame_sink_id` as parent to the list tracking parents of
@@ -430,7 +430,7 @@ void FrameSinkManagerImpl::UnregisterFrameSinkHierarchy(
   CHECK(iter_child != frame_sink_source_map_.end());
 
   auto& child_mapping = iter_child->second;
-  DCHECK(std::ranges::contains(child_mapping.parent, parent_frame_sink_id));
+  DCHECK(base::Contains(child_mapping.parent, parent_frame_sink_id));
 
   // Delete `parent_frame_sink_id` from parent list of `child_frame_sink_id` in
   // `frame_sink_source_map_`.
@@ -450,7 +450,7 @@ void FrameSinkManagerImpl::UnregisterFrameSinkHierarchy(
 
   // Remove |child_frame_sink_id| from parents list of children.
   auto& mapping = iter_parent->second;
-  DCHECK(mapping.children.contains(child_frame_sink_id));
+  DCHECK(base::Contains(mapping.children, child_frame_sink_id));
   mapping.children.erase(child_frame_sink_id);
 
   for (auto& observer : observer_list_) {
@@ -598,7 +598,7 @@ void FrameSinkManagerImpl::OnFirstSurfaceActivation(
 void FrameSinkManagerImpl::UpdateHitTestRegionData(
     const FrameSinkId& frame_sink_id,
     const std::vector<AggregatedHitTestRegion>& hit_test_data) {
-  if (!display_hit_test_query_.contains(frame_sink_id)) {
+  if (!base::Contains(display_hit_test_query_, frame_sink_id)) {
     // The corresponding HitTestQuery has already been deleted, so drop the
     // in-flight hit-test data.
     return;
@@ -682,7 +682,7 @@ void FrameSinkManagerImpl::RegisterCompositorFrameSinkSupport(
     const FrameSinkId& frame_sink_id,
     CompositorFrameSinkSupport* support) {
   DCHECK(support);
-  DCHECK(!support_map_.contains(frame_sink_id));
+  DCHECK(!base::Contains(support_map_, frame_sink_id));
 
   support_map_[frame_sink_id] = support;
 
@@ -711,7 +711,7 @@ void FrameSinkManagerImpl::UnregisterCompositorFrameSinkSupport(
   TRACE_EVENT("viz",
               "FrameSinkManagerImpl::UnregisterCompositorFrameSinkSupport",
               "frame_sink_id", frame_sink_id);
-  DCHECK(support_map_.contains(frame_sink_id));
+  DCHECK(base::Contains(support_map_, frame_sink_id));
 
   for (auto& observer : observer_list_)
     observer.OnDestroyedCompositorFrameSink(frame_sink_id);
@@ -840,7 +840,7 @@ CapturableFrameSink* FrameSinkManagerImpl::FindCapturableFrameSink(
     for (const auto& id_and_sink : support_map_) {
       const RegionCaptureBounds& bounds =
           id_and_sink.second->current_capture_bounds();
-      if (bounds.bounds().contains(crop_id)) {
+      if (base::Contains(bounds.bounds(), crop_id)) {
         return id_and_sink.second;
       }
     }
@@ -971,7 +971,7 @@ std::vector<FrameSinkId> FrameSinkManagerImpl::GetRegisteredFrameSinkIds()
 
 FrameSinkId FrameSinkManagerImpl::GetOldestParentByChildFrameId(
     const FrameSinkId& child_frame_sink_id) const {
-  CHECK(!root_sink_map_.contains(child_frame_sink_id));
+  CHECK(!base::Contains(root_sink_map_, child_frame_sink_id));
 
   const FrameSinkSourceMapping* mapping =
       base::FindOrNull(frame_sink_source_map_, child_frame_sink_id);
@@ -991,7 +991,7 @@ FrameSinkId FrameSinkManagerImpl::GetOldestRootCompositorFrameSinkId(
     const FrameSinkId& child_frame_sink_id) const {
   auto parent_id = GetOldestParentByChildFrameId(child_frame_sink_id);
 
-  while (parent_id.is_valid() && !root_sink_map_.contains(parent_id)) {
+  while (parent_id.is_valid() && !base::Contains(root_sink_map_, parent_id)) {
     parent_id = GetOldestParentByChildFrameId(parent_id);
   }
   return parent_id;
@@ -1318,7 +1318,7 @@ void FrameSinkManagerImpl::OnViewTransitionResourcesCaptured(
 
 bool FrameSinkManagerImpl::IsFrameSinkIdInRootSinkMap(
     const FrameSinkId& frame_sink_id) {
-  return root_sink_map_.contains(frame_sink_id);
+  return base::Contains(root_sink_map_, frame_sink_id);
 }
 
 gpu::SharedImageInterface* FrameSinkManagerImpl::GetSharedImageInterface() {
diff --git a/components/viz/service/input/mock_input_manager.cc b/components/viz/service/input/mock_input_manager.cc
index f16cd6bff4..201f89d861 100644
--- a/components/viz/service/input/mock_input_manager.cc
+++ b/components/viz/service/input/mock_input_manager.cc
@@ -13,7 +13,7 @@ MockInputManager::~MockInputManager() = default;
 
 bool MockInputManager::RIRExistsForFrameSinkId(
     const FrameSinkId& frame_sink_id) {
-  return rir_map_.contains(frame_sink_id);
+  return base::Contains(rir_map_, frame_sink_id);
 }
 
 RenderInputRouterSupportBase* MockInputManager::GetSupportForFrameSink(
diff --git a/components/viz/service/performance_hint/hint_session.cc b/components/viz/service/performance_hint/hint_session.cc
index f9182a0d13..2251079875 100644
--- a/components/viz/service/performance_hint/hint_session.cc
+++ b/components/viz/service/performance_hint/hint_session.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/system/sys_info.h"
 #include "base/time/time.h"
diff --git a/components/viz/service/surfaces/surface.cc b/components/viz/service/surfaces/surface.cc
index 38e837b104..544ffdd6cb 100644
--- a/components/viz/service/surfaces/surface.cc
+++ b/components/viz/service/surfaces/surface.cc
@@ -14,6 +14,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
@@ -800,7 +801,7 @@ void Surface::UpdateActivationDependencies(
        current_frame.metadata.activation_dependencies) {
     SurfaceAllocationGroup* group =
         surface_manager_->GetOrCreateAllocationGroupForSurfaceId(surface_id);
-    if (new_blocking_allocation_groups.contains(group))
+    if (base::Contains(new_blocking_allocation_groups, group))
       continue;
     if (group)
       group->UpdateLastPendingReferenceAndMaybeActivate(surface_id);
diff --git a/components/viz/service/transitions/transferable_resource_tracker.cc b/components/viz/service/transitions/transferable_resource_tracker.cc
index 858189a446..d4bee8dd5d 100644
--- a/components/viz/service/transitions/transferable_resource_tracker.cc
+++ b/components/viz/service/transitions/transferable_resource_tracker.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "components/viz/common/resources/release_callback.h"
 #include "components/viz/common/resources/resource_id.h"
 #include "components/viz/common/resources/transferable_resource.h"
@@ -74,7 +75,7 @@ TransferableResourceTracker::ImportResource(
   }
 
   resource.id = id_tracker_->AllocId(/*initial_ref_count=*/1);
-  DCHECK(!managed_resources_.contains(resource.id));
+  DCHECK(!base::Contains(managed_resources_, resource.id));
   managed_resources_.emplace(
       resource.id,
       TransferableResourceHolder(resource, std::move(release_callback)));
@@ -93,7 +94,7 @@ void TransferableResourceTracker::ReturnFrame(const ResourceFrame& frame) {
 }
 
 void TransferableResourceTracker::RefResource(ResourceId id) {
-  if (!managed_resources_.contains(id)) {
+  if (!base::Contains(managed_resources_, id)) {
     return;
   }
 
@@ -104,7 +105,7 @@ void TransferableResourceTracker::UnrefResource(
     ResourceId id,
     int count,
     const gpu::SyncToken& sync_token) {
-  if (!managed_resources_.contains(id)) {
+  if (!base::Contains(managed_resources_, id)) {
     return;
   }
 
diff --git a/components/viz/test/test_gles2_interface.cc b/components/viz/test/test_gles2_interface.cc
index c7c2d1b1b6..ea4e1e33c5 100644
--- a/components/viz/test/test_gles2_interface.cc
+++ b/components/viz/test/test_gles2_interface.cc
@@ -5,6 +5,7 @@
 #include "components/viz/test/test_gles2_interface.h"
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/heap_array.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
@@ -111,7 +112,7 @@ GLuint TestGLES2Interface::CreateProgram() {
 void TestGLES2Interface::BindTexture(GLenum target, GLuint texture) {
   if (!texture)
     return;
-  DCHECK(textures_.contains(texture));
+  DCHECK(base::Contains(textures_, texture));
   used_textures_.insert(texture);
 }
 
@@ -499,7 +500,7 @@ GLuint TestGLES2Interface::NextFramebufferId() {
 }
 
 void TestGLES2Interface::RetireFramebufferId(GLuint id) {
-  DCHECK(framebuffer_set_.contains(id));
+  DCHECK(base::Contains(framebuffer_set_, id));
   framebuffer_set_.erase(id);
 }
 
@@ -512,7 +513,7 @@ GLuint TestGLES2Interface::NextRenderbufferId() {
 }
 
 void TestGLES2Interface::RetireRenderbufferId(GLuint id) {
-  DCHECK(renderbuffer_set_.contains(id));
+  DCHECK(base::Contains(renderbuffer_set_, id));
   renderbuffer_set_.erase(id);
 }
 
diff --git a/components/viz/test/test_gles2_interface.h b/components/viz/test/test_gles2_interface.h
index fdfd3bb6aa..de1d74a073 100644
--- a/components/viz/test/test_gles2_interface.h
+++ b/components/viz/test/test_gles2_interface.h
@@ -15,6 +15,7 @@
 #include <vector>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/heap_array.h"
 #include "base/functional/callback.h"
diff --git a/components/wallet/core/browser/walletable_permission_utils.cc b/components/wallet/core/browser/walletable_permission_utils.cc
index f3290de249..6c7f53c870 100644
--- a/components/wallet/core/browser/walletable_permission_utils.cc
+++ b/components/wallet/core/browser/walletable_permission_utils.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_split.h"
@@ -73,7 +74,7 @@ bool IsWalletSupportedCountry(const GeoIpCountryCode& country_code) {
   }
 
   // Returns whether the allowlist contains `country_code` (case-insensitive).
-  return GetAllowedCountries().contains(country_code.value());
+  return base::Contains(GetAllowedCountries(), country_code.value());
 }
 
 }  // namespace
diff --git a/components/web_package/input_reader.cc b/components/web_package/input_reader.cc
index 43e8a83282..7543661000 100644
--- a/components/web_package/input_reader.cc
+++ b/components/web_package/input_reader.cc
@@ -4,12 +4,11 @@
 
 #include "components/web_package/input_reader.h"
 
-#include <algorithm>
-#include <utility>
-
+#include "base/containers/contains.h"
 #include "base/numerics/checked_math.h"
 #include "base/strings/string_util.h"
 #include "base/strings/string_view_util.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/cbor/constants.h"
 #include "components/cbor/values.h"
 
@@ -93,10 +92,10 @@ std::optional<CBORHeader> InputReader::ReadCBORHeader() {
   switch (type) {
     case CBORType::kSimpleValue: {
       using SimpleValue = cbor::Value::SimpleValue;
-      if (additional_info == std::to_underlying(SimpleValue::TRUE_VALUE)) {
+      if (additional_info == base::to_underlying(SimpleValue::TRUE_VALUE)) {
         return {{true}};
       } else if (additional_info ==
-                 std::to_underlying(SimpleValue::FALSE_VALUE)) {
+                 base::to_underlying(SimpleValue::FALSE_VALUE)) {
         return {{false}};
       } else {
         return std::nullopt;
@@ -148,8 +147,8 @@ InputReader::ReadTypeAndArgument() {
   // during `static_cast<CBORType>`, it's safer to validate the type beforehand.
   uint8_t type_byte = (*first_byte & cbor::constants::kMajorTypeMask) >>
                       cbor::constants::kMajorTypeBitShift;
-  if (!std::ranges::contains(kAcceptedCBORTypes, type_byte,
-                             &std::to_underlying<CBORType>)) {
+  if (!base::Contains(kAcceptedCBORTypes, type_byte,
+                             &base::to_underlying<CBORType>)) {
     return std::nullopt;
   }
 
diff --git a/components/web_package/signed_web_bundles/attribute_map_parser.cc b/components/web_package/signed_web_bundles/attribute_map_parser.cc
index 0e8b8235f7..978fb0ff7f 100644
--- a/components/web_package/signed_web_bundles/attribute_map_parser.cc
+++ b/components/web_package/signed_web_bundles/attribute_map_parser.cc
@@ -6,6 +6,7 @@
 
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/strings/stringprintf.h"
 #include "base/types/expected_macros.h"
@@ -106,7 +107,7 @@ void AttributeMapParser::ReadAttributeName(
     return;
   }
 
-  if (attributes_map_.contains(*attribute_name)) {
+  if (base::Contains(attributes_map_, *attribute_name)) {
     RunErrorCallback(base::StringPrintf(
         "Found duplicate attribute name <%s> in the attributes map.",
         std::string(*attribute_name).c_str()));
diff --git a/components/webapps/browser/banners/app_banner_manager.cc b/components/webapps/browser/banners/app_banner_manager.cc
index d3c0ef36f0..1b0faa85bb 100644
--- a/components/webapps/browser/banners/app_banner_manager.cc
+++ b/components/webapps/browser/banners/app_banner_manager.cc
@@ -12,6 +12,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
diff --git a/components/webapps/browser/installable/installable_evaluator.cc b/components/webapps/browser/installable/installable_evaluator.cc
index 305f08b63b..1e9581e907 100644
--- a/components/webapps/browser/installable/installable_evaluator.cc
+++ b/components/webapps/browser/installable/installable_evaluator.cc
@@ -4,8 +4,6 @@
 
 #include "components/webapps/browser/installable/installable_evaluator.h"
 
-#include <algorithm>
-
 #include "base/feature_list.h"
 #include "base/strings/string_util.h"
 #include "components/security_state/core/security_state.h"
diff --git a/components/webapps/browser/installable/installable_manager.cc b/components/webapps/browser/installable/installable_manager.cc
index bb4f9b7f08..f03fae01cf 100644
--- a/components/webapps/browser/installable/installable_manager.cc
+++ b/components/webapps/browser/installable/installable_manager.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
diff --git a/components/webapps/browser/installable/installable_task.cc b/components/webapps/browser/installable/installable_task.cc
index 9379206712..22c4536567 100644
--- a/components/webapps/browser/installable/installable_task.cc
+++ b/components/webapps/browser/installable/installable_task.cc
@@ -4,8 +4,7 @@
 
 #include "components/webapps/browser/installable/installable_task.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "components/webapps/browser/installable/installable_manager.h"
 #include "content/public/browser/web_contents.h"
 #include "third_party/blink/public/common/manifest/manifest_util.h"
@@ -143,7 +142,7 @@ void InstallableTask::CheckInstallability() {
   std::vector<InstallableStatusCode> installable_errors =
       evaluator_->CheckInstallability();
   for (auto new_error : installable_errors) {
-    if (std::ranges::contains(errors_, new_error)) {
+    if (base::Contains(errors_, new_error)) {
       // Don't add duplicated errors.
       continue;
     }
diff --git a/components/webapps/isolated_web_apps/reading/signed_web_bundle_reader.cc b/components/webapps/isolated_web_apps/reading/signed_web_bundle_reader.cc
index 3c989554fc..464452713e 100644
--- a/components/webapps/isolated_web_apps/reading/signed_web_bundle_reader.cc
+++ b/components/webapps/isolated_web_apps/reading/signed_web_bundle_reader.cc
@@ -33,6 +33,7 @@
 #include "base/task/task_traits.h"
 #include "base/task/thread_pool.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected.h"
 #include "base/types/expected_macros.h"
 #include "components/web_package/mojom/web_bundle_parser.mojom.h"
@@ -274,7 +275,7 @@ class SignedWebBundleReaderImpl : public SignedWebBundleReader {
   void OnFileClosed(base::OnceClosure callback) {
     DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
     CHECK(state_ == State::kClosed || state_ == State::kError)
-        << std::to_underlying(state_);
+        << base::to_underlying(state_);
 
     parser_->Close(base::BindOnce(&SignedWebBundleReaderImpl::OnParserClosed,
                                   weak_ptr_factory_.GetWeakPtr(),
@@ -284,7 +285,7 @@ class SignedWebBundleReaderImpl : public SignedWebBundleReader {
   void OnParserClosed(base::OnceClosure callback) {
     DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
     CHECK(state_ == State::kClosed || state_ == State::kError)
-        << std::to_underlying(state_);
+        << base::to_underlying(state_);
     close_callback_ = std::move(callback);
     ReplyClosedIfNecessary();
   }
diff --git a/components/webauthn/core/browser/passkey_sync_bridge.cc b/components/webauthn/core/browser/passkey_sync_bridge.cc
index e09de66f48..9308f3eefb 100644
--- a/components/webauthn/core/browser/passkey_sync_bridge.cc
+++ b/components/webauthn/core/browser/passkey_sync_bridge.cc
@@ -12,6 +12,7 @@
 #include <string>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/flat_tree.h"
 #include "base/containers/span.h"
@@ -488,7 +489,7 @@ void PasskeySyncBridge::CreatePasskey(
   CHECK(passkey_model_utils::IsPasskeyValid(passkey));
 
   std::string sync_id = passkey.sync_id();
-  CHECK(!data_.contains(sync_id));
+  CHECK(!base::Contains(data_, sync_id));
 
   AddShadowedCredentialIdsToNewPasskey(passkey);
   AddPasskeyInternal(passkey);
@@ -508,7 +509,7 @@ void PasskeySyncBridge::AddPasskeyInternal(
   CHECK(store_);
 
   std::string sync_id = specifics.sync_id();
-  CHECK(!data_.contains(sync_id));
+  CHECK(!base::Contains(data_, sync_id));
 
   std::unique_ptr<syncer::DataTypeStore::WriteBatch> write_batch =
       store_->CreateWriteBatch();
diff --git a/components/webui/flags/flags_state.cc b/components/webui/flags/flags_state.cc
index cc014a6584..1fd7e4eaf1 100644
--- a/components/webui/flags/flags_state.cc
+++ b/components/webui/flags/flags_state.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/feature_list_buildflags.h"
@@ -207,7 +208,7 @@ std::string CombineAndSanitizeOriginLists(const std::string& value1,
   std::vector<std::string> origin_vector;
   for (const std::string& list : {value1, value2}) {
     for (const std::string& origin : TokenizeOriginList(list)) {
-      if (!seen_origins.contains(origin)) {
+      if (!base::Contains(seen_origins, origin)) {
         origin_vector.push_back(origin);
         seen_origins.insert(origin);
       }
@@ -488,7 +489,7 @@ void FlagsState::SetOriginListFlag(const std::string& internal_name,
 
   std::set<std::string> enabled_entries;
   GetSanitizedEnabledFlags(flags_storage, &enabled_entries);
-  const bool enabled = enabled_entries.contains(entry->internal_name);
+  const bool enabled = base::Contains(enabled_entries, entry->internal_name);
   if (enabled) {
     DidModifyOriginListFlag(*flags_storage, *entry);
   }
@@ -506,7 +507,7 @@ void FlagsState::SetStringFlag(const std::string& internal_name,
 
   std::set<std::string> enabled_entries;
   GetSanitizedEnabledFlags(flags_storage, &enabled_entries);
-  const bool enabled = enabled_entries.contains(entry->internal_name);
+  const bool enabled = base::Contains(enabled_entries, entry->internal_name);
   if (enabled) {
     DidModifyStringFlag(*flags_storage, *entry);
   }
@@ -539,7 +540,7 @@ void FlagsState::RemoveFlagsSwitches(
     // For any featrue name in |features| that is not in |switch_added_values| -
     // i.e. it wasn't added by about_flags code, add it to |remaining_features|.
     for (const auto& feature : features) {
-      if (!switch_added_values.contains(std::string(feature))) {
+      if (!base::Contains(switch_added_values, std::string(feature))) {
         remaining_features.push_back(feature);
       }
     }
@@ -792,7 +793,7 @@ void FlagsState::AddSwitchMapping(
     const std::string& switch_name,
     const std::string& switch_value,
     std::map<std::string, SwitchEntry>* name_to_switch_map) const {
-  DCHECK(!name_to_switch_map->contains(key));
+  DCHECK(!base::Contains(*name_to_switch_map, key));
 
   SwitchEntry* entry = &(*name_to_switch_map)[key];
   entry->switch_name = switch_name;
@@ -805,7 +806,7 @@ void FlagsState::AddFeatureMapping(
     bool feature_state,
     const std::string& variation_id,
     std::map<std::string, SwitchEntry>* name_to_switch_map) const {
-  DCHECK(!name_to_switch_map->contains(key));
+  DCHECK(!base::Contains(*name_to_switch_map, key));
 
   SwitchEntry* entry = &(*name_to_switch_map)[key];
   entry->feature_name = feature_name;
@@ -889,11 +890,11 @@ void FlagsState::MergeFeatureCommandLineSwitch(
   std::vector<std::string_view> features =
       base::FeatureList::SplitFeatureListString(original_switch_value);
   // Only add features that don't already exist in the lists.
-  // Note: The std::ranges::contains() call results in O(n^2) performance, but
-  // in practice n should be very small.
+  // Note: The base::Contains() call results in O(n^2) performance, but in
+  // practice n should be very small.
   for (const auto& entry : feature_switches) {
     if (entry.second == feature_state &&
-        !std::ranges::contains(features, entry.first)) {
+        !base::Contains(features, entry.first)) {
       features.push_back(entry.first);
       appended_switches_[switch_name].insert(entry.first);
     }
diff --git a/components/webui/flags/flags_test_helpers.cc b/components/webui/flags/flags_test_helpers.cc
index 7c9504881a..ee31e7483e 100644
--- a/components/webui/flags/flags_test_helpers.cc
+++ b/components/webui/flags/flags_test_helpers.cc
@@ -6,13 +6,13 @@
 
 #include <gtest/gtest.h>
 
-#include <algorithm>
 #include <map>
 #include <string>
 #include <string_view>
 #include <vector>
 
 #include "base/base_paths.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/json/json_file_value_serializer.h"
 #include "base/path_service.h"
@@ -273,7 +273,7 @@ void EnsureOnlyPermittedFlagsNeverExpire() {
 
   for (const auto& entry : metadata) {
     if (entry.second.expiry_milestone == -1 &&
-        !std::ranges::contains(listed_flags, entry.first)) {
+        !base::Contains(listed_flags, entry.first)) {
       missing_flags.push_back(entry.first);
     }
   }
--- a/components/contextual_search/input_state_model.cc
+++ b/components/contextual_search/input_state_model.cc
@@ -11,6 +11,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "components/contextual_search/contextual_search_session_handle.h"
 #include "components/contextual_search/contextual_search_types.h"
 #include "components/contextual_search/pref_names.h"
@@ -220,7 +221,7 @@
 bool AreItemsAllowed(const T& items, const U& allowed_items) {
   return std::all_of(items.begin(), items.end(),
                      [&allowed_items](const auto& item) {
-                       return std::ranges::contains(allowed_items, item);
+                       return base::Contains(allowed_items, item);
                      });
 }
 
@@ -377,7 +378,7 @@
     bool incompatible_with_model =
         state_.active_model != omnibox::ModelMode::MODEL_MODE_UNSPECIFIED &&
         active_model_rule && !active_model_rule->allow_all_tools() &&
-        !std::ranges::contains(active_model_rule->allowed_tools(), tool);
+        !base::Contains(active_model_rule->allowed_tools(), tool);
 
     const omnibox::ToolRule* tool_rule = GetToolRule(rule_set_, tool);
     bool incompatible_with_inputs =
@@ -387,7 +388,7 @@
                           tool_rule->allowed_input_types()));
 
     if (incompatible_with_model || incompatible_with_inputs ||
-        std::ranges::contains(permanently_disabled_tools_, tool)) {
+        base::Contains(permanently_disabled_tools_, tool)) {
       state_.disabled_tools.push_back(tool);
     }
   }
@@ -410,7 +411,7 @@
     bool incompatible_with_tool =
         state_.active_tool != omnibox::ToolMode::TOOL_MODE_UNSPECIFIED &&
         (!model_rule || (!model_rule->allow_all_tools() &&
-                         !std::ranges::contains(model_rule->allowed_tools(),
+                         !base::Contains(model_rule->allowed_tools(),
                                                 state_.active_tool)));
 
     bool incompatible_with_inputs =
@@ -481,18 +482,18 @@
     bool incompatible_with_model =
         state_.active_model != omnibox::ModelMode::MODEL_MODE_UNSPECIFIED &&
         active_model_rule && !active_model_rule->allow_all_input_types() &&
-        !std::ranges::contains(active_model_rule->allowed_input_types(),
+        !base::Contains(active_model_rule->allowed_input_types(),
                                input_type);
 
     bool incompatible_with_tool =
         state_.active_tool != omnibox::ToolMode::TOOL_MODE_UNSPECIFIED &&
         active_tool_rule && !active_tool_rule->allow_all_input_types() &&
-        !std::ranges::contains(active_tool_rule->allowed_input_types(),
+        !base::Contains(active_tool_rule->allowed_input_types(),
                                input_type);
 
     if (input_limit_reached || incompatible_with_model ||
         incompatible_with_tool ||
-        std::ranges::contains(permanently_disabled_input_types_, input_type)) {
+        base::Contains(permanently_disabled_input_types_, input_type)) {
       state_.disabled_input_types.push_back(input_type);
     }
   }
