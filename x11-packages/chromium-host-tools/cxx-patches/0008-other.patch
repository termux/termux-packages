From 9b398533090b2e571c5b1f5b49250f20300dce35 Mon Sep 17 00:00:00 2001
From: Chongyun Lee <licy183@termux.dev>
Date: Tue, 10 Mar 2026 02:01:13 +0800
Subject: [PATCH 8/8] other

---
 apps/load_and_launch_browsertest.cc           |  4 +-
 cc/animation/animation_host.cc                |  3 +-
 cc/layers/picture_layer_impl.cc               |  3 +-
 cc/layers/tile_based_layer_impl.h             |  3 +-
 cc/paint/filter_operations.cc                 |  3 +-
 cc/raster/categorized_worker_pool.cc          |  7 +-
 cc/raster/raster_buffer_provider_perftest.cc  |  6 +-
 cc/raster/staging_buffer_pool.cc              |  5 +-
 cc/raster/task_graph_work_queue.cc            | 14 ++--
 cc/raster/task_graph_work_queue.h             |  1 +
 cc/resources/resource_pool.cc                 |  9 +--
 cc/resources/ui_resource_manager.cc           |  6 +-
 cc/test/fake_tile_manager.cc                  |  4 +-
 cc/tiles/checker_image_tracker.h              |  1 +
 cc/tiles/tile_manager.cc                      |  3 +-
 cc/trees/damage_tracker.cc                    | 13 ++--
 cc/trees/layer_tree_impl.cc                   |  5 +-
 cc/trees/tree_synchronizer.cc                 |  3 +-
 .../components/kcer/token_results_merger.h    |  1 +
 .../components/tether/fake_host_connection.h  |  1 +
 .../quick_pair/fast_pair_data_parser.cc       |  4 +-
 .../secure_channel/fake_connection_attempt.h  |  1 +
 .../cpp/client/fake_secure_channel_client.h   |  1 +
 device/bluetooth/adapter.cc                   |  4 +-
 device/bluetooth/bluetooth_adapter_winrt.cc   |  4 +-
 .../bluez/bluetooth_adapter_bluez.cc          |  9 +--
 ...etooth_remote_gatt_characteristic_bluez.cc |  3 +-
 .../bluetooth_remote_gatt_service_bluez.cc    |  3 +-
 .../bluez/bluetooth_service_record_bluez.cc   |  3 +-
 device/bluetooth/bluez/metrics_recorder.cc    | 67 ++++++++++---------
 .../chromeos/bluetooth_connection_logger.cc   |  3 +-
 device/bluetooth/chromeos/bluetooth_utils.cc  | 11 +--
 ...nitor_application_service_provider_impl.cc |  3 +-
 .../fake_bluetooth_admin_policy_client.cc     |  7 +-
 ...nt_monitor_application_service_provider.cc |  5 +-
 .../dbus/fake_bluetooth_battery_client.cc     |  7 +-
 .../dbus/fake_bluetooth_device_client.cc      |  7 +-
 ...ke_bluetooth_gatt_characteristic_client.cc |  5 +-
 .../fake_bluetooth_gatt_descriptor_client.cc  |  3 +-
 .../dbus/fake_bluetooth_input_client.cc       |  3 +-
 .../floss/bluetooth_adapter_floss.cc          | 29 ++++----
 .../floss/bluetooth_gatt_service_floss.cc     | 27 ++++----
 .../bluetooth_local_gatt_descriptor_floss.cc  |  9 ++-
 .../floss/exported_callback_manager.h         |  1 +
 .../floss/fake_floss_adapter_client.cc        | 10 +--
 .../floss/fake_floss_gatt_manager_client.cc   |  9 +--
 .../floss/fake_floss_socket_manager.cc        |  7 +-
 device/bluetooth/floss/floss_dbus_client.h    |  1 +
 device/bluetooth/floss/floss_lescan_client.cc |  1 +
 .../bluetooth/floss/floss_manager_client.cc   | 15 +++--
 device/bluetooth/floss/floss_sdp_types.cc     |  5 +-
 .../bluetooth/floss/floss_socket_manager.cc   |  7 +-
 device/bluetooth/gatt_service.cc              |  3 +-
 .../bluetooth/test/fake_local_gatt_service.cc |  3 +-
 device/fido/auth_token_requester.cc           | 14 ++--
 .../fido/authenticator_get_info_response.cc   |  4 +-
 device/fido/cable/fido_cable_discovery.cc     | 15 +++--
 device/fido/cable/v2_discovery.cc             |  3 +-
 device/fido/cable/v2_test_util.cc             |  3 +-
 device/fido/device_response_converter.cc      |  3 +-
 device/fido/fido_device.cc                    |  3 +-
 device/fido/fido_device_authenticator.cc      |  9 ++-
 device/fido/fido_discovery_factory.cc         |  7 +-
 device/fido/fido_request_handler_base.cc      |  9 +--
 device/fido/get_assertion_request_handler.cc  |  5 +-
 device/fido/get_assertion_task.cc             |  5 +-
 device/fido/hid/fake_hid_impl_for_testing.cc  |  5 +-
 device/fido/hid/fido_hid_device.cc            |  4 +-
 device/fido/hid/fido_hid_discovery.cc         |  3 +-
 device/fido/hid/fido_hid_packet.cc            |  3 +-
 device/fido/mac/credential_store.mm           |  1 +
 device/fido/mac/make_credential_operation.mm  |  4 +-
 .../fido/make_credential_request_handler.cc   |  4 +-
 device/fido/make_credential_task.cc           |  3 +-
 device/fido/u2f_command_constructor.cc        |  4 +-
 device/fido/virtual_ctap2_device.cc           | 11 +--
 device/fido/virtual_u2f_device.cc             | 11 +--
 device/fido/win/logging.cc                    |  4 +-
 device/gamepad/gamepad_blocklist.cc           |  3 +-
 device/vr/android/arcore/arcore_device.cc     |  9 +--
 device/vr/android/arcore/arcore_impl.cc       | 15 +++--
 .../android/openxr_depth_sensor_android.cc    |  7 +-
 .../openxr_graphics_binding_open_gles.cc      |  4 +-
 device/vr/openxr/openxr_api_wrapper.cc        | 33 ++++-----
 device/vr/openxr/openxr_device.cc             |  1 +
 device/vr/openxr/openxr_hit_test_manager.cc   |  6 +-
 device/vr/openxr/openxr_path_helper.cc        |  6 +-
 device/vr/openxr/openxr_platform_helper.cc    | 10 +--
 device/vr/openxr/openxr_render_loop.cc        |  6 +-
 .../openxr/openxr_spatial_anchor_manager.cc   |  3 +-
 .../openxr/openxr_spatial_hit_test_manager.cc | 13 ++--
 .../vr/openxr/openxr_spatial_plane_manager.cc | 18 +++--
 device/vr/openxr/test/fake_openxr_impl_api.cc |  5 +-
 device/vr/openxr/test/openxr_test_helper.cc   |  4 +-
 .../windows/openxr_graphics_binding_d3d11.cc  |  1 +
 .../windows/openxr_platform_helper_windows.cc |  1 +
 .../api/app_runtime/app_runtime_api.cc        | 17 ++---
 .../browser/api/app_window/app_window_api.cc  |  4 +-
 .../api/audio/audio_service_chromeos.cc       | 15 ++---
 .../automation_event_router.cc                |  1 +
 .../api/bluetooth/bluetooth_event_router.cc   |  1 +
 .../bluetooth_low_energy_api.cc               |  5 +-
 .../api/declarative/deduping_factory.h        |  1 +
 .../browser/api/declarative/rules_registry.cc |  3 +-
 .../api/declarative/rules_registry_service.cc | 18 +++--
 .../composite_matcher.cc                      |  5 +-
 .../declarative_net_request_api.cc            |  3 +-
 .../file_sequence_helper.cc                   |  5 +-
 .../declarative_net_request/indexed_rule.cc   | 10 +--
 .../declarative_net_request/prefs_helper.cc   |  5 +-
 .../regex_rules_matcher.cc                    |  5 +-
 .../rules_monitor_service.cc                  |  9 +--
 .../ruleset_manager.cc                        |  5 +-
 .../api/declarative_net_request/test_utils.cc |  3 +-
 .../webrequest_condition.cc                   |  4 +-
 .../webrequest_condition_attribute.cc         |  9 +--
 .../webrequest_rules_registry.cc              |  5 +-
 .../browser/api/device_permissions_manager.cc | 11 +--
 .../log_source_access_manager.cc              |  3 +-
 .../file_handlers/app_file_handler_util.cc    |  5 +-
 .../api/file_system/file_system_api.cc        |  5 +-
 .../browser/api/hid/hid_device_manager.cc     |  5 +-
 .../browser/api/management/management_api.cc  |  4 +-
 .../api/messaging/extension_message_port.cc   |  4 +-
 .../browser/api/messaging/message_service.cc  |  5 +-
 .../networking_private_chromeos.cc            |  3 +-
 .../offscreen/lifetime_enforcer_factories.cc  |  5 +-
 .../browser/api/offscreen/offscreen_api.cc    |  3 +-
 .../offscreen/offscreen_document_manager.cc   |  9 +--
 extensions/browser/api/runtime/runtime_api.cc | 23 +++----
 extensions/browser/api/socket/udp_socket.cc   |  5 +-
 .../browser/api/storage/storage_frontend.cc   |  3 +-
 .../browser/api/storage/storage_utils.cc      |  7 +-
 .../browser/api/usb/usb_device_manager.cc     |  5 +-
 .../api/user_scripts/user_scripts_api.cc      |  3 +-
 .../extension_web_request_event_router.cc     | 17 +++--
 .../web_request/web_request_api_helpers.cc    | 15 +++--
 .../api/web_request/web_request_info.cc       |  3 +-
 .../app_window/app_window_geometry_cache.cc   |  3 +-
 .../browser/app_window/app_window_registry.cc |  3 +-
 extensions/browser/blocklist.cc               |  3 +-
 .../browser/blocklist_extension_prefs.cc      |  4 +-
 extensions/browser/blocklist_state_fetcher.cc |  3 +-
 .../content_verifier/content_verifier.cc      | 14 ++--
 .../browser/content_verifier/test_utils.cc    |  3 +-
 extensions/browser/disable_reason.cc          |  4 +-
 extensions/browser/event_listener_map.cc      |  3 +-
 extensions/browser/event_router.cc            | 11 +--
 .../browser/events/event_dispatch_helper.cc   |  3 +-
 extensions/browser/extension_action.cc        |  5 +-
 extensions/browser/extension_action.h         |  1 +
 .../browser/extension_creator_filter.cc       |  3 +-
 extensions/browser/extension_host_registry.cc | 13 ++--
 .../browser/extension_host_test_helper.cc     |  3 +-
 .../browser/extension_navigation_throttle.cc  |  6 +-
 extensions/browser/extension_prefs.cc         |  3 +-
 extensions/browser/extension_registrar.cc     |  9 ++-
 .../browser/extension_user_script_loader.cc   | 15 +++--
 .../guest_view/extensions_guest_view.cc       |  3 +-
 .../mime_handler_view_attach_helper.cc        |  3 +-
 .../mime_handler_view_embedder.cc             |  4 +-
 .../web_view/web_view_renderer_state.cc       |  3 +-
 .../browser/install/crx_install_error.cc      |  6 +-
 extensions/browser/install_verifier.cc        | 23 ++++---
 .../browser/lazy_background_task_queue.cc     |  3 +-
 extensions/browser/message_tracker.cc         |  3 +-
 .../browser/mock_display_info_provider.cc     | 13 ++--
 extensions/browser/mock_external_provider.cc  |  8 ++-
 .../browser/pending_extension_manager.cc      |  1 +
 .../active_tab_permission_granter.cc          |  5 +-
 .../permissions/permissions_updater.cc        |  9 +--
 .../browser/permissions/permissions_updater.h | 13 ++--
 extensions/browser/permissions_manager.cc     |  9 +--
 extensions/browser/process_manager.cc         |  9 +--
 extensions/browser/process_map.cc             |  3 +-
 extensions/browser/renderer_startup_helper.cc | 17 ++---
 extensions/browser/script_executor.cc         | 11 +--
 .../browser/script_injection_tracker.cc       |  3 +-
 extensions/browser/scripting_utils.cc         |  8 +--
 extensions/browser/scripting_utils.h          |  1 +
 .../service_worker_task_queue.cc              | 19 +++---
 .../browser/state_store_test_observer.cc      |  3 +-
 extensions/browser/test_blocklist.cc          |  3 +-
 .../browser/test_blocklist_state_fetcher.cc   |  3 +-
 .../browser/test_event_router_observer.cc     |  4 +-
 .../browser/updater/extension_cache_fake.cc   |  3 +-
 .../browser/updater/manifest_fetch_data.cc    |  5 +-
 extensions/browser/user_script_manager.cc     |  5 +-
 extensions/browser/verified_contents.cc       |  4 +-
 extensions/browser/zipfile_installer.cc       |  4 +-
 extensions/common/constants.cc                |  9 +--
 .../content_script_injection_url_getter.cc    | 10 +--
 extensions/common/csp_validator.cc            |  3 +-
 extensions/common/extension.cc                |  5 +-
 extensions/common/extension_api.cc            |  3 +-
 extensions/common/extension_l10n_util.cc      |  5 +-
 extensions/common/extension_set.cc            |  3 +-
 extensions/common/features/simple_feature.cc  | 14 ++--
 extensions/common/file_util.cc                |  4 +-
 extensions/common/manifest.cc                 |  4 +-
 extensions/common/manifest_handler.cc         |  1 +
 .../manifest_handlers/mime_types_handler.cc   |  3 +-
 .../manifest_handlers/requirements_info.cc    |  4 +-
 .../manifest_handlers/shared_module_info.cc   |  3 +-
 .../web_accessible_resources_info.cc          |  3 +-
 extensions/common/manifest_test.cc            |  3 +-
 extensions/common/message_bundle.cc           |  3 +-
 .../common/permissions/api_permission_set.cc  |  3 +-
 .../common/permissions/permissions_data.cc    |  4 +-
 .../common/permissions/permissions_info.cc    |  9 +--
 extensions/common/url_pattern.cc              |  5 +-
 extensions/common/url_pattern_set.cc          |  7 +-
 .../native_renderer_messaging_service.cc      | 13 ++--
 .../api/messaging/one_time_message_handler.cc |  3 +-
 .../renderer/bindings/api_bindings_system.cc  |  5 +-
 .../renderer/bindings/api_event_handler.cc    |  3 +-
 .../renderer/bindings/api_event_listeners.cc  |  1 +
 .../bindings/api_response_validator.cc        |  3 +-
 extensions/renderer/bindings/api_signature.cc |  1 +
 .../bindings/api_type_reference_map.cc        | 13 ++--
 extensions/renderer/content_setting.cc        | 10 +--
 extensions/renderer/dispatcher.cc             |  5 +-
 .../mime_handler_view_container_manager.cc    |  1 +
 .../native_extension_bindings_system.cc       |  3 +-
 .../renderer/renderer_extension_registry.cc   |  5 +-
 .../renderer/resource_bundle_source_map.cc    |  3 +-
 extensions/renderer/script_injection.cc       |  3 +-
 extensions/renderer/string_source_map.cc      |  3 +-
 .../extension_test_notification_observer.cc   |  3 +-
 google_apis/drive/drive_base_requests.cc      |  3 +-
 google_apis/gaia/core_account_id.cc           |  3 +-
 google_apis/gaia/fake_gaia.cc                 |  7 +-
 google_apis/gaia/gaia_auth_util.cc            |  3 +-
 google_apis/gaia/oauth2_id_token_decoder.cc   |  5 +-
 ...nstance_id_delete_token_request_handler.cc |  3 +-
 .../gcm/engine/registration_request.cc        | 21 +++---
 .../gcm/engine/unregistration_request.cc      |  7 +-
 .../client/client_discardable_manager.cc      |  3 +-
 .../internal/mappable_buffer_test_template.h  |  9 ++-
 gpu/command_buffer/client/webgpu_interface.h  |  5 +-
 .../service/drm_modifiers_filter_vulkan.cc    |  3 +-
 .../service/framebuffer_completeness_cache.cc |  3 +-
 .../service/gles2_cmd_decoder.cc              | 47 ++++++-------
 .../gles2_cmd_decoder_passthrough_doers.cc    | 11 +--
 gpu/command_buffer/service/program_cache.cc   |  3 +-
 .../shared_image/ozone_image_backing.cc       |  3 +-
 .../shared_image/shared_image_factory.cc      |  1 +
 .../shared_image/shared_image_manager.cc      |  3 +-
 gpu/command_buffer/service/texture_manager.cc |  7 +-
 .../service/transfer_buffer_manager.cc        |  3 +-
 gpu/command_buffer/service/value_validator.h  |  3 +-
 .../service/webgpu_decoder_impl.cc            |  5 +-
 gpu/command_buffer/tests/gl_test_utils.cc     |  5 +-
 .../tests/gl_webgl_multi_draw_test.cc         |  7 +-
 gpu/config/gpu_feature_info.cc                |  3 +-
 gpu/config/gpu_switching.cc                   |  5 +-
 gpu/ipc/client/gpu_channel_host.cc            |  3 +-
 media/audio/alsa/audio_manager_alsa.cc        |  3 +-
 media/audio/audio_output_dispatcher_impl.cc   |  7 +-
 media/audio/fuchsia/audio_manager_fuchsia.cc  |  8 +--
 media/base/audio_converter.cc                 |  5 +-
 media/base/channel_mixing_matrix.cc           |  3 +-
 media/base/frame_buffer_pool.cc               |  3 +-
 .../video/chromeos/camera_3a_controller.cc    |  9 +--
 .../video/chromeos/camera_buffer_factory.cc   |  3 +-
 .../video/chromeos/camera_device_delegate.cc  |  9 +--
 .../video/chromeos/camera_hal_delegate.cc     |  2 +-
 .../video/chromeos/camera_metadata_utils.h    |  5 +-
 .../capture/video/chromeos/request_manager.cc |  3 +-
 media/capture/video/chromeos/token_manager.cc |  4 +-
 .../video_capture_device_factory_v4l2.cc      |  3 +-
 .../video/video_capture_device_client.cc      |  5 +-
 media/capture/video/video_capture_feedback.cc |  1 +
 media/capture/video/video_capture_metrics.cc  | 15 ++---
 .../win/video_capture_device_factory_win.cc   |  9 +--
 .../video/win/video_capture_device_mf_win.cc  |  1 +
 .../encoding/media_video_encoder_wrapper.cc   |  5 +-
 media/cast/encoding/vpx_quantizer_parser.cc   |  4 +-
 media/cast/logging/log_event_dispatcher.cc    |  3 +-
 media/filters/decoder_stream.cc               | 21 +++---
 media/filters/ffmpeg_glue.cc                  |  7 +-
 media/filters/offloading_video_decoder.cc     |  4 +-
 media/filters/source_buffer_state.cc          |  4 +-
 media/formats/hls/types.cc                    |  3 +-
 media/formats/webm/webm_cluster_parser.cc     |  8 +--
 .../gpu/android/maybe_render_early_manager.cc |  7 +-
 .../ndk_video_encode_accelerator_tests.cc     |  3 +-
 .../generic_dmabuf_video_frame_mapper.cc      |  4 +-
 media/gpu/chromeos/image_processor_factory.cc |  8 +--
 media/gpu/chromeos/video_decoder_pipeline.cc  |  9 ++-
 .../mac/vt_video_encode_accelerator_mac.mm    | 10 +--
 media/gpu/test/video_bitstream.cc             |  5 +-
 .../test/video_decode_accelerator_tests.cc    |  1 +
 .../video_encode_accelerator_perf_tests.cc    |  4 +-
 .../video_encoder/decoder_buffer_validator.cc |  7 +-
 .../v4l2_video_decoder_backend_stateful.cc    |  4 +-
 media/gpu/v4l2/test/h265_decoder.cc           |  1 +
 media/gpu/v4l2/test/video_decoder.cc          |  3 +-
 media/gpu/v4l2/v4l2_device.cc                 |  9 +--
 media/gpu/v4l2/v4l2_vda_helpers.cc            |  3 +-
 media/gpu/v4l2/v4l2_video_decoder.cc          |  3 +-
 .../v4l2_video_decoder_backend_stateless.cc   |  4 +-
 .../vaapi/vaapi_image_processor_backend.cc    |  1 +
 media/gpu/vaapi/vaapi_video_decoder.cc        |  3 +-
 .../gpu/vaapi/vaapi_video_decoder_delegate.cc |  2 +-
 media/gpu/vaapi/vaapi_wrapper.cc              | 23 ++++---
 .../vaapi/vp8_vaapi_video_encoder_delegate.cc |  3 +-
 ...foundation_video_encode_accelerator_win.cc |  2 +-
 media/gpu/windows/mf_audio_encoder.cc         |  5 +-
 media/midi/midi_manager_win.cc                |  7 +-
 .../services/gpu_mojo_media_client_linux.cc   |  6 +-
 media/mojo/services/media_metrics_provider.cc |  3 +-
 media/mojo/services/watch_time_recorder.cc    |  3 +-
 media/remoting/renderer_controller.cc         | 10 ++-
 media/video/software_video_encoder_test.cc    |  3 +-
 net/base/base64.cc                            |  3 +-
 net/base/filename_util_internal.cc            |  7 +-
 net/base/reconnect_notifier.cc                |  1 +
 net/base/scheme_host_port_matcher.cc          |  3 +-
 net/cert/ev_root_ca_metadata.cc               |  1 +
 net/cert/internal/trust_store_mac.cc          | 10 +--
 net/cert/qwac.cc                              |  7 +-
 net/cert/two_qwac.cc                          |  5 +-
 net/cert/x509_certificate.cc                  |  8 +--
 net/cookies/canonical_cookie.cc               |  3 +-
 net/cookies/cookie_base.cc                    |  5 +-
 .../cookie_partition_key_collection.cc        |  1 +
 net/cookies/cookie_util.cc                    |  3 +-
 net/cookies/parsed_cookie.cc                  |  5 +-
 net/cookies/test_cookie_access_delegate.cc    |  1 +
 net/device_bound_sessions/cookie_craving.cc   |  3 +-
 .../registration_fetcher.cc                   |  9 ++-
 .../registration_fetcher_param.cc             |  5 +-
 .../session_service_impl.cc                   |  1 +
 net/dns/dns_response.cc                       |  4 +-
 net/dns/dns_response_result_extractor.cc      |  5 +-
 net/dns/dns_util.cc                           |  3 +-
 net/dns/host_cache.cc                         |  5 +-
 net/dns/host_resolver_dns_task.cc             |  2 +-
 net/dns/host_resolver_manager.cc              |  4 +-
 net/dns/host_resolver_manager_job.cc          |  3 +-
 net/dns/host_resolver_nat64_task.cc           |  2 +-
 net/dns/https_record_rdata.cc                 |  9 +--
 net/dns/httpssvc_metrics.cc                   |  4 +-
 net/dns/mdns_cache.cc                         |  3 +-
 net/dns/opt_record_rdata.cc                   |  3 +-
 .../public/dns_over_https_server_config.cc    |  3 +-
 net/dns/resolve_context.cc                    |  7 +-
 .../global_first_party_sets.cc                |  5 +-
 net/first_party_sets/local_set_declaration.cc |  5 +-
 net/http/http_auth_handler_factory.cc         |  4 +-
 net/http/http_network_session.cc              |  7 +-
 net/http/http_no_vary_search_data.cc          |  9 ++-
 net/http/http_server_properties.cc            |  3 +-
 net/http/http_stream_factory_job.cc           |  1 +
 .../http_stream_factory_job_controller.cc     |  3 +-
 net/http/http_stream_pool_attempt.cc          |  6 +-
 net/http/http_stream_pool_attempt_manager.cc  |  7 +-
 net/http/no_vary_search_cache_storage.cc      |  5 +-
 net/http/transport_security_state.cc          |  3 +-
 net/log/net_log.cc                            |  5 +-
 .../network_error_logging_test_util.cc        |  7 +-
 net/nqe/network_quality.cc                    |  4 +-
 net/quic/crypto/proof_verifier_chromium.cc    |  6 +-
 .../dedicated_web_transport_http3_client.cc   |  6 +-
 net/quic/quic_chromium_client_session.cc      |  5 +-
 net/quic/quic_context.cc                      |  1 +
 net/quic/quic_context.h                       |  4 +-
 net/quic/quic_session_pool.cc                 |  5 +-
 net/quic/quic_session_pool_peer.cc            |  3 +-
 net/reporting/reporting_cache_impl.cc         |  3 +-
 net/reporting/reporting_delivery_agent.cc     |  7 +-
 net/socket/ssl_client_socket_impl.cc          |  2 +-
 net/socket/transport_client_socket_pool.cc    | 19 +++---
 .../websocket_transport_client_socket_pool.cc |  4 +-
 net/spdy/spdy_session.cc                      |  6 +-
 net/spdy/spdy_session_pool.cc                 |  4 +-
 net/ssl/test_ssl_private_key.cc               |  4 +-
 .../connection_tracker.cc                     |  3 +-
 net/tools/tld_cleanup/tld_cleanup_util.cc     |  8 ++-
 .../input_file_parsers.cc                     |  5 +-
 net/url_request/url_request_job_factory.cc    |  3 +-
 pdf/pdf_annotation_agent.h                    |  4 +-
 pdf/pdfium/pdfium_engine.cc                   |  5 +-
 pdf/pdfium/pdfium_on_demand_searchifier.cc    |  3 +-
 printing/backend/cups_ipp_helper.cc           |  7 +-
 printing/backend/cups_jobs.cc                 |  5 +-
 remoting/host/linux/gvariant_ref.h            |  4 +-
 .../browser/blob/blob_memory_controller.cc    |  2 +-
 storage/browser/blob/blob_registry_impl.cc    |  2 +-
 storage/browser/blob/blob_storage_registry.cc |  5 +-
 storage/browser/blob/blob_url_registry.cc     |  5 +-
 storage/browser/blob/blob_url_store_impl.cc   | 14 ++--
 storage/browser/blob/blob_url_utils.cc        |  3 +-
 .../file_system/file_system_context.cc        |  5 +-
 .../file_system_operation_runner.cc           |  7 +-
 .../file_system/file_system_quota_client.cc   |  3 +-
 .../file_system/file_system_usage_cache.cc    |  6 +-
 .../browser/file_system/isolated_context.cc   |  5 +-
 .../file_system/sandbox_directory_database.cc |  4 +-
 .../sandbox_file_system_backend_delegate.cc   |  4 +-
 storage/browser/quota/client_usage_tracker.cc |  5 +-
 storage/browser/quota/quota_callbacks.h       |  1 +
 storage/browser/quota/quota_database.cc       |  3 +-
 storage/browser/quota/quota_manager_impl.cc   |  5 +-
 storage/browser/quota/quota_task.cc           |  3 +-
 .../test/mock_special_storage_policy.cc       |  9 +--
 .../common/database/database_identifier.cc    |  3 +-
 .../handler/linux/exception_handler_server.cc |  5 +-
 .../crashpad_types/crashpad_info_reader.cc    | 12 ++--
 .../image_annotation_reader_test.cc           |  4 +-
 .../crashpad/util/linux/proc_task_reader.cc   |  6 +-
 url/origin.cc                                 |  5 +-
 url/scheme_host_port.cc                       |  6 +-
 url/url_util.cc                               |  9 ++-
 415 files changed, 1350 insertions(+), 1068 deletions(-)

diff --git a/apps/load_and_launch_browsertest.cc b/apps/load_and_launch_browsertest.cc
index 3499d5faac..dd090cebf7 100644
--- a/apps/load_and_launch_browsertest.cc
+++ b/apps/load_and_launch_browsertest.cc
@@ -6,11 +6,11 @@
 // The two cases are when chrome is running and another process uses the switch
 // and when chrome is started from scratch.
 
-#include <algorithm>
 #include <iterator>
 
 #include "apps/switches.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/process/launch.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/test/test_switches.h"
@@ -205,7 +205,7 @@ IN_PROC_BROWSER_TEST_F(LoadAndLaunchExtensionBrowserTest,
 #else
   // Expect |extension_instead_of_app_error|.
   EXPECT_EQ(1u, errors->size());
-  EXPECT_TRUE(std::ranges::contains(
+  EXPECT_TRUE(base::Contains(
       *errors, u"App loading flags cannot be used to load extensions"));
 #endif
 
diff --git a/cc/animation/animation_host.cc b/cc/animation/animation_host.cc
index 2965676753..69d913e90e 100644
--- a/cc/animation/animation_host.cc
+++ b/cc/animation/animation_host.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/ptr_util.h"
@@ -923,7 +924,7 @@ void AnimationHost::HandleRemovedScrollAnimatingElements(
 }
 
 void AnimationHost::AddToTicking(scoped_refptr<Animation> animation) {
-  DCHECK(!std::ranges::contains(ticking_animations_.Read(*this), animation));
+  DCHECK(!base::Contains(ticking_animations_.Read(*this), animation));
   ticking_animations_.Write(*this).push_back(animation);
 }
 
diff --git a/cc/layers/picture_layer_impl.cc b/cc/layers/picture_layer_impl.cc
index ff953c20c4..61d03975d9 100644
--- a/cc/layers/picture_layer_impl.cc
+++ b/cc/layers/picture_layer_impl.cc
@@ -14,6 +14,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/no_destructor.h"
@@ -2123,7 +2124,7 @@ void PictureLayerImpl::InvalidatePaintWorklets(
     // If the PaintWorklet depends on the property whose value was changed by
     // the animation system, then invalidate its associated PaintRecord so that
     // we can repaint the PaintWorklet during impl side invalidation.
-    if (std::ranges::contains(prop_ids, key) &&
+    if (base::Contains(prop_ids, key) &&
         entry.first->ValueChangeShouldCauseRepaint(prev, next)) {
       entry.second.second = std::nullopt;
     }
diff --git a/cc/layers/tile_based_layer_impl.h b/cc/layers/tile_based_layer_impl.h
index d72ec4c808..d4186bb688 100644
--- a/cc/layers/tile_based_layer_impl.h
+++ b/cc/layers/tile_based_layer_impl.h
@@ -8,6 +8,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "cc/base/math_util.h"
 #include "cc/cc_export.h"
 #include "cc/debug/debug_colors.h"
@@ -88,7 +89,7 @@ class CC_EXPORT TileBasedLayerImpl : public LayerImpl {
   }
 
   bool LastAppendQuadsScalesContains(float scale) const {
-    return std::ranges::contains(last_append_quads_scales_, scale);
+    return base::Contains(last_append_quads_scales_, scale);
   }
 
  private:
diff --git a/cc/paint/filter_operations.cc b/cc/paint/filter_operations.cc
index abd597505f..4c039e041c 100644
--- a/cc/paint/filter_operations.cc
+++ b/cc/paint/filter_operations.cc
@@ -11,6 +11,7 @@
 #include <numeric>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/trace_event/traced_value.h"
 #include "base/values.h"
@@ -157,7 +158,7 @@ bool FilterOperations::HasReferenceFilter() const {
 }
 
 bool FilterOperations::HasFilterOfType(FilterOperation::FilterType type) const {
-  return std::ranges::contains(operations_, type, &FilterOperation::type);
+  return base::Contains(operations_, type, &FilterOperation::type);
 }
 
 FilterOperations FilterOperations::Blend(const FilterOperations& from,
diff --git a/cc/raster/categorized_worker_pool.cc b/cc/raster/categorized_worker_pool.cc
index 81c60e79b7..9c6473e418 100644
--- a/cc/raster/categorized_worker_pool.cc
+++ b/cc/raster/categorized_worker_pool.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_number_conversions.h"
@@ -209,7 +210,7 @@ bool CategorizedWorkerPoolJob::PostDelayedTask(const base::Location& from_here,
     std::erase_if(tasks_,
                   [this](const scoped_refptr<Task>& e)
                       EXCLUSIVE_LOCKS_REQUIRED(lock_) {
-                        return std::ranges::contains(this->completed_tasks_, e);
+                        return base::Contains(this->completed_tasks_, e);
                       });
 
     tasks_.push_back(base::MakeRefCounted<ClosureTask>(std::move(task)));
@@ -391,7 +392,7 @@ size_t CategorizedWorkerPoolJob::GetMaxJobConcurrency(
   size_t num_foreground_tasks = 0;
   size_t num_background_tasks = 0;
   for (TaskCategory category : categories) {
-    if (std::ranges::contains(kBackgroundCategories, category)) {
+    if (base::Contains(kBackgroundCategories, category)) {
       if (work_queue_.NumRunningTasksForCategory(category) > 0) {
         num_background_tasks = 1;
       }
@@ -516,7 +517,7 @@ bool CategorizedWorkerPool::ShouldRunTaskForCategoryWithLockAcquired(
     return false;
   }
 
-  if (std::ranges::contains(kBackgroundCategories, category)) {
+  if (base::Contains(kBackgroundCategories, category)) {
     // Only run background tasks if there are no foreground tasks running or
     // ready to run.
     for (TaskCategory foreground_category : kForegroundCategories) {
diff --git a/cc/raster/raster_buffer_provider_perftest.cc b/cc/raster/raster_buffer_provider_perftest.cc
index b402660069..a51796744c 100644
--- a/cc/raster/raster_buffer_provider_perftest.cc
+++ b/cc/raster/raster_buffer_provider_perftest.cc
@@ -7,10 +7,10 @@
 #include <stddef.h>
 #include <stdint.h>
 
-#include <algorithm>
 #include <array>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/raw_ptr.h"
 #include "base/test/test_simple_task_runner.h"
@@ -267,8 +267,8 @@ class RasterBufferProviderPerfTestBase {
 
       for (auto& decode_task : raster_task->dependencies()) {
         // Add decode task if it doesn't already exist in graph.
-        if (!std::ranges::contains(graph->nodes, decode_task,
-                                   &TaskGraph::Node::task)) {
+        if (!base::Contains(graph->nodes, decode_task,
+                            &TaskGraph::Node::task)) {
           graph->nodes.push_back(
               TaskGraph::Node(decode_task.get(), 0u /* group */, priority, 0u));
         }
diff --git a/cc/raster/staging_buffer_pool.cc b/cc/raster/staging_buffer_pool.cc
index d739f49c59..25bf2b6dd8 100644
--- a/cc/raster/staging_buffer_pool.cc
+++ b/cc/raster/staging_buffer_pool.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/stringprintf.h"
 #include "base/task/sequenced_task_runner.h"
@@ -161,8 +162,8 @@ bool StagingBufferPool::OnMemoryDump(
     for (const StagingBuffer* buffer : buffers_) {
       buffer->OnMemoryDump(
           pmd, buffer->format,
-          std::ranges::contains(free_buffers_, buffer,
-                                &std::unique_ptr<StagingBuffer>::get));
+          base::Contains(free_buffers_, buffer,
+                         &std::unique_ptr<StagingBuffer>::get));
     }
   }
   return true;
diff --git a/cc/raster/task_graph_work_queue.cc b/cc/raster/task_graph_work_queue.cc
index 25611ce9c8..006bcdb2f6 100644
--- a/cc/raster/task_graph_work_queue.cc
+++ b/cc/raster/task_graph_work_queue.cc
@@ -12,6 +12,7 @@
 #include <unordered_map>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr_exclusion.h"
 #include "base/trace_event/trace_event.h"
 #include "base/trace_event/trace_id_helper.h"
@@ -201,8 +202,8 @@ void TaskGraphWorkQueue::ScheduleTasks(NamespaceToken token, TaskGraph* graph) {
       continue;
 
     // Skip if already running.
-    if (std::ranges::contains(task_namespace.running_tasks, node.task.get(),
-                              &CategorizedTask::second)) {
+    if (base::Contains(task_namespace.running_tasks, node.task.get(),
+                       &CategorizedTask::second)) {
       continue;
     }
 
@@ -231,13 +232,12 @@ void TaskGraphWorkQueue::ScheduleTasks(NamespaceToken token, TaskGraph* graph) {
       continue;
 
     // Skip if already running.
-    if (std::ranges::contains(task_namespace.running_tasks, node.task.get(),
-                              &CategorizedTask::second)) {
+    if (base::Contains(task_namespace.running_tasks, node.task.get(),
+                       &CategorizedTask::second)) {
       continue;
     }
 
-    DCHECK(!std::ranges::contains(task_namespace.completed_tasks,
-                                  node.task.get()));
+    DCHECK(!base::Contains(task_namespace.completed_tasks, node.task.get()));
     node.task->state().DidCancel();
     task_namespace.completed_tasks.push_back(node.task);
   }
@@ -343,7 +343,7 @@ bool TaskGraphWorkQueue::DecrementNodeDependencies(
       TaskNamespace::Vector& ready_to_run_namespaces =
           ready_to_run_namespaces_[node.category];
 
-      DCHECK(!std::ranges::contains(ready_to_run_namespaces, task_namespace));
+      DCHECK(!base::Contains(ready_to_run_namespaces, task_namespace));
       // TODO(paint-dev): The following line could be:
       //   if (rebuild_heap) {
       //     ready_to_run_namspaces.push_heap();
diff --git a/cc/raster/task_graph_work_queue.h b/cc/raster/task_graph_work_queue.h
index 3c9d301848..595a275fb9 100644
--- a/cc/raster/task_graph_work_queue.h
+++ b/cc/raster/task_graph_work_queue.h
@@ -12,6 +12,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "cc/cc_export.h"
 #include "cc/raster/task_graph_runner.h"
diff --git a/cc/resources/resource_pool.cc b/cc/resources/resource_pool.cc
index 99bf998556..b1ac20783d 100644
--- a/cc/resources/resource_pool.cc
+++ b/cc/resources/resource_pool.cc
@@ -14,6 +14,7 @@
 #include <utility>
 
 #include "base/atomic_sequence_num.h"
+#include "base/containers/contains.h"
 #include "base/format_macros.h"
 #include "base/functional/bind.h"
 #include "base/notreached.h"
@@ -457,12 +458,12 @@ void ResourcePool::ReleaseResource(InUsePoolResource in_use_resource) {
 
     // Maybe this is a double free - see if the resource exists in our busy
     // list.
-    CHECK(!std::ranges::contains(busy_resources_, pool_resource->unique_id(),
-                                 &PoolResource::unique_id));
+    CHECK(!base::Contains(busy_resources_, pool_resource->unique_id(),
+                          &PoolResource::unique_id));
 
     // Also check if the resource exists in our unused resources list.
-    CHECK(!std::ranges::contains(unused_resources_, pool_resource->unique_id(),
-                                 &PoolResource::unique_id));
+    CHECK(!base::Contains(unused_resources_, pool_resource->unique_id(),
+                          &PoolResource::unique_id));
 
     // Resource doesn't exist in any of our lists. NOTREACHED().
     NOTREACHED();
diff --git a/cc/resources/ui_resource_manager.cc b/cc/resources/ui_resource_manager.cc
index 96e557b74d..bec7d15cf4 100644
--- a/cc/resources/ui_resource_manager.cc
+++ b/cc/resources/ui_resource_manager.cc
@@ -4,12 +4,12 @@
 
 #include "cc/resources/ui_resource_manager.h"
 
-#include <algorithm>
 #include <unordered_map>
 #include <utility>
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "cc/resources/scoped_ui_resource.h"
 #include "third_party/skia/include/core/SkBitmap.h"
 
@@ -54,8 +54,8 @@ void UIResourceManager::RecreateUIResources() {
     UIResourceId uid = resource.first;
     const UIResourceClientData& data = resource.second;
     bool resource_lost = true;
-    if (!std::ranges::contains(ui_resource_request_queue_, uid,
-                               &UIResourceRequest::GetId)) {
+    if (!base::Contains(ui_resource_request_queue_, uid,
+                        &UIResourceRequest::GetId)) {
       UIResourceRequest request(UIResourceRequest::Type::kCreate, uid,
                                 data.client->GetBitmap(uid, resource_lost));
       ui_resource_request_queue_.push_back(request);
diff --git a/cc/test/fake_tile_manager.cc b/cc/test/fake_tile_manager.cc
index a24a3c37b2..8009930d10 100644
--- a/cc/test/fake_tile_manager.cc
+++ b/cc/test/fake_tile_manager.cc
@@ -7,10 +7,10 @@
 #include <stddef.h>
 #include <stdint.h>
 
-#include <algorithm>
 #include <limits>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/task/single_thread_task_runner.h"
 #include "cc/raster/raster_buffer.h"
 #include "cc/raster/synchronous_task_graph_runner.h"
@@ -54,7 +54,7 @@ FakeTileManager::FakeTileManager(TileManagerClient* client,
 FakeTileManager::~FakeTileManager() = default;
 
 bool FakeTileManager::HasBeenAssignedMemory(Tile* tile) {
-  return std::ranges::contains(tiles_for_raster, tile);
+  return base::Contains(tiles_for_raster, tile);
 }
 
 }  // namespace cc
diff --git a/cc/tiles/checker_image_tracker.h b/cc/tiles/checker_image_tracker.h
index 27a654c123..a0d8a3a97e 100644
--- a/cc/tiles/checker_image_tracker.h
+++ b/cc/tiles/checker_image_tracker.h
@@ -10,6 +10,7 @@
 #include <unordered_map>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "cc/cc_export.h"
 #include "cc/paint/image_id.h"
diff --git a/cc/tiles/tile_manager.cc b/cc/tiles/tile_manager.cc
index acfcfbebdb..74124b73a8 100644
--- a/cc/tiles/tile_manager.cc
+++ b/cc/tiles/tile_manager.cc
@@ -12,6 +12,7 @@
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/json/json_writer.h"
@@ -252,7 +253,7 @@ void InsertNodeForTask(TaskGraph* graph,
                        bool has_external_dependency = false) {
   TRACE_EVENT("cc", __PRETTY_FUNCTION__, "category", category, "deps",
               dependencies);
-  DCHECK(!std::ranges::contains(graph->nodes, task, &TaskGraph::Node::task));
+  DCHECK(!base::Contains(graph->nodes, task, &TaskGraph::Node::task));
   graph->nodes.emplace_back(task, category, priority, dependencies,
                             has_external_dependency);
 }
diff --git a/cc/trees/damage_tracker.cc b/cc/trees/damage_tracker.cc
index a7fcfa86fc..9ce1b9a083 100644
--- a/cc/trees/damage_tracker.cc
+++ b/cc/trees/damage_tracker.cc
@@ -8,6 +8,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "cc/base/math_util.h"
 #include "cc/layers/heads_up_display_layer_impl.h"
@@ -609,8 +610,8 @@ DamageTracker::GetViewTransitionContentSurfaceDamageInSharedElementLayerSpace(
 
     // A live content surface is found. Add this id to the current id list.
     // Don't add this id if no surface is found.
-    DCHECK(!std::ranges::contains(
-        current_view_transition_content_surfaces_by_id_, vt_resource_id));
+    DCHECK(!base::Contains(current_view_transition_content_surfaces_by_id_,
+                           vt_resource_id));
     current_view_transition_content_surfaces_by_id_.push_back(vt_resource_id);
   }
 
@@ -656,10 +657,10 @@ DamageTracker::GetViewTransitionContentSurfaceDamageInSharedElementLayerSpace(
   // (1) If this is a new content surface with vt_resource_id.
   // (2) If the content surface with vt_resource_id was there in the previous
   // frame but missing in the current frame.
-  if (std::ranges::contains(previous_view_transition_content_surfaces_by_id_,
-                            vt_resource_id) !=
-      std::ranges::contains(current_view_transition_content_surfaces_by_id_,
-                            vt_resource_id)) {
+  if (base::Contains(previous_view_transition_content_surfaces_by_id_,
+                     vt_resource_id) !=
+      base::Contains(current_view_transition_content_surfaces_by_id_,
+                     vt_resource_id)) {
     // The whole view transition layer is considered damaged.
     return layer_drawable_bounds;
   }
diff --git a/cc/trees/layer_tree_impl.cc b/cc/trees/layer_tree_impl.cc
index 70236d7bf5..a64470d856 100644
--- a/cc/trees/layer_tree_impl.cc
+++ b/cc/trees/layer_tree_impl.cc
@@ -15,6 +15,7 @@
 #include <utility>
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/json/json_writer.h"
@@ -1972,7 +1973,7 @@ void LayerTreeImpl::ReserveLayers(size_t count) {
 
 void LayerTreeImpl::AddLayer(std::unique_ptr<LayerImpl> layer) {
   DCHECK(layer);
-  DCHECK(!std::ranges::contains(layer_list_, layer));
+  DCHECK(!base::Contains(layer_list_, layer));
   layer_list_.push_back(std::move(layer));
   set_needs_update_draw_properties();
 }
@@ -2323,7 +2324,7 @@ void LayerTreeImpl::ProcessUIResourceRequestQueue() {
 }
 
 void LayerTreeImpl::RegisterPictureLayerImpl(PictureLayerImpl* layer) {
-  DCHECK(!std::ranges::contains(picture_layers_, layer));
+  DCHECK(!base::Contains(picture_layers_, layer));
   picture_layers_.push_back(layer);
 }
 
diff --git a/cc/trees/tree_synchronizer.cc b/cc/trees/tree_synchronizer.cc
index 58e3a8b287..71b969b347 100644
--- a/cc/trees/tree_synchronizer.cc
+++ b/cc/trees/tree_synchronizer.cc
@@ -13,6 +13,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
@@ -59,7 +60,7 @@ static bool LayerWillPushProperties(const LayerTreeImpl* tree,
   return tree->LayersThatShouldPushProperties().contains(layer) ||
          // TODO(crbug.com/40335690): Stop always pushing PictureLayerImpl
          // properties.
-         std::ranges::contains(tree->picture_layers(), layer);
+         base::Contains(tree->picture_layers(), layer);
 }
 #endif
 
diff --git a/chromeos/ash/components/kcer/token_results_merger.h b/chromeos/ash/components/kcer/token_results_merger.h
index 8e3776bc70..2a827692ae 100644
--- a/chromeos/ash/components/kcer/token_results_merger.h
+++ b/chromeos/ash/components/kcer/token_results_merger.h
@@ -8,6 +8,7 @@
 #include <type_traits>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/callback.h"
 #include "base/memory/ref_counted.h"
diff --git a/chromeos/ash/components/tether/fake_host_connection.h b/chromeos/ash/components/tether/fake_host_connection.h
index 102e5d0857..bcef86246e 100644
--- a/chromeos/ash/components/tether/fake_host_connection.h
+++ b/chromeos/ash/components/tether/fake_host_connection.h
@@ -5,6 +5,7 @@
 #ifndef CHROMEOS_ASH_COMPONENTS_TETHER_FAKE_HOST_CONNECTION_H_
 #define CHROMEOS_ASH_COMPONENTS_TETHER_FAKE_HOST_CONNECTION_H_
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/memory/raw_ptr.h"
 #include "base/observer_list.h"
diff --git a/chromeos/ash/services/quick_pair/fast_pair_data_parser.cc b/chromeos/ash/services/quick_pair/fast_pair_data_parser.cc
index 904c2eee76..d8e7870b73 100644
--- a/chromeos/ash/services/quick_pair/fast_pair_data_parser.cc
+++ b/chromeos/ash/services/quick_pair/fast_pair_data_parser.cc
@@ -7,7 +7,6 @@
 #include <algorithm>
 #include <cstdint>
 #include <optional>
-#include <ranges>
 #include <vector>
 
 #include "ash/constants/ash_features.h"
@@ -320,7 +319,8 @@ void FastPairDataParser::ParseMessageStreamMessages(
     return;
   }
 
-  base::circular_deque<uint8_t> remaining_bytes(std::from_range, message_bytes);
+  base::circular_deque<uint8_t> remaining_bytes(base::from_range,
+                                                message_bytes);
   while (remaining_bytes.size() >= kMinMessageByteCount) {
     uint8_t message_group_byte = remaining_bytes.front();
     remaining_bytes.pop_front();
diff --git a/chromeos/ash/services/secure_channel/fake_connection_attempt.h b/chromeos/ash/services/secure_channel/fake_connection_attempt.h
index 603d0e4200..7f972216d9 100644
--- a/chromeos/ash/services/secure_channel/fake_connection_attempt.h
+++ b/chromeos/ash/services/secure_channel/fake_connection_attempt.h
@@ -7,6 +7,7 @@
 
 #include <unordered_map>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/time/default_clock.h"
 #include "base/unguessable_token.h"
diff --git a/chromeos/ash/services/secure_channel/public/cpp/client/fake_secure_channel_client.h b/chromeos/ash/services/secure_channel/public/cpp/client/fake_secure_channel_client.h
index 4c35f30139..098040645f 100644
--- a/chromeos/ash/services/secure_channel/public/cpp/client/fake_secure_channel_client.h
+++ b/chromeos/ash/services/secure_channel/public/cpp/client/fake_secure_channel_client.h
@@ -10,6 +10,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "chromeos/ash/components/multidevice/remote_device_ref.h"
 #include "chromeos/ash/services/secure_channel/public/cpp/client/connection_attempt.h"
diff --git a/device/bluetooth/adapter.cc b/device/bluetooth/adapter.cc
index 9382f3030d..795c61bf0f 100644
--- a/device/bluetooth/adapter.cc
+++ b/device/bluetooth/adapter.cc
@@ -4,11 +4,11 @@
 
 #include "device/bluetooth/adapter.h"
 
-#include <algorithm>
 #include <string>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/ptr_util.h"
@@ -314,7 +314,7 @@ void Adapter::CreateLocalGattService(
 void Adapter::IsLeScatternetDualRoleSupported(
     IsLeScatternetDualRoleSupportedCallback callback) {
 #if BUILDFLAG(IS_CHROMEOS)
-  std::move(callback).Run(std::ranges::contains(
+  std::move(callback).Run(base::Contains(
       adapter_->GetSupportedRoles(),
       device::BluetoothAdapter::BluetoothRole::kCentralPeripheral));
 #else
diff --git a/device/bluetooth/bluetooth_adapter_winrt.cc b/device/bluetooth/bluetooth_adapter_winrt.cc
index 4a9e74c38e..6fb72b71ab 100644
--- a/device/bluetooth/bluetooth_adapter_winrt.cc
+++ b/device/bluetooth/bluetooth_adapter_winrt.cc
@@ -9,11 +9,11 @@
 #include <windows.storage.streams.h>
 #include <wrl/event.h>
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -1383,7 +1383,7 @@ void BluetoothAdapterWinrt::OnAdvertisementWatcherStopped(
 void BluetoothAdapterWinrt::OnRegisterAdvertisement(
     BluetoothAdvertisement* advertisement,
     CreateAdvertisementCallback callback) {
-  DCHECK(std::ranges::contains(pending_advertisements_, advertisement));
+  DCHECK(base::Contains(pending_advertisements_, advertisement));
   auto wrapped_advertisement = base::WrapRefCounted(advertisement);
   std::erase(pending_advertisements_, advertisement);
   std::move(callback).Run(std::move(wrapped_advertisement));
diff --git a/device/bluetooth/bluez/bluetooth_adapter_bluez.cc b/device/bluetooth/bluez/bluetooth_adapter_bluez.cc
index d27e1d8da3..ebc922c0e6 100644
--- a/device/bluetooth/bluez/bluetooth_adapter_bluez.cc
+++ b/device/bluetooth/bluez/bluetooth_adapter_bluez.cc
@@ -12,6 +12,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -630,7 +631,7 @@ bool BluetoothAdapterBlueZ::IsExtendedAdvertisementsAvailable() const {
   // Based on the implementation of kernel bluez, if the controller supports Ext
   // Advertisement, it must support HardwareOffload.
   // (net/bluetooth/mgmt.c:get_supported_adv_flags)
-  return std::ranges::contains(
+  return base::Contains(
       properties->supported_features.value(),
       bluetooth_advertising_manager::kSupportedFeaturesHardwareOffload);
 }
@@ -1666,9 +1667,9 @@ BluetoothAdapterBlueZ::GetLowEnergyScanSessionHardwareOffloadingStatus() {
     return LowEnergyScanSessionHardwareOffloadingStatus::kUndetermined;
   }
 
-  return std::ranges::contains(properties->supported_features.value(),
-                               bluetooth_advertisement_monitor_manager::
-                                   kSupportedFeaturesControllerPatterns)
+  return base::Contains(properties->supported_features.value(),
+                        bluetooth_advertisement_monitor_manager::
+                            kSupportedFeaturesControllerPatterns)
              ? LowEnergyScanSessionHardwareOffloadingStatus::kSupported
              : LowEnergyScanSessionHardwareOffloadingStatus::kNotSupported;
 }
diff --git a/device/bluetooth/bluez/bluetooth_remote_gatt_characteristic_bluez.cc b/device/bluetooth/bluez/bluetooth_remote_gatt_characteristic_bluez.cc
index 114d157b7d..a46c7ee2c6 100644
--- a/device/bluetooth/bluez/bluetooth_remote_gatt_characteristic_bluez.cc
+++ b/device/bluetooth/bluez/bluetooth_remote_gatt_characteristic_bluez.cc
@@ -8,6 +8,7 @@
 #include <limits>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -281,7 +282,7 @@ void BluetoothRemoteGattCharacteristicBlueZ::UnsubscribeFromNotifications(
 
 void BluetoothRemoteGattCharacteristicBlueZ::GattDescriptorAdded(
     const dbus::ObjectPath& object_path) {
-  if (descriptors_.contains(object_path.value())) {
+  if (base::Contains(descriptors_, object_path.value())) {
     DVLOG(1) << "Remote GATT characteristic descriptor already exists: "
              << object_path.value();
     return;
diff --git a/device/bluetooth/bluez/bluetooth_remote_gatt_service_bluez.cc b/device/bluetooth/bluez/bluetooth_remote_gatt_service_bluez.cc
index 5961c11998..6b065d6078 100644
--- a/device/bluetooth/bluez/bluetooth_remote_gatt_service_bluez.cc
+++ b/device/bluetooth/bluez/bluetooth_remote_gatt_service_bluez.cc
@@ -5,6 +5,7 @@
 #include <iterator>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/ptr_util.h"
 #include "dbus/property.h"
@@ -146,7 +147,7 @@ void BluetoothRemoteGattServiceBlueZ::GattServicePropertyChanged(
 
 void BluetoothRemoteGattServiceBlueZ::GattCharacteristicAdded(
     const dbus::ObjectPath& object_path) {
-  if (characteristics_.contains(object_path.value())) {
+  if (base::Contains(characteristics_, object_path.value())) {
     DVLOG(1) << "Remote GATT characteristic already exists: "
              << object_path.value();
     return;
diff --git a/device/bluetooth/bluez/bluetooth_service_record_bluez.cc b/device/bluetooth/bluez/bluetooth_service_record_bluez.cc
index 319b303a5b..bd8cbb4050 100644
--- a/device/bluetooth/bluez/bluetooth_service_record_bluez.cc
+++ b/device/bluetooth/bluez/bluetooth_service_record_bluez.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/values.h"
 
 namespace bluez {
@@ -48,7 +49,7 @@ void BluetoothServiceRecordBlueZ::AddRecordEntry(
 }
 
 bool BluetoothServiceRecordBlueZ::IsAttributePresented(uint16_t id) const {
-  return attributes_.contains(id);
+  return base::Contains(attributes_, id);
 }
 
 }  // namespace bluez
diff --git a/device/bluetooth/bluez/metrics_recorder.cc b/device/bluetooth/bluez/metrics_recorder.cc
index eda51387b3..2f2222088d 100644
--- a/device/bluetooth/bluez/metrics_recorder.cc
+++ b/device/bluetooth/bluez/metrics_recorder.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/bluez/metrics_recorder.h"
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 
 namespace bluetooth {
@@ -53,59 +54,59 @@ const char kBlueZUnknown[] = "br-connection-unknown";
 
 std::optional<ConnectToServiceInsecurelyResult> ExtractResultFromErrorString(
     const std::string& error_string) {
-  if (error_string.contains(kBlueZInvalidArgumentsError)) {
+  if (base::Contains(error_string, kBlueZInvalidArgumentsError)) {
     return ConnectToServiceInsecurelyResult::kInvalidArgumentsError;
   }
 
-  if (error_string.contains(kBlueZInProgressError)) {
+  if (base::Contains(error_string, kBlueZInProgressError)) {
     return ConnectToServiceInsecurelyResult::kInProgressError;
   }
 
-  if (error_string.contains(kBlueZAlreadyExistsError)) {
+  if (base::Contains(error_string, kBlueZAlreadyExistsError)) {
     return ConnectToServiceInsecurelyResult::kAlreadyExistsError;
   }
 
-  if (error_string.contains(kBlueZNotSupportedError)) {
+  if (base::Contains(error_string, kBlueZNotSupportedError)) {
     return ConnectToServiceInsecurelyResult::kNotSupportedError;
   }
 
-  if (error_string.contains(kBlueZNotConnectedError)) {
+  if (base::Contains(error_string, kBlueZNotConnectedError)) {
     return ConnectToServiceInsecurelyResult::kNotConnectedError;
   }
 
-  if (error_string.contains(kBlueZAlreadyConnectedError)) {
+  if (base::Contains(error_string, kBlueZAlreadyConnectedError)) {
     return ConnectToServiceInsecurelyResult::kAlreadyConnectedError;
   }
 
-  if (error_string.contains(kBlueZNotAvailableError)) {
+  if (base::Contains(error_string, kBlueZNotAvailableError)) {
     return ConnectToServiceInsecurelyResult::kNotAvailableError;
   }
 
-  if (error_string.contains(kBlueZDoesNotExistError)) {
+  if (base::Contains(error_string, kBlueZDoesNotExistError)) {
     return ConnectToServiceInsecurelyResult::kDoesNotExistError;
   }
 
-  if (error_string.contains(kBlueZNotAuthorizedError)) {
+  if (base::Contains(error_string, kBlueZNotAuthorizedError)) {
     return ConnectToServiceInsecurelyResult::kNotAuthorizedError;
   }
 
-  if (error_string.contains(kBlueZNotPermittedError)) {
+  if (base::Contains(error_string, kBlueZNotPermittedError)) {
     return ConnectToServiceInsecurelyResult::kNotPermittedError;
   }
 
-  if (error_string.contains(kBlueZNoSuchAdapterError)) {
+  if (base::Contains(error_string, kBlueZNoSuchAdapterError)) {
     return ConnectToServiceInsecurelyResult::kNoSuchAdapterError;
   }
 
-  if (error_string.contains(kBlueZAgentNotAvailableError)) {
+  if (base::Contains(error_string, kBlueZAgentNotAvailableError)) {
     return ConnectToServiceInsecurelyResult::kAgentNotAvailableError;
   }
 
-  if (error_string.contains(kBlueZNotReadyError)) {
+  if (base::Contains(error_string, kBlueZNotReadyError)) {
     return ConnectToServiceInsecurelyResult::kNotReadyError;
   }
 
-  if (error_string.contains(kBlueZFailedError)) {
+  if (base::Contains(error_string, kBlueZFailedError)) {
     return ConnectToServiceInsecurelyResult::kFailedError;
   }
 
@@ -114,79 +115,79 @@ std::optional<ConnectToServiceInsecurelyResult> ExtractResultFromErrorString(
 
 ConnectToServiceFailureReason ExtractFailureReasonFromErrorString(
     const std::string& error_string) {
-  if (error_string.contains(kBlueZConnectionAlreadyConnected)) {
+  if (base::Contains(error_string, kBlueZConnectionAlreadyConnected)) {
     return ConnectToServiceFailureReason::kReasonConnectionAlreadyConnected;
   }
 
-  if (error_string.contains(kBlueZPageTimeout)) {
+  if (base::Contains(error_string, kBlueZPageTimeout)) {
     return ConnectToServiceFailureReason::kReasonPageTimeout;
   }
 
-  if (error_string.contains(kBlueZProfileUnavailable)) {
+  if (base::Contains(error_string, kBlueZProfileUnavailable)) {
     return ConnectToServiceFailureReason::kReasonProfileUnavailable;
   }
 
-  if (error_string.contains(kBlueZSdpSearch)) {
+  if (base::Contains(error_string, kBlueZSdpSearch)) {
     return ConnectToServiceFailureReason::kReasonSdpSearch;
   }
 
-  if (error_string.contains(kBlueZCreateSocket)) {
+  if (base::Contains(error_string, kBlueZCreateSocket)) {
     return ConnectToServiceFailureReason::kReasonCreateSocket;
   }
 
-  if (error_string.contains(kBlueZInvalidArgument)) {
+  if (base::Contains(error_string, kBlueZInvalidArgument)) {
     return ConnectToServiceFailureReason::kReasonInvalidArgument;
   }
 
-  if (error_string.contains(kBlueZAdapterNotPowered)) {
+  if (base::Contains(error_string, kBlueZAdapterNotPowered)) {
     return ConnectToServiceFailureReason::kReasonAdapterNotPowered;
   }
 
-  if (error_string.contains(kBlueZNotSupported)) {
+  if (base::Contains(error_string, kBlueZNotSupported)) {
     return ConnectToServiceFailureReason::kReasonNotSupported;
   }
 
-  if (error_string.contains(kBlueZBadSocket)) {
+  if (base::Contains(error_string, kBlueZBadSocket)) {
     return ConnectToServiceFailureReason::kReasonBadSocket;
   }
 
-  if (error_string.contains(kBlueZMemoryAllocation)) {
+  if (base::Contains(error_string, kBlueZMemoryAllocation)) {
     return ConnectToServiceFailureReason::kReasonMemoryAllocation;
   }
 
-  if (error_string.contains(kBlueZBusy)) {
+  if (base::Contains(error_string, kBlueZBusy)) {
     return ConnectToServiceFailureReason::kReasonBusy;
   }
 
-  if (error_string.contains(kBlueZConcurrentConnectionLimit)) {
+  if (base::Contains(error_string, kBlueZConcurrentConnectionLimit)) {
     return ConnectToServiceFailureReason::kReasonConcurrentConnectionLimit;
   }
 
-  if (error_string.contains(kBlueZTimeout)) {
+  if (base::Contains(error_string, kBlueZTimeout)) {
     return ConnectToServiceFailureReason::kReasonTimeout;
   }
 
-  if (error_string.contains(kBlueZRefused)) {
+  if (base::Contains(error_string, kBlueZRefused)) {
     return ConnectToServiceFailureReason::kReasonRefused;
   }
 
-  if (error_string.contains(kBlueZAbortedByRemote)) {
+  if (base::Contains(error_string, kBlueZAbortedByRemote)) {
     return ConnectToServiceFailureReason::kReasonAbortedByRemote;
   }
 
-  if (error_string.contains(kBlueZAbortedByLocal)) {
+  if (base::Contains(error_string, kBlueZAbortedByLocal)) {
     return ConnectToServiceFailureReason::kReasonAbortedByLocal;
   }
 
-  if (error_string.contains(kBlueZLmpProtocolError)) {
+  if (base::Contains(error_string, kBlueZLmpProtocolError)) {
     return ConnectToServiceFailureReason::kReasonLmpProtocolError;
   }
 
-  if (error_string.contains(kBlueZCanceled)) {
+  if (base::Contains(error_string, kBlueZCanceled)) {
     return ConnectToServiceFailureReason::kReasonCanceled;
   }
 
-  if (error_string.contains(kBlueZUnknown)) {
+  if (base::Contains(error_string, kBlueZUnknown)) {
     return ConnectToServiceFailureReason::kReasonUnknown;
   }
 
diff --git a/device/bluetooth/chromeos/bluetooth_connection_logger.cc b/device/bluetooth/chromeos/bluetooth_connection_logger.cc
index 83ec1fe7c0..43bf045b0f 100644
--- a/device/bluetooth/chromeos/bluetooth_connection_logger.cc
+++ b/device/bluetooth/chromeos/bluetooth_connection_logger.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/chromeos/bluetooth_connection_logger.h"
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/strcat.h"
@@ -43,7 +44,7 @@ void BluetoothConnectionLogger::RecordDeviceConnectedMetric(
   base::UmaHistogramEnumeration(
       base::StrCat({kBaseHistogramName, ".AllConnections"}), device_type);
 
-  if (device_ids_logged_this_session_.contains(device_identifier)) {
+  if (base::Contains(device_ids_logged_this_session_, device_identifier)) {
     return;
   }
 
diff --git a/device/bluetooth/chromeos/bluetooth_utils.cc b/device/bluetooth/chromeos/bluetooth_utils.cc
index d5f277e861..759839689d 100644
--- a/device/bluetooth/chromeos/bluetooth_utils.cc
+++ b/device/bluetooth/chromeos/bluetooth_utils.cc
@@ -10,6 +10,7 @@
 #include "ash/constants/ash_features.h"
 #include "ash/constants/ash_pref_names.h"
 #include "ash/constants/ash_switches.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
@@ -281,7 +282,8 @@ bool IsNonPhonePolyDevice(const device::BluetoothDevice* device) {
   constexpr auto kNonPhonePolyOuis = base::MakeFixedFlatSet<std::string_view>(
       {"64:16:7F", "48:25:67", "00:04:F2", "00:E0:DB", "00:10:1A"});
 
-  return kNonPhonePolyOuis.contains(device->GetOuiPortionOfBluetoothAddress());
+  return base::Contains(kNonPhonePolyOuis,
+                        device->GetOuiPortionOfBluetoothAddress());
 }
 
 // Provide heuristics for which transport to use for a dual device
@@ -426,9 +428,10 @@ bool IsUnsupportedDevice(const device::BluetoothDevice* device) {
       }
       // Check the service UUID to determine if it supports HID or second factor
       // authenticator (security key).
-      if (device->GetUUIDs().contains(device::BluetoothUUID(kHIDServiceUUID)) ||
-          device->GetUUIDs().contains(
-              device::BluetoothUUID(kSecurityKeyServiceUUID))) {
+      if (base::Contains(device->GetUUIDs(),
+                         device::BluetoothUUID(kHIDServiceUUID)) ||
+          base::Contains(device->GetUUIDs(),
+                         device::BluetoothUUID(kSecurityKeyServiceUUID))) {
         return false;
       }
       break;
diff --git a/device/bluetooth/dbus/bluetooth_advertisement_monitor_application_service_provider_impl.cc b/device/bluetooth/dbus/bluetooth_advertisement_monitor_application_service_provider_impl.cc
index 898a6a8a29..b2c9736ca6 100644
--- a/device/bluetooth/dbus/bluetooth_advertisement_monitor_application_service_provider_impl.cc
+++ b/device/bluetooth/dbus/bluetooth_advertisement_monitor_application_service_provider_impl.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/dbus/bluetooth_advertisement_monitor_application_service_provider_impl.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "third_party/cros_system_api/dbus/service_constants.h"
@@ -78,7 +79,7 @@ void BluetoothAdvertisementMonitorApplicationServiceProviderImpl::AddMonitor(
 
 void BluetoothAdvertisementMonitorApplicationServiceProviderImpl::RemoveMonitor(
     const dbus::ObjectPath& monitor_path) {
-  if (!advertisement_monitor_providers_.contains(monitor_path.value())) {
+  if (!base::Contains(advertisement_monitor_providers_, monitor_path.value())) {
     LOG(WARNING)
         << "Failed to remove Advertisement Monitor because it does not exist "
         << monitor_path.value();
diff --git a/device/bluetooth/dbus/fake_bluetooth_admin_policy_client.cc b/device/bluetooth/dbus/fake_bluetooth_admin_policy_client.cc
index f04619704e..b74cd2f0aa 100644
--- a/device/bluetooth/dbus/fake_bluetooth_admin_policy_client.cc
+++ b/device/bluetooth/dbus/fake_bluetooth_admin_policy_client.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/dbus/fake_bluetooth_admin_policy_client.h"
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/observer_list.h"
 #include "device/bluetooth/dbus/fake_bluetooth_adapter_client.h"
@@ -73,7 +74,7 @@ FakeBluetoothAdminPolicyClient::GetProperties(
 void FakeBluetoothAdminPolicyClient::CreateAdminPolicy(
     const dbus::ObjectPath& path,
     bool is_blocked_by_policy) {
-  DCHECK(!properties_map_.contains(path));
+  DCHECK(!base::Contains(properties_map_, path));
 
   auto properties = std::make_unique<Properties>(
       base::BindRepeating(&FakeBluetoothAdminPolicyClient::OnPropertyChanged,
@@ -91,7 +92,7 @@ void FakeBluetoothAdminPolicyClient::CreateAdminPolicy(
 void FakeBluetoothAdminPolicyClient::ChangeAdminPolicy(
     const dbus::ObjectPath& path,
     bool is_blocked_by_policy) {
-  DCHECK(properties_map_.contains(path));
+  DCHECK(base::Contains(properties_map_, path));
 
   properties_map_[path]->is_blocked_by_policy.ReplaceValue(
       is_blocked_by_policy);
@@ -104,7 +105,7 @@ void FakeBluetoothAdminPolicyClient::ChangeAdminPolicy(
 
 void FakeBluetoothAdminPolicyClient::RemoveAdminPolicy(
     const dbus::ObjectPath& path) {
-  DCHECK(properties_map_.contains(path));
+  DCHECK(base::Contains(properties_map_, path));
   properties_map_.erase(path);
 
   for (auto& observer : observers_) {
diff --git a/device/bluetooth/dbus/fake_bluetooth_advertisement_monitor_application_service_provider.cc b/device/bluetooth/dbus/fake_bluetooth_advertisement_monitor_application_service_provider.cc
index 9ab3dba1b8..4c69dd22aa 100644
--- a/device/bluetooth/dbus/fake_bluetooth_advertisement_monitor_application_service_provider.cc
+++ b/device/bluetooth/dbus/fake_bluetooth_advertisement_monitor_application_service_provider.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/dbus/fake_bluetooth_advertisement_monitor_application_service_provider.h"
 
+#include "base/containers/contains.h"
 #include "device/bluetooth/dbus/bluez_dbus_manager.h"
 #include "device/bluetooth/dbus/fake_bluetooth_advertisement_monitor_manager_client.h"
 #include "device/bluetooth/dbus/fake_bluetooth_advertisement_monitor_service_provider.h"
@@ -50,8 +51,8 @@ size_t FakeBluetoothAdvertisementMonitorApplicationServiceProvider::
 FakeBluetoothAdvertisementMonitorServiceProvider*
 FakeBluetoothAdvertisementMonitorApplicationServiceProvider::
     GetLastAddedAdvertisementMonitorServiceProvider() {
-  if (!advertisement_monitor_providers_.contains(
-          last_added_advertisement_monitor_provider_path_)) {
+  if (!base::Contains(advertisement_monitor_providers_,
+                      last_added_advertisement_monitor_provider_path_)) {
     return nullptr;
   }
 
diff --git a/device/bluetooth/dbus/fake_bluetooth_battery_client.cc b/device/bluetooth/dbus/fake_bluetooth_battery_client.cc
index 269c8d866a..d674a08b17 100644
--- a/device/bluetooth/dbus/fake_bluetooth_battery_client.cc
+++ b/device/bluetooth/dbus/fake_bluetooth_battery_client.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/observer_list.h"
 #include "device/bluetooth/dbus/fake_bluetooth_adapter_client.h"
@@ -46,7 +47,7 @@ FakeBluetoothBatteryClient::~FakeBluetoothBatteryClient() = default;
 
 void FakeBluetoothBatteryClient::CreateBattery(const dbus::ObjectPath& path,
                                                uint8_t percentage) {
-  DCHECK(!std::ranges::contains(battery_list_, path));
+  DCHECK(!base::Contains(battery_list_, path));
 
   auto properties = std::make_unique<Properties>(
       base::BindRepeating(&FakeBluetoothBatteryClient::OnPropertyChanged,
@@ -65,7 +66,7 @@ void FakeBluetoothBatteryClient::CreateBattery(const dbus::ObjectPath& path,
 void FakeBluetoothBatteryClient::ChangeBatteryPercentage(
     const dbus::ObjectPath& path,
     uint8_t percentage) {
-  DCHECK(std::ranges::contains(battery_list_, path));
+  DCHECK(base::Contains(battery_list_, path));
   DCHECK(properties_map_.contains(path));
 
   properties_map_[path]->percentage.ReplaceValue(percentage);
@@ -77,7 +78,7 @@ void FakeBluetoothBatteryClient::ChangeBatteryPercentage(
 }
 
 void FakeBluetoothBatteryClient::RemoveBattery(const dbus::ObjectPath& path) {
-  DCHECK(std::ranges::contains(battery_list_, path));
+  DCHECK(base::Contains(battery_list_, path));
 
   properties_map_.erase(path);
   battery_list_.erase(std::ranges::find(battery_list_, path));
diff --git a/device/bluetooth/dbus/fake_bluetooth_device_client.cc b/device/bluetooth/dbus/fake_bluetooth_device_client.cc
index 44a8037e48..5834bc91a3 100644
--- a/device/bluetooth/dbus/fake_bluetooth_device_client.cc
+++ b/device/bluetooth/dbus/fake_bluetooth_device_client.cc
@@ -16,6 +16,7 @@
 #include <utility>
 
 #include "base/base64.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -744,7 +745,7 @@ void FakeBluetoothDeviceClient::SetSimulationIntervalMs(int interval_ms) {
 void FakeBluetoothDeviceClient::CreateDevice(
     const dbus::ObjectPath& adapter_path,
     const dbus::ObjectPath& device_path) {
-  if (std::ranges::contains(device_list_, device_path))
+  if (base::Contains(device_list_, device_path))
     return;
 
   std::unique_ptr<Properties> properties(new Properties(
@@ -890,7 +891,7 @@ void FakeBluetoothDeviceClient::CreateDeviceWithProperties(
     const dbus::ObjectPath& adapter_path,
     const IncomingDeviceProperties& props) {
   dbus::ObjectPath device_path(props.device_path);
-  if (std::ranges::contains(device_list_, device_path))
+  if (base::Contains(device_list_, device_path))
     return;
 
   std::unique_ptr<Properties> properties(new Properties(
@@ -1945,7 +1946,7 @@ void FakeBluetoothDeviceClient::CreateTestDevice(
     id = base::Base64Encode(base::RandBytesAsVector(10));
     base::RemoveChars(id, "+/=", &id);
     device_path = dbus::ObjectPath(adapter_path.value() + "/dev" + id);
-  } while (std::ranges::contains(device_list_, device_path));
+  } while (base::Contains(device_list_, device_path));
 
   std::unique_ptr<Properties> properties(new Properties(
       base::BindRepeating(&FakeBluetoothDeviceClient::OnPropertyChanged,
diff --git a/device/bluetooth/dbus/fake_bluetooth_gatt_characteristic_client.cc b/device/bluetooth/dbus/fake_bluetooth_gatt_characteristic_client.cc
index 527fc174cf..6db8149a27 100644
--- a/device/bluetooth/dbus/fake_bluetooth_gatt_characteristic_client.cc
+++ b/device/bluetooth/dbus/fake_bluetooth_gatt_characteristic_client.cc
@@ -7,6 +7,7 @@
 #include <memory>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/logging.h"
@@ -171,7 +172,7 @@ void FakeBluetoothGattCharacteristicClient::ReadValue(
     return;
   }
 
-  if (action_extra_requests_.contains("ReadValue")) {
+  if (base::Contains(action_extra_requests_, "ReadValue")) {
     DelayedCallback* delayed = action_extra_requests_["ReadValue"];
     delayed->delay_--;
     std::move(error_callback)
@@ -244,7 +245,7 @@ void FakeBluetoothGattCharacteristicClient::WriteValue(
   }
 
   DCHECK(heart_rate_control_point_properties_.get());
-  if (action_extra_requests_.contains("WriteValue")) {
+  if (base::Contains(action_extra_requests_, "WriteValue")) {
     DelayedCallback* delayed = action_extra_requests_["WriteValue"];
     delayed->delay_--;
     std::move(error_callback)
diff --git a/device/bluetooth/dbus/fake_bluetooth_gatt_descriptor_client.cc b/device/bluetooth/dbus/fake_bluetooth_gatt_descriptor_client.cc
index 0766406151..e71585c812 100644
--- a/device/bluetooth/dbus/fake_bluetooth_gatt_descriptor_client.cc
+++ b/device/bluetooth/dbus/fake_bluetooth_gatt_descriptor_client.cc
@@ -6,6 +6,7 @@
 
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
@@ -130,7 +131,7 @@ void FakeBluetoothGattDescriptorClient::WriteValue(
     base::span<const uint8_t> value,
     base::OnceClosure callback,
     ErrorCallback error_callback) {
-  if (!properties_.contains(object_path)) {
+  if (!base::Contains(properties_, object_path)) {
     std::move(error_callback).Run(kUnknownDescriptorError, "");
     return;
   }
diff --git a/device/bluetooth/dbus/fake_bluetooth_input_client.cc b/device/bluetooth/dbus/fake_bluetooth_input_client.cc
index f626176416..662e992376 100644
--- a/device/bluetooth/dbus/fake_bluetooth_input_client.cc
+++ b/device/bluetooth/dbus/fake_bluetooth_input_client.cc
@@ -6,6 +6,7 @@
 
 #include <map>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/memory/ptr_util.h"
@@ -72,7 +73,7 @@ FakeBluetoothInputClient::Properties* FakeBluetoothInputClient::GetProperties(
 
 void FakeBluetoothInputClient::AddInputDevice(
     const dbus::ObjectPath& object_path) {
-  if (properties_map_.contains(object_path)) {
+  if (base::Contains(properties_map_, object_path)) {
     return;
   }
 
diff --git a/device/bluetooth/floss/bluetooth_adapter_floss.cc b/device/bluetooth/floss/bluetooth_adapter_floss.cc
index 4e254c4221..4bdc68d9c7 100644
--- a/device/bluetooth/floss/bluetooth_adapter_floss.cc
+++ b/device/bluetooth/floss/bluetooth_adapter_floss.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/floss/bluetooth_adapter_floss.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
 #include "base/logging.h"
@@ -259,7 +260,7 @@ void BluetoothAdapterFloss::Init() {
 
 void BluetoothAdapterFloss::NotifyDeviceFound(uint8_t scanner_id,
                                               const std::string& address) {
-  if (!devices_.contains(address)) {
+  if (!base::Contains(devices_, address)) {
     return;
   }
 
@@ -279,7 +280,7 @@ BluetoothDeviceFloss* BluetoothAdapterFloss::CreateOrGetDeviceForUpdate(
   BluetoothDeviceFloss* device_ptr;
   std::string canonical_address = device::CanonicalizeBluetoothAddress(address);
 
-  if (devices_.contains(canonical_address)) {
+  if (base::Contains(devices_, canonical_address)) {
     device_ptr =
         static_cast<BluetoothDeviceFloss*>(devices_[canonical_address].get());
     device_ptr->UpdateTimestamp();
@@ -747,7 +748,7 @@ void BluetoothAdapterFloss::UpdateDeviceProperties(
 
   // Devices are newly found if they aren't in the devices_ map or they were
   // added via ScanResult (which doesn't trigger property reads).
-  if (!devices_.contains(canonical_address)) {
+  if (!base::Contains(devices_, canonical_address)) {
     new_device_ptr = device_floss.get();
     devices_.emplace(canonical_address, std::move(device_floss));
   } else if (DeviceNeedsToReadProperties(devices_[canonical_address].get())) {
@@ -800,7 +801,7 @@ void BluetoothAdapterFloss::AdapterClearedDevice(
   auto device_floss = CreateBluetoothDeviceFloss(device_cleared);
   std::string canonical_address =
       device::CanonicalizeBluetoothAddress(device_floss->GetAddress());
-  if (devices_.contains(canonical_address)) {
+  if (base::Contains(devices_, canonical_address)) {
     BluetoothDeviceFloss* device_ptr = device_floss.get();
     BluetoothDeviceFloss* found_ptr = static_cast<BluetoothDeviceFloss*>(
         GetDevice(device_floss->GetAddress()));
@@ -1055,7 +1056,7 @@ void BluetoothAdapterFloss::DeviceBondStateChanged(
   std::string canonical_address =
       device::CanonicalizeBluetoothAddress(remote_device.address);
 
-  if (!devices_.contains(canonical_address)) {
+  if (!base::Contains(devices_, canonical_address)) {
     LOG(WARNING) << "Received BondStateChanged for a non-existent device";
     return;
   }
@@ -1165,7 +1166,7 @@ std::optional<device::BluetoothDevice::BatteryType> variant_to_battery_type(
            device::BluetoothDevice::BatteryType::kRightBudTrueWireless},
           {"case", device::BluetoothDevice::BatteryType::kCaseTrueWireless},
       };
-  if (!battery_type_lookup.contains(variant)) {
+  if (!base::Contains(battery_type_lookup, variant)) {
     return std::nullopt;
   }
   return battery_type_lookup[variant];
@@ -1372,7 +1373,7 @@ void BluetoothAdapterFloss::ConnectDevice(
   BluetoothDeviceFloss* device_ptr;
   std::string canonical_address = device::CanonicalizeBluetoothAddress(address);
 
-  if (devices_.contains(canonical_address)) {
+  if (base::Contains(devices_, canonical_address)) {
     device_ptr =
         static_cast<BluetoothDeviceFloss*>(devices_[canonical_address].get());
   } else {
@@ -1387,7 +1388,7 @@ void BluetoothAdapterFloss::ConnectDevice(
 
 void BluetoothAdapterFloss::AddLocalGattService(
     std::unique_ptr<BluetoothLocalGattServiceFloss> service) {
-  DCHECK(!owned_gatt_services_.contains(service->GetIdentifier()));
+  DCHECK(!base::Contains(owned_gatt_services_, service->GetIdentifier()));
   owned_gatt_services_[service->GetIdentifier()] = std::move(service);
 }
 
@@ -1581,7 +1582,7 @@ void BluetoothAdapterFloss::ScannerRegistered(device::BluetoothUUID uuid,
                        << ", scanner id = " << static_cast<int>(scanner_id)
                        << ", status = " << static_cast<int>(status);
 
-  if (!scanners_.contains(uuid)) {
+  if (!base::Contains(scanners_, uuid)) {
     VLOG(1) << "ScannerRegistered but no longer exists " << uuid;
     return;
   }
@@ -1602,8 +1603,8 @@ void BluetoothAdapterFloss::ScannerRegistered(device::BluetoothUUID uuid,
 void BluetoothAdapterFloss::ScanResultReceived(ScanResult scan_result) {
   BLUETOOTH_LOG(DEBUG) << __func__ << ": " << scan_result.address;
 
-  bool already_found = devices_.contains(
-      device::CanonicalizeBluetoothAddress(scan_result.address));
+  bool already_found = base::Contains(
+      devices_, device::CanonicalizeBluetoothAddress(scan_result.address));
 
   BluetoothDeviceFloss* device_ptr =
       CreateOrGetDeviceForUpdate(scan_result.address, scan_result.name);
@@ -1673,7 +1674,7 @@ void BluetoothAdapterFloss::AdvertisementLost(uint8_t scanner_id,
       {.address = scan_result.address, .name = scan_result.name}));
   std::string canonical_address =
       device::CanonicalizeBluetoothAddress(device->GetAddress());
-  if (!devices_.contains(canonical_address)) {
+  if (!base::Contains(devices_, canonical_address)) {
     BLUETOOTH_LOG(EVENT) << __func__
                          << ": Device lost but never previously found: "
                          << scan_result.address;
@@ -1802,7 +1803,7 @@ void BluetoothAdapterFloss::OnStartScan(
     device::BluetoothUUID uuid,
     uint8_t scanner_id,
     DBusResult<FlossDBusClient::BtifStatus> ret) {
-  if (!scanners_.contains(uuid)) {
+  if (!base::Contains(scanners_, uuid)) {
     VLOG(1) << "Started scanning but scanner no longer exists " << uuid;
     return;
   }
@@ -1828,7 +1829,7 @@ void BluetoothAdapterFloss::OnLowEnergyScanSessionDestroyed(
   BLUETOOTH_LOG(EVENT) << __func__ << ": UUID = " << uuid_str;
 
   device::BluetoothUUID uuid = device::BluetoothUUID(uuid_str);
-  if (!scanners_.contains(uuid)) {
+  if (!base::Contains(scanners_, uuid)) {
     return;
   }
 
diff --git a/device/bluetooth/floss/bluetooth_gatt_service_floss.cc b/device/bluetooth/floss/bluetooth_gatt_service_floss.cc
index 7a777d3810..3ce79f329e 100644
--- a/device/bluetooth/floss/bluetooth_gatt_service_floss.cc
+++ b/device/bluetooth/floss/bluetooth_gatt_service_floss.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/floss/bluetooth_gatt_service_floss.h"
 
+#include "base/containers/contains.h"
 #include "device/bluetooth/floss/floss_dbus_manager.h"
 #include "device/bluetooth/floss/floss_gatt_manager_client.h"
 
@@ -69,7 +70,7 @@ GattStatus BluetoothGattServiceFloss::GattServiceErrorToStatus(
 void BluetoothGattServiceFloss::AddObserverForHandle(
     int32_t handle,
     FlossGattClientObserver* observer) {
-  DCHECK(!observer_by_handle_.contains(handle));
+  DCHECK(!base::Contains(observer_by_handle_, handle));
   DCHECK(observer);
 
   if (observer)
@@ -79,7 +80,7 @@ void BluetoothGattServiceFloss::AddObserverForHandle(
 void BluetoothGattServiceFloss::AddServerObserverForHandle(
     int32_t handle,
     FlossGattServerObserver* observer) {
-  DCHECK(!server_observer_by_handle_.contains(handle));
+  DCHECK(!base::Contains(server_observer_by_handle_, handle));
   DCHECK(observer);
 
   if (observer) {
@@ -88,13 +89,13 @@ void BluetoothGattServiceFloss::AddServerObserverForHandle(
 }
 
 void BluetoothGattServiceFloss::RemoveObserverForHandle(int32_t handle) {
-  DCHECK(observer_by_handle_.contains(handle));
+  DCHECK(base::Contains(observer_by_handle_, handle));
 
   observer_by_handle_.erase(handle);
 }
 
 void BluetoothGattServiceFloss::RemoveServerObserverForHandle(int32_t handle) {
-  if (!server_observer_by_handle_.contains(handle)) {
+  if (!base::Contains(server_observer_by_handle_, handle)) {
     return;
   }
 
@@ -106,7 +107,7 @@ void BluetoothGattServiceFloss::GattCharacteristicRead(
     GattStatus status,
     int32_t handle,
     const std::vector<uint8_t>& data) {
-  if (observer_by_handle_.contains(handle)) {
+  if (base::Contains(observer_by_handle_, handle)) {
     observer_by_handle_[handle]->GattCharacteristicRead(address, status, handle,
                                                         data);
   }
@@ -115,7 +116,7 @@ void BluetoothGattServiceFloss::GattCharacteristicRead(
 void BluetoothGattServiceFloss::GattCharacteristicWrite(std::string address,
                                                         GattStatus status,
                                                         int32_t handle) {
-  if (observer_by_handle_.contains(handle)) {
+  if (base::Contains(observer_by_handle_, handle)) {
     observer_by_handle_[handle]->GattCharacteristicWrite(address, status,
                                                          handle);
   }
@@ -125,7 +126,7 @@ void BluetoothGattServiceFloss::GattDescriptorRead(
     GattStatus status,
     int32_t handle,
     const std::vector<uint8_t>& data) {
-  if (observer_by_handle_.contains(handle)) {
+  if (base::Contains(observer_by_handle_, handle)) {
     observer_by_handle_[handle]->GattDescriptorRead(address, status, handle,
                                                     data);
   }
@@ -134,7 +135,7 @@ void BluetoothGattServiceFloss::GattDescriptorRead(
 void BluetoothGattServiceFloss::GattDescriptorWrite(std::string address,
                                                     GattStatus status,
                                                     int32_t handle) {
-  if (observer_by_handle_.contains(handle)) {
+  if (base::Contains(observer_by_handle_, handle)) {
     observer_by_handle_[handle]->GattDescriptorWrite(address, status, handle);
   }
 }
@@ -142,7 +143,7 @@ void BluetoothGattServiceFloss::GattDescriptorWrite(std::string address,
 void BluetoothGattServiceFloss::GattNotify(std::string address,
                                            int32_t handle,
                                            const std::vector<uint8_t>& data) {
-  if (observer_by_handle_.contains(handle)) {
+  if (base::Contains(observer_by_handle_, handle)) {
     observer_by_handle_[handle]->GattNotify(address, handle, data);
   }
 }
@@ -153,7 +154,7 @@ void BluetoothGattServiceFloss::GattServerCharacteristicReadRequest(
     int32_t offset,
     bool is_long,
     int32_t handle) {
-  if (server_observer_by_handle_.contains(handle)) {
+  if (base::Contains(server_observer_by_handle_, handle)) {
     server_observer_by_handle_[handle]->GattServerCharacteristicReadRequest(
         address, request_id, offset, is_long, handle);
   }
@@ -165,7 +166,7 @@ void BluetoothGattServiceFloss::GattServerDescriptorReadRequest(
     int32_t offset,
     bool is_long,
     int32_t handle) {
-  if (server_observer_by_handle_.contains(handle)) {
+  if (base::Contains(server_observer_by_handle_, handle)) {
     server_observer_by_handle_[handle]->GattServerDescriptorReadRequest(
         address, request_id, offset, is_long, handle);
   }
@@ -180,7 +181,7 @@ void BluetoothGattServiceFloss::GattServerCharacteristicWriteRequest(
     bool needs_response,
     int32_t handle,
     std::vector<uint8_t> value) {
-  if (server_observer_by_handle_.contains(handle)) {
+  if (base::Contains(server_observer_by_handle_, handle)) {
     server_observer_by_handle_[handle]->GattServerCharacteristicWriteRequest(
         address, request_id, offset, length, is_prepared_write, needs_response,
         handle, value);
@@ -196,7 +197,7 @@ void BluetoothGattServiceFloss::GattServerDescriptorWriteRequest(
     bool needs_response,
     int32_t handle,
     std::vector<uint8_t> value) {
-  if (server_observer_by_handle_.contains(handle)) {
+  if (base::Contains(server_observer_by_handle_, handle)) {
     server_observer_by_handle_[handle]->GattServerDescriptorWriteRequest(
         address, request_id, offset, length, is_prepared_write, needs_response,
         handle, value);
diff --git a/device/bluetooth/floss/bluetooth_local_gatt_descriptor_floss.cc b/device/bluetooth/floss/bluetooth_local_gatt_descriptor_floss.cc
index 70cdc26e15..ef58f25524 100644
--- a/device/bluetooth/floss/bluetooth_local_gatt_descriptor_floss.cc
+++ b/device/bluetooth/floss/bluetooth_local_gatt_descriptor_floss.cc
@@ -4,12 +4,11 @@
 
 #include "device/bluetooth/floss/bluetooth_local_gatt_descriptor_floss.h"
 
-#include <utility>
-
 #include "base/logging.h"
 #include "base/memory/ptr_util.h"
 #include "base/rand_util.h"
 #include "base/strings/stringprintf.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "device/bluetooth/floss/bluetooth_gatt_characteristic_floss.h"
 #include "device/bluetooth/floss/floss_dbus_manager.h"
 
@@ -293,12 +292,12 @@ GattStatus BluetoothLocalGattDescriptorFloss::HandleCccDescriptor(
 
   auto properties = characteristic_->GetProperties();
   switch (notification_type) {
-    case std::to_underlying(
+    case base::to_underlying(
         device::BluetoothGattCharacteristic::NotificationType::kNone):
       cccd_type_ = device::BluetoothGattCharacteristic::NotificationType::kNone;
       delegate->OnNotificationsStop(device, characteristic);
       break;
-    case std::to_underlying(
+    case base::to_underlying(
         device::BluetoothGattCharacteristic::NotificationType::kNotification):
       if (!(properties &
             device::BluetoothGattCharacteristic::PROPERTY_NOTIFY)) {
@@ -313,7 +312,7 @@ GattStatus BluetoothLocalGattDescriptorFloss::HandleCccDescriptor(
           device::BluetoothGattCharacteristic::NotificationType::kNotification;
       delegate->OnNotificationsStart(device, cccd_type_, characteristic);
       break;
-    case std::to_underlying(
+    case base::to_underlying(
         device::BluetoothGattCharacteristic::NotificationType::kIndication):
       if (!(properties &
             device::BluetoothGattCharacteristic::PROPERTY_INDICATE)) {
diff --git a/device/bluetooth/floss/exported_callback_manager.h b/device/bluetooth/floss/exported_callback_manager.h
index e93a8585d7..4aa8026951 100644
--- a/device/bluetooth/floss/exported_callback_manager.h
+++ b/device/bluetooth/floss/exported_callback_manager.h
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
diff --git a/device/bluetooth/floss/fake_floss_adapter_client.cc b/device/bluetooth/floss/fake_floss_adapter_client.cc
index c59db2b274..37f6d9086e 100644
--- a/device/bluetooth/floss/fake_floss_adapter_client.cc
+++ b/device/bluetooth/floss/fake_floss_adapter_client.cc
@@ -161,7 +161,7 @@ void FakeFlossAdapterClient::StartDiscovery(ResponseCallback<Void> callback) {
       FlossDeviceId{kJustWorksAddress, kJustWorksName}};
 
   for (const auto& device : discoverable_devices) {
-    if (connected_addresses_.contains(device.address)) {
+    if (base::Contains(connected_addresses_, device.address)) {
       // Skip connected devices.
       continue;
     }
@@ -352,7 +352,7 @@ void FakeFlossAdapterClient::CancelBondProcess(ResponseCallback<bool> callback,
 
 void FakeFlossAdapterClient::RemoveBond(ResponseCallback<bool> callback,
                                         FlossDeviceId device) {
-  if (!bonded_addresses_.contains(device.address)) {
+  if (!base::Contains(bonded_addresses_, device.address)) {
     PostDelayedTask(base::BindOnce(std::move(callback), false));
     return;
   }
@@ -399,7 +399,7 @@ void FakeFlossAdapterClient::GetConnectionState(
   FlossAdapterClient::ConnectionState conn_state =
       FlossAdapterClient::ConnectionState::kDisconnected;
 
-  if (connected_addresses_.contains(device.address)) {
+  if (base::Contains(connected_addresses_, device.address)) {
     if (device.address == kPairedAddressBrEdr) {
       conn_state = FlossAdapterClient::ConnectionState::kPairedBREDROnly;
     } else if (device.address == kPairedAddressLE) {
@@ -438,7 +438,7 @@ void FakeFlossAdapterClient::GetRemoteAddressType(
 void FakeFlossAdapterClient::GetBondState(ResponseCallback<uint32_t> callback,
                                           const FlossDeviceId& device) {
   FlossAdapterClient::BondState bond_state =
-      bonded_addresses_.contains(device.address)
+      base::Contains(bonded_addresses_, device.address)
           ? floss::FlossAdapterClient::BondState::kBonded
           : floss::FlossAdapterClient::BondState::kNotBonded;
   base::SingleThreadTaskRunner::GetCurrentDefault()->PostTask(
@@ -475,7 +475,7 @@ void FakeFlossAdapterClient::PostDelayedTask(base::OnceClosure callback) {
 
 void FakeFlossAdapterClient::SetConnected(const std::string& address,
                                           bool connected) {
-  if (connected_addresses_.contains(address) == connected) {
+  if (base::Contains(connected_addresses_, address) == connected) {
     return;
   }
   const auto device = ConvertAddressToDevice(address);
diff --git a/device/bluetooth/floss/fake_floss_gatt_manager_client.cc b/device/bluetooth/floss/fake_floss_gatt_manager_client.cc
index 3d32ab507c..157714c9b1 100644
--- a/device/bluetooth/floss/fake_floss_gatt_manager_client.cc
+++ b/device/bluetooth/floss/fake_floss_gatt_manager_client.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/floss/fake_floss_gatt_manager_client.h"
 
+#include "base/containers/contains.h"
 #include "base/rand_util.h"
 #include "base/task/single_thread_task_runner.h"
 #include "device/bluetooth/floss/floss_dbus_client.h"
@@ -43,11 +44,11 @@ void FakeFlossGattManagerClient::AddService(ResponseCallback<Void> callback,
   }
 
   int32_t instance_id = added_service.instance_id;
-  if (services_.contains(instance_id)) {
+  if (base::Contains(services_, instance_id)) {
     GattServerServiceAdded(GattStatus::kError, added_service);
     return;
   }
-  DCHECK(!services_.contains(instance_id));
+  DCHECK(!base::Contains(services_, instance_id));
   services_[instance_id] = added_service;
 
   GattServerServiceAdded(GattStatus::kSuccess, added_service);
@@ -57,11 +58,11 @@ void FakeFlossGattManagerClient::RemoveService(ResponseCallback<Void> callback,
                                                int32_t handle) {
   std::move(callback).Run(DBusResult<Void>({}));
 
-  if (!services_.contains(handle)) {
+  if (!base::Contains(services_, handle)) {
     GattServerServiceRemoved(GattStatus::kError, handle);
     return;
   }
-  DCHECK(services_.contains(handle));
+  DCHECK(base::Contains(services_, handle));
   services_.erase(handle);
 
   GattServerServiceRemoved(GattStatus::kSuccess, handle);
diff --git a/device/bluetooth/floss/fake_floss_socket_manager.cc b/device/bluetooth/floss/fake_floss_socket_manager.cc
index f756c900e6..c5f4a5de00 100644
--- a/device/bluetooth/floss/fake_floss_socket_manager.cc
+++ b/device/bluetooth/floss/fake_floss_socket_manager.cc
@@ -10,6 +10,7 @@
 #include <sys/types.h>
 #include <unistd.h>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/task/thread_pool.h"
@@ -214,7 +215,7 @@ void FakeFlossSocketManager::Close(const SocketId id,
 void FakeFlossSocketManager::SendSocketReady(const SocketId id,
                                              const device::BluetoothUUID& uuid,
                                              const BtifStatus status) {
-  if (listening_sockets_to_callbacks_.contains(id)) {
+  if (base::Contains(listening_sockets_to_callbacks_, id)) {
     FlossListeningSocket socket;
     socket.id = id;
     socket.type = SocketType::kRfcomm;
@@ -227,7 +228,7 @@ void FakeFlossSocketManager::SendSocketReady(const SocketId id,
 
 void FakeFlossSocketManager::SendSocketClosed(const SocketId id,
                                               const BtifStatus status) {
-  if (listening_sockets_to_callbacks_.contains(id)) {
+  if (base::Contains(listening_sockets_to_callbacks_, id)) {
     FlossListeningSocket socket;
     socket.id = id;
     socket.type = SocketType::kRfcomm;
@@ -242,7 +243,7 @@ void FakeFlossSocketManager::SendIncomingConnection(
     const SocketId listener_id,
     const FlossDeviceId& remote_device,
     const device::BluetoothUUID& uuid) {
-  if (listening_sockets_to_callbacks_.contains(listener_id)) {
+  if (base::Contains(listening_sockets_to_callbacks_, listener_id)) {
     // Create a fake socket and send a new connection callback.
     FlossSocket socket;
     socket.id = listener_id;
diff --git a/device/bluetooth/floss/floss_dbus_client.h b/device/bluetooth/floss/floss_dbus_client.h
index 6b78dfe211..567c6a3555 100644
--- a/device/bluetooth/floss/floss_dbus_client.h
+++ b/device/bluetooth/floss/floss_dbus_client.h
@@ -15,6 +15,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
diff --git a/device/bluetooth/floss/floss_lescan_client.cc b/device/bluetooth/floss/floss_lescan_client.cc
index 328ed2470b..c2017a7c42 100644
--- a/device/bluetooth/floss/floss_lescan_client.cc
+++ b/device/bluetooth/floss/floss_lescan_client.cc
@@ -6,6 +6,7 @@
 #include <algorithm>
 #include <map>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
diff --git a/device/bluetooth/floss/floss_manager_client.cc b/device/bluetooth/floss/floss_manager_client.cc
index a29bd58822..c4a7457698 100644
--- a/device/bluetooth/floss/floss_manager_client.cc
+++ b/device/bluetooth/floss/floss_manager_client.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -99,7 +100,7 @@ int FlossManagerClient::GetDefaultAdapter() const {
 }
 
 bool FlossManagerClient::GetAdapterPresent(int adapter) const {
-  return adapter_to_enabled_.contains(adapter);
+  return base::Contains(adapter_to_enabled_, adapter);
 }
 
 bool FlossManagerClient::GetAdapterEnabled(int adapter) const {
@@ -328,7 +329,7 @@ void FlossManagerClient::HandleGetAvailableAdapters(
     // Emit present for new adapters that weren't in old list. Also emit the
     // enabled changed for them.
     for (auto& [adapter, enabled] : adapter_to_enabled_) {
-      if (!previous_adapters.contains(adapter)) {
+      if (!base::Contains(previous_adapters, adapter)) {
         observer.AdapterPresent(adapter, true);
         observer.AdapterEnabledChanged(adapter, enabled);
       }
@@ -338,7 +339,7 @@ void FlossManagerClient::HandleGetAvailableAdapters(
     // We don't need to emit AdapterEnabledChanged since we emit
     // AdapterPresent is false
     for (auto& [adapter, enabled] : previous_adapters) {
-      if (!adapter_to_enabled_.contains(adapter)) {
+      if (!base::Contains(adapter_to_enabled_, adapter)) {
         observer.AdapterPresent(adapter, false);
       }
     }
@@ -362,7 +363,7 @@ void FlossManagerClient::HandleRegisterCallback(DBusResult<Void> result) {
 void FlossManagerClient::HandleGetAdapterEnabledAfterPresent(
     int32_t adapter,
     DBusResult<bool> response) {
-  if (!adapter_present_pending_.contains(adapter)) {
+  if (!base::Contains(adapter_present_pending_, adapter)) {
     // We may have cleared the pending list in OnHciEnabledChanged.
     return;
   }
@@ -386,10 +387,10 @@ void FlossManagerClient::HandleGetAdapterEnabledAfterPresent(
 }
 
 void FlossManagerClient::OnHciDeviceChanged(int32_t adapter, bool present) {
-  auto was_present = adapter_to_enabled_.contains(adapter);
+  auto was_present = base::Contains(adapter_to_enabled_, adapter);
   // Newly present
   if (!was_present && present) {
-    if (!adapter_present_pending_.contains(adapter)) {
+    if (!base::Contains(adapter_present_pending_, adapter)) {
       // Defer the AdapterPresent event until we know the actual state.
       adapter_present_pending_.insert(adapter);
       CallManagerMethod<bool>(
@@ -427,7 +428,7 @@ void FlossManagerClient::OnHciDeviceChanged(int32_t adapter, bool present) {
 void FlossManagerClient::OnHciEnabledChanged(int32_t adapter, bool enabled) {
   adapter_to_enabled_[adapter] = enabled;
 
-  if (adapter_present_pending_.contains(adapter)) {
+  if (base::Contains(adapter_present_pending_, adapter)) {
     // We haven't notified the presence for this adapter. Notify now.
     adapter_present_pending_.erase(adapter);
     for (auto& observer : observers_) {
diff --git a/device/bluetooth/floss/floss_sdp_types.cc b/device/bluetooth/floss/floss_sdp_types.cc
index ee3044d00f..083024df3d 100644
--- a/device/bluetooth/floss/floss_sdp_types.cc
+++ b/device/bluetooth/floss/floss_sdp_types.cc
@@ -6,6 +6,7 @@
 
 #include <variant>
 
+#include "base/containers/contains.h"
 
 namespace floss {
 
@@ -424,12 +425,12 @@ bool FlossDBusClient::ReadDBusParam(dbus::MessageReader* reader,
     unparsed_args[key] = std::move(entry_reader);
   }
 
-  if (!unparsed_args.contains(kTypeKey)) {
+  if (!base::Contains(unparsed_args, kTypeKey)) {
     LOG(ERROR) << "BtSdpRecord did not contain type identifier";
     return false;
   }
 
-  if (!unparsed_args.contains(kVariantValueKey)) {
+  if (!base::Contains(unparsed_args, kVariantValueKey)) {
     LOG(ERROR) << "BtSdpRecord did not contain argument #0";
     return false;
   }
diff --git a/device/bluetooth/floss/floss_socket_manager.cc b/device/bluetooth/floss/floss_socket_manager.cc
index f19b2533d8..80318fe14f 100644
--- a/device/bluetooth/floss/floss_socket_manager.cc
+++ b/device/bluetooth/floss/floss_socket_manager.cc
@@ -3,6 +3,7 @@
 // found in the LICENSE file.
 #include "device/bluetooth/floss/floss_socket_manager.h"
 
+#include "base/containers/contains.h"
 #include "base/types/expected.h"
 
 namespace floss {
@@ -80,7 +81,7 @@ bool FlossDBusClient::ReadDBusParam(
     std::string key;
     dict.PopString(&key);
 
-    if (required_keys.contains(key)) {
+    if (base::Contains(required_keys, key)) {
       if (key == kListeningPropId) {
         required_keys[key] = ReadDBusParamFromVariant(&dict, &socket->id);
       } else if (key == kListeningPropSockType) {
@@ -159,7 +160,7 @@ bool FlossDBusClient::ReadDBusParam(dbus::MessageReader* reader,
     std::string key;
     dict.PopString(&key);
 
-    if (required_keys.contains(key)) {
+    if (base::Contains(required_keys, key)) {
       if (key == kConnectingPropId) {
         required_keys[key] = ReadDBusParamFromVariant(&dict, &socket->id);
       } else if (key == kConnectingPropRemoteDevice) {
@@ -244,7 +245,7 @@ bool FlossDBusClient::ReadDBusParam(
     std::string key;
     dict.PopString(&key);
 
-    if (required_keys.contains(key)) {
+    if (base::Contains(required_keys, key)) {
       if (key == kResultPropStatus) {
         required_keys[key] =
             ReadDBusParamFromVariant(&dict, &socket_result->status);
diff --git a/device/bluetooth/gatt_service.cc b/device/bluetooth/gatt_service.cc
index 57f98b6516..b83cc74a99 100644
--- a/device/bluetooth/gatt_service.cc
+++ b/device/bluetooth/gatt_service.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/gatt_service.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
 #include "base/notimplemented.h"
@@ -123,7 +124,7 @@ void GattService::OnCharacteristicReadRequest(
     int offset,
     ValueCallback callback) {
   CHECK(characteristic);
-  CHECK(characteristic_uuids_.contains(characteristic->GetUUID()));
+  CHECK(base::Contains(characteristic_uuids_, characteristic->GetUUID()));
 
   observer_remote_->OnLocalCharacteristicRead(
       /*remote_device=*/Device::ConstructDeviceInfoStruct(device),
diff --git a/device/bluetooth/test/fake_local_gatt_service.cc b/device/bluetooth/test/fake_local_gatt_service.cc
index 3712e6fa10..f1d4f299d4 100644
--- a/device/bluetooth/test/fake_local_gatt_service.cc
+++ b/device/bluetooth/test/fake_local_gatt_service.cc
@@ -4,6 +4,7 @@
 
 #include "device/bluetooth/test/fake_local_gatt_service.h"
 
+#include "base/containers/contains.h"
 #include "base/notimplemented.h"
 
 namespace bluetooth {
@@ -23,7 +24,7 @@ FakeLocalGattCharacteristic* FakeLocalGattService::AddFakeCharacteristic(
     const device::BluetoothUUID& characteristic_uuid,
     device::BluetoothGattCharacteristic::Properties properties,
     device::BluetoothGattCharacteristic::Permissions permissions) {
-  CHECK(!uuid_to_fake_characteristic_map_.contains(characteristic_uuid));
+  CHECK(!base::Contains(uuid_to_fake_characteristic_map_, characteristic_uuid));
   auto fake_characteristic = std::make_unique<FakeLocalGattCharacteristic>(
       /*characteristic_id=*/characteristic_id,
       /*characteristic_uuid=*/characteristic_uuid, /*service=*/this, properties,
diff --git a/device/fido/auth_token_requester.cc b/device/fido/auth_token_requester.cc
index e35e8f290c..d2b44e244a 100644
--- a/device/fido/auth_token_requester.cc
+++ b/device/fido/auth_token_requester.cc
@@ -7,6 +7,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/stl_util.h"
@@ -153,12 +154,13 @@ void AuthTokenRequester::OnGetUVRetries(
 void AuthTokenRequester::OnGetUVToken(
     CtapDeviceResponseCode status,
     std::optional<pin::TokenResponse> response) {
-  if (!(std::set<CtapDeviceResponseCode>{
-            CtapDeviceResponseCode::kCtap2ErrUvInvalid,
-            CtapDeviceResponseCode::kCtap2ErrOperationDenied,
-            CtapDeviceResponseCode::kCtap2ErrUvBlocked,
-            CtapDeviceResponseCode::kSuccess})
-           .contains(status)) {
+  if (!base::Contains(
+          std::set<CtapDeviceResponseCode>{
+              CtapDeviceResponseCode::kCtap2ErrUvInvalid,
+              CtapDeviceResponseCode::kCtap2ErrOperationDenied,
+              CtapDeviceResponseCode::kCtap2ErrUvBlocked,
+              CtapDeviceResponseCode::kSuccess},
+          status)) {
     // The request was rejected outright, no touch occurred.
     FIDO_LOG(ERROR) << "Ignoring status " << static_cast<int>(status)
                     << " from " << authenticator_->GetDisplayName();
diff --git a/device/fido/authenticator_get_info_response.cc b/device/fido/authenticator_get_info_response.cc
index 4592007c1e..b4bf9685ed 100644
--- a/device/fido/authenticator_get_info_response.cc
+++ b/device/fido/authenticator_get_info_response.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/numerics/safe_conversions.h"
 #include "components/cbor/values.h"
 #include "components/cbor/writer.h"
@@ -35,7 +36,8 @@ AuthenticatorGetInfoResponse::AuthenticatorGetInfoResponse(
     : versions(std::move(in_versions)),
       ctap2_versions(std::move(in_ctap2_versions)),
       aaguid(fido_parsing_utils::Materialize(in_aaguid)) {
-  DCHECK_NE(versions.contains(ProtocolVersion::kCtap2), ctap2_versions.empty());
+  DCHECK_NE(base::Contains(versions, ProtocolVersion::kCtap2),
+            ctap2_versions.empty());
 }
 
 AuthenticatorGetInfoResponse::AuthenticatorGetInfoResponse(
diff --git a/device/fido/cable/fido_cable_discovery.cc b/device/fido/cable/fido_cable_discovery.cc
index 31a1f3f8e6..e3a941abcb 100644
--- a/device/fido/cable/fido_cable_discovery.cc
+++ b/device/fido/cable/fido_cable_discovery.cc
@@ -10,6 +10,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
@@ -206,10 +207,10 @@ const BluetoothUUID& FidoCableDiscovery::FIDOCableUUID() {
 bool FidoCableDiscovery::IsCableDevice(const BluetoothDevice* device) {
   const auto& uuid1 = GoogleCableUUID();
   const auto& uuid2 = FIDOCableUUID();
-  return device->GetServiceData().contains(uuid1) ||
-         device->GetUUIDs().contains(uuid1) ||
-         device->GetServiceData().contains(uuid2) ||
-         device->GetUUIDs().contains(uuid2);
+  return base::Contains(device->GetServiceData(), uuid1) ||
+         base::Contains(device->GetUUIDs(), uuid1) ||
+         base::Contains(device->GetServiceData(), uuid2) ||
+         base::Contains(device->GetUUIDs(), uuid2);
 }
 
 void FidoCableDiscovery::OnGetAdapter(scoped_refptr<BluetoothAdapter> adapter) {
@@ -289,7 +290,7 @@ void FidoCableDiscovery::DeviceRemoved(BluetoothAdapter* adapter,
   if (IsCableDevice(device) &&
       // It only matters if V1 devices are "removed" because V2 devices do not
       // transport data over BLE.
-      active_devices_.contains(device_address)) {
+      base::Contains(active_devices_, device_address)) {
     FIDO_LOG(DEBUG) << "caBLE device removed: " << device_address;
     RemoveDevice(FidoCableDevice::GetIdForAddress(device_address));
   }
@@ -501,7 +502,7 @@ void FidoCableDiscovery::OnAdvertisementRegistered(
 void FidoCableDiscovery::CableDeviceFound(BluetoothAdapter* adapter,
                                           BluetoothDevice* device) {
   const std::string device_address = device->GetAddress();
-  if (active_devices_.contains(device_address)) {
+  if (base::Contains(active_devices_, device_address)) {
     return;
   }
 
@@ -510,7 +511,7 @@ void FidoCableDiscovery::CableDeviceFound(BluetoothAdapter* adapter,
     return;
   }
 
-  if (active_authenticator_eids_.contains(v1_match->second)) {
+  if (base::Contains(active_authenticator_eids_, v1_match->second)) {
     return;
   }
   active_authenticator_eids_.insert(v1_match->second);
diff --git a/device/fido/cable/v2_discovery.cc b/device/fido/cable/v2_discovery.cc
index 730f8703f8..f97cb4a53d 100644
--- a/device/fido/cable/v2_discovery.cc
+++ b/device/fido/cable/v2_discovery.cc
@@ -4,6 +4,7 @@
 
 #include "device/fido/cable/v2_discovery.h"
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -123,7 +124,7 @@ void Discovery::OnBLEAdvertSeen(base::span<const uint8_t, kAdvertSize> advert) {
     return;
   }
 
-  if (observed_adverts_.contains(advert_array)) {
+  if (base::Contains(observed_adverts_, advert_array)) {
     return;
   }
   observed_adverts_.insert(advert_array);
diff --git a/device/fido/cable/v2_test_util.cc b/device/fido/cable/v2_test_util.cc
index 29a168538e..9c7b352ad8 100644
--- a/device/fido/cable/v2_test_util.cc
+++ b/device/fido/cable/v2_test_util.cc
@@ -12,6 +12,7 @@
 #include "base/base64url.h"
 #include "base/check.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/callback.h"
 #include "base/memory/raw_ptr.h"
@@ -83,7 +84,7 @@ class TestNetworkContext : public network::TestNetworkContext {
     static const char kContactPrefix[] = "/cable/contact/";
     if (path.find(kNewPrefix) == 0) {
       path.remove_prefix(sizeof(kNewPrefix) - 1);
-      CHECK(!connections_.contains(std::string(path)));
+      CHECK(!base::Contains(connections_, std::string(path)));
       connections_.emplace(std::string(path), std::make_unique<Connection>(
                                                   Connection::Type::NEW,
                                                   std::move(handshake_client)));
diff --git a/device/fido/device_response_converter.cc b/device/fido/device_response_converter.cc
index 910e47dc22..012597982c 100644
--- a/device/fido/device_response_converter.cc
+++ b/device/fido/device_response_converter.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/i18n/streaming_utf8_validator.h"
 #include "base/numerics/safe_conversions.h"
@@ -406,7 +407,7 @@ std::optional<AuthenticatorGetInfoResponse> ReadCTAPGetInfoResponse(
   }
 
   if (protocol_versions.empty() ||
-      (protocol_versions.contains(ProtocolVersion::kCtap2) &&
+      (base::Contains(protocol_versions, ProtocolVersion::kCtap2) &&
        ctap2_versions.empty())) {
     return std::nullopt;
   }
diff --git a/device/fido/fido_device.cc b/device/fido/fido_device.cc
index 999a6d8cc4..9b3f81591e 100644
--- a/device/fido/fido_device.cc
+++ b/device/fido/fido_device.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "components/device_event_log/device_event_log.h"
 #include "device/fido/device_response_converter.h"
@@ -60,7 +61,7 @@ void FidoDevice::OnDeviceInfoReceived(
   std::optional<AuthenticatorGetInfoResponse> get_info_response =
       response ? ReadCTAPGetInfoResponse(*response) : std::nullopt;
   if (!get_info_response ||
-      !get_info_response->versions.contains(ProtocolVersion::kCtap2)) {
+      !base::Contains(get_info_response->versions, ProtocolVersion::kCtap2)) {
     supported_protocol_ = ProtocolVersion::kU2f;
     needs_explicit_wink_ = true;
     FIDO_LOG(DEBUG) << "The device only supports the U2F protocol.";
diff --git a/device/fido/fido_device_authenticator.cc b/device/fido/fido_device_authenticator.cc
index 69a4ab07d0..0272471109 100644
--- a/device/fido/fido_device_authenticator.cc
+++ b/device/fido/fido_device_authenticator.cc
@@ -9,6 +9,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
@@ -588,11 +589,9 @@ void FidoDeviceAuthenticator::GetPINToken(
   DCHECK(options_.client_pin_availability !=
          ClientPinAvailability::kNotSupported);
   DCHECK_NE(permissions.size(), 0u);
-  DCHECK(
-      !((std::ranges::contains(permissions,
-                               pin::Permissions::kMakeCredential)) ||
-        std::ranges::contains(permissions, pin::Permissions::kGetAssertion)) ||
-      rp_id);
+  DCHECK(!((base::Contains(permissions, pin::Permissions::kMakeCredential)) ||
+           base::Contains(permissions, pin::Permissions::kGetAssertion)) ||
+         rp_id);
 
   GetEphemeralKey(base::BindOnce(
       &FidoDeviceAuthenticator::OnHaveEphemeralKeyForGetPINToken,
diff --git a/device/fido/fido_discovery_factory.cc b/device/fido/fido_discovery_factory.cc
index fa36f3163e..aadb5800ff 100644
--- a/device/fido/fido_discovery_factory.cc
+++ b/device/fido/fido_discovery_factory.cc
@@ -4,8 +4,7 @@
 
 #include "device/fido/fido_discovery_factory.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "build/build_config.h"
 #include "components/device_event_log/device_event_log.h"
@@ -92,8 +91,8 @@ std::vector<std::unique_ptr<FidoDiscoveryBase>> FidoDiscoveryFactory::Create(
         std::vector<std::unique_ptr<FidoDiscoveryBase>> ret;
         const bool have_v2_discovery_data =
             cable_data_.has_value() &&
-            std::ranges::contains(*cable_data_, CableDiscoveryData::Version::V2,
-                                  &CableDiscoveryData::version);
+            base::Contains(*cable_data_, CableDiscoveryData::Version::V2,
+                           &CableDiscoveryData::version);
         if (qr_generator_key_.has_value() || have_v2_discovery_data) {
           ret.emplace_back(std::make_unique<cablev2::Discovery>(
               request_type_.value(), network_context_factory_,
diff --git a/device/fido/fido_request_handler_base.cc b/device/fido/fido_request_handler_base.cc
index 92809136af..23500a436e 100644
--- a/device/fido/fido_request_handler_base.cc
+++ b/device/fido/fido_request_handler_base.cc
@@ -7,6 +7,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -252,7 +253,7 @@ void FidoRequestHandlerBase::InitDiscoveries(
   for (auto& discovery : additional_discoveries) {
     // TODO: Make this work better for non-standard discoveries like Windows,
     // which currently pretends to be `kInternal`.
-    if (!available_transports.contains(discovery->transport())) {
+    if (!base::Contains(available_transports, discovery->transport())) {
       continue;
     }
     discovery->set_observer(this);
@@ -311,8 +312,8 @@ void FidoRequestHandlerBase::InitDiscoveries(
   // so we don't need this additional check.
   if (can_call_ble_apis &&
       device::BluetoothAdapterFactory::Get()->IsLowEnergySupported() &&
-      transport_availability_info_.available_transports.contains(
-          FidoTransportProtocol::kHybrid)) {
+      base::Contains(transport_availability_info_.available_transports,
+                     FidoTransportProtocol::kHybrid)) {
     FIDO_LOG(DEBUG) << "Checking for bluetooth availability";
     transport_availability_callback_readiness_->ble_information_pending = true;
     base::SequencedTaskRunner::GetCurrentDefault()->PostTask(
@@ -620,7 +621,7 @@ void FidoRequestHandlerBase::OnHavePlatformCredentialStatus(
 
 bool FidoRequestHandlerBase::HasAuthenticator(
     const std::string& authenticator_id) const {
-  return active_authenticators_.contains(authenticator_id);
+  return base::Contains(active_authenticators_, authenticator_id);
 }
 
 void FidoRequestHandlerBase::MaybeSignalTransportsEnumerated() {
diff --git a/device/fido/get_assertion_request_handler.cc b/device/fido/get_assertion_request_handler.cc
index ad82ac045a..f77bd6cfca 100644
--- a/device/fido/get_assertion_request_handler.cc
+++ b/device/fido/get_assertion_request_handler.cc
@@ -11,6 +11,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/json/json_writer.h"
@@ -328,8 +329,8 @@ void GetAssertionRequestHandler::PreselectAccount(
     DiscoverableCredentialMetadata credential) {
   DCHECK(!preselected_credential_);
   DCHECK(request_.allow_list.empty() ||
-         std::ranges::contains(request_.allow_list, credential.cred_id,
-                               &PublicKeyCredentialDescriptor::id));
+         base::Contains(request_.allow_list, credential.cred_id,
+                        &PublicKeyCredentialDescriptor::id));
   preselected_credential_ = std::move(credential);
 }
 
diff --git a/device/fido/get_assertion_task.cc b/device/fido/get_assertion_task.cc
index d8e94e7fd4..8ad0bbfb56 100644
--- a/device/fido/get_assertion_task.cc
+++ b/device/fido/get_assertion_task.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "device/base/features.h"
 #include "device/fido/authenticator_get_assertion_response.h"
@@ -40,8 +41,8 @@ bool SetResponseCredential(
     const std::vector<PublicKeyCredentialDescriptor>& allow_list) {
   if (response->credential) {
     if (!allow_list.empty() &&
-        !std::ranges::contains(allow_list, response->credential->id,
-                               &PublicKeyCredentialDescriptor::id)) {
+        !base::Contains(allow_list, response->credential->id,
+                        &PublicKeyCredentialDescriptor::id)) {
       return false;
     }
 
diff --git a/device/fido/hid/fake_hid_impl_for_testing.cc b/device/fido/hid/fake_hid_impl_for_testing.cc
index d413ec8923..7031d743bf 100644
--- a/device/fido/hid/fake_hid_impl_for_testing.cc
+++ b/device/fido/hid/fake_hid_impl_for_testing.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "device/fido/fido_parsing_utils.h"
 #include "device/fido/hid/fido_hid_discovery.h"
@@ -218,7 +219,7 @@ void FakeFidoHidManager::Connect(
 }
 
 void FakeFidoHidManager::AddDevice(device::mojom::HidDeviceInfoPtr device) {
-  DCHECK(!devices_.contains(device->guid));
+  DCHECK(!base::Contains(devices_, device->guid));
   device::mojom::HidDeviceInfo* device_info = device.get();
   for (auto& client : clients_)
     client->DeviceAdded(device_info->Clone());
@@ -245,7 +246,7 @@ void FakeFidoHidManager::RemoveDevice(const std::string device_guid) {
 }
 
 void FakeFidoHidManager::ChangeDevice(device::mojom::HidDeviceInfoPtr device) {
-  DCHECK(devices_.contains(device->guid));
+  DCHECK(base::Contains(devices_, device->guid));
   device::mojom::HidDeviceInfo* device_info = device.get();
   for (auto& client : clients_)
     client->DeviceChanged(device_info->Clone());
diff --git a/device/fido/hid/fido_hid_device.cc b/device/fido/hid/fido_hid_device.cc
index 30b01f0b62..7c3456ca03 100644
--- a/device/fido/hid/fido_hid_device.cc
+++ b/device/fido/hid/fido_hid_device.cc
@@ -4,13 +4,13 @@
 
 #include "device/fido/hid/fido_hid_device.h"
 
-#include <algorithm>
 #include <limits>
 #include <string_view>
 #include <vector>
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -451,7 +451,7 @@ void FidoHidDevice::MessageReceived(FidoHidMessage message) {
   constexpr FidoHidDeviceCommand kValidCommands[] = {
       FidoHidDeviceCommand::kMsg, FidoHidDeviceCommand::kCbor,
       FidoHidDeviceCommand::kWink, FidoHidDeviceCommand::kError};
-  if (!std::ranges::contains(kValidCommands, cmd)) {
+  if (!base::Contains(kValidCommands, cmd)) {
     FIDO_LOG(ERROR) << "Unknown CTAPHID command: " << static_cast<int>(cmd)
                     << " " << base::HexEncode(response);
     Transition(State::kDeviceError);
diff --git a/device/fido/hid/fido_hid_discovery.cc b/device/fido/hid/fido_hid_discovery.cc
index 996366288b..6e1954a3e0 100644
--- a/device/fido/hid/fido_hid_discovery.cc
+++ b/device/fido/hid/fido_hid_discovery.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/no_destructor.h"
 #include "components/device_event_log/device_event_log.h"
@@ -79,7 +80,7 @@ void FidoHidDiscovery::DeviceAdded(
   }
 
   const VidPid vid_pid{device_info->vendor_id, device_info->product_id};
-  if (ignore_list_.contains(vid_pid)) {
+  if (base::Contains(ignore_list_, vid_pid)) {
     FIDO_LOG(EVENT) << "Ignoring HID device " << vid_pid.vid << ":"
                     << vid_pid.pid;
     return;
diff --git a/device/fido/hid/fido_hid_packet.cc b/device/fido/hid/fido_hid_packet.cc
index 1c2351d1c4..7f438b111a 100644
--- a/device/fido/hid/fido_hid_packet.cc
+++ b/device/fido/hid/fido_hid_packet.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/numerics/safe_conversions.h"
 
@@ -34,7 +35,7 @@ std::unique_ptr<FidoHidInitPacket> FidoHidInitPacket::CreateFromSerializedData(
   channel_id |= serialized[index++] & 0xff;
 
   auto command = static_cast<FidoHidDeviceCommand>(serialized[index++] & 0x7f);
-  if (!std::ranges::contains(GetFidoHidDeviceCommandList(), command)) {
+  if (!base::Contains(GetFidoHidDeviceCommandList(), command)) {
     return nullptr;
   }
 
diff --git a/device/fido/mac/credential_store.mm b/device/fido/mac/credential_store.mm
index f7f05094cf..65a17de912 100644
--- a/device/fido/mac/credential_store.mm
+++ b/device/fido/mac/credential_store.mm
@@ -17,6 +17,7 @@
 #include "base/apple/foundation_util.h"
 #include "base/apple/osstatus_logging.h"
 #include "base/apple/scoped_cftyperef.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/numerics/safe_conversions.h"
diff --git a/device/fido/mac/make_credential_operation.mm b/device/fido/mac/make_credential_operation.mm
index 5f0b488869..d475f4c56d 100644
--- a/device/fido/mac/make_credential_operation.mm
+++ b/device/fido/mac/make_credential_operation.mm
@@ -6,12 +6,12 @@
 
 #import <Foundation/Foundation.h>
 
-#include <algorithm>
 #include <string>
 
 #include "base/apple/foundation_util.h"
 #include "base/apple/osstatus_logging.h"
 #include "base/apple/scoped_cftyperef.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/sys_string_conversions.h"
 #include "base/strings/utf_string_conversions.h"
@@ -43,7 +43,7 @@ MakeCredentialOperation::MakeCredentialOperation(
 MakeCredentialOperation::~MakeCredentialOperation() = default;
 
 void MakeCredentialOperation::Run() {
-  if (!std::ranges::contains(
+  if (!base::Contains(
           request_.public_key_credential_params.public_key_credential_params(),
           static_cast<int>(CoseAlgorithmIdentifier::kEs256),
           &PublicKeyCredentialParams::CredentialInfo::algorithm)) {
diff --git a/device/fido/make_credential_request_handler.cc b/device/fido/make_credential_request_handler.cc
index b357c23725..09beda114b 100644
--- a/device/fido/make_credential_request_handler.cc
+++ b/device/fido/make_credential_request_handler.cc
@@ -4,12 +4,12 @@
 
 #include "device/fido/make_credential_request_handler.h"
 
-#include <algorithm>
 #include <map>
 #include <set>
 #include <utility>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/json/json_writer.h"
@@ -155,7 +155,7 @@ MakeCredentialStatus IsCandidateAuthenticatorPostTouch(
         continue;
       }
 
-      if (std::ranges::contains(*supported_algorithms, algo.algorithm)) {
+      if (base::Contains(*supported_algorithms, algo.algorithm)) {
         at_least_one_common_algorithm = true;
         break;
       }
diff --git a/device/fido/make_credential_task.cc b/device/fido/make_credential_task.cc
index 352529846d..cb58739930 100644
--- a/device/fido/make_credential_task.cc
+++ b/device/fido/make_credential_task.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "device/base/features.h"
 #include "device/fido/ctap2_device_operation.h"
@@ -57,7 +58,7 @@ bool CtapDeviceShouldUseU2fBecauseClientPinIsSet(
       device->device_info()->options.client_pin_availability ==
       AuthenticatorSupportedOptions::ClientPinAvailability::kSupportedAndPinSet;
   bool supports_u2f =
-      device->device_info()->versions.contains(ProtocolVersion::kU2f);
+      base::Contains(device->device_info()->versions, ProtocolVersion::kU2f);
   return client_pin_set && supports_u2f;
 }
 
diff --git a/device/fido/u2f_command_constructor.cc b/device/fido/u2f_command_constructor.cc
index c630629369..2bdb8e3fa8 100644
--- a/device/fido/u2f_command_constructor.cc
+++ b/device/fido/u2f_command_constructor.cc
@@ -4,10 +4,10 @@
 
 #include "device/fido/u2f_command_constructor.h"
 
-#include <algorithm>
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "components/apdu/apdu_command.h"
 #include "crypto/hash.h"
 #include "device/fido/fido_parsing_utils.h"
@@ -21,7 +21,7 @@ bool IsConvertibleToU2fRegisterCommand(
       request.resident_key_required)
     return false;
 
-  return std::ranges::contains(
+  return base::Contains(
       request.public_key_credential_params.public_key_credential_params(),
       static_cast<int32_t>(CoseAlgorithmIdentifier::kEs256),
       &PublicKeyCredentialParams::CredentialInfo::algorithm);
diff --git a/device/fido/virtual_ctap2_device.cc b/device/fido/virtual_ctap2_device.cc
index 5c4bd66c82..b2f3cc9faf 100644
--- a/device/fido/virtual_ctap2_device.cc
+++ b/device/fido/virtual_ctap2_device.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
@@ -1158,9 +1159,9 @@ std::optional<CtapDeviceResponseCode> VirtualCtap2Device::OnMakeCredential(
   std::unique_ptr<PrivateKey> private_key;
   for (const auto& param :
        request.public_key_credential_params.public_key_credential_params()) {
-    const bool advertised = std::ranges::contains(
-        config_.advertised_algorithms, param.algorithm,
-        [](auto algo) { return static_cast<int32_t>(algo); });
+    const bool advertised =
+        base::Contains(config_.advertised_algorithms, param.algorithm,
+                       [](auto algo) { return static_cast<int32_t>(algo); });
     if (!advertised && !config_.advertised_algorithms.empty()) {
       continue;
     }
@@ -1383,8 +1384,8 @@ std::optional<CtapDeviceResponseCode> VirtualCtap2Device::OnMakeCredential(
       switch (request.attestation_preference) {
         case AttestationConveyancePreference::
             kEnterpriseIfRPListedOnAuthenticator:
-          if (std::ranges::contains(config_.enterprise_attestation_rps,
-                                    request.rp.id)) {
+          if (base::Contains(config_.enterprise_attestation_rps,
+                             request.rp.id)) {
             enterprise_attestation_requested = true;
           }
           break;
diff --git a/device/fido/virtual_u2f_device.cc b/device/fido/virtual_u2f_device.cc
index b5e60ae788..cb2d5b4150 100644
--- a/device/fido/virtual_u2f_device.cc
+++ b/device/fido/virtual_u2f_device.cc
@@ -9,6 +9,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/numerics/safe_conversions.h"
@@ -43,11 +44,11 @@ std::optional<std::vector<uint8_t>> ErrorStatus(
 
 // static
 bool VirtualU2fDevice::IsTransportSupported(FidoTransportProtocol transport) {
-  return (base::flat_set<FidoTransportProtocol>(
-              {FidoTransportProtocol::kUsbHumanInterfaceDevice,
-               FidoTransportProtocol::kBluetoothLowEnergy,
-               FidoTransportProtocol::kNearFieldCommunication}))
-      .contains(transport);
+  return base::Contains(base::flat_set<FidoTransportProtocol>(
+                            {FidoTransportProtocol::kUsbHumanInterfaceDevice,
+                             FidoTransportProtocol::kBluetoothLowEnergy,
+                             FidoTransportProtocol::kNearFieldCommunication}),
+                        transport);
 }
 
 VirtualU2fDevice::VirtualU2fDevice() = default;
diff --git a/device/fido/win/logging.cc b/device/fido/win/logging.cc
index 6ac204d9fb..e2e21b6619 100644
--- a/device/fido/win/logging.cc
+++ b/device/fido/win/logging.cc
@@ -4,11 +4,11 @@
 
 #include "device/fido/win/logging.h"
 
-#include <algorithm>
 #include <string>
 #include <string_view>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "components/device_event_log/device_event_log.h"
@@ -105,7 +105,7 @@ std::ostream& operator<<(std::ostream& out,
 std::ostream& operator<<(std::ostream& out, const WEBAUTHN_EXTENSION& in) {
   constexpr const wchar_t* kRedactedExtensions[] = {L"largeBlob", L"prf"};
   out << "{" << Quoted(in.pwszExtensionIdentifier) << kSep;
-  if (std::ranges::contains(kRedactedExtensions, in.pwszExtensionIdentifier)) {
+  if (base::Contains(kRedactedExtensions, in.pwszExtensionIdentifier)) {
     out << "[redacted]";
   } else {
     out << base::HexEncode(in.pvExtension, in.cbExtension);
diff --git a/device/gamepad/gamepad_blocklist.cc b/device/gamepad/gamepad_blocklist.cc
index 1998c7d6cb..a5a8c44955 100644
--- a/device/gamepad/gamepad_blocklist.cc
+++ b/device/gamepad/gamepad_blocklist.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 #include <iterator>
 
+#include "base/containers/contains.h"
 
 namespace device {
 namespace {
@@ -97,7 +98,7 @@ constexpr uint16_t kBlockedVendors[] = {
 }  // namespace
 
 bool GamepadIsExcluded(uint16_t vendor_id, uint16_t product_id) {
-  return std::ranges::contains(kBlockedVendors, vendor_id) ||
+  return base::Contains(kBlockedVendors, vendor_id) ||
          std::ranges::any_of(
              kBlockedDevices, [=](const VendorProductPair& item) {
                return vendor_id == item.vendor && product_id == item.product;
diff --git a/device/vr/android/arcore/arcore_device.cc b/device/vr/android/arcore/arcore_device.cc
index 01933eed8c..dbef3afbd8 100644
--- a/device/vr/android/arcore/arcore_device.cc
+++ b/device/vr/android/arcore/arcore_device.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/no_destructor.h"
 #include "base/task/bind_post_task.h"
@@ -150,10 +151,10 @@ void ArCoreDevice::RequestSession(
   session_state_->request_session_trace_id_ = options->trace_id;
 
   const bool use_dom_overlay =
-      std::ranges::contains(options->required_features,
-                            device::mojom::XRSessionFeature::DOM_OVERLAY) ||
-      std::ranges::contains(options->optional_features,
-                            device::mojom::XRSessionFeature::DOM_OVERLAY);
+      base::Contains(options->required_features,
+                     device::mojom::XRSessionFeature::DOM_OVERLAY) ||
+      base::Contains(options->optional_features,
+                     device::mojom::XRSessionFeature::DOM_OVERLAY);
 
   session_state_->depth_options_ = std::move(options->depth_options);
 
diff --git a/device/vr/android/arcore/arcore_impl.cc b/device/vr/android/arcore/arcore_impl.cc
index b09d9b9670..7667966bc9 100644
--- a/device/vr/android/arcore/arcore_impl.cc
+++ b/device/vr/android/arcore/arcore_impl.cc
@@ -9,6 +9,7 @@
 
 #include "base/android/jni_android.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
@@ -771,8 +772,8 @@ bool ArCoreImpl::ConfigureDepthSensing(
   // are allowed to return any supported depth usage.
   const auto& usage_preference = depth_sensing_config->depth_usage_preference;
   if (!usage_preference.empty() &&
-      !std::ranges::contains(usage_preference,
-                             device::mojom::XRDepthUsage::kCPUOptimized)) {
+      !base::Contains(usage_preference,
+                      device::mojom::XRDepthUsage::kCPUOptimized)) {
     return false;
   }
 
@@ -787,7 +788,7 @@ bool ArCoreImpl::ConfigureDepthSensing(
     const auto format_it = std::ranges::find_if(
         format_preference.begin(), format_preference.end(),
         [](const device::mojom::XRDepthDataFormat& format) {
-          return std::ranges::contains(kSupportedDepthFormats, format);
+          return base::Contains(kSupportedDepthFormats, format);
         });
 
     if (format_it != format_preference.end()) {
@@ -804,8 +805,8 @@ bool ArCoreImpl::ConfigureDepthSensing(
   // not a reason to reject the session, but only expose it as a part of the
   // configuration if it matches what the site has asked for.
   std::optional<mojom::XRDepthType> depth_type;
-  if (std::ranges::contains(depth_sensing_config->depth_type_request,
-                            mojom::XRDepthType::kSmooth)) {
+  if (base::Contains(depth_sensing_config->depth_type_request,
+                     mojom::XRDepthType::kSmooth)) {
     depth_type = mojom::XRDepthType::kSmooth;
   }
 
@@ -1444,8 +1445,8 @@ ArCoreImpl::GetMojoFromInputSources(
 
   for (const auto& input_source_state : input_state) {
     if (input_source_state && input_source_state->description) {
-      if (std::ranges::contains(input_source_state->description->profiles,
-                                profile_name)) {
+      if (base::Contains(input_source_state->description->profiles,
+                         profile_name)) {
         // Input source represented by input_state matches the profile, find
         // the transform and grab input source id.
         std::optional<gfx::Transform> maybe_mojo_from_input_source =
diff --git a/device/vr/openxr/android/openxr_depth_sensor_android.cc b/device/vr/openxr/android/openxr_depth_sensor_android.cc
index 5028e45770..8bd922d26d 100644
--- a/device/vr/openxr/android/openxr_depth_sensor_android.cc
+++ b/device/vr/openxr/android/openxr_depth_sensor_android.cc
@@ -4,12 +4,12 @@
 
 #include "device/vr/openxr/android/openxr_depth_sensor_android.h"
 
-#include <algorithm>
 #include <array>
 #include <concepts>
 #include <memory>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
@@ -299,8 +299,7 @@ OpenXrDepthSensorAndroid::OpenXrDepthSensorAndroid(
   const auto& usage_preferences = depth_options.usage_preferences;
   const bool can_support_depth =
       usage_preferences.empty() ||
-      std::ranges::contains(usage_preferences,
-                            mojom::XRDepthUsage::kCPUOptimized);
+      base::Contains(usage_preferences, mojom::XRDepthUsage::kCPUOptimized);
 
   if (can_support_depth) {
     depth_config_ = mojom::XRDepthConfig::New();
@@ -465,7 +464,7 @@ XrResult OpenXrDepthSensorAndroid::Initialize() {
       kResolutionPreferences.begin(), kResolutionPreferences.end(),
       [&supported_resolutions](
           const XrDepthCameraResolutionANDROID& resolution) {
-        return std::ranges::contains(supported_resolutions, resolution);
+        return base::Contains(supported_resolutions, resolution);
       });
 
   if (it == kResolutionPreferences.end()) {
diff --git a/device/vr/openxr/android/openxr_graphics_binding_open_gles.cc b/device/vr/openxr/android/openxr_graphics_binding_open_gles.cc
index 3b2d431899..9c72b50ab2 100644
--- a/device/vr/openxr/android/openxr_graphics_binding_open_gles.cc
+++ b/device/vr/openxr/android/openxr_graphics_binding_open_gles.cc
@@ -3,9 +3,9 @@
 // found in the LICENSE file.
 #include "device/vr/openxr/android/openxr_graphics_binding_open_gles.h"
 
-#include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "components/viz/common/resources/shared_image_format.h"
 #include "device/vr/android/xr_image_transport_base.h"
 #include "device/vr/openxr/openxr_api_wrapper.h"
@@ -192,7 +192,7 @@ int64_t OpenXrGraphicsBindingOpenGLES::GetSwapchainFormat(
   // still return it anyway as that will cause an error with creating the
   // swapchain, which is better than arbitrarily returning a type that the
   // runtime supports, but we don't.
-  if (!std::ranges::contains(swapchain_formats, kSwapchainFormat)) {
+  if (!base::Contains(swapchain_formats, kSwapchainFormat)) {
     LOG(ERROR) << "No matching supported swapchain formats with OpenXr Runtime";
   }
 
diff --git a/device/vr/openxr/openxr_api_wrapper.cc b/device/vr/openxr/openxr_api_wrapper.cc
index 3ef8c2418b..bf7b7ba5ee 100644
--- a/device/vr/openxr/openxr_api_wrapper.cc
+++ b/device/vr/openxr/openxr_api_wrapper.cc
@@ -12,6 +12,7 @@
 #include <type_traits>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
@@ -361,7 +362,7 @@ XrResult OpenXrApiWrapper::InitializeSystem() {
       view_config_types.data()));
 
   for (const auto& view_config_type : kSecondaryViewConfigurations) {
-    if (std::ranges::contains(view_config_types, view_config_type)) {
+    if (base::Contains(view_config_types, view_config_type)) {
       OpenXrViewConfiguration view_config;
       RETURN_IF_XR_FAILED(InitializeViewConfig(view_config_type, view_config));
       secondary_view_configs_.emplace(view_config_type, std::move(view_config));
@@ -399,19 +400,19 @@ OpenXrApiWrapper::PickEnvironmentBlendModeForSession(
 
   switch (session_mode) {
     case device::mojom::XRSessionMode::kImmersiveVr:
-      if (std::ranges::contains(supported_blend_modes,
-                                XR_ENVIRONMENT_BLEND_MODE_OPAQUE))
+      if (base::Contains(supported_blend_modes,
+                         XR_ENVIRONMENT_BLEND_MODE_OPAQUE))
         blend_mode_ = XR_ENVIRONMENT_BLEND_MODE_OPAQUE;
       break;
     case device::mojom::XRSessionMode::kImmersiveAr:
       // Prefer Alpha Blend when both Alpha Blend and Additive modes are
       // supported. This only concerns video see through devices with an
       // Additive compatibility mode
-      if (std::ranges::contains(supported_blend_modes,
-                                XR_ENVIRONMENT_BLEND_MODE_ALPHA_BLEND)) {
+      if (base::Contains(supported_blend_modes,
+                         XR_ENVIRONMENT_BLEND_MODE_ALPHA_BLEND)) {
         blend_mode_ = XR_ENVIRONMENT_BLEND_MODE_ALPHA_BLEND;
-      } else if (std::ranges::contains(supported_blend_modes,
-                                       XR_ENVIRONMENT_BLEND_MODE_ADDITIVE)) {
+      } else if (base::Contains(supported_blend_modes,
+                                XR_ENVIRONMENT_BLEND_MODE_ADDITIVE)) {
         blend_mode_ = XR_ENVIRONMENT_BLEND_MODE_ADDITIVE;
       }
       break;
@@ -488,7 +489,7 @@ XrResult OpenXrApiWrapper::EnableSupportedFeatures(
   for (const auto& feature : requested_features) {
     bool is_enabled = false;
     bool is_required =
-        std::ranges::contains(session_options_->required_features, feature);
+        base::Contains(session_options_->required_features, feature);
 
     switch (feature) {
       case mojom::XRSessionFeature::REF_SPACE_LOCAL_FLOOR:
@@ -677,10 +678,10 @@ XrResult OpenXrApiWrapper::InitSession(
   // session prior to any swap chain images being activated because the
   // associated shared images need to be created with WebGPU-specific flags.
   const bool webgpu_session =
-      std::ranges::contains(session_options_->required_features,
-                            device::mojom::XRSessionFeature::WEBGPU) ||
-      std::ranges::contains(session_options_->optional_features,
-                            device::mojom::XRSessionFeature::WEBGPU);
+      base::Contains(session_options_->required_features,
+                     device::mojom::XRSessionFeature::WEBGPU) ||
+      base::Contains(session_options_->optional_features,
+                     device::mojom::XRSessionFeature::WEBGPU);
   graphics_binding_->OnSessionCreated(local_space_, webgpu_session);
 
   // Now the primary layer should be available.
@@ -691,10 +692,10 @@ XrResult OpenXrApiWrapper::InitSession(
   RETURN_IF_XR_FAILED(CreateSwapchain());
 
   bool enable_hand_tracking =
-      std::ranges::contains(session_options_->required_features,
-                            device::mojom::XRSessionFeature::HAND_INPUT) ||
-      std::ranges::contains(session_options_->optional_features,
-                            device::mojom::XRSessionFeature::HAND_INPUT);
+      base::Contains(session_options_->required_features,
+                     device::mojom::XRSessionFeature::HAND_INPUT) ||
+      base::Contains(session_options_->optional_features,
+                     device::mojom::XRSessionFeature::HAND_INPUT);
 
   RETURN_IF_XR_FAILED(OpenXRInputHelper::CreateOpenXRInputHelper(
       instance_, system_properties.systemName, extension_helper, session_,
diff --git a/device/vr/openxr/openxr_device.cc b/device/vr/openxr/openxr_device.cc
index 4579c525af..07c864586d 100644
--- a/device/vr/openxr/openxr_device.cc
+++ b/device/vr/openxr/openxr_device.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/no_destructor.h"
 #include "build/build_config.h"
diff --git a/device/vr/openxr/openxr_hit_test_manager.cc b/device/vr/openxr/openxr_hit_test_manager.cc
index eae2f0cdb4..0014f67a3c 100644
--- a/device/vr/openxr/openxr_hit_test_manager.cc
+++ b/device/vr/openxr/openxr_hit_test_manager.cc
@@ -4,13 +4,13 @@
 
 #include "device/vr/openxr/openxr_hit_test_manager.h"
 
-#include <algorithm>
 #include <optional>
 #include <string>
 #include <tuple>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/trace_event/trace_event.h"
 #include "device/vr/openxr/openxr_util.h"
 #include "device/vr/public/mojom/hit_test_subscription_id.h"
@@ -241,8 +241,8 @@ OpenXrHitTestManager::GetMojoFromInputSources(
   }
   for (const auto& input_source_state : input_state.value()) {
     if (input_source_state && input_source_state->description) {
-      if (std::ranges::contains(input_source_state->description->profiles,
-                                profile_name)) {
+      if (base::Contains(input_source_state->description->profiles,
+                         profile_name)) {
         // Input source represented by input_state matches the profile, find
         // the transform and grab input source id.
         std::optional<gfx::Transform> maybe_mojo_from_input_source =
diff --git a/device/vr/openxr/openxr_path_helper.cc b/device/vr/openxr/openxr_path_helper.cc
index e8158ea6b9..60181d4b5a 100644
--- a/device/vr/openxr/openxr_path_helper.cc
+++ b/device/vr/openxr/openxr_path_helper.cc
@@ -4,9 +4,8 @@
 
 #include "device/vr/openxr/openxr_path_helper.h"
 
-#include <algorithm>
-
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "device/vr/openxr/openxr_interaction_profiles.h"
 #include "device/vr/openxr/openxr_util.h"
 #include "device/vr/public/mojom/openxr_interaction_profile_type.mojom.h"
@@ -61,8 +60,7 @@ std::vector<std::string> OpenXRPathHelper::GetInputProfiles(
   DCHECK(initialized_);
 
   bool can_use_hand_joint_profile =
-      hand_joints_enabled &&
-      std::ranges::contains(kHandProfiles, interaction_profile);
+      hand_joints_enabled && base::Contains(kHandProfiles, interaction_profile);
 
   const auto& input_profiles_map = GetOpenXrInputProfilesMap();
   if (input_profiles_map.contains(interaction_profile)) {
diff --git a/device/vr/openxr/openxr_platform_helper.cc b/device/vr/openxr/openxr_platform_helper.cc
index fa557d1c28..85d7023a52 100644
--- a/device/vr/openxr/openxr_platform_helper.cc
+++ b/device/vr/openxr/openxr_platform_helper.cc
@@ -3,12 +3,12 @@
 // found in the LICENSE file.
 #include "device/vr/openxr/openxr_platform_helper.h"
 
-#include <algorithm>
 #include <memory>
 #include <set>
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/strcat.h"
 #include "base/strings/string_util.h"
@@ -261,10 +261,10 @@ bool OpenXrPlatformHelper::IsArBlendModeSupported(XrInstance instance) {
   std::vector<XrEnvironmentBlendMode> environment_blend_modes =
       OpenXrApiWrapper::GetSupportedBlendModes(instance, system);
 
-  return std::ranges::contains(environment_blend_modes,
-                               XR_ENVIRONMENT_BLEND_MODE_ADDITIVE) ||
-         std::ranges::contains(environment_blend_modes,
-                               XR_ENVIRONMENT_BLEND_MODE_ALPHA_BLEND);
+  return base::Contains(environment_blend_modes,
+                        XR_ENVIRONMENT_BLEND_MODE_ADDITIVE) ||
+         base::Contains(environment_blend_modes,
+                        XR_ENVIRONMENT_BLEND_MODE_ALPHA_BLEND);
 }
 
 }  // namespace device
diff --git a/device/vr/openxr/openxr_render_loop.cc b/device/vr/openxr/openxr_render_loop.cc
index bf2f560171..1936871322 100644
--- a/device/vr/openxr/openxr_render_loop.cc
+++ b/device/vr/openxr/openxr_render_loop.cc
@@ -6,7 +6,6 @@
 
 #include <algorithm>
 #include <optional>
-#include <utility>
 
 #include "base/debug/dump_without_crashing.h"
 #include "base/functional/bind.h"
@@ -14,6 +13,7 @@
 #include "base/task/bind_post_task.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/trace_event/trace_event.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "components/viz/common/gpu/context_provider.h"
 #include "device/vr/openxr/exit_xr_present_reason.h"
@@ -81,10 +81,10 @@ OpenXrRenderLoop::~OpenXrRenderLoop() {
 }
 
 void OpenXrRenderLoop::ExitPresent(ExitXrPresentReason reason) {
-  DVLOG(1) << __func__ << " reason=" << std::to_underlying(reason);
+  DVLOG(1) << __func__ << " reason=" << base::to_underlying(reason);
   TRACE_EVENT_INSTANT1("xr", "OpenXrRenderLoop::ExitPresent",
                        TRACE_EVENT_SCOPE_THREAD, "reason",
-                       std::to_underlying(reason));
+                       base::to_underlying(reason));
   if (!is_presenting_) {
     return;
   }
diff --git a/device/vr/openxr/openxr_spatial_anchor_manager.cc b/device/vr/openxr/openxr_spatial_anchor_manager.cc
index 580efbed01..2042d9b64a 100644
--- a/device/vr/openxr/openxr_spatial_anchor_manager.cc
+++ b/device/vr/openxr/openxr_spatial_anchor_manager.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/types/expected.h"
 #include "device/vr/openxr/openxr_api_wrapper.h"
@@ -33,7 +34,7 @@ bool OpenXrSpatialAnchorManager::IsSupported(
   // XR_SPATIAL_COMPONENT_TYPE_ANCHOR_EXT, which is guaranteed to be supported
   // if the XR_SPATIAL_CAPABILITY_ANCHOR_EXT is supported, so that's all we need
   // to check.
-  return std::ranges::contains(capabilities, XR_SPATIAL_CAPABILITY_ANCHOR_EXT);
+  return base::Contains(capabilities, XR_SPATIAL_CAPABILITY_ANCHOR_EXT);
 }
 
 OpenXrSpatialAnchorManager::OpenXrSpatialAnchorManager(
diff --git a/device/vr/openxr/openxr_spatial_hit_test_manager.cc b/device/vr/openxr/openxr_spatial_hit_test_manager.cc
index 3623d53d1d..cc8886f274 100644
--- a/device/vr/openxr/openxr_spatial_hit_test_manager.cc
+++ b/device/vr/openxr/openxr_spatial_hit_test_manager.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "device/vr/openxr/openxr_spatial_capability_configuration_base.h"
 #include "device/vr/openxr/openxr_spatial_framework_manager.h"
 #include "device/vr/openxr/openxr_spatial_plane_manager.h"
@@ -32,8 +33,7 @@ bool SupportsPlaneBasedHitTest(
   // To check for plane based hit test, we'll need to check for the
   // XR_SPATIAL_COMPONENT_TYPE_RAYCAST_RESULT_ANDROID component on
   // XR_SPATIAL_CAPABILITY_PLANE_TRACKING_EXT, which is not guaranteed.
-  if (!std::ranges::contains(capabilities,
-                             XR_SPATIAL_CAPABILITY_PLANE_TRACKING_EXT)) {
+  if (!base::Contains(capabilities, XR_SPATIAL_CAPABILITY_PLANE_TRACKING_EXT)) {
     return false;
   }
 
@@ -41,9 +41,8 @@ bool SupportsPlaneBasedHitTest(
       GetSupportedComponentTypes(xrEnumerateSpatialCapabilityComponentTypesEXT,
                                  instance, system,
                                  XR_SPATIAL_CAPABILITY_PLANE_TRACKING_EXT);
-  return std::ranges::contains(
-      plane_tracking_components,
-      XR_SPATIAL_COMPONENT_TYPE_RAYCAST_RESULT_ANDROID);
+  return base::Contains(plane_tracking_components,
+                        XR_SPATIAL_COMPONENT_TYPE_RAYCAST_RESULT_ANDROID);
 }
 
 bool SupportsDepthBasedHitTest(
@@ -56,8 +55,8 @@ bool SupportsDepthBasedHitTest(
   // XR_SPATIAL_COMPONENT_TYPE_RAYCAST_RESULT_ANDROID, which is guaranteed to be
   // supported if the XR_SPATIAL_CAPABILITY_DEPTH_RAYCAST_ANDROID is supported,
   // so that's all we need to check for it.
-  return std::ranges::contains(capabilities,
-                               XR_SPATIAL_CAPABILITY_DEPTH_RAYCAST_ANDROID);
+  return base::Contains(capabilities,
+                        XR_SPATIAL_CAPABILITY_DEPTH_RAYCAST_ANDROID);
 }
 }  // namespace
 
diff --git a/device/vr/openxr/openxr_spatial_plane_manager.cc b/device/vr/openxr/openxr_spatial_plane_manager.cc
index c99acc980e..3e684d5066 100644
--- a/device/vr/openxr/openxr_spatial_plane_manager.cc
+++ b/device/vr/openxr/openxr_spatial_plane_manager.cc
@@ -4,8 +4,7 @@
 
 #include "device/vr/openxr/openxr_spatial_plane_manager.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "device/vr/openxr/openxr_api_wrapper.h"
 #include "device/vr/openxr/openxr_extension_helper.h"
 #include "device/vr/openxr/openxr_spatial_framework_manager.h"
@@ -85,8 +84,7 @@ bool OpenXrSpatialPlaneManager::IsSupported(
   // XR_SPATIAL_COMPONENT_TYPE_PLANE_ALIGNMENT_EXT which are guaranteed to be
   // supported if the XR_SPATIAL_CAPABILITY_PLANE_TRACKING_EXT is supported, so
   //  that's all we need to check.
-  return std::ranges::contains(capabilities,
-                               XR_SPATIAL_CAPABILITY_PLANE_TRACKING_EXT);
+  return base::Contains(capabilities, XR_SPATIAL_CAPABILITY_PLANE_TRACKING_EXT);
 }
 
 OpenXrSpatialPlaneManager::OpenXrSpatialPlaneManager(
@@ -109,15 +107,15 @@ OpenXrSpatialPlaneManager::OpenXrSpatialPlaneManager(
               .xrEnumerateSpatialCapabilityComponentTypesEXT,
           instance, system, XR_SPATIAL_CAPABILITY_PLANE_TRACKING_EXT);
 
-  polygon_enabled_ = std::ranges::contains(
-      plane_tracking_components, XR_SPATIAL_COMPONENT_TYPE_POLYGON_2D_EXT);
+  polygon_enabled_ = base::Contains(plane_tracking_components,
+                                    XR_SPATIAL_COMPONENT_TYPE_POLYGON_2D_EXT);
   if (polygon_enabled_) {
     enabled_components_.insert(XR_SPATIAL_COMPONENT_TYPE_POLYGON_2D_EXT);
   }
 
   semantic_label_enabled_ =
-      std::ranges::contains(plane_tracking_components,
-                            XR_SPATIAL_COMPONENT_TYPE_PLANE_SEMANTIC_LABEL_EXT);
+      base::Contains(plane_tracking_components,
+                     XR_SPATIAL_COMPONENT_TYPE_PLANE_SEMANTIC_LABEL_EXT);
   if (semantic_label_enabled_) {
     enabled_components_.insert(
         XR_SPATIAL_COMPONENT_TYPE_PLANE_SEMANTIC_LABEL_EXT);
@@ -132,7 +130,7 @@ OpenXrSpatialPlaneManager::OpenXrSpatialPlaneManager(
   auto first_attachable_component = std::find_if(
       attachable_components.begin(), attachable_components.end(),
       [&plane_tracking_components](XrSpatialComponentTypeEXT component) {
-        return std::ranges::contains(plane_tracking_components, component);
+        return base::Contains(plane_tracking_components, component);
       });
 
   if (first_attachable_component != attachable_components.end()) {
@@ -142,7 +140,7 @@ OpenXrSpatialPlaneManager::OpenXrSpatialPlaneManager(
     bool attachable_component_enabled = std::any_of(
         enabled_components_.begin(), enabled_components_.end(),
         [&attachable_components](XrSpatialComponentTypeEXT component) {
-          return std::ranges::contains(attachable_components, component);
+          return base::Contains(attachable_components, component);
         });
 
     // If not, let's enable the first attachable component that we found, since
diff --git a/device/vr/openxr/test/fake_openxr_impl_api.cc b/device/vr/openxr/test/fake_openxr_impl_api.cc
index 6c4078b67d..d968885648 100644
--- a/device/vr/openxr/test/fake_openxr_impl_api.cc
+++ b/device/vr/openxr/test/fake_openxr_impl_api.cc
@@ -2,8 +2,7 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "device/vr/openxr/openxr_util.h"
 #include "device/vr/openxr/test/openxr_negotiate.h"
@@ -219,7 +218,7 @@ XrResult xrCreateInstance(const XrInstanceCreateInfo* create_info,
   auto supported_extensions = OpenXrTestHelper::GetSupportedExtensions();
   bool all_valid = std::ranges::all_of(
       enabled_extensions, [&supported_extensions](std::string_view name) {
-        return std::ranges::contains(supported_extensions, name);
+        return base::Contains(supported_extensions, name);
       });
 
   RETURN_IF_FALSE(all_valid, XR_ERROR_VALIDATION_FAILURE,
diff --git a/device/vr/openxr/test/openxr_test_helper.cc b/device/vr/openxr/test/openxr_test_helper.cc
index 31a1c9cb63..9afc8acf71 100644
--- a/device/vr/openxr/test/openxr_test_helper.cc
+++ b/device/vr/openxr/test/openxr_test_helper.cc
@@ -4,10 +4,10 @@
 
 #include "device/vr/openxr/test/openxr_test_helper.h"
 
-#include <algorithm>
 #include <cmath>
 #include <limits>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/no_destructor.h"
 #include "device/vr/openxr/openxr_interaction_profile_paths.h"
@@ -637,7 +637,7 @@ XrResult OpenXrTestHelper::BeginSession(
     }
 
     // Check for duplicates.
-    if (std::ranges::contains(view_configs_enabled_, view_configs[i])) {
+    if (base::Contains(view_configs_enabled_, view_configs[i])) {
       return XR_ERROR_VALIDATION_FAILURE;
     }
 
diff --git a/device/vr/openxr/windows/openxr_graphics_binding_d3d11.cc b/device/vr/openxr/windows/openxr_graphics_binding_d3d11.cc
index e3e087d0b0..fc2fadd543 100644
--- a/device/vr/openxr/windows/openxr_graphics_binding_d3d11.cc
+++ b/device/vr/openxr/windows/openxr_graphics_binding_d3d11.cc
@@ -8,6 +8,7 @@
 #include <wrl.h>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/notreached.h"
diff --git a/device/vr/openxr/windows/openxr_platform_helper_windows.cc b/device/vr/openxr/windows/openxr_platform_helper_windows.cc
index 1c9f3b7706..f6c6dfb1c8 100644
--- a/device/vr/openxr/windows/openxr_platform_helper_windows.cc
+++ b/device/vr/openxr/windows/openxr_platform_helper_windows.cc
@@ -6,6 +6,7 @@
 #include <memory>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/weak_ptr.h"
 #include "base/win/scoped_handle.h"
 #include "base/win/windows_types.h"
diff --git a/extensions/browser/api/app_runtime/app_runtime_api.cc b/extensions/browser/api/app_runtime/app_runtime_api.cc
index 1bd3841bf8..b42113491d 100644
--- a/extensions/browser/api/app_runtime/app_runtime_api.cc
+++ b/extensions/browser/api/app_runtime/app_runtime_api.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "extensions/browser/entry_info.h"
 #include "extensions/browser/event_router.h"
 #include "extensions/browser/extension_prefs.h"
@@ -71,10 +72,10 @@ void DispatchOnLaunchedEventImpl(const ExtensionId& extension_id,
                                                   base::Time::Now());
 }
 
-#define ASSERT_ENUM_EQUAL(Name, Name2)                                    \
-  static_assert(std::to_underlying(extensions::AppLaunchSource::Name) ==  \
-                    std::to_underlying(app_runtime::LaunchSource::Name2), \
-                "The value of extensions::" #Name                         \
+#define ASSERT_ENUM_EQUAL(Name, Name2)                                     \
+  static_assert(base::to_underlying(extensions::AppLaunchSource::Name) ==  \
+                    base::to_underlying(app_runtime::LaunchSource::Name2), \
+                "The value of extensions::" #Name                          \
                 " and app_runtime::LAUNCH_" #Name2 " should be the same");
 
 app_runtime::LaunchSource GetLaunchSourceEnum(AppLaunchSource source) {
@@ -107,8 +108,8 @@ app_runtime::LaunchSource GetLaunchSourceEnum(AppLaunchSource source) {
   // kSourceReparenting not having a corresponding entry in
   // app_runtime::LaunchSource.
   static_assert(
-      std::to_underlying(extensions::AppLaunchSource::kMaxValue) ==
-          std::to_underlying(app_runtime::LaunchSource::kMaxValue) + 3,
+      base::to_underlying(extensions::AppLaunchSource::kMaxValue) ==
+          base::to_underlying(app_runtime::LaunchSource::kMaxValue) + 3,
       "");
 
   switch (source) {
@@ -152,8 +153,8 @@ app_runtime::LaunchSource GetLaunchSourceEnum(AppLaunchSource source) {
     case AppLaunchSource::kSourceAppHomePage:
     case AppLaunchSource::kSourceFocusMode:
     case AppLaunchSource::kSourceSparky:
-      return static_cast<app_runtime::LaunchSource>(std::to_underlying(source) -
-                                                    3);
+      return static_cast<app_runtime::LaunchSource>(
+          base::to_underlying(source) - 3);
   }
 }
 
diff --git a/extensions/browser/api/app_window/app_window_api.cc b/extensions/browser/api/app_window/app_window_api.cc
index cfdf681d07..94421e8058 100644
--- a/extensions/browser/api/app_window/app_window_api.cc
+++ b/extensions/browser/api/app_window/app_window_api.cc
@@ -4,7 +4,6 @@
 
 #include "extensions/browser/api/app_window/app_window_api.h"
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 
@@ -287,8 +286,7 @@ ExtensionFunction::ResponseAction AppWindowCreateFunction::Run() {
           "F16F23C83C5F6DAD9B65A120448B34056DD80691",
           "0F585FB1D0FDFBEBCE1FEB5E9DFFB6DA476B8C9B"};
       if (AppWindowClient::Get()->IsCurrentChannelOlderThanDev() &&
-          !std::ranges::contains(kAllowlist,
-                                 extension()->hashed_id().value())) {
+          !base::Contains(kAllowlist, extension()->hashed_id().value())) {
         return RespondNow(
             Error(app_window_constants::kAlphaEnabledWrongChannel));
       }
diff --git a/extensions/browser/api/audio/audio_service_chromeos.cc b/extensions/browser/api/audio/audio_service_chromeos.cc
index 649be7aaeb..39a0b20bf6 100644
--- a/extensions/browser/api/audio/audio_service_chromeos.cc
+++ b/extensions/browser/api/audio/audio_service_chromeos.cc
@@ -7,8 +7,7 @@
 #include <stddef.h>
 #include <stdint.h>
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
@@ -171,12 +170,12 @@ void AudioServiceImpl::GetDevices(
   ash::AudioDeviceList devices;
   cras_audio_handler_->GetAudioDevices(&devices);
 
-  bool accept_input = !(filter && filter->stream_types) ||
-                      std::ranges::contains(*filter->stream_types,
-                                            api::audio::StreamType::kInput);
-  bool accept_output = !(filter && filter->stream_types) ||
-                       std::ranges::contains(*filter->stream_types,
-                                             api::audio::StreamType::kOutput);
+  bool accept_input =
+      !(filter && filter->stream_types) ||
+      base::Contains(*filter->stream_types, api::audio::StreamType::kInput);
+  bool accept_output =
+      !(filter && filter->stream_types) ||
+      base::Contains(*filter->stream_types, api::audio::StreamType::kOutput);
 
   for (const auto& device : devices) {
     if (filter && filter->is_active && *filter->is_active != device.active)
diff --git a/extensions/browser/api/automation_internal/automation_event_router.cc b/extensions/browser/api/automation_internal/automation_event_router.cc
index 09d4f4acbc..8015323fa8 100644
--- a/extensions/browser/api/automation_internal/automation_event_router.cc
+++ b/extensions/browser/api/automation_internal/automation_event_router.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "base/values.h"
 #include "build/build_config.h"
diff --git a/extensions/browser/api/bluetooth/bluetooth_event_router.cc b/extensions/browser/api/bluetooth/bluetooth_event_router.cc
index c5a91cd343..f3fba9ca3f 100644
--- a/extensions/browser/api/bluetooth/bluetooth_event_router.cc
+++ b/extensions/browser/api/bluetooth/bluetooth_event_router.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/lazy_instance.h"
diff --git a/extensions/browser/api/bluetooth_low_energy/bluetooth_low_energy_api.cc b/extensions/browser/api/bluetooth_low_energy/bluetooth_low_energy_api.cc
index f3ae14b668..5d7875f2ad 100644
--- a/extensions/browser/api/bluetooth_low_energy/bluetooth_low_energy_api.cc
+++ b/extensions/browser/api/bluetooth_low_energy/bluetooth_low_energy_api.cc
@@ -18,6 +18,7 @@
 #include "base/lazy_instance.h"
 #include "base/logging.h"
 #include "base/memory/weak_ptr.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected_macros.h"
 #include "base/values.h"
 #include "build/build_config.h"
@@ -196,7 +197,7 @@ device::BluetoothGattCharacteristic::Properties GetBluetoothProperties(
       device::BluetoothGattCharacteristic::PROPERTY_NONE;
 
   static_assert(
-      std::to_underlying(apibtle::CharacteristicProperty::kMaxValue) == 14,
+      base::to_underlying(apibtle::CharacteristicProperty::kMaxValue) == 14,
       "Update required if the number of characteristic properties changes.");
 
   if (HasProperty(api_properties,
@@ -282,7 +283,7 @@ device::BluetoothGattCharacteristic::Permissions GetBluetoothPermissions(
       device::BluetoothGattCharacteristic::PERMISSION_NONE;
 
   static_assert(
-      std::to_underlying(apibtle::DescriptorPermission::kMaxValue) == 6,
+      base::to_underlying(apibtle::DescriptorPermission::kMaxValue) == 6,
       "Update required if the number of descriptor permissions changes.");
 
   if (HasPermission(api_permissions, apibtle::DescriptorPermission::kRead)) {
diff --git a/extensions/browser/api/declarative/deduping_factory.h b/extensions/browser/api/declarative/deduping_factory.h
index 5573b10d31..39df57f02e 100644
--- a/extensions/browser/api/declarative/deduping_factory.h
+++ b/extensions/browser/api/declarative/deduping_factory.h
@@ -12,6 +12,7 @@
 
 #include "base/check.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/memory/ref_counted.h"
diff --git a/extensions/browser/api/declarative/rules_registry.cc b/extensions/browser/api/declarative/rules_registry.cc
index 9659847a52..f900f579bd 100644
--- a/extensions/browser/api/declarative/rules_registry.cc
+++ b/extensions/browser/api/declarative/rules_registry.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_macros.h"
@@ -344,7 +345,7 @@ void RulesRegistry::MarkReady() {
 void RulesRegistry::ProcessChangedRules(const ExtensionId& extension_id) {
   DCHECK_CURRENTLY_ON(content::BrowserThread::UI);
 
-  DCHECK(process_changed_rules_requested_.contains(extension_id));
+  DCHECK(base::Contains(process_changed_rules_requested_, extension_id));
   process_changed_rules_requested_[extension_id] = NOT_SCHEDULED_FOR_PROCESSING;
 
   std::vector<const api::events::Rule*> new_rules;
diff --git a/extensions/browser/api/declarative/rules_registry_service.cc b/extensions/browser/api/declarative/rules_registry_service.cc
index 4a27341546..df5dc12fb9 100644
--- a/extensions/browser/api/declarative/rules_registry_service.cc
+++ b/extensions/browser/api/declarative/rules_registry_service.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/lazy_instance.h"
 #include "base/observer_list.h"
@@ -78,7 +79,7 @@ void RulesRegistryService::RegisterRulesRegistry(
     scoped_refptr<RulesRegistry> rule_registry) {
   const std::string event_name(rule_registry->event_name());
   RulesRegistryKey key(event_name, rule_registry->id());
-  DCHECK(!rule_registries_.contains(key));
+  DCHECK(!base::Contains(rule_registries_, key));
   rule_registries_[key] = rule_registry;
 }
 
@@ -103,7 +104,7 @@ scoped_refptr<RulesRegistry> RulesRegistryService::GetRulesRegistry(
 
   scoped_refptr<RulesRegistry> registry = RegisterWebRequestRulesRegistry(
       rules_registry_id, RulesCacheDelegate::Type::kEphemeral);
-  DCHECK(rule_registries_.contains(key));
+  DCHECK(base::Contains(rule_registries_, key));
   return registry;
 }
 
@@ -149,8 +150,8 @@ void RulesRegistryService::SimulateExtensionUninstalled(
 bool RulesRegistryService::HasRulesRegistryForTesting(
     int rules_registry_id,
     const std::string& event_name) {
-  return rule_registries_.contains(
-      RulesRegistryKey(event_name, rules_registry_id));
+  return base::Contains(rule_registries_,
+                        RulesRegistryKey(event_name, rules_registry_id));
 }
 
 void RulesRegistryService::OnUpdateRules() {
@@ -164,8 +165,10 @@ RulesRegistryService::RegisterWebRequestRulesRegistry(
     int rules_registry_id,
     RulesCacheDelegate::Type cache_delegate_type) {
   DCHECK(browser_context_);
-  DCHECK(!rule_registries_.contains(RulesRegistryKey(
-      declarative_webrequest_constants::kOnRequest, rules_registry_id)));
+  DCHECK(!base::Contains(
+      rule_registries_,
+      RulesRegistryKey(declarative_webrequest_constants::kOnRequest,
+                       rules_registry_id)));
 
   auto web_request_cache_delegate =
       std::make_unique<RulesCacheDelegate>(cache_delegate_type);
@@ -184,7 +187,8 @@ RulesRegistryService::RegisterWebRequestRulesRegistry(
 
 void RulesRegistryService::EnsureDefaultRulesRegistriesRegistered() {
   DCHECK(browser_context_);
-  DCHECK(!rule_registries_.contains(
+  DCHECK(!base::Contains(
+      rule_registries_,
       RulesRegistryKey(declarative_webrequest_constants::kOnRequest,
                        rules_registry_ids::kDefaultRulesRegistryID)));
 
diff --git a/extensions/browser/api/declarative_net_request/composite_matcher.cc b/extensions/browser/api/declarative_net_request/composite_matcher.cc
index fd041f79dc..7349dcd041 100644
--- a/extensions/browser/api/declarative_net_request/composite_matcher.cc
+++ b/extensions/browser/api/declarative_net_request/composite_matcher.cc
@@ -13,6 +13,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/time/time.h"
 #include "base/timer/elapsed_timer.h"
@@ -123,7 +124,7 @@ void CompositeMatcher::AddOrUpdateRulesets(MatcherList matchers) {
 void CompositeMatcher::RemoveRulesetsWithIDs(const std::set<RulesetID>& ids) {
   size_t erased_count = std::erase_if(
       matchers_, [&ids](const std::unique_ptr<RulesetMatcher>& matcher) {
-        return ids.contains(matcher->id());
+        return base::Contains(ids, matcher->id());
       });
 
   if (erased_count > 0) {
@@ -220,7 +221,7 @@ std::vector<RequestAction> CompositeMatcher::GetModifyHeadersActions(
   // for a rule, specified in `kMinValidPriority`, is 1.)
   uint64_t max_allow_rule_priority = 0u;
 
-  DCHECK(params.max_priority_allow_action.contains(extension_id_));
+  DCHECK(base::Contains(params.max_priority_allow_action, extension_id_));
   if (auto& allow_action = params.max_priority_allow_action.at(extension_id_)) {
     max_allow_rule_priority = allow_action->index_priority;
   }
diff --git a/extensions/browser/api/declarative_net_request/declarative_net_request_api.cc b/extensions/browser/api/declarative_net_request/declarative_net_request_api.cc
index be15017c32..d4d1b89214 100644
--- a/extensions/browser/api/declarative_net_request/declarative_net_request_api.cc
+++ b/extensions/browser/api/declarative_net_request/declarative_net_request_api.cc
@@ -12,6 +12,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/time/time.h"
@@ -349,7 +350,7 @@ DeclarativeNetRequestUpdateEnabledRulesetsFunction::Run() {
 
       // |ruleset_ids_to_enable| takes priority over |ruleset_ids_to_disable|.
       RulesetID id = it->second->id;
-      if (ids_to_enable.contains(id)) {
+      if (base::Contains(ids_to_enable, id)) {
         continue;
       }
 
diff --git a/extensions/browser/api/declarative_net_request/file_sequence_helper.cc b/extensions/browser/api/declarative_net_request/file_sequence_helper.cc
index 31b8a9a4a1..9297559de0 100644
--- a/extensions/browser/api/declarative_net_request/file_sequence_helper.cc
+++ b/extensions/browser/api/declarative_net_request/file_sequence_helper.cc
@@ -12,6 +12,7 @@
 
 #include "base/barrier_closure.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/files/important_file_writer.h"
 #include "base/functional/bind.h"
@@ -210,7 +211,7 @@ bool GetNewDynamicRules(const FileBackedRulesetSource& source,
   // Remove old rules
   std::set<int> ids_to_remove(rule_ids_to_remove.begin(), rule_ids_to_remove.end());
   std::erase_if(*new_rules, [&ids_to_remove](const dnr_api::Rule& rule) {
-    return ids_to_remove.contains(rule.id);
+    return base::Contains(ids_to_remove, rule.id);
   });
 
   // Add new rules
@@ -299,7 +300,7 @@ bool UpdateAndIndexDynamicRules(const FileBackedRulesetSource& source,
   // Treat rules which exceed the regex memory limit as errors if these are new
   // rules. Just surface an error for the first such rule.
   for (const auto& warning : info.rule_ignored_warnings()) {
-    if (!rule_ids_to_add.contains(warning.rule_id)) {
+    if (!base::Contains(rule_ids_to_add, warning.rule_id)) {
       // Any rule added earlier which is ignored now (say due to exceeding the
       // regex memory limit), will be silently ignored.
       // TODO(crbug.com/40118204): Notify the extension about the same.
diff --git a/extensions/browser/api/declarative_net_request/indexed_rule.cc b/extensions/browser/api/declarative_net_request/indexed_rule.cc
index d301758bfe..f1af4ff1ee 100644
--- a/extensions/browser/api/declarative_net_request/indexed_rule.cc
+++ b/extensions/browser/api/declarative_net_request/indexed_rule.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/notreached.h"
@@ -531,8 +532,8 @@ ParseResult ValidateHeadersForModification(
         header_info.operation == dnr_api::HeaderOperation::kAppend) {
       // Check against the allowlist in lowercase since header names will be
       // stored in lowercase in flatbuffers later on.
-      if (!kDNRRequestHeaderAppendAllowList.contains(
-              base::ToLowerASCII(header_info.header))) {
+      if (!base::Contains(kDNRRequestHeaderAppendAllowList,
+                          base::ToLowerASCII(header_info.header))) {
         return ParseResult::ERROR_APPEND_INVALID_REQUEST_HEADER;
       }
     }
@@ -630,7 +631,7 @@ ParseResult ValidateResponseHeadersForMatching(
     // non-existence of a header.
     if (!header_info.values.has_value() &&
         !header_info.excluded_values.has_value() &&
-        response_header_names.contains(header_info.header)) {
+        base::Contains(response_header_names, header_info.header)) {
       return ParseResult::ERROR_MATCHING_RESPONSE_HEADER_DUPLICATED;
     }
   }
@@ -841,7 +842,8 @@ ParseResult IndexedRule::CreateIndexedRule(dnr_api::Rule parsed_rule,
                 indexed_rule->excluded_tab_ids);
     if (std::ranges::any_of(
             indexed_rule->tab_ids, [indexed_rule](int included_tab_id) {
-              return indexed_rule->excluded_tab_ids.contains(included_tab_id);
+              return base::Contains(indexed_rule->excluded_tab_ids,
+                                    included_tab_id);
             })) {
       return ParseResult::ERROR_TAB_ID_DUPLICATED;
     }
diff --git a/extensions/browser/api/declarative_net_request/prefs_helper.cc b/extensions/browser/api/declarative_net_request/prefs_helper.cc
index 8e78c1f805..096ce93267 100644
--- a/extensions/browser/api/declarative_net_request/prefs_helper.cc
+++ b/extensions/browser/api/declarative_net_request/prefs_helper.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "extensions/browser/api/declarative_net_request/utils.h"
@@ -134,7 +135,7 @@ PrefsHelper::RuleIdsToUpdate::RuleIdsToUpdate(
   if (ids_to_enable) {
     for (int id : *ids_to_enable) {
       // |ids_to_disable| takes priority over |ids_to_enable|.
-      if (this->ids_to_disable.contains(id)) {
+      if (base::Contains(this->ids_to_disable, id)) {
         continue;
       }
       this->ids_to_enable.insert(id);
@@ -219,7 +220,7 @@ PrefsHelper::UpdateDisabledStaticRules(
       GetDisabledStaticRuleIdsFromDict(disabled_rule_ids_dict, ruleset_id));
 
   for (int id : old_disabled_rule_ids) {
-    if (rule_ids_to_update.ids_to_enable.contains(id)) {
+    if (base::Contains(rule_ids_to_update.ids_to_enable, id)) {
       result.changed = true;
       continue;
     }
diff --git a/extensions/browser/api/declarative_net_request/regex_rules_matcher.cc b/extensions/browser/api/declarative_net_request/regex_rules_matcher.cc
index 26d363450d..644bc90473 100644
--- a/extensions/browser/api/declarative_net_request/regex_rules_matcher.cc
+++ b/extensions/browser/api/declarative_net_request/regex_rules_matcher.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/notreached.h"
 #include "base/strings/string_util.h"
@@ -31,8 +32,8 @@ bool IsExtraHeadersMatcherInternal(
                 "Modify this method to ensure IsExtraHeadersMatcherInternal is "
                 "updated as new actions are added.");
 
-  return std::ranges::contains(*regex_list, flat::ActionType_modify_headers,
-                               &flat::RegexRule::action_type);
+  return base::Contains(*regex_list, flat::ActionType_modify_headers,
+                        &flat::RegexRule::action_type);
 }
 
 // Helper to check if the `rule` metadata matches the given request `params`.
diff --git a/extensions/browser/api/declarative_net_request/rules_monitor_service.cc b/extensions/browser/api/declarative_net_request/rules_monitor_service.cc
index 00cae196ee..2dac002b3b 100644
--- a/extensions/browser/api/declarative_net_request/rules_monitor_service.cc
+++ b/extensions/browser/api/declarative_net_request/rules_monitor_service.cc
@@ -10,6 +10,7 @@
 
 #include "base/auto_reset.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/queue.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -586,7 +587,7 @@ void RulesMonitorService::OnExtensionLoaded(
     bool ruleset_failed_to_load = false;
     for (auto& source : sources) {
       bool enabled = prefs_enabled_rulesets
-                         ? prefs_enabled_rulesets->contains(source.id())
+                         ? base::Contains(*prefs_enabled_rulesets, source.id())
                          : source.enabled_by_default();
 
       bool ignored = helper.ShouldIgnoreRuleset(extension->id(), source.id());
@@ -773,7 +774,7 @@ void RulesMonitorService::UpdateSessionRulesInternal(
   std::set<int> ids_to_remove(rule_ids_to_remove.begin(),
                               rule_ids_to_remove.end());
   std::erase_if(new_rules, [&ids_to_remove](const dnr_api::Rule& rule) {
-    return ids_to_remove.contains(rule.id);
+    return base::Contains(ids_to_remove, rule.id);
   });
 
   new_rules.insert(new_rules.end(),
@@ -1095,13 +1096,13 @@ void RulesMonitorService::OnNewStaticRulesetsLoaded(
       }
 
       // Exclude since we'll be removing this |matcher|.
-      if (ids_to_disable.contains(ruleset_matcher->id())) {
+      if (base::Contains(ids_to_disable, ruleset_matcher->id())) {
         continue;
       }
 
       // Exclude to prevent double counting. This will be a part of
       // |new_matchers| below.
-      if (ids_to_enable.contains(ruleset_matcher->id())) {
+      if (base::Contains(ids_to_enable, ruleset_matcher->id())) {
         continue;
       }
 
diff --git a/extensions/browser/api/declarative_net_request/ruleset_manager.cc b/extensions/browser/api/declarative_net_request/ruleset_manager.cc
index 09c4afa17c..1546f3d1a4 100644
--- a/extensions/browser/api/declarative_net_request/ruleset_manager.cc
+++ b/extensions/browser/api/declarative_net_request/ruleset_manager.cc
@@ -11,6 +11,7 @@
 
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -230,8 +231,8 @@ bool RulesetManager::HasExtraHeadersMatcherForRequest(
   // request would potentially match any onHeadersReceived rules based on
   // non-header parameters.
   return HasRulesets(RulesetMatchingStage::kOnHeadersReceived) ||
-         std::ranges::contains(actions, RequestAction::Type::MODIFY_HEADERS,
-                               &RequestAction::type);
+         base::Contains(actions, RequestAction::Type::MODIFY_HEADERS,
+                        &RequestAction::type);
 }
 
 void RulesetManager::OnRenderFrameCreated(content::RenderFrameHost* host) {
diff --git a/extensions/browser/api/declarative_net_request/test_utils.cc b/extensions/browser/api/declarative_net_request/test_utils.cc
index f3b73bc72a..dffa1029a9 100644
--- a/extensions/browser/api/declarative_net_request/test_utils.cc
+++ b/extensions/browser/api/declarative_net_request/test_utils.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/json/json_file_value_serializer.h"
@@ -571,7 +572,7 @@ void WarningServiceObserver::WaitForWarning() {
 
 void WarningServiceObserver::ExtensionWarningsChanged(
     const ExtensionIdSet& affected_extensions) {
-  if (!affected_extensions.contains(extension_id_)) {
+  if (!base::Contains(affected_extensions, extension_id_)) {
     return;
   }
 
diff --git a/extensions/browser/api/declarative_webrequest/webrequest_condition.cc b/extensions/browser/api/declarative_webrequest/webrequest_condition.cc
index c5a0d7e961..9afe378419 100644
--- a/extensions/browser/api/declarative_webrequest/webrequest_condition.cc
+++ b/extensions/browser/api/declarative_webrequest/webrequest_condition.cc
@@ -4,6 +4,7 @@
 
 #include "extensions/browser/api/declarative_webrequest/webrequest_condition.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/stringprintf.h"
 #include "base/values.h"
@@ -94,7 +95,8 @@ bool WebRequestCondition::IsFulfilled(
 
   // Check URL attributes if present.
   if (url_matcher_conditions_.get() &&
-      !request_data.url_match_ids.contains(url_matcher_conditions_->id()))
+      !base::Contains(request_data.url_match_ids,
+                      url_matcher_conditions_->id()))
     return false;
 
   // All condition attributes must be fulfilled for a fulfilled condition.
diff --git a/extensions/browser/api/declarative_webrequest/webrequest_condition_attribute.cc b/extensions/browser/api/declarative_webrequest/webrequest_condition_attribute.cc
index cec9afc9e5..a3f970502b 100644
--- a/extensions/browser/api/declarative_webrequest/webrequest_condition_attribute.cc
+++ b/extensions/browser/api/declarative_webrequest/webrequest_condition_attribute.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/no_destructor.h"
 #include "base/notreached.h"
@@ -167,7 +168,7 @@ bool WebRequestConditionAttributeResourceType::IsFulfilled(
     const WebRequestData& request_data) const {
   if (!(request_data.stage & GetStages()))
     return false;
-  return std::ranges::contains(types_, request_data.request->web_request_type);
+  return base::Contains(types_, request_data.request->web_request_type);
 }
 
 WebRequestConditionAttribute::Type
@@ -249,9 +250,9 @@ bool WebRequestConditionAttributeContentType::IsFulfilled(
                                   &had_charset, nullptr);
 
   if (inclusive_) {
-    return std::ranges::contains(content_types_, mime_type);
+    return base::Contains(content_types_, mime_type);
   } else {
-    return !std::ranges::contains(content_types_, mime_type);
+    return !base::Contains(content_types_, mime_type);
   }
 }
 
@@ -425,7 +426,7 @@ bool HeaderMatcher::StringMatchTest::Matches(
                                    CaseInsensitiveCompareASCII<char>())
                    .begin() != str.end();
       } else {
-        return str.contains(data_);
+        return base::Contains(str, data_);
       }
   }
   // We never get past the "switch", but the compiler worries about no return.
diff --git a/extensions/browser/api/declarative_webrequest/webrequest_rules_registry.cc b/extensions/browser/api/declarative_webrequest/webrequest_rules_registry.cc
index 082c8e4f51..53689183fd 100644
--- a/extensions/browser/api/declarative_webrequest/webrequest_rules_registry.cc
+++ b/extensions/browser/api/declarative_webrequest/webrequest_rules_registry.cc
@@ -9,6 +9,7 @@
 #include <limits>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "content/public/browser/browser_thread.h"
 #include "extensions/browser/api/declarative_webrequest/webrequest_condition.h"
@@ -123,7 +124,7 @@ WebRequestRulesRegistry::CreateDeltas(PermissionHelper* permission_helper,
     if (!rule->tags().empty() && !ignore_tags[extension_id].empty()) {
       bool ignore_rule = false;
       for (const std::string& tag : rule->tags())
-        ignore_rule |= ignore_tags[extension_id].contains(tag);
+        ignore_rule |= base::Contains(ignore_tags[extension_id], tag);
       if (ignore_rule)
         continue;
     }
@@ -365,7 +366,7 @@ void WebRequestRulesRegistry::AddTriggeredRules(
   for (const auto& url_match : url_matches) {
     auto rule_trigger = rule_triggers_.find(url_match);
     CHECK(rule_trigger != rule_triggers_.end());
-    if (!result->contains(rule_trigger->second) &&
+    if (!base::Contains(*result, rule_trigger->second) &&
         rule_trigger->second->conditions().IsFulfilled(url_match, request_data))
       result->insert(rule_trigger->second);
   }
diff --git a/extensions/browser/api/device_permissions_manager.cc b/extensions/browser/api/device_permissions_manager.cc
index 9f2624f8e5..a5e31e8a1c 100644
--- a/extensions/browser/api/device_permissions_manager.cc
+++ b/extensions/browser/api/device_permissions_manager.cc
@@ -9,6 +9,7 @@
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/memory/singleton.h"
@@ -522,8 +523,8 @@ void DevicePermissionsManager::AllowUsbDevice(
 
     device_permissions->entries_.insert(device_entry);
     SaveDevicePermissionEntry(context_, extension_id, device_entry);
-  } else if (!device_permissions->ephemeral_usb_devices_.contains(
-                 device_info.guid)) {
+  } else if (!base::Contains(device_permissions->ephemeral_usb_devices_,
+                             device_info.guid)) {
     // Non-persistent devices cannot be reliably identified when they are
     // reconnected so such devices are only remembered until disconnect.
     // Register an observer here so that this set doesn't grow undefinitely.
@@ -559,8 +560,8 @@ void DevicePermissionsManager::AllowHidDevice(
 
     device_permissions->entries_.insert(device_entry);
     SaveDevicePermissionEntry(context_, extension_id, device_entry);
-  } else if (!device_permissions->ephemeral_hid_devices_.contains(
-                 device.guid)) {
+  } else if (!base::Contains(device_permissions->ephemeral_hid_devices_,
+                             device.guid)) {
     // Non-persistent devices cannot be reliably identified when they are
     // reconnected so such devices are only remembered until disconnect.
     // Register an observer here so that this set doesn't grow undefinitely.
@@ -592,7 +593,7 @@ void DevicePermissionsManager::RemoveEntry(
   DCHECK(thread_checker_.CalledOnValidThread());
   DevicePermissions* device_permissions = GetInternal(extension_id);
   DCHECK(device_permissions);
-  DCHECK(device_permissions->entries_.contains(entry));
+  DCHECK(base::Contains(device_permissions->entries_, entry));
   device_permissions->entries_.erase(entry);
   if (entry->IsPersistent()) {
     RemoveDevicePermissionEntry(context_, extension_id, entry);
diff --git a/extensions/browser/api/feedback_private/log_source_access_manager.cc b/extensions/browser/api/feedback_private/log_source_access_manager.cc
index 4edf7b59e3..548237e084 100644
--- a/extensions/browser/api/feedback_private/log_source_access_manager.cc
+++ b/extensions/browser/api/feedback_private/log_source_access_manager.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/string_split.h"
 #include "base/strings/stringprintf.h"
@@ -236,7 +237,7 @@ LogSourceAccessManager::ResourceId LogSourceAccessManager::CreateResource(
   if (resource_id == kInvalidResourceId)
     return kInvalidResourceId;
 
-  if (open_handles_.contains(resource_id)) {
+  if (base::Contains(open_handles_, resource_id)) {
     return kInvalidResourceId;
   }
 
diff --git a/extensions/browser/api/file_handlers/app_file_handler_util.cc b/extensions/browser/api/file_handlers/app_file_handler_util.cc
index f644757af6..64ecc55152 100644
--- a/extensions/browser/api/file_handlers/app_file_handler_util.cc
+++ b/extensions/browser/api/file_handlers/app_file_handler_util.cc
@@ -7,6 +7,7 @@
 #include <set>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/files/file.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -195,7 +196,7 @@ WritableFileChecker::WritableFileChecker(
 void WritableFileChecker::Check() {
   outstanding_tasks_ = paths_.size();
   for (const auto& path : paths_) {
-    bool is_directory = directory_paths_.contains(path);
+    bool is_directory = base::Contains(directory_paths_, path);
 #if BUILDFLAG(IS_CHROMEOS)
     NonNativeFileSystemDelegate* delegate =
         ExtensionsAPIClient::Get()->GetNonNativeFileSystemDelegate();
@@ -542,7 +543,7 @@ std::vector<extensions::EntryInfo> CreateEntryInfos(
   for (size_t i = 0; i < entry_paths.size(); ++i) {
     const std::string mime_type =
         mime_types[i].empty() ? kFallbackMimeType : mime_types[i];
-    bool is_directory = directory_paths.contains(entry_paths[i]);
+    bool is_directory = base::Contains(directory_paths, entry_paths[i]);
     entry_infos.emplace_back(entry_paths[i], mime_type, is_directory);
   }
   return entry_infos;
diff --git a/extensions/browser/api/file_system/file_system_api.cc b/extensions/browser/api/file_system/file_system_api.cc
index 1cfe10e9dd..9f102fe95b 100644
--- a/extensions/browser/api/file_system/file_system_api.cc
+++ b/extensions/browser/api/file_system/file_system_api.cc
@@ -6,7 +6,6 @@
 
 #include <stddef.h>
 
-#include <algorithm>
 #include <array>
 #include <memory>
 #include <set>
@@ -15,6 +14,7 @@
 #include <vector>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -649,8 +649,7 @@ void FileSystemChooseEntryFunction::BuildFileTypeInfo(
 
       // If we still need to find suggested_extension, hunt for it inside the
       // extensions returned from GetFileTypesFromAcceptOption.
-      if (need_suggestion &&
-          std::ranges::contains(extensions, suggested_extension)) {
+      if (need_suggestion && base::Contains(extensions, suggested_extension)) {
         need_suggestion = false;
       }
     }
diff --git a/extensions/browser/api/hid/hid_device_manager.cc b/extensions/browser/api/hid/hid_device_manager.cc
index c0daba7685..d3470374b4 100644
--- a/extensions/browser/api/hid/hid_device_manager.cc
+++ b/extensions/browser/api/hid/hid_device_manager.cc
@@ -15,6 +15,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/lazy_instance.h"
 #include "base/location.h"
@@ -278,7 +279,7 @@ void HidDeviceManager::DeviceAdded(device::mojom::HidDeviceInfoPtr device) {
   DCHECK(thread_checker_.CalledOnValidThread());
   DCHECK_LT(next_resource_id_, std::numeric_limits<int>::max());
   int new_id = next_resource_id_++;
-  DCHECK(!resource_ids_.contains(device->guid));
+  DCHECK(!base::Contains(resource_ids_, device->guid));
   resource_ids_[device->guid] = new_id;
   devices_[new_id] = std::move(device);
 
@@ -328,7 +329,7 @@ void HidDeviceManager::DeviceChanged(device::mojom::HidDeviceInfoPtr device) {
   const auto& resource_entry = resource_ids_.find(device->guid);
   CHECK(resource_entry != resource_ids_.end());
   int resource_id = resource_entry->second;
-  DCHECK(devices_.contains(resource_id));
+  DCHECK(base::Contains(devices_, resource_id));
 
   // Update the device information.
   devices_[resource_id] = std::move(device);
diff --git a/extensions/browser/api/management/management_api.cc b/extensions/browser/api/management/management_api.cc
index c110c1bd9e..079321f004 100644
--- a/extensions/browser/api/management/management_api.cc
+++ b/extensions/browser/api/management/management_api.cc
@@ -4,12 +4,12 @@
 
 #include "extensions/browser/api/management/management_api.h"
 
-#include <algorithm>
 #include <memory>
 #include <string>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/json/json_writer.h"
 #include "base/lazy_instance.h"
@@ -1000,7 +1000,7 @@ ExtensionFunction::ResponseAction ManagementSetLaunchTypeFunction::Run() {
       GetAvailableLaunchTypes(*extension);
 
   management::LaunchType app_launch_type = params->launch_type;
-  if (!std::ranges::contains(available_launch_types, app_launch_type)) {
+  if (!base::Contains(available_launch_types, app_launch_type)) {
     return RespondNow(Error(keys::kLaunchTypeNotAvailableError));
   }
 
diff --git a/extensions/browser/api/messaging/extension_message_port.cc b/extensions/browser/api/messaging/extension_message_port.cc
index 6d23ba8929..ce2c66c6a3 100644
--- a/extensions/browser/api/messaging/extension_message_port.cc
+++ b/extensions/browser/api/messaging/extension_message_port.cc
@@ -9,6 +9,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/notreached.h"
@@ -388,7 +389,8 @@ void ExtensionMessagePort::RemoveCommonFrames(const MessagePort& port) {
 
 bool ExtensionMessagePort::HasFrame(
     const content::GlobalRenderFrameHostToken& frame_token) const {
-  return frames_.contains(frame_token) || pending_frames_.contains(frame_token);
+  return base::Contains(frames_, frame_token) ||
+         base::Contains(pending_frames_, frame_token);
 }
 
 bool ExtensionMessagePort::IsValidPort() {
diff --git a/extensions/browser/api/messaging/message_service.cc b/extensions/browser/api/messaging/message_service.cc
index 355e183fed..6408458b13 100644
--- a/extensions/browser/api/messaging/message_service.cc
+++ b/extensions/browser/api/messaging/message_service.cc
@@ -12,6 +12,7 @@
 #include <utility>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -1050,7 +1051,7 @@ void MessageService::AddChannel(std::unique_ptr<MessageChannel> channel,
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
 
   ChannelId channel_id = receiver_port_id.GetChannelId();
-  CHECK(!channels_.contains(channel_id));
+  CHECK(!base::Contains(channels_, channel_id));
   channels_[channel_id] = std::move(channel);
   pending_lazy_context_channels_.erase(channel_id);
 }
@@ -1175,7 +1176,7 @@ void MessageService::EnqueuePendingMessage(const PortId& source_port_id,
         PendingMessage(source_port_id, message));
     // A channel should only be holding pending messages because it is in one
     // of these states.
-    DCHECK(!pending_lazy_context_channels_.contains(channel_id));
+    DCHECK(!base::Contains(pending_lazy_context_channels_, channel_id));
     return;
   }
   EnqueuePendingMessageForLazyBackgroundLoad(source_port_id, channel_id,
diff --git a/extensions/browser/api/networking_private/networking_private_chromeos.cc b/extensions/browser/api/networking_private/networking_private_chromeos.cc
index 5e125d0fdb..4d60a2f81b 100644
--- a/extensions/browser/api/networking_private/networking_private_chromeos.cc
+++ b/extensions/browser/api/networking_private/networking_private_chromeos.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/logging.h"
@@ -718,7 +719,7 @@ void NetworkingPrivateChromeOS::GetDeviceStateList(
                                     ::onc::network_type::kWiFi,
                                     ::onc::network_type::kCellular};
   for (const char* technology : technology_types) {
-    if (technologies_found.contains(technology)) {
+    if (base::Contains(technologies_found, technology)) {
       continue;
     }
     AppendDeviceState(technology, nullptr /* device */, device_state_list);
diff --git a/extensions/browser/api/offscreen/lifetime_enforcer_factories.cc b/extensions/browser/api/offscreen/lifetime_enforcer_factories.cc
index 3c3a7acca6..2ff68502d9 100644
--- a/extensions/browser/api/offscreen/lifetime_enforcer_factories.cc
+++ b/extensions/browser/api/offscreen/lifetime_enforcer_factories.cc
@@ -4,11 +4,10 @@
 
 #include "extensions/browser/api/offscreen/lifetime_enforcer_factories.h"
 
-#include <utility>
-
 #include "base/check.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "extensions/browser/api/offscreen/audio_lifetime_enforcer.h"
 #include "extensions/browser/api/offscreen/offscreen_document_lifetime_enforcer.h"
 #include "extensions/common/api/offscreen.h"
@@ -99,7 +98,7 @@ constexpr ReasonAndFactoryMethodPair kReasonAndFactoryMethodPairs[] = {
     {api::offscreen::Reason::kGeolocation, &CreateEmptyEnforcer}};
 
 static_assert(std::size(kReasonAndFactoryMethodPairs) ==
-                  std::to_underlying(api::offscreen::Reason::kMaxValue),
+                  base::to_underlying(api::offscreen::Reason::kMaxValue),
               "Factory method size does not equal reason size.");
 
 }  // namespace
diff --git a/extensions/browser/api/offscreen/offscreen_api.cc b/extensions/browser/api/offscreen/offscreen_api.cc
index 35f6eccc3f..5bc6e8d3cc 100644
--- a/extensions/browser/api/offscreen/offscreen_api.cc
+++ b/extensions/browser/api/offscreen/offscreen_api.cc
@@ -5,6 +5,7 @@
 #include "extensions/browser/api/offscreen/offscreen_api.h"
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/strings/stringprintf.h"
 #include "content/public/browser/browser_context.h"
 #include "content/public/browser/navigation_controller.h"
@@ -96,7 +97,7 @@ ExtensionFunction::ResponseAction OffscreenCreateDocumentFunction::Run() {
     return RespondNow(Error("A `reason` must be provided."));
   }
 
-  if (deduped_reasons.contains(api::offscreen::Reason::kTesting) &&
+  if (base::Contains(deduped_reasons, api::offscreen::Reason::kTesting) &&
       !base::CommandLine::ForCurrentProcess()->HasSwitch(
           switches::kOffscreenDocumentTesting)) {
     return RespondNow(Error(base::StringPrintf(
diff --git a/extensions/browser/api/offscreen/offscreen_document_manager.cc b/extensions/browser/api/offscreen/offscreen_document_manager.cc
index e20871def6..61dba34dff 100644
--- a/extensions/browser/api/offscreen/offscreen_document_manager.cc
+++ b/extensions/browser/api/offscreen/offscreen_document_manager.cc
@@ -5,6 +5,7 @@
 #include "extensions/browser/api/offscreen/offscreen_document_manager.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "components/keyed_service/content/browser_context_dependency_manager.h"
 #include "components/keyed_service/content/browser_context_keyed_service_factory.h"
@@ -113,8 +114,8 @@ OffscreenDocumentHost* OffscreenDocumentManager::CreateOffscreenDocument(
   DCHECK_EQ(url::Origin::Create(url), extension.origin());
   // Currently only a single offscreen document is supported per extension.
   DCHECK_EQ(nullptr, GetOffscreenDocumentForExtension(extension));
-  DCHECK(!offscreen_documents_.contains(extension.id()));
-  CHECK(!reasons.contains(api::offscreen::Reason::kNone));
+  DCHECK(!base::Contains(offscreen_documents_, extension.id()));
+  CHECK(!base::Contains(reasons, api::offscreen::Reason::kNone));
 #if DCHECK_IS_ON()
   // This should only be for an off-the-record context if the extension is both
   // enabled in incognito *and* runs in split mode. For spanning mode
@@ -172,13 +173,13 @@ void OffscreenDocumentManager::CloseOffscreenDocumentForExtension(
 
 void OffscreenDocumentManager::CloseOffscreenDocumentForExtensionId(
     const ExtensionId& extension_id) {
-  DCHECK(offscreen_documents_.contains(extension_id));
+  DCHECK(base::Contains(offscreen_documents_, extension_id));
   offscreen_documents_.erase(extension_id);
 }
 
 void OffscreenDocumentManager::OnOffscreenDocumentActivityChanged(
     const ExtensionId& extension_id) {
-  DCHECK(offscreen_documents_.contains(extension_id));
+  DCHECK(base::Contains(offscreen_documents_, extension_id));
   OffscreenDocumentData& data = offscreen_documents_[extension_id];
   DCHECK(data.host);
 
diff --git a/extensions/browser/api/runtime/runtime_api.cc b/extensions/browser/api/runtime/runtime_api.cc
index 9ab06a2a12..a0ee176346 100644
--- a/extensions/browser/api/runtime/runtime_api.cc
+++ b/extensions/browser/api/runtime/runtime_api.cc
@@ -4,7 +4,6 @@
 
 #include "extensions/browser/api/runtime/runtime_api.h"
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 #include <vector>
@@ -166,39 +165,37 @@ bool ExtensionContextMatchesFilter(
     const api::runtime::ExtensionContext& context,
     const api::runtime::ContextFilter& filter) {
   if (filter.context_types &&
-      !std::ranges::contains(*filter.context_types, context.context_type)) {
+      !base::Contains(*filter.context_types, context.context_type)) {
     return false;
   }
   if (filter.context_ids &&
-      !std::ranges::contains(*filter.context_ids, context.context_id)) {
+      !base::Contains(*filter.context_ids, context.context_id)) {
     return false;
   }
-  if (filter.tab_ids &&
-      !std::ranges::contains(*filter.tab_ids, context.tab_id)) {
+  if (filter.tab_ids && !base::Contains(*filter.tab_ids, context.tab_id)) {
     return false;
   }
   if (filter.window_ids &&
-      !std::ranges::contains(*filter.window_ids, context.window_id)) {
+      !base::Contains(*filter.window_ids, context.window_id)) {
     return false;
   }
   if (filter.document_ids &&
       (!context.document_id ||
-       !std::ranges::contains(*filter.document_ids, *context.document_id))) {
+       !base::Contains(*filter.document_ids, *context.document_id))) {
     return false;
   }
   if (filter.frame_ids &&
-      !std::ranges::contains(*filter.frame_ids, context.frame_id)) {
+      !base::Contains(*filter.frame_ids, context.frame_id)) {
     return false;
   }
   if (filter.document_urls &&
       (!context.document_url ||
-       !std::ranges::contains(*filter.document_urls, *context.document_url))) {
+       !base::Contains(*filter.document_urls, *context.document_url))) {
     return false;
   }
   if (filter.document_origins &&
       (!context.document_origin ||
-       !std::ranges::contains(*filter.document_origins,
-                              *context.document_origin))) {
+       !base::Contains(*filter.document_origins, *context.document_origin))) {
     return false;
   }
   if (filter.incognito && *filter.incognito != context.incognito) {
@@ -820,8 +817,8 @@ ExtensionFunction::ResponseAction RuntimeGetContextsFunction::Run() {
   // Minor optimization: only construct the context if there's a chance it will
   // match the filter.
   if (!filter.context_types ||
-      std::ranges::contains(*filter.context_types,
-                            api::runtime::ContextType::kBackground)) {
+      base::Contains(*filter.context_types,
+                     api::runtime::ContextType::kBackground)) {
     if (std::optional<api::runtime::ExtensionContext> worker =
             GetWorkerContext()) {
       result.push_back(std::move(*worker));
diff --git a/extensions/browser/api/socket/udp_socket.cc b/extensions/browser/api/socket/udp_socket.cc
index d8bab71f96..cf96c108f2 100644
--- a/extensions/browser/api/socket/udp_socket.cc
+++ b/extensions/browser/api/socket/udp_socket.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/lazy_instance.h"
 #include "extensions/browser/api/api_resource.h"
@@ -323,7 +324,7 @@ void UDPSocket::JoinGroup(const std::string& address,
   }
 
   std::string normalized_address = ip.ToString();
-  if (std::ranges::contains(multicast_groups_, normalized_address)) {
+  if (base::Contains(multicast_groups_, normalized_address)) {
     std::move(callback).Run(net::ERR_ADDRESS_INVALID);
     return;
   }
@@ -343,7 +344,7 @@ void UDPSocket::LeaveGroup(const std::string& address,
   }
 
   std::string normalized_address = ip.ToString();
-  if (!std::ranges::contains(multicast_groups_, normalized_address)) {
+  if (!base::Contains(multicast_groups_, normalized_address)) {
     std::move(callback).Run(net::ERR_ADDRESS_INVALID);
     return;
   }
diff --git a/extensions/browser/api/storage/storage_frontend.cc b/extensions/browser/api/storage/storage_frontend.cc
index 8adf0cf521..c2ae319bb6 100644
--- a/extensions/browser/api/storage/storage_frontend.cc
+++ b/extensions/browser/api/storage/storage_frontend.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
@@ -520,7 +521,7 @@ ValueStoreCache* StorageFrontend::GetValueStoreCache(
 
 bool StorageFrontend::IsStorageEnabled(
     settings_namespace::Namespace settings_namespace) const {
-  return caches_.contains(settings_namespace);
+  return base::Contains(caches_, settings_namespace);
 }
 
 void StorageFrontend::RunWithStorage(
diff --git a/extensions/browser/api/storage/storage_utils.cc b/extensions/browser/api/storage/storage_utils.cc
index 93a6915f77..b5334e93fc 100644
--- a/extensions/browser/api/storage/storage_utils.cc
+++ b/extensions/browser/api/storage/storage_utils.cc
@@ -4,8 +4,7 @@
 
 #include "extensions/browser/api/storage/storage_utils.h"
 
-#include <utility>
-
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "extensions/browser/extension_prefs.h"
 #include "extensions/browser/extension_util.h"
@@ -63,7 +62,7 @@ api::storage::AccessLevel GetAccessLevelForArea(
     // Return access level iff it's a valid value.
     if (stored_access_level_int > 0 &&
         stored_access_level_int <=
-            std::to_underlying(api::storage::AccessLevel::kMaxValue)) {
+            base::to_underlying(api::storage::AccessLevel::kMaxValue)) {
       return static_cast<api::storage::AccessLevel>(stored_access_level_int);
     }
   }
@@ -88,7 +87,7 @@ void SetAccessLevelForArea(const ExtensionId& extension_id,
   ExtensionPrefs* prefs = ExtensionPrefs::Get(&browser_context);
   const PrefMap* pref_map = GetPrefMapForStorageArea(storage_area);
   prefs->SetIntegerPref(extension_id, *pref_map,
-                        std::to_underlying(access_level));
+                        base::to_underlying(access_level));
 }
 
 base::Value ValueChangeToValue(
diff --git a/extensions/browser/api/usb/usb_device_manager.cc b/extensions/browser/api/usb/usb_device_manager.cc
index 3f7697aeac..5eb2ee6215 100644
--- a/extensions/browser/api/usb/usb_device_manager.cc
+++ b/extensions/browser/api/usb/usb_device_manager.cc
@@ -9,6 +9,7 @@
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/observer_list.h"
 #include "base/strings/utf_string_conversions.h"
@@ -270,7 +271,7 @@ void UsbDeviceManager::OnDeviceAdded(
     device::mojom::UsbDeviceInfoPtr device_info) {
   DCHECK(device_info);
   // Update the device list.
-  DCHECK(!devices_.contains(device_info->guid));
+  DCHECK(!base::Contains(devices_, device_info->guid));
   if (!ShouldExposeDevice(*device_info))
     return;
   std::string guid = device_info->guid;
@@ -290,7 +291,7 @@ void UsbDeviceManager::OnDeviceRemoved(
   DCHECK(device_info);
 
   // Handle if ShouldExposeDevice() returned false when the device was added.
-  if (!devices_.contains(device_info->guid))
+  if (!base::Contains(devices_, device_info->guid))
     return;
 
   // Update the device list.
diff --git a/extensions/browser/api/user_scripts/user_scripts_api.cc b/extensions/browser/api/user_scripts/user_scripts_api.cc
index c5f00b8940..6a64acac43 100644
--- a/extensions/browser/api/user_scripts/user_scripts_api.cc
+++ b/extensions/browser/api/user_scripts/user_scripts_api.cc
@@ -409,7 +409,8 @@ ExtensionFunction::ResponseAction UserScriptsGetScriptsFunction::Run() {
     }
 
     std::string id_without_prefix = script->GetIDWithoutPrefix();
-    if (filter && filter->ids && !id_filter.contains(id_without_prefix)) {
+    if (filter && filter->ids &&
+        !base::Contains(id_filter, id_without_prefix)) {
       continue;
     }
 
diff --git a/extensions/browser/api/web_request/extension_web_request_event_router.cc b/extensions/browser/api/web_request/extension_web_request_event_router.cc
index c2bfb7b8d1..dbf4c59092 100644
--- a/extensions/browser/api/web_request/extension_web_request_event_router.cc
+++ b/extensions/browser/api/web_request/extension_web_request_event_router.cc
@@ -8,7 +8,6 @@
 #include <iterator>
 #include <memory>
 #include <string_view>
-#include <utility>
 #include <vector>
 
 #include "base/containers/fixed_flat_map.h"
@@ -22,6 +21,7 @@
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "base/trace_event/trace_event.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "components/guest_view/buildflags/buildflags.h"
 #include "content/public/browser/browser_context.h"
 #include "content/public/browser/browser_thread.h"
@@ -957,11 +957,11 @@ bool WebRequestEventRouter::SignaledRequestIDTracker::GetAndSet(
     EventTypes event_type) {
   auto iter = signaled_requests_.find(request_id);
   if (iter == signaled_requests_.end()) {
-    signaled_requests_[request_id] = std::to_underlying(event_type);
+    signaled_requests_[request_id] = base::to_underlying(event_type);
     return false;
   }
-  bool was_signaled_before = iter->second & std::to_underlying(event_type);
-  iter->second |= std::to_underlying(event_type);
+  bool was_signaled_before = iter->second & base::to_underlying(event_type);
+  iter->second |= base::to_underlying(event_type);
   return was_signaled_before;
 }
 
@@ -970,7 +970,7 @@ void WebRequestEventRouter::SignaledRequestIDTracker::ClearEventType(
     EventTypes event_type) {
   auto iter = signaled_requests_.find(request_id);
   if (iter != signaled_requests_.end()) {
-    iter->second &= ~std::to_underlying(event_type);
+    iter->second &= ~base::to_underlying(event_type);
   }
 }
 
@@ -2303,8 +2303,8 @@ void WebRequestEventRouter::OnOTRBrowserContextDestroyed(
   // ways that break the expectations we have in production code.
   if (event_router) {
     event_router->OnBrowserContextShutdown(otr_browser_context);
-    DCHECK(!event_router->data_.contains(
-        GetBrowserContextID(otr_browser_context)));
+    DCHECK(!base::Contains(event_router->data_,
+                           GetBrowserContextID(otr_browser_context)));
   }
 }
 
@@ -2701,8 +2701,7 @@ bool WebRequestEventRouter::ListenerMatchesRequest(
   }
 
   const std::vector<WebRequestResourceType>& types = listener.filter.types;
-  if (!types.empty() &&
-      !std::ranges::contains(types, request.web_request_type)) {
+  if (!types.empty() && !base::Contains(types, request.web_request_type)) {
     return false;
   }
 
diff --git a/extensions/browser/api/web_request/web_request_api_helpers.cc b/extensions/browser/api/web_request/web_request_api_helpers.cc
index fe79f5f12e..4f0811b18c 100644
--- a/extensions/browser/api/web_request/web_request_api_helpers.cc
+++ b/extensions/browser/api/web_request/web_request_api_helpers.cc
@@ -15,6 +15,7 @@
 #include <utility>
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/functional/bind.h"
@@ -1230,14 +1231,14 @@ void MergeOnBeforeSendHeadersResponses(
         }
 
         // We must not modify anything that has been deleted before.
-        if (removed_headers->contains(key)) {
+        if (base::Contains(*removed_headers, key)) {
           extension_conflicts = true;
           break;
         }
 
         // We must not modify anything that has been set to a *different*
         // value before.
-        if (set_headers->contains(key) &&
+        if (base::Contains(*set_headers, key) &&
             request_headers->GetHeader(key) != value) {
           extension_conflicts = true;
           break;
@@ -1249,7 +1250,7 @@ void MergeOnBeforeSendHeadersResponses(
     // modified before.
     {
       for (const std::string& key : delta.deleted_request_headers) {
-        if (set_headers->contains(base::ToLowerASCII(key))) {
+        if (base::Contains(*set_headers, base::ToLowerASCII(key))) {
           extension_conflicts = true;
           break;
         }
@@ -1266,7 +1267,7 @@ void MergeOnBeforeSendHeadersResponses(
         std::string key = base::ToLowerASCII(modification.name());
         if (!request_headers->HasHeader(key)) {
           web_request_added_headers.insert(key);
-        } else if (!web_request_added_headers.contains(key)) {
+        } else if (!base::Contains(web_request_added_headers, key)) {
           // Note: |key| will only be present in |added_headers| if this is an
           // identical edit.
           web_request_overridden_headers.insert(key);
@@ -1638,8 +1639,8 @@ void MergeOnHeadersReceivedResponses(
     bool extension_conflicts = false;
     for (const ResponseHeader& header : delta.deleted_response_headers) {
       ResponseHeader lowercase_header(ToLowerCase(header));
-      if (removed_headers.contains(lowercase_header) ||
-          dnr_header_actions.contains(lowercase_header.first)) {
+      if (base::Contains(removed_headers, lowercase_header) ||
+          base::Contains(dnr_header_actions, lowercase_header.first)) {
         extension_conflicts = true;
         break;
       }
@@ -1822,7 +1823,7 @@ bool ShouldHideRequestHeader(content::BrowserContext* browser_context,
                                                 "accept-language", "cookie",
                                                 "origin", "referer"});
   return !(extra_info_spec & ExtraInfoSpec::EXTRA_HEADERS) &&
-         kRequestHeaders.contains(base::ToLowerASCII(name));
+         base::Contains(kRequestHeaders, base::ToLowerASCII(name));
 }
 
 bool ShouldHideResponseHeader(int extra_info_spec, const std::string& name) {
diff --git a/extensions/browser/api/web_request/web_request_info.cc b/extensions/browser/api/web_request/web_request_info.cc
index 9b5cf554c9..c1cd6d0fdf 100644
--- a/extensions/browser/api/web_request/web_request_info.cc
+++ b/extensions/browser/api/web_request/web_request_info.cc
@@ -307,7 +307,8 @@ void WebRequestInfo::EraseOutprioritizedDNRActions() {
           // Check that the cache contains the action's extension ID
           // to make sure that rule matching was performed for that
           // extension.
-          DCHECK(max_priority_allow_action.contains(action.extension_id));
+          DCHECK(
+              base::Contains(max_priority_allow_action, action.extension_id));
 
           uint64_t allow_rule_priority_for_extension = 0u;
           if (auto& allow_action =
diff --git a/extensions/browser/app_window/app_window_geometry_cache.cc b/extensions/browser/app_window/app_window_geometry_cache.cc
index 5a183a7c17..898e04c16a 100644
--- a/extensions/browser/app_window/app_window_geometry_cache.cc
+++ b/extensions/browser/app_window/app_window_geometry_cache.cc
@@ -8,6 +8,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/json/values_util.h"
 #include "base/observer_list.h"
@@ -57,7 +58,7 @@ void AppWindowGeometryCache::SaveGeometry(
   if (extension_data[window_id].bounds == bounds &&
       extension_data[window_id].window_state == window_state &&
       extension_data[window_id].screen_bounds == screen_bounds &&
-      !unsynced_extensions_.contains(extension_id))
+      !base::Contains(unsynced_extensions_, extension_id))
     return;
 
   base::Time now = base::Time::Now();
diff --git a/extensions/browser/app_window/app_window_registry.cc b/extensions/browser/app_window/app_window_registry.cc
index 404c52fd2f..86b7a6edf3 100644
--- a/extensions/browser/app_window/app_window_registry.cc
+++ b/extensions/browser/app_window/app_window_registry.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/observer_list.h"
 #include "base/strings/stringprintf.h"
 #include "components/keyed_service/content/browser_context_dependency_manager.h"
@@ -176,7 +177,7 @@ void AppWindowRegistry::DevToolsAgentHostDetached(
 }
 
 void AppWindowRegistry::AddAppWindowToList(AppWindow* app_window) {
-  if (std::ranges::contains(app_windows_, app_window))
+  if (base::Contains(app_windows_, app_window))
     return;
   app_windows_.push_back(app_window);
 }
diff --git a/extensions/browser/blocklist.cc b/extensions/browser/blocklist.cc
index 37006bcae5..b9e277d9a9 100644
--- a/extensions/browser/blocklist.cc
+++ b/extensions/browser/blocklist.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/callback_list.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/lazy_instance.h"
 #include "base/memory/ref_counted.h"
@@ -296,7 +297,7 @@ void Blocklist::OnBlocklistStateReceived(const ExtensionId& id,
 
     bool have_all_in_cache = true;
     for (const auto& id_str : ids) {
-      if (!blocklist_state_cache_.contains(id_str)) {
+      if (!base::Contains(blocklist_state_cache_, id_str)) {
         have_all_in_cache = false;
         break;
       }
diff --git a/extensions/browser/blocklist_extension_prefs.cc b/extensions/browser/blocklist_extension_prefs.cc
index 3e8448407c..b02ea11098 100644
--- a/extensions/browser/blocklist_extension_prefs.cc
+++ b/extensions/browser/blocklist_extension_prefs.cc
@@ -4,9 +4,9 @@
 
 #include "extensions/browser/blocklist_extension_prefs.h"
 
-#include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "extensions/browser/blocklist_state.h"
 #include "extensions/browser/extension_prefs.h"
 #include "extensions/common/extension_id.h"
@@ -120,7 +120,7 @@ bool IsExtensionGreylisted(const ExtensionId& extension_id,
                            ExtensionPrefs* extension_prefs) {
   const BitMapBlocklistState state =
       GetExtensionBlocklistState(extension_id, extension_prefs);
-  return std::ranges::contains(kGreylistStates, state);
+  return base::Contains(kGreylistStates, state);
 }
 
 void AddOmahaBlocklistState(const ExtensionId& extension_id,
diff --git a/extensions/browser/blocklist_state_fetcher.cc b/extensions/browser/blocklist_state_fetcher.cc
index c6deec37d7..af3ab52896 100644
--- a/extensions/browser/blocklist_state_fetcher.cc
+++ b/extensions/browser/blocklist_state_fetcher.cc
@@ -7,6 +7,7 @@
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/escape.h"
 #include "base/task/single_thread_task_runner.h"
@@ -52,7 +53,7 @@ void BlocklistStateFetcher::Request(const std::string& id,
     }
   }
 
-  bool request_already_sent = callbacks_.contains(id);
+  bool request_already_sent = base::Contains(callbacks_, id);
   callbacks_.insert(std::make_pair(id, std::move(callback)));
   if (request_already_sent) {
     return;
diff --git a/extensions/browser/content_verifier/content_verifier.cc b/extensions/browser/content_verifier/content_verifier.cc
index daa511e941..5bfa97e054 100644
--- a/extensions/browser/content_verifier/content_verifier.cc
+++ b/extensions/browser/content_verifier/content_verifier.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
@@ -481,11 +482,12 @@ class ContentVerifier::VerifiedFileTypeHelper {
     if (canonical_path_value == data_->canonical_service_worker_script_path) {
       return ContentVerifier::VerifiedFileType::kServiceWorkerScript;
     }
-    if (data_->canonical_background_scripts_paths.contains(
-            canonical_path_value)) {
+    if (base::Contains(data_->canonical_background_scripts_paths,
+                       canonical_path_value)) {
       return ContentVerifier::VerifiedFileType::kBackgroundScript;
     }
-    if (data_->canonical_content_scripts_paths.contains(canonical_path_value)) {
+    if (base::Contains(data_->canonical_content_scripts_paths,
+                       canonical_path_value)) {
       return ContentVerifier::VerifiedFileType::kContentScript;
     }
 
@@ -505,12 +507,14 @@ class ContentVerifier::VerifiedFileTypeHelper {
 
     // The browser re-writes image files during extension load, so they can't
     // be verified.
-    if (data_->canonical_browser_image_paths.contains(canonical_path_value)) {
+    if (base::Contains(data_->canonical_browser_image_paths,
+                       canonical_path_value)) {
       return ContentVerifier::VerifiedFileType::kNone;
     }
 
     // Skip indexed rulesets since these are generated.
-    if (data_->canonical_indexed_ruleset_paths.contains(canonical_path_value)) {
+    if (base::Contains(data_->canonical_indexed_ruleset_paths,
+                       canonical_path_value)) {
       return ContentVerifier::VerifiedFileType::kNone;
     }
 
diff --git a/extensions/browser/content_verifier/test_utils.cc b/extensions/browser/content_verifier/test_utils.cc
index 9516091b85..258ff666a9 100644
--- a/extensions/browser/content_verifier/test_utils.cc
+++ b/extensions/browser/content_verifier/test_utils.cc
@@ -5,6 +5,7 @@
 #include "extensions/browser/content_verifier/test_utils.h"
 
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/json/json_writer.h"
@@ -263,7 +264,7 @@ VerifierObserver::~VerifierObserver() {
 
 void VerifierObserver::EnsureFetchCompleted(const ExtensionId& extension_id) {
   EXPECT_TRUE(content::BrowserThread::CurrentlyOn(creation_thread_));
-  if (completed_fetches_.contains(extension_id))
+  if (base::Contains(completed_fetches_, extension_id))
     return;
   EXPECT_TRUE(id_to_wait_for_.empty());
   EXPECT_EQ(loop_runner_.get(), nullptr);
diff --git a/extensions/browser/disable_reason.cc b/extensions/browser/disable_reason.cc
index 1d7bea7d10..9a5a6c109f 100644
--- a/extensions/browser/disable_reason.cc
+++ b/extensions/browser/disable_reason.cc
@@ -5,9 +5,9 @@
 #include "extensions/browser/disable_reason.h"
 
 #include <ostream>
-#include <utility>
 
 #include "base/check_op.h"
+#include "base/types/cxx23_to_underlying.h"
 
 namespace extensions {
 
@@ -65,7 +65,7 @@ base::flat_set<int> BitflagToIntegerSet(int bit_flag) {
 base::flat_set<int> DisableReasonSetToIntegerSet(const DisableReasonSet& set) {
   base::flat_set<int> result;
   for (disable_reason::DisableReason reason : set) {
-    result.insert(std::to_underlying(reason));
+    result.insert(base::to_underlying(reason));
   }
   return result;
 }
diff --git a/extensions/browser/event_listener_map.cc b/extensions/browser/event_listener_map.cc
index a559b7f6fc..f85e5c368a 100644
--- a/extensions/browser/event_listener_map.cc
+++ b/extensions/browser/event_listener_map.cc
@@ -8,6 +8,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "content/public/browser/render_process_host.h"
 #include "extensions/browser/event_router.h"
@@ -441,7 +442,7 @@ void EventListenerMap::CleanupListener(EventListener* listener) {
 }
 
 bool EventListenerMap::IsFilteredEvent(const Event& event) const {
-  return filtered_events_.contains(event.event_name);
+  return base::Contains(filtered_events_, event.event_name);
 }
 
 }  // namespace extensions
diff --git a/extensions/browser/event_router.cc b/extensions/browser/event_router.cc
index c3324ee6d0..9c40ca4c71 100644
--- a/extensions/browser/event_router.cc
+++ b/extensions/browser/event_router.cc
@@ -15,6 +15,7 @@
 
 #include "base/atomic_sequence_num.h"
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -177,7 +178,7 @@ void EventRouter::RouteDispatchEvent(
     mojom::DispatchEventParamsPtr params,
     base::ListValue event_args,
     mojom::EventDispatcher::DispatchEventCallback callback) {
-  CHECK(observed_process_set_.contains(rph));
+  CHECK(base::Contains(observed_process_set_, rph));
   int worker_thread_id = params->worker_thread_id;
   mojo::AssociatedRemote<mojom::EventDispatcher>& dispatcher =
       rph_dispatcher_map_[rph][worker_thread_id];
@@ -590,7 +591,7 @@ void EventRouter::AddEventListener(const std::string& event_name,
                                    const ExtensionId& extension_id) {
   listeners_.AddListener(EventListener::ForExtension(event_name, extension_id,
                                                      process, std::nullopt));
-  CHECK(observed_process_set_.contains(process));
+  CHECK(base::Contains(observed_process_set_, process));
 }
 
 void EventRouter::AddServiceWorkerEventListener(
@@ -603,7 +604,7 @@ void EventRouter::AddServiceWorkerEventListener(
       event_listener->listener_owner->get_extension_id(), process,
       process->GetBrowserContext(), service_worker.scope_url,
       service_worker.version_id, service_worker.thread_id, std::nullopt));
-  CHECK(observed_process_set_.contains(process));
+  CHECK(base::Contains(observed_process_set_, process));
 }
 
 void EventRouter::RemoveEventListener(const std::string& event_name,
@@ -633,7 +634,7 @@ void EventRouter::AddEventListenerForURL(const std::string& event_name,
                                          const GURL& listener_url) {
   listeners_.AddListener(
       EventListener::ForURL(event_name, listener_url, process, std::nullopt));
-  CHECK(observed_process_set_.contains(process));
+  CHECK(base::Contains(observed_process_set_, process));
 }
 
 void EventRouter::RemoveEventListenerForURL(const std::string& event_name,
@@ -647,7 +648,7 @@ void EventRouter::RemoveEventListenerForURL(const std::string& event_name,
 void EventRouter::RegisterObserver(Observer* observer,
                                    const std::string& event_name) {
   // Observing sub-event names like "foo.onBar/123" is not allowed.
-  DCHECK(!event_name.contains('/'));
+  DCHECK(!base::Contains(event_name, '/'));
   auto& observers = observer_map_[event_name];
   if (!observers) {
     observers = std::make_unique<Observers>();
diff --git a/extensions/browser/events/event_dispatch_helper.cc b/extensions/browser/events/event_dispatch_helper.cc
index fc248e8c25..72756214f5 100644
--- a/extensions/browser/events/event_dispatch_helper.cc
+++ b/extensions/browser/events/event_dispatch_helper.cc
@@ -6,6 +6,7 @@
 
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/task/sequenced_task_runner.h"
 #include "content/public/browser/render_process_host.h"
@@ -426,7 +427,7 @@ void EventDispatchHelper::RecordAlreadyQueued(
 
 bool EventDispatchHelper::IsAlreadyQueued(
     const LazyContextId& dispatch_context) const {
-  return dispatched_ids_.contains(dispatch_context);
+  return base::Contains(dispatched_ids_, dispatch_context);
 }
 
 bool EventDispatchHelper::ListenerMeetsRestrictions(
diff --git a/extensions/browser/extension_action.cc b/extensions/browser/extension_action.cc
index 53a6d58261..668577b6f2 100644
--- a/extensions/browser/extension_action.cc
+++ b/extensions/browser/extension_action.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "extensions/browser/extension_icon_image.h"
 #include "extensions/browser/extension_icon_placeholder.h"
@@ -64,7 +65,7 @@ struct IconRepresentationInfo {
 
 template <class T>
 bool HasValue(const std::map<int, T>& map, ExtensionAction::TabID tab_id) {
-  return map.contains(tab_id);
+  return base::Contains(map, tab_id);
 }
 
 }  // namespace
@@ -314,7 +315,7 @@ bool ExtensionAction::GetIsVisibleInternal(TabID tab_id,
     return *tab_is_visible;
   }
 
-  if (include_declarative && declarative_show_count_.contains(tab_id)) {
+  if (include_declarative && base::Contains(declarative_show_count_, tab_id)) {
     return true;
   }
 
diff --git a/extensions/browser/extension_action.h b/extensions/browser/extension_action.h
index 44596528e0..20d95bb53a 100644
--- a/extensions/browser/extension_action.h
+++ b/extensions/browser/extension_action.h
@@ -10,6 +10,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "extensions/common/api/extension_action/action_info.h"
 #include "extensions/common/constants.h"
diff --git a/extensions/browser/extension_creator_filter.cc b/extensions/browser/extension_creator_filter.cc
index 71ea04afde..6116ee32fb 100644
--- a/extensions/browser/extension_creator_filter.cc
+++ b/extensions/browser/extension_creator_filter.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "build/build_config.h"
 #include "extensions/common/constants.h"
 
@@ -45,7 +46,7 @@ bool ExtensionCreatorFilter::ShouldPackageFile(
       FILE_PATH_LITERAL(".svn"),        FILE_PATH_LITERAL("__MACOSX"),
       FILE_PATH_LITERAL("desktop.ini"), FILE_PATH_LITERAL("Thumbs.db")};
   for (const auto& component : file_path.GetComponents()) {
-    if (std::ranges::contains(kNamesToExclude, component)) {
+    if (base::Contains(kNamesToExclude, component)) {
       return false;
     }
   }
diff --git a/extensions/browser/extension_host_registry.cc b/extensions/browser/extension_host_registry.cc
index 3392c3dea2..1a34328fc5 100644
--- a/extensions/browser/extension_host_registry.cc
+++ b/extensions/browser/extension_host_registry.cc
@@ -5,6 +5,7 @@
 #include "extensions/browser/extension_host_registry.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/observer_list.h"
 #include "components/keyed_service/content/browser_context_dependency_manager.h"
@@ -84,7 +85,7 @@ BrowserContextKeyedServiceFactory* ExtensionHostRegistry::GetFactory() {
 
 void ExtensionHostRegistry::ExtensionHostCreated(
     ExtensionHost* extension_host) {
-  DCHECK(!extension_hosts_.contains(extension_host));
+  DCHECK(!base::Contains(extension_hosts_, extension_host));
   extension_hosts_.insert(extension_host);
 
   // Note: There's not currently any observer method corresponding to host
@@ -95,7 +96,7 @@ void ExtensionHostRegistry::ExtensionHostCreated(
 
 void ExtensionHostRegistry::ExtensionHostRenderProcessReady(
     ExtensionHost* extension_host) {
-  DCHECK(extension_hosts_.contains(extension_host));
+  DCHECK(base::Contains(extension_hosts_, extension_host));
 
   for (Observer& observer : observers_) {
     observer.OnExtensionHostRenderProcessReady(
@@ -105,7 +106,7 @@ void ExtensionHostRegistry::ExtensionHostRenderProcessReady(
 
 void ExtensionHostRegistry::ExtensionHostCompletedFirstLoad(
     ExtensionHost* extension_host) {
-  DCHECK(extension_hosts_.contains(extension_host));
+  DCHECK(base::Contains(extension_hosts_, extension_host));
 
   // TODO(devlin): This can unexpectedly fire when a renderer process is
   // terminating.  When a renderer process is terminated, it causes the
@@ -125,7 +126,7 @@ void ExtensionHostRegistry::ExtensionHostCompletedFirstLoad(
 
 void ExtensionHostRegistry::ExtensionHostDocumentElementAvailable(
     ExtensionHost* extension_host) {
-  DCHECK(extension_hosts_.contains(extension_host));
+  DCHECK(base::Contains(extension_hosts_, extension_host));
 
   for (Observer& observer : observers_) {
     observer.OnExtensionHostDocumentElementAvailable(
@@ -135,7 +136,7 @@ void ExtensionHostRegistry::ExtensionHostDocumentElementAvailable(
 
 void ExtensionHostRegistry::ExtensionHostRenderProcessGone(
     ExtensionHost* extension_host) {
-  DCHECK(extension_hosts_.contains(extension_host));
+  DCHECK(base::Contains(extension_hosts_, extension_host));
 
   for (Observer& observer : observers_) {
     observer.OnExtensionHostRenderProcessGone(extension_host->browser_context(),
@@ -145,7 +146,7 @@ void ExtensionHostRegistry::ExtensionHostRenderProcessGone(
 
 void ExtensionHostRegistry::ExtensionHostDestroyed(
     ExtensionHost* extension_host) {
-  DCHECK(extension_hosts_.contains(extension_host));
+  DCHECK(base::Contains(extension_hosts_, extension_host));
   extension_hosts_.erase(extension_host);
 
   for (Observer& observer : observers_) {
diff --git a/extensions/browser/extension_host_test_helper.cc b/extensions/browser/extension_host_test_helper.cc
index 1c9797c2a4..9e6f00b220 100644
--- a/extensions/browser/extension_host_test_helper.cc
+++ b/extensions/browser/extension_host_test_helper.cc
@@ -5,6 +5,7 @@
 #include "extensions/browser/extension_host_test_helper.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/run_loop.h"
 #include "extensions/browser/extension_host.h"
 
@@ -85,7 +86,7 @@ ExtensionHost* ExtensionHostTestHelper::WaitFor(HostEvent event) {
   waiting_for_ = event;
   run_loop.Run();
 
-  DCHECK(observed_events_.contains(event));
+  DCHECK(base::Contains(observed_events_, event));
   // Note: This can still be null here if the corresponding ExtensionHost was
   // destroyed.  This is always true when waiting for
   // OnExtensionHostDestroyed(), but can also happen if the ExtensionHost is
diff --git a/extensions/browser/extension_navigation_throttle.cc b/extensions/browser/extension_navigation_throttle.cc
index b2219160db..a02fad5aa2 100644
--- a/extensions/browser/extension_navigation_throttle.cc
+++ b/extensions/browser/extension_navigation_throttle.cc
@@ -4,10 +4,10 @@
 
 #include "extensions/browser/extension_navigation_throttle.h"
 
-#include <algorithm>
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "components/guest_view/buildflags/buildflags.h"
 #include "content/public/browser/browser_thread.h"
@@ -361,8 +361,8 @@ ExtensionNavigationThrottle::WillStartOrRedirectRequest() {
   const url::Origin& initiator_origin =
       navigation_handle()->GetInitiatorOrigin().value();
   if (initiator_origin.scheme() == kExtensionScheme &&
-      std::ranges::contains(MimeTypesHandler::GetMIMETypeAllowlist(),
-                            initiator_origin.host())) {
+      base::Contains(MimeTypesHandler::GetMIMETypeAllowlist(),
+                     initiator_origin.host())) {
     return content::NavigationThrottle::PROCEED;
   }
 
diff --git a/extensions/browser/extension_prefs.cc b/extensions/browser/extension_prefs.cc
index d181bffec8..5e4aef91ec 100644
--- a/extensions/browser/extension_prefs.cc
+++ b/extensions/browser/extension_prefs.cc
@@ -15,6 +15,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/json/values_util.h"
 #include "base/observer_list.h"
@@ -1432,7 +1433,7 @@ bool ExtensionPrefs::IsExternalExtensionUninstalled(
     const ExtensionId& id) const {
   ExtensionIdList uninstalled_ids;
   GetUserExtensionPrefIntoContainer(kExternalUninstalls, &uninstalled_ids);
-  return std::ranges::contains(uninstalled_ids, id);
+  return base::Contains(uninstalled_ids, id);
 }
 
 bool ExtensionPrefs::ClearExternalExtensionUninstalled(const ExtensionId& id) {
diff --git a/extensions/browser/extension_registrar.cc b/extensions/browser/extension_registrar.cc
index 089b989e52..de288d872a 100644
--- a/extensions/browser/extension_registrar.cc
+++ b/extensions/browser/extension_registrar.cc
@@ -6,6 +6,7 @@
 
 #include "base/check_is_test.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -824,7 +825,8 @@ void ExtensionRegistrar::OnBlocklistStateAdded(
   if (blocklist_prefs::HasAcknowledgedBlocklistState(
           extension_id, BitMapBlocklistState::BLOCKLISTED_MALWARE,
           extension_prefs_)) {
-    DCHECK(registry_->blocklisted_extensions().GetIDs().contains(extension_id));
+    DCHECK(base::Contains(registry_->blocklisted_extensions().GetIDs(),
+                          extension_id));
     return;
   }
 
@@ -948,7 +950,7 @@ void ExtensionRegistrar::TerminateExtension(const ExtensionId& extension_id) {
   // even if it's not permanently installed.
   unloaded_extension_paths_[extension->id()] = extension->path();
 
-  DCHECK(!reloading_extensions_.contains(extension->id()))
+  DCHECK(!base::Contains(reloading_extensions_, extension->id()))
       << "Enabled extension shouldn't be marked for reloading";
 
   registry_->AddTerminated(extension);
@@ -1248,7 +1250,8 @@ void ExtensionRegistrar::MaybeSpinUpLazyContext(const Extension* extension,
 
   // For orphaned devtools, we will reconnect devtools to it later in
   // DidCreateMainFrameForBackgroundPage().
-  bool has_orphaned_dev_tools = orphaned_dev_tools_.contains(extension->id());
+  bool has_orphaned_dev_tools =
+      base::Contains(orphaned_dev_tools_, extension->id());
 
   // Reloading component extension does not trigger install, so RuntimeAPI won't
   // be able to detect its loading. Therefore, we need to spin up its lazy
diff --git a/extensions/browser/extension_user_script_loader.cc b/extensions/browser/extension_user_script_loader.cc
index d1f4058f92..b908f026ac 100644
--- a/extensions/browser/extension_user_script_loader.cc
+++ b/extensions/browser/extension_user_script_loader.cc
@@ -15,6 +15,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -624,7 +625,7 @@ void ExtensionUserScriptLoader::AddDynamicScripts(
   // are quickly unregistered.
   std::erase_if(scripts, [&pending_ids = pending_dynamic_script_ids_](
                              const std::unique_ptr<UserScript>& script) {
-    return !pending_ids.contains(script->id());
+    return !base::Contains(pending_ids, script->id());
   });
 
   if (scripts.empty()) {
@@ -636,7 +637,7 @@ void ExtensionUserScriptLoader::AddDynamicScripts(
   for (const auto& script : scripts) {
     // Additionally, only add scripts to the set of active scripts in renderers
     // (through `AddScripts()`) if the `source` for that script is enabled.
-    if (!disabled_sources_.contains(script->GetSource())) {
+    if (!base::Contains(disabled_sources_, script->GetSource())) {
       // TODO(crbug.com/40938420): This results in an additional copy being
       // stored in the browser for each of these scripts. Optimize the usage of
       // inline code.
@@ -833,7 +834,7 @@ void ExtensionUserScriptLoader::DynamicScriptsStorageHelper::SetDynamicScripts(
   base::ListValue scripts_value;
   URLPatternSet persistent_patterns;
   for (const std::unique_ptr<UserScript>& script : scripts) {
-    if (!persistent_dynamic_script_ids.contains(script->id())) {
+    if (!base::Contains(persistent_dynamic_script_ids, script->id())) {
       continue;
     }
 
@@ -886,7 +887,7 @@ void ExtensionUserScriptLoader::LoadScripts(
 
   ScriptResourceIds script_resource_ids;
   for (const std::unique_ptr<UserScript>& script : user_scripts) {
-    if (!added_script_ids.contains(script->id())) {
+    if (!base::Contains(added_script_ids, script->id())) {
       continue;
     }
     FillScriptFileResourceIds(script->js_scripts(), script_resource_ids);
@@ -912,7 +913,7 @@ void ExtensionUserScriptLoader::OnInitialDynamicScriptsReadFromStateStore(
   for (const std::unique_ptr<UserScript>& script : initial_dynamic_scripts) {
     // Only add the script to the `UserScriptLoader`'s set (thus sending it to
     // renderers) if the script source type is enabled.
-    if (!disabled_sources_.contains(script->GetSource())) {
+    if (!base::Contains(disabled_sources_, script->GetSource())) {
       scripts_to_add.push_back(CopyDynamicScriptInfo(*script));
       pending_dynamic_script_ids_.insert(script->id());
     }
@@ -990,12 +991,12 @@ void ExtensionUserScriptLoader::OnDynamicScriptsRemoved(
     std::erase_if(
         loaded_dynamic_scripts_,
         [&removed_script_ids](const std::unique_ptr<UserScript>& script) {
-          return removed_script_ids.contains(script->id());
+          return base::Contains(removed_script_ids, script->id());
         });
 
     std::erase_if(persistent_dynamic_script_ids_,
                   [&removed_script_ids](const auto& id) {
-                    return removed_script_ids.contains(id);
+                    return base::Contains(removed_script_ids, id);
                   });
 
     helper_.SetDynamicScripts(loaded_dynamic_scripts_,
diff --git a/extensions/browser/guest_view/extensions_guest_view.cc b/extensions/browser/guest_view/extensions_guest_view.cc
index bce04790af..2c26750555 100644
--- a/extensions/browser/guest_view/extensions_guest_view.cc
+++ b/extensions/browser/guest_view/extensions_guest_view.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "content/public/browser/browser_thread.h"
 #include "content/public/browser/render_frame_host.h"
@@ -76,7 +77,7 @@ void ExtensionsGuestView::CanExecuteContentScript(
     std::move(callback).Run(false);
     return;
   }
-  const bool can_execute = info.content_script_ids.contains(script_id);
+  const bool can_execute = base::Contains(info.content_script_ids, script_id);
   std::move(callback).Run(can_execute);
 }
 
diff --git a/extensions/browser/guest_view/mime_handler_view/mime_handler_view_attach_helper.cc b/extensions/browser/guest_view/mime_handler_view/mime_handler_view_attach_helper.cc
index 27568610cf..9e2f680dc8 100644
--- a/extensions/browser/guest_view/mime_handler_view/mime_handler_view_attach_helper.cc
+++ b/extensions/browser/guest_view/mime_handler_view/mime_handler_view_attach_helper.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
@@ -85,7 +86,7 @@ MimeHandlerViewAttachHelper* MimeHandlerViewAttachHelper::Get(
     int32_t render_process_id) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   auto& map = *GetProcessIdToHelperMap();
-  if (!map.contains(render_process_id)) {
+  if (!base::Contains(map, render_process_id)) {
     auto* process_host = content::RenderProcessHost::FromID(render_process_id);
     if (!process_host) {
       return nullptr;
diff --git a/extensions/browser/guest_view/mime_handler_view/mime_handler_view_embedder.cc b/extensions/browser/guest_view/mime_handler_view/mime_handler_view_embedder.cc
index 2ad3985808..417ef83627 100644
--- a/extensions/browser/guest_view/mime_handler_view/mime_handler_view_embedder.cc
+++ b/extensions/browser/guest_view/mime_handler_view/mime_handler_view_embedder.cc
@@ -4,6 +4,7 @@
 
 #include "extensions/browser/guest_view/mime_handler_view/mime_handler_view_embedder.h"
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/memory/ptr_util.h"
 #include "base/no_destructor.h"
@@ -51,7 +52,8 @@ void MimeHandlerViewEmbedder::Create(
     const GURL& resource_url,
     const std::string& stream_id,
     const std::string& internal_id) {
-  DCHECK(!GetMimeHandlerViewEmbeddersMap()->contains(frame_tree_node_id));
+  DCHECK(
+      !base::Contains(*GetMimeHandlerViewEmbeddersMap(), frame_tree_node_id));
   GetMimeHandlerViewEmbeddersMap()->insert_or_assign(
       frame_tree_node_id,
       base::WrapUnique(new MimeHandlerViewEmbedder(
diff --git a/extensions/browser/guest_view/web_view/web_view_renderer_state.cc b/extensions/browser/guest_view/web_view/web_view_renderer_state.cc
index 1b1f50fb30..d085c5482a 100644
--- a/extensions/browser/guest_view/web_view/web_view_renderer_state.cc
+++ b/extensions/browser/guest_view/web_view/web_view_renderer_state.cc
@@ -4,6 +4,7 @@
 
 #include "extensions/browser/guest_view/web_view/web_view_renderer_state.h"
 
+#include "base/containers/contains.h"
 #include "content/public/browser/browser_thread.h"
 
 using content::BrowserThread;
@@ -28,7 +29,7 @@ WebViewRendererState::~WebViewRendererState() = default;
 
 bool WebViewRendererState::IsGuest(int render_process_id) const {
   base::AutoLock auto_lock(web_view_partition_id_map_lock_);
-  return web_view_partition_id_map_.contains(render_process_id);
+  return base::Contains(web_view_partition_id_map_, render_process_id);
 }
 
 void WebViewRendererState::AddGuest(int guest_process_id,
diff --git a/extensions/browser/install/crx_install_error.cc b/extensions/browser/install/crx_install_error.cc
index f864ae599f..9c16dcaf27 100644
--- a/extensions/browser/install/crx_install_error.cc
+++ b/extensions/browser/install/crx_install_error.cc
@@ -4,9 +4,8 @@
 
 #include "extensions/browser/install/crx_install_error.h"
 
-#include <algorithm>
-
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "extensions/browser/install/sandboxed_unpacker_failure_reason.h"
 
 namespace extensions {
@@ -79,8 +78,7 @@ bool CrxInstallError::IsCrxVerificationFailedError() const {
   }
   const SandboxedUnpackerFailureReason unpacker_failure_reason =
       sandbox_failure_detail();
-  return std::ranges::contains(kVerificationFailureReasons,
-                               unpacker_failure_reason);
+  return base::Contains(kVerificationFailureReasons, unpacker_failure_reason);
 }
 
 // Returns true if the error occurred during crx installation due to mismatch in
diff --git a/extensions/browser/install_verifier.cc b/extensions/browser/install_verifier.cc
index ab4806ac8b..8925f2ba05 100644
--- a/extensions/browser/install_verifier.cc
+++ b/extensions/browser/install_verifier.cc
@@ -10,6 +10,7 @@
 
 #include "base/base_switches.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/field_trial.h"
 #include "base/metrics/histogram_macros.h"
@@ -171,12 +172,12 @@ base::Time InstallVerifier::SignatureTimestamp() {
 }
 
 bool InstallVerifier::IsKnownId(const std::string& id) const {
-  return signature_.get() &&
-         (signature_->ids.contains(id) || signature_->invalid_ids.contains(id));
+  return signature_.get() && (base::Contains(signature_->ids, id) ||
+                              base::Contains(signature_->invalid_ids, id));
 }
 
 bool InstallVerifier::IsInvalid(const std::string& id) const {
-  return ((signature_.get() && signature_->invalid_ids.contains(id)));
+  return ((signature_.get() && base::Contains(signature_->invalid_ids, id)));
 }
 
 void InstallVerifier::VerifyExtension(const std::string& extension_id) {
@@ -227,8 +228,8 @@ void InstallVerifier::RemoveMany(const ExtensionIdSet& ids) {
     return;
 
   if (std::ranges::any_of(ids, [this](const std::string& id) {
-        return signature_->ids.contains(id) ||
-               signature_->invalid_ids.contains(id);
+        return base::Contains(signature_->ids, id) ||
+               base::Contains(signature_->invalid_ids, id);
       })) {
     return;
   }
@@ -270,7 +271,8 @@ bool InstallVerifier::MustRemainDisabled(
   }
 
   bool verified = true;
-  if (InstallSigner::GetForcedNotFromWebstore().contains(extension->id())) {
+  if (base::Contains(InstallSigner::GetForcedNotFromWebstore(),
+                     extension->id())) {
     verified = false;
   } else if (!IsFromStore(*extension, context_)) {
     verified = false;
@@ -286,7 +288,8 @@ bool InstallVerifier::MustRemainDisabled(
     // added extension ids. To avoid false positives, consider all extensions to
     // be from the webstore unless the signature explicitly lists the extension
     // as invalid.
-    if (!signature_ || signature_->invalid_ids.contains(extension->id()) ||
+    if (!signature_ ||
+        base::Contains(signature_->invalid_ids, extension->id()) ||
         GetStatus() >= VerifyStatus::ENFORCE_STRICT) {
       verified = false;
     }
@@ -387,8 +390,8 @@ void InstallVerifier::GarbageCollect() {
 }
 
 bool InstallVerifier::IsVerified(const std::string& id) const {
-  return ((signature_.get() && signature_->ids.contains(id)) ||
-          provisional_.contains(id));
+  return ((signature_.get() && base::Contains(signature_->ids, id)) ||
+          base::Contains(provisional_, id));
 }
 
 void InstallVerifier::BeginFetch() {
@@ -406,7 +409,7 @@ void InstallVerifier::BeginFetch() {
   }
   if (operation.type == InstallVerifier::REMOVE) {
     for (const std::string& id : operation.ids) {
-      if (ids_to_sign.contains(id))
+      if (base::Contains(ids_to_sign, id))
         ids_to_sign.erase(id);
     }
   } else {  // All other operation types are some form of "ADD".
diff --git a/extensions/browser/lazy_background_task_queue.cc b/extensions/browser/lazy_background_task_queue.cc
index ce1e85de3e..2a8fe938df 100644
--- a/extensions/browser/lazy_background_task_queue.cc
+++ b/extensions/browser/lazy_background_task_queue.cc
@@ -5,6 +5,7 @@
 #include "extensions/browser/lazy_background_task_queue.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/notreached.h"
 #include "content/public/browser/browser_context.h"
@@ -240,7 +241,7 @@ void LazyBackgroundTaskQueue::CreateLazyBackgroundHostOnExtensionLoaded(
     const Extension* extension) {
   const auto key = LazyContextId::ForExtension(browser_context, extension);
   CHECK(key.IsForBackgroundPage());
-  if (!pending_tasks_.contains(key)) {
+  if (!base::Contains(pending_tasks_, key)) {
     return;
   }
 
diff --git a/extensions/browser/message_tracker.cc b/extensions/browser/message_tracker.cc
index dff7626c84..0bba776af6 100644
--- a/extensions/browser/message_tracker.cc
+++ b/extensions/browser/message_tracker.cc
@@ -6,6 +6,7 @@
 
 #include <map>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/singleton.h"
 #include "base/memory/weak_ptr.h"
@@ -106,7 +107,7 @@ void MessageTracker::StartTrackingMessagingStage(
     const base::UnguessableToken& tracking_id,
     std::string base_metric_name,
     mojom::ChannelType channel_type) {
-  CHECK(!tracked_stages_.contains(tracking_id));
+  CHECK(!base::Contains(tracked_stages_, tracking_id));
   tracked_stages_.emplace(
       tracking_id, TrackedStage(std::move(base_metric_name), channel_type));
 
diff --git a/extensions/browser/mock_display_info_provider.cc b/extensions/browser/mock_display_info_provider.cc
index 8eb9dcd892..8734c04920 100644
--- a/extensions/browser/mock_display_info_provider.cc
+++ b/extensions/browser/mock_display_info_provider.cc
@@ -7,6 +7,7 @@
 #include <stdint.h>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/task/single_thread_task_runner.h"
@@ -37,7 +38,7 @@ void MockDisplayInfoProvider::EnableUnifiedDesktop(bool enable) {
 }
 
 bool MockDisplayInfoProvider::OverscanCalibrationStart(const std::string& id) {
-  if (overscan_started_.contains(id)) {
+  if (base::Contains(overscan_started_, id)) {
     return false;
   }
   overscan_started_.insert(id);
@@ -47,7 +48,7 @@ bool MockDisplayInfoProvider::OverscanCalibrationStart(const std::string& id) {
 bool MockDisplayInfoProvider::OverscanCalibrationAdjust(
     const std::string& id,
     const api::system_display::Insets& delta) {
-  if (!overscan_started_.contains(id)) {
+  if (!base::Contains(overscan_started_, id)) {
     return false;
   }
   overscan_adjusted_.insert(id);
@@ -55,7 +56,7 @@ bool MockDisplayInfoProvider::OverscanCalibrationAdjust(
 }
 
 bool MockDisplayInfoProvider::OverscanCalibrationReset(const std::string& id) {
-  if (!overscan_started_.contains(id)) {
+  if (!base::Contains(overscan_started_, id)) {
     return false;
   }
   overscan_adjusted_.erase(id);
@@ -64,7 +65,7 @@ bool MockDisplayInfoProvider::OverscanCalibrationReset(const std::string& id) {
 
 bool MockDisplayInfoProvider::OverscanCalibrationComplete(
     const std::string& id) {
-  if (!overscan_started_.contains(id)) {
+  if (!base::Contains(overscan_started_, id)) {
     return false;
   }
   overscan_started_.erase(id);
@@ -72,11 +73,11 @@ bool MockDisplayInfoProvider::OverscanCalibrationComplete(
 }
 
 bool MockDisplayInfoProvider::calibration_started(const std::string& id) const {
-  return overscan_started_.contains(id);
+  return base::Contains(overscan_started_, id);
 }
 
 bool MockDisplayInfoProvider::calibration_changed(const std::string& id) const {
-  return overscan_adjusted_.contains(id);
+  return base::Contains(overscan_adjusted_, id);
 }
 
 void MockDisplayInfoProvider::ShowNativeTouchCalibration(
diff --git a/extensions/browser/mock_external_provider.cc b/extensions/browser/mock_external_provider.cc
index 2cc7039d4a..a8d0f12a89 100644
--- a/extensions/browser/mock_external_provider.cc
+++ b/extensions/browser/mock_external_provider.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/version.h"
 #include "extensions/browser/external_install_info.h"
 #include "extensions/common/extension.h"
@@ -30,14 +31,14 @@ void MockExternalProvider::UpdateOrAddExtension(const ExtensionId& id,
 void MockExternalProvider::UpdateOrAddExtension(
     std::unique_ptr<ExternalInstallInfoFile> info) {
   const std::string& id = info->extension_id;
-  CHECK(!url_extension_map_.contains(id));
+  CHECK(!base::Contains(url_extension_map_, id));
   file_extension_map_[id] = std::move(info);
 }
 
 void MockExternalProvider::UpdateOrAddExtension(
     std::unique_ptr<ExternalInstallInfoUpdateUrl> info) {
   const std::string& id = info->extension_id;
-  CHECK(!file_extension_map_.contains(id));
+  CHECK(!base::Contains(file_extension_map_, id));
   url_extension_map_[id] = std::move(info);
 }
 
@@ -65,7 +66,8 @@ void MockExternalProvider::TriggerOnExternalExtensionFound() {
 }
 
 bool MockExternalProvider::HasExtension(const std::string& id) const {
-  return file_extension_map_.contains(id) || url_extension_map_.contains(id);
+  return base::Contains(file_extension_map_, id) ||
+         base::Contains(url_extension_map_, id);
 }
 
 bool MockExternalProvider::HasExtensionWithLocation(
diff --git a/extensions/browser/pending_extension_manager.cc b/extensions/browser/pending_extension_manager.cc
index 8d7d14d823..b50078d33b 100644
--- a/extensions/browser/pending_extension_manager.cc
+++ b/extensions/browser/pending_extension_manager.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
diff --git a/extensions/browser/permissions/active_tab_permission_granter.cc b/extensions/browser/permissions/active_tab_permission_granter.cc
index 70f85379a1..02e7c0eea1 100644
--- a/extensions/browser/permissions/active_tab_permission_granter.cc
+++ b/extensions/browser/permissions/active_tab_permission_granter.cc
@@ -7,6 +7,7 @@
 #include <set>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -84,7 +85,7 @@ void SendRendererMessageToProcesses(
   std::set<content::RenderProcessHost*> sent_to_hosts;
   for (content::RenderFrameHost* frame_host : frame_hosts) {
     content::RenderProcessHost* process_host = frame_host->GetProcess();
-    if (!sent_to_hosts.contains(process_host)) {
+    if (!base::Contains(sent_to_hosts, process_host)) {
       // Extension processes have to update the origin allowlists.
       renderer_message.Run(true, process_host);
       sent_to_hosts.insert(frame_host->GetProcess());
@@ -92,7 +93,7 @@ void SendRendererMessageToProcesses(
   }
   // If the tab wasn't one of those processes already updated (it likely
   // wasn't), update it. Tabs don't need to update the origin allowlist.
-  if (!sent_to_hosts.contains(tab_process)) {
+  if (!base::Contains(sent_to_hosts, tab_process)) {
     renderer_message.Run(false, tab_process);
   }
 }
diff --git a/extensions/browser/permissions/permissions_updater.cc b/extensions/browser/permissions/permissions_updater.cc
index b64b285cd3..0616f610c6 100644
--- a/extensions/browser/permissions/permissions_updater.cc
+++ b/extensions/browser/permissions/permissions_updater.cc
@@ -13,6 +13,7 @@
 #include "base/functional/callback_helpers.h"
 #include "base/memory/ref_counted.h"
 #include "base/no_destructor.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/values.h"
 #include "components/keyed_service/content/browser_context_keyed_service_shutdown_notifier_factory.h"
 #include "components/keyed_service/core/keyed_service_shutdown_notifier.h"
@@ -293,7 +294,7 @@ void PermissionsUpdater::GrantRuntimePermissions(
   // runtime host permissions feature is confined to when the experiment is
   // enabled.
   constexpr int permissions_store_mask =
-      std::to_underlying(PermissionsStore::kRuntimeGrantedPermissions);
+      base::to_underlying(PermissionsStore::kRuntimeGrantedPermissions);
   AddPermissionsImpl(extension, *active_permissions_to_add,
                      permissions_store_mask, permissions,
                      std::move(completion_callback));
@@ -315,7 +316,7 @@ void PermissionsUpdater::RevokeOptionalPermissions(
   // without prompting the user. They are always removed from the active set,
   // which is the set of permissions the extension currently requests.
   int permissions_store_mask =
-      std::to_underlying(PermissionsStore::kActivePermissions);
+      base::to_underlying(PermissionsStore::kActivePermissions);
   if (remove_type == RemoveType::kHard) {
     permissions_store_mask |= PermissionsStore::kGrantedPermissions |
                               PermissionsStore::kRuntimeGrantedPermissions;
@@ -438,7 +439,7 @@ void PermissionsUpdater::RevokeRuntimePermissions(
   // that when a user changes a site from permitted to non-permitted or vice
   // versa, and extension's specific stored permissions are unaffected.
   constexpr int permissions_store_mask =
-      std::to_underlying(PermissionsStore::kRuntimeGrantedPermissions);
+      base::to_underlying(PermissionsStore::kRuntimeGrantedPermissions);
   RemovePermissionsImpl(extension, std::move(new_active_permissions),
                         permissions, permissions_store_mask,
                         /*withhold_optional_permissions=*/true,
@@ -605,7 +606,7 @@ void PermissionsUpdater::AddPermissionsForTesting(
     const Extension& extension,
     const PermissionSet& permissions) {
   AddPermissionsImpl(extension, permissions,
-                     std::to_underlying(PermissionsStore::kNone), permissions,
+                     base::to_underlying(PermissionsStore::kNone), permissions,
                      base::DoNothing());
 }
 
diff --git a/extensions/browser/permissions/permissions_updater.h b/extensions/browser/permissions/permissions_updater.h
index aa775452b7..dcc3fab7f6 100644
--- a/extensions/browser/permissions/permissions_updater.h
+++ b/extensions/browser/permissions/permissions_updater.h
@@ -11,6 +11,7 @@
 #include "base/functional/callback_forward.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/scoped_refptr.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "extensions/browser/extension_event_histogram_value.h"
 #include "extensions/buildflags/buildflags.h"
 
@@ -44,13 +45,13 @@ class PermissionsUpdater {
 
   // Enable enum class bitmasking.
   friend constexpr int operator|(InitFlag lhs, InitFlag rhs) {
-    return std::to_underlying(lhs) | std::to_underlying(rhs);
+    return base::to_underlying(lhs) | base::to_underlying(rhs);
   }
   friend constexpr int operator|(int lhs, InitFlag rhs) {
-    return lhs | std::to_underlying(rhs);
+    return lhs | base::to_underlying(rhs);
   }
   friend constexpr bool operator&(InitFlag lhs, InitFlag rhs) {
-    return std::to_underlying(lhs) & std::to_underlying(rhs);
+    return base::to_underlying(lhs) & base::to_underlying(rhs);
   }
 
   enum class RemoveType {
@@ -186,13 +187,13 @@ class PermissionsUpdater {
 
   // Enable enum class bitmasking.
   friend constexpr int operator|(PermissionsStore lhs, PermissionsStore rhs) {
-    return std::to_underlying(lhs) | std::to_underlying(rhs);
+    return base::to_underlying(lhs) | base::to_underlying(rhs);
   }
   friend constexpr int operator|(int lhs, PermissionsStore rhs) {
-    return lhs | std::to_underlying(rhs);
+    return lhs | base::to_underlying(rhs);
   }
   friend constexpr bool operator&(int lhs, PermissionsStore rhs) {
-    return lhs & std::to_underlying(rhs);
+    return lhs & base::to_underlying(rhs);
   }
 
   // Issues the relevant events, messages and notifications when the
diff --git a/extensions/browser/permissions_manager.cc b/extensions/browser/permissions_manager.cc
index a12d4d9045..91280e088a 100644
--- a/extensions/browser/permissions_manager.cc
+++ b/extensions/browser/permissions_manager.cc
@@ -8,6 +8,7 @@
 #include <memory>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/no_destructor.h"
 #include "base/observer_list.h"
@@ -353,7 +354,7 @@ void PermissionsManager::UpdateUserSiteSetting(const url::Origin& origin,
 }
 
 void PermissionsManager::AddUserRestrictedSite(const url::Origin& origin) {
-  if (user_permissions_.restricted_sites.contains(origin)) {
+  if (base::Contains(user_permissions_.restricted_sites, origin)) {
     return;
   }
 
@@ -375,7 +376,7 @@ void PermissionsManager::AddUserPermittedSite(const url::Origin& origin) {
   DCHECK(base::FeatureList::IsEnabled(
       extensions_features::kExtensionsMenuAccessControlWithPermittedSites));
 
-  if (user_permissions_.permitted_sites.contains(origin)) {
+  if (base::Contains(user_permissions_.permitted_sites, origin)) {
     return;
   }
 
@@ -433,10 +434,10 @@ PermissionsManager::GetUserPermissionsSettings() const {
 
 PermissionsManager::UserSiteSetting PermissionsManager::GetUserSiteSetting(
     const url::Origin& origin) const {
-  if (user_permissions_.permitted_sites.contains(origin)) {
+  if (base::Contains(user_permissions_.permitted_sites, origin)) {
     return UserSiteSetting::kGrantAllExtensions;
   }
-  if (user_permissions_.restricted_sites.contains(origin)) {
+  if (base::Contains(user_permissions_.restricted_sites, origin)) {
     return UserSiteSetting::kBlockAllExtensions;
   }
   return UserSiteSetting::kCustomizeByExtension;
diff --git a/extensions/browser/process_manager.cc b/extensions/browser/process_manager.cc
index b34a9522e0..3f64c6b0d4 100644
--- a/extensions/browser/process_manager.cc
+++ b/extensions/browser/process_manager.cc
@@ -8,6 +8,7 @@
 #include <unordered_set>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -282,7 +283,7 @@ ProcessManager::FrameSet ProcessManager::GetRenderFrameHostsForExtension(
 
 bool ProcessManager::IsRenderFrameHostRegistered(
     content::RenderFrameHost* render_frame_host) {
-  return all_extension_frames_.contains(render_frame_host);
+  return base::Contains(all_extension_frames_, render_frame_host);
 }
 
 void ProcessManager::AddObserver(ProcessManagerObserver* observer) {
@@ -566,7 +567,7 @@ void ProcessManager::NetworkRequestDone(
   ExtensionHost* host = result->second;
   pending_network_requests_.erase(result);
 
-  if (!background_hosts_.contains(host)) {
+  if (!base::Contains(background_hosts_, host)) {
     return;
   }
 
@@ -673,7 +674,7 @@ void ProcessManager::CloseBackgroundHost(ExtensionHost* host) {
         mojom::ViewType::kExtensionBackgroundPage);
   delete host;
   // |host| should deregister itself from our structures.
-  CHECK(!background_hosts_.contains(host));
+  CHECK(!base::Contains(background_hosts_, host));
 
   for (auto& observer : observer_list_)
     observer.OnBackgroundHostClose(extension_id);
@@ -1035,7 +1036,7 @@ void ProcessManager::OnExtensionHostDestroyed(ExtensionHost* host) {
   TRACE_EVENT0("browser,startup", "ProcessManager::OnExtensionHostDestroyed");
   host->RemoveObserver(this);
 
-  DCHECK(background_hosts_.contains(host));
+  DCHECK(base::Contains(background_hosts_, host));
   background_hosts_.erase(host);
   // Note: |host->extension()| may be null at this point.
   ClearBackgroundPageData(host->extension_id());
diff --git a/extensions/browser/process_map.cc b/extensions/browser/process_map.cc
index 3059d914ba..1ab9c0a38d 100644
--- a/extensions/browser/process_map.cc
+++ b/extensions/browser/process_map.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <tuple>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/types/optional_util.h"
 #include "components/guest_view/buildflags/buildflags.h"
@@ -97,7 +98,7 @@ bool ProcessMap::Contains(const ExtensionId& extension_id_in,
 }
 
 bool ProcessMap::Contains(int process_id) const {
-  return items_.contains(process_id);
+  return base::Contains(items_, process_id);
 }
 
 bool ProcessMap::ExtensionHasProcess(const ExtensionId& extension_id) const {
diff --git a/extensions/browser/renderer_startup_helper.cc b/extensions/browser/renderer_startup_helper.cc
index 639bef4b7f..9f49e98abe 100644
--- a/extensions/browser/renderer_startup_helper.cc
+++ b/extensions/browser/renderer_startup_helper.cc
@@ -10,6 +10,7 @@
 
 #include "base/check_is_test.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
@@ -370,8 +371,8 @@ void RendererStartupHelper::InitializeProcess(
       ExtensionRegistry::Get(browser_context_)->enabled_extensions();
   for (const auto& ext : extensions) {
     // OnExtensionLoaded should have already been called for the extension.
-    DCHECK(extension_process_map_.contains(ext->id()));
-    DCHECK(!extension_process_map_[ext->id()].contains(process));
+    DCHECK(base::Contains(extension_process_map_, ext->id()));
+    DCHECK(!base::Contains(extension_process_map_[ext->id()], process));
 
     if (!util::IsExtensionVisibleToContext(*ext, renderer_context)) {
       continue;
@@ -399,8 +400,8 @@ void RendererStartupHelper::InitializeProcess(
     for (const ExtensionId& id : iter->second) {
       // The extension should be loaded in the process.
       DCHECK(extensions.Contains(id));
-      DCHECK(extension_process_map_.contains(id));
-      DCHECK(extension_process_map_[id].contains(process));
+      DCHECK(base::Contains(extension_process_map_, id));
+      DCHECK(base::Contains(extension_process_map_[id], process));
       renderer->ActivateExtension(id);
     }
   }
@@ -427,7 +428,7 @@ void RendererStartupHelper::ActivateExtensionInProcess(
     content::RenderProcessHost* process) {
   // The extension should have been loaded already. Dump without crashing to
   // debug crbug.com/528026.
-  if (!extension_process_map_.contains(extension.id())) {
+  if (!base::Contains(extension_process_map_, extension.id())) {
     DUMP_WILL_BE_NOTREACHED()
         << "Extension " << extension.id() << " activated before loading";
     return;
@@ -458,7 +459,7 @@ void RendererStartupHelper::ActivateExtensionInProcess(
 
   auto remote = process_mojo_map_.find(process);
   if (remote != process_mojo_map_.end()) {
-    DCHECK(extension_process_map_[extension.id()].contains(process));
+    DCHECK(base::Contains(extension_process_map_[extension.id()], process));
     remote->second->ActivateExtension(extension.id());
   } else {
     pending_active_extensions_[process].insert(extension.id());
@@ -466,7 +467,7 @@ void RendererStartupHelper::ActivateExtensionInProcess(
 }
 
 void RendererStartupHelper::OnExtensionLoaded(const Extension& extension) {
-  DCHECK(!extension_process_map_.contains(extension.id()));
+  DCHECK(!base::Contains(extension_process_map_, extension.id()));
 
   // Mark the extension as loaded.
   std::set<raw_ptr<content::RenderProcessHost, SetExperimental>>&
@@ -502,7 +503,7 @@ void RendererStartupHelper::OnExtensionLoaded(const Extension& extension) {
 }
 
 void RendererStartupHelper::OnExtensionUnloaded(const Extension& extension) {
-  DCHECK(extension_process_map_.contains(extension.id()));
+  DCHECK(base::Contains(extension_process_map_, extension.id()));
 
   const std::set<raw_ptr<content::RenderProcessHost, SetExperimental>>&
       loaded_process_set = extension_process_map_[extension.id()];
diff --git a/extensions/browser/script_executor.cc b/extensions/browser/script_executor.cc
index 51c4f9478c..622214ca9a 100644
--- a/extensions/browser/script_executor.cc
+++ b/extensions/browser/script_executor.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/functional/bind.h"
 #include "base/hash/hash.h"
@@ -72,7 +73,7 @@ class Handler : public content::WebContentsObserver {
         continue;
       }
 
-      DCHECK(!std::ranges::contains(pending_render_frames_, frame));
+      DCHECK(!base::Contains(pending_render_frames_, frame));
       if (!frame->IsRenderFrameLive()) {
         ExtensionApiFrameIdMap::DocumentId document_id =
             ExtensionApiFrameIdMap::GetDocumentId(frame);
@@ -192,7 +193,7 @@ class Handler : public content::WebContentsObserver {
 #endif  // BUILDFLAG(ENABLE_PDF)
 
     if (!frame->IsRenderFrameLive() ||
-        std::ranges::contains(pending_render_frames_, frame)) {
+        base::Contains(pending_render_frames_, frame)) {
       return content::RenderFrameHost::FrameIterationAction::kContinue;
     }
 
@@ -212,7 +213,7 @@ class Handler : public content::WebContentsObserver {
     ScriptExecutor::FrameResult result;
     result.frame_id = frame_id;
     result.document_id = ExtensionApiFrameIdMap::GetDocumentId(frame);
-    DCHECK(!results_.contains(frame->GetFrameToken()));
+    DCHECK(!base::Contains(results_, frame->GetFrameToken()));
     results_[frame->GetFrameToken()] = std::move(result);
   }
 
@@ -243,7 +244,7 @@ class Handler : public content::WebContentsObserver {
 
   ScriptExecutor::FrameResult& GetFrameResult(
       const blink::LocalFrameToken& frame_token) {
-    DCHECK(results_.contains(frame_token));
+    DCHECK(base::Contains(results_, frame_token));
     return results_[frame_token];
   }
 
@@ -253,7 +254,7 @@ class Handler : public content::WebContentsObserver {
                        mojom::ExecuteCodeParamsPtr params,
                        content::RenderFrameHost* frame) {
     DCHECK(frame->IsRenderFrameLive());
-    DCHECK(std::ranges::contains(pending_render_frames_, frame));
+    DCHECK(base::Contains(pending_render_frames_, frame));
 
     if (params->injection->is_js()) {
       ScriptInjectionTracker::ScriptType script_type =
diff --git a/extensions/browser/script_injection_tracker.cc b/extensions/browser/script_injection_tracker.cc
index 748c401233..8331e06da9 100644
--- a/extensions/browser/script_injection_tracker.cc
+++ b/extensions/browser/script_injection_tracker.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/memory/ptr_util.h"
 #include "base/memory/raw_ref.h"
@@ -99,7 +100,7 @@ class RenderProcessHostUserData : public base::SupportsUserData::Data {
 
   bool HasScript(ScriptInjectionTracker::ScriptType script_type,
                  const ExtensionId& extension_id) const {
-    return GetScripts(script_type).contains(extension_id);
+    return base::Contains(GetScripts(script_type), extension_id);
   }
 
   void AddScript(ScriptInjectionTracker::ScriptType script_type,
diff --git a/extensions/browser/scripting_utils.cc b/extensions/browser/scripting_utils.cc
index b5849e4c09..3387b8101a 100644
--- a/extensions/browser/scripting_utils.cc
+++ b/extensions/browser/scripting_utils.cc
@@ -4,8 +4,6 @@
 
 #include "extensions/browser/scripting_utils.h"
 
-#include <algorithm>
-
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "content/public/browser/browser_context.h"
@@ -316,7 +314,7 @@ bool RemoveScripts(
     // `existing_script_ids`.
     std::string id_with_prefix =
         scripting::AddPrefixToDynamicScriptId(id, source);
-    if (!existing_script_ids.contains(id_with_prefix)) {
+    if (!base::Contains(existing_script_ids, id_with_prefix)) {
       *error =
           ErrorUtils::FormatErrorMessage(kNonExistentScriptIdError, id.c_str());
       return false;
@@ -495,8 +493,8 @@ bool GetFileResources(const std::vector<std::string>& files,
     }
 
     // ExtensionResource doesn't implement an operator==.
-    if (std::ranges::contains(resources, resource.relative_path(),
-                              &ExtensionResource::relative_path)) {
+    if (base::Contains(resources, resource.relative_path(),
+                       &ExtensionResource::relative_path)) {
       // Disallow duplicates. Note that we could allow this, if we wanted (and
       // there *might* be reason to with JS injection, to perform an operation
       // twice?). However, this matches content script behavior, and injecting
diff --git a/extensions/browser/scripting_utils.h b/extensions/browser/scripting_utils.h
index edc0ca67d2..c44f75b87a 100644
--- a/extensions/browser/scripting_utils.h
+++ b/extensions/browser/scripting_utils.h
@@ -7,6 +7,7 @@
 
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_forward.h"
 #include "base/strings/utf_string_conversions.h"
 #include "extensions/browser/extension_user_script_loader.h"
diff --git a/extensions/browser/service_worker/service_worker_task_queue.cc b/extensions/browser/service_worker/service_worker_task_queue.cc
index 7154f60c12..33c8e8253a 100644
--- a/extensions/browser/service_worker/service_worker_task_queue.cc
+++ b/extensions/browser/service_worker/service_worker_task_queue.cc
@@ -12,6 +12,7 @@
 
 #include "base/auto_reset.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
@@ -20,6 +21,7 @@
 #include "base/strings/utf_string_conversions.h"
 #include "base/syslog_logging.h"
 #include "base/timer/timer.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "content/public/browser/browser_context.h"
 #include "content/public/browser/browser_task_traits.h"
 #include "content/public/browser/browser_thread.h"
@@ -294,7 +296,7 @@ void ServiceWorkerTaskQueue::AddPendingTask(
       lazy_context_id.extension_id(),
       lazy_context_id.browser_context()->UniqueId(), *activation_token};
 
-  if (!worker_registered_.contains(context_id)) {
+  if (!base::Contains(worker_registered_, context_id)) {
     // If the worker hasn't finished registration, wait for it to complete. The
     // worker can't be started until a registration is found for it in the
     // //content layer. `DidRegisterServiceWorker()` will start the worker to
@@ -356,7 +358,7 @@ void ServiceWorkerTaskQueue::ActivateExtension(const Extension* extension) {
   activation_tokens_[extension_id] = activation_token;
   const SequencedContextId context_id = {
       extension_id, browser_context_->UniqueId(), activation_token};
-  DCHECK(!worker_state_map_.contains(context_id));
+  DCHECK(!base::Contains(worker_state_map_, context_id));
 
   content::ServiceWorkerContext* service_worker_context =
       GetServiceWorkerContext(extension->id());
@@ -380,7 +382,7 @@ void ServiceWorkerTaskQueue::ActivateExtension(const Extension* extension) {
                                          !service_worker_already_registered);
   }
 
-  DCHECK(!worker_registered_.contains(context_id));
+  DCHECK(!base::Contains(worker_registered_, context_id));
   if (service_worker_already_registered) {
     worker_registered_.insert(context_id);
     VerifyRegistration(service_worker_context, context_id, extension->url());
@@ -670,8 +672,9 @@ void ServiceWorkerTaskQueue::RetryRegisterServiceWorker(
   // Ensure the activation is still current.
   if (!IsCurrentActivation(context_id.extension_id, context_id.token)) {
     // NOTE: retry state has been cleared when `DeactivateExtension` was called.
-    DCHECK(!worker_registration_retries_.contains(context_id.token));
-    DCHECK(!worker_unregistration_wait_retries_.contains(context_id.token));
+    DCHECK(!base::Contains(worker_registration_retries_, context_id.token));
+    DCHECK(
+        !base::Contains(worker_unregistration_wait_retries_, context_id.token));
     return;
   }
 
@@ -696,7 +699,7 @@ void ServiceWorkerTaskQueue::RetryStartWorker(
   // Ensure the activation is still current.
   if (!IsCurrentActivation(context_id.extension_id, context_id.token)) {
     // NOTE: retry state has been cleared when `DeactivateExtension` was called.
-    DCHECK(!worker_start_retries_.contains(context_id.token));
+    DCHECK(!base::Contains(worker_start_retries_, context_id.token));
     return;
   }
   ServiceWorkerState* worker_state = GetWorkerState(context_id);
@@ -1008,7 +1011,7 @@ void ServiceWorkerTaskQueue::DidUnregisterServiceWorker(
   if (!success) {
     LOG(ERROR) << "Failed to unregister service worker for extension id: "
                << extension_id
-               << " error status was: " << std::to_underlying(status);
+               << " error status was: " << base::to_underlying(status);
     base::UmaHistogramEnumeration(
         "Extensions.ServiceWorkerBackground.WorkerUnregistrationFailureStatus",
         status);
@@ -1214,7 +1217,7 @@ bool ServiceWorkerTaskQueue::IsWorkerRegistered(
   }
   const SequencedContextId context_id = {
       extension_id, browser_context_->UniqueId(), *activation_token};
-  return worker_registered_.contains(context_id);
+  return base::Contains(worker_registered_, context_id);
 }
 
 size_t ServiceWorkerTaskQueue::GetNumPendingTasksForTest(
diff --git a/extensions/browser/state_store_test_observer.cc b/extensions/browser/state_store_test_observer.cc
index d38c284b6c..425c7e513b 100644
--- a/extensions/browser/state_store_test_observer.cc
+++ b/extensions/browser/state_store_test_observer.cc
@@ -4,6 +4,7 @@
 
 #include "extensions/browser/state_store_test_observer.h"
 
+#include "base/containers/contains.h"
 #include "content/public/browser/browser_thread.h"
 #include "extensions/browser/extension_system.h"
 #include "extensions/common/extension_id.h"
@@ -24,7 +25,7 @@ void StateStoreTestObserver::WaitForExtensionAndKey(
 
   waiting_for_map_key_ = std::make_pair(extension_id, key);
   // If the key has already been reported, flush the state store.
-  if (ids_and_keys_with_writes_.contains(waiting_for_map_key_)) {
+  if (base::Contains(ids_and_keys_with_writes_, waiting_for_map_key_)) {
     state_store_->FlushForTesting(run_loop_.QuitWhenIdleClosure());
   }
   run_loop_.Run();
diff --git a/extensions/browser/test_blocklist.cc b/extensions/browser/test_blocklist.cc
index 3d74ce176d..7bc43bf309 100644
--- a/extensions/browser/test_blocklist.cc
+++ b/extensions/browser/test_blocklist.cc
@@ -6,6 +6,7 @@
 
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/run_loop.h"
 #include "base/task/single_thread_task_runner.h"
@@ -34,7 +35,7 @@ void BlocklistStateFetcherMock::Request(const std::string& id,
   ++request_count_;
 
   BlocklistState result = NOT_BLOCKLISTED;
-  if (states_.contains(id)) {
+  if (base::Contains(states_, id)) {
     result = states_[id];
   }
 
diff --git a/extensions/browser/test_blocklist_state_fetcher.cc b/extensions/browser/test_blocklist_state_fetcher.cc
index 8fa673a790..64c2b31f4d 100644
--- a/extensions/browser/test_blocklist_state_fetcher.cc
+++ b/extensions/browser/test_blocklist_state_fetcher.cc
@@ -4,6 +4,7 @@
 
 #include "extensions/browser/test_blocklist_state_fetcher.h"
 
+#include "base/containers/contains.h"
 #include "components/safe_browsing/core/browser/db/v4_test_util.h"
 #include "mojo/public/cpp/bindings/pending_receiver.h"
 #include "mojo/public/cpp/bindings/pending_remote.h"
@@ -81,7 +82,7 @@ bool TestBlocklistStateFetcher::HandleFetcher(const std::string& id) {
   }
 
   ClientCRXListInfoResponse response;
-  if (verdicts_.contains(id)) {
+  if (base::Contains(verdicts_, id)) {
     response.set_verdict(verdicts_[id]);
   } else {
     response.set_verdict(ClientCRXListInfoResponse::NOT_IN_BLOCKLIST);
diff --git a/extensions/browser/test_event_router_observer.cc b/extensions/browser/test_event_router_observer.cc
index e5653a76f4..1cafedbff3 100644
--- a/extensions/browser/test_event_router_observer.cc
+++ b/extensions/browser/test_event_router_observer.cc
@@ -24,7 +24,7 @@ void TestEventRouterObserver::ClearEvents() {
 }
 
 void TestEventRouterObserver::WaitForEventWithName(const std::string& name) {
-  while (!events_.contains(name)) {
+  while (!base::Contains(events_, name)) {
     // Create a new `RunLoop` since reuse is not supported.
     run_loop_ = std::make_unique<base::RunLoop>();
     run_loop_->Run();
@@ -34,7 +34,7 @@ void TestEventRouterObserver::WaitForEventWithName(const std::string& name) {
 
 void TestEventRouterObserver::WaitForDispatchedEventWithName(
     const std::string& name) {
-  while (!dispatched_events_.contains(name)) {
+  while (!base::Contains(dispatched_events_, name)) {
     // Create a new `RunLoop` since reuse is not supported.
     run_loop_ = std::make_unique<base::RunLoop>();
     run_loop_->Run();
diff --git a/extensions/browser/updater/extension_cache_fake.cc b/extensions/browser/updater/extension_cache_fake.cc
index aac52400f7..0d7bb007ea 100644
--- a/extensions/browser/updater/extension_cache_fake.cc
+++ b/extensions/browser/updater/extension_cache_fake.cc
@@ -4,6 +4,7 @@
 
 #include "extensions/browser/updater/extension_cache_fake.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "content/public/browser/browser_task_traits.h"
 #include "content/public/browser/browser_thread.h"
@@ -49,7 +50,7 @@ void ExtensionCacheFake::PutExtension(const std::string& id,
                                       const base::FilePath& file_path,
                                       const std::string& version,
                                       PutExtensionCallback callback) {
-  if (allowed_extensions_.contains(id)) {
+  if (base::Contains(allowed_extensions_, id)) {
     cache_[id].first = version;
     cache_[id].second = file_path;
     content::GetUIThreadTaskRunner({})->PostTask(
diff --git a/extensions/browser/updater/manifest_fetch_data.cc b/extensions/browser/updater/manifest_fetch_data.cc
index 931fc6e64b..29ca7d8ff9 100644
--- a/extensions/browser/updater/manifest_fetch_data.cc
+++ b/extensions/browser/updater/manifest_fetch_data.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "base/strings/escape.h"
 #include "base/strings/string_number_conversions.h"
@@ -146,7 +147,7 @@ bool ManifestFetchData::AddExtension(const std::string& id,
                                      DownloadFetchPriority fetch_priority) {
   DCHECK(!is_all_external_policy_download_ ||
          extension_location == ManifestLocation::kExternalPolicyDownload);
-  if (extensions_data_.contains(id)) {
+  if (base::Contains(extensions_data_, id)) {
     NOTREACHED() << "Duplicate extension id " << id;
   }
 
@@ -267,7 +268,7 @@ ExtensionIdSet ManifestFetchData::GetExtensionIds() const {
 }
 
 bool ManifestFetchData::Includes(const ExtensionId& extension_id) const {
-  return extensions_data_.contains(extension_id);
+  return base::Contains(extensions_data_, extension_id);
 }
 
 bool ManifestFetchData::DidPing(const ExtensionId& extension_id,
diff --git a/extensions/browser/user_script_manager.cc b/extensions/browser/user_script_manager.cc
index 3fb2dd67e7..056adce94b 100644
--- a/extensions/browser/user_script_manager.cc
+++ b/extensions/browser/user_script_manager.cc
@@ -4,6 +4,7 @@
 
 #include "extensions/browser/user_script_manager.h"
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/one_shot_event.h"
 #include "content/public/browser/browser_context.h"
@@ -243,7 +244,7 @@ void UserScriptManager::RemovePendingExtensionLoadAndSignal(
 
 ExtensionUserScriptLoader* UserScriptManager::CreateExtensionUserScriptLoader(
     const Extension* extension) {
-  CHECK(!extension_script_loaders_.contains(extension->id()));
+  CHECK(!base::Contains(extension_script_loaders_, extension->id()));
   // Inserts a new ExtensionUserScriptLoader and returns a ptr to it.
   ExtensionUserScriptLoader* loader =
       extension_script_loaders_
@@ -261,7 +262,7 @@ ExtensionUserScriptLoader* UserScriptManager::CreateExtensionUserScriptLoader(
 
 EmbedderUserScriptLoader* UserScriptManager::CreateEmbedderUserScriptLoader(
     const mojom::HostID& host_id) {
-  CHECK(!embedder_script_loaders_.contains(host_id));
+  CHECK(!base::Contains(embedder_script_loaders_, host_id));
   // Inserts a new EmbedderUserScriptLoader and returns a ptr to it.
   EmbedderUserScriptLoader* loader =
       embedder_script_loaders_
diff --git a/extensions/browser/verified_contents.cc b/extensions/browser/verified_contents.cc
index 7b21bfd38b..c743e85163 100644
--- a/extensions/browser/verified_contents.cc
+++ b/extensions/browser/verified_contents.cc
@@ -10,6 +10,7 @@
 #include <string_view>
 
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/json/json_reader.h"
 #include "base/logging.h"
@@ -198,7 +199,8 @@ std::unique_ptr<VerifiedContents> VerifiedContents::Create(
 
 bool VerifiedContents::HasTreeHashRoot(
     const base::FilePath& relative_path) const {
-  return root_hashes_.contains(
+  return base::Contains(
+      root_hashes_,
       content_verifier_utils::CanonicalizeRelativePath(relative_path));
 }
 
diff --git a/extensions/browser/zipfile_installer.cc b/extensions/browser/zipfile_installer.cc
index b1aac444c4..fbe9a9f8c2 100644
--- a/extensions/browser/zipfile_installer.cc
+++ b/extensions/browser/zipfile_installer.cc
@@ -4,10 +4,10 @@
 
 #include "extensions/browser/zipfile_installer.h"
 
-#include <algorithm>
 #include <optional>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -232,7 +232,7 @@ bool ZipFileInstaller::ShouldExtractFile(bool is_theme,
     if (extension.empty()) {
       return true;
     }
-    return std::ranges::contains(kAllowedThemeFiletypes, extension);
+    return base::Contains(kAllowedThemeFiletypes, extension);
   }
   return !base::FilePath::CompareEqualIgnoreCase(file_path.FinalExtension(),
                                                  FILE_PATH_LITERAL(".exe"));
diff --git a/extensions/common/constants.cc b/extensions/common/constants.cc
index 3b8b107427..124967c55e 100644
--- a/extensions/common/constants.cc
+++ b/extensions/common/constants.cc
@@ -6,6 +6,7 @@
 
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "build/build_config.h"
 #include "build/chromecast_buildflags.h"
@@ -74,7 +75,7 @@ bool IsDemoModeChromeApp(std::string_view extension_id) {
       kBlazeyHighlightsAppId
       // clang-format on
   });
-  return kDemoModeApps.contains(extension_id);
+  return base::Contains(kDemoModeApps, extension_id);
 }
 
 bool IsSystemUIApp(std::string_view extension_id) {
@@ -86,7 +87,7 @@ bool IsSystemUIApp(std::string_view extension_id) {
       kScreensaverAppId,
       // clang-format on
   });
-  return kApps.contains(extension_id);
+  return base::Contains(kApps, extension_id);
 }
 
 #endif  // BUILDFLAG(IS_CHROMEOS)
@@ -99,7 +100,7 @@ bool IsQuickOfficeExtension(std::string_view extension_id) {
       kQuickOfficeInternalExtensionId,
       kQuickOfficeExtensionId,
   });
-  return kQuickOfficeIds.contains(extension_id);
+  return base::Contains(kQuickOfficeIds, extension_id);
 }
 
 // TODO(crbug.com/40796281): remove after default app migration is done.
@@ -114,7 +115,7 @@ bool IsPreinstalledAppId(std::string_view app_id) {
       kYoutubeAppId,
       // clang-format on
   });
-  return kPreinstalledApps.contains(app_id);
+  return base::Contains(kPreinstalledApps, app_id);
 }
 
 }  // namespace extension_misc
diff --git a/extensions/common/content_script_injection_url_getter.cc b/extensions/common/content_script_injection_url_getter.cc
index 664b0b84d7..11af719230 100644
--- a/extensions/common/content_script_injection_url_getter.cc
+++ b/extensions/common/content_script_injection_url_getter.cc
@@ -4,8 +4,7 @@
 
 #include "extensions/common/content_script_injection_url_getter.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/notreached.h"
 #include "base/trace_event/typed_macros.h"
@@ -61,8 +60,8 @@ GURL ContentScriptInjectionUrlGetter::Get(
         TRACE_EVENT_INSTANT("extensions",
                             "ContentScriptInjectionUrlGetter::Get/"
                             "should_consider_origin: origin-always");
-        result = std::ranges::contains(kAllowedSchemesToMatchOriginAsFallback,
-                                       document_url.GetScheme());
+        result = base::Contains(kAllowedSchemesToMatchOriginAsFallback,
+                                document_url.GetScheme());
         break;
       }
     }
@@ -166,7 +165,8 @@ GURL ContentScriptInjectionUrlGetter::Get(
 
     // Avoid an infinite loop - see https://crbug.com/568432 and
     // https://crbug.com/883526.
-    if (already_visited_frame_ids.contains(parent_context_data->GetId())) {
+    if (base::Contains(already_visited_frame_ids,
+                       parent_context_data->GetId())) {
       TRACE_EVENT_INSTANT("extensions",
                           "ContentScriptInjectionUrlGetter::Get/infinite-loop");
       return document_url;
diff --git a/extensions/common/csp_validator.cc b/extensions/common/csp_validator.cc
index f5eeb03f16..007ba42a23 100644
--- a/extensions/common/csp_validator.cc
+++ b/extensions/common/csp_validator.cc
@@ -15,6 +15,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -192,7 +193,7 @@ bool isNonWildcardTLD(const std::string& url,
 
   std::string host(url, start_of_host, end_of_host - start_of_host);
   // Global wildcards are not allowed.
-  if (host.empty() || host.contains("*")) {
+  if (host.empty() || base::Contains(host, "*")) {
     return false;
   }
 
diff --git a/extensions/common/extension.cc b/extensions/common/extension.cc
index 25f6860b1d..4997b4e30b 100644
--- a/extensions/common/extension.cc
+++ b/extensions/common/extension.cc
@@ -15,6 +15,7 @@
 
 #include "base/base64.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/files/file_path.h"
 #include "base/i18n/rtl.h"
@@ -80,7 +81,7 @@ bool ContainsReservedCharacters(const base::FilePath& path) {
   // Extensions are cross-platform.
   // Since FilePath uses backslash '\\' as file path separator on Windows, so we
   // need to check manually.
-  if (path.value().contains('\\')) {
+  if (base::Contains(path.value(), '\\')) {
     return true;
   }
   return !net::IsSafePortableRelativePath(path);
@@ -770,7 +771,7 @@ bool Extension::LoadExtent(const char* key,
 
     // We do not allow authors to put wildcards in their paths. Instead, we
     // imply one at the end.
-    if (pattern.path().contains('*')) {
+    if (base::Contains(pattern.path(), '*')) {
       *error = ErrorUtils::FormatErrorMessageUTF16(
           value_error, base::NumberToString(i), errors::kNoWildCardsInPaths);
       return false;
diff --git a/extensions/common/extension_api.cc b/extensions/common/extension_api.cc
index ef26446a8e..1c1f4e1851 100644
--- a/extensions/common/extension_api.cc
+++ b/extensions/common/extension_api.cc
@@ -13,6 +13,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/json/json_reader.h"
 #include "base/json/json_writer.h"
 #include "base/strings/span_printf.h"
@@ -296,7 +297,7 @@ std::string ExtensionAPI::GetAPINameFromFullName(std::string_view full_name,
 bool ExtensionAPI::IsKnownAPI(const std::string& name,
                               ExtensionsClient* client) {
   lock_.AssertAcquired();
-  return schemas_.contains(name) || client->IsAPISchemaGenerated(name);
+  return base::Contains(schemas_, name) || client->IsAPISchemaGenerated(name);
 }
 
 Feature::Availability ExtensionAPI::IsAliasAvailable(
diff --git a/extensions/common/extension_l10n_util.cc b/extensions/common/extension_l10n_util.cc
index 66234b98a5..3d2b8e0dc1 100644
--- a/extensions/common/extension_l10n_util.cc
+++ b/extensions/common/extension_l10n_util.cc
@@ -13,6 +13,7 @@
 #include <vector>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/containers/extend.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_util.h"
@@ -387,7 +388,7 @@ bool AddLocale(const std::set<std::string>& chrome_locales,
   // locales.
   if (base::StartsWith(locale_name, ".", base::CompareCase::SENSITIVE))
     return true;
-  if (!chrome_locales.contains(locale_name)) {
+  if (!base::Contains(chrome_locales, locale_name)) {
     // Warn if there is an extension locale that's not in the Chrome list,
     // but don't fail.
     DLOG(WARNING) << base::StringPrintf("Supplied locale %s is not supported.",
@@ -572,7 +573,7 @@ bool ShouldSkipValidation(const base::FilePath& locales_path,
   if (subdir.empty())
     return true;  // Non-ASCII.
 
-  if (subdir.contains('.'))
+  if (base::Contains(subdir, '.'))
     return true;
 
   // On case-insensitive file systems we will load messages by matching them
diff --git a/extensions/common/extension_set.cc b/extensions/common/extension_set.cc
index 2b9fa06987..a6d05320c2 100644
--- a/extensions/common/extension_set.cc
+++ b/extensions/common/extension_set.cc
@@ -4,6 +4,7 @@
 
 #include "extensions/common/extension_set.h"
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "extensions/common/constants.h"
 #include "extensions/common/url_pattern_set.h"
@@ -52,7 +53,7 @@ ExtensionSet::ExtensionSet(ExtensionSet&&) = default;
 ExtensionSet& ExtensionSet::operator=(ExtensionSet&&) noexcept = default;
 
 bool ExtensionSet::Contains(const ExtensionId& extension_id) const {
-  return extensions_.contains(extension_id);
+  return base::Contains(extensions_, extension_id);
 }
 
 bool ExtensionSet::Insert(const scoped_refptr<const Extension>& extension) {
diff --git a/extensions/common/features/simple_feature.cc b/extensions/common/features/simple_feature.cc
index 1dc58bfe74..7bfddd77cc 100644
--- a/extensions/common/features/simple_feature.cc
+++ b/extensions/common/features/simple_feature.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/no_destructor.h"
@@ -474,7 +475,7 @@ bool SimpleFeature::IsIdInList(const HashedExtensionId& hashed_id,
   if (!IsValidHashedExtensionId(hashed_id))
     return false;
 
-  return std::ranges::contains(list, hashed_id.value());
+  return base::Contains(list, hashed_id.value());
 }
 
 bool SimpleFeature::MatchesManifestLocation(
@@ -499,15 +500,14 @@ bool SimpleFeature::MatchesSessionTypes(
   if (session_types_.empty())
     return true;
 
-  if (std::ranges::contains(session_types_, session_type))
+  if (base::Contains(session_types_, session_type))
     return true;
 
   // AUTOLAUNCHED_KIOSK session type is subset of KIOSK - accept auto-lauched
   // kiosk session if kiosk session is allowed. This is the only exception to
   // rejecting session type that is not present in |session_types_|
   return session_type == mojom::FeatureSessionType::kAutolaunchedKiosk &&
-         std::ranges::contains(session_types_,
-                               mojom::FeatureSessionType::kKiosk);
+         base::Contains(session_types_, mojom::FeatureSessionType::kKiosk);
 }
 
 bool SimpleFeature::RequiresDelegatedAvailabilityCheck() const {
@@ -614,7 +614,7 @@ Feature::Availability SimpleFeature::GetEnvironmentAvailability(
     int context_id,
     bool check_developer_mode) const {
   base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (!platforms_.empty() && !std::ranges::contains(platforms_, platform))
+  if (!platforms_.empty() && !base::Contains(platforms_, platform))
     return CreateAvailability(AvailabilityResult::kInvalidPlatform);
 
   if (channel_ && *channel_ < GetCurrentChannel()) {
@@ -686,7 +686,7 @@ Feature::Availability SimpleFeature::GetManifestAvailability(
   Manifest::Type type_to_check =
       (type == Manifest::Type::kUserScript) ? Manifest::Type::kExtension : type;
   if (!extension_types_.empty() &&
-      !std::ranges::contains(extension_types_, type_to_check)) {
+      !base::Contains(extension_types_, type_to_check)) {
     return CreateAvailability(AvailabilityResult::kInvalidType, type);
   }
 
@@ -728,7 +728,7 @@ Feature::Availability SimpleFeature::GetContextAvailability(
   // extension API calls, since there's no guarantee that the extension is
   // "active" in current renderer process when the API permission check is
   // done.
-  if (contexts_ && !std::ranges::contains(*contexts_, context))
+  if (contexts_ && !base::Contains(*contexts_, context))
     return CreateAvailability(AvailabilityResult::kInvalidContext, context);
 
   // TODO(kalman): Consider checking |matches_| regardless of context type.
diff --git a/extensions/common/file_util.cc b/extensions/common/file_util.cc
index 7dc0ad50d0..ae74cacef5 100644
--- a/extensions/common/file_util.cc
+++ b/extensions/common/file_util.cc
@@ -15,6 +15,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
@@ -592,7 +593,8 @@ MessageBundle* LoadMessageBundle(
   extension_l10n_util::GetAllLocales(&chrome_locales);
 
   base::FilePath default_locale_path = locale_path.AppendASCII(default_locale);
-  if (default_locale.empty() || !chrome_locales.contains(default_locale) ||
+  if (default_locale.empty() ||
+      !base::Contains(chrome_locales, default_locale) ||
       !base::PathExists(default_locale_path)) {
     *error = l10n_util::GetStringUTF8(
         IDS_EXTENSION_LOCALES_NO_DEFAULT_LOCALE_SPECIFIED);
diff --git a/extensions/common/manifest.cc b/extensions/common/manifest.cc
index 23351c8d6d..96f57fd76d 100644
--- a/extensions/common/manifest.cc
+++ b/extensions/common/manifest.cc
@@ -4,11 +4,11 @@
 
 #include "extensions/common/manifest.h"
 
-#include <algorithm>
 #include <string_view>
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/ptr_util.h"
 #include "base/notreached.h"
@@ -310,7 +310,7 @@ void Manifest::ValidateManifest(std::vector<InstallWarning>* warnings) const {
     if (!manifest_feature_provider->GetFeature(item.first)) {
       // There are a set of keys that are not handled by Chrome, but that we
       // explicitly allow. Don't add a warning for those keys.
-      if (std::ranges::contains(keys::kIgnoredUnrecognizedKeys, item.first)) {
+      if (base::Contains(keys::kIgnoredUnrecognizedKeys, item.first)) {
         continue;
       }
 
diff --git a/extensions/common/manifest_handler.cc b/extensions/common/manifest_handler.cc
index c6abee1a0d..9106a1c32c 100644
--- a/extensions/common/manifest_handler.cc
+++ b/extensions/common/manifest_handler.cc
@@ -10,6 +10,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "extensions/common/extension.h"
 #include "extensions/common/manifest_handler_registry.h"
 #include "extensions/common/permissions/manifest_permission.h"
diff --git a/extensions/common/manifest_handlers/mime_types_handler.cc b/extensions/common/manifest_handlers/mime_types_handler.cc
index 7f29cad365..7fe3808d53 100644
--- a/extensions/common/manifest_handlers/mime_types_handler.cc
+++ b/extensions/common/manifest_handlers/mime_types_handler.cc
@@ -8,6 +8,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_util.h"
@@ -70,7 +71,7 @@ void MimeTypesHandler::AddMIMEType(const std::string& mime_type) {
 }
 
 bool MimeTypesHandler::CanHandleMIMEType(const std::string& mime_type) const {
-  return mime_type_set_.contains(mime_type);
+  return base::Contains(mime_type_set_, mime_type);
 }
 
 bool MimeTypesHandler::HasPlugin() const {
diff --git a/extensions/common/manifest_handlers/requirements_info.cc b/extensions/common/manifest_handlers/requirements_info.cc
index e4ca3dab19..5325c1e24d 100644
--- a/extensions/common/manifest_handlers/requirements_info.cc
+++ b/extensions/common/manifest_handlers/requirements_info.cc
@@ -4,9 +4,9 @@
 
 #include "extensions/common/manifest_handlers/requirements_info.h"
 
-#include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/strings/utf_string_conversions.h"
 #include "base/values.h"
 #include "extensions/common/api/requirements.h"
@@ -76,7 +76,7 @@ bool RequirementsHandler::Parse(Extension* extension, std::u16string* error) {
   if (requirements._3d) {
     // css3d is always available, so no check is needed, but no error is
     // generated.
-    requirements_info->webgl = std::ranges::contains(
+    requirements_info->webgl = base::Contains(
         requirements._3d->features, api::requirements::_3DFeature::kWebgl);
   }
 
diff --git a/extensions/common/manifest_handlers/shared_module_info.cc b/extensions/common/manifest_handlers/shared_module_info.cc
index 9223028228..91231ee29a 100644
--- a/extensions/common/manifest_handlers/shared_module_info.cc
+++ b/extensions/common/manifest_handlers/shared_module_info.cc
@@ -11,6 +11,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/lazy_instance.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_split.h"
@@ -101,7 +102,7 @@ bool SharedModuleInfo::IsExportAllowedByAllowlist(const Extension* extension,
   if (info.export_allowlist_.empty()) {
     return true;
   }
-  return info.export_allowlist_.contains(other_id);
+  return base::Contains(info.export_allowlist_, other_id);
 }
 
 // static
diff --git a/extensions/common/manifest_handlers/web_accessible_resources_info.cc b/extensions/common/manifest_handlers/web_accessible_resources_info.cc
index 719d252a3d..cef1a38d04 100644
--- a/extensions/common/manifest_handlers/web_accessible_resources_info.cc
+++ b/extensions/common/manifest_handlers/web_accessible_resources_info.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/utf_string_conversions.h"
@@ -241,7 +242,7 @@ bool IsResourceWebAccessibleImpl(
       if (initiator_url.SchemeIs(extensions::kExtensionScheme) &&
           (entry.allow_all_extensions ||
            extension.id() == initiator_url.GetHost() ||
-           entry.extension_ids.contains(initiator_url.GetHost()))) {
+           base::Contains(entry.extension_ids, initiator_url.GetHost()))) {
         return result;
       }
     }
diff --git a/extensions/common/manifest_test.cc b/extensions/common/manifest_test.cc
index 650e1fedb2..f85dc87baa 100644
--- a/extensions/common/manifest_test.cc
+++ b/extensions/common/manifest_test.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/json/json_file_value_serializer.h"
@@ -55,7 +56,7 @@ std::optional<base::DictValue> LoadManifestFile(
   // localize them, since their manifests don't have a default_locale key.
   // Only localize manifests that indicate they want to be localized.
   // Calling LocalizeExtension at this point mirrors file_util::LoadExtension.
-  if (manifest_path.value().contains(FILE_PATH_LITERAL("localized"))) {
+  if (base::Contains(manifest_path.value(), FILE_PATH_LITERAL("localized"))) {
     extension_l10n_util::LocalizeExtension(
         extension_path, manifest->GetIfDict(),
         extension_l10n_util::GzippedMessagesPermission::kDisallow, error);
diff --git a/extensions/common/message_bundle.cc b/extensions/common/message_bundle.cc
index e15d364ae6..2ab53cbf5d 100644
--- a/extensions/common/message_bundle.cc
+++ b/extensions/common/message_bundle.cc
@@ -9,6 +9,7 @@
 #include <vector>
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/i18n/rtl.h"
 #include "base/lazy_instance.h"
 #include "base/strings/string_util.h"
@@ -116,7 +117,7 @@ bool MessageBundle::AppendReservedMessagesForLocale(
   // Add all reserved messages to the dictionary, but check for collisions.
   auto it = append_messages.begin();
   for (; it != append_messages.end(); ++it) {
-    if (dictionary_.contains(it->first)) {
+    if (base::Contains(dictionary_, it->first)) {
       *error = ErrorUtils::FormatErrorMessage(
           errors::kReservedMessageFound, it->first);
       return false;
diff --git a/extensions/common/permissions/api_permission_set.cc b/extensions/common/permissions/api_permission_set.cc
index 84e5dd5d75..e207e9ab8e 100644
--- a/extensions/common/permissions/api_permission_set.cc
+++ b/extensions/common/permissions/api_permission_set.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/stl_util.h"
 #include "base/strings/string_number_conversions.h"
@@ -297,7 +298,7 @@ PermissionIDSet PermissionIDSet::GetAllPermissionsWithIDs(
     const std::set<APIPermissionID>& permission_ids) const {
   PermissionIDSet subset;
   for (const auto& permission : permissions_) {
-    if (permission_ids.contains(permission.id())) {
+    if (base::Contains(permission_ids, permission.id())) {
       subset.permissions_.insert(permission);
     }
   }
diff --git a/extensions/common/permissions/permissions_data.cc b/extensions/common/permissions/permissions_data.cc
index 5d465d71f2..cf509c8abc 100644
--- a/extensions/common/permissions/permissions_data.cc
+++ b/extensions/common/permissions/permissions_data.cc
@@ -4,11 +4,11 @@
 
 #include "extensions/common/permissions/permissions_data.h"
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/memory/stack_allocated.h"
 #include "base/no_destructor.h"
@@ -120,7 +120,7 @@ bool PermissionsData::CanExecuteScriptEverywhere(
   const ExtensionsClient::ScriptingAllowlist& allowlist =
       ExtensionsClient::Get()->GetScriptingAllowlist();
 
-  return std::ranges::contains(allowlist, extension_id);
+  return base::Contains(allowlist, extension_id);
 }
 
 bool PermissionsData::IsRestrictedUrl(const GURL& document_url,
diff --git a/extensions/common/permissions/permissions_info.cc b/extensions/common/permissions/permissions_info.cc
index 7d2fab1866..2dc17c4f09 100644
--- a/extensions/common/permissions/permissions_info.cc
+++ b/extensions/common/permissions/permissions_info.cc
@@ -5,6 +5,7 @@
 #include "extensions/common/permissions/permissions_info.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/strings/string_util.h"
 #include "extensions/common/alias.h"
@@ -72,15 +73,15 @@ PermissionsInfo::~PermissionsInfo() {
 }
 
 void PermissionsInfo::RegisterAlias(const Alias& alias) {
-  DCHECK(name_map_.contains(alias.real_name));
-  DCHECK(!name_map_.contains(alias.name));
+  DCHECK(base::Contains(name_map_, alias.real_name));
+  DCHECK(!base::Contains(name_map_, alias.name));
   name_map_[alias.name] = name_map_[alias.real_name];
 }
 
 void PermissionsInfo::RegisterPermission(
     std::unique_ptr<APIPermissionInfo> permission) {
-  DCHECK(!id_map_.contains(permission->id()));
-  DCHECK(!name_map_.contains(permission->name()));
+  DCHECK(!base::Contains(id_map_, permission->id()));
+  DCHECK(!base::Contains(name_map_, permission->name()));
 
   name_map_[permission->name()] = permission.get();
   id_map_[permission->id()] = std::move(permission);
diff --git a/extensions/common/url_pattern.cc b/extensions/common/url_pattern.cc
index 4054af7280..8975a936d6 100644
--- a/extensions/common/url_pattern.cc
+++ b/extensions/common/url_pattern.cc
@@ -10,6 +10,7 @@
 #include <ostream>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/strings/pattern.h"
 #include "base/strings/strcat.h"
 #include "base/strings/string_number_conversions.h"
@@ -325,7 +326,7 @@ URLPattern::ParseResult URLPattern::Parse(std::string_view pattern) {
   // No other '*' can occur in the host, though. This isn't necessary, but is
   // done as a convenience to developers who might otherwise be confused and
   // think '*' works as a glob in the host.
-  if (host_.contains('*')) {
+  if (base::Contains(host_, '*')) {
     return ParseResult::kInvalidHostWildcard;
   }
 
@@ -341,7 +342,7 @@ URLPattern::ParseResult URLPattern::Parse(std::string_view pattern) {
   }
 
   // Null characters are not allowed in hosts.
-  if (host_.contains('\0')) {
+  if (base::Contains(host_, '\0')) {
     return ParseResult::kInvalidHost;
   }
 
diff --git a/extensions/common/url_pattern_set.cc b/extensions/common/url_pattern_set.cc
index bdf25b893f..f089602aa8 100644
--- a/extensions/common/url_pattern_set.cc
+++ b/extensions/common/url_pattern_set.cc
@@ -7,6 +7,7 @@
 #include <iterator>
 #include <ostream>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/no_destructor.h"
 #include "base/stl_util.h"
@@ -282,9 +283,9 @@ bool URLPatternSet::OverlapsWith(const URLPatternSet& other) const {
 base::ListValue URLPatternSet::ToValue() const {
   base::ListValue result;
   for (const auto& pattern : patterns_) {
-    std::string pattern_str_value(pattern.GetAsString());
-    if (!result.contains(pattern_str_value)) {
-      result.Append(pattern_str_value);
+    base::Value pattern_str_value(pattern.GetAsString());
+    if (!base::Contains(result, pattern_str_value)) {
+      result.Append(std::move(pattern_str_value));
     }
   }
   return result;
diff --git a/extensions/renderer/api/messaging/native_renderer_messaging_service.cc b/extensions/renderer/api/messaging/native_renderer_messaging_service.cc
index c2782df12a..d13b5c1503 100644
--- a/extensions/renderer/api/messaging/native_renderer_messaging_service.cc
+++ b/extensions/renderer/api/messaging/native_renderer_messaging_service.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/raw_ptr.h"
@@ -119,7 +120,7 @@ class NativeRendererMessagingService::MessagePortScope
           port_host) {
     auto receiver_id =
         message_port_dispatchers_.Add(this, std::move(port), target_port_id);
-    CHECK(!message_port_hosts_.contains(target_port_id));
+    CHECK(!base::Contains(message_port_hosts_, target_port_id));
     auto& bound_port_host = message_port_hosts_[target_port_id] =
         mojo::AssociatedRemote(std::move(port_host));
     bound_port_host.set_disconnect_handler(
@@ -142,7 +143,7 @@ class NativeRendererMessagingService::MessagePortScope
         message_port_host_remote.InitWithNewEndpointAndPassReceiver();
     message_port_dispatchers_.Add(this, std::move(message_port_receiver),
                                   port_id);
-    CHECK(!message_port_hosts_.contains(port_id));
+    CHECK(!base::Contains(message_port_hosts_, port_id));
     auto& bound_port_host = message_port_hosts_[port_id] =
         mojo::AssociatedRemote(std::move(message_port_host_remote));
     bound_port_host.set_disconnect_handler(base::BindOnce(
@@ -183,7 +184,7 @@ class NativeRendererMessagingService::MessagePortScope
   }
 
   bool HasPort(const PortId& port_id) {
-    return message_port_hosts_.contains(port_id);
+    return base::Contains(message_port_hosts_, port_id);
   }
 
   mojom::MessagePortHost* GetMessagePortHost(const PortId& port_id) {
@@ -603,7 +604,7 @@ bool NativeRendererMessagingService::ContextHasMessagePort(
   v8::HandleScope handle_scope(script_context->isolate());
   MessagingPerContextData* data = GetPerContextData<MessagingPerContextData>(
       script_context->v8_context(), CreatePerContextData::kDontCreateIfMissing);
-  return data && data->ports.contains(port_id);
+  return data && base::Contains(data->ports, port_id);
 }
 
 void NativeRendererMessagingService::DispatchOnConnectToListeners(
@@ -796,7 +797,7 @@ GinPort* NativeRendererMessagingService::CreatePort(
   MessagingPerContextData* data = GetPerContextData<MessagingPerContextData>(
       context, CreatePerContextData::kCreateIfMissing);
   DCHECK(data);
-  DCHECK(!data->ports.contains(port_id));
+  DCHECK(!base::Contains(data->ports, port_id));
 
   GinPort* port = cppgc::MakeGarbageCollected<GinPort>(
       isolate->GetCppHeap()->GetAllocationHandle(), context, port_id,
@@ -817,7 +818,7 @@ GinPort* NativeRendererMessagingService::GetPort(
   MessagingPerContextData* data = GetPerContextData<MessagingPerContextData>(
       script_context->v8_context(), CreatePerContextData::kDontCreateIfMissing);
   DCHECK(data);
-  DCHECK(data->ports.contains(port_id));
+  DCHECK(base::Contains(data->ports, port_id));
 
   GinPort* port = nullptr;
   gin::Converter<GinPort*>::FromV8(isolate, data->ports[port_id].Get(isolate),
diff --git a/extensions/renderer/api/messaging/one_time_message_handler.cc b/extensions/renderer/api/messaging/one_time_message_handler.cc
index 7ef6c0a47e..b089a65de9 100644
--- a/extensions/renderer/api/messaging/one_time_message_handler.cc
+++ b/extensions/renderer/api/messaging/one_time_message_handler.cc
@@ -10,6 +10,7 @@
 #include <optional>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -536,7 +537,7 @@ void OneTimeMessageHandler::AddReceiver(ScriptContext* script_context,
       GetPerContextData<OneTimeMessageContextData>(
           context, CreatePerContextData::kCreateIfMissing);
   DCHECK(data);
-  DCHECK(!data->receivers.contains(target_port_id));
+  DCHECK(!base::Contains(data->receivers, target_port_id));
   OneTimeReceiver& receiver = data->receivers[target_port_id];
   receiver.sender.Reset(isolate, sender);
   receiver.event_name = event_name;
diff --git a/extensions/renderer/bindings/api_bindings_system.cc b/extensions/renderer/bindings/api_bindings_system.cc
index c9ca2f4483..52216db412 100644
--- a/extensions/renderer/bindings/api_bindings_system.cc
+++ b/extensions/renderer/bindings/api_bindings_system.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/values.h"
 #include "extensions/common/mojom/event_dispatcher.mojom.h"
@@ -111,7 +112,7 @@ void APIBindingsSystem::InitializeType(const std::string& type_name) {
   std::string api_name = type_name.substr(0, dot);
   // If we've already instantiated the binding, the type should have been in
   // there.
-  DCHECK(!api_bindings_.contains(api_name)) << api_name;
+  DCHECK(!base::Contains(api_bindings_, api_name)) << api_name;
 
   api_bindings_[api_name] = CreateNewAPIBinding(api_name);
 }
@@ -148,7 +149,7 @@ void APIBindingsSystem::RegisterHooksDelegate(
 
 void APIBindingsSystem::RegisterCustomType(const std::string& type_name,
                                            CustomTypeHandler function) {
-  DCHECK(!custom_types_.contains(type_name))
+  DCHECK(!base::Contains(custom_types_, type_name))
       << "Custom type already registered: " << type_name;
   custom_types_[type_name] = std::move(function);
 }
diff --git a/extensions/renderer/bindings/api_event_handler.cc b/extensions/renderer/bindings/api_event_handler.cc
index ec5ba3b17d..7774685379 100644
--- a/extensions/renderer/bindings/api_event_handler.cc
+++ b/extensions/renderer/bindings/api_event_handler.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/raw_ptr.h"
@@ -358,7 +359,7 @@ void APIEventHandler::RegisterArgumentMassager(
     v8::Local<v8::Function> massager) {
   APIEventPerContextData* data = APIEventPerContextData::GetFrom(
       context, CreatePerContextData::kCreateIfMissing);
-  DCHECK(!data->massagers.contains(event_name));
+  DCHECK(!base::Contains(data->massagers, event_name));
   data->massagers[event_name].Reset(v8::Isolate::GetCurrent(), massager);
 }
 
diff --git a/extensions/renderer/bindings/api_event_listeners.cc b/extensions/renderer/bindings/api_event_listeners.cc
index af4972cbdc..09f8c629fc 100644
--- a/extensions/renderer/bindings/api_event_listeners.cc
+++ b/extensions/renderer/bindings/api_event_listeners.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "content/public/renderer/v8_value_converter.h"
 #include "extensions/common/event_matcher.h"
 #include "extensions/common/mojom/event_dispatcher.mojom.h"
diff --git a/extensions/renderer/bindings/api_response_validator.cc b/extensions/renderer/bindings/api_response_validator.cc
index 53b4c60b9b..b622140dc5 100644
--- a/extensions/renderer/bindings/api_response_validator.cc
+++ b/extensions/renderer/bindings/api_response_validator.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <ostream>
 
+#include "base/containers/contains.h"
 #include "extensions/renderer/bindings/api_binding_util.h"
 #include "extensions/renderer/bindings/api_signature.h"
 #include "extensions/renderer/bindings/api_type_reference_map.h"
@@ -43,7 +44,7 @@ void APIResponseValidator::TestHandler::HandleFailure(
 
 bool APIResponseValidator::TestHandler::ShouldIgnoreSignature(
     const std::string& signature_name) const {
-  return signatures_to_ignore_.contains(signature_name);
+  return base::Contains(signatures_to_ignore_, signature_name);
 }
 
 APIResponseValidator::APIResponseValidator(const APITypeReferenceMap* type_refs)
diff --git a/extensions/renderer/bindings/api_signature.cc b/extensions/renderer/bindings/api_signature.cc
index 0d2c3668ae..8029aa2eff 100644
--- a/extensions/renderer/bindings/api_signature.cc
+++ b/extensions/renderer/bindings/api_signature.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ref.h"
 #include "base/strings/string_util.h"
 #include "base/strings/stringprintf.h"
diff --git a/extensions/renderer/bindings/api_type_reference_map.cc b/extensions/renderer/bindings/api_type_reference_map.cc
index 86bf243a8f..a5b80f607f 100644
--- a/extensions/renderer/bindings/api_type_reference_map.cc
+++ b/extensions/renderer/bindings/api_type_reference_map.cc
@@ -6,6 +6,7 @@
 
 #include <ostream>
 
+#include "base/containers/contains.h"
 #include "extensions/renderer/bindings/api_signature.h"
 #include "extensions/renderer/bindings/argument_spec.h"
 
@@ -17,7 +18,7 @@ APITypeReferenceMap::~APITypeReferenceMap() = default;
 
 void APITypeReferenceMap::AddSpec(const std::string& name,
                                   std::unique_ptr<ArgumentSpec> spec) {
-  DCHECK(!type_refs_.contains(name));
+  DCHECK(!base::Contains(type_refs_, name));
   type_refs_[name] = std::move(spec);
 }
 
@@ -34,7 +35,7 @@ const ArgumentSpec* APITypeReferenceMap::GetSpec(
 void APITypeReferenceMap::AddAPIMethodSignature(
     const std::string& name,
     std::unique_ptr<APISignature> signature) {
-  DCHECK(!api_methods_.contains(name))
+  DCHECK(!base::Contains(api_methods_, name))
       << "Cannot re-register signature for: " << name;
   api_methods_[name] = std::move(signature);
 }
@@ -52,7 +53,7 @@ const APISignature* APITypeReferenceMap::GetAPIMethodSignature(
 void APITypeReferenceMap::AddTypeMethodSignature(
     const std::string& name,
     std::unique_ptr<APISignature> signature) {
-  DCHECK(!type_methods_.contains(name))
+  DCHECK(!base::Contains(type_methods_, name))
       << "Cannot re-register signature for: " << name;
   type_methods_[name] = std::move(signature);
 }
@@ -74,7 +75,7 @@ const APISignature* APITypeReferenceMap::GetTypeMethodSignature(
 
 bool APITypeReferenceMap::HasTypeMethodSignature(
     const std::string& name) const {
-  return type_methods_.contains(name);
+  return base::Contains(type_methods_, name);
 }
 
 const APISignature* APITypeReferenceMap::GetAsyncResponseSignature(
@@ -86,7 +87,7 @@ const APISignature* APITypeReferenceMap::GetAsyncResponseSignature(
 void APITypeReferenceMap::AddCustomSignature(
     const std::string& name,
     std::unique_ptr<APISignature> signature) {
-  DCHECK(!custom_signatures_.contains(name))
+  DCHECK(!base::Contains(custom_signatures_, name))
       << "Cannot re-register signature for: " << name;
   custom_signatures_[name] = std::move(signature);
 }
@@ -100,7 +101,7 @@ const APISignature* APITypeReferenceMap::GetCustomSignature(
 void APITypeReferenceMap::AddEventSignature(
     const std::string& event_name,
     std::unique_ptr<APISignature> signature) {
-  DCHECK(!event_signatures_.contains(event_name))
+  DCHECK(!base::Contains(event_signatures_, event_name))
       << "Cannot re-register signature for: " << event_name;
   event_signatures_[event_name] = std::move(signature);
 }
diff --git a/extensions/renderer/content_setting.cc b/extensions/renderer/content_setting.cc
index 420c4bcea1..981ff3d0e2 100644
--- a/extensions/renderer/content_setting.cc
+++ b/extensions/renderer/content_setting.cc
@@ -4,9 +4,9 @@
 
 #include "extensions/renderer/content_setting.h"
 
-#include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/strings/stringprintf.h"
 #include "base/values.h"
 #include "extensions/renderer/bindings/api_binding_types.h"
@@ -41,15 +41,15 @@ const char* const kDeprecatedTypesToBlock[] = {
 };
 
 const char* GetForcedValueForDeprecatedSetting(std::string_view type) {
-  if (std::ranges::contains(kDeprecatedTypesToAllow, type))
+  if (base::Contains(kDeprecatedTypesToAllow, type))
     return "allow";
-  DCHECK(std::ranges::contains(kDeprecatedTypesToBlock, type));
+  DCHECK(base::Contains(kDeprecatedTypesToBlock, type));
   return "block";
 }
 
 bool IsDeprecated(std::string_view type) {
-  return std::ranges::contains(kDeprecatedTypesToAllow, type) ||
-         std::ranges::contains(kDeprecatedTypesToBlock, type);
+  return base::Contains(kDeprecatedTypesToAllow, type) ||
+         base::Contains(kDeprecatedTypesToBlock, type);
 }
 
 }  // namespace
diff --git a/extensions/renderer/dispatcher.cc b/extensions/renderer/dispatcher.cc
index 095ba031a6..ca57555f25 100644
--- a/extensions/renderer/dispatcher.cc
+++ b/extensions/renderer/dispatcher.cc
@@ -13,6 +13,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/debug/alias.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
@@ -484,7 +485,7 @@ void Dispatcher::OnRenderFrameCreated(content::RenderFrame* render_frame) {
 }
 
 bool Dispatcher::IsExtensionActive(const ExtensionId& extension_id) const {
-  const bool is_active = active_extension_ids_.contains(extension_id);
+  const bool is_active = base::Contains(active_extension_ids_, extension_id);
   if (is_active)
     CHECK(RendererExtensionRegistry::Get()->Contains(extension_id));
   return is_active;
@@ -652,7 +653,7 @@ void Dispatcher::WillEvaluateServiceWorkerOnWorkerThread(
       ExtensionRendererLoadStatus::kUnknownExtension;
   if (extension) {
     load_status = ExtensionRendererLoadStatus::kExtensionLoaded;
-  } else if (unloaded_extensions_.contains(script_url.GetHost())) {
+  } else if (base::Contains(unloaded_extensions_, script_url.GetHost())) {
     // script_url.host() is the extension's ID.
     load_status = ExtensionRendererLoadStatus::kExtensionUnloaded;
   }
diff --git a/extensions/renderer/guest_view/mime_handler_view/mime_handler_view_container_manager.cc b/extensions/renderer/guest_view/mime_handler_view/mime_handler_view_container_manager.cc
index 0b3bdc66f8..1ca05439a8 100644
--- a/extensions/renderer/guest_view/mime_handler_view/mime_handler_view_container_manager.cc
+++ b/extensions/renderer/guest_view/mime_handler_view/mime_handler_view_container_manager.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/no_destructor.h"
diff --git a/extensions/renderer/native_extension_bindings_system.cc b/extensions/renderer/native_extension_bindings_system.cc
index ddaf64eca3..9b4bf9508a 100644
--- a/extensions/renderer/native_extension_bindings_system.cc
+++ b/extensions/renderer/native_extension_bindings_system.cc
@@ -4,7 +4,6 @@
 
 #include "extensions/renderer/native_extension_bindings_system.h"
 
-#include <algorithm>
 #include <string_view>
 #include <utility>
 
@@ -423,7 +422,7 @@ bool ShouldCollectJSStackTrace(const APIRequestHandler::Request& request) {
           extensions_features::kIncludeJSCallStackInExtensionApiRequest)) {
     return false;
   }
-  if (!std::ranges::contains(kApiMethods, request.method_name)) {
+  if (!base::Contains(kApiMethods, request.method_name)) {
     return false;
   }
   return true;
diff --git a/extensions/renderer/renderer_extension_registry.cc b/extensions/renderer/renderer_extension_registry.cc
index a439535875..19e0b27623 100644
--- a/extensions/renderer/renderer_extension_registry.cc
+++ b/extensions/renderer/renderer_extension_registry.cc
@@ -5,6 +5,7 @@
 #include "extensions/renderer/renderer_extension_registry.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/lazy_instance.h"
 #include "base/unguessable_token.h"
 #include "build/build_config.h"
@@ -57,7 +58,7 @@ bool RendererExtensionRegistry::Insert(
 
   if (!BackgroundInfo::IsServiceWorkerBased(extension.get())) {
     // Non-SW based extension should never have an activation token.
-    CHECK(!worker_activation_tokens_.contains(extension->id()));
+    CHECK(!base::Contains(worker_activation_tokens_, extension->id()));
     return extensions_.Insert(extension);
   }
 
@@ -73,7 +74,7 @@ bool RendererExtensionRegistry::Insert(
   bool is_incognito_spanning = client->IsIncognitoProcess() &&
                                IncognitoInfo::IsSpanningMode(extension.get());
   if (is_incognito_spanning) {
-    CHECK(!worker_activation_tokens_.contains(extension->id()));
+    CHECK(!base::Contains(worker_activation_tokens_, extension->id()));
   }
 #endif  // !BUILDFLAG(IS_CHROMEOS)
 
diff --git a/extensions/renderer/resource_bundle_source_map.cc b/extensions/renderer/resource_bundle_source_map.cc
index 0bb80ecff0..8c8de9dabc 100644
--- a/extensions/renderer/resource_bundle_source_map.cc
+++ b/extensions/renderer/resource_bundle_source_map.cc
@@ -7,6 +7,7 @@
 #include <ostream>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "extensions/renderer/static_v8_external_one_byte_string_resource.h"
 #include "third_party/zlib/google/compression_utils.h"
@@ -92,7 +93,7 @@ v8::Local<v8::String> ResourceBundleSourceMap::GetSource(
 
 bool ResourceBundleSourceMap::Contains(const std::string& name) const {
   base::AutoLock lock(lock_);
-  return resource_map_.contains(name);
+  return base::Contains(resource_map_, name);
 }
 
 }  // namespace extensions
diff --git a/extensions/renderer/script_injection.cc b/extensions/renderer/script_injection.cc
index 8ce2c7f273..aa9df589ce 100644
--- a/extensions/renderer/script_injection.cc
+++ b/extensions/renderer/script_injection.cc
@@ -7,6 +7,7 @@
 #include <map>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/lazy_instance.h"
@@ -281,7 +282,7 @@ void ScriptInjection::InjectJs(std::set<std::string>* executing_scripts,
     constexpr size_t kMaxActiveUserScriptWorldCount = 10;
     if (active_user_script_worlds &&
         active_user_script_worlds->size() >= kMaxActiveUserScriptWorldCount &&
-        !active_user_script_worlds->contains(world_id)) {
+        !base::Contains(*active_user_script_worlds, world_id)) {
       // If there are 10 or more active user script worlds, we use the default
       // world for future injections.
       // Note: This *can* mean that up to 11 user script worlds for this
diff --git a/extensions/renderer/string_source_map.cc b/extensions/renderer/string_source_map.cc
index ab92fb0169..a3d191d820 100644
--- a/extensions/renderer/string_source_map.cc
+++ b/extensions/renderer/string_source_map.cc
@@ -7,6 +7,7 @@
 #include <ostream>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "gin/converter.h"
 #include "third_party/zlib/google/compression_utils.h"
 
@@ -26,7 +27,7 @@ v8::Local<v8::String> StringSourceMap::GetSource(
 }
 
 bool StringSourceMap::Contains(const std::string& name) const {
-  return sources_.contains(name);
+  return base::Contains(sources_, name);
 }
 
 void StringSourceMap::RegisterModule(const std::string& name,
diff --git a/extensions/test/extension_test_notification_observer.cc b/extensions/test/extension_test_notification_observer.cc
index 63276eabe0..e1b5df5406 100644
--- a/extensions/test/extension_test_notification_observer.cc
+++ b/extensions/test/extension_test_notification_observer.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/run_loop.h"
@@ -89,7 +90,7 @@ void ExtensionTestNotificationObserver::NotificationSet::OnWebContentsCreated(
 
 void ExtensionTestNotificationObserver::NotificationSet::
     StartObservingWebContents(content::WebContents* web_contents) {
-  CHECK(!web_contents_observers_.contains(web_contents));
+  CHECK(!base::Contains(web_contents_observers_, web_contents));
   web_contents_observers_[web_contents] =
       std::make_unique<ForwardingWebContentsObserver>(web_contents, this);
 }
diff --git a/google_apis/drive/drive_base_requests.cc b/google_apis/drive/drive_base_requests.cc
index 4e2ebafcf6..3dcc9f1c6c 100644
--- a/google_apis/drive/drive_base_requests.cc
+++ b/google_apis/drive/drive_base_requests.cc
@@ -10,6 +10,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/json/json_reader.h"
@@ -128,7 +129,7 @@ void GenerateMultipartBody(MultipartType multipart_type,
       boundary = net::GenerateMimeMultipartBoundary();
       bool conflict_with_content = false;
       for (const auto& part : parts) {
-        if (part.data.contains(boundary)) {
+        if (base::Contains(part.data, boundary)) {
           conflict_with_content = true;
           break;
         }
diff --git a/google_apis/gaia/core_account_id.cc b/google_apis/gaia/core_account_id.cc
index a685194d8f..5738dddc1a 100644
--- a/google_apis/gaia/core_account_id.cc
+++ b/google_apis/gaia/core_account_id.cc
@@ -5,6 +5,7 @@
 #include "google_apis/gaia/core_account_id.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "build/build_config.h"
 #include "google_apis/gaia/gaia_auth_util.h"
@@ -19,7 +20,7 @@ namespace {
 // Returns whether the string looks like an email (the test is
 // crude an only checks whether it includes an '@').
 bool IsEmailString(const std::string& string) {
-  return string.contains('@');
+  return base::Contains(string, '@');
 }
 }  // anonymous namespace
 
diff --git a/google_apis/gaia/fake_gaia.cc b/google_apis/gaia/fake_gaia.cc
index b4a96cab48..a7cdc51d1b 100644
--- a/google_apis/gaia/fake_gaia.cc
+++ b/google_apis/gaia/fake_gaia.cc
@@ -11,6 +11,7 @@
 
 #include "base/base64.h"
 #include "base/base_paths.h"
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -507,7 +508,7 @@ void FakeGaia::SetOAuthCodeCookie(BasicHttpResponse* http_response) const {
 void FakeGaia::AddSyncTrustedKeysHeader(BasicHttpResponse* http_response,
                                         const std::string& email) const {
   DCHECK(http_response);
-  DCHECK(email_to_sync_trusted_vault_keys_map_.contains(email));
+  DCHECK(base::Contains(email_to_sync_trusted_vault_keys_map_, email));
   http_response->AddCustomHeader(
       "fake-sync-trusted-vault-keys",
       FormatSyncTrustedVaultKeysHeader(
@@ -803,7 +804,7 @@ void FakeGaia::HandleEmbeddedLookupAccountLookup(
   std::string email;
   const bool is_saml =
       GetQueryParameter(request.content, "identifier", &email) &&
-      saml_account_idp_map_.contains(email);
+      base::Contains(saml_account_idp_map_, email);
 
   if (!is_saml)
     return;
@@ -844,7 +845,7 @@ void FakeGaia::HandleEmbeddedSigninChallenge(const HttpRequest& request,
   if (issue_oauth_code_cookie_)
     SetOAuthCodeCookie(http_response);
 
-  if (email_to_sync_trusted_vault_keys_map_.contains(email)) {
+  if (base::Contains(email_to_sync_trusted_vault_keys_map_, email)) {
     AddSyncTrustedKeysHeader(http_response, email);
   }
 }
diff --git a/google_apis/gaia/gaia_auth_util.cc b/google_apis/gaia/gaia_auth_util.cc
index a53e3ea32e..0820abb026 100644
--- a/google_apis/gaia/gaia_auth_util.cc
+++ b/google_apis/gaia/gaia_auth_util.cc
@@ -11,6 +11,7 @@
 
 #include "base/base64.h"
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/notreached.h"
@@ -89,7 +90,7 @@ std::string SanitizeEmail(std::string_view email_address) {
   std::string sanitized(email_address);
 
   // Apply a default domain if necessary.
-  if (!sanitized.contains('@')) {
+  if (!base::Contains(sanitized, '@')) {
     sanitized += '@';
     sanitized += kGmailDomain;
   }
diff --git a/google_apis/gaia/oauth2_id_token_decoder.cc b/google_apis/gaia/oauth2_id_token_decoder.cc
index 9b9ef41047..a1ea90bdd2 100644
--- a/google_apis/gaia/oauth2_id_token_decoder.cc
+++ b/google_apis/gaia/oauth2_id_token_decoder.cc
@@ -10,6 +10,7 @@
 #include <string_view>
 
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/json/json_reader.h"
 #include "base/logging.h"
 #include "base/strings/string_split.h"
@@ -98,8 +99,8 @@ TokenServiceFlags ParseServiceFlags(const std::string& id_token) {
   }
 
   token_service_flags.is_child_account =
-      std::ranges::contains(service_flags, kChildAccountServiceFlag);
-  token_service_flags.is_under_advanced_protection = std::ranges::contains(
+      base::Contains(service_flags, kChildAccountServiceFlag);
+  token_service_flags.is_under_advanced_protection = base::Contains(
       service_flags, kAdvancedProtectionAccountServiceFlag);
   return token_service_flags;
 }
diff --git a/google_apis/gcm/engine/instance_id_delete_token_request_handler.cc b/google_apis/gcm/engine/instance_id_delete_token_request_handler.cc
index 5020255b78..2939dfeea2 100644
--- a/google_apis/gcm/engine/instance_id_delete_token_request_handler.cc
+++ b/google_apis/gcm/engine/instance_id_delete_token_request_handler.cc
@@ -5,6 +5,7 @@
 #include "google_apis/gcm/engine/instance_id_delete_token_request_handler.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/string_number_conversions.h"
 #include "google_apis/gcm/base/gcm_util.h"
@@ -52,7 +53,7 @@ void InstanceIDDeleteTokenRequestHandler::BuildRequestBody(std::string* body) {
 UnregistrationRequest::Status
 InstanceIDDeleteTokenRequestHandler::ParseResponse(
     const std::string& response) {
-  if (!response.contains(kTokenPrefix)) {
+  if (!base::Contains(response, kTokenPrefix)) {
     return UnregistrationRequest::RESPONSE_PARSING_FAILED;
   }
 
diff --git a/google_apis/gcm/engine/registration_request.cc b/google_apis/gcm/engine/registration_request.cc
index eb0c911a00..f8808ca3b9 100644
--- a/google_apis/gcm/engine/registration_request.cc
+++ b/google_apis/gcm/engine/registration_request.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/metrics/histogram.h"
@@ -58,34 +59,34 @@ const char kFisAuthError[] = "FIS_AUTH_ERROR";
 
 // Gets correct status from the error message.
 RegistrationRequest::Status GetStatusFromError(std::string_view error) {
-  if (error.contains(kDeviceRegistrationError)) {
+  if (base::Contains(error, kDeviceRegistrationError)) {
     return RegistrationRequest::DEVICE_REGISTRATION_ERROR;
   }
-  if (error.contains(kAuthenticationFailed)) {
+  if (base::Contains(error, kAuthenticationFailed)) {
     return RegistrationRequest::AUTHENTICATION_FAILED;
   }
-  if (error.contains(kInvalidSender)) {
+  if (base::Contains(error, kInvalidSender)) {
     return RegistrationRequest::INVALID_SENDER;
   }
-  if (error.contains(kInvalidParameters)) {
+  if (base::Contains(error, kInvalidParameters)) {
     return RegistrationRequest::INVALID_PARAMETERS;
   }
-  if (error.contains(kInternalServerError)) {
+  if (base::Contains(error, kInternalServerError)) {
     return RegistrationRequest::INTERNAL_SERVER_ERROR;
   }
-  if (error.contains(kQuotaExceeded)) {
+  if (base::Contains(error, kQuotaExceeded)) {
     return RegistrationRequest::QUOTA_EXCEEDED;
   }
-  if (error.contains(kTooManyRegistrations)) {
+  if (base::Contains(error, kTooManyRegistrations)) {
     return RegistrationRequest::TOO_MANY_REGISTRATIONS;
   }
-  if (error.contains(kTooManySubscribers)) {
+  if (base::Contains(error, kTooManySubscribers)) {
     return RegistrationRequest::TOO_MANY_SUBSCRIBERS;
   }
-  if (error.contains(kInvalidTargetVersion)) {
+  if (base::Contains(error, kInvalidTargetVersion)) {
     return RegistrationRequest::INVALID_TARGET_VERSION;
   }
-  if (error.contains(kFisAuthError)) {
+  if (base::Contains(error, kFisAuthError)) {
     return RegistrationRequest::FIS_AUTH_ERROR;
   }
   // Should not be reached, unless the server adds new error types.
diff --git a/google_apis/gcm/engine/unregistration_request.cc b/google_apis/gcm/engine/unregistration_request.cc
index 47e90572b8..6fd50259ee 100644
--- a/google_apis/gcm/engine/unregistration_request.cc
+++ b/google_apis/gcm/engine/unregistration_request.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/strings/escape.h"
@@ -49,13 +50,13 @@ const char kDeviceRegistrationError[] = "PHONE_REGISTRATION_ERROR";
 
 // Gets correct status from the error message.
 UnregistrationRequest::Status GetStatusFromError(const std::string& error) {
-  if (error.contains(kInvalidParameters)) {
+  if (base::Contains(error, kInvalidParameters)) {
     return UnregistrationRequest::INVALID_PARAMETERS;
   }
-  if (error.contains(kInternalServerError)) {
+  if (base::Contains(error, kInternalServerError)) {
     return UnregistrationRequest::INTERNAL_SERVER_ERROR;
   }
-  if (error.contains(kDeviceRegistrationError)) {
+  if (base::Contains(error, kDeviceRegistrationError)) {
     return UnregistrationRequest::DEVICE_REGISTRATION_ERROR;
   }
   // Should not be reached, unless the server adds new error types.
diff --git a/gpu/command_buffer/client/client_discardable_manager.cc b/gpu/command_buffer/client/client_discardable_manager.cc
index 41171e997a..c78ccf917d 100644
--- a/gpu/command_buffer/client/client_discardable_manager.cc
+++ b/gpu/command_buffer/client/client_discardable_manager.cc
@@ -5,6 +5,7 @@
 #include "gpu/command_buffer/client/client_discardable_manager.h"
 
 #include "base/atomic_sequence_num.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/system/sys_info.h"
@@ -178,7 +179,7 @@ void ClientDiscardableManager::FreeHandle(
 
 bool ClientDiscardableManager::HandleIsValid(
     ClientDiscardableHandle::Id handle_id) const {
-  return handles_.contains(handle_id);
+  return base::Contains(handles_, handle_id);
 }
 
 ClientDiscardableHandle ClientDiscardableManager::GetHandle(
diff --git a/gpu/command_buffer/client/internal/mappable_buffer_test_template.h b/gpu/command_buffer/client/internal/mappable_buffer_test_template.h
index d3f065eb38..f0e81a2cdd 100644
--- a/gpu/command_buffer/client/internal/mappable_buffer_test_template.h
+++ b/gpu/command_buffer/client/internal/mappable_buffer_test_template.h
@@ -11,7 +11,6 @@
 #include <stddef.h>
 #include <string.h>
 
-#include <algorithm>
 #include <memory>
 
 #include "base/compiler_specific.h"
@@ -322,8 +321,8 @@ TYPED_TEST_P(MappableBufferTest, Map) {
   // Use a multiple of 4 for both dimensions to support compressed formats.
   const gfx::Size kBufferSize(4, 4);
 
-  if (!std::ranges::contains(TestFixture::usages(),
-                             gfx::BufferUsage::GPU_READ_CPU_READ_WRITE)) {
+  if (!base::Contains(TestFixture::usages(),
+                      gfx::BufferUsage::GPU_READ_CPU_READ_WRITE)) {
     GTEST_SKIP();
   }
 
@@ -390,8 +389,8 @@ TYPED_TEST_P(MappableBufferTest, PersistentMap) {
   // Use a multiple of 4 for both dimensions to support compressed formats.
   const gfx::Size kBufferSize(4, 4);
 
-  if (!std::ranges::contains(TestFixture::usages(),
-                             gfx::BufferUsage::GPU_READ_CPU_READ_WRITE)) {
+  if (!base::Contains(TestFixture::usages(),
+                      gfx::BufferUsage::GPU_READ_CPU_READ_WRITE)) {
     GTEST_SKIP();
   }
 
diff --git a/gpu/command_buffer/client/webgpu_interface.h b/gpu/command_buffer/client/webgpu_interface.h
index 2686237124..b2a857f565 100644
--- a/gpu/command_buffer/client/webgpu_interface.h
+++ b/gpu/command_buffer/client/webgpu_interface.h
@@ -8,9 +8,8 @@
 #include <dawn/dawn_proc_table.h>
 #include <dawn/webgpu.h>
 
-#include <utility>
-
 #include "base/memory/ref_counted.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "gpu/command_buffer/client/interface_base.h"
 #include "gpu/command_buffer/common/webgpu_cmd_enums.h"
 #include "gpu/command_buffer/common/webgpu_cmd_ids.h"
@@ -166,7 +165,7 @@ class WebGPUInterface : public InterfaceBase {
       const blink::WebGPUExecutionContextToken& token) {
     uint64_t high = token.value().GetHighForSerialization();
     uint64_t low = token.value().GetLowForSerialization();
-    SetWebGPUExecutionContextToken(std::to_underlying(token.variant_index()),
+    SetWebGPUExecutionContextToken(base::to_underlying(token.variant_index()),
                                    high >> 32, high & 0xFFFFFFFF, low >> 32,
                                    low & 0xFFFFFFFF);
   }
diff --git a/gpu/command_buffer/service/drm_modifiers_filter_vulkan.cc b/gpu/command_buffer/service/drm_modifiers_filter_vulkan.cc
index 04825f0715..10fe66e6c6 100644
--- a/gpu/command_buffer/service/drm_modifiers_filter_vulkan.cc
+++ b/gpu/command_buffer/service/drm_modifiers_filter_vulkan.cc
@@ -5,6 +5,7 @@
 #include "gpu/command_buffer/service/drm_modifiers_filter_vulkan.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "components/viz/common/resources/shared_image_format_utils.h"
 #include "gpu/command_buffer/service/shared_image/shared_image_format_service_utils.h"
@@ -85,7 +86,7 @@ std::vector<uint64_t> DrmModifiersFilterVulkan::Filter(
   }
   std::vector<uint64_t> intersection;
   for (const auto& modifier : modifiers) {
-    if (vulkan_modifiers.contains(modifier)) {
+    if (base::Contains(vulkan_modifiers, modifier)) {
       intersection.push_back(modifier);
     }
   }
diff --git a/gpu/command_buffer/service/framebuffer_completeness_cache.cc b/gpu/command_buffer/service/framebuffer_completeness_cache.cc
index 0ce9ab6c35..368bbabbff 100644
--- a/gpu/command_buffer/service/framebuffer_completeness_cache.cc
+++ b/gpu/command_buffer/service/framebuffer_completeness_cache.cc
@@ -4,6 +4,7 @@
 
 #include "gpu/command_buffer/service/framebuffer_completeness_cache.h"
 
+#include "base/containers/contains.h"
 
 namespace gpu::gles2 {
 
@@ -13,7 +14,7 @@ FramebufferCompletenessCache::~FramebufferCompletenessCache() = default;
 
 bool FramebufferCompletenessCache::IsComplete(
     const std::string& signature) const {
-  return cache_.contains(signature);
+  return base::Contains(cache_, signature);
 }
 
 void FramebufferCompletenessCache::SetComplete(const std::string& signature) {
diff --git a/gpu/command_buffer/service/gles2_cmd_decoder.cc b/gpu/command_buffer/service/gles2_cmd_decoder.cc
index 0ea85e73ea..8abc8e5ff1 100644
--- a/gpu/command_buffer/service/gles2_cmd_decoder.cc
+++ b/gpu/command_buffer/service/gles2_cmd_decoder.cc
@@ -23,6 +23,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/heap_array.h"
 #include "base/containers/queue.h"
@@ -14650,28 +14651,28 @@ error::Error GLES2DecoderImpl::HandleRequestExtensionCHROMIUM(
   bool desire_nv_egl_stream_consumer_external = false;
   if (feature_info_->context_type() == CONTEXT_TYPE_WEBGL1) {
     desire_standard_derivatives =
-        feature_str.contains("GL_OES_standard_derivatives ");
+        base::Contains(feature_str, "GL_OES_standard_derivatives ");
     desire_fbo_render_mipmap =
-        feature_str.contains("GL_OES_fbo_render_mipmap ");
-    desire_frag_depth = feature_str.contains("GL_EXT_frag_depth ");
-    desire_draw_buffers = feature_str.contains("GL_EXT_draw_buffers ");
+        base::Contains(feature_str, "GL_OES_fbo_render_mipmap ");
+    desire_frag_depth = base::Contains(feature_str, "GL_EXT_frag_depth ");
+    desire_draw_buffers = base::Contains(feature_str, "GL_EXT_draw_buffers ");
     desire_shader_texture_lod =
-        feature_str.contains("GL_EXT_shader_texture_lod ");
+        base::Contains(feature_str, "GL_EXT_shader_texture_lod ");
   } else if (feature_info_->context_type() == CONTEXT_TYPE_WEBGL2) {
-    desire_draw_instanced_base_vertex_base_instance = feature_str.contains(
-        "GL_WEBGL_draw_instanced_base_vertex_base_instance ");
-    desire_multi_draw_instanced_base_vertex_base_instance =
-        feature_str.contains(
-            "GL_WEBGL_multi_draw_instanced_base_vertex_base_instance ");
+    desire_draw_instanced_base_vertex_base_instance = base::Contains(
+        feature_str, "GL_WEBGL_draw_instanced_base_vertex_base_instance ");
+    desire_multi_draw_instanced_base_vertex_base_instance = base::Contains(
+        feature_str,
+        "GL_WEBGL_multi_draw_instanced_base_vertex_base_instance ");
   }
   if (feature_info_->IsWebGLContext()) {
-    desire_multi_draw = feature_str.contains("GL_WEBGL_multi_draw ");
+    desire_multi_draw = base::Contains(feature_str, "GL_WEBGL_multi_draw ");
     desire_arb_texture_rectangle =
-        feature_str.contains("GL_ANGLE_texture_rectangle ");
+        base::Contains(feature_str, "GL_ANGLE_texture_rectangle ");
     desire_oes_egl_image_external =
-        feature_str.contains("GL_OES_EGL_image_external ");
+        base::Contains(feature_str, "GL_OES_EGL_image_external ");
     desire_nv_egl_stream_consumer_external =
-        feature_str.contains("GL_NV_EGL_stream_consumer_external ");
+        base::Contains(feature_str, "GL_NV_EGL_stream_consumer_external ");
   }
   if (desire_standard_derivatives != derivatives_explicitly_enabled_ ||
       desire_fbo_render_mipmap != fbo_render_mipmap_explicitly_enabled_ ||
@@ -14704,31 +14705,31 @@ error::Error GLES2DecoderImpl::HandleRequestExtensionCHROMIUM(
     DestroyShaderTranslator();
   }
 
-  if (feature_str.contains("GL_CHROMIUM_color_buffer_float_rgba ")) {
+  if (base::Contains(feature_str, "GL_CHROMIUM_color_buffer_float_rgba ")) {
     feature_info_->EnableCHROMIUMColorBufferFloatRGBA();
   }
-  if (feature_str.contains("GL_CHROMIUM_color_buffer_float_rgb ")) {
+  if (base::Contains(feature_str, "GL_CHROMIUM_color_buffer_float_rgb ")) {
     feature_info_->EnableCHROMIUMColorBufferFloatRGB();
   }
-  if (feature_str.contains("GL_EXT_color_buffer_float ")) {
+  if (base::Contains(feature_str, "GL_EXT_color_buffer_float ")) {
     feature_info_->EnableEXTColorBufferFloat();
   }
-  if (feature_str.contains("GL_EXT_color_buffer_half_float ")) {
+  if (base::Contains(feature_str, "GL_EXT_color_buffer_half_float ")) {
     feature_info_->EnableEXTColorBufferHalfFloat();
   }
-  if (feature_str.contains("GL_EXT_texture_filter_anisotropic ")) {
+  if (base::Contains(feature_str, "GL_EXT_texture_filter_anisotropic ")) {
     feature_info_->EnableEXTTextureFilterAnisotropic();
   }
-  if (feature_str.contains("GL_OES_texture_float_linear ")) {
+  if (base::Contains(feature_str, "GL_OES_texture_float_linear ")) {
     feature_info_->EnableOESTextureFloatLinear();
   }
-  if (feature_str.contains("GL_OES_texture_half_float_linear ")) {
+  if (base::Contains(feature_str, "GL_OES_texture_half_float_linear ")) {
     feature_info_->EnableOESTextureHalfFloatLinear();
   }
-  if (feature_str.contains("GL_EXT_float_blend ")) {
+  if (base::Contains(feature_str, "GL_EXT_float_blend ")) {
     feature_info_->EnableEXTFloatBlend();
   }
-  if (feature_str.contains("GL_OES_fbo_render_mipmap ")) {
+  if (base::Contains(feature_str, "GL_OES_fbo_render_mipmap ")) {
     feature_info_->EnableOESFboRenderMipmap();
   }
 
diff --git a/gpu/command_buffer/service/gles2_cmd_decoder_passthrough_doers.cc b/gpu/command_buffer/service/gles2_cmd_decoder_passthrough_doers.cc
index f4574f864a..6d963dcf14 100644
--- a/gpu/command_buffer/service/gles2_cmd_decoder_passthrough_doers.cc
+++ b/gpu/command_buffer/service/gles2_cmd_decoder_passthrough_doers.cc
@@ -8,6 +8,7 @@
 
 #include "base/bits.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_macros.h"
@@ -392,7 +393,7 @@ error::Error GLES2DecoderPassthroughImpl::DoBindBuffer(GLenum target,
     return error::kNoError;
   }
 
-  DCHECK(bound_buffers_.contains(target));
+  DCHECK(base::Contains(bound_buffers_, target));
   bound_buffers_[target] = buffer;
   if (target == GL_ELEMENT_ARRAY_BUFFER) {
     bound_element_array_buffer_dirty_ = false;
@@ -411,7 +412,7 @@ error::Error GLES2DecoderPassthroughImpl::DoBindBufferBase(GLenum target,
     return error::kNoError;
   }
 
-  DCHECK(bound_buffers_.contains(target));
+  DCHECK(base::Contains(bound_buffers_, target));
   bound_buffers_[target] = buffer;
   if (target == GL_ELEMENT_ARRAY_BUFFER) {
     bound_element_array_buffer_dirty_ = false;
@@ -433,7 +434,7 @@ error::Error GLES2DecoderPassthroughImpl::DoBindBufferRange(GLenum target,
     return error::kNoError;
   }
 
-  DCHECK(bound_buffers_.contains(target));
+  DCHECK(base::Contains(bound_buffers_, target));
   bound_buffers_[target] = buffer;
   if (target == GL_ELEMENT_ARRAY_BUFFER) {
     bound_element_array_buffer_dirty_ = false;
@@ -3734,7 +3735,7 @@ error::Error GLES2DecoderPassthroughImpl::DoBeginQueryEXT(
     linking_program_service_id_ = 0u;
   }
   if (IsEmulatedQueryTarget(target)) {
-    if (active_queries_.contains(target)) {
+    if (base::Contains(active_queries_, target)) {
       InsertError(GL_INVALID_OPERATION, "Query already active on target.");
       return error::kNoError;
     }
@@ -3826,7 +3827,7 @@ error::Error GLES2DecoderPassthroughImpl::DoEndQueryEXT(GLenum target,
     }
   }
 
-  CHECK(active_queries_.contains(target));
+  CHECK(base::Contains(active_queries_, target));
   ActiveQuery active_query = std::move(active_queries_[target]);
   active_queries_.erase(target);
 
diff --git a/gpu/command_buffer/service/program_cache.cc b/gpu/command_buffer/service/program_cache.cc
index ba5b85ad13..d90415cfa4 100644
--- a/gpu/command_buffer/service/program_cache.cc
+++ b/gpu/command_buffer/service/program_cache.cc
@@ -9,6 +9,7 @@
 #include <memory>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/containers/heap_array.h"
 #include "base/containers/span_writer.h"
 #include "base/hash/hash.h"
@@ -50,7 +51,7 @@ bool ProgramCache::HasSuccessfullyCompiledShader(
     const std::string& shader_signature) const {
   Hash sha;
   ComputeShaderHash(shader_signature, sha);
-  return compiled_shaders_.contains(sha);
+  return base::Contains(compiled_shaders_, sha);
 }
 
 ProgramCache::LinkedProgramStatus ProgramCache::GetLinkedProgramStatus(
diff --git a/gpu/command_buffer/service/shared_image/ozone_image_backing.cc b/gpu/command_buffer/service/shared_image/ozone_image_backing.cc
index 1ea4718dba..12283b1f1c 100644
--- a/gpu/command_buffer/service/shared_image/ozone_image_backing.cc
+++ b/gpu/command_buffer/service/shared_image/ozone_image_backing.cc
@@ -9,6 +9,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/logging.h"
 #include "base/memory/scoped_refptr.h"
@@ -746,7 +747,7 @@ void OzoneImageBacking::EndAccess(bool readonly,
       read_fences_[access_stream] = std::move(fence);
     }
   } else {
-    DCHECK(!read_fences_.contains(access_stream));
+    DCHECK(!base::Contains(read_fences_, access_stream));
     write_fence_ = std::move(fence);
     last_write_stream_ = access_stream;
   }
diff --git a/gpu/command_buffer/service/shared_image/shared_image_factory.cc b/gpu/command_buffer/service/shared_image/shared_image_factory.cc
index 2d6f134e28..9c3a90fb78 100644
--- a/gpu/command_buffer/service/shared_image/shared_image_factory.cc
+++ b/gpu/command_buffer/service/shared_image/shared_image_factory.cc
@@ -9,6 +9,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/metrics/histogram_functions.h"
diff --git a/gpu/command_buffer/service/shared_image/shared_image_manager.cc b/gpu/command_buffer/service/shared_image/shared_image_manager.cc
index 06c252fec8..c9e710f041 100644
--- a/gpu/command_buffer/service/shared_image/shared_image_manager.cc
+++ b/gpu/command_buffer/service/shared_image/shared_image_manager.cc
@@ -10,6 +10,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/logging.h"
@@ -145,7 +146,7 @@ SharedImageManager::Register(std::unique_ptr<SharedImageBacking> backing,
   CALLED_ON_VALID_THREAD();
 
   AutoLock autolock(this);
-  if (images_.contains(backing->mailbox())) {
+  if (base::Contains(images_, backing->mailbox())) {
     LOG(ERROR) << "SharedImageManager::Register: Trying to register an "
                   "already registered mailbox.";
     backing->MarkForDestruction();
diff --git a/gpu/command_buffer/service/texture_manager.cc b/gpu/command_buffer/service/texture_manager.cc
index a327d2af29..6c3805f008 100644
--- a/gpu/command_buffer/service/texture_manager.cc
+++ b/gpu/command_buffer/service/texture_manager.cc
@@ -16,6 +16,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/heap_array.h"
 #include "base/format_macros.h"
 #include "base/lazy_instance.h"
@@ -274,12 +275,12 @@ class FormatTypeValidator {
                GLenum format,
                GLenum type) const {
     FormatType query = {internal_format, format, type};
-    if (supported_combinations_.contains(query)) {
+    if (base::Contains(supported_combinations_, query)) {
       return true;
     }
     if (context_type == CONTEXT_TYPE_OPENGLES2 ||
         context_type == CONTEXT_TYPE_WEBGL1) {
-      if (supported_combinations_es2_only_.contains(query)) {
+      if (base::Contains(supported_combinations_es2_only_, query)) {
         return true;
       }
     }
@@ -540,7 +541,7 @@ Texture::Texture(GLuint service_id)
 Texture::~Texture() = default;
 
 void Texture::AddTextureRef(TextureRef* ref) {
-  DCHECK(!refs_.contains(ref));
+  DCHECK(!base::Contains(refs_, ref));
   refs_.insert(ref);
   ScopedMemTrackerChange change(this);
   if (!memory_tracking_ref_)
diff --git a/gpu/command_buffer/service/transfer_buffer_manager.cc b/gpu/command_buffer/service/transfer_buffer_manager.cc
index 139f5575d6..ea49fad644 100644
--- a/gpu/command_buffer/service/transfer_buffer_manager.cc
+++ b/gpu/command_buffer/service/transfer_buffer_manager.cc
@@ -9,6 +9,7 @@
 #include <limits>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/process/process_handle.h"
 #include "base/strings/stringprintf.h"
@@ -57,7 +58,7 @@ bool TransferBufferManager::RegisterTransferBuffer(
   }
 
   // Fail if the ID is in use.
-  if (registered_buffers_.contains(id)) {
+  if (base::Contains(registered_buffers_, id)) {
     DVLOG(0) << "Buffer ID already in use.";
     return false;
   }
diff --git a/gpu/command_buffer/service/value_validator.h b/gpu/command_buffer/service/value_validator.h
index 3a91acd0f6..f60f4fdf7d 100644
--- a/gpu/command_buffer/service/value_validator.h
+++ b/gpu/command_buffer/service/value_validator.h
@@ -10,6 +10,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 
 namespace gpu {
@@ -45,7 +46,7 @@ class ValueValidator {
   }
 
   bool IsValid(const T value) const {
-    return std::ranges::contains(valid_values_, value);
+    return base::Contains(valid_values_, value);
   }
 
   const std::vector<T>& GetValues() const { return valid_values_; }
diff --git a/gpu/command_buffer/service/webgpu_decoder_impl.cc b/gpu/command_buffer/service/webgpu_decoder_impl.cc
index 11e836531a..9d7c387a7a 100644
--- a/gpu/command_buffer/service/webgpu_decoder_impl.cc
+++ b/gpu/command_buffer/service/webgpu_decoder_impl.cc
@@ -13,6 +13,7 @@
 #include "base/auto_reset.h"
 #include "base/bits.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/memory/ptr_util.h"
@@ -1180,7 +1181,7 @@ WebGPUDecoderImpl::WebGPUDecoderImpl(
 
   // Force adapters to report their limits in predetermined tiers unless the
   // adapter_limit_tiers toggle is explicitly disabled.
-  tiered_adapter_limits_ = !std::ranges::contains(require_disabled_toggles_,
+  tiered_adapter_limits_ = !base::Contains(require_disabled_toggles_,
                                                   "tiered_adapter_limits");
 
   DawnProcTable wire_procs = dawn::native::GetProcs();
@@ -1644,7 +1645,7 @@ bool WebGPUDecoderImpl::use_blocklist() const {
   // --disable-dawn-features=adapter_blocklist
   return !(
       safety_level_ == webgpu::SafetyLevel::kUnsafe ||
-      std::ranges::contains(require_disabled_toggles_, "adapter_blocklist"));
+      base::Contains(require_disabled_toggles_, "adapter_blocklist"));
 }
 
 wgpu::Adapter WebGPUDecoderImpl::CreatePreferredAdapter(
diff --git a/gpu/command_buffer/tests/gl_test_utils.cc b/gpu/command_buffer/tests/gl_test_utils.cc
index 9f03eb3dc9..34212edc84 100644
--- a/gpu/command_buffer/tests/gl_test_utils.cc
+++ b/gpu/command_buffer/tests/gl_test_utils.cc
@@ -14,6 +14,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/heap_array.h"
 #include "base/containers/span.h"
 #include "base/logging.h"
@@ -87,7 +88,7 @@ bool GLTestHelper::HasExtension(const char* extension) {
       std::string(reinterpret_cast<const char*>(glGetString(GL_EXTENSIONS))) +
       " ";
   std::string extension_padded = std::string(extension) + " ";
-  return extensions.contains(extension_padded);
+  return base::Contains(extensions, extension_padded);
 }
 
 bool GLTestHelper::CheckGLError(const char* msg, int line) {
@@ -401,7 +402,7 @@ bool GpuCommandBufferTestEGL::InitializeEGL(int width, int height) {
                                   &gpu_info);
     // See crbug.com/822716, the ATI proprietary driver has eglGetProcAddress
     // but eglInitialize crashes with x11.
-    if (gpu_info.gl_vendor.contains("ATI Technologies Inc.")) {
+    if (base::Contains(gpu_info.gl_vendor, "ATI Technologies Inc.")) {
       LOG(INFO) << "Skip test, ATI proprietary driver crashes with egl/x11";
       return false;
     }
diff --git a/gpu/command_buffer/tests/gl_webgl_multi_draw_test.cc b/gpu/command_buffer/tests/gl_webgl_multi_draw_test.cc
index 9ab2309b38..23e19198e1 100644
--- a/gpu/command_buffer/tests/gl_webgl_multi_draw_test.cc
+++ b/gpu/command_buffer/tests/gl_webgl_multi_draw_test.cc
@@ -10,6 +10,7 @@
 
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "gpu/command_buffer/client/gles2_implementation.h"
 #include "gpu/command_buffer/client/shared_memory_limits.h"
@@ -68,14 +69,16 @@ TEST_F(GLWebGLMultiDrawTest, MultiDrawLargerThanTransferBuffer) {
 
   // This test is only valid if the multi draw extension is supported
   if (!GLTestHelper::HasExtension("GL_ANGLE_multi_draw")) {
-    if (!requestable_extensions_string.contains("GL_ANGLE_multi_draw ")) {
+    if (!base::Contains(requestable_extensions_string,
+                        "GL_ANGLE_multi_draw ")) {
       return;
     }
     glRequestExtensionCHROMIUM("GL_ANGLE_multi_draw");
   }
 
   if (!GLTestHelper::HasExtension("GL_WEBGL_multi_draw")) {
-    if (!requestable_extensions_string.contains("GL_WEBGL_multi_draw ")) {
+    if (!base::Contains(requestable_extensions_string,
+                        "GL_WEBGL_multi_draw ")) {
       return;
     }
     glRequestExtensionCHROMIUM("GL_WEBGL_multi_draw");
diff --git a/gpu/config/gpu_feature_info.cc b/gpu/config/gpu_feature_info.cc
index 7416f66f84..ca4764874a 100644
--- a/gpu/config/gpu_feature_info.cc
+++ b/gpu/config/gpu_feature_info.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "gpu/config/gpu_blocklist.h"
 #include "gpu/config/gpu_driver_bug_list.h"
 #include "ui/gl/gl_context.h"
@@ -34,7 +35,7 @@ void GpuFeatureInfo::ApplyToGLContext(gl::GLContext* gl_context) const {
 }
 
 bool GpuFeatureInfo::IsWorkaroundEnabled(int32_t workaround) const {
-  return std::ranges::contains(this->enabled_gpu_driver_bug_workarounds,
+  return base::Contains(this->enabled_gpu_driver_bug_workarounds,
                                workaround);
 }
 
diff --git a/gpu/config/gpu_switching.cc b/gpu/config/gpu_switching.cc
index 6efae76e40..bb0fe432e0 100644
--- a/gpu/config/gpu_switching.cc
+++ b/gpu/config/gpu_switching.cc
@@ -15,6 +15,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "gpu/config/gpu_driver_bug_workaround_type.h"
 #include "gpu/config/gpu_info.h"
 #include "ui/gl/gl_context.h"
@@ -92,11 +93,11 @@ bool SwitchableGPUsSupported(const GPUInfo& gpu_info,
 void InitializeSwitchableGPUs(
     const std::vector<int32_t>& driver_bug_workarounds) {
   gl::GLContext::SetSwitchableGPUsSupported();
-  if (std::ranges::contains(driver_bug_workarounds,
+  if (base::Contains(driver_bug_workarounds,
                             FORCE_HIGH_PERFORMANCE_GPU)) {
     gl::GLSurface::SetForcedGpuPreference(gl::GpuPreference::kHighPerformance);
     ForceDiscreteGPU();
-  } else if (std::ranges::contains(driver_bug_workarounds,
+  } else if (base::Contains(driver_bug_workarounds,
                                    FORCE_LOW_POWER_GPU)) {
     gl::GLSurface::SetForcedGpuPreference(gl::GpuPreference::kLowPower);
   }
diff --git a/gpu/ipc/client/gpu_channel_host.cc b/gpu/ipc/client/gpu_channel_host.cc
index a644281e9a..5b2ef6b53f 100644
--- a/gpu/ipc/client/gpu_channel_host.cc
+++ b/gpu/ipc/client/gpu_channel_host.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/atomic_sequence_num.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
 #include "base/metrics/histogram_macros.h"
@@ -399,7 +400,7 @@ bool GpuChannelHost::ConnectionTracker::AddObserverIfNotAlreadyLost(
   if (!is_connected()) {
     return false;
   }
-  CHECK(!std::ranges::contains(observer_list_, obs));
+  CHECK(!base::Contains(observer_list_, obs));
   observer_list_.push_back(obs);
   return true;
 }
diff --git a/media/audio/alsa/audio_manager_alsa.cc b/media/audio/alsa/audio_manager_alsa.cc
index 934fd3d6c2..3082aef4ba 100644
--- a/media/audio/alsa/audio_manager_alsa.cc
+++ b/media/audio/alsa/audio_manager_alsa.cc
@@ -10,6 +10,7 @@
 #include <array>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/free_deleter.h"
 #include "base/metrics/histogram.h"
@@ -221,7 +222,7 @@ void AudioManagerAlsa::AddAlsaDeviceFromSwitch(const char* switch_name,
             switch_name);
     // Only append the specified device if it is not already present on the
     // list.
-    if (!std::ranges::contains(
+    if (!base::Contains(
             *device_names, switch_device_name,
             [](const auto& device_name) { return device_name.unique_id; })) {
       AudioDeviceName name;
diff --git a/media/audio/audio_output_dispatcher_impl.cc b/media/audio/audio_output_dispatcher_impl.cc
index a02fc285d3..2e198c9ee2 100644
--- a/media/audio/audio_output_dispatcher_impl.cc
+++ b/media/audio/audio_output_dispatcher_impl.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/not_fatal_until.h"
 #include "base/task/single_thread_task_runner.h"
@@ -91,7 +92,7 @@ bool AudioOutputDispatcherImpl::StartStream(
   double volume = 0;
   stream_proxy->GetVolume(&volume);
   physical_stream->SetVolume(volume);
-  DCHECK(audio_logs_.contains(physical_stream));
+  DCHECK(base::Contains(audio_logs_, physical_stream));
   AudioLog* const audio_log = audio_logs_[physical_stream].get();
   audio_log->OnSetVolume(volume);
   physical_stream->Start(callback);
@@ -118,7 +119,7 @@ void AudioOutputDispatcherImpl::StreamVolumeSet(AudioOutputProxy* stream_proxy,
   if (it != proxy_to_physical_map_.end()) {
     AudioOutputStream* physical_stream = it->second;
     physical_stream->SetVolume(volume);
-    DCHECK(audio_logs_.contains(physical_stream));
+    DCHECK(base::Contains(audio_logs_, physical_stream));
     audio_logs_[physical_stream]->OnSetVolume(volume);
   }
 }
@@ -204,7 +205,7 @@ void AudioOutputDispatcherImpl::CloseIdleStreams(size_t keep_alive) {
 void AudioOutputDispatcherImpl::StopPhysicalStream(AudioOutputStream* stream) {
   DCHECK(audio_manager()->GetTaskRunner()->BelongsToCurrentThread());
   stream->Stop();
-  DCHECK(audio_logs_.contains(stream));
+  DCHECK(base::Contains(audio_logs_, stream));
   audio_logs_[stream]->OnStopped();
   idle_streams_.push_back(stream);
   close_timer_.Reset();
diff --git a/media/audio/fuchsia/audio_manager_fuchsia.cc b/media/audio/fuchsia/audio_manager_fuchsia.cc
index 85e2fb3594..a930f223fe 100644
--- a/media/audio/fuchsia/audio_manager_fuchsia.cc
+++ b/media/audio/fuchsia/audio_manager_fuchsia.cc
@@ -6,10 +6,10 @@
 
 #include <lib/sys/cpp/component_context.h>
 
-#include <algorithm>
 #include <memory>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/fuchsia/fuchsia_logging.h"
 #include "base/fuchsia/process_context.h"
 #include "base/fuchsia/scheduler.h"
@@ -253,9 +253,9 @@ void AudioManagerFuchsia::OnDeviceRemoved(uint64_t device_token) {
 bool AudioManagerFuchsia::HasAudioDevice(bool is_input) {
   DCHECK(GetTaskRunner()->BelongsToCurrentThread());
 
-  return std::ranges::contains(
-      audio_devices_, is_input,
-      [](const auto& device) { return device.second.is_input; });
+  return base::Contains(audio_devices_, is_input, [](const auto& device) {
+    return device.second.is_input;
+  });
 }
 
 void AudioManagerFuchsia::GetAudioDevices(AudioDeviceNames* device_names,
diff --git a/media/base/audio_converter.cc b/media/base/audio_converter.cc
index 586dcd3494..94d39061b8 100644
--- a/media/base/audio_converter.cc
+++ b/media/base/audio_converter.cc
@@ -13,6 +13,7 @@
 #include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
@@ -88,12 +89,12 @@ AudioConverter::AudioConverter(const AudioParameters& input_params,
 AudioConverter::~AudioConverter() = default;
 
 void AudioConverter::AddInput(InputCallback* input) {
-  DCHECK(!std::ranges::contains(transform_inputs_, input));
+  DCHECK(!base::Contains(transform_inputs_, input));
   transform_inputs_.push_back(input);
 }
 
 void AudioConverter::RemoveInput(InputCallback* input) {
-  DCHECK(std::ranges::contains(transform_inputs_, input));
+  DCHECK(base::Contains(transform_inputs_, input));
   transform_inputs_.remove(input);
 
   if (transform_inputs_.empty())
diff --git a/media/base/channel_mixing_matrix.cc b/media/base/channel_mixing_matrix.cc
index 483be76589..44359c5cb2 100644
--- a/media/base/channel_mixing_matrix.cc
+++ b/media/base/channel_mixing_matrix.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "media/base/channel_mixer.h"
 
 namespace media {
@@ -271,7 +272,7 @@ void ChannelMixingMatrix::AccountFor(Channels ch) {
 }
 
 bool ChannelMixingMatrix::IsUnaccounted(Channels ch) const {
-  return std::ranges::contains(unaccounted_inputs_, ch);
+  return base::Contains(unaccounted_inputs_, ch);
 }
 
 bool ChannelMixingMatrix::IsMonoInputLayout() const {
diff --git a/media/base/frame_buffer_pool.cc b/media/base/frame_buffer_pool.cc
index e90f07036b..2a78ad6955 100644
--- a/media/base/frame_buffer_pool.cc
+++ b/media/base/frame_buffer_pool.cc
@@ -23,6 +23,7 @@
 #include "base/trace_event/memory_allocator_dump.h"
 #include "base/trace_event/memory_dump_manager.h"
 #include "base/trace_event/process_memory_dump.h"
+#include "base/types/cxx23_to_underlying.h"
 
 namespace media {
 
@@ -277,7 +278,7 @@ void FrameBufferPool::OnVideoFrameDestroyed(FrameBuffer* frame_buffer) {
   std::erase_if(frame_buffers_, [now](const std::unique_ptr<FrameBuffer>& buf) {
     return !IsUsedLocked(buf.get()) &&
            now - buf->last_use_time >
-               base::Seconds(std::to_underlying(kStaleFrameLimitSecs));
+               base::Seconds(base::to_underlying(kStaleFrameLimitSecs));
   });
 }
 
diff --git a/media/capture/video/chromeos/camera_3a_controller.cc b/media/capture/video/chromeos/camera_3a_controller.cc
index 3b7d0cf399..3496750c9f 100644
--- a/media/capture/video/chromeos/camera_3a_controller.cc
+++ b/media/capture/video/chromeos/camera_3a_controller.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/trace_event/typed_macros.h"
@@ -108,7 +109,7 @@ Camera3AController::Camera3AController(
     if (available_modes.empty()) {
       return false;
     }
-    if (!std::ranges::contains(
+    if (!base::Contains(
             available_modes,
             base::checked_cast<uint8_t>(
                 cros::mojom::AndroidControlMode::ANDROID_CONTROL_MODE_AUTO))) {
@@ -167,8 +168,8 @@ Camera3AController::Camera3AController(
   // mode should be enough.
   const auto face_mode_simple = cros::mojom::AndroidStatisticsFaceDetectMode::
       ANDROID_STATISTICS_FACE_DETECT_MODE_SIMPLE;
-  if (std::ranges::contains(face_modes,
-                            base::checked_cast<uint8_t>(face_mode_simple))) {
+  if (base::Contains(face_modes,
+                     base::checked_cast<uint8_t>(face_mode_simple))) {
     SetRepeatingCaptureMetadata(
         cros::mojom::CameraMetadataTag::ANDROID_STATISTICS_FACE_DETECT_MODE,
         face_mode_simple);
@@ -177,7 +178,7 @@ Camera3AController::Camera3AController(
   auto request_keys = GetMetadataEntryAsSpan<int32_t>(
       *static_metadata_,
       cros::mojom::CameraMetadataTag::ANDROID_REQUEST_AVAILABLE_REQUEST_KEYS);
-  zero_shutter_lag_supported_ = std::ranges::contains(
+  zero_shutter_lag_supported_ = base::Contains(
       request_keys,
       static_cast<int32_t>(
           cros::mojom::CameraMetadataTag::ANDROID_CONTROL_ENABLE_ZSL));
diff --git a/media/capture/video/chromeos/camera_buffer_factory.cc b/media/capture/video/chromeos/camera_buffer_factory.cc
index c446f37731..34d4835146 100644
--- a/media/capture/video/chromeos/camera_buffer_factory.cc
+++ b/media/capture/video/chromeos/camera_buffer_factory.cc
@@ -4,6 +4,7 @@
 
 #include "media/capture/video/chromeos/camera_buffer_factory.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "components/viz/common/resources/shared_image_format_utils.h"
 #include "gpu/ipc/client/gpu_channel_host.h"
@@ -97,7 +98,7 @@ ChromiumPixelFormat CameraBufferFactory::ResolveStreamBufferFormat(
     cros::mojom::HalPixelFormat hal_format,
     gfx::BufferUsage usage) {
   const auto key = std::make_pair(hal_format, usage);
-  if (resolved_format_usages_.contains(key)) {
+  if (base::Contains(resolved_format_usages_, key)) {
     return resolved_format_usages_[key];
   }
 
diff --git a/media/capture/video/chromeos/camera_device_delegate.cc b/media/capture/video/chromeos/camera_device_delegate.cc
index 68753b2487..f91dca620e 100644
--- a/media/capture/video/chromeos/camera_device_delegate.cc
+++ b/media/capture/video/chromeos/camera_device_delegate.cc
@@ -13,6 +13,7 @@
 
 #include "ash/constants/ash_features.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/no_destructor.h"
@@ -1306,7 +1307,7 @@ gfx::Size CameraDeviceDelegate::GetBlobResolution(
   // resolution specified through Mojo API as a fallback. If it fails too,
   // use the largest resolution as default.
   if (new_blob_resolution.has_value() &&
-      std::ranges::contains(blob_resolutions, *new_blob_resolution)) {
+      base::Contains(blob_resolutions, *new_blob_resolution)) {
     return *new_blob_resolution;
   }
 
@@ -1317,7 +1318,7 @@ gfx::Size CameraDeviceDelegate::GetBlobResolution(
     auto specified_capture_resolution =
         camera_app_device->GetStillCaptureResolution();
     if (!specified_capture_resolution.IsEmpty() &&
-        std::ranges::contains(blob_resolutions, specified_capture_resolution)) {
+        base::Contains(blob_resolutions, specified_capture_resolution)) {
       return specified_capture_resolution;
     }
   }
@@ -1791,7 +1792,7 @@ void CameraDeviceDelegate::DoGetPhotoState(
       cros::mojom::CameraMetadataTag::ANDROID_CONTROL_AE_AVAILABLE_MODES);
   bool support_manual_exposure_time = false;
   if (ae_available_modes.size() > 1 && result_metadata_.ae_mode) {
-    support_manual_exposure_time = std::ranges::contains(
+    support_manual_exposure_time = base::Contains(
         ae_available_modes,
         static_cast<uint8_t>(
             cros::mojom::AndroidControlAeMode::ANDROID_CONTROL_AE_MODE_OFF));
@@ -1830,7 +1831,7 @@ void CameraDeviceDelegate::DoGetPhotoState(
       cros::mojom::CameraMetadataTag::ANDROID_CONTROL_AF_AVAILABLE_MODES);
   bool support_manual_focus_distance = false;
   if (af_available_modes.size() > 1 && result_metadata_.af_mode) {
-    support_manual_focus_distance = std::ranges::contains(
+    support_manual_focus_distance = base::Contains(
         af_available_modes,
         static_cast<uint8_t>(
             cros::mojom::AndroidControlAfMode::ANDROID_CONTROL_AF_MODE_OFF));
diff --git a/media/capture/video/chromeos/camera_hal_delegate.cc b/media/capture/video/chromeos/camera_hal_delegate.cc
index 63445abbde..17cf023d5d 100644
--- a/media/capture/video/chromeos/camera_hal_delegate.cc
+++ b/media/capture/video/chromeos/camera_hal_delegate.cc
@@ -987,7 +987,7 @@ int32_t CameraHalDelegate::GetMaskedModuleID(const std::string& module_id) {
     int vid = UNSAFE_TODO(strtol(module_id.substr(0, 4).c_str(), nullptr, 16));
     int pid = UNSAFE_TODO(strtol(module_id.substr(5, 8).c_str(), nullptr, 16));
     int decimal_module_id = (vid << 16) + pid;
-    if (module_id_set.contains(decimal_module_id)) {
+    if (base::Contains(module_id_set, decimal_module_id)) {
       return decimal_module_id;
     }
   }
diff --git a/media/capture/video/chromeos/camera_metadata_utils.h b/media/capture/video/chromeos/camera_metadata_utils.h
index 29e94623c7..d7246dadf1 100644
--- a/media/capture/video/chromeos/camera_metadata_utils.h
+++ b/media/capture/video/chromeos/camera_metadata_utils.h
@@ -5,9 +5,8 @@
 #ifndef MEDIA_CAPTURE_VIDEO_CHROMEOS_CAMERA_METADATA_UTILS_H_
 #define MEDIA_CAPTURE_VIDEO_CHROMEOS_CAMERA_METADATA_UTILS_H_
 
-#include <algorithm>
-
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "media/capture/capture_export.h"
 #include "media/capture/video/chromeos/mojom/camera_metadata.mojom.h"
 
@@ -101,7 +100,7 @@ CAPTURE_EXPORT cros::mojom::CameraMetadataEntryPtr BuildMetadataEntry(
 
   // Mojo uses int32_t as the underlying type of enum classes, but
   // the camera metadata expect uint8_t for them.
-  if (std::is_enum<T>::value && !std::ranges::contains(kInt32EnumTags, tag)) {
+  if (std::is_enum<T>::value && !base::Contains(kInt32EnumTags, tag)) {
     e->data.push_back(base::checked_cast<uint8_t>(value));
   } else {
     e->data.resize(sizeof(T));
diff --git a/media/capture/video/chromeos/request_manager.cc b/media/capture/video/chromeos/request_manager.cc
index d518471bc4..ea9162644b 100644
--- a/media/capture/video/chromeos/request_manager.cc
+++ b/media/capture/video/chromeos/request_manager.cc
@@ -14,6 +14,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/posix/safe_strerror.h"
@@ -164,7 +165,7 @@ void RequestManager::SetUpStreamsAndBuffers(
   auto request_keys = GetMetadataEntryAsSpan<int32_t>(
       static_metadata,
       cros::mojom::CameraMetadataTag::ANDROID_REQUEST_AVAILABLE_REQUEST_KEYS);
-  zero_shutter_lag_supported_ = std::ranges::contains(
+  zero_shutter_lag_supported_ = base::Contains(
       request_keys,
       static_cast<int32_t>(
           cros::mojom::CameraMetadataTag::ANDROID_CONTROL_ENABLE_ZSL));
diff --git a/media/capture/video/chromeos/token_manager.cc b/media/capture/video/chromeos/token_manager.cc
index 1b832844b2..848486ccf0 100644
--- a/media/capture/video/chromeos/token_manager.cc
+++ b/media/capture/video/chromeos/token_manager.cc
@@ -9,9 +9,9 @@
 #include <sys/types.h>
 #include <unistd.h>
 
-#include <algorithm>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/files/file_util.h"
 #include "base/strings/string_number_conversions.h"
@@ -89,7 +89,7 @@ bool TokenManager::GenerateTestClientToken() {
 base::UnguessableToken TokenManager::GetTokenForTrustedClient(
     cros::mojom::CameraClientType type) {
   base::AutoLock l(client_token_map_lock_);
-  if (!std::ranges::contains(kTrustedClientTypes, type)) {
+  if (!base::Contains(kTrustedClientTypes, type)) {
     return base::UnguessableToken();
   }
   auto& token_set = client_token_map_[type];
diff --git a/media/capture/video/linux/video_capture_device_factory_v4l2.cc b/media/capture/video/linux/video_capture_device_factory_v4l2.cc
index 00864fe858..ef6b134e34 100644
--- a/media/capture/video/linux/video_capture_device_factory_v4l2.cc
+++ b/media/capture/video/linux/video_capture_device_factory_v4l2.cc
@@ -17,6 +17,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_enumerator.h"
 #include "base/files/file_util.h"
 #include "base/notimplemented.h"
@@ -257,7 +258,7 @@ bool VideoCaptureDeviceFactoryV4L2::HasUsableFormats(int fd,
   v4l2_fmtdesc fmtdesc = {};
   fmtdesc.type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
   for (; DoIoctl(fd, VIDIOC_ENUM_FMT, &fmtdesc) == 0; ++fmtdesc.index) {
-    if (std::ranges::contains(usable_fourccs, fmtdesc.pixelformat)) {
+    if (base::Contains(usable_fourccs, fmtdesc.pixelformat)) {
       return true;
     }
   }
diff --git a/media/capture/video/video_capture_device_client.cc b/media/capture/video/video_capture_device_client.cc
index f5abbcadf4..92b8c3096e 100644
--- a/media/capture/video/video_capture_device_client.cc
+++ b/media/capture/video/video_capture_device_client.cc
@@ -15,6 +15,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -682,7 +683,7 @@ VideoCaptureDeviceClient::CreateReadyFrameFromExternalBuffer(
   }
 
   // Register the buffer with the receiver if it is new.
-  if (!std::ranges::contains(buffer_ids_known_by_receiver_, buffer_id)) {
+  if (!base::Contains(buffer_ids_known_by_receiver_, buffer_id)) {
     media::mojom::VideoBufferHandlePtr buffer_handle =
         buffer_pool_->GetVideoBufferHandle(buffer_id);
     receiver_->OnNewBuffer(buffer_id, std::move(buffer_handle));
@@ -750,7 +751,7 @@ VideoCaptureDeviceClient::ReserveOutputBuffer(const gfx::Size& frame_size,
 
   CHECK_NE(VideoCaptureBufferPool::kInvalidId, buffer_id);
 
-  if (!std::ranges::contains(buffer_ids_known_by_receiver_, buffer_id)) {
+  if (!base::Contains(buffer_ids_known_by_receiver_, buffer_id)) {
     media::mojom::VideoBufferHandlePtr buffer_handle =
         buffer_pool_->GetVideoBufferHandle(buffer_id);
     receiver_->OnNewBuffer(buffer_id, std::move(buffer_handle));
diff --git a/media/capture/video/video_capture_feedback.cc b/media/capture/video/video_capture_feedback.cc
index ccc3534adc..bc40245f0c 100644
--- a/media/capture/video/video_capture_feedback.cc
+++ b/media/capture/video/video_capture_feedback.cc
@@ -6,6 +6,7 @@
 
 #include <cmath>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 
 namespace media {
diff --git a/media/capture/video/video_capture_metrics.cc b/media/capture/video/video_capture_metrics.cc
index fce5ef8dc6..ba8af66d22 100644
--- a/media/capture/video/video_capture_metrics.cc
+++ b/media/capture/video/video_capture_metrics.cc
@@ -4,8 +4,7 @@
 
 #include "media/capture/video/video_capture_metrics.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
@@ -198,8 +197,8 @@ VideoEffectStatus GetStatus(bool is_supported, bool is_enabled) {
 
 void LogCaptureDeviceEffects(mojom::PhotoStatePtr photo_state) {
   const bool has_background_blur =
-      std::ranges::contains(photo_state->supported_background_blur_modes,
-                            mojom::BackgroundBlurMode::BLUR);
+      base::Contains(photo_state->supported_background_blur_modes,
+                     mojom::BackgroundBlurMode::BLUR);
   const bool background_blur_enabled =
       photo_state->background_blur_mode != mojom::BackgroundBlurMode::OFF;
   UMA_HISTOGRAM_ENUMERATION(
@@ -215,10 +214,10 @@ void LogCaptureDeviceEffects(mojom::PhotoStatePtr photo_state) {
 
   const bool has_eye_gaze_correction =
 
-      std::ranges::contains(photo_state->supported_eye_gaze_correction_modes,
-                            mojom::EyeGazeCorrectionMode::ON) ||
-      std::ranges::contains(photo_state->supported_eye_gaze_correction_modes,
-                            mojom::EyeGazeCorrectionMode::STARE);
+      base::Contains(photo_state->supported_eye_gaze_correction_modes,
+                     mojom::EyeGazeCorrectionMode::ON) ||
+      base::Contains(photo_state->supported_eye_gaze_correction_modes,
+                     mojom::EyeGazeCorrectionMode::STARE);
   const bool eye_gaze_correction_enabled =
       photo_state->current_eye_gaze_correction_mode !=
       mojom::EyeGazeCorrectionMode::OFF;
diff --git a/media/capture/video/win/video_capture_device_factory_win.cc b/media/capture/video/win/video_capture_device_factory_win.cc
index b060a18380..727232b456 100644
--- a/media/capture/video/win/video_capture_device_factory_win.cc
+++ b/media/capture/video/win/video_capture_device_factory_win.cc
@@ -28,6 +28,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -314,10 +315,10 @@ std::string GetDeviceModelId(const std::string& device_id) {
 
 bool DevicesInfoContainsDeviceId(const DevicesInfo& devices_info,
                                  const std::string& device_id) {
-  return std::ranges::contains(devices_info, device_id,
-                               [](const VideoCaptureDeviceInfo& device_info) {
-                                 return device_info.descriptor.device_id;
-                               });
+  return base::Contains(devices_info, device_id,
+                        [](const VideoCaptureDeviceInfo& device_info) {
+                          return device_info.descriptor.device_id;
+                        });
 }
 
 // Returns a non DirectShow descriptor DevicesInfo with the provided name and
diff --git a/media/capture/video/win/video_capture_device_mf_win.cc b/media/capture/video/win/video_capture_device_mf_win.cc
index bac6ab031c..8cac331daf 100644
--- a/media/capture/video/win/video_capture_device_mf_win.cc
+++ b/media/capture/video/win/video_capture_device_mf_win.cc
@@ -19,6 +19,7 @@
 #include <thread>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
diff --git a/media/cast/encoding/media_video_encoder_wrapper.cc b/media/cast/encoding/media_video_encoder_wrapper.cc
index cd803559aa..7b96a389a2 100644
--- a/media/cast/encoding/media_video_encoder_wrapper.cc
+++ b/media/cast/encoding/media_video_encoder_wrapper.cc
@@ -13,6 +13,7 @@
 #include "base/functional/callback_helpers.h"
 #include "base/task/bind_post_task.h"
 #include "base/task/thread_pool.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "media/base/async_destroy_video_encoder.h"
 #include "media/base/encoder_status.h"
 #include "media/base/media_util.h"
@@ -68,7 +69,7 @@ std::unique_ptr<media::VideoEncoder> CreateSoftwareEncoder(VideoCodec codec) {
       return std::make_unique<media::Av1VideoEncoder>();
 #endif  // BUILDFLAG(ENABLE_LIBAOM)
     default:
-      NOTREACHED() << "Unhandled codec. value=" << std::to_underlying(codec);
+      NOTREACHED() << "Unhandled codec. value=" << base::to_underlying(codec);
   }
 }
 
@@ -86,7 +87,7 @@ VideoCodecProfile ToProfile(VideoCodec codec) {
     case VideoCodec::kAV1:
       return AV1PROFILE_PROFILE_MAIN;
     default:
-      NOTREACHED() << "Unhandled codec. value=" << std::to_underlying(codec);
+      NOTREACHED() << "Unhandled codec. value=" << base::to_underlying(codec);
   }
 }
 
diff --git a/media/cast/encoding/vpx_quantizer_parser.cc b/media/cast/encoding/vpx_quantizer_parser.cc
index 9c694eaae4..e31dbe70ba 100644
--- a/media/cast/encoding/vpx_quantizer_parser.cc
+++ b/media/cast/encoding/vpx_quantizer_parser.cc
@@ -6,11 +6,11 @@
 
 #include <array>
 #include <optional>
-#include <utility>
 
 #include "base/logging.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/raw_span.h"
+#include "base/types/cxx23_to_underlying.h"
 
 namespace media::cast {
 namespace {
@@ -163,7 +163,7 @@ void ParseSegmentHeader(VpxBitReader* bit_reader) {
   const VpxBitReader::BitResult update_mb_segmentation_data =
       bit_reader->DecodeBit();
   DVLOG(2) << "update_mb_segmentation_data:"
-           << std::to_underlying(update_mb_segmentation_data);
+           << base::to_underlying(update_mb_segmentation_data);
   if (update_mb_segmentation_map == VpxBitReader::BitResult::kError ||
       update_mb_segmentation_data == VpxBitReader::BitResult::kError) {
     return;
diff --git a/media/cast/logging/log_event_dispatcher.cc b/media/cast/logging/log_event_dispatcher.cc
index 1fae0334c8..20bd34a9c1 100644
--- a/media/cast/logging/log_event_dispatcher.cc
+++ b/media/cast/logging/log_event_dispatcher.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -134,7 +135,7 @@ void LogEventDispatcher::Impl::DispatchBatchOfEvents(
 }
 
 void LogEventDispatcher::Impl::Subscribe(RawEventSubscriber* subscriber) {
-  CHECK(!std::ranges::contains(subscribers_, subscriber));
+  CHECK(!base::Contains(subscribers_, subscriber));
   subscribers_.push_back(subscriber);
 }
 
diff --git a/media/filters/decoder_stream.cc b/media/filters/decoder_stream.cc
index 2f77bfa42e..f0c0552ac6 100644
--- a/media/filters/decoder_stream.cc
+++ b/media/filters/decoder_stream.cc
@@ -15,6 +15,7 @@
 #include "base/task/bind_post_task.h"
 #include "base/task/sequenced_task_runner.h"
 #include "base/trace_event/trace_event.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "media/base/cdm_context.h"
 #include "media/base/decoder_buffer.h"
 #include "media/base/limits.h"
@@ -182,7 +183,7 @@ void DecoderStream<StreamType>::Read(ReadCB read_cb) {
   DCHECK(task_runner_->RunsTasksInCurrentSequence());
   DCHECK(state_ != State::kStateUninitialized &&
          state_ != State::kStateInitializing)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
   // No two reads in the flight at any time.
   DCHECK(!read_cb_);
   // No read during resetting or stopping process.
@@ -386,7 +387,7 @@ void DecoderStream<StreamType>::OnDecoderSelected(
   DCHECK(task_runner_->RunsTasksInCurrentSequence());
   DCHECK(state_ == State::kStateInitializing ||
          state_ == State::kStateReinitializingDecoder)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
 
   if (state_ == State::kStateInitializing) {
     DCHECK(init_cb_);
@@ -529,7 +530,7 @@ void DecoderStream<StreamType>::DecodeInternal(
   FUNCTION_DVLOG(3);
   DCHECK(state_ == State::kStateNormal ||
          state_ == State::kStateFlushingDecoder)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
   DCHECK_LT(pending_decode_requests_, GetMaxDecodeRequests());
   DCHECK(!reset_cb_);
   DCHECK(buffer);
@@ -577,7 +578,7 @@ void DecoderStream<StreamType>::OnDecodeDone(
       << ": " << static_cast<int>(status.code());
   DCHECK(state_ == State::kStateNormal ||
          state_ == State::kStateFlushingDecoder || state_ == State::kStateError)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
   DCHECK_GT(pending_decode_requests_, 0);
 
   --pending_decode_requests_;
@@ -690,7 +691,7 @@ void DecoderStream<StreamType>::OnDecodeOutputReady(
   DCHECK(output);
   DCHECK(state_ == State::kStateNormal ||
          state_ == State::kStateFlushingDecoder || state_ == State::kStateError)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
 
   if (state_ == State::kStateError) {
     DCHECK(!read_cb_);
@@ -797,7 +798,7 @@ void DecoderStream<StreamType>::OnBuffersReady(
     DCHECK(state_ == State::kStateError ||
            state_ == State::kStateReinitializingDecoder ||
            state_ == State::kStateNormal)
-        << std::to_underlying(state_);
+        << base::to_underlying(state_);
   }
   pending_demuxer_read_ = false;
 
@@ -1023,7 +1024,7 @@ void DecoderStream<StreamType>::ResetDecoder() {
   DCHECK(state_ == State::kStateNormal ||
          state_ == State::kStateFlushingDecoder ||
          state_ == State::kStateError || state_ == State::kStateEndOfStream)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
   DCHECK(reset_cb_);
 
   decoder_->Reset(base::BindOnce(&DecoderStream<StreamType>::OnDecoderReset,
@@ -1037,7 +1038,7 @@ void DecoderStream<StreamType>::OnDecoderReset() {
   DCHECK(state_ == State::kStateNormal ||
          state_ == State::kStateFlushingDecoder ||
          state_ == State::kStateError || state_ == State::kStateEndOfStream)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
   // If Reset() was called during pending read, read callback should be fired
   // before the reset callback is fired.
   DCHECK(!read_cb_);
@@ -1078,7 +1079,7 @@ void DecoderStream<StreamType>::MaybePrepareAnotherOutput() {
          state_ == State::kStateFlushingDecoder ||
          state_ == State::kStateEndOfStream ||
          state_ == State::kStateReinitializingDecoder)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
 
   // If there's nothing to prepare or a prepare is underway, we're done.
   if (!prepare_cb_ || unprepared_outputs_.empty() || preparing_output_)
@@ -1112,7 +1113,7 @@ void DecoderStream<StreamType>::OnPreparedOutputReady(
          state_ == State::kStateFlushingDecoder ||
          state_ == State::kStateEndOfStream ||
          state_ == State::kStateReinitializingDecoder)
-      << std::to_underlying(state_);
+      << base::to_underlying(state_);
   DCHECK(!reset_cb_);
   DCHECK(!unprepared_outputs_.empty());
   DCHECK(preparing_output_);
diff --git a/media/filters/ffmpeg_glue.cc b/media/filters/ffmpeg_glue.cc
index 6ecbf5d8b3..d7e06e0940 100644
--- a/media/filters/ffmpeg_glue.cc
+++ b/media/filters/ffmpeg_glue.cc
@@ -4,14 +4,13 @@
 
 #include "media/filters/ffmpeg_glue.h"
 
-#include <utility>
-
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/no_destructor.h"
 #include "base/notreached.h"
 #include "base/strings/string_util.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "media/base/container_names.h"
 #include "media/base/media_switches.h"
 #include "media/base/supported_types.h"
@@ -75,10 +74,10 @@ static int64_t AVIOSeekOperation(void* opaque, int64_t offset, int whence) {
 static void LogContainer(bool is_local_file,
                          container_names::MediaContainerName container) {
   base::UmaHistogramSparse("Media.DetectedContainer",
-                           std::to_underlying(container));
+                           base::to_underlying(container));
   if (is_local_file) {
     base::UmaHistogramSparse("Media.DetectedContainer.Local",
-                             std::to_underlying(container));
+                             base::to_underlying(container));
   }
 }
 
diff --git a/media/filters/offloading_video_decoder.cc b/media/filters/offloading_video_decoder.cc
index 3165005bdd..6f70b2ae41 100644
--- a/media/filters/offloading_video_decoder.cc
+++ b/media/filters/offloading_video_decoder.cc
@@ -4,9 +4,9 @@
 
 #include "media/filters/offloading_video_decoder.h"
 
-#include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/synchronization/atomic_flag.h"
@@ -96,7 +96,7 @@ void OffloadingVideoDecoder::Initialize(const VideoDecoderConfig& config,
   const bool disable_offloading =
       config.is_encrypted() ||
       config.coded_size().width() < min_offloading_width_ ||
-      !std::ranges::contains(supported_codecs_, config.codec());
+      !base::Contains(supported_codecs_, config.codec());
 
   if (initialized_) {
     initialized_ = false;
diff --git a/media/filters/source_buffer_state.cc b/media/filters/source_buffer_state.cc
index a3fdc0fd32..2c6a620172 100644
--- a/media/filters/source_buffer_state.cc
+++ b/media/filters/source_buffer_state.cc
@@ -7,12 +7,12 @@
 #include <algorithm>
 #include <set>
 #include <string_view>
-#include <utility>
 
 #include "base/command_line.h"
 #include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
 #include "base/strings/string_number_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "media/base/media_switches.h"
 #include "media/base/media_track.h"
@@ -717,7 +717,7 @@ bool SourceBufferState::OnNewConfigs(std::unique_ptr<MediaTracks> tracks) {
                                            media_log_);
     } else {
       MEDIA_LOG(ERROR, media_log_) << "Error: unsupported media track type "
-                                   << std::to_underlying(track->type());
+                                   << base::to_underlying(track->type());
       return false;
     }
   }
diff --git a/media/formats/hls/types.cc b/media/formats/hls/types.cc
index fe282edfe1..4a407e15ea 100644
--- a/media/formats/hls/types.cc
+++ b/media/formats/hls/types.cc
@@ -9,6 +9,7 @@
 #include <functional>
 #include <limits>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
@@ -113,7 +114,7 @@ DecimalResolution::Parse(ResolvedSourceString str) {
 
 namespace {
 bool IsOneOf(char c, std::string_view set) {
-  return set.contains(c);
+  return base::Contains(set, c);
 }
 
 // Returns the substring matching a valid AttributeName, advancing `source_str`
diff --git a/media/formats/webm/webm_cluster_parser.cc b/media/formats/webm/webm_cluster_parser.cc
index 4d59578ec2..fc55e12968 100644
--- a/media/formats/webm/webm_cluster_parser.cc
+++ b/media/formats/webm/webm_cluster_parser.cc
@@ -4,7 +4,6 @@
 
 #include "media/formats/webm/webm_cluster_parser.h"
 
-#include <bit>
 #include <memory>
 #include <utility>
 #include <vector>
@@ -15,6 +14,7 @@
 #include "base/numerics/byte_conversions.h"
 #include "base/numerics/checked_math.h"
 #include "base/numerics/safe_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/optional_util.h"
 #include "media/base/decrypt_config.h"
 #include "media/base/stream_parser_buffer.h"
@@ -365,7 +365,7 @@ bool WebMClusterParser::OnBinary(int id, const uint8_t* data_ptr, int size) {
       return true;
 
     case kWebMIdBlockAdditional: {
-      uint64_t block_add_id = std::byteswap(block_add_id_);
+      uint64_t block_add_id = base::ByteSwap(block_add_id_);
       if (block_additional_data_) {
         // TODO(vigneshv): Technically, more than 1 BlockAdditional is allowed
         // as per matroska spec. But for now we don't have a use case to
@@ -744,11 +744,11 @@ base::TimeDelta WebMClusterParser::Track::GetDurationEstimate() {
     DVLOG(3) << __func__ << " : using hardcoded default duration";
     if (track_type_ == TrackType::AUDIO) {
       duration = base::Milliseconds(
-          std::to_underlying(kDefaultAudioBufferDurationInMs));
+          base::to_underlying(kDefaultAudioBufferDurationInMs));
     } else {
       // Video tracks use the larger video default duration.
       duration = base::Milliseconds(
-          std::to_underlying(kDefaultVideoBufferDurationInMs));
+          base::to_underlying(kDefaultVideoBufferDurationInMs));
     }
   } else {
     // Use max duration to minimize the risk of introducing gaps in the buffered
diff --git a/media/gpu/android/maybe_render_early_manager.cc b/media/gpu/android/maybe_render_early_manager.cc
index d9b5e3fb6b..6708536079 100644
--- a/media/gpu/android/maybe_render_early_manager.cc
+++ b/media/gpu/android/maybe_render_early_manager.cc
@@ -4,9 +4,9 @@
 
 #include "media/gpu/android/maybe_render_early_manager.h"
 
-#include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
 #include "base/task/sequenced_task_runner.h"
@@ -37,8 +37,7 @@ class GpuMaybeRenderEarlyImpl {
     codec_image_holder->codec_image_raw()->AddUnusedCB(base::BindOnce(
         &GpuMaybeRenderEarlyImpl::OnImageUnused, weak_factory_.GetWeakPtr()));
 
-    DCHECK(
-        !std::ranges::contains(images_, codec_image_holder->codec_image_raw()));
+    DCHECK(!base::Contains(images_, codec_image_holder->codec_image_raw()));
     images_.push_back(codec_image_holder->codec_image_raw());
 
     // Add |image| to our current image group.  This makes sure that any overlay
@@ -55,7 +54,7 @@ class GpuMaybeRenderEarlyImpl {
  private:
   void OnImageUnused(CodecImage* image) {
     // |image| is no longer used, so try to render a new image speculatively.
-    DCHECK(std::ranges::contains(images_, image));
+    DCHECK(base::Contains(images_, image));
     // Remember that |image_group_| might not be the same one that |image|
     // belongs to.
     std::erase(images_, image);
diff --git a/media/gpu/android/ndk_video_encode_accelerator_tests.cc b/media/gpu/android/ndk_video_encode_accelerator_tests.cc
index b2c97a7d13..bb654d362d 100644
--- a/media/gpu/android/ndk_video_encode_accelerator_tests.cc
+++ b/media/gpu/android/ndk_video_encode_accelerator_tests.cc
@@ -14,6 +14,7 @@
 #include "base/android/scoped_hardware_buffer_handle.h"
 #include "base/base64.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
@@ -142,7 +143,7 @@ class NdkVideoEncoderAcceleratorTest
     pixel_format_ = args.pixel_format;
 
     auto profiles = MakeNdkAccelerator()->GetSupportedProfiles();
-    bool codec_supported = std::ranges::contains(
+    bool codec_supported = base::Contains(
         profiles, profile_, &VideoEncodeAccelerator::SupportedProfile::profile);
 
     if (!codec_supported) {
diff --git a/media/gpu/chromeos/generic_dmabuf_video_frame_mapper.cc b/media/gpu/chromeos/generic_dmabuf_video_frame_mapper.cc
index 5fe29dbdf6..728d956374 100644
--- a/media/gpu/chromeos/generic_dmabuf_video_frame_mapper.cc
+++ b/media/gpu/chromeos/generic_dmabuf_video_frame_mapper.cc
@@ -6,12 +6,12 @@
 
 #include <sys/mman.h>
 
-#include <algorithm>
 #include <array>
 #include <utility>
 #include <vector>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/memory/ptr_util.h"
@@ -89,7 +89,7 @@ bool IsFormatSupported(VideoPixelFormat format) {
       // Compressed format.
       PIXEL_FORMAT_MJPEG,
   };
-  return std::ranges::contains(supported_formats, format);
+  return base::Contains(supported_formats, format);
 }
 
 }  // namespace
diff --git a/media/gpu/chromeos/image_processor_factory.cc b/media/gpu/chromeos/image_processor_factory.cc
index f1ab037776..4689de240f 100644
--- a/media/gpu/chromeos/image_processor_factory.cc
+++ b/media/gpu/chromeos/image_processor_factory.cc
@@ -6,8 +6,7 @@
 
 #include <stddef.h>
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -61,7 +60,7 @@ std::unique_ptr<ImageProcessor> CreateVaapiImageProcessorWithInputCandidates(
       VaapiWrapper::GetVppSupportedFormats();
   std::optional<PixelLayoutCandidate> chosen_input_candidate;
   for (const auto& input_candidate : input_candidates) {
-    if (std::ranges::contains(vpp_supported_formats, input_candidate.fourcc) &&
+    if (base::Contains(vpp_supported_formats, input_candidate.fourcc) &&
         VaapiWrapper::IsVppResolutionAllowed(input_candidate.size)) {
       chosen_input_candidate = input_candidate;
       break;
@@ -141,8 +140,7 @@ std::unique_ptr<ImageProcessor> CreateV4L2ImageProcessorWithInputCandidates(
     const Fourcc input_fourcc = input_candidate.fourcc;
     const gfx::Size& input_size = input_candidate.size;
 
-    if (!std::ranges::contains(supported_input_pixfmts,
-                               input_fourcc.ToV4L2PixFmt()))
+    if (!base::Contains(supported_input_pixfmts, input_fourcc.ToV4L2PixFmt()))
       continue;
 
     // Ideally the ImageProcessor would be able to scale and crop |input_size|
diff --git a/media/gpu/chromeos/video_decoder_pipeline.cc b/media/gpu/chromeos/video_decoder_pipeline.cc
index 742c94d7a0..4dbb443477 100644
--- a/media/gpu/chromeos/video_decoder_pipeline.cc
+++ b/media/gpu/chromeos/video_decoder_pipeline.cc
@@ -4,11 +4,11 @@
 
 #include "media/gpu/chromeos/video_decoder_pipeline.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/memory/ptr_util.h"
@@ -61,13 +61,12 @@ std::optional<Fourcc> PickRenderableFourcc(
     const std::vector<Fourcc>& renderable_fourccs,
     const std::vector<Fourcc>& candidates,
     std::optional<Fourcc> preferred_fourcc) {
-  if (preferred_fourcc &&
-      std::ranges::contains(candidates, *preferred_fourcc) &&
-      std::ranges::contains(renderable_fourccs, *preferred_fourcc)) {
+  if (preferred_fourcc && base::Contains(candidates, *preferred_fourcc) &&
+      base::Contains(renderable_fourccs, *preferred_fourcc)) {
     return preferred_fourcc;
   }
   for (const auto& value : renderable_fourccs) {
-    if (std::ranges::contains(candidates, value))
+    if (base::Contains(candidates, value))
       return value;
   }
   return std::nullopt;
diff --git a/media/gpu/mac/vt_video_encode_accelerator_mac.mm b/media/gpu/mac/vt_video_encode_accelerator_mac.mm
index c332cf5150..8e0b0495a3 100644
--- a/media/gpu/mac/vt_video_encode_accelerator_mac.mm
+++ b/media/gpu/mac/vt_video_encode_accelerator_mac.mm
@@ -6,13 +6,13 @@
 
 #import <Foundation/Foundation.h>
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 
 #include "base/apple/bridging.h"
 #include "base/apple/foundation_util.h"
 #include "base/apple/osstatus_logging.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/logging.h"
 #include "base/mac/mac_util.h"
@@ -231,8 +231,8 @@ bool IsHardwareEncoder(VTSessionRef compression_session) {
   if (VTSessionCopyProperty(
           compression_session, kVTCompressionPropertyKey_EncoderID,
           kCFAllocatorDefault, encoder_id.InitializeInto()) == noErr) {
-    if (std::ranges::contains(kRealtimeHardwareEncoderIDs,
-                              base::SysCFStringRefToUTF8(encoder_id.get()))) {
+    if (base::Contains(kRealtimeHardwareEncoderIDs,
+                       base::SysCFStringRefToUTF8(encoder_id.get()))) {
       DVLOG(1) << "But " << encoder_id.get() << " is a known hardware encoder";
       return true;
     }
@@ -578,8 +578,8 @@ EncoderStatus VTVideoEncodeAccelerator::Initialize(
         << VideoPixelFormatToString(config.input_format);
     return {EncoderStatus::Codes::kEncoderInitializationError};
   }
-  if (!std::ranges::contains(GetSupportedVideoCodecProfiles(),
-                             config.output_profile)) {
+  if (!base::Contains(GetSupportedVideoCodecProfiles(),
+                      config.output_profile)) {
     MEDIA_LOG(ERROR, media_log) << "Output profile not supported= "
                                 << GetProfileName(config.output_profile);
     return {EncoderStatus::Codes::kEncoderInitializationError};
diff --git a/media/gpu/test/video_bitstream.cc b/media/gpu/test/video_bitstream.cc
index 1c1772e1db..460077cce5 100644
--- a/media/gpu/test/video_bitstream.cc
+++ b/media/gpu/test/video_bitstream.cc
@@ -7,6 +7,7 @@
 #include <optional>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/files/memory_mapped_file.h"
 #include "base/json/json_reader.h"
@@ -190,8 +191,8 @@ std::unique_ptr<VideoBitstream> VideoBitstream::Create(
       std::find_if(std::cbegin(kKeyFrameLessResolutionChangeFiles),
                    std::cend(kKeyFrameLessResolutionChangeFiles),
                    [filepath = data_file_path.value()](base::FilePath substr) {
-                     return base::ToLowerASCII(filepath).contains(
-                         base::ToLowerASCII(substr.value()));
+                     return base::Contains(base::ToLowerASCII(filepath),
+                                           base::ToLowerASCII(substr.value()));
                    });
   return base::WrapUnique(
       new VideoBitstream(std::move(memory_mapped_file), metadata));
diff --git a/media/gpu/test/video_decode_accelerator_tests.cc b/media/gpu/test/video_decode_accelerator_tests.cc
index 541e1ea3f3..bee4afec03 100644
--- a/media/gpu/test/video_decode_accelerator_tests.cc
+++ b/media/gpu/test/video_decode_accelerator_tests.cc
@@ -7,6 +7,7 @@
 
 #include "base/base_switches.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/cpu.h"
 #include "base/functional/callback_helpers.h"
 #include "base/numerics/safe_conversions.h"
diff --git a/media/gpu/test/video_encode_accelerator_perf_tests.cc b/media/gpu/test/video_encode_accelerator_perf_tests.cc
index e4c613c6a0..2fca239b3e 100644
--- a/media/gpu/test/video_encode_accelerator_perf_tests.cc
+++ b/media/gpu/test/video_encode_accelerator_perf_tests.cc
@@ -12,6 +12,7 @@
 
 #include "base/base_switches.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/files/file_util.h"
 #include "base/json/json_writer.h"
@@ -809,8 +810,7 @@ class VideoEncoderTest : public ::testing::Test {
         }
       }
     }
-    LOG_ASSERT(
-        !std::ranges::contains(bitstream_processors, nullptr,
+    LOG_ASSERT(!base::Contains(bitstream_processors, nullptr,
                                &std::unique_ptr<BitstreamProcessor>::get));
     return bitstream_processors;
   }
diff --git a/media/gpu/test/video_encoder/decoder_buffer_validator.cc b/media/gpu/test/video_encoder/decoder_buffer_validator.cc
index 05da9517dd..c8f8a0706c 100644
--- a/media/gpu/test/video_encoder/decoder_buffer_validator.cc
+++ b/media/gpu/test/video_encoder/decoder_buffer_validator.cc
@@ -7,6 +7,7 @@
 #include <set>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/numerics/safe_conversions.h"
@@ -911,7 +912,7 @@ bool VP9Validator::ValidateSVCStream(const DecoderBuffer& decoder_buffer,
                    << static_cast<int>(ref_frame_index);
         return false;
       }
-      if (used_indices.contains(ref_frame_index)) {
+      if (base::Contains(used_indices, ref_frame_index)) {
         // |header.ref_frame_index| might have the same indices because an
         // encoder fills the same index if the actually used ref frames is less
         // than |kVp9NumRefsPerFrame|.
@@ -1083,7 +1084,7 @@ bool VP9Validator::ValidateSmodeStream(const DecoderBuffer& decoder_buffer,
                    << static_cast<int>(ref_frame_index);
         return false;
       }
-      if (used_indices.contains(ref_frame_index)) {
+      if (base::Contains(used_indices, ref_frame_index)) {
         // |header.ref_frame_index| might have the same indices because an
         // encoder fills the same index if the actually used ref frames is less
         // than |kVp9NumRefsPerFrame|.
@@ -1277,7 +1278,7 @@ bool AV1Validator::ValidateTemporalSVCStream(
                    << static_cast<int>(ref_frame_index);
         return false;
       }
-      if (used_indices.contains(ref_frame_index)) {
+      if (base::Contains(used_indices, ref_frame_index)) {
         // |header.ref_frame_index| might have the same indices because an
         // encoder fills the same index if the actually used ref frames is less
         // than |kNumReferenceFrameTypes|.
diff --git a/media/gpu/v4l2/legacy/v4l2_video_decoder_backend_stateful.cc b/media/gpu/v4l2/legacy/v4l2_video_decoder_backend_stateful.cc
index d4f48f6594..e41c3f302b 100644
--- a/media/gpu/v4l2/legacy/v4l2_video_decoder_backend_stateful.cc
+++ b/media/gpu/v4l2/legacy/v4l2_video_decoder_backend_stateful.cc
@@ -9,13 +9,13 @@
 
 #include "media/gpu/v4l2/legacy/v4l2_video_decoder_backend_stateful.h"
 
-#include <algorithm>
 #include <cstddef>
 #include <memory>
 #include <optional>
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_macros.h"
@@ -768,7 +768,7 @@ bool V4L2StatefulVideoDecoderBackend::IsSupportedProfile(
     for (const auto& entry : profiles)
       supported_profiles_.push_back(entry.profile);
   }
-  return std::ranges::contains(supported_profiles_, profile);
+  return base::Contains(supported_profiles_, profile);
 }
 
 bool V4L2StatefulVideoDecoderBackend::StopInputQueueOnResChange() const {
diff --git a/media/gpu/v4l2/test/h265_decoder.cc b/media/gpu/v4l2/test/h265_decoder.cc
index cfc8d9f7db..5098e4a3e2 100644
--- a/media/gpu/v4l2/test/h265_decoder.cc
+++ b/media/gpu/v4l2/test/h265_decoder.cc
@@ -11,6 +11,7 @@
 
 #include <linux/videodev2.h>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/notreached.h"
diff --git a/media/gpu/v4l2/test/video_decoder.cc b/media/gpu/v4l2/test/video_decoder.cc
index e8fcc1ce8c..2c936d938b 100644
--- a/media/gpu/v4l2/test/video_decoder.cc
+++ b/media/gpu/v4l2/test/video_decoder.cc
@@ -14,6 +14,7 @@
 #include <vector>
 
 #include "base/bits.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "media/base/video_types.h"
 #include "media/gpu/v4l2/test/upstream_pix_fmt.h"
@@ -63,7 +64,7 @@ void VideoDecoder::NegotiateCAPTUREFormat() {
   // may prefer a different format than what is needed. If
   // not, negotiations need to be done to see if the preferred format can
   // be used.
-  if (!std::ranges::contains(kPreferredFormats, fourcc)) {
+  if (!base::Contains(kPreferredFormats, fourcc)) {
     bool format_found = false;
     for (const auto& preferred_fourcc : kPreferredFormats) {
       VLOG(1) << "Trying to see if preferred format ("
diff --git a/media/gpu/v4l2/v4l2_device.cc b/media/gpu/v4l2/v4l2_device.cc
index bd84a19341..ea35d10be8 100644
--- a/media/gpu/v4l2/v4l2_device.cc
+++ b/media/gpu/v4l2/v4l2_device.cc
@@ -24,6 +24,7 @@
 #include <algorithm>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/not_fatal_until.h"
@@ -427,8 +428,8 @@ bool V4L2Device::CanCreateEGLImageFrom(const Fourcc fourcc) const {
 #endif
   };
 
-  return std::ranges::contains(kEGLImageDrmFmtsSupported,
-                               V4L2PixFmtToDrmFormat(fourcc.ToV4L2PixFmt()));
+  return base::Contains(kEGLImageDrmFmtsSupported,
+                        V4L2PixFmtToDrmFormat(fourcc.ToV4L2PixFmt()));
 }
 
 std::vector<uint32_t> V4L2Device::PreferredInputFormat(Type type) const {
@@ -569,7 +570,7 @@ V4L2Device::EnumerateSupportedDecodeProfiles(
                                 V4L2_BUF_TYPE_VIDEO_OUTPUT_MPLANE);
 
   for (uint32_t pixelformat : v4l2_codecs_as_pix_fmts) {
-    if (!std::ranges::contains(pixelformats, pixelformat)) {
+    if (!base::Contains(pixelformats, pixelformat)) {
       continue;
     }
 
@@ -1038,7 +1039,7 @@ std::string V4L2Device::GetDevicePathFor(Type type, uint32_t pixfmt) {
   const Devices& devices = GetDevicesForType(type);
 
   for (const auto& device : devices) {
-    if (std::ranges::contains(device.second, pixfmt)) {
+    if (base::Contains(device.second, pixfmt)) {
       return device.first;
     }
   }
diff --git a/media/gpu/v4l2/v4l2_vda_helpers.cc b/media/gpu/v4l2/v4l2_vda_helpers.cc
index 0897c8a679..734a3ca3d8 100644
--- a/media/gpu/v4l2/v4l2_vda_helpers.cc
+++ b/media/gpu/v4l2/v4l2_vda_helpers.cc
@@ -12,6 +12,7 @@
 #include <algorithm>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/task/sequenced_task_runner.h"
 #include "base/types/to_address.h"
@@ -34,7 +35,7 @@ std::optional<Fourcc> FindImageProcessorInputFormat(V4L2Device* vda_device) {
   memset(&fmtdesc, 0, sizeof(fmtdesc));
   fmtdesc.type = V4L2_BUF_TYPE_VIDEO_CAPTURE_MPLANE;
   while (vda_device->Ioctl(VIDIOC_ENUM_FMT, &fmtdesc) == 0) {
-    if (std::ranges::contains(processor_input_formats, fmtdesc.pixelformat)) {
+    if (base::Contains(processor_input_formats, fmtdesc.pixelformat)) {
       DVLOGF(3) << "Image processor input format=" << fmtdesc.description;
       return Fourcc::FromV4L2PixFmt(fmtdesc.pixelformat);
     }
diff --git a/media/gpu/v4l2/v4l2_video_decoder.cc b/media/gpu/v4l2/v4l2_video_decoder.cc
index 3125d6e506..92f2094a51 100644
--- a/media/gpu/v4l2/v4l2_video_decoder.cc
+++ b/media/gpu/v4l2/v4l2_video_decoder.cc
@@ -13,6 +13,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/logging.h"
@@ -587,7 +588,7 @@ bool V4L2VideoDecoder::SetupInputFormat() {
   const auto v4l2_codecs_as_pix_fmts = EnumerateSupportedPixFmts(
       base::BindRepeating(&V4L2Device::Ioctl, device_),
       V4L2_BUF_TYPE_VIDEO_OUTPUT_MPLANE);
-  if (!std::ranges::contains(v4l2_codecs_as_pix_fmts, input_format_fourcc_)) {
+  if (!base::Contains(v4l2_codecs_as_pix_fmts, input_format_fourcc_)) {
     DVLOGF(1) << FourccToString(input_format_fourcc_)
               << " not recognised, skipping...";
     return false;
diff --git a/media/gpu/v4l2/v4l2_video_decoder_backend_stateless.cc b/media/gpu/v4l2/v4l2_video_decoder_backend_stateless.cc
index bb920a8d61..2c1d8cdb4e 100644
--- a/media/gpu/v4l2/v4l2_video_decoder_backend_stateless.cc
+++ b/media/gpu/v4l2/v4l2_video_decoder_backend_stateless.cc
@@ -13,9 +13,9 @@
 #include <linux/media.h>
 #include <sys/ioctl.h>
 
-#include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_macros.h"
@@ -715,7 +715,7 @@ bool V4L2StatelessVideoDecoderBackend::IsSupportedProfile(
     for (const auto& entry : profiles)
       supported_profiles_.push_back(entry.profile);
   }
-  return std::ranges::contains(supported_profiles_, profile);
+  return base::Contains(supported_profiles_, profile);
 }
 
 bool V4L2StatelessVideoDecoderBackend::CreateDecoder() {
diff --git a/media/gpu/vaapi/vaapi_image_processor_backend.cc b/media/gpu/vaapi/vaapi_image_processor_backend.cc
index 9f0bc99c3f..39252d372d 100644
--- a/media/gpu/vaapi/vaapi_image_processor_backend.cc
+++ b/media/gpu/vaapi/vaapi_image_processor_backend.cc
@@ -7,6 +7,7 @@
 #include <stdint.h>
 #include <va/va.h>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/ptr_util.h"
diff --git a/media/gpu/vaapi/vaapi_video_decoder.cc b/media/gpu/vaapi/vaapi_video_decoder.cc
index 4070e20ee3..bea3e73611 100644
--- a/media/gpu/vaapi/vaapi_video_decoder.cc
+++ b/media/gpu/vaapi/vaapi_video_decoder.cc
@@ -15,6 +15,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -71,7 +72,7 @@ std::optional<VideoPixelFormat> GetPixelFormatForBitDepth(uint8_t bit_depth) {
           {8u, viz::MultiPlaneFormat::kNV12},
           {10u, viz::MultiPlaneFormat::kP010},
       });
-  if (!kSupportedBitDepthAndVizFormats.contains(bit_depth)) {
+  if (!base::Contains(kSupportedBitDepthAndVizFormats, bit_depth)) {
     VLOGF(1) << "Unsupported bit depth: " << base::strict_cast<int>(bit_depth);
     return std::nullopt;
   }
diff --git a/media/gpu/vaapi/vaapi_video_decoder_delegate.cc b/media/gpu/vaapi/vaapi_video_decoder_delegate.cc
index 54b7338618..0cd24d3c3b 100644
--- a/media/gpu/vaapi/vaapi_video_decoder_delegate.cc
+++ b/media/gpu/vaapi/vaapi_video_decoder_delegate.cc
@@ -298,7 +298,7 @@ void VaapiVideoDecoderDelegate::OnGetHwKeyData(
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   // There's a special case here where we are updating usage times/checking on
   // key validity, and in that case the key is already in the map.
-  if (hw_key_data_map_.contains(key_id)) {
+  if (base::Contains(hw_key_data_map_, key_id)) {
     if (status == Decryptor::Status::kSuccess)
       return;
     // This key is no longer valid, decryption will fail, so stop playback
diff --git a/media/gpu/vaapi/vaapi_wrapper.cc b/media/gpu/vaapi/vaapi_wrapper.cc
index 4c220c3385..f661ca1dc1 100644
--- a/media/gpu/vaapi/vaapi_wrapper.cc
+++ b/media/gpu/vaapi/vaapi_wrapper.cc
@@ -26,6 +26,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/cpu.h"
 #include "base/environment.h"
@@ -810,10 +811,10 @@ bool IsVAProfileSupported(VAProfile va_profile, bool is_encoding) {
         VAProfileVP9Profile0,
         VAProfileAV1Profile0,
     };
-    return std::ranges::contains(kSupportableEncoderProfiles, va_profile);
+    return base::Contains(kSupportableEncoderProfiles, va_profile);
   }
-  return std::ranges::contains(GetProfileCodecMap(), va_profile,
-                               &ProfileCodecMap::value_type::second);
+  return base::Contains(GetProfileCodecMap(), va_profile,
+                        &ProfileCodecMap::value_type::second);
 }
 
 bool IsBlockedDriver(VaapiWrapper::CodecMode mode,
@@ -912,8 +913,8 @@ std::vector<VAEntrypoint> GetEntryPointsForProfile(const base::Lock* va_lock,
   std::vector<VAEntrypoint> entrypoints;
   std::ranges::copy_if(va_entrypoints, std::back_inserter(entrypoints),
                        [&kAllowedEntryPoints, mode](VAEntrypoint entry_point) {
-                         return std::ranges::contains(kAllowedEntryPoints[mode],
-                                                      entry_point);
+                         return base::Contains(kAllowedEntryPoints[mode],
+                                               entry_point);
                        });
   return entrypoints;
 }
@@ -972,7 +973,7 @@ bool GetRequiredAttribs(const base::Lock* va_lock,
   constexpr VAProfile kSupportedH264VaProfilesForEncoding[] = {
       VAProfileH264ConstrainedBaseline, VAProfileH264Main, VAProfileH264High};
   // VAConfigAttribEncPackedHeaders is H.264 specific.
-  if (std::ranges::contains(kSupportedH264VaProfilesForEncoding, profile)) {
+  if (base::Contains(kSupportedH264VaProfilesForEncoding, profile)) {
     // Encode with Packed header if the driver supports.
     VAConfigAttrib attrib{};
     attrib.type = VAConfigAttribEncPackedHeaders;
@@ -1402,8 +1403,8 @@ const VASupportedImageFormats& VASupportedImageFormats::Get() {
 
 bool VASupportedImageFormats::IsImageFormatSupported(
     const VAImageFormat& va_image_format) const {
-  return std::ranges::contains(supported_formats_, va_image_format.fourcc,
-                               &VAImageFormat::fourcc);
+  return base::Contains(supported_formats_, va_image_format.fourcc,
+                        &VAImageFormat::fourcc);
 }
 
 const std::vector<VAImageFormat>&
@@ -1482,7 +1483,7 @@ bool IsLowPowerEncSupported(VAProfile va_profile) {
       VAProfileVP9Profile0,
       VAProfileAV1Profile0,
   };
-  if (!std::ranges::contains(kSupportedLowPowerEncodeProfiles, va_profile))
+  if (!base::Contains(kSupportedLowPowerEncodeProfiles, va_profile))
     return false;
 
   if ((IsGen95Gpu() || IsGen9Gpu()) &&
@@ -2042,7 +2043,7 @@ bool VaapiWrapper::IsVppFormatSupported(uint32_t va_fourcc) {
   if (!profile_info)
     return false;
 
-  return std::ranges::contains(profile_info->pixel_formats, va_fourcc);
+  return base::Contains(profile_info->pixel_formats, va_fourcc);
 }
 
 // static
@@ -3459,7 +3460,7 @@ VaapiWrapper::CreateScopedVASurfaces(
   VA_SUCCESS_OR_RETURN(va_res, VaapiFunctions::kVACreateSurfaces_Allocating,
                        std::vector<std::unique_ptr<ScopedVASurface>>{});
 
-  DCHECK(!std::ranges::contains(va_surface_ids, VA_INVALID_ID))
+  DCHECK(!base::Contains(va_surface_ids, VA_INVALID_ID))
       << "Invalid VA surface id after vaCreateSurfaces";
 
   DCHECK(!visible_size.has_value() || !visible_size->IsEmpty());
diff --git a/media/gpu/vaapi/vp8_vaapi_video_encoder_delegate.cc b/media/gpu/vaapi/vp8_vaapi_video_encoder_delegate.cc
index 5b881bb5a6..851ef275b1 100644
--- a/media/gpu/vaapi/vp8_vaapi_video_encoder_delegate.cc
+++ b/media/gpu/vaapi/vp8_vaapi_video_encoder_delegate.cc
@@ -13,6 +13,7 @@
 
 #include "base/bits.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/memory/ref_counted_memory.h"
 #include "base/strings/string_number_conversions.h"
 #include "build/build_config.h"
@@ -388,7 +389,7 @@ VP8VaapiVideoEncoderDelegate::PrepareEncodeJob(EncodeJob& encode_job) {
   }
 
   DCHECK(!picture->frame_hdr->IsKeyframe() ||
-         !std::ranges::contains(ref_frames_used, true));
+         !base::Contains(ref_frames_used, true));
 
   if (!SubmitFrameParameters(encode_job, current_params_, picture,
                              reference_frames_, ref_frames_used)) {
diff --git a/media/gpu/windows/media_foundation_video_encode_accelerator_win.cc b/media/gpu/windows/media_foundation_video_encode_accelerator_win.cc
index 096d18020c..04901347e2 100644
--- a/media/gpu/windows/media_foundation_video_encode_accelerator_win.cc
+++ b/media/gpu/windows/media_foundation_video_encode_accelerator_win.cc
@@ -1489,7 +1489,7 @@ void MediaFoundationVideoEncodeAccelerator::SetSWRateControl() {
 #endif  // BUILDFLAG(ENABLE_PLATFORM_HEVC)
       VideoCodec::kAV1,
   };
-  if (!std::ranges::contains(kCodecsHaveSWBRC, codec_)) {
+  if (!base::Contains(kCodecsHaveSWBRC, codec_)) {
     return;
   }
 
diff --git a/media/gpu/windows/mf_audio_encoder.cc b/media/gpu/windows/mf_audio_encoder.cc
index a853cc5a13..77cb2f49e7 100644
--- a/media/gpu/windows/mf_audio_encoder.cc
+++ b/media/gpu/windows/mf_audio_encoder.cc
@@ -14,6 +14,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/heap_array.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
@@ -76,7 +77,7 @@ EncoderStatus::Codes ValidateInputOptions(const AudioEncoder::Options& options,
   if (options.codec != AudioCodec::kAAC)
     return EncoderStatus::Codes::kEncoderUnsupportedCodec;
 
-  if (!std::ranges::contains(kSupportedSampleRates, options.sample_rate)) {
+  if (!base::Contains(kSupportedSampleRates, options.sample_rate)) {
     return EncoderStatus::Codes::kEncoderUnsupportedConfig;
   }
 
@@ -95,7 +96,7 @@ EncoderStatus::Codes ValidateInputOptions(const AudioEncoder::Options& options,
   }
 
   *bitrate = options.bitrate.value_or(kDefaultBitrate);
-  if (!std::ranges::contains(kSupportedBitrates, *bitrate)) {
+  if (!base::Contains(kSupportedBitrates, *bitrate)) {
     return EncoderStatus::Codes::kEncoderUnsupportedConfig;
   }
 
diff --git a/media/midi/midi_manager_win.cc b/media/midi/midi_manager_win.cc
index 01580c8b5f..33fa639a91 100644
--- a/media/midi/midi_manager_win.cc
+++ b/media/midi/midi_manager_win.cc
@@ -27,6 +27,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -875,9 +876,9 @@ void MidiManagerWin::ReflectActiveDeviceList(
 
   // Find new ports from active ports and append them to known ports.
   for (auto& port : *active_ports) {
-    if (!std::ranges::contains(
-            *known_ports, *port,
-            [](const auto& candidate) -> T& { return *candidate; })) {
+    if (!base::Contains(*known_ports, *port, [](const auto& candidate) -> T& {
+          return *candidate;
+        })) {
       size_t index = known_ports->size();
       port->set_index(index);
       known_ports->push_back(std::move(port));
diff --git a/media/mojo/services/gpu_mojo_media_client_linux.cc b/media/mojo/services/gpu_mojo_media_client_linux.cc
index 0347aeedc9..5b937089a3 100644
--- a/media/mojo/services/gpu_mojo_media_client_linux.cc
+++ b/media/mojo/services/gpu_mojo_media_client_linux.cc
@@ -4,8 +4,6 @@
 
 #include "media/mojo/services/gpu_mojo_media_client.h"
 
-#include <algorithm>
-
 #include "base/metrics/histogram_functions.h"
 #include "base/task/sequenced_task_runner.h"
 #include "components/viz/common/resources/shared_image_format.h"
@@ -65,7 +63,7 @@ std::vector<Fourcc> GetPreferredRenderableFourccs(
       // GLX can only import native pixmap of format AR24. Ozone expose this
       // capability through gpu_feature_info so we can selectively allow hw
       // accelerated formats.
-      if (std::ranges::contains(
+      if (base::Contains(
               gpu_feature_info.supported_formats_for_gl_native_pixmap_import,
               viz::MultiPlaneFormat::kNV12)) {
         if (base::FeatureList::IsEnabled(kRenderableMM21)) {
@@ -73,7 +71,7 @@ std::vector<Fourcc> GetPreferredRenderableFourccs(
         }
         renderable_fourccs.emplace_back(Fourcc::NV12);
       }
-      if (std::ranges::contains(
+      if (base::Contains(
               gpu_feature_info.supported_formats_for_gl_native_pixmap_import,
               viz::MultiPlaneFormat::kP010)) {
         renderable_fourccs.emplace_back(Fourcc::P010);
diff --git a/media/mojo/services/media_metrics_provider.cc b/media/mojo/services/media_metrics_provider.cc
index 10c0165427..388cbab80a 100644
--- a/media/mojo/services/media_metrics_provider.cc
+++ b/media/mojo/services/media_metrics_provider.cc
@@ -11,6 +11,7 @@
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "build/chromecast_buildflags.h"
 #include "media/base/key_systems.h"
@@ -114,7 +115,7 @@ MediaMetricsProvider::~MediaMetricsProvider() {
   if (!media_info_->is_mse) {
     builder.SetURLScheme(static_cast<int64_t>(media_info_->url_scheme));
     if (container_name_)
-      builder.SetContainerName(std::to_underlying(*container_name_));
+      builder.SetContainerName(base::to_underlying(*container_name_));
   }
 
   if (time_to_metadata_ != kNoTimestamp)
diff --git a/media/mojo/services/watch_time_recorder.cc b/media/mojo/services/watch_time_recorder.cc
index d51b30732d..9ab56bb0b7 100644
--- a/media/mojo/services/watch_time_recorder.cc
+++ b/media/mojo/services/watch_time_recorder.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <cmath>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/hash/hash.h"
 #include "base/metrics/histogram_functions.h"
@@ -116,7 +117,7 @@ void WatchTimeRecorder::FinalizeWatchTime(
   // needed by for UKM and MTBR recording below.
   for (auto& kv : watch_time_info_) {
     if (!should_finalize_everything &&
-        !std::ranges::contains(keys_to_finalize, kv.first)) {
+        !base::Contains(keys_to_finalize, kv.first)) {
       continue;
     }
 
diff --git a/media/remoting/renderer_controller.cc b/media/remoting/renderer_controller.cc
index 2c91adef2b..5b22b5a063 100644
--- a/media/remoting/renderer_controller.cc
+++ b/media/remoting/renderer_controller.cc
@@ -4,8 +4,7 @@
 
 #include "media/remoting/renderer_controller.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/logging.h"
 #include "base/time/default_tick_clock.h"
@@ -595,19 +594,18 @@ void RendererController::SetClient(MediaObserverClient* client) {
 bool RendererController::HasVideoCapability(
     mojom::RemotingSinkVideoCapability capability) const {
   return sink_metadata_ &&
-         std::ranges::contains(sink_metadata_->video_capabilities, capability);
+         base::Contains(sink_metadata_->video_capabilities, capability);
 }
 
 bool RendererController::HasAudioCapability(
     mojom::RemotingSinkAudioCapability capability) const {
   return sink_metadata_ &&
-         std::ranges::contains(sink_metadata_->audio_capabilities, capability);
+         base::Contains(sink_metadata_->audio_capabilities, capability);
 }
 
 bool RendererController::HasFeatureCapability(
     RemotingSinkFeature capability) const {
-  return sink_metadata_ &&
-         std::ranges::contains(sink_metadata_->features, capability);
+  return sink_metadata_ && base::Contains(sink_metadata_->features, capability);
 }
 
 bool RendererController::SinkSupportsRemoting() const {
diff --git a/media/video/software_video_encoder_test.cc b/media/video/software_video_encoder_test.cc
index be8f2e902f..d0fdc537a9 100644
--- a/media/video/software_video_encoder_test.cc
+++ b/media/video/software_video_encoder_test.cc
@@ -2,7 +2,6 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
-#include <algorithm>
 #include <array>
 #include <memory>
 #include <string>
@@ -887,7 +886,7 @@ TEST_P(SVCVideoEncoderTest, EncodeClipTemporalSvcWithEnablingDrop) {
     size_t encoded_frame_index = 0;
     size_t decoded_frame_index = 0;
     for (size_t i = 0; i < frames_to_encode.size(); ++i) {
-      if (std::ranges::contains(dropped_frame_indices, i)) {
+      if (base::Contains(dropped_frame_indices, i)) {
         // Dropped
         continue;
       }
diff --git a/net/base/base64.cc b/net/base/base64.cc
index cb853d8899..6fbdc5a14c 100644
--- a/net/base/base64.cc
+++ b/net/base/base64.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "net/base/features.h"
 #include "third_party/simdutf/simdutf.h"
@@ -25,7 +26,7 @@ bool SimdutfBase64Decode(std::string_view input,
       return false;
     }
     if (std::ranges::any_of(input, [](char c) {
-          return std::ranges::contains(base::kInfraAsciiWhitespace, c);
+          return base::Contains(base::kInfraAsciiWhitespace, c);
         })) {
       return false;
     }
diff --git a/net/base/filename_util_internal.cc b/net/base/filename_util_internal.cc
index d579960480..49e4ee880c 100644
--- a/net/base/filename_util_internal.cc
+++ b/net/base/filename_util_internal.cc
@@ -4,8 +4,7 @@
 
 #include "net/base/filename_util_internal.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/files/file_path.h"
 #include "base/strings/escape.h"
 #include "base/strings/string_util.h"
@@ -55,7 +54,7 @@ base::FilePath::StringType GetCorrectedExtensionUnsafe(
   // "foo.jpg" to "foo.jpeg".
   std::vector<base::FilePath::StringType> all_mime_extensions;
   GetExtensionsForMimeType(mime_type, &all_mime_extensions);
-  if (std::ranges::contains(all_mime_extensions, extension))
+  if (base::Contains(all_mime_extensions, extension))
     return extension;
 
   // Get the "final" extension. In most cases, this is the same as the
@@ -69,7 +68,7 @@ base::FilePath::StringType GetCorrectedExtensionUnsafe(
   // If there's a double extension, and the second extension is in the
   // list of valid extensions for the given type, keep the double extension.
   // This avoids renaming things like "foo.tar.gz" to "foo.gz".
-  if (std::ranges::contains(all_mime_extensions, final_extension))
+  if (base::Contains(all_mime_extensions, final_extension))
     return extension;
   return preferred_mime_extension;
 }
diff --git a/net/base/reconnect_notifier.cc b/net/base/reconnect_notifier.cc
index e0695956e7..8c9357a3ff 100644
--- a/net/base/reconnect_notifier.cc
+++ b/net/base/reconnect_notifier.cc
@@ -5,6 +5,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 
 namespace net {
diff --git a/net/base/scheme_host_port_matcher.cc b/net/base/scheme_host_port_matcher.cc
index 15bc1421c2..1862c5d5c1 100644
--- a/net/base/scheme_host_port_matcher.cc
+++ b/net/base/scheme_host_port_matcher.cc
@@ -5,6 +5,7 @@
 #include "net/base/scheme_host_port_matcher.h"
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/strings/string_tokenizer.h"
 #include "base/strings/string_util.h"
 #include "base/trace_event/memory_usage_estimator.h"
@@ -91,7 +92,7 @@ SchemeHostPortMatcherResult SchemeHostPortMatcher::Evaluate(
 std::string SchemeHostPortMatcher::ToString() const {
   std::string result;
   for (const auto& rule : rules_) {
-    DCHECK(!rule->ToString().contains(kParseRuleListDelimiterList));
+    DCHECK(!base::Contains(rule->ToString(), kParseRuleListDelimiterList));
     result += rule->ToString();
     result.push_back(kPrintRuleListDelimiter);
   }
diff --git a/net/cert/ev_root_ca_metadata.cc b/net/cert/ev_root_ca_metadata.cc
index 0ab208f9c1..621781316b 100644
--- a/net/cert/ev_root_ca_metadata.cc
+++ b/net/cert/ev_root_ca_metadata.cc
@@ -6,6 +6,7 @@
 
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_view_util.h"
diff --git a/net/cert/internal/trust_store_mac.cc b/net/cert/internal/trust_store_mac.cc
index d3206fd845..ee3bb9ab51 100644
--- a/net/cert/internal/trust_store_mac.cc
+++ b/net/cert/internal/trust_store_mac.cc
@@ -6,7 +6,6 @@
 
 #include <Security/Security.h>
 
-#include <algorithm>
 #include <atomic>
 #include <map>
 #include <string_view>
@@ -16,6 +15,7 @@
 #include "base/apple/osstatus_logging.h"
 #include "base/apple/scoped_cftyperef.h"
 #include "base/callback_list.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/to_vector.h"
 #include "base/functional/bind.h"
@@ -309,10 +309,10 @@ bool IsNotAcceptableIntermediate(const bssl::ParsedCertificate* cert,
   // actually care about.
   if (cert->has_extended_key_usage() &&
       CFEqual(policy_oid, kSecPolicyAppleSSL) &&
-      !std::ranges::contains(cert->extended_key_usage(),
-                             bssl::der::Input(bssl::kAnyEKU)) &&
-      !std::ranges::contains(cert->extended_key_usage(),
-                             bssl::der::Input(bssl::kServerAuth))) {
+      !base::Contains(cert->extended_key_usage(),
+                      bssl::der::Input(bssl::kAnyEKU)) &&
+      !base::Contains(cert->extended_key_usage(),
+                      bssl::der::Input(bssl::kServerAuth))) {
     return true;
   }
 
diff --git a/net/cert/qwac.cc b/net/cert/qwac.cc
index 3fd7919b85..1891047579 100644
--- a/net/cert/qwac.cc
+++ b/net/cert/qwac.cc
@@ -4,8 +4,7 @@
 
 #include "net/cert/qwac.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "third_party/boringssl/src/pki/parser.h"
 
@@ -194,8 +193,8 @@ QwacEkuStatus Has2QwacEku(const bssl::ParsedCertificate* cert) {
   if (!cert->has_extended_key_usage()) {
     return QwacEkuStatus::kNotQwac;
   }
-  if (!std::ranges::contains(cert->extended_key_usage(),
-                             bssl::der::Input(kIdKpTlsBinding))) {
+  if (!base::Contains(cert->extended_key_usage(),
+                      bssl::der::Input(kIdKpTlsBinding))) {
     return QwacEkuStatus::kNotQwac;
   }
   if (cert->extended_key_usage().size() != 1) {
diff --git a/net/cert/two_qwac.cc b/net/cert/two_qwac.cc
index 8fa1a84a81..bc530733f9 100644
--- a/net/cert/two_qwac.cc
+++ b/net/cert/two_qwac.cc
@@ -4,10 +4,9 @@
 
 #include "net/cert/two_qwac.h"
 
-#include <algorithm>
-
 #include "base/base64.h"
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/json/json_reader.h"
 #include "base/strings/string_split.h"
 #include "base/types/expected.h"
@@ -553,7 +552,7 @@ bool TwoQwacCertBinding::BindsTlsCert(base::span<const uint8_t> tls_cert_der) {
       crypto::hash::DigestSizeForHashKind(header_.hash_alg));
   crypto::hash::Hash(header_.hash_alg, tls_cert_b64, tls_cert_hash);
 
-  return std::ranges::contains(header_.bound_cert_hashes, tls_cert_hash);
+  return base::Contains(header_.bound_cert_hashes, tls_cert_hash);
 }
 
 }  // namespace net
diff --git a/net/cert/x509_certificate.cc b/net/cert/x509_certificate.cc
index dc8cc30b16..9d12c3579c 100644
--- a/net/cert/x509_certificate.cc
+++ b/net/cert/x509_certificate.cc
@@ -7,12 +7,12 @@
 #include <limits.h>
 #include <stdlib.h>
 
-#include <algorithm>
 #include <memory>
 #include <string>
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/containers/to_vector.h"
 #include "base/logging.h"
@@ -428,7 +428,7 @@ bool X509Certificate::IsIssuedByEncoded(
     if (!GetNormalizedCertIssuer(cert.get(), &normalized_cert_issuer)) {
       return false;
     }
-    if (std::ranges::contains(normalized_issuers, normalized_cert_issuer))
+    if (base::Contains(normalized_issuers, normalized_cert_issuer))
       return true;
   }
   return false;
@@ -463,8 +463,8 @@ bool X509Certificate::VerifyHostname(
 
   // Fully handle all cases where |hostname| contains an IP address.
   if (host_info.IsIPAddress()) {
-    return std::ranges::contains(cert_san_ip_addrs,
-                                 base::as_string_view(host_info.AddressSpan()));
+    return base::Contains(cert_san_ip_addrs,
+                          base::as_string_view(host_info.AddressSpan()));
   }
 
   // The host portion of a URL may support a variety of name resolution formats
diff --git a/net/cookies/canonical_cookie.cc b/net/cookies/canonical_cookie.cc
index acd8a08369..d71f09a2b0 100644
--- a/net/cookies/canonical_cookie.cc
+++ b/net/cookies/canonical_cookie.cc
@@ -52,6 +52,7 @@
 
 #include "base/check_is_test.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/feature_list.h"
 #include "base/format_macros.h"
@@ -399,7 +400,7 @@ std::unique_ptr<CanonicalCookie> CanonicalCookie::Create(
   UMA_HISTOGRAM_BOOLEAN("Cookie.Parse.EmptyName", parsed_cookie.Name().empty());
   if (parsed_cookie.Name().empty()) {
     UMA_HISTOGRAM_BOOLEAN("Cookie.Parse.EmptyNameAmbiguousValue",
-                          parsed_cookie.Value().contains('='));
+                          base::Contains(parsed_cookie.Value(), '='));
   }
 
   std::optional<std::string> cookie_domain =
diff --git a/net/cookies/cookie_base.cc b/net/cookies/cookie_base.cc
index ce84f7f0a1..c83ebc335f 100644
--- a/net/cookies/cookie_base.cc
+++ b/net/cookies/cookie_base.cc
@@ -4,8 +4,7 @@
 
 #include "net/cookies/cookie_base.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/strings/strcat.h"
@@ -422,7 +421,7 @@ CookieAccessResult CookieBase::IsSetPermittedInContext(
     access_result = *cookie_access_result;
   }
 
-  if (!std::ranges::contains(cookieable_schemes, source_url.scheme())) {
+  if (!base::Contains(cookieable_schemes, source_url.scheme())) {
     access_result.status.AddExclusionReason(
         CookieInclusionStatus::ExclusionReason::EXCLUDE_NONCOOKIEABLE_SCHEME);
   }
diff --git a/net/cookies/cookie_partition_key_collection.cc b/net/cookies/cookie_partition_key_collection.cc
index 681ed61bed..e705cf5742 100644
--- a/net/cookies/cookie_partition_key_collection.cc
+++ b/net/cookies/cookie_partition_key_collection.cc
@@ -7,6 +7,7 @@
 #include <optional>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/functional/bind.h"
diff --git a/net/cookies/cookie_util.cc b/net/cookies/cookie_util.cc
index 89835dddf5..494f5c344c 100644
--- a/net/cookies/cookie_util.cc
+++ b/net/cookies/cookie_util.cc
@@ -14,6 +14,7 @@
 #include "base/check.h"
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -450,7 +451,7 @@ std::optional<std::string> GetCookieDomainWithString(
   }
 
   // Disallow domain names with %-escaped characters.
-  if (domain_string.contains('%')) {
+  if (base::Contains(domain_string, '%')) {
     return std::nullopt;
   }
 
diff --git a/net/cookies/parsed_cookie.cc b/net/cookies/parsed_cookie.cc
index 8e36b41dbb..57fe1858d0 100644
--- a/net/cookies/parsed_cookie.cc
+++ b/net/cookies/parsed_cookie.cc
@@ -49,6 +49,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/numerics/checked_math.h"
@@ -163,11 +164,11 @@ GetNamelessParseTypeForInitialPair(std::string_view cookie_name,
     return std::nullopt;
   }
   if (had_token_value_separator) {
-    return cookie_value.contains('=')
+    return base::Contains(cookie_value, '=')
                ? net::NamelessCookieLineParseType::kNamelessWithAmbiguousValue
                : net::NamelessCookieLineParseType::kEqualsPrecedingToken;
   }
-  if (kCookieAttributeNames.contains(base::ToLowerASCII(cookie_value))) {
+  if (base::Contains(kCookieAttributeNames, base::ToLowerASCII(cookie_value))) {
     return net::NamelessCookieLineParseType::kBareTokenMatchingAttributeName;
   }
   return net::NamelessCookieLineParseType::kBareToken;
diff --git a/net/cookies/test_cookie_access_delegate.cc b/net/cookies/test_cookie_access_delegate.cc
index 7ef41a5cee..932c62db4e 100644
--- a/net/cookies/test_cookie_access_delegate.cc
+++ b/net/cookies/test_cookie_access_delegate.cc
@@ -11,6 +11,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/functional/callback.h"
diff --git a/net/device_bound_sessions/cookie_craving.cc b/net/device_bound_sessions/cookie_craving.cc
index 32c35b1a37..2db6917faa 100644
--- a/net/device_bound_sessions/cookie_craving.cc
+++ b/net/device_bound_sessions/cookie_craving.cc
@@ -7,6 +7,7 @@
 #include <optional>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
@@ -122,7 +123,7 @@ base::expected<CookieCraving, SessionError> CookieCraving::Create(
           {"domain", "path", "secure", "httponly", "samesite"});
   if (!parsed_cookie.ForEachAttribute(
           [](std::string_view attribute, std::string_view value) {
-            return kPermittedAttributes.contains(attribute);
+            return base::Contains(kPermittedAttributes, attribute);
           })) {
     return base::unexpected(SessionError{
         SessionError::kInvalidCredentialsCookieUnpermittedAttribute});
diff --git a/net/device_bound_sessions/registration_fetcher.cc b/net/device_bound_sessions/registration_fetcher.cc
index 970c3dd955..6f45041083 100644
--- a/net/device_bound_sessions/registration_fetcher.cc
+++ b/net/device_bound_sessions/registration_fetcher.cc
@@ -4,11 +4,11 @@
 
 #include "net/device_bound_sessions/registration_fetcher.h"
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
 #include "base/metrics/histogram_functions.h"
@@ -374,7 +374,7 @@ class RegistrationFetcherImpl : public RegistrationFetcher {
     std::string target_origin =
         url::Origin::Create(fetcher_endpoint_).Serialize();
     if (!maybe_params->relying_origins.has_value() ||
-        !std::ranges::contains(*maybe_params->relying_origins, target_origin)) {
+        !base::Contains(*maybe_params->relying_origins, target_origin)) {
       return SessionError::kFederatedNotAuthorizedByProvider;
     }
 
@@ -738,9 +738,8 @@ class RegistrationFetcherImpl : public RegistrationFetcher {
     }
 
     if (!maybe_params->registering_origins.has_value() ||
-        !std::ranges::contains(
-            *maybe_params->registering_origins,
-            url::Origin::Create(fetcher_endpoint_).Serialize())) {
+        !base::Contains(*maybe_params->registering_origins,
+                        url::Origin::Create(fetcher_endpoint_).Serialize())) {
       return RegistrationResult(
           SessionError{SessionError::kSubdomainRegistrationUnauthorized});
     }
diff --git a/net/device_bound_sessions/registration_fetcher_param.cc b/net/device_bound_sessions/registration_fetcher_param.cc
index 6edacfc2f5..02cf739281 100644
--- a/net/device_bound_sessions/registration_fetcher_param.cc
+++ b/net/device_bound_sessions/registration_fetcher_param.cc
@@ -8,6 +8,7 @@
 #include <vector>
 
 #include "base/base64url.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/strings/escape.h"
 #include "base/strings/string_split.h"
@@ -189,7 +190,7 @@ std::vector<RegistrationFetcherParam> RegistrationFetcherParam::CreateIfValid(
   }
 
   SchemefulSite site(request_url);
-  if (std::ranges::contains(restricted_sites, site) &&
+  if (base::Contains(restricted_sites, site) &&
       !base::FeatureList::IsEnabled(
           features::kDeviceBoundSessionsForRestrictedSites)) {
     return params;
@@ -206,7 +207,7 @@ std::vector<RegistrationFetcherParam> RegistrationFetcherParam::CreateIfValid(
       std::optional<RegistrationFetcherParam> fetcher_param =
           ParseItem(request_url, item);
       if (fetcher_param) {
-        if (std::ranges::contains(restricted_sites, site) &&
+        if (base::Contains(restricted_sites, site) &&
             !net::features::
                  kDeviceBoundSessionsForRestrictedSitesExperimentIdParam.Get()
                      .empty()) {
diff --git a/net/device_bound_sessions/session_service_impl.cc b/net/device_bound_sessions/session_service_impl.cc
index 37e36200bd..c9c6f99920 100644
--- a/net/device_bound_sessions/session_service_impl.cc
+++ b/net/device_bound_sessions/session_service_impl.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/barrier_callback.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
diff --git a/net/dns/dns_response.cc b/net/dns/dns_response.cc
index d2366f6e09..8337d29349 100644
--- a/net/dns/dns_response.cc
+++ b/net/dns/dns_response.cc
@@ -232,7 +232,9 @@ unsigned DnsRecordParser::ReadName(const void* const vpos,
         if (out) {
           if (!out->empty())
             out->append(".");
-          out->append_range(packet_.subspan(offset, label_len));
+          // TODO(danakj): Use append_range() in C++23.
+          auto range = packet_.subspan(offset, label_len);
+          out->append(range.begin(), range.end());
           CHECK_LE(out->size(), dns_protocol::kMaxCharNameLength);
         }
         offset += label_len;
diff --git a/net/dns/dns_response_result_extractor.cc b/net/dns/dns_response_result_extractor.cc
index c7c62bd79a..e37415b021 100644
--- a/net/dns/dns_response_result_extractor.cc
+++ b/net/dns/dns_response_result_extractor.cc
@@ -19,6 +19,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/dcheck_is_on.h"
 #include "base/metrics/histogram_macros.h"
@@ -559,8 +560,8 @@ ResultsOrError ExtractHttpsResults(const DnsResponse& response,
 
     metadata.supported_protocol_alpns = service->alpn_ids();
     if (service->default_alpn() &&
-        !std::ranges::contains(metadata.supported_protocol_alpns,
-                               dns_protocol::kHttpsServiceDefaultAlpn)) {
+        !base::Contains(metadata.supported_protocol_alpns,
+                        dns_protocol::kHttpsServiceDefaultAlpn)) {
       metadata.supported_protocol_alpns.push_back(
           dns_protocol::kHttpsServiceDefaultAlpn);
     }
diff --git a/net/dns/dns_util.cc b/net/dns/dns_util.cc
index 47ea143646..eff286ec20 100644
--- a/net/dns/dns_util.cc
+++ b/net/dns/dns_util.cc
@@ -16,6 +16,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial.h"
 #include "base/metrics/histogram_macros.h"
@@ -58,7 +59,7 @@ DohProviderEntry::List GetDohProviderEntriesFromNameservers(
       // experiment if the provider feature flag is checked).
       if (entry->ip_addresses.contains(server.address()) &&
           base::FeatureList::IsEnabled(entry->feature.get()) &&
-          !std::ranges::contains(entries, entry)) {
+          !base::Contains(entries, entry)) {
         entries.push_back(entry);
       }
     }
diff --git a/net/dns/host_cache.cc b/net/dns/host_cache.cc
index db5a8351aa..1d109c408b 100644
--- a/net/dns/host_cache.cc
+++ b/net/dns/host_cache.cc
@@ -20,6 +20,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/field_trial.h"
 #include "base/metrics/histogram_functions.h"
@@ -412,7 +413,7 @@ std::vector<HostResolverEndpointResult> HostCache::Entry::GetEndpoints() const {
     // Currently Chrome uses HTTPS records only when A and AAAA records are at
     // the same canonical name and that matches the HTTPS target name.
     for (ConnectionEndpointMetadata& metadata : metadatas) {
-      if (!canonical_names_.contains(metadata.target_name)) {
+      if (!base::Contains(canonical_names_, metadata.target_name)) {
         continue;
       }
       endpoints.emplace_back();
@@ -525,7 +526,7 @@ std::vector<ServiceEndpoint> HostCache::Entry::ConvertToServiceEndpoints(
   // See HostCache::Entry::GetEndpoints.
   if (!ipv4_endpoints.empty() || !ipv6_endpoints.empty()) {
     for (const auto& metadata : GetMetadatas()) {
-      if (!canonical_names().contains(metadata.target_name)) {
+      if (!base::Contains(canonical_names(), metadata.target_name)) {
         continue;
       }
 
diff --git a/net/dns/host_resolver_dns_task.cc b/net/dns/host_resolver_dns_task.cc
index 7438522c42..6492c8d78d 100644
--- a/net/dns/host_resolver_dns_task.cc
+++ b/net/dns/host_resolver_dns_task.cc
@@ -498,7 +498,7 @@ void HostResolverDnsTask::OnDnsTransactionComplete(
     int net_error,
     const DnsResponse* response) {
   CHECK(transaction_info_it != transactions_in_progress_.end());
-  DCHECK(transactions_in_progress_.contains(*transaction_info_it));
+  DCHECK(base::Contains(transactions_in_progress_, *transaction_info_it));
 
   // Pull the TransactionInfo out of `transactions_in_progress_` now, so it
   // and its underlying DnsTransaction will be deleted on completion of
diff --git a/net/dns/host_resolver_manager.cc b/net/dns/host_resolver_manager.cc
index e93ef22ad9..387046073b 100644
--- a/net/dns/host_resolver_manager.cc
+++ b/net/dns/host_resolver_manager.cc
@@ -24,6 +24,7 @@
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
 #include "base/containers/circular_deque.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/linked_list.h"
 #include "base/debug/debugger.h"
@@ -799,8 +800,7 @@ void HostResolverManager::InitializeJobKeyAndIPAddress(
       out_job_key.host.HasScheme()) {
     static constexpr std::string_view kSchemesForHttpsQuery[] = {
         url::kHttpScheme, url::kHttpsScheme, url::kWsScheme, url::kWssScheme};
-    if (std::ranges::contains(kSchemesForHttpsQuery,
-                              out_job_key.host.GetScheme())) {
+    if (base::Contains(kSchemesForHttpsQuery, out_job_key.host.GetScheme())) {
       effective_types.Put(DnsQueryType::HTTPS);
     }
   }
diff --git a/net/dns/host_resolver_manager_job.cc b/net/dns/host_resolver_manager_job.cc
index 3d7eda059c..e31480a08f 100644
--- a/net/dns/host_resolver_manager_job.cc
+++ b/net/dns/host_resolver_manager_job.cc
@@ -4,7 +4,6 @@
 
 #include "net/dns/host_resolver_manager_job.h"
 
-#include <algorithm>
 #include <deque>
 #include <memory>
 #include <optional>
@@ -320,7 +319,7 @@ base::OnceClosure HostResolverManager::Job::GetAbortInsecureDnsTaskClosure(
 
 void HostResolverManager::Job::AbortInsecureDnsTask(int error,
                                                     bool fallback_only) {
-  bool has_system_fallback = std::ranges::contains(tasks_, TaskType::SYSTEM);
+  bool has_system_fallback = base::Contains(tasks_, TaskType::SYSTEM);
   if (has_system_fallback) {
     for (auto it = tasks_.begin(); it != tasks_.end();) {
       if (*it == TaskType::DNS) {
diff --git a/net/dns/host_resolver_nat64_task.cc b/net/dns/host_resolver_nat64_task.cc
index 5cae0c4ae8..ee4b0306bc 100644
--- a/net/dns/host_resolver_nat64_task.cc
+++ b/net/dns/host_resolver_nat64_task.cc
@@ -155,7 +155,7 @@ int HostResolverNat64Task::DoSynthesizeToIpv6() {
           ipv4_address, ipv4onlyarpa_AAAA_address, pref64_length);
 
       IPEndPoint converted_ip_endpoint(converted_address, 0);
-      if (!std::ranges::contains(converted_addresses, converted_ip_endpoint)) {
+      if (!base::Contains(converted_addresses, converted_ip_endpoint)) {
         converted_addresses.push_back(std::move(converted_ip_endpoint));
       }
     }
diff --git a/net/dns/https_record_rdata.cc b/net/dns/https_record_rdata.cc
index 4dffce9d5f..d406f9ee5c 100644
--- a/net/dns/https_record_rdata.cc
+++ b/net/dns/https_record_rdata.cc
@@ -15,6 +15,7 @@
 
 #include "base/big_endian.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/to_vector.h"
@@ -294,8 +295,8 @@ ServiceFormHttpsRecordRdata::ServiceFormHttpsRecordRdata(
       ipv6_hint_(std::move(ipv6_hint)),
       trust_anchor_ids_(std::move(trust_anchor_ids)) {
   DCHECK_NE(priority_, 0);
-  DCHECK(
-      !mandatory_keys_.contains(dns_protocol::kHttpsServiceParamKeyMandatory));
+  DCHECK(!base::Contains(mandatory_keys_,
+                         dns_protocol::kHttpsServiceParamKeyMandatory));
 
 #if DCHECK_IS_ON()
   for (const IPAddress& address : ipv4_hint_) {
@@ -490,7 +491,7 @@ std::unique_ptr<ServiceFormHttpsRecordRdata> ServiceFormHttpsRecordRdata::Parse(
 bool ServiceFormHttpsRecordRdata::IsCompatible() const {
   for (uint16_t mandatory_key : mandatory_keys_) {
     DCHECK_NE(mandatory_key, dns_protocol::kHttpsServiceParamKeyMandatory);
-    if (!kSupportedKeys.contains(mandatory_key)) {
+    if (!base::Contains(kSupportedKeys, mandatory_key)) {
       return false;
     }
   }
@@ -500,7 +501,7 @@ bool ServiceFormHttpsRecordRdata::IsCompatible() const {
 // static
 bool ServiceFormHttpsRecordRdata::IsSupportedKey(uint16_t key) {
 #if DCHECK_IS_ON()
-  return kSupportedKeys.contains(key);
+  return base::Contains(kSupportedKeys, key);
 #else
   // Only intended for DCHECKs.
   base::ImmediateCrash();
diff --git a/net/dns/httpssvc_metrics.cc b/net/dns/httpssvc_metrics.cc
index b19df57b8e..75d85be95d 100644
--- a/net/dns/httpssvc_metrics.cc
+++ b/net/dns/httpssvc_metrics.cc
@@ -4,9 +4,9 @@
 
 #include "net/dns/httpssvc_metrics.h"
 
-#include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram.h"
 #include "base/metrics/histogram_base.h"
@@ -65,7 +65,7 @@ void HttpssvcMetrics::SaveForHttps(enum HttpssvcDnsRcode rcode,
   // We only record one "parsable" sample per HTTPS query. In case multiple
   // matching records are present in the response, we combine their parsable
   // values with logical AND.
-  const bool parsable = !std::ranges::contains(condensed_records, false);
+  const bool parsable = !base::Contains(condensed_records, false);
 
   DCHECK(!is_https_parsable_.has_value());
   is_https_parsable_ = parsable;
diff --git a/net/dns/mdns_cache.cc b/net/dns/mdns_cache.cc
index 4f73e6667f..328cf54b17 100644
--- a/net/dns/mdns_cache.cc
+++ b/net/dns/mdns_cache.cc
@@ -10,6 +10,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "net/dns/public/dns_protocol.h"
@@ -77,7 +78,7 @@ MDnsCache::UpdateType MDnsCache::UpdateDnsRecord(
   Key cache_key = Key::CreateFor(record.get());
 
   // Ignore "goodbye" packets for records not in cache.
-  if (record->ttl() == 0 && !mdns_cache_.contains(cache_key)) {
+  if (record->ttl() == 0 && !base::Contains(mdns_cache_, cache_key)) {
     return NoChange;
   }
 
diff --git a/net/dns/opt_record_rdata.cc b/net/dns/opt_record_rdata.cc
index 27cc167f56..824d8d7ae1 100644
--- a/net/dns/opt_record_rdata.cc
+++ b/net/dns/opt_record_rdata.cc
@@ -15,6 +15,7 @@
 
 #include "base/big_endian.h"
 #include "base/check_is_test.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/containers/span_reader.h"
 #include "base/containers/span_writer.h"
@@ -252,7 +253,7 @@ OptRecordRdata::UnknownOpt::CreateForTesting(uint16_t code,
 OptRecordRdata::UnknownOpt::UnknownOpt(uint16_t code,
                                        base::span<const uint8_t> data)
     : Opt(data), code_(code) {
-  CHECK(!std::ranges::contains(kOptsWithDedicatedClasses, code));
+  CHECK(!base::Contains(kOptsWithDedicatedClasses, code));
 }
 
 uint16_t OptRecordRdata::UnknownOpt::GetCode() const {
diff --git a/net/dns/public/dns_over_https_server_config.cc b/net/dns/public/dns_over_https_server_config.cc
index 0926982341..d77d717b67 100644
--- a/net/dns/public/dns_over_https_server_config.cc
+++ b/net/dns/public/dns_over_https_server_config.cc
@@ -10,6 +10,7 @@
 #include <string_view>
 #include <unordered_map>
 
+#include "base/containers/contains.h"
 #include "base/json/json_reader.h"
 #include "base/json/json_writer.h"
 #include "base/values.h"
@@ -66,7 +67,7 @@ bool IsValidDohTemplate(const std::string& server_template, bool* use_post) {
     return false;
   }
   // If the template contains a dns variable, use GET, otherwise use POST.
-  *use_post = !vars_found.contains("dns");
+  *use_post = !base::Contains(vars_found, "dns");
   return true;
 }
 
diff --git a/net/dns/resolve_context.cc b/net/dns/resolve_context.cc
index ac5ffdd63d..85aca1a145 100644
--- a/net/dns/resolve_context.cc
+++ b/net/dns/resolve_context.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/metrics/bucket_ranges.h"
 #include "base/metrics/histogram.h"
 #include "base/metrics/histogram_base.h"
@@ -628,9 +629,9 @@ bool ResolveContext::GetProviderUseExtraLogging(size_t server_index,
 
   // Use extra logging if any matching provider entries have
   // `LoggingLevel::kExtra` set.
-  return std::ranges::contains(matching_entries,
-                               DohProviderEntry::LoggingLevel::kExtra,
-                               &DohProviderEntry::logging_level);
+  return base::Contains(matching_entries,
+                        DohProviderEntry::LoggingLevel::kExtra,
+                        &DohProviderEntry::logging_level);
 }
 
 void ResolveContext::NotifyDohStatusObserversOfSessionChanged() {
diff --git a/net/first_party_sets/global_first_party_sets.cc b/net/first_party_sets/global_first_party_sets.cc
index a071ba293c..d7869e0803 100644
--- a/net/first_party_sets/global_first_party_sets.cc
+++ b/net/first_party_sets/global_first_party_sets.cc
@@ -12,6 +12,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/map_util.h"
@@ -344,10 +345,10 @@ FirstPartySetsContextConfig GlobalFirstPartySets::ComputeConfig(
   // which is not already contained in the overlay, we explicitly ignore that
   // alias.
   ForEachAlias([&](const SchemefulSite& alias, const SchemefulSite& canonical) {
-    if (std::ranges::contains(
+    if (base::Contains(
             site_to_override, canonical,
             &std::pair<SchemefulSite, FirstPartySetEntryOverride>::first) &&
-        !std::ranges::contains(
+        !base::Contains(
             site_to_override, alias,
             &std::pair<SchemefulSite, FirstPartySetEntryOverride>::first)) {
       site_to_override.emplace_back(alias, FirstPartySetEntryOverride());
diff --git a/net/first_party_sets/local_set_declaration.cc b/net/first_party_sets/local_set_declaration.cc
index 43f47776dd..9f1f1f663b 100644
--- a/net/first_party_sets/local_set_declaration.cc
+++ b/net/first_party_sets/local_set_declaration.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/containers/map_util.h"
 #include "base/logging.h"
 #include "net/base/schemeful_site.h"
@@ -29,14 +30,14 @@ bool CheckPreconditions(
   if (!std::ranges::all_of(aliases, [&](const auto& p) {
         const auto& [alias_site, canonical_site] = p;
         // The canonical entry must exist.
-        if (!entries.contains(canonical_site)) {
+        if (!base::Contains(entries, canonical_site)) {
           emit(
               "Invalid local Related Website Set: alias names a site that has "
               "no entry in the set.");
           return false;
         }
         // The alias entry must not exist explicitly.
-        if (entries.contains(alias_site)) {
+        if (base::Contains(entries, alias_site)) {
           emit(
               "Invalid local Related Website Set: alias site should not be "
               "listed in `entries`.");
diff --git a/net/http/http_auth_handler_factory.cc b/net/http/http_auth_handler_factory.cc
index b28ec5c2d9..dc1e8a75c2 100644
--- a/net/http/http_auth_handler_factory.cc
+++ b/net/http/http_auth_handler_factory.cc
@@ -4,12 +4,12 @@
 
 #include "net/http/http_auth_handler_factory.h"
 
-#include <algorithm>
 #include <array>
 #include <optional>
 #include <set>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 #include "base/strings/string_util.h"
 #include "build/build_config.h"
@@ -262,7 +262,7 @@ bool HttpAuthHandlerRegistryFactory::IsSchemeAllowed(
   if (http_auth_preferences() && http_auth_preferences()->allowed_schemes()) {
     return http_auth_preferences()->allowed_schemes()->contains(scheme);
   }
-  return std::ranges::contains(kDefaultAuthSchemes, scheme);
+  return base::Contains(kDefaultAuthSchemes, scheme);
 }
 
 #if BUILDFLAG(USE_KERBEROS) && !BUILDFLAG(IS_ANDROID) && BUILDFLAG(IS_POSIX)
diff --git a/net/http/http_network_session.cc b/net/http/http_network_session.cc
index 5e87a9d484..020d0c5ff1 100644
--- a/net/http/http_network_session.cc
+++ b/net/http/http_network_session.cc
@@ -11,6 +11,7 @@
 #include "base/atomic_sequence_num.h"
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/notreached.h"
 #include "base/power_monitor/power_monitor.h"
@@ -85,7 +86,7 @@ spdy::SettingsMap AddDefaultHttp2Settings(spdy::SettingsMap http2_settings) {
 bool OriginToForceQuicOnInternal(const QuicParams& quic_params,
                                  const url::SchemeHostPort& destination) {
   return (quic_params.force_quic_everywhere ||
-          quic_params.origins_to_force_quic_on.contains(destination));
+          base::Contains(quic_params.origins_to_force_quic_on, destination));
 }
 
 }  // unnamed namespace
@@ -248,7 +249,7 @@ HttpNetworkSession::~HttpNetworkSession() {
 
 void HttpNetworkSession::StartResponseDrainer(
     std::unique_ptr<HttpResponseBodyDrainer> drainer) {
-  DCHECK(!response_drainers_.contains(drainer.get()));
+  DCHECK(!base::Contains(response_drainers_, drainer.get()));
   HttpResponseBodyDrainer* drainer_ptr = drainer.get();
   response_drainers_.insert(std::move(drainer));
   drainer_ptr->Start(this);
@@ -256,7 +257,7 @@ void HttpNetworkSession::StartResponseDrainer(
 
 void HttpNetworkSession::RemoveResponseDrainer(
     HttpResponseBodyDrainer* drainer) {
-  DCHECK(response_drainers_.contains(drainer));
+  DCHECK(base::Contains(response_drainers_, drainer));
 
   response_drainers_.erase(response_drainers_.find(drainer));
 }
diff --git a/net/http/http_no_vary_search_data.cc b/net/http/http_no_vary_search_data.cc
index 0613d4e046..c5b6d4d8a8 100644
--- a/net/http/http_no_vary_search_data.cc
+++ b/net/http/http_no_vary_search_data.cc
@@ -4,10 +4,10 @@
 
 #include "net/http/http_no_vary_search_data.h"
 
-#include <algorithm>
 #include <optional>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
@@ -202,10 +202,9 @@ HttpNoVarySearchData::ParseNoVarySearchDictionary(
   bool vary_by_default = true;
 
   // If the dictionary contains unknown keys, maybe fail parsing.
-  const bool has_unrecognized_keys =
-      !std::ranges::all_of(dict, [&](const auto& pair) {
-        return std::ranges::contains(kValidKeys, pair.first);
-      });
+  const bool has_unrecognized_keys = !std::ranges::all_of(
+      dict,
+      [&](const auto& pair) { return base::Contains(kValidKeys, pair.first); });
 
   UMA_HISTOGRAM_BOOLEAN("Net.HttpNoVarySearch.HasUnrecognizedKeys",
                         has_unrecognized_keys);
diff --git a/net/http/http_server_properties.cc b/net/http/http_server_properties.cc
index 113e88bc7e..7dc7f582b7 100644
--- a/net/http/http_server_properties.cc
+++ b/net/http/http_server_properties.cc
@@ -6,6 +6,7 @@
 
 #include "base/check_op.h"
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
@@ -1338,7 +1339,7 @@ void HttpServerProperties::OnServerInfoLoaded(
                             kCanonicalPort),
         it.first.network_anonymization_key);
     // If we already have a valid canonical server, we're done.
-    if (canonical_alt_svc_map_.contains(key)) {
+    if (base::Contains(canonical_alt_svc_map_, key)) {
       auto key_it = server_info_map_.Peek(key);
       if (key_it != server_info_map_.end() &&
           key_it->second.alternative_services.has_value()) {
diff --git a/net/http/http_stream_factory_job.cc b/net/http/http_stream_factory_job.cc
index de34bb8e82..eb77a83cac 100644
--- a/net/http/http_stream_factory_job.cc
+++ b/net/http/http_stream_factory_job.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
diff --git a/net/http/http_stream_factory_job_controller.cc b/net/http/http_stream_factory_job_controller.cc
index 6872137c41..bf0a027a89 100644
--- a/net/http/http_stream_factory_job_controller.cc
+++ b/net/http/http_stream_factory_job_controller.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -1532,7 +1533,7 @@ bool HttpStreamFactory::JobController::IsQuicAllowedForHost(
   }
 
   std::string lowered_host = base::ToLowerASCII(host);
-  return host_allowlist.contains(lowered_host);
+  return base::Contains(host_allowlist, lowered_host);
 }
 
 void HttpStreamFactory::JobController::SwitchToHttpStreamPool() {
diff --git a/net/http/http_stream_pool_attempt.cc b/net/http/http_stream_pool_attempt.cc
index 6b38a7c613..64040b7633 100644
--- a/net/http/http_stream_pool_attempt.cc
+++ b/net/http/http_stream_pool_attempt.cc
@@ -4,7 +4,6 @@
 
 #include "net/http/http_stream_pool_attempt.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <ranges>
@@ -12,6 +11,7 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/containers/span.h"
 #include "base/memory/raw_ref.h"
@@ -294,7 +294,7 @@ bool HttpStreamPool::Attempt::IsEndpointUsable(const ServiceEndpoint& endpoint,
         if (!kTcpBasedProtocols.Has(next_proto)) {
           return false;
         }
-        return std::ranges::contains(delegate_->GetAlpnProtos(), next_proto);
+        return base::Contains(delegate_->GetAlpnProtos(), next_proto);
       });
 }
 
@@ -364,7 +364,7 @@ HttpStreamPool::Attempt::GetServiceEndpointForTlsHandshake(
         ip_endpoint.address().IsIPv4() ? service_endpoint.ipv4_endpoints
                                        : service_endpoint.ipv6_endpoints;
 
-    if (!std::ranges::contains(ip_endpoints, ip_endpoint)) {
+    if (!base::Contains(ip_endpoints, ip_endpoint)) {
       continue;
     }
     return service_endpoint;
diff --git a/net/http/http_stream_pool_attempt_manager.cc b/net/http/http_stream_pool_attempt_manager.cc
index 2d15ff4fb0..a568552d27 100644
--- a/net/http/http_stream_pool_attempt_manager.cc
+++ b/net/http/http_stream_pool_attempt_manager.cc
@@ -10,6 +10,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/debug/alias.h"
 #include "base/debug/crash_logging.h"
@@ -408,8 +409,8 @@ bool HttpStreamPool::AttemptManager::IsEndpointUsableForTcpBasedAttempt(
   // configured ones.
   return std::ranges::any_of(
       endpoint.metadata.supported_protocol_alpns, [&](const auto& alpn) {
-        return std::ranges::contains(http_network_session()->GetAlpnProtos(),
-                                     NextProtoFromString(alpn));
+        return base::Contains(http_network_session()->GetAlpnProtos(),
+                              NextProtoFromString(alpn));
       });
 }
 
@@ -487,7 +488,7 @@ HttpStreamPool::AttemptManager::GetServiceEndpoint(
     const std::vector<IPEndPoint>& ip_endpoints = ip_endpoint.address().IsIPv4()
                                                       ? endpoint.ipv4_endpoints
                                                       : endpoint.ipv6_endpoints;
-    if (!std::ranges::contains(ip_endpoints, ip_endpoint)) {
+    if (!base::Contains(ip_endpoints, ip_endpoint)) {
       continue;
     }
     return endpoint;
diff --git a/net/http/no_vary_search_cache_storage.cc b/net/http/no_vary_search_cache_storage.cc
index 3fa5114b2f..11aeb13b34 100644
--- a/net/http/no_vary_search_cache_storage.cc
+++ b/net/http/no_vary_search_cache_storage.cc
@@ -24,6 +24,7 @@
 #include "base/task/task_traits.h"
 #include "base/task/thread_pool.h"
 #include "base/time/time.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "net/base/pickle.h"
 #include "net/base/pickle_base_types.h"
 #include "net/base/pickle_traits.h"
@@ -82,7 +83,7 @@ enum class JournalEntryType : uint32_t {
 template <>
 struct PickleTraits<JournalEntryType> {
   static void Serialize(base::Pickle& pickle, const JournalEntryType& value) {
-    WriteToPickle(pickle, std::to_underlying(value));
+    WriteToPickle(pickle, base::to_underlying(value));
   }
 
   static std::optional<JournalEntryType> Deserialize(
@@ -101,7 +102,7 @@ struct PickleTraits<JournalEntryType> {
   }
 
   static size_t PickleSize(const JournalEntryType& value) {
-    return EstimatePickleSize(std::to_underlying(value));
+    return EstimatePickleSize(base::to_underlying(value));
   }
 };
 
diff --git a/net/http/transport_security_state.cc b/net/http/transport_security_state.cc
index 6366d90fc4..24813afc1f 100644
--- a/net/http/transport_security_state.cc
+++ b/net/http/transport_security_state.cc
@@ -16,6 +16,7 @@
 #include "base/base64.h"
 #include "base/build_time.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -373,7 +374,7 @@ void TransportSecurityState::UpdatePinList(
     pinset_names_map[pinset.name()] = &pinset;
   }
   for (const auto& pin : host_pins) {
-    if (!pinset_names_map.contains(pin.pinset_name_)) {
+    if (!base::Contains(pinset_names_map, pin.pinset_name_)) {
       // This should never happen, but if the component is bad and missing an
       // entry, we will ignore that particular pin.
       continue;
diff --git a/net/log/net_log.cc b/net/log/net_log.cc
index e15bce8267..3067edb1ff 100644
--- a/net/log/net_log.cc
+++ b/net/log/net_log.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/notreached.h"
 #include "base/strings/string_number_conversions.h"
@@ -179,12 +180,12 @@ void NetLog::UpdateObserverCaptureModes() {
 
 bool NetLog::HasObserver(ThreadSafeObserver* observer) {
   lock_.AssertAcquired();
-  return std::ranges::contains(observers_, observer);
+  return base::Contains(observers_, observer);
 }
 
 bool NetLog::HasCaptureModeObserver(ThreadSafeCaptureModeObserver* observer) {
   lock_.AssertAcquired();
-  return std::ranges::contains(capture_mode_observers_, observer);
+  return base::Contains(capture_mode_observers_, observer);
 }
 
 // static
diff --git a/net/network_error_logging/network_error_logging_test_util.cc b/net/network_error_logging/network_error_logging_test_util.cc
index 3393ee304c..56d20f6f0b 100644
--- a/net/network_error_logging/network_error_logging_test_util.cc
+++ b/net/network_error_logging/network_error_logging_test_util.cc
@@ -4,8 +4,7 @@
 
 #include "net/network_error_logging/network_error_logging_test_util.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "net/base/ip_address.h"
 
 namespace net {
@@ -44,8 +43,8 @@ void TestNetworkErrorLoggingService::RemoveAllBrowsingData() {}
 
 bool TestNetworkErrorLoggingService::Header::MatchesAddressList(
     const AddressList& address_list) const {
-  return std::ranges::contains(address_list, received_ip_address,
-                               &IPEndPoint::address);
+  return base::Contains(address_list, received_ip_address,
+                        &IPEndPoint::address);
 }
 
 }  // namespace net
diff --git a/net/nqe/network_quality.cc b/net/nqe/network_quality.cc
index cdf25c01be..4b9dbcce2b 100644
--- a/net/nqe/network_quality.cc
+++ b/net/nqe/network_quality.cc
@@ -6,10 +6,12 @@
 
 #include <utility>
 
+#include "base/types/cxx23_to_underlying.h"
+
 namespace net::nqe::internal {
 
 base::TimeDelta InvalidRTT() {
-  return base::Milliseconds(std::to_underlying(INVALID_RTT_THROUGHPUT));
+  return base::Milliseconds(base::to_underlying(INVALID_RTT_THROUGHPUT));
 }
 
 NetworkQuality::NetworkQuality()
diff --git a/net/quic/crypto/proof_verifier_chromium.cc b/net/quic/crypto/proof_verifier_chromium.cc
index 3e6d245b8f..40f5cef42e 100644
--- a/net/quic/crypto/proof_verifier_chromium.cc
+++ b/net/quic/crypto/proof_verifier_chromium.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -382,10 +383,11 @@ int ProofVerifierChromium::Job::DoVerifyCert(int result) {
 
 bool ProofVerifierChromium::Job::ShouldAllowUnknownRootForHost(
     const std::string& hostname) {
-  if (proof_verifier_->hostnames_to_allow_unknown_roots_.contains("")) {
+  if (base::Contains(proof_verifier_->hostnames_to_allow_unknown_roots_, "")) {
     return true;
   }
-  return proof_verifier_->hostnames_to_allow_unknown_roots_.contains(hostname);
+  return base::Contains(proof_verifier_->hostnames_to_allow_unknown_roots_,
+                        hostname);
 }
 
 int ProofVerifierChromium::Job::DoVerifyCertComplete(int result) {
diff --git a/net/quic/dedicated_web_transport_http3_client.cc b/net/quic/dedicated_web_transport_http3_client.cc
index 8821ff4638..eaf6d03f9f 100644
--- a/net/quic/dedicated_web_transport_http3_client.cc
+++ b/net/quic/dedicated_web_transport_http3_client.cc
@@ -4,10 +4,10 @@
 
 #include "net/quic/dedicated_web_transport_http3_client.h"
 
-#include <algorithm>
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/field_trial_params.h"
@@ -533,7 +533,7 @@ int DedicatedWebTransportHttp3Client::DoInit() {
   // Add other supported versions if available.
   for (quic::ParsedQuicVersion& version :
        quic_context_->params()->supported_versions) {
-    if (std::ranges::contains(supported_versions_, version))
+    if (base::Contains(supported_versions_, version))
       continue;  // Skip as we've already added it above.
     supported_versions_.push_back(version);
   }
@@ -992,7 +992,7 @@ void DedicatedWebTransportHttp3Client::OnConnectionClosed(
     original_supported_versions_ = supported_versions_;
     std::erase_if(
         supported_versions_, [this](const quic::ParsedQuicVersion& version) {
-          return !std::ranges::contains(
+          return !base::Contains(
               session_->connection()->server_supported_versions(), version);
         });
     if (!supported_versions_.empty()) {
diff --git a/net/quic/quic_chromium_client_session.cc b/net/quic/quic_chromium_client_session.cc
index 5246fc59bb..e182c7ee8b 100644
--- a/net/quic/quic_chromium_client_session.cc
+++ b/net/quic/quic_chromium_client_session.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -1297,12 +1298,12 @@ void QuicChromiumClientSession::AddHandle(Handle* handle) {
     return;
   }
 
-  DCHECK(!handles_.contains(handle));
+  DCHECK(!base::Contains(handles_, handle));
   handles_.insert(handle);
 }
 
 void QuicChromiumClientSession::RemoveHandle(Handle* handle) {
-  DCHECK(handles_.contains(handle));
+  DCHECK(base::Contains(handles_, handle));
   handles_.erase(handle);
 }
 
diff --git a/net/quic/quic_context.cc b/net/quic/quic_context.cc
index 69f2699f99..487e253e6e 100644
--- a/net/quic/quic_context.cc
+++ b/net/quic/quic_context.cc
@@ -4,6 +4,7 @@
 
 #include "net/quic/quic_context.h"
 
+#include "base/containers/contains.h"
 #include "net/base/features.h"
 #include "net/quic/platform/impl/quic_chromium_clock.h"
 #include "net/quic/quic_chromium_connection_helper.h"
diff --git a/net/quic/quic_context.h b/net/quic/quic_context.h
index fb68e214d6..89eb654c52 100644
--- a/net/quic/quic_context.h
+++ b/net/quic/quic_context.h
@@ -5,9 +5,9 @@
 #ifndef NET_QUIC_QUIC_CONTEXT_H_
 #define NET_QUIC_QUIC_CONTEXT_H_
 
-#include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/time/time.h"
 #include "net/base/features.h"
@@ -57,7 +57,7 @@ AllSupportedQuicVersions() {
       quic::AllSupportedVersions();
   quic::ParsedQuicVersionVector filtered_versions;
   for (const auto& version : all_supported_versions) {
-    if (!std::ranges::contains(obsolete_versions, version)) {
+    if (!base::Contains(obsolete_versions, version)) {
       filtered_versions.push_back(version);
     }
   }
diff --git a/net/quic/quic_session_pool.cc b/net/quic/quic_session_pool.cc
index cf8c1bef70..d01e72a654 100644
--- a/net/quic/quic_session_pool.cc
+++ b/net/quic/quic_session_pool.cc
@@ -13,6 +13,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -1553,8 +1554,8 @@ quic::ParsedQuicVersion QuicSessionPool::SelectQuicVersion(
   // https://datatracker.ietf.org/doc/html/rfc9460#name-interaction-with-alt-svc
   if (known_quic_version.IsKnown()) {
     std::string expected_alpn = quic::AlpnForVersion(known_quic_version);
-    if (std::ranges::contains(metadata.supported_protocol_alpns,
-                              quic::AlpnForVersion(known_quic_version))) {
+    if (base::Contains(metadata.supported_protocol_alpns,
+                       quic::AlpnForVersion(known_quic_version))) {
       return known_quic_version;
     }
     return quic::ParsedQuicVersion::Unsupported();
diff --git a/net/quic/quic_session_pool_peer.cc b/net/quic/quic_session_pool_peer.cc
index 6db8d903ac..bcfa69c614 100644
--- a/net/quic/quic_session_pool_peer.cc
+++ b/net/quic/quic_session_pool_peer.cc
@@ -7,6 +7,7 @@
 #include <string>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/task/sequenced_task_runner.h"
 #include "net/base/network_anonymization_key.h"
 #include "net/base/privacy_mode.h"
@@ -108,7 +109,7 @@ QuicChromiumClientSession* QuicSessionPoolPeer::GetActiveSession(
 
 bool QuicSessionPoolPeer::IsLiveSession(QuicSessionPool* pool,
                                         QuicChromiumClientSession* session) {
-  return pool->all_sessions_.contains(session);
+  return base::Contains(pool->all_sessions_, session);
 }
 
 void QuicSessionPoolPeer::SetTaskRunner(
diff --git a/net/reporting/reporting_cache_impl.cc b/net/reporting/reporting_cache_impl.cc
index 7069c8f1ea..fccf6311cf 100644
--- a/net/reporting/reporting_cache_impl.cc
+++ b/net/reporting/reporting_cache_impl.cc
@@ -10,6 +10,7 @@
 #include <unordered_set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/stl_util.h"
 #include "base/time/clock.h"
@@ -1184,7 +1185,7 @@ void ReportingCacheImpl::ConsistencyCheckEndpoint(
        ++index_it) {
     endpoint_its_for_url.push_back(index_it->second);
   }
-  DCHECK(std::ranges::contains(endpoint_its_for_url, endpoint_it));
+  DCHECK(base::Contains(endpoint_its_for_url, endpoint_it));
 #endif  // DCHECK_IS_ON()
 }
 
diff --git a/net/reporting/reporting_delivery_agent.cc b/net/reporting/reporting_delivery_agent.cc
index fcc3cc6255..e9effd72e5 100644
--- a/net/reporting/reporting_delivery_agent.cc
+++ b/net/reporting/reporting_delivery_agent.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/json/json_writer.h"
@@ -368,14 +369,14 @@ class ReportingDeliveryAgentImpl : public ReportingDeliveryAgent,
       if (!report_group_key.origin.has_value()) {
         DCHECK_EQ(ReportingTargetType::kEnterprise,
                   report_group_key.target_type);
-      } else if (!allowed_report_origins.contains(
-                     report_group_key.origin.value())) {
+      } else if (!base::Contains(allowed_report_origins,
+                                 report_group_key.origin.value())) {
         continue;
       }
 
       // Skip this group if there is already a pending upload for it.
       // We don't allow multiple concurrent uploads for the same group.
-      if (pending_groups_.contains(report_group_key))
+      if (base::Contains(pending_groups_, report_group_key))
         continue;
 
       // Find an endpoint to deliver these reports to.
diff --git a/net/socket/ssl_client_socket_impl.cc b/net/socket/ssl_client_socket_impl.cc
index 8ac4477210..4aeac3d114 100644
--- a/net/socket/ssl_client_socket_impl.cc
+++ b/net/socket/ssl_client_socket_impl.cc
@@ -1544,7 +1544,7 @@ int SSLClientSocketImpl::ClientCertRequestCallback(SSL* ssl) {
     // If the key supports rsa_pkcs1_sha256, automatically add support for
     // rsa_pkcs1_sha256_legacy, for use with TLS 1.3. We convert here so that
     // not every `SSLPrivateKey` needs to implement it explicitly.
-    if (std::ranges::contains(preferences, SSL_SIGN_RSA_PKCS1_SHA256)) {
+    if (base::Contains(preferences, SSL_SIGN_RSA_PKCS1_SHA256)) {
       preferences.push_back(SSL_SIGN_RSA_PKCS1_SHA256_LEGACY);
     }
 
diff --git a/net/socket/transport_client_socket_pool.cc b/net/socket/transport_client_socket_pool.cc
index 8a97a50abd..beed937d1b 100644
--- a/net/socket/transport_client_socket_pool.cc
+++ b/net/socket/transport_client_socket_pool.cc
@@ -12,6 +12,7 @@
 #include "base/barrier_closure.h"
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/format_macros.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
@@ -1674,10 +1675,9 @@ void TransportClientSocketPool::Group::SanityCheck() const {
       ConnectJob* job = pointer.value()->job();
       DCHECK(job);
       // The request's job is not in |unassigned_jobs_|
-      DCHECK(!std::ranges::contains(unassigned_jobs_, job));
+      DCHECK(!base::Contains(unassigned_jobs_, job));
       // The request's job is in |jobs_|
-      DCHECK(
-          std::ranges::contains(jobs_, job, &std::unique_ptr<ConnectJob>::get));
+      DCHECK(base::Contains(jobs_, job, &std::unique_ptr<ConnectJob>::get));
       // The same job is not assigned to any other request with a job.
       RequestQueue::Pointer pointer2 =
           unbound_requests_.GetNextTowardsLastMin(pointer);
@@ -1698,18 +1698,17 @@ void TransportClientSocketPool::Group::SanityCheck() const {
   for (auto it = unassigned_jobs_.begin(); it != unassigned_jobs_.end(); ++it) {
     // Check that all unassigned jobs are in |jobs_|
     ConnectJob* job = *it;
-    DCHECK(
-        std::ranges::contains(jobs_, job, &std::unique_ptr<ConnectJob>::get));
+    DCHECK(base::Contains(jobs_, job, &std::unique_ptr<ConnectJob>::get));
     // Check that there are no duplicated entries in |unassigned_jobs_|
     for (auto it2 = std::next(it); it2 != unassigned_jobs_.end(); ++it2) {
       DCHECK_NE(job, *it2);
     }
 
     // Check that no |unassigned_jobs_| are in |bound_requests_|.
-    DCHECK(!std::ranges::contains(bound_requests_, job,
-                                  [](const BoundRequest& bound_request) {
-                                    return bound_request.connect_job.get();
-                                  }));
+    DCHECK(!base::Contains(bound_requests_, job,
+                           [](const BoundRequest& bound_request) {
+                             return bound_request.connect_job.get();
+                           }));
   }
 #endif
 }
@@ -1976,7 +1975,7 @@ TransportClientSocketPool::Group::FindUnboundRequestWithJob(
       return pointer;
   }
   // If a request with the job was not found, it must be in |unassigned_jobs_|.
-  DCHECK(std::ranges::contains(unassigned_jobs_, job));
+  DCHECK(base::Contains(unassigned_jobs_, job));
   return RequestQueue::Pointer();
 }
 
diff --git a/net/socket/websocket_transport_client_socket_pool.cc b/net/socket/websocket_transport_client_socket_pool.cc
index 187cd429ee..18272681c4 100644
--- a/net/socket/websocket_transport_client_socket_pool.cc
+++ b/net/socket/websocket_transport_client_socket_pool.cc
@@ -166,8 +166,8 @@ void WebSocketTransportClientSocketPool::CancelRequest(
                   handle->group_generation());
   if (DeleteJob(handle)) {
     UpdateStateAfterRelease();
-    CHECK(!pending_callbacks_.contains(
-        reinterpret_cast<ClientSocketHandleID>(handle)));
+    CHECK(!base::Contains(pending_callbacks_,
+                          reinterpret_cast<ClientSocketHandleID>(handle)));
   } else {
     pending_callbacks_.erase(reinterpret_cast<ClientSocketHandleID>(handle));
   }
diff --git a/net/spdy/spdy_session.cc b/net/spdy/spdy_session.cc
index 7465a28030..f5d605450f 100644
--- a/net/spdy/spdy_session.cc
+++ b/net/spdy/spdy_session.cc
@@ -14,6 +14,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/containers/span_writer.h"
 #include "base/functional/bind.h"
@@ -1713,9 +1714,8 @@ bool SpdySession::CancelStreamRequest(
   for (int i = MINIMUM_PRIORITY; i <= MAXIMUM_PRIORITY; ++i) {
     if (priority == i)
       continue;
-    DCHECK(!std::ranges::contains(pending_create_stream_queues_[i],
-                                  request.get(),
-                                  &base::WeakPtr<SpdyStreamRequest>::get));
+    DCHECK(!base::Contains(pending_create_stream_queues_[i], request.get(),
+                           &base::WeakPtr<SpdyStreamRequest>::get));
   }
 #endif
 
diff --git a/net/spdy/spdy_session_pool.cc b/net/spdy/spdy_session_pool.cc
index 187973644c..1089972e00 100644
--- a/net/spdy/spdy_session_pool.cc
+++ b/net/spdy/spdy_session_pool.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/notimplemented.h"
@@ -328,8 +329,7 @@ OnHostResolutionCallbackResult SpdySessionPool::OnHostResolutionComplete(
     // If `endpoint` has no associated ALPN protocols, it is TCP-based and thus
     // would have been eligible for connecting with HTTP/2.
     if (!endpoint.metadata.supported_protocol_alpns.empty() &&
-        !std::ranges::contains(endpoint.metadata.supported_protocol_alpns,
-                               "h2")) {
+        !base::Contains(endpoint.metadata.supported_protocol_alpns, "h2")) {
       continue;
     }
     for (const auto& address : endpoint.ip_endpoints) {
diff --git a/net/ssl/test_ssl_private_key.cc b/net/ssl/test_ssl_private_key.cc
index 646ba0e076..62c39ec2ad 100644
--- a/net/ssl/test_ssl_private_key.cc
+++ b/net/ssl/test_ssl_private_key.cc
@@ -4,10 +4,10 @@
 
 #include "net/ssl/test_ssl_private_key.h"
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/to_vector.h"
 #include "base/task/sequenced_task_runner.h"
 #include "net/base/net_errors.h"
@@ -59,7 +59,7 @@ class SSLPrivateKeyWithPreferences : public SSLPrivateKey {
   void Sign(uint16_t algorithm,
             base::span<const uint8_t> input,
             SignCallback callback) override {
-    if (!std::ranges::contains(prefs_, algorithm)) {
+    if (!base::Contains(prefs_, algorithm)) {
       base::SequencedTaskRunner::GetCurrentDefault()->PostTask(
           FROM_HERE, base::BindOnce(std::move(callback),
                                     ERR_SSL_CLIENT_AUTH_SIGNATURE_FAILED,
diff --git a/net/test/embedded_test_server/connection_tracker.cc b/net/test/embedded_test_server/connection_tracker.cc
index 14d411e65c..bd76029661 100644
--- a/net/test/embedded_test_server/connection_tracker.cc
+++ b/net/test/embedded_test_server/connection_tracker.cc
@@ -4,6 +4,7 @@
 
 #include "net/test/embedded_test_server/connection_tracker.h"
 
+#include "base/containers/contains.h"
 #include "base/run_loop.h"
 #include "base/task/single_thread_task_runner.h"
 #include "net/test/embedded_test_server/embedded_test_server.h"
@@ -46,7 +47,7 @@ void ConnectionTracker::AcceptedSocketWithPort(uint16_t port) {
 }
 
 void ConnectionTracker::ReadFromSocketWithPort(uint16_t port) {
-  EXPECT_TRUE(sockets_.contains(port));
+  EXPECT_TRUE(base::Contains(sockets_, port));
   if (sockets_[port] == SocketStatus::kAccepted)
     num_read_sockets_++;
   sockets_[port] = SocketStatus::kReadFrom;
diff --git a/net/tools/tld_cleanup/tld_cleanup_util.cc b/net/tools/tld_cleanup/tld_cleanup_util.cc
index 6a67ba7c5c..71aefe3c3a 100644
--- a/net/tools/tld_cleanup/tld_cleanup_util.cc
+++ b/net/tools/tld_cleanup/tld_cleanup_util.cc
@@ -10,6 +10,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/logging.h"
 #include "base/strings/strcat.h"
@@ -97,7 +98,7 @@ std::tuple<NormalizeResult, std::string, Rule> NormalizeRule(std::string domain,
   }
 
   // Warn about additional '*.' or '!'.
-  if (domain.contains("*.") || domain.contains('!')) {
+  if (base::Contains(domain, "*.") || base::Contains(domain, '!')) {
     LOG(WARNING) << "Keeping probably invalid rule: " << domain;
     result = NormalizeResult::kWarning;
   }
@@ -165,7 +166,8 @@ std::pair<NormalizeResult, RuleMap> NormalizeDataToRuleMap(
     // wildcard for the same rule, or that the same domain is listed as both
     // private and not private. If we did, we'd have to update our
     // parsing code to handle this case.
-    CHECK(!rules.contains(domain)) << "Duplicate rule found for " << domain;
+    CHECK(!base::Contains(rules, domain))
+        << "Duplicate rule found for " << domain;
 
     rules[domain] = rule;
     // Add true TLD for multi-level rules.  We don't add them right now, in
@@ -191,7 +193,7 @@ std::pair<NormalizeResult, RuleMap> NormalizeDataToRuleMap(
 
   std::ranges::copy_if(extra_rules, std::inserter(rules, rules.end()),
                        [&](const auto& extra_rule) {
-                         return !rules.contains(extra_rule.first);
+                         return !base::Contains(rules, extra_rule.first);
                        });
 
   return std::make_pair(result, rules);
diff --git a/net/tools/transport_security_state_generator/input_file_parsers.cc b/net/tools/transport_security_state_generator/input_file_parsers.cc
index a97704bdc1..afc293c71e 100644
--- a/net/tools/transport_security_state_generator/input_file_parsers.cc
+++ b/net/tools/transport_security_state_generator/input_file_parsers.cc
@@ -8,6 +8,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/json/json_reader.h"
 #include "base/logging.h"
@@ -375,7 +376,7 @@ bool ParseJSON(std::string_view hsts_json,
     }
 
     for (auto entry_value : *parsed) {
-      if (!valid_pins_keys.contains(entry_value.first)) {
+      if (!base::Contains(valid_pins_keys, entry_value.first)) {
         LOG(ERROR) << "The entry for " << *maybe_hostname
                    << " contains an unknown " << entry_value.first << " field";
         return false;
@@ -430,7 +431,7 @@ bool ParseJSON(std::string_view hsts_json,
     }
 
     for (auto entry_value : *parsed) {
-      if (!valid_hsts_keys.contains(entry_value.first)) {
+      if (!base::Contains(valid_hsts_keys, entry_value.first)) {
         LOG(ERROR) << "The entry for " << entry->hostname
                    << " contains an unknown " << entry_value.first << " field";
         return false;
diff --git a/net/url_request/url_request_job_factory.cc b/net/url_request/url_request_job_factory.cc
index 091a592299..d47fa04c47 100644
--- a/net/url_request/url_request_job_factory.cc
+++ b/net/url_request/url_request_job_factory.cc
@@ -4,6 +4,7 @@
 
 #include "net/url_request/url_request_job_factory.h"
 
+#include "base/containers/contains.h"
 #include "net/base/net_errors.h"
 #include "net/net_buildflags.h"
 #include "net/url_request/url_request.h"
@@ -85,7 +86,7 @@ bool URLRequestJobFactory::SetProtocolHandler(
     return true;
   }
 
-  if (protocol_handler_map_.contains(scheme))
+  if (base::Contains(protocol_handler_map_, scheme))
     return false;
   protocol_handler_map_[scheme] = std::move(protocol_handler);
   return true;
diff --git a/pdf/pdf_annotation_agent.h b/pdf/pdf_annotation_agent.h
index 30c041a7b8..28b5dc1bf0 100644
--- a/pdf/pdf_annotation_agent.h
+++ b/pdf/pdf_annotation_agent.h
@@ -6,11 +6,11 @@
 #define PDF_PDF_ANNOTATION_AGENT_H_
 
 #include <ostream>
-#include <utility>
 
 #include "base/containers/span.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "mojo/public/cpp/bindings/receiver.h"
 #include "mojo/public/cpp/bindings/remote.h"
 #include "third_party/blink/public/mojom/annotation/annotation.mojom.h"
@@ -75,7 +75,7 @@ class PdfAnnotationAgent : public blink::mojom::AnnotationAgent {
   };
 
   friend std::ostream& operator<<(std::ostream& o, State state) {
-    o << std::to_underlying(state);
+    o << base::to_underlying(state);
     return o;
   }
 
diff --git a/pdf/pdfium/pdfium_engine.cc b/pdf/pdfium/pdfium_engine.cc
index df49e4b9fd..bbdf7f75b3 100644
--- a/pdf/pdfium/pdfium_engine.cc
+++ b/pdf/pdfium/pdfium_engine.cc
@@ -18,6 +18,7 @@
 #include "base/auto_reset.h"
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/span.h"
 #include "base/dcheck_is_on.h"
@@ -3437,7 +3438,7 @@ void PDFiumEngine::CalculateVisiblePages() {
 bool PDFiumEngine::IsPageVisible(int page_index) const {
   // CalculateVisiblePages() must have been called first to populate
   // `visible_pages_`. Otherwise, this will always return false.
-  return std::ranges::contains(visible_pages_, page_index);
+  return base::Contains(visible_pages_, page_index);
 }
 
 void PDFiumEngine::ScrollToPage(int page) {
@@ -3462,7 +3463,7 @@ bool PDFiumEngine::CheckPageAvailable(uint32_t index,
 
   FX_DOWNLOADHINTS& download_hints = document_->download_hints();
   if (!FPDFAvail_IsPageAvail(fpdf_availability(), index, &download_hints)) {
-    if (!std::ranges::contains(*pending, index)) {
+    if (!base::Contains(*pending, index)) {
       pending->push_back(index);
     }
     return false;
diff --git a/pdf/pdfium/pdfium_on_demand_searchifier.cc b/pdf/pdfium/pdfium_on_demand_searchifier.cc
index f6abaedc6c..8639c4392c 100644
--- a/pdf/pdfium/pdfium_on_demand_searchifier.cc
+++ b/pdf/pdfium/pdfium_on_demand_searchifier.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/task/single_thread_task_runner.h"
 #include "pdf/pdfium/pdfium_searchify.h"
@@ -113,7 +114,7 @@ bool PDFiumOnDemandSearchifier::IsPageScheduled(uint32_t page_index) const {
     return true;
   }
 
-  return std::ranges::contains(pages_queue_, page_index);
+  return base::Contains(pages_queue_, page_index);
 }
 
 void PDFiumOnDemandSearchifier::SchedulePage(uint32_t page_index) {
diff --git a/printing/backend/cups_ipp_helper.cc b/printing/backend/cups_ipp_helper.cc
index cf252e4e3a..5680584f40 100644
--- a/printing/backend/cups_ipp_helper.cc
+++ b/printing/backend/cups_ipp_helper.cc
@@ -13,6 +13,7 @@
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/logging.h"
@@ -432,8 +433,8 @@ void ExtractMediaTypes(const CupsOptionProvider& printer,
 bool CollateCapable(const CupsOptionProvider& printer) {
   std::vector<std::string_view> values =
       printer.GetSupportedOptionValueStrings(kIppCollate);
-  return std::ranges::contains(values, kCollated) &&
-         std::ranges::contains(values, kUncollated);
+  return base::Contains(values, kCollated) &&
+         base::Contains(values, kUncollated);
 }
 
 bool CollateDefault(const CupsOptionProvider& printer) {
@@ -456,7 +457,7 @@ bool PinSupported(const CupsOptionProvider& printer) {
 
   std::vector<std::string_view> values =
       printer.GetSupportedOptionValueStrings(kIppPinEncryption);
-  return std::ranges::contains(values, kPinEncryptionNone);
+  return base::Contains(values, kPinEncryptionNone);
 }
 
 // Returns the number of IPP attributes added to `caps` (not necessarily in
diff --git a/printing/backend/cups_jobs.cc b/printing/backend/cups_jobs.cc
index 21319e9609..f499a3ff13 100644
--- a/printing/backend/cups_jobs.cc
+++ b/printing/backend/cups_jobs.cc
@@ -13,6 +13,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_map.h"
 #include "base/logging.h"
 #include "base/notreached.h"
@@ -372,7 +373,7 @@ bool ParsePrinterInfo(ipp_t* response, PrinterInfo* printer_info) {
       ParseCollection(attr, &features);
       printer_info->ipp_features = features;
       printer_info->ipp_everywhere =
-          std::ranges::contains(features, kIppEverywhere);
+          base::Contains(features, kIppEverywhere);
     } else if (name == kDocumentFormatSupported) {
       ParseCollection(attr, &printer_info->document_formats);
     } else if (name == kOauthAuthorizationServerUri) {
@@ -473,7 +474,7 @@ CupsJob::CupsJob(const CupsJob& other) = default;
 CupsJob::~CupsJob() = default;
 
 bool CupsJob::ContainsStateReason(CupsJob::JobStateReason reason) const {
-  return std::ranges::contains(state_reasons, ToJobStateReasonString(reason));
+  return base::Contains(state_reasons, ToJobStateReasonString(reason));
 }
 
 PrinterInfo::PrinterInfo() = default;
diff --git a/remoting/host/linux/gvariant_ref.h b/remoting/host/linux/gvariant_ref.h
index 196b9e6f2b..c110b80ec0 100644
--- a/remoting/host/linux/gvariant_ref.h
+++ b/remoting/host/linux/gvariant_ref.h
@@ -1087,7 +1087,7 @@ constexpr const char* ObjectPathCStr::c_str() const {
 
 constexpr bool operator==(const ObjectPathCStr& lhs,
                           const ObjectPathCStr& rhs) {
-  if consteval {
+  if (std::is_constant_evaluated()) {
     return std::string_view(lhs.c_str()) == std::string_view(rhs.c_str());
   } else {
     return UNSAFE_TODO(std::strcmp(lhs.c_str(), rhs.c_str())) == 0;
@@ -1124,7 +1124,7 @@ constexpr const char* TypeSignatureCStr::c_str() const {
 
 constexpr bool operator==(const TypeSignatureCStr& lhs,
                           const TypeSignatureCStr& rhs) {
-  if consteval {
+  if (std::is_constant_evaluated()) {
     return std::string_view(lhs.c_str()) == std::string_view(rhs.c_str());
   } else {
     return UNSAFE_TODO(std::strcmp(lhs.c_str(), rhs.c_str())) == 0;
diff --git a/storage/browser/blob/blob_memory_controller.cc b/storage/browser/blob/blob_memory_controller.cc
index 250c829854..335e95d1aa 100644
--- a/storage/browser/blob/blob_memory_controller.cc
+++ b/storage/browser/blob/blob_memory_controller.cc
@@ -728,7 +728,7 @@ void BlobMemoryController::NotifyMemoryItemsUsed(
       continue;
     }
     // We don't want to re-add the item if we're currently paging it to disk.
-    if (items_paging_to_file_.contains(item->item_id())) {
+    if (base::Contains(items_paging_to_file_, item->item_id())) {
       return;
     }
     auto iterator = populated_memory_items_.Get(item->item_id());
diff --git a/storage/browser/blob/blob_registry_impl.cc b/storage/browser/blob/blob_registry_impl.cc
index 7e2b6c3ef8..01267fcd85 100644
--- a/storage/browser/blob/blob_registry_impl.cc
+++ b/storage/browser/blob/blob_registry_impl.cc
@@ -522,7 +522,7 @@ void BlobRegistryImpl::Register(
   }
 
   if (uuid.empty() || context_->registry().HasEntry(uuid) ||
-      blobs_under_construction_.contains(uuid)) {
+      base::Contains(blobs_under_construction_, uuid)) {
     receivers_.ReportBadMessage(
         "Invalid UUID passed to BlobRegistry::Register");
     return;
diff --git a/storage/browser/blob/blob_storage_registry.cc b/storage/browser/blob/blob_storage_registry.cc
index 00a738b28e..6ef9abc5b3 100644
--- a/storage/browser/blob/blob_storage_registry.cc
+++ b/storage/browser/blob/blob_storage_registry.cc
@@ -9,6 +9,7 @@
 #include <memory>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "storage/browser/blob/blob_entry.h"
@@ -27,7 +28,7 @@ BlobEntry* BlobStorageRegistry::CreateEntry(
     const std::string& uuid,
     const std::string& content_type,
     const std::string& content_disposition) {
-  DCHECK(!blob_map_.contains(uuid));
+  DCHECK(!base::Contains(blob_map_, uuid));
   std::unique_ptr<BlobEntry> entry =
       std::make_unique<BlobEntry>(content_type, content_disposition);
   BlobEntry* entry_ptr = entry.get();
@@ -40,7 +41,7 @@ bool BlobStorageRegistry::DeleteEntry(const std::string& uuid) {
 }
 
 bool BlobStorageRegistry::HasEntry(const std::string& uuid) const {
-  return blob_map_.contains(uuid);
+  return base::Contains(blob_map_, uuid);
 }
 
 BlobEntry* BlobStorageRegistry::GetEntry(const std::string& uuid) {
diff --git a/storage/browser/blob/blob_url_registry.cc b/storage/browser/blob/blob_url_registry.cc
index b5bd1aa655..ffa14487c2 100644
--- a/storage/browser/blob/blob_url_registry.cc
+++ b/storage/browser/blob/blob_url_registry.cc
@@ -5,6 +5,7 @@
 #include "storage/browser/blob/blob_url_registry.h"
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -203,13 +204,13 @@ void BlobUrlRegistry::AddTokenMapping(
     const GURL& url,
     mojo::PendingRemote<blink::mojom::Blob> blob) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  DCHECK(!token_to_url_and_blob_.contains(token));
+  DCHECK(!base::Contains(token_to_url_and_blob_, token));
   token_to_url_and_blob_.emplace(token, std::make_pair(url, std::move(blob)));
 }
 
 void BlobUrlRegistry::RemoveTokenMapping(const base::UnguessableToken& token) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  DCHECK(token_to_url_and_blob_.contains(token));
+  DCHECK(base::Contains(token_to_url_and_blob_, token));
   token_to_url_and_blob_.erase(token);
 }
 
diff --git a/storage/browser/blob/blob_url_store_impl.cc b/storage/browser/blob/blob_url_store_impl.cc
index b8e434ca87..f7c00fa397 100644
--- a/storage/browser/blob/blob_url_store_impl.cc
+++ b/storage/browser/blob/blob_url_store_impl.cc
@@ -4,8 +4,6 @@
 
 #include "storage/browser/blob/blob_url_store_impl.h"
 
-#include <algorithm>
-
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -276,12 +274,12 @@ bool BlobURLStoreImpl::BlobUrlIsValid(const GURL& url,
     // TODO(crbug.com/40051700): Once `storage_key_` corresponds to an
     // opaque origin under the circumstances described in the crbug, remove the
     // ALLOW_OPAQUE_ORIGIN_STORAGE_KEY_MISMATCH workaround here.
-    valid_origin = storage_key_origin.opaque() ||
-                   std::ranges::contains(url::GetLocalSchemes(),
-                                         storage_key_origin.scheme()) ||
-                   validity_check_behavior_ ==
-                       BlobURLValidityCheckBehavior::
-                           ALLOW_OPAQUE_ORIGIN_STORAGE_KEY_MISMATCH;
+    valid_origin =
+        storage_key_origin.opaque() ||
+        base::Contains(url::GetLocalSchemes(), storage_key_origin.scheme()) ||
+        validity_check_behavior_ ==
+            BlobURLValidityCheckBehavior::
+                ALLOW_OPAQUE_ORIGIN_STORAGE_KEY_MISMATCH;
   } else {
     valid_origin = storage_key_origin == url_origin;
   }
diff --git a/storage/browser/blob/blob_url_utils.cc b/storage/browser/blob/blob_url_utils.cc
index 6594d10867..38f013c053 100644
--- a/storage/browser/blob/blob_url_utils.cc
+++ b/storage/browser/blob/blob_url_utils.cc
@@ -4,11 +4,12 @@
 
 #include "storage/browser/blob/blob_url_utils.h"
 
+#include "base/containers/contains.h"
 
 namespace storage::BlobUrlUtils {
 
 bool UrlHasFragment(const GURL& url) {
-  return url.spec().contains('#');
+  return base::Contains(url.spec(), '#');
 }
 
 GURL ClearUrlFragment(const GURL& url) {
diff --git a/storage/browser/file_system/file_system_context.cc b/storage/browser/file_system/file_system_context.cc
index 457049d00a..4a6ea84d56 100644
--- a/storage/browser/file_system/file_system_context.cc
+++ b/storage/browser/file_system/file_system_context.cc
@@ -10,6 +10,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
@@ -221,8 +222,8 @@ FileSystemContext::FileSystemContext(
   // Chrome OS the additional backend ash::FileSystemBackend handles these
   // types.
   isolated_backend_ = std::make_unique<IsolatedFileSystemBackend>(
-      !backend_map_.contains(kFileSystemTypeLocal),
-      !backend_map_.contains(kFileSystemTypeLocalForPlatformApp));
+      !base::Contains(backend_map_, kFileSystemTypeLocal),
+      !base::Contains(backend_map_, kFileSystemTypeLocalForPlatformApp));
   RegisterBackend(isolated_backend_.get());
 }
 
diff --git a/storage/browser/file_system/file_system_operation_runner.cc b/storage/browser/file_system/file_system_operation_runner.cc
index aba2a34311..7e2290a728 100644
--- a/storage/browser/file_system/file_system_operation_runner.cc
+++ b/storage/browser/file_system/file_system_operation_runner.cc
@@ -11,6 +11,7 @@
 #include <utility>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -355,8 +356,8 @@ OperationID FileSystemOperationRunner::Truncate(const FileSystemURL& url,
 
 void FileSystemOperationRunner::Cancel(OperationID id,
                                        StatusCallback callback) {
-  if (finished_operations_.contains(id)) {
-    DCHECK(!stray_cancel_callbacks_.contains(id));
+  if (base::Contains(finished_operations_, id)) {
+    DCHECK(!base::Contains(stray_cancel_callbacks_, id));
     stray_cancel_callbacks_[id] = std::move(callback);
     return;
   }
@@ -747,7 +748,7 @@ OperationID FileSystemOperationRunner::BeginOperation(
     std::unique_ptr<FileSystemOperation> operation) {
   OperationID id = next_operation_id_++;
 
-  DCHECK(!operations_.contains(id));
+  DCHECK(!base::Contains(operations_, id));
   operations_[id] = std::move(operation);
   return id;
 }
diff --git a/storage/browser/file_system/file_system_quota_client.cc b/storage/browser/file_system/file_system_quota_client.cc
index 8ad9df02e3..3941ac421e 100644
--- a/storage/browser/file_system/file_system_quota_client.cc
+++ b/storage/browser/file_system/file_system_quota_client.cc
@@ -4,7 +4,6 @@
 
 #include "storage/browser/file_system/file_system_quota_client.h"
 
-#include <algorithm>
 #include <numeric>
 #include <string>
 #include <utility>
@@ -226,7 +225,7 @@ std::vector<FileSystemType> FileSystemQuotaClient::GetFileSystemTypes() const {
       file_system_context_->GetFileSystemTypes();
   std::vector<FileSystemType> fs_types;
   for (auto fs_type : kTemporaryAndPersistentAndSyncable) {
-    if (std::ranges::contains(backend_types, fs_type)) {
+    if (base::Contains(backend_types, fs_type)) {
       fs_types.push_back(fs_type);
     }
   }
diff --git a/storage/browser/file_system/file_system_usage_cache.cc b/storage/browser/file_system/file_system_usage_cache.cc
index 741c662a3e..982316c28c 100644
--- a/storage/browser/file_system/file_system_usage_cache.cc
+++ b/storage/browser/file_system/file_system_usage_cache.cc
@@ -150,7 +150,7 @@ bool FileSystemUsageCache::Exists(const base::FilePath& usage_file_path) {
   TRACE_EVENT0("FileSystem", "UsageCache::Exists");
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   if (is_incognito_)
-    return incognito_usages_.contains(usage_file_path);
+    return base::Contains(incognito_usages_, usage_file_path);
   return base::PathExists(usage_file_path);
 }
 
@@ -281,7 +281,7 @@ bool FileSystemUsageCache::FlushFile(const base::FilePath& file_path) {
   TRACE_EVENT0("FileSystem", "UsageCache::FlushFile");
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   if (is_incognito_)
-    return incognito_usages_.contains(file_path);
+    return base::Contains(incognito_usages_, file_path);
   base::File* file = GetFile(file_path);
   if (!file)
     return false;
@@ -299,7 +299,7 @@ void FileSystemUsageCache::ScheduleCloseTimer() {
 bool FileSystemUsageCache::HasCacheFileHandle(const base::FilePath& file_path) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   DCHECK_LE(cache_files_.size(), kMaxHandleCacheSize);
-  return cache_files_.contains(file_path);
+  return base::Contains(cache_files_, file_path);
 }
 
 }  // namespace storage
diff --git a/storage/browser/file_system/isolated_context.cc b/storage/browser/file_system/isolated_context.cc
index 75e210c935..4689bbfcd7 100644
--- a/storage/browser/file_system/isolated_context.cc
+++ b/storage/browser/file_system/isolated_context.cc
@@ -10,6 +10,7 @@
 #include <memory>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "base/rand_util.h"
 #include "base/strings/string_number_conversions.h"
@@ -431,7 +432,7 @@ void IsolatedContext::RevokeFileSystemByPath(const base::FilePath& path_in) {
 
 void IsolatedContext::AddReference(const std::string& filesystem_id) {
   base::AutoLock locker(lock_);
-  DCHECK(instance_map_.contains(filesystem_id));
+  DCHECK(base::Contains(instance_map_, filesystem_id));
   instance_map_[filesystem_id]->AddRef();
 }
 
@@ -522,7 +523,7 @@ std::string IsolatedContext::GetNewFileSystemId() const {
   do {
     base::RandBytes(random_data);
     id = base::HexEncode(random_data);
-  } while (instance_map_.contains(id));
+  } while (base::Contains(instance_map_, id));
   return id;
 }
 
diff --git a/storage/browser/file_system/sandbox_directory_database.cc b/storage/browser/file_system/sandbox_directory_database.cc
index 21925506cd..77f6986bd6 100644
--- a/storage/browser/file_system/sandbox_directory_database.cc
+++ b/storage/browser/file_system/sandbox_directory_database.cc
@@ -8,10 +8,10 @@
 #include <stddef.h>
 #include <stdint.h>
 
-#include <algorithm>
 #include <memory>
 #include <set>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/containers/stack.h"
 #include "base/files/file_enumerator.h"
@@ -296,7 +296,7 @@ bool DatabaseCheckHelper::ScanDirectory() {
       if (!path_.AppendRelativePath(absolute_file_path, &relative_file_path))
         return false;
 
-      if (std::ranges::contains(kExcludes, relative_file_path))
+      if (base::Contains(kExcludes, relative_file_path))
         continue;
 
       if (find_info.IsDirectory()) {
diff --git a/storage/browser/file_system/sandbox_file_system_backend_delegate.cc b/storage/browser/file_system/sandbox_file_system_backend_delegate.cc
index d737a34304..888dae6b81 100644
--- a/storage/browser/file_system/sandbox_file_system_backend_delegate.cc
+++ b/storage/browser/file_system/sandbox_file_system_backend_delegate.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_error_or.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -366,7 +367,8 @@ int64_t SandboxFileSystemBackendDelegate::GetBucketUsageOnFileTaskRunner(
     FileSystemType type) {
   DCHECK(file_task_runner_->RunsTasksInCurrentSequence());
 
-  if (sticky_dirty_origins_.contains(
+  if (base::Contains(
+          sticky_dirty_origins_,
           std::make_pair(bucket_locator.storage_key.origin(), type))) {
     return RecalculateBucketUsage(file_system_context, bucket_locator, type);
   }
diff --git a/storage/browser/quota/client_usage_tracker.cc b/storage/browser/quota/client_usage_tracker.cc
index bae16aca45..3af6d3ab94 100644
--- a/storage/browser/quota/client_usage_tracker.cc
+++ b/storage/browser/quota/client_usage_tracker.cc
@@ -8,6 +8,7 @@
 #include <iterator>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "third_party/blink/public/common/storage_key/storage_key.h"
@@ -147,8 +148,8 @@ void ClientUsageTracker::SetUsageCacheEnabled(
 bool ClientUsageTracker::IsUsageCacheEnabledForStorageKey(
     const blink::StorageKey& storage_key) const {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  return !non_cached_limited_storage_keys_.contains(storage_key) &&
-         !non_cached_unlimited_storage_keys_.contains(storage_key);
+  return !base::Contains(non_cached_limited_storage_keys_, storage_key) &&
+         !base::Contains(non_cached_unlimited_storage_keys_, storage_key);
 }
 
 void ClientUsageTracker::AccumulateBucketsUsage(
diff --git a/storage/browser/quota/quota_callbacks.h b/storage/browser/quota/quota_callbacks.h
index 35993476ca..71edd3b1dd 100644
--- a/storage/browser/quota/quota_callbacks.h
+++ b/storage/browser/quota/quota_callbacks.h
@@ -14,6 +14,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback.h"
 #include "components/services/storage/public/cpp/buckets/bucket_locator.h"
 #include "third_party/blink/public/mojom/quota/quota_types.mojom-forward.h"
diff --git a/storage/browser/quota/quota_database.cc b/storage/browser/quota/quota_database.cc
index 8f3007b3fa..6da764d67a 100644
--- a/storage/browser/quota/quota_database.cc
+++ b/storage/browser/quota/quota_database.cc
@@ -12,6 +12,7 @@
 #include <vector>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
@@ -656,7 +657,7 @@ QuotaErrorOr<std::set<BucketLocator>> QuotaDatabase::GetBucketsForEviction(
     }
 
     BucketId read_bucket_id = BucketId(statement.ColumnInt64(0));
-    if (bucket_exceptions.contains(read_bucket_id)) {
+    if (base::Contains(bucket_exceptions, read_bucket_id)) {
       continue;
     }
 
diff --git a/storage/browser/quota/quota_manager_impl.cc b/storage/browser/quota/quota_manager_impl.cc
index 3a1102c6f4..690b208f72 100644
--- a/storage/browser/quota/quota_manager_impl.cc
+++ b/storage/browser/quota/quota_manager_impl.cc
@@ -18,6 +18,7 @@
 #include "base/barrier_closure.h"
 #include "base/check_op.h"
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -827,7 +828,7 @@ class QuotaManagerImpl::BucketSetDataDeleter {
     DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
     DCHECK(deleter->completed());
 
-    DCHECK(bucket_deleters_.contains(deleter));
+    DCHECK(base::Contains(bucket_deleters_, deleter));
     bucket_deleters_.erase(deleter);
 
     if (!entry.has_value()) {
@@ -2518,7 +2519,7 @@ void QuotaManagerImpl::WithdrawOverridesForHandle(int handle_id) {
 std::optional<int64_t> QuotaManagerImpl::GetQuotaOverrideForStorageKey(
     const StorageKey& storage_key) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
-  if (!devtools_overrides_.contains(storage_key)) {
+  if (!base::Contains(devtools_overrides_, storage_key)) {
     return std::nullopt;
   }
   return devtools_overrides_[storage_key].quota_size;
diff --git a/storage/browser/quota/quota_task.cc b/storage/browser/quota/quota_task.cc
index 83ec7a125f..00e28f1eb7 100644
--- a/storage/browser/quota/quota_task.cc
+++ b/storage/browser/quota/quota_task.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <functional>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/task/single_thread_task_runner.h"
@@ -71,7 +72,7 @@ void QuotaTaskObserver::RegisterTask(QuotaTask* task) {
 }
 
 void QuotaTaskObserver::UnregisterTask(QuotaTask* task) {
-  DCHECK(running_quota_tasks_.contains(task));
+  DCHECK(base::Contains(running_quota_tasks_, task));
   running_quota_tasks_.erase(task);
 }
 
diff --git a/storage/browser/test/mock_special_storage_policy.cc b/storage/browser/test/mock_special_storage_policy.cc
index f5c8b432a5..042d7f881a 100644
--- a/storage/browser/test/mock_special_storage_policy.cc
+++ b/storage/browser/test/mock_special_storage_policy.cc
@@ -4,28 +4,29 @@
 
 #include "storage/browser/test/mock_special_storage_policy.h"
 
+#include "base/containers/contains.h"
 
 namespace storage {
 
 MockSpecialStoragePolicy::MockSpecialStoragePolicy() : all_unlimited_(false) {}
 
 bool MockSpecialStoragePolicy::IsStorageProtected(const GURL& origin) {
-  return protected_.contains(origin);
+  return base::Contains(protected_, origin);
 }
 
 bool MockSpecialStoragePolicy::IsStorageUnlimited(const GURL& origin) {
   if (all_unlimited_) {
     return true;
   }
-  return unlimited_.contains(origin);
+  return base::Contains(unlimited_, origin);
 }
 
 bool MockSpecialStoragePolicy::IsStorageSessionOnly(const GURL& origin) {
-  return session_only_.contains(origin);
+  return base::Contains(session_only_, origin);
 }
 
 bool MockSpecialStoragePolicy::HasIsolatedStorage(const GURL& origin) {
-  return isolated_.contains(origin);
+  return base::Contains(isolated_, origin);
 }
 
 bool MockSpecialStoragePolicy::HasSessionOnlyOrigins() {
diff --git a/storage/common/database/database_identifier.cc b/storage/common/database/database_identifier.cc
index 1cd8d49e45..9f335e6492 100644
--- a/storage/common/database/database_identifier.cc
+++ b/storage/common/database/database_identifier.cc
@@ -10,6 +10,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/strings/strcat.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
@@ -107,7 +108,7 @@ DatabaseIdentifier DatabaseIdentifier::CreateFromOrigin(const GURL& origin) {
 DatabaseIdentifier DatabaseIdentifier::Parse(std::string_view identifier) {
   if (!base::IsStringASCII(identifier))
     return DatabaseIdentifier();
-  if (identifier.contains("..")) {
+  if (base::Contains(identifier, "..")) {
     return DatabaseIdentifier();
   }
   static const char kForbidden[] = {'\\', '/', ':', '\0'};
diff --git a/third_party/crashpad/crashpad/handler/linux/exception_handler_server.cc b/third_party/crashpad/crashpad/handler/linux/exception_handler_server.cc
index 2761b90d50..64c77aa388 100644
--- a/third_party/crashpad/crashpad/handler/linux/exception_handler_server.cc
+++ b/third_party/crashpad/crashpad/handler/linux/exception_handler_server.cc
@@ -30,6 +30,7 @@
 #include "base/logging.h"
 #include "base/posix/eintr_wrapper.h"
 #include "base/strings/string_number_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "util/file/file_io.h"
 #include "util/file/filesystem.h"
@@ -324,8 +325,8 @@ void ExceptionHandlerServer::Stop() {
 }
 
 void ExceptionHandlerServer::HandleEvent(Event* event, uint32_t event_type) {
-  DCHECK_NE(std::to_underlying(event->type),
-            std::to_underlying(Event::Type::kShutdown));
+  DCHECK_NE(base::to_underlying(event->type),
+            base::to_underlying(Event::Type::kShutdown));
 
   if (event_type & EPOLLERR) {
     LogSocketError(event->fd.get());
diff --git a/third_party/crashpad/crashpad/snapshot/crashpad_types/crashpad_info_reader.cc b/third_party/crashpad/crashpad/snapshot/crashpad_types/crashpad_info_reader.cc
index ca1fa99a61..39d876cc4a 100644
--- a/third_party/crashpad/crashpad/snapshot/crashpad_types/crashpad_info_reader.cc
+++ b/third_party/crashpad/crashpad/snapshot/crashpad_types/crashpad_info_reader.cc
@@ -15,8 +15,8 @@
 #include "snapshot/crashpad_types/crashpad_info_reader.h"
 
 #include <type_traits>
-#include <utility>
 
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "client/crashpad_info.h"
 
@@ -33,13 +33,13 @@ namespace crashpad {
 namespace {
 
 void UnsetIfNotValidTriState(TriState* value) {
-  switch (std::to_underlying(*value)) {
-    case std::to_underlying(TriState::kUnset):
-    case std::to_underlying(TriState::kEnabled):
-    case std::to_underlying(TriState::kDisabled):
+  switch (base::to_underlying(*value)) {
+    case base::to_underlying(TriState::kUnset):
+    case base::to_underlying(TriState::kEnabled):
+    case base::to_underlying(TriState::kDisabled):
       return;
   }
-  LOG(WARNING) << "Unsetting invalid TriState " << std::to_underlying(*value);
+  LOG(WARNING) << "Unsetting invalid TriState " << base::to_underlying(*value);
   *value = TriState::kUnset;
 }
 
diff --git a/third_party/crashpad/crashpad/snapshot/crashpad_types/image_annotation_reader_test.cc b/third_party/crashpad/crashpad/snapshot/crashpad_types/image_annotation_reader_test.cc
index a9fead3424..55644ff78e 100644
--- a/third_party/crashpad/crashpad/snapshot/crashpad_types/image_annotation_reader_test.cc
+++ b/third_party/crashpad/crashpad/snapshot/crashpad_types/image_annotation_reader_test.cc
@@ -19,8 +19,8 @@
 #include <unistd.h>
 
 #include <algorithm>
-#include <utility>
 
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "client/annotation.h"
 #include "client/annotation_list.h"
@@ -54,7 +54,7 @@ void ExpectAnnotationList(const std::vector<AnnotationSnapshot>& list,
   for (const Annotation* expected_annotation : expected_list) {
     const AnnotationSnapshot& annotation = list[index++];
     EXPECT_EQ(annotation.name, expected_annotation->name());
-    EXPECT_EQ(annotation.type, std::to_underlying(expected_annotation->type()));
+    EXPECT_EQ(annotation.type, base::to_underlying(expected_annotation->type()));
     EXPECT_EQ(annotation.value.size(), expected_annotation->size());
     EXPECT_EQ(memcmp(annotation.value.data(),
                      expected_annotation->value(),
diff --git a/third_party/crashpad/crashpad/util/linux/proc_task_reader.cc b/third_party/crashpad/crashpad/util/linux/proc_task_reader.cc
index 7aacb69676..2dd7dd7ea1 100644
--- a/third_party/crashpad/crashpad/util/linux/proc_task_reader.cc
+++ b/third_party/crashpad/crashpad/util/linux/proc_task_reader.cc
@@ -17,12 +17,12 @@
 #include <stdio.h>
 
 #include <iterator>
-#include <utility>
 
 #include "base/check_op.h"
 #include "base/files/file_path.h"
 #include "base/logging.h"
 #include "base/strings/string_number_conversions.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "util/file/directory_reader.h"
 
 namespace crashpad {
@@ -50,8 +50,8 @@ bool ReadThreadIDs(pid_t pid, std::vector<pid_t>* tids) {
 
     local_tids.push_back(tid);
   }
-  DCHECK_EQ(std::to_underlying(result),
-            std::to_underlying(DirectoryReader::Result::kNoMoreFiles));
+  DCHECK_EQ(base::to_underlying(result),
+            base::to_underlying(DirectoryReader::Result::kNoMoreFiles));
   DCHECK(!local_tids.empty());
 
   tids->swap(local_tids);
diff --git a/url/origin.cc b/url/origin.cc
index cb1cb61b6d..01cbf91ff8 100644
--- a/url/origin.cc
+++ b/url/origin.cc
@@ -18,6 +18,7 @@
 #include "base/check.h"
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/debug/crash_logging.h"
 #include "base/pickle.h"
@@ -55,7 +56,7 @@ Origin Origin::Create(const GURL& url) {
     // It's SchemeHostPort's responsibility to filter out unrecognized schemes;
     // sanity check that this is happening.
     DCHECK(!tuple.IsValid() || url.IsStandard() ||
-           std::ranges::contains(GetLocalSchemes(), url.scheme()) ||
+           base::Contains(GetLocalSchemes(), url.scheme()) ||
            AllowNonStandardSchemesForAndroidWebView());
   }
 
@@ -185,7 +186,7 @@ bool Origin::CanBeDerivedFrom(const GURL& url) const {
   // For "no access" schemes, blink's SecurityOrigin will always create an
   // opaque unique one. However, about: scheme is also registered as such but
   // does not behave this way, therefore exclude it from this check.
-  if (std::ranges::contains(url::GetNoAccessSchemes(), url.GetScheme()) &&
+  if (base::Contains(url::GetNoAccessSchemes(), url.GetScheme()) &&
       !url.SchemeIs(kAboutScheme)) {
     // If |this| is not opaque, definitely return false as the expectation
     // is for opaque origin.
diff --git a/url/scheme_host_port.cc b/url/scheme_host_port.cc
index f4aebf5c7a..ef98e56bf1 100644
--- a/url/scheme_host_port.cc
+++ b/url/scheme_host_port.cc
@@ -7,13 +7,13 @@
 #include <stdint.h>
 #include <string.h>
 
-#include <algorithm>
 #include <compare>
 #include <ostream>
 #include <string_view>
 #include <tuple>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/strings/string_number_conversions.h"
@@ -73,7 +73,7 @@ bool IsValidInput(std::string_view scheme,
 
   // about:blank and other no-access schemes translate into an opaque origin.
   // This helps consistency with ShouldTreatAsOpaqueOrigin in Blink.
-  if (std::ranges::contains(GetNoAccessSchemes(), scheme))
+  if (base::Contains(GetNoAccessSchemes(), scheme))
     return false;
 
   SchemeType scheme_type = SCHEME_WITH_HOST_PORT_AND_USER_INFORMATION;
@@ -86,7 +86,7 @@ bool IsValidInput(std::string_view scheme,
     // remove this local scheme exception.
     // For standard compliant non special scheme url parsing, a host can be
     // empty for non-special URLs. Therefore, we don't check a host nor port.
-    if (std::ranges::contains(GetLocalSchemes(), scheme)) {
+    if (base::Contains(GetLocalSchemes(), scheme)) {
       return true;
     }
 
diff --git a/url/url_util.cc b/url/url_util.cc
index 3d88fa831f..a87289913e 100644
--- a/url/url_util.cc
+++ b/url/url_util.cc
@@ -7,13 +7,13 @@
 #include <stddef.h>
 #include <string.h>
 
-#include <algorithm>
 #include <atomic>
 #include <ostream>
 #include <string_view>
 
 #include "base/check_op.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_util.h"
 #include "url/url_canon_internal.h"
@@ -516,8 +516,7 @@ void DoAddSchemeWithHandler(std::string_view new_scheme,
   DCHECK(!new_scheme.empty());
   DCHECK(!handler.empty());
   DCHECK_EQ(base::ToLowerASCII(new_scheme), new_scheme);
-  DCHECK(
-      !std::ranges::contains(*schemes, new_scheme, &SchemeWithHandler::scheme));
+  DCHECK(!base::Contains(*schemes, new_scheme, &SchemeWithHandler::scheme));
   schemes->push_back({std::string(new_scheme), std::string(handler)});
 }
 
@@ -527,7 +526,7 @@ void DoAddScheme(std::string_view new_scheme,
   DCHECK(schemes);
   DCHECK(!new_scheme.empty());
   DCHECK_EQ(base::ToLowerASCII(new_scheme), new_scheme);
-  DCHECK(!std::ranges::contains(*schemes, new_scheme));
+  DCHECK(!base::Contains(*schemes, new_scheme));
   schemes->push_back(std::string(new_scheme));
 }
 
@@ -538,7 +537,7 @@ void DoAddSchemeWithType(std::string_view new_scheme,
   DCHECK(schemes);
   DCHECK(!new_scheme.empty());
   DCHECK_EQ(base::ToLowerASCII(new_scheme), new_scheme);
-  DCHECK(!std::ranges::contains(*schemes, new_scheme, &SchemeWithType::scheme));
+  DCHECK(!base::Contains(*schemes, new_scheme, &SchemeWithType::scheme));
   schemes->push_back({std::string(new_scheme), type});
 }
 
