# Build demos as programs like in Linux

--- a/demos/meson.build
+++ b/demos/meson.build
@@ -6,7 +6,7 @@
 gmodule_headers_dep = gmodule_dep.partial_dependency(includes: true)
 
 demo_kwargs = {}
-if meson.version().version_compare('>= 1.8.0')
+if false and meson.version().version_compare('>= 1.8.0')
   demo_kwargs += { 'android_exe_type': 'application' }
 endif
 
