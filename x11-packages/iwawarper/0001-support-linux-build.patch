diff --git a/sources/CMakeLists.txt b/sources/CMakeLists.txt
index bbe290c..506a8df 100644
--- a/sources/CMakeLists.txt
+++ b/sources/CMakeLists.txt
@@ -200,15 +200,19 @@ fill_frag.glsl
 #-----------------------------------------------------------------------------
 # Select build environment
 set(BUILD_ENV_MSVC     OFF)
+set(BUILD_ENV_LINUX    OFF)
 set(BUILD_ENV_APPLE    OFF)
 if(MSVC)
     set(BUILD_ENV_MSVC ON)
     message(STATUS "Build environment is MSVC")
+elseif(UNIX AND NOT APPLE)
+    set(BUILD_ENV_LINUX ON)
+    message(STATUS "Build environment is LINUX")
 elseif(CMAKE_HOST_APPLE)
     set(BUILD_ENV_APPLE ON)
     message(STATUS "Build environment is APPLE")
 else()
-    message(ERROR "Unsupported build environment, only MSVC and APPLE are supported")
+    message(ERROR "Unsupported build environment, only MSVC, APPLE, and LINUX are supported")
 endif()
 
 #-----------------------------------------------------------------------------
@@ -317,6 +321,9 @@ if(BUILD_ENV_MSVC)
 elseif(BUILD_ENV_APPLE)
     find_library(GLUT_LIB GLUT)
     find_library(GL_LIB OpenGL)
+else()
+    set(GLUT_LIB glut)
+    set(GL_LIB GL)
 endif()
 
 macro(_find_toonz_library OUT_LIB_LIST IN_LIB_LIST)
@@ -326,6 +333,9 @@ macro(_find_toonz_library OUT_LIB_LIST IN_LIB_LIST)
             set(TOONZLIBPATH "${SDKROOT}/OpenToonz/lib/${arg}.lib")
         elseif(BUILD_ENV_APPLE)
             set(TOONZLIBPATH "${SDKROOT}/OpenToonz/dylib/lib${arg}.dylib")
+        else()
+            find_library(TOONZLIB_${arg} ${arg} HINTS ${CMAKE_INSTALL_PREFIX}/lib/opentoonz ${CMAKE_INSTALL_PREFIX}/lib)
+            set(TOONZLIBPATH ${TOONZLIB_${arg}})
         endif()
         set(OUTLIST ${OUTLIST} ${TOONZLIBPATH})
     endforeach()
@@ -336,10 +346,13 @@ _find_toonz_library(TOONZ_LIBS "tnzcore;tnzbase;toonzlib;image;colorfx")
 # 生成する実行ファイルを追加する
 if(BUILD_ENV_MSVC)
     add_executable(${PROJECT_NAME} WIN32 ${HEADERS} ${SOURCES} ${RESOURCES} ${RC_FILE})
-else()
+elseif(BUILD_ENV_APPLE)
     set(MACOSX_BUNDLE_ICON_FILE "IwaWarper.icns")
     set_source_files_properties(IwaWarper.icns PROPERTIES MACOSX_PACKAGE_LOCATION "Resources")
     add_executable(${PROJECT_NAME} MACOSX_BUNDLE IwaWarper.icns ${HEADERS} ${SOURCES} ${RESOURCES})
+else()
+    add_executable(${PROJECT_NAME} ${HEADERS} ${SOURCES} ${RESOURCES})
+    install(TARGETS ${PROJECT_NAME} DESTINATION bin)
 endif()
 
 # link Qt library. Core and Gui will be linked accordingly
@@ -347,6 +360,10 @@ target_link_libraries(${PROJECT_NAME} Qt5::Widgets Qt5::OpenGL Qt5::Svg
 ${GL_LIB} ${GLUT_LIB} ${TOONZ_LIBS}
 )
 
+if(BUILD_ENV_LINUX)
+    add_definitions(-DLINUX)
+endif()
+
 add_definitions(
     -DNOMINMAX
     -D_USE_MATH_DEFINES
