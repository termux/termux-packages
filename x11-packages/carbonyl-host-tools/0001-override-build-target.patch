--- a/src/browser/BUILD.gn
+++ b/src/browser/BUILD.gn
@@ -40,7 +40,7 @@
   if (is_mac) {
     target += "apple-darwin"
   } else if (is_linux) {
-    target += "unknown-linux-gnu"
+    target += "linux-android"
   }
 
   libs = ["carbonyl"]
