From d821c20649f7f711da46ba3037d908618cfc77ec Mon Sep 17 00:00:00 2001
From: Chongyun Lee <licy183@termux.dev>
Date: Tue, 10 Mar 2026 01:59:51 +0800
Subject: [PATCH 4/8] blink and mojo

---
 .../c++/interface.test-mojom.cc.golden        |  5 +-
 .../c++/results.test-mojom.cc.golden          | 11 +++--
 .../c++/typemap.test-mojom.cc.golden          |  5 +-
 mojo/public/cpp/base/proto_wrapper.cc         |  4 +-
 mojo/public/cpp/base/values_mojom_traits.cc   |  6 ++-
 .../cpp/bindings/lib/multiplex_router.cc      |  6 +--
 mojo/public/cpp/bindings/remote_set.h         |  1 +
 .../cpp_templates/interface_definition.tmpl   |  8 ++--
 .../associated_interface_provider.cc          |  3 +-
 .../custom_handlers/protocol_handler_utils.cc |  4 +-
 .../web_coalesced_input_event_mojom_traits.cc |  6 +--
 .../auction_config_mojom_traits.cc            |  4 +-
 .../common/interest_group/interest_group.cc   |  7 ++-
 .../common/loader/throttling_url_loader.cc    |  4 +-
 .../common/manifest/manifest_icon_selector.cc |  3 +-
 .../blink/common/mime_util/mime_util.cc       |  1 +
 ...manual_completion_origin_trial_features.cc |  5 +-
 .../navigation_origin_trial_features.cc       |  5 +-
 .../origin_trials/persistent_origin_trials.cc |  4 +-
 third_party/blink/common/scheme_registry.cc   |  5 +-
 .../service_worker_scope_match.cc             |  5 +-
 .../common/user_agent/user_agent_metadata.cc  |  3 +-
 .../web_package_request_matcher.cc            |  6 +--
 third_party/blink/renderer/DEPS               |  2 +
 .../core/v8/serialization/trailer_reader.cc   |  8 ++--
 .../scripts/templates/origin_trials.cc.tmpl   | 11 +++--
 .../core/css/css_image_generator_value.cc     |  1 +
 .../css/cssom/computed_style_property_map.cc  |  4 +-
 .../core/css/parser/css_variable_parser.cc    |  2 +-
 .../blink/renderer/core/css/rule_set.cc       |  1 +
 .../renderer/core/css/selector_checker.cc     |  6 +--
 .../core/css/style_environment_variables.cc   |  1 +
 .../renderer/core/dom/focusgroup_flags.h      | 12 ++---
 .../core/dom/live_node_list_registry.cc       |  3 +-
 .../blink/renderer/core/dom/subscriber.cc     |  5 +-
 .../renderer/core/editing/ime/edit_context.cc |  5 +-
 .../blink/renderer/core/events/event_util.cc  |  5 +-
 .../security_context_init.cc                  |  5 +-
 .../web_language_detection_details.cc         |  6 +--
 .../core/frame/csp/content_security_policy.cc |  3 +-
 .../renderer/core/frame/integrity_policy.cc   |  7 ++-
 .../renderer/core/frame/local_dom_window.cc   |  1 +
 .../renderer/core/html/client_hints_util.cc   |  4 +-
 .../core/html/forms/html_button_element.cc    |  4 +-
 .../core/html/forms/html_button_element.h     |  8 ++--
 .../core/html/forms/html_form_element.cc      |  3 +-
 .../renderer/core/html/forms/input_type.h     | 47 +++++++++----------
 .../core/html/html_capability_element_base.cc |  3 +-
 .../core/html/html_iframe_element_sandbox.cc  |  5 +-
 .../renderer/core/html/html_slot_element.cc   |  1 +
 .../media/media_video_visibility_tracker.cc   |  4 +-
 .../core/html/parser/atomic_html_token.h      |  6 +--
 .../renderer/core/layout/fragment_builder.cc  |  5 +-
 .../layout/svg/svg_text_layout_algorithm.cc   |  9 ++--
 .../renderer/core/loader/document_loader.cc   |  3 +-
 .../core/loader/mixed_content_checker.cc      |  6 +--
 .../renderer/core/loader/preload_helper.cc    |  5 +-
 .../web_bundle/script_web_bundle_rule.cc      |  4 +-
 .../permissions_policy_parser.cc              |  5 +-
 .../scheduler/task_attribution_tracker_impl.h |  1 +
 .../document_rule_predicate.cc                |  5 +-
 .../document_speculation_rules.cc             |  3 +-
 .../speculation_rules/speculation_rule_set.cc |  9 ++--
 .../speculation_rules_header.cc               |  1 +
 .../renderer/core/style/counter_directives.h  |  6 ++-
 .../renderer/core/style/filter_operations.cc  |  6 +--
 .../core/testing/mock_clipboard_host.cc       |  5 +-
 .../blink/renderer/core/timing/performance.cc |  1 +
 .../view_transition_style_tracker.cc          |  2 +-
 .../core/xmlhttprequest/xml_http_request.cc   |  3 +-
 .../modules/accessibility/ax_node_object.cc   |  3 +-
 .../accessibility/ax_object_cache_impl.cc     |  1 +
 .../modules/ad_auction/navigator_auction.cc   |  4 +-
 .../renderer/modules/cache_storage/cache.cc   |  4 +-
 .../modules/cookie_store/cookie_store.cc      |  5 +-
 .../filesystem/file_system_dispatcher.cc      |  1 +
 .../modules/font_access/font_access.cc        |  3 +-
 .../modules/imagecapture/image_capture.cc     | 44 +++++++++--------
 .../audio/audio_renderer_mixer_manager.cc     |  3 +-
 .../media_recorder_encoder_wrapper.cc         |  5 +-
 .../modules/mediastream/media_constraints.cc  |  5 +-
 .../media_stream_constraints_util_audio.cc    |  9 ++--
 .../media_stream_constraints_util_sets.h      |  5 +-
 ...ia_stream_constraints_util_video_device.cc |  9 ++--
 .../mediastream/media_stream_video_source.cc  |  3 +-
 .../mediastream/media_stream_video_track.cc   |  3 +-
 .../mediastream/user_media_processor.cc       |  3 +-
 .../blink/renderer/modules/ml/ml_context.cc   |  7 ++-
 .../modules/ml/webnn/ml_graph_builder.cc      |  5 +-
 ...mock_peer_connection_dependency_factory.cc |  1 +
 .../mock_peer_connection_impl.cc              |  5 +-
 .../peerconnection/peer_connection_tracker.cc |  1 +
 .../rtc_peer_connection_handler.cc            |  4 +-
 .../modules/plugins/dom_mime_type_array.cc    |  6 +--
 .../modules/screen_details/screen_details.cc  | 11 ++---
 .../service_worker_event_queue.cc             |  1 +
 .../thread_safe_script_container.cc           |  1 +
 .../modules/webcodecs/audio_encoder.cc        |  4 +-
 .../modules/webcodecs/video_encoder.cc        |  5 +-
 .../webgl/webgl_rendering_context_base.cc     |  1 +
 .../blink/renderer/modules/webgpu/gpu.cc      |  5 +-
 .../webrtc/webrtc_audio_device_impl.cc        |  7 ++-
 .../modules/webrtc/webrtc_audio_renderer.cc   |  3 +-
 .../renderer/modules/xr/xr_plane_manager.cc   |  1 +
 .../renderer/modules/xr/xr_render_state.cc    |  5 +-
 .../blink/renderer/modules/xr/xr_session.cc   |  1 +
 .../blink/renderer/modules/xr/xr_system.cc    |  7 +--
 .../blink/renderer/modules/xr/xr_view.cc      |  1 +
 .../renderer/platform/disk_data_allocator.cc  |  3 +-
 .../platform/exported/file_path_conversion.cc |  3 +-
 .../platform/fonts/font_description.cc        |  3 +-
 .../platform/fonts/opentype/open_type_types.h | 18 ++++---
 .../platform/fonts/script_run_iterator.cc     |  3 +-
 .../platform/fonts/simple_font_data.cc        |  5 +-
 .../paint_worklet_paint_dispatcher.cc         |  1 +
 .../renderer/platform/loader/cors/cors.cc     |  5 +-
 .../platform/loader/fetch/resource_fetcher.cc |  4 +-
 .../loader/fetch/resource_load_scheduler.cc   |  1 +
 .../loader/fetch/resource_response.cc         |  1 +
 .../loader/fetch/resource_timing_utils.cc     |  5 +-
 .../renderer/platform/media/cache_util.cc     | 11 +++--
 .../platform/media/cdm_session_adapter.cc     |  5 +-
 .../renderer/platform/media/multi_buffer.cc   |  4 +-
 .../media/new_session_cdm_result_promise.cc   |  4 +-
 .../resource_multi_buffer_data_provider.cc    |  3 +-
 .../media/video_decode_stats_reporter.cc      |  5 +-
 .../media_stream_audio_deliverer.h            |  5 +-
 .../renderer/platform/mhtml/mhtml_archive.cc  |  5 +-
 .../renderer/platform/mhtml/mhtml_parser.cc   |  1 +
 .../network/network_state_notifier.cc         |  1 +
 .../platform/peerconnection/rtc_stats.cc      |  1 +
 .../rtc_video_decoder_adapter.cc              |  3 +-
 .../peerconnection/rtc_video_encoder.cc       | 12 ++---
 .../rtc_video_encoder_factory.cc              |  9 ++--
 .../video_encoder_state_observer_impl.cc      |  3 +-
 .../peerconnection/webrtc_audio_sink.cc       |  3 +-
 .../worker/worker_thread_scheduler.cc         |  1 +
 .../testing/url_loader_mock_factory_impl.cc   |  1 +
 .../blink/renderer/platform/weborigin/kurl.cc |  4 +-
 .../platform/weborigin/security_origin.cc     | 13 +++--
 .../convert_to_webrtc_video_frame_buffer.cc   |  5 +-
 .../webrtc/webrtc_video_frame_adapter.cc      |  6 +--
 .../platform/wtf/text/text_codec_utf16.cc     |  5 +-
 143 files changed, 379 insertions(+), 338 deletions(-)

diff --git a/mojo/golden/generated/c++/interface.test-mojom.cc.golden b/mojo/golden/generated/c++/interface.test-mojom.cc.golden
index 0a063b0066..e044e2f524 100644
--- a/mojo/golden/generated/c++/interface.test-mojom.cc.golden
+++ b/mojo/golden/generated/c++/interface.test-mojom.cc.golden
@@ -22,6 +22,7 @@
 #include "base/task/thread_pool/thread_pool_instance.h"
 #include "base/trace_event/trace_event.h"
 #include "base/trace_event/typed_macros.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "mojo/public/cpp/bindings/lib/default_construct_tag_internal.h"
 #include "mojo/public/cpp/bindings/lib/generated_code_util.h"
 #include "mojo/public/cpp/bindings/lib/message_internal.h"
@@ -145,7 +146,7 @@ void IFaceProxy::Method(
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::IFace::kMethod), kFlags, estimated_payload_size);
+      base::to_underlying(messages::IFace::kMethod), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::IFace_Method_Params_Data> params(
           message);
@@ -262,7 +263,7 @@ void IFace_Method_ProxyToResponder::Run(
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::IFace::kMethod), kFlags, estimated_payload_size);
+      base::to_underlying(messages::IFace::kMethod), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::IFace_Method_ResponseParams_Data> params(
           message);
diff --git a/mojo/golden/generated/c++/results.test-mojom.cc.golden b/mojo/golden/generated/c++/results.test-mojom.cc.golden
index 8e471dcbd0..bd10cf152a 100644
--- a/mojo/golden/generated/c++/results.test-mojom.cc.golden
+++ b/mojo/golden/generated/c++/results.test-mojom.cc.golden
@@ -22,6 +22,7 @@
 #include "base/task/thread_pool/thread_pool_instance.h"
 #include "base/trace_event/trace_event.h"
 #include "base/trace_event/typed_macros.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "mojo/public/cpp/bindings/lib/default_construct_tag_internal.h"
 #include "mojo/public/cpp/bindings/lib/generated_code_util.h"
 #include "mojo/public/cpp/bindings/lib/message_internal.h"
@@ -412,7 +413,7 @@ void ResultInterfaceProxy::Method(
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::ResultInterface::kMethod), kFlags, estimated_payload_size);
+      base::to_underlying(messages::ResultInterface::kMethod), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::ResultInterface_Method_Params_Data> params(
           message);
@@ -462,7 +463,7 @@ base::expected<
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::ResultInterface::kSyncMethod), kFlags, estimated_payload_size);
+      base::to_underlying(messages::ResultInterface::kSyncMethod), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::ResultInterface_SyncMethod_Params_Data> params(
           message);
@@ -520,7 +521,7 @@ void ResultInterfaceProxy::SyncMethod(
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::ResultInterface::kSyncMethod), kFlags, estimated_payload_size);
+      base::to_underlying(messages::ResultInterface::kSyncMethod), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::ResultInterface_SyncMethod_Params_Data> params(
           message);
@@ -650,7 +651,7 @@ void ResultInterface_Method_ProxyToResponder::Run(
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::ResultInterface::kMethod), kFlags, estimated_payload_size);
+      base::to_underlying(messages::ResultInterface::kMethod), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::ResultInterface_Method_ResponseParams_Data> params(
           message);
@@ -800,7 +801,7 @@ void ResultInterface_SyncMethod_ProxyToResponder::Run(
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::ResultInterface::kSyncMethod), kFlags, estimated_payload_size);
+      base::to_underlying(messages::ResultInterface::kSyncMethod), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::ResultInterface_SyncMethod_ResponseParams_Data> params(
           message);
diff --git a/mojo/golden/generated/c++/typemap.test-mojom.cc.golden b/mojo/golden/generated/c++/typemap.test-mojom.cc.golden
index 4ab22004ad..8365260862 100644
--- a/mojo/golden/generated/c++/typemap.test-mojom.cc.golden
+++ b/mojo/golden/generated/c++/typemap.test-mojom.cc.golden
@@ -22,6 +22,7 @@
 #include "base/task/thread_pool/thread_pool_instance.h"
 #include "base/trace_event/trace_event.h"
 #include "base/trace_event/typed_macros.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "mojo/public/cpp/bindings/lib/default_construct_tag_internal.h"
 #include "mojo/public/cpp/bindings/lib/generated_code_util.h"
 #include "mojo/public/cpp/bindings/lib/message_internal.h"
@@ -197,7 +198,7 @@ void IFaceWithTypemapProxy::Echo(
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::IFaceWithTypemap::kEcho), kFlags, estimated_payload_size);
+      base::to_underlying(messages::IFaceWithTypemap::kEcho), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::IFaceWithTypemap_Echo_Params_Data> params(
           message);
@@ -329,7 +330,7 @@ void IFaceWithTypemap_Echo_ProxyToResponder::Run(
   const size_t estimated_payload_size =
     0;
   mojo::Message message(
-      std::to_underlying(messages::IFaceWithTypemap::kEcho), kFlags, estimated_payload_size);
+      base::to_underlying(messages::IFaceWithTypemap::kEcho), kFlags, estimated_payload_size);
   mojo::internal::MessageFragment<
       ::golden::internal::IFaceWithTypemap_Echo_ResponseParams_Data> params(
           message);
diff --git a/mojo/public/cpp/base/proto_wrapper.cc b/mojo/public/cpp/base/proto_wrapper.cc
index 73fb78c746..4e3065fbc6 100644
--- a/mojo/public/cpp/base/proto_wrapper.cc
+++ b/mojo/public/cpp/base/proto_wrapper.cc
@@ -7,6 +7,7 @@
 #include <limits>
 
 #include "base/check_op.h"
+#include "base/compiler_specific.h"
 #include "third_party/protobuf/src/google/protobuf/message_lite.h"
 
 namespace mojo_base {
@@ -56,7 +57,8 @@ bool ProtoWrapper::DeserializeToMessage(
   } else {
     // Make an in-process copy here as protobuf is not designed to
     // safely parse data that might be changing underneath it.
-    const std::vector<uint8_t> copy(std::from_range, *bytes_);
+    auto as_span = UNSAFE_TODO(base::span(bytes_->data(), bytes_->size()));
+    const std::vector<uint8_t> copy(as_span.begin(), as_span.end());
     return message.ParseFromArray(copy.data(), copy.size());
   }
 }
diff --git a/mojo/public/cpp/base/values_mojom_traits.cc b/mojo/public/cpp/base/values_mojom_traits.cc
index 39269572e1..30552a6693 100644
--- a/mojo/public/cpp/base/values_mojom_traits.cc
+++ b/mojo/public/cpp/base/values_mojom_traits.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/compiler_specific.h"
 #include "base/features.h"
 
 namespace mojo {
@@ -86,7 +87,10 @@ bool UnionTraits<mojo_base::mojom::ValueDataView, base::Value>::Read(
     case mojo_base::mojom::ValueDataView::Tag::kBinaryValue: {
       mojo::ArrayDataView<uint8_t> binary_data_view;
       data.GetBinaryValueDataView(&binary_data_view);
-      base::Value::BlobStorage blob_storage(std::from_range, binary_data_view);
+      const char* data_pointer =
+          reinterpret_cast<const char*>(binary_data_view.data());
+      base::Value::BlobStorage blob_storage(
+          data_pointer, UNSAFE_TODO(data_pointer + binary_data_view.size()));
       *value_out = base::Value(std::move(blob_storage));
       return true;
     }
diff --git a/mojo/public/cpp/bindings/lib/multiplex_router.cc b/mojo/public/cpp/bindings/lib/multiplex_router.cc
index 6086f9b360..001c4a3c65 100644
--- a/mojo/public/cpp/bindings/lib/multiplex_router.cc
+++ b/mojo/public/cpp/bindings/lib/multiplex_router.cc
@@ -6,12 +6,12 @@
 
 #include <stdint.h>
 
-#include <algorithm>
 #include <atomic>
 #include <utility>
 
 #include "base/auto_reset.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
@@ -1170,8 +1170,8 @@ bool MultiplexRouter::ProcessIncomingMessage(
   bool can_direct_call;
   if (message->has_flag(Message::kFlagIsSync)) {
     if (!message->has_flag(Message::kFlagIsResponse) &&
-        !std::ranges::contains(endpoint->client()->sync_method_ordinals(),
-                               message->name())) {
+        !base::Contains(endpoint->client()->sync_method_ordinals(),
+                        message->name())) {
       RaiseErrorInNonTestingMode();
       return true;
     }
diff --git a/mojo/public/cpp/bindings/remote_set.h b/mojo/public/cpp/bindings/remote_set.h
index 9fbe634c72..b3ca784cc4 100644
--- a/mojo/public/cpp/bindings/remote_set.h
+++ b/mojo/public/cpp/bindings/remote_set.h
@@ -10,6 +10,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
 #include "base/memory/scoped_refptr.h"
diff --git a/mojo/public/tools/bindings/generators/cpp_templates/interface_definition.tmpl b/mojo/public/tools/bindings/generators/cpp_templates/interface_definition.tmpl
index 85174a0176..4398196cb9 100644
--- a/mojo/public/tools/bindings/generators/cpp_templates/interface_definition.tmpl
+++ b/mojo/public/tools/bindings/generators/cpp_templates/interface_definition.tmpl
@@ -239,7 +239,7 @@ class {{class_name}}_{{method.name}}_ForwardToCallback
 {%-   for method in interface.methods %}
 {%-     if method.estimate_message_size %}
 {%-       set message_name =
-              "std::to_underlying(messages::%s::k%s)"|format(interface.name, method.name) %}
+              "base::to_underlying(messages::%s::k%s)"|format(interface.name, method.name) %}
   size_estimator_.EnablePredictiveAllocation({{message_name}});
 {%-     endif %}
 {%-   endfor %}
@@ -250,7 +250,7 @@ class {{class_name}}_{{method.name}}_ForwardToCallback
 
 {%- for method in interface.methods %}
 {%-   set message_name =
-          "std::to_underlying(messages::%s::k%s)"|format(interface.name, method.name) %}
+          "base::to_underlying(messages::%s::k%s)"|format(interface.name, method.name) %}
 {%-   set params_struct = method.param_struct %}
 {%-   set params_description =
           "%s.%s request"|format(interface.name, method.name) %}
@@ -371,7 +371,7 @@ void {{proxy_name}}::{{method.name}}(
 {%- for method in interface.methods -%}
 {%-   if method.response_parameters != None %}
 {%-     set message_name =
-            "std::to_underlying(messages::%s::k%s)"|format(interface.name, method.name) %}
+            "base::to_underlying(messages::%s::k%s)"|format(interface.name, method.name) %}
 {%-     set response_params_struct = method.response_param_struct %}
 {%-     set params_description =
             "%s.%s response"|format(interface.name, method.name) %}
@@ -646,7 +646,7 @@ static const mojo::internal::GenericValidationInfo k{{class_name}}ValidationInfo
 {%- elif interface.methods %}
 static const std::pair<uint32_t, mojo::internal::GenericValidationInfo> k{{class_name}}ValidationInfo[] = {
 {%-   for method in interface.methods %}
-    {std::to_underlying(messages::{{class_name}}::k{{method.name}}),
+    {base::to_underlying(messages::{{class_name}}::k{{method.name}}),
      { {{request_validator(class_name, method)}},
 {%-     if method.response_parameters != None %}
       &internal::{{class_name}}_{{method.name}}_ResponseParams_Data::Validate}},
diff --git a/mojo/public/tools/bindings/generators/cpp_templates/module.cc.tmpl b/mojo/public/tools/bindings/generators/cpp_templates/module.cc.tmpl
index 8e1854256b..6c8746347d 100644
--- a/mojo/public/tools/bindings/generators/cpp_templates/module.cc.tmpl
+++ b/mojo/public/tools/bindings/generators/cpp_templates/module.cc.tmpl
@@ -28,6 +28,7 @@
 #include "base/task/thread_pool/thread_pool_instance.h"
 #include "base/trace_event/trace_event.h"
 #include "base/trace_event/typed_macros.h"
+#include "base/types/cxx23_to_underlying.h"
 {%- endif %}
 #include "mojo/public/cpp/bindings/lib/default_construct_tag_internal.h"
 {%- if interfaces %}
diff --git a/third_party/blink/common/associated_interfaces/associated_interface_provider.cc b/third_party/blink/common/associated_interfaces/associated_interface_provider.cc
index 76438b5c0c..8f87c048d7 100644
--- a/third_party/blink/common/associated_interfaces/associated_interface_provider.cc
+++ b/third_party/blink/common/associated_interfaces/associated_interface_provider.cc
@@ -6,6 +6,7 @@
 
 #include <map>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/task/single_thread_task_runner.h"
 #include "mojo/public/cpp/bindings/associated_receiver.h"
@@ -36,7 +37,7 @@ class AssociatedInterfaceProvider::LocalProvider
   void ResetBinderForName(const std::string& name) { binders_.erase(name); }
 
   bool HasInterface(const std::string& name) const {
-    return binders_.contains(name);
+    return base::Contains(binders_, name);
   }
 
   void GetInterface(const std::string& name,
diff --git a/third_party/blink/common/custom_handlers/protocol_handler_utils.cc b/third_party/blink/common/custom_handlers/protocol_handler_utils.cc
index a9efd61028..022437569b 100644
--- a/third_party/blink/common/custom_handlers/protocol_handler_utils.cc
+++ b/third_party/blink/common/custom_handlers/protocol_handler_utils.cc
@@ -4,9 +4,9 @@
 
 #include "third_party/blink/public/common/custom_handlers/protocol_handler_utils.h"
 
-#include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/strings/string_split.h"
 #include "base/strings/string_util.h"
@@ -113,7 +113,7 @@ bool IsValidCustomHandlerScheme(std::string_view scheme,
       "webcal",  "wtai",   "xmpp"};
 
   std::string lower_scheme = base::ToLowerASCII(scheme);
-  if (std::ranges::contains(kProtocolSafelist, lower_scheme)) {
+  if (base::Contains(kProtocolSafelist, lower_scheme)) {
     return true;
   }
   if (lower_scheme == "ftp" || lower_scheme == "ftps" ||
diff --git a/third_party/blink/common/input/web_coalesced_input_event_mojom_traits.cc b/third_party/blink/common/input/web_coalesced_input_event_mojom_traits.cc
index d05d633e87..45e7a95da2 100644
--- a/third_party/blink/common/input/web_coalesced_input_event_mojom_traits.cc
+++ b/third_party/blink/common/input/web_coalesced_input_event_mojom_traits.cc
@@ -4,9 +4,9 @@
 
 #include "third_party/blink/public/common/input/web_coalesced_input_event_mojom_traits.h"
 
-#include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "base/time/time.h"
 #include "mojo/public/cpp/base/time_mojom_traits.h"
@@ -389,8 +389,8 @@ StructTraits<blink::mojom::EventDataView,
       static_cast<const blink::WebKeyboardEvent*>(event->EventPointer());
   // Assure std::array<char16_t, N> fields are nul-terminated before converting
   // them to std::u16string.
-  CHECK(std::ranges::contains(key_event->text, 0));
-  CHECK(std::ranges::contains(key_event->unmodified_text, 0));
+  CHECK(base::Contains(key_event->text, 0));
+  CHECK(base::Contains(key_event->unmodified_text, 0));
   return blink::mojom::KeyData::New(
       key_event->dom_key, key_event->dom_code, key_event->windows_key_code,
       key_event->native_key_code, key_event->is_system_key,
diff --git a/third_party/blink/common/interest_group/auction_config_mojom_traits.cc b/third_party/blink/common/interest_group/auction_config_mojom_traits.cc
index 243c605ae4..791bde7f68 100644
--- a/third_party/blink/common/interest_group/auction_config_mojom_traits.cc
+++ b/third_party/blink/common/interest_group/auction_config_mojom_traits.cc
@@ -8,6 +8,7 @@
 #include <optional>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/flat_set.h"
 #include "base/feature_list.h"
@@ -326,7 +327,8 @@ bool StructTraits<blink::mojom::AuctionAdConfigNonSharedParamsDataView,
     }
 
     // If `all_slots_requested_sizes` is set, `requested_size` must be in it.
-    if (out->requested_size && !ad_sizes.contains(*out->requested_size)) {
+    if (out->requested_size &&
+        !base::Contains(ad_sizes, *out->requested_size)) {
       return false;
     }
   }
diff --git a/third_party/blink/common/interest_group/interest_group.cc b/third_party/blink/common/interest_group/interest_group.cc
index fae1ccc295..e30062f423 100644
--- a/third_party/blink/common/interest_group/interest_group.cc
+++ b/third_party/blink/common/interest_group/interest_group.cc
@@ -6,7 +6,6 @@
 
 #include <stdint.h>
 
-#include <algorithm>
 #include <cmath>
 #include <cstring>
 #include <optional>
@@ -15,6 +14,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/numerics/byte_conversions.h"
@@ -628,7 +628,7 @@ std::string DEPRECATED_KAnonKeyForAdBid(
     const InterestGroup& group,
     const std::string& ad_url_from_gurl_spec) {
   DCHECK(group.ads);
-  DCHECK(std::ranges::contains(
+  DCHECK(base::Contains(
       *group.ads, ad_url_from_gurl_spec,
       [](const blink::InterestGroup::Ad& ad) { return ad.render_url(); }))
       << "No such ad: " << ad_url_from_gurl_spec;
@@ -683,8 +683,7 @@ std::string DEPRECATED_KAnonKeyForAdNameReporting(
     base::optional_ref<const std::string>
         selected_buyer_and_seller_reporting_id) {
   DCHECK(group.ads);
-  DCHECK(std::ranges::contains(*group.ads, ad))
-      << "No such ad: " << ad.render_url();
+  DCHECK(base::Contains(*group.ads, ad)) << "No such ad: " << ad.render_url();
   DCHECK(group.bidding_url);
   return InternalPlainTextKAnonKeyForAdNameReporting(
       group.owner, group.name, group.bidding_url.value_or(GURL()),
diff --git a/third_party/blink/common/loader/throttling_url_loader.cc b/third_party/blink/common/loader/throttling_url_loader.cc
index c00d392c37..6d3b45899c 100644
--- a/third_party/blink/common/loader/throttling_url_loader.cc
+++ b/third_party/blink/common/loader/throttling_url_loader.cc
@@ -4,10 +4,10 @@
 
 #include "third_party/blink/public/common/loader/throttling_url_loader.h"
 
-#include <algorithm>
 #include <string_view>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_functions.h"
@@ -43,7 +43,7 @@ void RemoveModifiedHeadersBeforeMerge(
 void MergeRemovedHeaders(std::vector<std::string>* removed_headers_A,
                          const std::vector<std::string>& removed_headers_B) {
   for (auto& header : removed_headers_B) {
-    if (!std::ranges::contains(*removed_headers_A, header))
+    if (!base::Contains(*removed_headers_A, header))
       removed_headers_A->emplace_back(std::move(header));
   }
 }
diff --git a/third_party/blink/common/manifest/manifest_icon_selector.cc b/third_party/blink/common/manifest/manifest_icon_selector.cc
index ee917e9c9c..e8137e3d1a 100644
--- a/third_party/blink/common/manifest/manifest_icon_selector.cc
+++ b/third_party/blink/common/manifest/manifest_icon_selector.cc
@@ -8,6 +8,7 @@
 #include <limits>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
 #include "net/base/mime_util.h"
@@ -71,7 +72,7 @@ ManifestIconSelector::FindBestMatchingIcon(
   int best_delta = std::numeric_limits<int>::min();
 
   for (const auto& icon : icons) {
-    if (!std::ranges::contains(icon.purpose, params.purpose) ||
+    if (!base::Contains(icon.purpose, params.purpose) ||
         !IsIconTypeSupported(icon,
                              params.limited_image_types_for_installable_icon)) {
       continue;
diff --git a/third_party/blink/common/mime_util/mime_util.cc b/third_party/blink/common/mime_util/mime_util.cc
index 746d63828d..7319fba348 100644
--- a/third_party/blink/common/mime_util/mime_util.cc
+++ b/third_party/blink/common/mime_util/mime_util.cc
@@ -9,6 +9,7 @@
 #include <string_view>
 #include <unordered_set>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/feature_list.h"
 #include "base/strings/string_util.h"
diff --git a/third_party/blink/common/origin_trials/manual_completion_origin_trial_features.cc b/third_party/blink/common/origin_trials/manual_completion_origin_trial_features.cc
index 7926f34a8f..695d42137f 100644
--- a/third_party/blink/common/origin_trials/manual_completion_origin_trial_features.cc
+++ b/third_party/blink/common/origin_trials/manual_completion_origin_trial_features.cc
@@ -7,8 +7,7 @@
 // changes to it require review from the origin trials team, listed in the
 // OWNERS file.
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "third_party/blink/public/common/origin_trials/origin_trials.h"
 #include "third_party/blink/public/mojom/origin_trials/origin_trial_feature.mojom-shared.h"
 
@@ -26,7 +25,7 @@ bool FeatureHasExpiryGracePeriod(blink::mojom::OriginTrialFeature feature) {
       // Production grace period trials start here:
       blink::mojom::OriginTrialFeature::kRTCEncodedFrameSetMetadata,
   };
-  return std::ranges::contains(kHasExpiryGracePeriod, feature);
+  return base::Contains(kHasExpiryGracePeriod, feature);
 }
 
 }  // namespace blink::origin_trials
diff --git a/third_party/blink/common/origin_trials/navigation_origin_trial_features.cc b/third_party/blink/common/origin_trials/navigation_origin_trial_features.cc
index 0f30938ebd..32e533bbf6 100644
--- a/third_party/blink/common/origin_trials/navigation_origin_trial_features.cc
+++ b/third_party/blink/common/origin_trials/navigation_origin_trial_features.cc
@@ -7,8 +7,7 @@
 // changes to it require review from security reviewers, listed in the
 // SECURITY_OWNERS file.
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "third_party/blink/public/common/origin_trials/origin_trials.h"
 #include "third_party/blink/public/mojom/origin_trials/origin_trial_feature.mojom-shared.h"
 
@@ -23,7 +22,7 @@ bool FeatureEnabledForNavigation(blink::mojom::OriginTrialFeature feature) {
       blink::mojom::OriginTrialFeature::kOriginTrialsSampleAPINavigation,
       blink::mojom::OriginTrialFeature::kTextFragmentIdentifiers,
   };
-  return std::ranges::contains(kEnabledForNavigation, feature);
+  return base::Contains(kEnabledForNavigation, feature);
 }
 
 }  // namespace origin_trials
diff --git a/third_party/blink/common/origin_trials/persistent_origin_trials.cc b/third_party/blink/common/origin_trials/persistent_origin_trials.cc
index 3f0a402b04..c37dacfe2f 100644
--- a/third_party/blink/common/origin_trials/persistent_origin_trials.cc
+++ b/third_party/blink/common/origin_trials/persistent_origin_trials.cc
@@ -7,9 +7,9 @@
 // since changes to it require review from security reviewers, listed in the
 // SECURITY_OWNERS file.
 
-#include <algorithm>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "third_party/blink/public/common/origin_trials/origin_trials.h"
 
 namespace blink::origin_trials {
@@ -25,7 +25,7 @@ bool IsTrialPersistentToNextResponse(std::string_view trial_name) {
       // Production persistent origin trials follow below:
       "DisableReduceAcceptLanguage",
   };
-  return std::ranges::contains(kPersistentTrials, trial_name);
+  return base::Contains(kPersistentTrials, trial_name);
 }
 
 }  // namespace blink::origin_trials
diff --git a/third_party/blink/common/scheme_registry.cc b/third_party/blink/common/scheme_registry.cc
index 8708f3966b..250c46f7f5 100644
--- a/third_party/blink/common/scheme_registry.cc
+++ b/third_party/blink/common/scheme_registry.cc
@@ -6,6 +6,7 @@
 
 #include <unordered_set>
 
+#include "base/containers/contains.h"
 #include "base/no_destructor.h"
 #include "base/strings/string_util.h"
 
@@ -53,7 +54,7 @@ bool CommonSchemeRegistry::IsExtensionScheme(const std::string& scheme) {
     return false;
   }
   DCHECK_EQ(scheme, base::ToLowerASCII(scheme));
-  return GetExtensionSchemes().contains(scheme);
+  return base::Contains(GetExtensionSchemes(), scheme);
 }
 
 bool CommonSchemeRegistry::IsIsolatedAppScheme(const std::string& scheme) {
@@ -61,7 +62,7 @@ bool CommonSchemeRegistry::IsIsolatedAppScheme(const std::string& scheme) {
     return false;
   }
   DCHECK_EQ(scheme, base::ToLowerASCII(scheme));
-  return GetIsolatedAppSchemes().contains(scheme);
+  return base::Contains(GetIsolatedAppSchemes(), scheme);
 }
 
 }  // namespace blink
diff --git a/third_party/blink/common/service_worker/service_worker_scope_match.cc b/third_party/blink/common/service_worker/service_worker_scope_match.cc
index 4870cbfb19..f0d2993ef2 100644
--- a/third_party/blink/common/service_worker/service_worker_scope_match.cc
+++ b/third_party/blink/common/service_worker/service_worker_scope_match.cc
@@ -4,6 +4,7 @@
 
 #include "third_party/blink/public/common/service_worker/service_worker_scope_match.h"
 
+#include "base/containers/contains.h"
 #include "base/strings/string_util.h"
 
 namespace blink {
@@ -16,10 +17,10 @@ bool PathContainsDisallowedCharacter(const GURL& url) {
 
   // We should avoid these escaped characters in the path component because
   // these can be handled differently depending on server implementation.
-  if (path.contains("%2f") || path.contains("%2F")) {
+  if (base::Contains(path, "%2f") || base::Contains(path, "%2F")) {
     return true;
   }
-  if (path.contains("%5c") || path.contains("%5C")) {
+  if (base::Contains(path, "%5c") || base::Contains(path, "%5C")) {
     return true;
   }
   return false;
diff --git a/third_party/blink/common/user_agent/user_agent_metadata.cc b/third_party/blink/common/user_agent/user_agent_metadata.cc
index 4eab85a17e..3cf785cdbd 100644
--- a/third_party/blink/common/user_agent/user_agent_metadata.cc
+++ b/third_party/blink/common/user_agent/user_agent_metadata.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/pickle.h"
 #include "net/http/structured_headers.h"
@@ -196,7 +197,7 @@ std::optional<UserAgentMetadata> UserAgentMetadata::Demarshal(
 
 // static
 bool UserAgentMetadata::IsValidFormFactor(std::string_view form_factor) {
-  return std::ranges::contains(kValidFormFactors, form_factor);
+  return base::Contains(kValidFormFactors, form_factor);
 }
 
 bool UserAgentBrandVersion::operator==(const UserAgentBrandVersion& a) const {
diff --git a/third_party/blink/common/web_package/web_package_request_matcher.cc b/third_party/blink/common/web_package/web_package_request_matcher.cc
index 6d4b81715e..32d269b6db 100644
--- a/third_party/blink/common/web_package/web_package_request_matcher.cc
+++ b/third_party/blink/common/web_package/web_package_request_matcher.cc
@@ -11,6 +11,7 @@
 #include <string_view>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/numerics/checked_math.h"
 #include "base/strings/strcat.h"
@@ -162,8 +163,7 @@ class AcceptEncodingNegotiation final : public ContentNegotiationAlgorithm {
 
     // Step 3. If "identity" is not a member of preferred-codings, append
     // "identity". [spec text]
-    if (!std::ranges::contains(preferred_codings, kIdentity,
-                               &WeightedValue::value)) {
+    if (!base::Contains(preferred_codings, kIdentity, &WeightedValue::value)) {
       preferred_codings.push_back({kIdentity, 0.0});
     }
 
@@ -551,7 +551,7 @@ bool WebPackageRequestMatcher::MatchRequest(
     DCHECK_EQ(vk.size(), sorted_variants.size());
     size_t i = 0;
     for (; i < sorted_variants.size(); ++i) {
-      if (!std::ranges::contains(sorted_variants[i], vk[i]))
+      if (!base::Contains(sorted_variants[i], vk[i]))
         break;
     }
     if (i == sorted_variants.size())
diff --git a/third_party/blink/renderer/DEPS b/third_party/blink/renderer/DEPS
index 13af4ddcae..6633672f05 100644
--- a/third_party/blink/renderer/DEPS
+++ b/third_party/blink/renderer/DEPS
@@ -12,6 +12,7 @@ include_rules = [
     "+base/containers/adapters.h",
     "+base/containers/buffer_iterator.h",
     "+base/containers/checked_iterators.h",
+    "+base/containers/contains.h",
     "+base/containers/cxx20_erase.h",
     "+base/containers/enum_set.h",
     "+base/containers/fixed_flat_map.h",
@@ -82,6 +83,7 @@ include_rules = [
     "+base/timer/elapsed_timer.h",
     "+base/timer/timer.h",
     "+base/token.h",
+    "+base/types/cxx23_to_underlying.h",
     "+base/types/expected.h",
     "+base/types/expected_macros.h",
     "+base/types/optional_ref.h",
diff --git a/third_party/blink/renderer/bindings/core/v8/serialization/trailer_reader.cc b/third_party/blink/renderer/bindings/core/v8/serialization/trailer_reader.cc
index 81a6ef3c69..4b46c93947 100644
--- a/third_party/blink/renderer/bindings/core/v8/serialization/trailer_reader.cc
+++ b/third_party/blink/renderer/bindings/core/v8/serialization/trailer_reader.cc
@@ -5,7 +5,6 @@
 #include "third_party/blink/renderer/bindings/core/v8/serialization/trailer_reader.h"
 
 #include <algorithm>
-#include <bit>
 
 #include "base/numerics/byte_conversions.h"
 #include "base/numerics/clamped_math.h"
@@ -105,11 +104,10 @@ base::expected<void, TrailerReader::Error> TrailerReader::Read() {
       return base::unexpected(Error::kInvalidTrailer);
 
     uint32_t num_exposed = 0;
-    if (auto num_exposed_raw = iterator_.CopyObject<uint32_t>()) {
-      num_exposed = std::byteswap(*num_exposed_raw);  // Big-endian.
-    } else {
+    if (auto num_exposed_raw = iterator_.CopyObject<uint32_t>())
+      num_exposed = base::ByteSwap(*num_exposed_raw);  // Big-endian.
+    else
       return base::unexpected(Error::kInvalidTrailer);
-    }
 
     auto exposed_raw = iterator_.Span<uint8_t>(num_exposed);
     if (exposed_raw.size() != num_exposed)
diff --git a/third_party/blink/renderer/build/scripts/templates/origin_trials.cc.tmpl b/third_party/blink/renderer/build/scripts/templates/origin_trials.cc.tmpl
index 6771760f04..472ebba603 100644
--- a/third_party/blink/renderer/build/scripts/templates/origin_trials.cc.tmpl
+++ b/third_party/blink/renderer/build/scripts/templates/origin_trials.cc.tmpl
@@ -10,6 +10,7 @@
 #include <iterator>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "build/build_config.h"
 #include "build/buildflag.h"
 #include "third_party/blink/public/mojom/origin_trials/origin_trial_feature.mojom-shared.h"
@@ -37,8 +38,8 @@ static constexpr struct TrialToFeature {
 } // namespace
 
 bool origin_trials::IsTrialValid(std::string_view trial_name) {
-  return std::ranges::contains(kTrialToFeaturesMap, trial_name,
-                               &TrialToFeature::trial_name);
+  return base::Contains(kTrialToFeaturesMap, trial_name,
+                        &TrialToFeature::trial_name);
 }
 
 bool origin_trials::IsTrialEnabledForInsecureContext(std::string_view trial_name) {
@@ -47,7 +48,7 @@ bool origin_trials::IsTrialEnabledForInsecureContext(std::string_view trial_name
       "{{trial}}",
   {% endfor %}
   };
-  return std::ranges::contains(kEnabledForInsecureContext, trial_name);
+  return base::Contains(kEnabledForInsecureContext, trial_name);
 }
 
 bool origin_trials::IsTrialEnabledForThirdPartyOrigins(std::string_view trial_name) {
@@ -56,7 +57,7 @@ bool origin_trials::IsTrialEnabledForThirdPartyOrigins(std::string_view trial_na
       "{{trial}}",
   {% endfor %}
   };
-  return std::ranges::contains(kEnabledForThirdPartyOrigins, trial_name);
+  return base::Contains(kEnabledForThirdPartyOrigins, trial_name);
 }
 
 bool origin_trials::IsTrialEnabledForBrowserProcessReadAccess(std::string_view trial_name) {
@@ -70,7 +71,7 @@ bool origin_trials::IsTrialEnabledForBrowserProcessReadAccess(std::string_view t
     {% endif %}
   {% endfor %}
   };
-  return std::ranges::contains(kEnabledForBrowserProcessReadWriteAccess, trial_name);
+  return base::Contains(kEnabledForBrowserProcessReadWriteAccess, trial_name);
 }
 
 bool origin_trials::IsDeprecationTrial(std::string_view trial_name) {
diff --git a/third_party/blink/renderer/core/css/css_image_generator_value.cc b/third_party/blink/renderer/core/css/css_image_generator_value.cc
index ebc1288650..fa8d3a9e83 100644
--- a/third_party/blink/renderer/core/css/css_image_generator_value.cc
+++ b/third_party/blink/renderer/core/css/css_image_generator_value.cc
@@ -25,6 +25,7 @@
 
 #include "third_party/blink/renderer/core/css/css_image_generator_value.h"
 
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/core/css/css_gradient_value.h"
 #include "third_party/blink/renderer/core/css/css_paint_value.h"
 #include "third_party/blink/renderer/core/loader/resource/image_resource_observer.h"
diff --git a/third_party/blink/renderer/core/css/cssom/computed_style_property_map.cc b/third_party/blink/renderer/core/css/cssom/computed_style_property_map.cc
index e8a4273f4d..65459a4714 100644
--- a/third_party/blink/renderer/core/css/cssom/computed_style_property_map.cc
+++ b/third_party/blink/renderer/core/css/cssom/computed_style_property_map.cc
@@ -4,8 +4,6 @@
 
 #include "third_party/blink/renderer/core/css/cssom/computed_style_property_map.h"
 
-#include <algorithm>
-
 #include "third_party/blink/renderer/core/css/computed_style_css_value_mapping.h"
 #include "third_party/blink/renderer/core/css/css_function_value.h"
 #include "third_party/blink/renderer/core/css/css_identifier_value.h"
@@ -111,7 +109,7 @@ const CSSValue* ComputedStylePropertyMap::GetProperty(
       CSSPropertyID::kColumnRuleWidth, CSSPropertyID::kOutlineWidth,
   };
 
-  if (std::ranges::contains(kWidthProperties, property_id)) {
+  if (base::Contains(kWidthProperties, property_id)) {
     RecordUseCounterForWidthStyleValues(property_id, *style);
   }
 
diff --git a/third_party/blink/renderer/core/css/parser/css_variable_parser.cc b/third_party/blink/renderer/core/css/parser/css_variable_parser.cc
index 3b23a6ad90..f44b82fa73 100644
--- a/third_party/blink/renderer/core/css/parser/css_variable_parser.cc
+++ b/third_party/blink/renderer/core/css/parser/css_variable_parser.cc
@@ -837,7 +837,7 @@ StringView CSSVariableParser::StripTrailingWhitespaceAndComments(
   // (i.e. not CSSOM, where we just get a string), we know we can't
   // have unfinished comments, so consider piping that knowledge all
   // the way through here.
-  if (text.Is8Bit() && !std::ranges::contains(text.Span8(), '/')) {
+  if (text.Is8Bit() && !base::Contains(text.Span8(), '/')) {
     // No comments, so we can strip whitespace only.
     // SAFETY: empty strings short-circuited in &&-expression means that
     // the last character is valid.
diff --git a/third_party/blink/renderer/core/css/rule_set.cc b/third_party/blink/renderer/core/css/rule_set.cc
index 8e16190f79..3cc799d17f 100644
--- a/third_party/blink/renderer/core/css/rule_set.cc
+++ b/third_party/blink/renderer/core/css/rule_set.cc
@@ -35,6 +35,7 @@
 #include <vector>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/substring_set_matcher/substring_set_matcher.h"
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/renderer/core/css/css_font_selector.h"
diff --git a/third_party/blink/renderer/core/css/selector_checker.cc b/third_party/blink/renderer/core/css/selector_checker.cc
index a56679924f..16c9ac45e8 100644
--- a/third_party/blink/renderer/core/css/selector_checker.cc
+++ b/third_party/blink/renderer/core/css/selector_checker.cc
@@ -29,8 +29,6 @@
 
 #include "third_party/blink/renderer/core/css/selector_checker.h"
 
-#include <algorithm>
-
 #include "base/auto_reset.h"
 #include "base/compiler_specific.h"
 #include "third_party/blink/public/mojom/input/focus_type.mojom-blink.h"
@@ -3359,8 +3357,8 @@ bool SelectorChecker::CheckPseudoElement(const SelectorCheckingContext& context,
       // contained in the pseudo-element's classes (pseudo_ident_list).
       return std::ranges::all_of(base::span(selector.IdentList()).subspan(1ul),
                                  [&](const AtomicString& class_from_selector) {
-                                   return std::ranges::contains(
-                                       pseudo_ident_list, class_from_selector);
+                                   return base::Contains(pseudo_ident_list,
+                                                         class_from_selector);
                                  });
     }
     case CSSSelector::kPseudoScrollbarButton:
diff --git a/third_party/blink/renderer/core/css/style_environment_variables.cc b/third_party/blink/renderer/core/css/style_environment_variables.cc
index 0012bb75f8..981ad7924b 100644
--- a/third_party/blink/renderer/core/css/style_environment_variables.cc
+++ b/third_party/blink/renderer/core/css/style_environment_variables.cc
@@ -4,6 +4,7 @@
 
 #include "third_party/blink/renderer/core/css/style_environment_variables.h"
 
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/core/css/parser/css_tokenizer.h"
 #include "third_party/blink/renderer/platform/runtime_enabled_features.h"
 namespace blink {
diff --git a/third_party/blink/renderer/core/dom/focusgroup_flags.h b/third_party/blink/renderer/core/dom/focusgroup_flags.h
index 8540e862c0..df0209ad0e 100644
--- a/third_party/blink/renderer/core/dom/focusgroup_flags.h
+++ b/third_party/blink/renderer/core/dom/focusgroup_flags.h
@@ -6,8 +6,8 @@
 #define THIRD_PARTY_BLINK_RENDERER_CORE_DOM_FOCUSGROUP_FLAGS_H_
 
 #include <iosfwd>
-#include <utility>
 
+#include "base/types/cxx23_to_underlying.h"
 #include "third_party/blink/renderer/core/core_export.h"
 #include "third_party/blink/renderer/platform/wtf/text/atomic_string.h"
 #include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"
@@ -58,14 +58,14 @@ enum FocusgroupFlags : uint8_t {
 
 inline constexpr FocusgroupFlags operator&(FocusgroupFlags a,
                                            FocusgroupFlags b) {
-  return static_cast<FocusgroupFlags>(std::to_underlying(a) &
-                                      std::to_underlying(b));
+  return static_cast<FocusgroupFlags>(base::to_underlying(a) &
+                                      base::to_underlying(b));
 }
 
 inline constexpr FocusgroupFlags operator|(FocusgroupFlags a,
                                            FocusgroupFlags b) {
-  return static_cast<FocusgroupFlags>(std::to_underlying(a) |
-                                      std::to_underlying(b));
+  return static_cast<FocusgroupFlags>(base::to_underlying(a) |
+                                      base::to_underlying(b));
 }
 
 inline FocusgroupFlags& operator|=(FocusgroupFlags& a, FocusgroupFlags b) {
@@ -77,7 +77,7 @@ inline FocusgroupFlags& operator&=(FocusgroupFlags& a, FocusgroupFlags b) {
 }
 
 inline constexpr FocusgroupFlags operator~(FocusgroupFlags flags) {
-  return static_cast<FocusgroupFlags>(~std::to_underlying(flags));
+  return static_cast<FocusgroupFlags>(~base::to_underlying(flags));
 }
 
 struct FocusgroupData {
diff --git a/third_party/blink/renderer/core/dom/live_node_list_registry.cc b/third_party/blink/renderer/core/dom/live_node_list_registry.cc
index 0637c9b69b..7e94703006 100644
--- a/third_party/blink/renderer/core/dom/live_node_list_registry.cc
+++ b/third_party/blink/renderer/core/dom/live_node_list_registry.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/core/dom/document.h"
 #include "third_party/blink/renderer/core/dom/live_node_list_base.h"
 
@@ -17,7 +18,7 @@ static_assert(kNumNodeListInvalidationTypes <= sizeof(unsigned) * 8,
 void LiveNodeListRegistry::Add(const LiveNodeListBase* list,
                                NodeListInvalidationType type) {
   Entry entry = {list, MaskForInvalidationType(type)};
-  DCHECK(!std::ranges::contains(data_, entry));
+  DCHECK(!base::Contains(data_, entry));
   data_.push_back(entry);
   mask_ |= entry.second;
 }
diff --git a/third_party/blink/renderer/core/dom/subscriber.cc b/third_party/blink/renderer/core/dom/subscriber.cc
index 5c3082018e..ffb17fec08 100644
--- a/third_party/blink/renderer/core/dom/subscriber.cc
+++ b/third_party/blink/renderer/core/dom/subscriber.cc
@@ -4,9 +4,8 @@
 
 #include "third_party/blink/renderer/core/dom/subscriber.h"
 
-#include <algorithm>
-
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "third_party/blink/public/mojom/use_counter/metrics/web_feature.mojom-blink.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_observer.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_observer_callback.h"
@@ -200,7 +199,7 @@ void Subscriber::ConsumerUnsubscribe(
 
   // Also remove `associated_observer` from `internal_observers_`, since it no
   // longer cares about values `this` produces.
-  DCHECK(std::ranges::contains(internal_observers_, associated_observer));
+  DCHECK(base::Contains(internal_observers_, associated_observer));
   internal_observers_.erase(
       std::ranges::find(internal_observers_, associated_observer));
 
diff --git a/third_party/blink/renderer/core/editing/ime/edit_context.cc b/third_party/blink/renderer/core/editing/ime/edit_context.cc
index 1b8e05aa3c..fc85ebead0 100644
--- a/third_party/blink/renderer/core/editing/ime/edit_context.cc
+++ b/third_party/blink/renderer/core/editing/ime/edit_context.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/trace_event/trace_event.h"
 #include "third_party/blink/public/platform/web_string.h"
 #include "third_party/blink/public/web/web_local_frame_client.h"
@@ -823,8 +824,8 @@ void EditContext::SetSelection(int start,
 }
 
 void EditContext::AttachElement(HTMLElement* element_to_attach) {
-  if (std::ranges::contains(attached_elements_, element_to_attach,
-                            &Member<HTMLElement>::Get)) {
+  if (base::Contains(attached_elements_, element_to_attach,
+                     &Member<HTMLElement>::Get)) {
     return;
   }
 
diff --git a/third_party/blink/renderer/core/events/event_util.cc b/third_party/blink/renderer/core/events/event_util.cc
index b5ec02ffdf..3a76eb5cd8 100644
--- a/third_party/blink/renderer/core/events/event_util.cc
+++ b/third_party/blink/renderer/core/events/event_util.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/core/events/event_util.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/core/event_type_names.h"
 #include "third_party/blink/renderer/core/frame/web_feature.h"
 #include "third_party/blink/renderer/platform/wtf/std_lib_extras.h"
@@ -24,7 +23,7 @@ const Vector<AtomicString>& MouseButtonEventTypes() {
 }
 
 bool IsMouseButtonEventType(const AtomicString& event_type) {
-  return std::ranges::contains(MouseButtonEventTypes(), event_type);
+  return base::Contains(MouseButtonEventTypes(), event_type);
 }
 
 bool IsPointerEventType(const AtomicString& event_type) {
diff --git a/third_party/blink/renderer/core/execution_context/security_context_init.cc b/third_party/blink/renderer/core/execution_context/security_context_init.cc
index 949fa87f5c..a86be33c27 100644
--- a/third_party/blink/renderer/core/execution_context/security_context_init.cc
+++ b/third_party/blink/renderer/core/execution_context/security_context_init.cc
@@ -4,7 +4,6 @@
 
 #include "third_party/blink/renderer/core/execution_context/security_context_init.h"
 
-#include <algorithm>
 #include <optional>
 
 #include "base/metrics/histogram_macros.h"
@@ -237,8 +236,8 @@ void SecurityContextInit::ApplyPermissionsPolicy(
 
         // Warn if a disallowed permissions policy is attempted to be enabled.
         for (const auto& policy : container_policy) {
-          if (!std::ranges::contains(network::kFencedFrameAllowedFeatures,
-                                     policy.feature)) {
+          if (!base::Contains(network::kFencedFrameAllowedFeatures,
+                              policy.feature)) {
             bool is_isolated_context =
                 execution_context_ && execution_context_->IsIsolatedContext();
             execution_context_->AddConsoleMessage(
diff --git a/third_party/blink/renderer/core/exported/web_language_detection_details.cc b/third_party/blink/renderer/core/exported/web_language_detection_details.cc
index c77667f391..d0e491c23a 100644
--- a/third_party/blink/renderer/core/exported/web_language_detection_details.cc
+++ b/third_party/blink/renderer/core/exported/web_language_detection_details.cc
@@ -4,8 +4,6 @@
 
 #include "third_party/blink/public/web/web_language_detection_details.h"
 
-#include <algorithm>
-
 #include "base/metrics/histogram_functions.h"
 #include "third_party/blink/public/common/metrics/accept_language_and_content_language_usage.h"
 #include "third_party/blink/public/web/web_document.h"
@@ -106,8 +104,8 @@ void MatchTargetLanguageWithAcceptLanguages(
                           kXmlLangMatchesPrimaryAcceptLanguage
                     : AcceptLanguageAndXmlHtmlLangUsage::
                           kHtmlLangMatchesPrimaryAcceptLanguage);
-  } else if (std::ranges::contains(accept_languages, target_language,
-                                   &GetLanguageCode)) {
+  } else if (base::Contains(accept_languages, target_language,
+                            &GetLanguageCode)) {
     base::UmaHistogramEnumeration(
         language_histogram_name,
         is_xml_lang ? AcceptLanguageAndXmlHtmlLangUsage::
diff --git a/third_party/blink/renderer/core/frame/csp/content_security_policy.cc b/third_party/blink/renderer/core/frame/csp/content_security_policy.cc
index 0ddb753a6f..f7cacc4d80 100644
--- a/third_party/blink/renderer/core/frame/csp/content_security_policy.cc
+++ b/third_party/blink/renderer/core/frame/csp/content_security_policy.cc
@@ -31,6 +31,7 @@
 #include <set>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram_functions.h"
@@ -198,7 +199,7 @@ String ContentSecurityPolicy::StripURLForUseInReport(
   // > 1. If url's scheme is not "`https`", "'http'", "`wss`" or "`ws`" then
   // >    return url's scheme.
   static const char* const allow_list[] = {"http", "https", "ws", "wss"};
-  if (!std::ranges::contains(allow_list, url.Protocol())) {
+  if (!base::Contains(allow_list, url.Protocol())) {
     return url.Protocol();
   }
 
diff --git a/third_party/blink/renderer/core/frame/integrity_policy.cc b/third_party/blink/renderer/core/frame/integrity_policy.cc
index 6e5af5ec6b..7104c57be1 100644
--- a/third_party/blink/renderer/core/frame/integrity_policy.cc
+++ b/third_party/blink/renderer/core/frame/integrity_policy.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/core/frame/integrity_policy.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "services/network/public/cpp/integrity_policy.h"
 #include "services/network/public/cpp/request_destination.h"
 #include "services/network/public/mojom/integrity_algorithm.mojom-blink.h"
@@ -53,10 +52,10 @@ void SendReport(ExecutionContext* context,
 }
 
 bool ShouldBlockOrReport(const network::IntegrityPolicy& integrity_policy) {
-  return (std::ranges::contains(
+  return (base::Contains(
               integrity_policy.blocked_destinations,
               ::network::mojom::blink::IntegrityPolicy::Destination::kScript) &&
-          std::ranges::contains(
+          base::Contains(
               integrity_policy.sources,
               ::network::mojom::blink::IntegrityPolicy::Source::kInline));
 }
diff --git a/third_party/blink/renderer/core/frame/local_dom_window.cc b/third_party/blink/renderer/core/frame/local_dom_window.cc
index 51f03729c2..1a27327b61 100644
--- a/third_party/blink/renderer/core/frame/local_dom_window.cc
+++ b/third_party/blink/renderer/core/frame/local_dom_window.cc
@@ -31,6 +31,7 @@
 #include <utility>
 
 #include "base/command_line.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/trace_event/trace_id_helper.h"
diff --git a/third_party/blink/renderer/core/html/client_hints_util.cc b/third_party/blink/renderer/core/html/client_hints_util.cc
index 1f1b53f28a..a90ebeeed0 100644
--- a/third_party/blink/renderer/core/html/client_hints_util.cc
+++ b/third_party/blink/renderer/core/html/client_hints_util.cc
@@ -4,6 +4,7 @@
 
 #include "third_party/blink/renderer/core/html/client_hints_util.h"
 
+#include "base/containers/contains.h"
 #include "services/network/public/cpp/client_hints.h"
 #include "services/network/public/cpp/permissions_policy/client_hints_permissions_policy_mapping.h"
 #include "services/network/public/cpp/permissions_policy/origin_with_possible_wildcards.h"
@@ -108,7 +109,8 @@ void UpdateIFrameContainerPolicyWithDelegationSupportForClientHints(
            network::ParsedPermissionsPolicyDeclaration>
       feature_to_container_policy;
   for (const auto& candidate_policy : container_policy) {
-    if (!feature_to_container_policy.contains(candidate_policy.feature)) {
+    if (!base::Contains(feature_to_container_policy,
+                        candidate_policy.feature)) {
       feature_to_container_policy[candidate_policy.feature] = candidate_policy;
     }
   }
diff --git a/third_party/blink/renderer/core/html/forms/html_button_element.cc b/third_party/blink/renderer/core/html/forms/html_button_element.cc
index ba5a442ddc..4feba74e60 100644
--- a/third_party/blink/renderer/core/html/forms/html_button_element.cc
+++ b/third_party/blink/renderer/core/html/forms/html_button_element.cc
@@ -25,8 +25,6 @@
 
 #include "third_party/blink/renderer/core/html/forms/html_button_element.h"
 
-#include <utility>
-
 #include "third_party/blink/renderer/core/dom/attribute.h"
 #include "third_party/blink/renderer/core/dom/element.h"
 #include "third_party/blink/renderer/core/dom/events/event.h"
@@ -80,7 +78,7 @@ void HTMLButtonElement::AdjustStyle(ComputedStyleBuilder& builder) {
 }
 
 FormControlType HTMLButtonElement::FormControlType() const {
-  return static_cast<mojom::blink::FormControlType>(std::to_underlying(type_));
+  return static_cast<mojom::blink::FormControlType>(base::to_underlying(type_));
 }
 
 const AtomicString& HTMLButtonElement::FormControlTypeAsString() const {
diff --git a/third_party/blink/renderer/core/html/forms/html_button_element.h b/third_party/blink/renderer/core/html/forms/html_button_element.h
index 038652d402..bad110497d 100644
--- a/third_party/blink/renderer/core/html/forms/html_button_element.h
+++ b/third_party/blink/renderer/core/html/forms/html_button_element.h
@@ -24,8 +24,6 @@
 #ifndef THIRD_PARTY_BLINK_RENDERER_CORE_HTML_FORMS_HTML_BUTTON_ELEMENT_H_
 #define THIRD_PARTY_BLINK_RENDERER_CORE_HTML_FORMS_HTML_BUTTON_ELEMENT_H_
 
-#include <utility>
-
 #include "third_party/blink/renderer/bindings/core/v8/script_iterator.h"
 #include "third_party/blink/renderer/core/dom/events/simulated_click_options.h"
 #include "third_party/blink/renderer/core/html/forms/html_form_control_element.h"
@@ -65,9 +63,9 @@ class CORE_EXPORT HTMLButtonElement final : public HTMLFormControlElement {
   // These values are a subset of the `FormControlType` enum. They have the same
   // binary representation so that FormControlType() reduces to a type cast.
   enum Type : std::underlying_type_t<mojom::blink::FormControlType> {
-    kSubmit = std::to_underlying(mojom::blink::FormControlType::kButtonSubmit),
-    kReset = std::to_underlying(mojom::blink::FormControlType::kButtonReset),
-    kButton = std::to_underlying(mojom::blink::FormControlType::kButtonButton),
+    kSubmit = base::to_underlying(mojom::blink::FormControlType::kButtonSubmit),
+    kReset = base::to_underlying(mojom::blink::FormControlType::kButtonReset),
+    kButton = base::to_underlying(mojom::blink::FormControlType::kButtonButton),
   };
 
   mojom::blink::FormControlType FormControlType() const override;
diff --git a/third_party/blink/renderer/core/html/forms/html_form_element.cc b/third_party/blink/renderer/core/html/forms/html_form_element.cc
index 4598e3aea8..496905eba5 100644
--- a/third_party/blink/renderer/core/html/forms/html_form_element.cc
+++ b/third_party/blink/renderer/core/html/forms/html_form_element.cc
@@ -25,7 +25,6 @@
 
 #include "third_party/blink/renderer/core/html/forms/html_form_element.h"
 
-#include <algorithm>
 #include <limits>
 
 #include "base/auto_reset.h"
@@ -1162,7 +1161,7 @@ void HTMLFormElement::CollectListedElements(
         if (elements_for_autofill) {
           elements_for_autofill->push_back(listed_element);
         }
-      } else if (std::ranges::contains(nested_forms, listed_element->Form())) {
+      } else if (base::Contains(nested_forms, listed_element->Form())) {
         elements_for_autofill->push_back(listed_element);
       }
     }
diff --git a/third_party/blink/renderer/core/html/forms/input_type.h b/third_party/blink/renderer/core/html/forms/input_type.h
index e377bf0b12..92556474e7 100644
--- a/third_party/blink/renderer/core/html/forms/input_type.h
+++ b/third_party/blink/renderer/core/html/forms/input_type.h
@@ -34,7 +34,6 @@
 #define THIRD_PARTY_BLINK_RENDERER_CORE_HTML_FORMS_INPUT_TYPE_H_
 
 #include <optional>
-#include <utility>
 
 #include "third_party/blink/public/mojom/forms/form_control_type.mojom-blink.h"
 #include "third_party/blink/renderer/core/core_export.h"
@@ -63,38 +62,38 @@ class CORE_EXPORT InputType : public GarbageCollected<InputType> {
   // These values are a subset of the `FormControlType` enum. They have the same
   // binary representation so that FormControlType() reduces to a type cast.
   enum class Type : std::underlying_type_t<mojom::blink::FormControlType> {
-    kButton = std::to_underlying(mojom::blink::FormControlType::kInputButton),
-    kColor = std::to_underlying(mojom::blink::FormControlType::kInputColor),
-    kFile = std::to_underlying(mojom::blink::FormControlType::kInputFile),
-    kHidden = std::to_underlying(mojom::blink::FormControlType::kInputHidden),
-    kImage = std::to_underlying(mojom::blink::FormControlType::kInputImage),
-    kNumber = std::to_underlying(mojom::blink::FormControlType::kInputNumber),
-    kRange = std::to_underlying(mojom::blink::FormControlType::kInputRange),
-    kReset = std::to_underlying(mojom::blink::FormControlType::kInputReset),
-    kSubmit = std::to_underlying(mojom::blink::FormControlType::kInputSubmit),
+    kButton = base::to_underlying(mojom::blink::FormControlType::kInputButton),
+    kColor = base::to_underlying(mojom::blink::FormControlType::kInputColor),
+    kFile = base::to_underlying(mojom::blink::FormControlType::kInputFile),
+    kHidden = base::to_underlying(mojom::blink::FormControlType::kInputHidden),
+    kImage = base::to_underlying(mojom::blink::FormControlType::kInputImage),
+    kNumber = base::to_underlying(mojom::blink::FormControlType::kInputNumber),
+    kRange = base::to_underlying(mojom::blink::FormControlType::kInputRange),
+    kReset = base::to_underlying(mojom::blink::FormControlType::kInputReset),
+    kSubmit = base::to_underlying(mojom::blink::FormControlType::kInputSubmit),
 
     // BaseCheckable
-    kRadio = std::to_underlying(mojom::blink::FormControlType::kInputRadio),
+    kRadio = base::to_underlying(mojom::blink::FormControlType::kInputRadio),
     kCheckbox =
-        std::to_underlying(mojom::blink::FormControlType::kInputCheckbox),
+        base::to_underlying(mojom::blink::FormControlType::kInputCheckbox),
 
     // BaseTemporal
-    kDate = std::to_underlying(mojom::blink::FormControlType::kInputDate),
+    kDate = base::to_underlying(mojom::blink::FormControlType::kInputDate),
     kDateTimeLocal =
-        std::to_underlying(mojom::blink::FormControlType::kInputDatetimeLocal),
-    kMonth = std::to_underlying(mojom::blink::FormControlType::kInputMonth),
-    kTime = std::to_underlying(mojom::blink::FormControlType::kInputTime),
-    kWeek = std::to_underlying(mojom::blink::FormControlType::kInputWeek),
+        base::to_underlying(mojom::blink::FormControlType::kInputDatetimeLocal),
+    kMonth = base::to_underlying(mojom::blink::FormControlType::kInputMonth),
+    kTime = base::to_underlying(mojom::blink::FormControlType::kInputTime),
+    kWeek = base::to_underlying(mojom::blink::FormControlType::kInputWeek),
 
     // BaseText
-    kEmail = std::to_underlying(mojom::blink::FormControlType::kInputEmail),
+    kEmail = base::to_underlying(mojom::blink::FormControlType::kInputEmail),
     kPassword =
-        std::to_underlying(mojom::blink::FormControlType::kInputPassword),
-    kSearch = std::to_underlying(mojom::blink::FormControlType::kInputSearch),
+        base::to_underlying(mojom::blink::FormControlType::kInputPassword),
+    kSearch = base::to_underlying(mojom::blink::FormControlType::kInputSearch),
     kTelephone =
-        std::to_underlying(mojom::blink::FormControlType::kInputTelephone),
-    kURL = std::to_underlying(mojom::blink::FormControlType::kInputUrl),
-    kText = std::to_underlying(mojom::blink::FormControlType::kInputText),
+        base::to_underlying(mojom::blink::FormControlType::kInputTelephone),
+    kURL = base::to_underlying(mojom::blink::FormControlType::kInputUrl),
+    kText = base::to_underlying(mojom::blink::FormControlType::kInputText),
   };
 
   static const AtomicString& TypeToString(Type);
@@ -113,7 +112,7 @@ class CORE_EXPORT InputType : public GarbageCollected<InputType> {
   const AtomicString& FormControlTypeAsString() const;
   mojom::blink::FormControlType FormControlType() const {
     return static_cast<mojom::blink::FormControlType>(
-        std::to_underlying(type_));
+        base::to_underlying(type_));
   }
 
   virtual bool IsInteractiveContent() const;
diff --git a/third_party/blink/renderer/core/html/html_capability_element_base.cc b/third_party/blink/renderer/core/html/html_capability_element_base.cc
index 022147762d..e41fc1c40f 100644
--- a/third_party/blink/renderer/core/html/html_capability_element_base.cc
+++ b/third_party/blink/renderer/core/html/html_capability_element_base.cc
@@ -9,6 +9,7 @@
 #include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/strings/string_split.h"
@@ -1511,7 +1512,7 @@ bool HTMLCapabilityElementBase::IsStyleValid() {
     return false;
   }
 
-  if (std::ranges::contains(kInvalidDisplayStyles,
+  if (base::Contains(kInvalidDisplayStyles,
                             style->GetDisplayStyle().Display()) ||
       style->IsDisplayTableType()) {
     AuditsIssue::ReportPermissionElementIssue(
diff --git a/third_party/blink/renderer/core/html/html_iframe_element_sandbox.cc b/third_party/blink/renderer/core/html/html_iframe_element_sandbox.cc
index 993bef215b..cb2d6a78ef 100644
--- a/third_party/blink/renderer/core/html/html_iframe_element_sandbox.cc
+++ b/third_party/blink/renderer/core/html/html_iframe_element_sandbox.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/core/html/html_iframe_element_sandbox.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/core/html/fenced_frame/html_fenced_frame_element.h"
 #include "third_party/blink/renderer/core/html/html_frame_owner_element.h"
 #include "third_party/blink/renderer/core/html/html_iframe_element.h"
@@ -39,7 +38,7 @@ constexpr char kAllowSameSiteNoneCookiesSandboxToken[] =
     "allow-same-site-none-cookies";
 
 bool IsTokenSupported(const AtomicString& token) {
-  if (std::ranges::contains(kSupportedSandboxTokens, token)) {
+  if (base::Contains(kSupportedSandboxTokens, token)) {
     return true;
   }
   return token == kAllowSameSiteNoneCookiesSandboxToken &&
diff --git a/third_party/blink/renderer/core/html/html_slot_element.cc b/third_party/blink/renderer/core/html/html_slot_element.cc
index deb9ad3e28..889b246d69 100644
--- a/third_party/blink/renderer/core/html/html_slot_element.cc
+++ b/third_party/blink/renderer/core/html/html_slot_element.cc
@@ -31,6 +31,7 @@
 #include "third_party/blink/renderer/core/html/html_slot_element.h"
 
 #include "base/containers/adapters.h"
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_assigned_nodes_options.h"
 #include "third_party/blink/renderer/core/css/style_change_reason.h"
 #include "third_party/blink/renderer/core/css/style_engine.h"
diff --git a/third_party/blink/renderer/core/html/media/media_video_visibility_tracker.cc b/third_party/blink/renderer/core/html/media/media_video_visibility_tracker.cc
index ba5d99ee22..92b8a97756 100644
--- a/third_party/blink/renderer/core/html/media/media_video_visibility_tracker.cc
+++ b/third_party/blink/renderer/core/html/media/media_video_visibility_tracker.cc
@@ -4,8 +4,6 @@
 
 #include "third_party/blink/renderer/core/html/media/media_video_visibility_tracker.h"
 
-#include <algorithm>
-
 #include "base/metrics/histogram_macros.h"
 #include "third_party/blink/renderer/core/dom/document.h"
 #include "third_party/blink/renderer/core/dom/shadow_root.h"
@@ -394,7 +392,7 @@ void MediaVideoVisibilityTracker::ElementDidMoveToNewDocument() {
 
 void MediaVideoVisibilityTracker::Invoke(ExecutionContext* context,
                                          Event* event) {
-  DCHECK(std::ranges::contains(FullscreenEventTypes(), event->type()));
+  DCHECK(base::Contains(FullscreenEventTypes(), event->type()));
 
   // Video is not loaded yet.
   if (VideoElement().getReadyState() < HTMLMediaElement::kHaveMetadata) {
diff --git a/third_party/blink/renderer/core/html/parser/atomic_html_token.h b/third_party/blink/renderer/core/html/parser/atomic_html_token.h
index 313fdf44c4..e11d507eca 100644
--- a/third_party/blink/renderer/core/html/parser/atomic_html_token.h
+++ b/third_party/blink/renderer/core/html/parser/atomic_html_token.h
@@ -26,11 +26,11 @@
 #ifndef THIRD_PARTY_BLINK_RENDERER_CORE_HTML_PARSER_ATOMIC_HTML_TOKEN_H_
 #define THIRD_PARTY_BLINK_RENDERER_CORE_HTML_PARSER_ATOMIC_HTML_TOKEN_H_
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "third_party/blink/renderer/core/core_export.h"
 #include "third_party/blink/renderer/core/dom/attribute.h"
@@ -368,8 +368,8 @@ void AtomicHTMLToken::InitializeAttributes(
         continue;
       }
     } else {
-      if (std::ranges::contains(attributes_, name.LocalName(),
-                                &Attribute::LocalName)) {
+      if (base::Contains(attributes_, name.LocalName(),
+                         &Attribute::LocalName)) {
         duplicate_attribute_ = true;
         continue;
       }
diff --git a/third_party/blink/renderer/core/layout/fragment_builder.cc b/third_party/blink/renderer/core/layout/fragment_builder.cc
index 52be88bc56..f80e24e823 100644
--- a/third_party/blink/renderer/core/layout/fragment_builder.cc
+++ b/third_party/blink/renderer/core/layout/fragment_builder.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/numerics/safe_conversions.h"
 #include "third_party/blink/public/mojom/use_counter/metrics/web_feature.mojom-shared.h"
 #include "third_party/blink/renderer/core/animation/animation_trigger.h"
@@ -828,8 +829,8 @@ void FragmentBuilder::PropagateOOFPositionedInfo(
     static_position.offset += adjusted_offset;
 
     // |oof_positioned_candidates_| should not have duplicated entries.
-    DCHECK(!std::ranges::contains(oof_positioned_candidates_, node,
-                                  &LogicalOofPositionedNode::Node));
+    DCHECK(!base::Contains(oof_positioned_candidates_, node,
+                           &LogicalOofPositionedNode::Node));
     oof_candidates_may_have_anchors_ |= node.MayContainAnchor();
     oof_positioned_candidates_.emplace_back(
         node, descendant.break_token, static_position,
diff --git a/third_party/blink/renderer/core/layout/svg/svg_text_layout_algorithm.cc b/third_party/blink/renderer/core/layout/svg/svg_text_layout_algorithm.cc
index 3806b78a82..6f338c552f 100644
--- a/third_party/blink/renderer/core/layout/svg/svg_text_layout_algorithm.cc
+++ b/third_party/blink/renderer/core/layout/svg/svg_text_layout_algorithm.cc
@@ -385,9 +385,8 @@ void SvgTextLayoutAlgorithm::ResolveTextLength(
       // 2.4.6.2. If the "middle" flag for result[k] is not true and k is not a
       // character in a resolved descendant node other than the first character
       // then shift = shift + small-delta.
-      if (!info.middle &&
-          (std::ranges::contains(resolved_descendant_node_starts, k) ||
-           !info.text_length_resolved)) {
+      if (!info.middle && (base::Contains(resolved_descendant_node_starts, k) ||
+                           !info.text_length_resolved)) {
         shift += character_delta;
       }
       info.text_length_resolved = true;
@@ -899,8 +898,8 @@ bool SvgTextLayoutAlgorithm::IsFirstCharacterInTextPath(
   // This implementation is O(N) where N is the number of <textPath>s in
   // a <text>. If this function is a performance bottleneck, we should add
   // |first_in_text_path| flag to SvgCharacterData.
-  return std::ranges::contains(inline_node_.SvgTextPathRangeList(), index,
-                               &SvgTextContentRange::start_index);
+  return base::Contains(inline_node_.SvgTextPathRangeList(), index,
+                        &SvgTextContentRange::start_index);
 }
 
 }  // namespace blink
diff --git a/third_party/blink/renderer/core/loader/document_loader.cc b/third_party/blink/renderer/core/loader/document_loader.cc
index b98cf42ff3..3c6971ad42 100644
--- a/third_party/blink/renderer/core/loader/document_loader.cc
+++ b/third_party/blink/renderer/core/loader/document_loader.cc
@@ -29,7 +29,6 @@
 
 #include "third_party/blink/renderer/core/loader/document_loader.h"
 
-#include <algorithm>
 #include <memory>
 #include <optional>
 #include <utility>
@@ -3471,7 +3470,7 @@ void DocumentLoader::RecordAcceptLanguageAndContentLanguageMetric() {
             kContentLanguageMatchesPrimaryAcceptLanguage);
   }
 
-  if (std::ranges::contains(accept_languages, content_language)) {
+  if (base::Contains(accept_languages, content_language)) {
     base::UmaHistogramEnumeration(language_histogram_name,
                                   AcceptLanguageAndContentLanguageUsage::
                                       kContentLanguageMatchesAnyAcceptLanguage);
diff --git a/third_party/blink/renderer/core/loader/mixed_content_checker.cc b/third_party/blink/renderer/core/loader/mixed_content_checker.cc
index 90b04bebaf..2ad30628c1 100644
--- a/third_party/blink/renderer/core/loader/mixed_content_checker.cc
+++ b/third_party/blink/renderer/core/loader/mixed_content_checker.cc
@@ -28,9 +28,9 @@
 
 #include "third_party/blink/renderer/core/loader/mixed_content_checker.h"
 
-#include <algorithm>
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/field_trial_params.h"
 #include "build/build_config.h"
@@ -284,8 +284,8 @@ static void MeasureStricterVersionOfIsMixedContent(Frame& frame,
           WebFeature::kMixedContentInNonHTTPSFrameThatRestrictsMixedContent);
     }
   } else if (!IsUrlPotentiallyTrustworthy(url) &&
-             std::ranges::contains(url::GetSecureSchemes(),
-                                   origin->Protocol().Ascii())) {
+             base::Contains(url::GetSecureSchemes(),
+                            origin->Protocol().Ascii())) {
     UseCounter::Count(
         source->GetDocument(),
         WebFeature::kMixedContentInSecureFrameThatDoesNotRestrictMixedContent);
diff --git a/third_party/blink/renderer/core/loader/preload_helper.cc b/third_party/blink/renderer/core/loader/preload_helper.cc
index 6242c18f27..14743bae8b 100644
--- a/third_party/blink/renderer/core/loader/preload_helper.cc
+++ b/third_party/blink/renderer/core/loader/preload_helper.cc
@@ -4,12 +4,11 @@
 
 #include "third_party/blink/renderer/core/loader/preload_helper.h"
 
-#include <utility>
-
 #include "base/feature_list.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/rand_util.h"
 #include "base/timer/elapsed_timer.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "services/metrics/public/cpp/ukm_builders.h"
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/public/mojom/fetch/fetch_api_request.mojom-blink.h"
@@ -906,7 +905,7 @@ void PreloadHelper::LoadLinksFromHeader(
       const OriginStatusOnSubresource origin_status =
           GetOriginStatus(from_same_origin, to_same_origin);
       ukm::builders::Blink_Preloading_ByLinkHeader(document->UkmSourceID())
-          .SetOriginStatusOnSubresource(std::to_underlying(origin_status))
+          .SetOriginStatusOnSubresource(base::to_underlying(origin_status))
           .Record(document->UkmRecorder());
     }
     if (is_subresource_load && !from_same_origin &&
diff --git a/third_party/blink/renderer/core/loader/web_bundle/script_web_bundle_rule.cc b/third_party/blink/renderer/core/loader/web_bundle/script_web_bundle_rule.cc
index 2c50dd6a07..5843f036f1 100644
--- a/third_party/blink/renderer/core/loader/web_bundle/script_web_bundle_rule.cc
+++ b/third_party/blink/renderer/core/loader/web_bundle/script_web_bundle_rule.cc
@@ -4,9 +4,9 @@
 
 #include "third_party/blink/renderer/core/loader/web_bundle/script_web_bundle_rule.h"
 
-#include <algorithm>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "third_party/blink/public/mojom/devtools/console_message.mojom-blink.h"
 #include "third_party/blink/renderer/platform/json/json_parser.h"
@@ -74,7 +74,7 @@ ScriptWebBundleRule::ParseJson(const String& inline_text,
   if (logger) {
     for (wtf_size_t i = 0; i < json_obj->size(); ++i) {
       JSONObject::Entry entry = json_obj->at(i);
-      if (!std::ranges::contains(kKnownKeys, entry.first)) {
+      if (!base::Contains(kKnownKeys, entry.first)) {
         logger->AddConsoleMessage(
             mojom::blink::ConsoleMessageSource::kOther,
             mojom::blink::ConsoleMessageLevel::kWarning,
diff --git a/third_party/blink/renderer/core/permissions_policy/permissions_policy_parser.cc b/third_party/blink/renderer/core/permissions_policy/permissions_policy_parser.cc
index 163b900aa1..1d131e1a85 100644
--- a/third_party/blink/renderer/core/permissions_policy/permissions_policy_parser.cc
+++ b/third_party/blink/renderer/core/permissions_policy/permissions_policy_parser.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/metrics/histogram_macros.h"
 #include "net/http/structured_headers.h"
@@ -774,8 +775,8 @@ PermissionsPolicyParser::ParsePermissionsPolicyForTest(
 
 bool IsFeatureDeclared(network::mojom::PermissionsPolicyFeature feature,
                        const network::ParsedPermissionsPolicy& policy) {
-  return std::ranges::contains(
-      policy, feature, &network::ParsedPermissionsPolicyDeclaration::feature);
+  return base::Contains(policy, feature,
+                        &network::ParsedPermissionsPolicyDeclaration::feature);
 }
 
 bool RemoveFeatureIfPresent(network::mojom::PermissionsPolicyFeature feature,
diff --git a/third_party/blink/renderer/core/scheduler/task_attribution_tracker_impl.h b/third_party/blink/renderer/core/scheduler/task_attribution_tracker_impl.h
index eafadab085..f29869b75f 100644
--- a/third_party/blink/renderer/core/scheduler/task_attribution_tracker_impl.h
+++ b/third_party/blink/renderer/core/scheduler/task_attribution_tracker_impl.h
@@ -7,6 +7,7 @@
 
 #include <optional>
 
+#include "base/containers/contains.h"
 #include "base/memory/weak_ptr.h"
 #include "third_party/blink/public/common/scheduler/task_attribution_id.h"
 #include "third_party/blink/renderer/core/core_export.h"
diff --git a/third_party/blink/renderer/core/speculation_rules/document_rule_predicate.cc b/third_party/blink/renderer/core/speculation_rules/document_rule_predicate.cc
index 0da3497ae8..6afe158b50 100644
--- a/third_party/blink/renderer/core/speculation_rules/document_rule_predicate.cc
+++ b/third_party/blink/renderer/core/speculation_rules/document_rule_predicate.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/core/speculation_rules/document_rule_predicate.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_union_urlpatterninit_usvstring.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_url_pattern_init.h"
 #include "third_party/blink/renderer/core/css/parser/css_parser.h"
@@ -447,7 +446,7 @@ DocumentRulePredicate* DocumentRulePredicate::Parse(
         // "document", then return null.
         String relative_to;
         if (!input->GetString("relative_to", &relative_to) ||
-            !std::ranges::contains(kKnownRelativeToValues, relative_to)) {
+            !base::Contains(kKnownRelativeToValues, relative_to)) {
           SetParseErrorMessage(
               out_error,
               StrCat({"Unrecognized \"relative_to\" value: ",
diff --git a/third_party/blink/renderer/core/speculation_rules/document_speculation_rules.cc b/third_party/blink/renderer/core/speculation_rules/document_speculation_rules.cc
index ce87f516f2..e9dee69ac3 100644
--- a/third_party/blink/renderer/core/speculation_rules/document_speculation_rules.cc
+++ b/third_party/blink/renderer/core/speculation_rules/document_speculation_rules.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/state_transitions.h"
@@ -274,7 +275,7 @@ void DocumentSpeculationRules::AddRuleSet(SpeculationRuleSet* rule_set) {
 
   CountSpeculationRulesLoadOutcome(outcome);
 
-  DCHECK(!std::ranges::contains(rule_sets_, rule_set));
+  DCHECK(!base::Contains(rule_sets_, rule_set));
   rule_sets_.push_back(rule_set);
   if (rule_set->has_document_rule()) {
     UseCounter::Count(GetSupplementable(),
diff --git a/third_party/blink/renderer/core/speculation_rules/speculation_rule_set.cc b/third_party/blink/renderer/core/speculation_rules/speculation_rule_set.cc
index 2337df8e89..1091f328e6 100644
--- a/third_party/blink/renderer/core/speculation_rules/speculation_rule_set.cc
+++ b/third_party/blink/renderer/core/speculation_rules/speculation_rule_set.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/core/speculation_rules/speculation_rule_set.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "services/network/public/mojom/no_vary_search.mojom-shared.h"
 #include "services/network/public/mojom/referrer_policy.mojom-shared.h"
 #include "third_party/blink/public/mojom/speculation_rules/speculation_rules.mojom-shared.h"
@@ -155,7 +154,7 @@ SpeculationRule* ParseSpeculationRule(JSONObject* input,
 
   for (wtf_size_t i = 0; i < input->size(); ++i) {
     const String& input_key = input->at(i).first;
-    if (!std::ranges::contains(kKnownKeys, input_key)) {
+    if (!base::Contains(kKnownKeys, input_key)) {
       SetParseErrorMessage(
           out_error,
           StrCat({"A rule contains an unknown key: \"", input_key, "\"."}));
@@ -222,7 +221,7 @@ SpeculationRule* ParseSpeculationRule(JSONObject* input,
       // If relativeTo is neither the string "ruleset" nor the string
       // "document", then return null.
       if (!relative_to->AsString(&value) ||
-          !std::ranges::contains(kKnownRelativeToValues, value)) {
+          !base::Contains(kKnownRelativeToValues, value)) {
         SetParseErrorMessage(out_error,
                              "A rule has an unknown \"relative_to\" value.");
         return nullptr;
@@ -653,7 +652,7 @@ SpeculationRuleSet* SpeculationRuleSet::Parse(Source* source,
       duplicate_key_warning = StrCat(
           {"An object contained more than one key named ",
            key.EncodeForDebugging(), ". All but the last are ignored.",
-           (std::ranges::contains(action_allow_list, key)
+           (base::Contains(action_allow_list, key)
                 ? " It is likely that either one of them was intended to be "
                   "another action, or that their rules should be merged into a "
                   "single array."
diff --git a/third_party/blink/renderer/core/speculation_rules/speculation_rules_header.cc b/third_party/blink/renderer/core/speculation_rules/speculation_rules_header.cc
index 5abe1ffa7c..8d77b05928 100644
--- a/third_party/blink/renderer/core/speculation_rules/speculation_rules_header.cc
+++ b/third_party/blink/renderer/core/speculation_rules/speculation_rules_header.cc
@@ -4,6 +4,7 @@
 
 #include "third_party/blink/renderer/core/speculation_rules/speculation_rules_header.h"
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "net/http/structured_headers.h"
 #include "services/network/public/mojom/fetch_api.mojom-blink-forward.h"
diff --git a/third_party/blink/renderer/core/style/counter_directives.h b/third_party/blink/renderer/core/style/counter_directives.h
index d2cd70d8e5..f19bf7a758 100644
--- a/third_party/blink/renderer/core/style/counter_directives.h
+++ b/third_party/blink/renderer/core/style/counter_directives.h
@@ -62,8 +62,10 @@ class CounterDirectives {
   // `nullopt` if the reversed function is specified and the <integer> is
   // omitted(e.g., `counter-reset: reversed(list-item)`).
   std::optional<int> ResetValue() const {
-    return reset_value_.transform(
-        [](int64_t v) { return base::saturated_cast<int>(v); });
+    if (!reset_value_.has_value()) {
+      return std::nullopt;
+    }
+    return base::saturated_cast<int>(reset_value_.value());
   }
   std::optional<int64_t> ResetValueInt64() const { return reset_value_; }
   void SetResetValue(int64_t value) { reset_value_ = value; }
diff --git a/third_party/blink/renderer/core/style/filter_operations.cc b/third_party/blink/renderer/core/style/filter_operations.cc
index 61902e73be..55237a54f4 100644
--- a/third_party/blink/renderer/core/style/filter_operations.cc
+++ b/third_party/blink/renderer/core/style/filter_operations.cc
@@ -28,6 +28,7 @@
 #include <algorithm>
 #include <numeric>
 
+#include "base/containers/contains.h"
 
 namespace blink {
 
@@ -93,9 +94,8 @@ bool FilterOperations::HasFilterThatMovesPixels() const {
 }
 
 bool FilterOperations::HasReferenceFilter() const {
-  return std::ranges::contains(operations_,
-                               FilterOperation::OperationType::kReference,
-                               &FilterOperation::GetType);
+  return base::Contains(operations_, FilterOperation::OperationType::kReference,
+                        &FilterOperation::GetType);
 }
 
 bool FilterOperations::UsesCurrentColor() const {
diff --git a/third_party/blink/renderer/core/testing/mock_clipboard_host.cc b/third_party/blink/renderer/core/testing/mock_clipboard_host.cc
index 7547eca627..3b26e9e24d 100644
--- a/third_party/blink/renderer/core/testing/mock_clipboard_host.cc
+++ b/third_party/blink/renderer/core/testing/mock_clipboard_host.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/core/testing/mock_clipboard_host.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/numerics/byte_conversions.h"
 #include "build/build_config.h"
 #include "mojo/public/cpp/base/big_buffer.h"
@@ -80,7 +79,7 @@ Vector<String> MockClipboardHost::ReadStandardFormatNames() {
   if (!png_.empty())
     types.push_back(ui::kMimeTypePng);
   for (auto& it : custom_data_) {
-    CHECK(!std::ranges::contains(types, it.key));
+    CHECK(!base::Contains(types, it.key));
     types.push_back(it.key);
   }
   return types;
diff --git a/third_party/blink/renderer/core/timing/performance.cc b/third_party/blink/renderer/core/timing/performance.cc
index acc773777d..a2b92e5b4b 100644
--- a/third_party/blink/renderer/core/timing/performance.cc
+++ b/third_party/blink/renderer/core/timing/performance.cc
@@ -35,6 +35,7 @@
 #include <optional>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/task/single_thread_task_runner.h"
diff --git a/third_party/blink/renderer/core/view_transition/view_transition_style_tracker.cc b/third_party/blink/renderer/core/view_transition/view_transition_style_tracker.cc
index c1812deaa5..dfca60c790 100644
--- a/third_party/blink/renderer/core/view_transition/view_transition_style_tracker.cc
+++ b/third_party/blink/renderer/core/view_transition/view_transition_style_tracker.cc
@@ -578,7 +578,7 @@ void ViewTransitionStyleTracker::AddTransitionElement(
                                });
   }
   // Find the existing name if one is there. If it is there, do nothing.
-  if (std::ranges::contains(value, name, &std::pair<AtomicString, int>::first))
+  if (base::Contains(value, name, &std::pair<AtomicString, int>::first))
     return;
   // Otherwise, insert a new sequence id with this name. We'll use the sequence
   // to sort later.
diff --git a/third_party/blink/renderer/core/xmlhttprequest/xml_http_request.cc b/third_party/blink/renderer/core/xmlhttprequest/xml_http_request.cc
index f39de99225..3fcd014368 100644
--- a/third_party/blink/renderer/core/xmlhttprequest/xml_http_request.cc
+++ b/third_party/blink/renderer/core/xmlhttprequest/xml_http_request.cc
@@ -28,6 +28,7 @@
 #include <utility>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/feature_list.h"
 #include "base/numerics/safe_conversions.h"
@@ -1541,7 +1542,7 @@ const AtomicString& XMLHttpRequest::getResponseHeader(
 
   if (response_.GetType() == network::mojom::FetchResponseType::kCors &&
       !cors::IsCorsSafelistedResponseHeader(name) &&
-      !access_control_expose_header_set.contains(name.Ascii())) {
+      !base::Contains(access_control_expose_header_set, name.Ascii())) {
     LogConsoleError(GetExecutionContext(),
                     StrCat({"Refused to get unsafe header \"", name, "\""}));
     return g_null_atom;
diff --git a/third_party/blink/renderer/modules/accessibility/ax_node_object.cc b/third_party/blink/renderer/modules/accessibility/ax_node_object.cc
index b2510bb26a..703b0dc465 100644
--- a/third_party/blink/renderer/modules/accessibility/ax_node_object.cc
+++ b/third_party/blink/renderer/modules/accessibility/ax_node_object.cc
@@ -37,6 +37,7 @@
 #include <queue>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/numerics/safe_conversions.h"
@@ -1174,7 +1175,7 @@ AXObjectInclusion AXNodeObject::ShouldIncludeBasedOnSemantics(
           ax::mojom::blink::Role::kVideo,
       });
 
-  if (always_included_computed_roles.contains(RoleValue())) {
+  if (base::Contains(always_included_computed_roles, RoleValue())) {
     return kIncludeObject;
   }
 
diff --git a/third_party/blink/renderer/modules/accessibility/ax_object_cache_impl.cc b/third_party/blink/renderer/modules/accessibility/ax_object_cache_impl.cc
index 1f6bcca34f..6547199051 100644
--- a/third_party/blink/renderer/modules/accessibility/ax_object_cache_impl.cc
+++ b/third_party/blink/renderer/modules/accessibility/ax_object_cache_impl.cc
@@ -35,6 +35,7 @@
 
 #include "base/auto_reset.h"
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/notreached.h"
diff --git a/third_party/blink/renderer/modules/ad_auction/navigator_auction.cc b/third_party/blink/renderer/modules/ad_auction/navigator_auction.cc
index f05b1e68a9..ec91d130cd 100644
--- a/third_party/blink/renderer/modules/ad_auction/navigator_auction.cc
+++ b/third_party/blink/renderer/modules/ad_auction/navigator_auction.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/check.h"
+#include "base/containers/contains.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/numerics/checked_math.h"
@@ -2635,7 +2636,8 @@ bool CopyAllSlotsRequestedSizesFromIdlToMojo(
 
   // If `requested_size` is set, `all_slots_requested_sizes` must include it.
   if (output.auction_ad_config_non_shared_params->requested_size &&
-      !distinct_sizes.contains(
+      !base::Contains(
+          distinct_sizes,
           *output.auction_ad_config_non_shared_params->requested_size)) {
     exception_state.ThrowTypeError(
         StrCat({"allSlotsRequestedSizes for AuctionAdConfig with seller '",
diff --git a/third_party/blink/renderer/modules/cache_storage/cache.cc b/third_party/blink/renderer/modules/cache_storage/cache.cc
index 391d0f4d90..993ea5c2d3 100644
--- a/third_party/blink/renderer/modules/cache_storage/cache.cc
+++ b/third_party/blink/renderer/modules/cache_storage/cache.cc
@@ -4,10 +4,10 @@
 
 #include "third_party/blink/renderer/modules/cache_storage/cache.h"
 
-#include <algorithm>
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/task/single_thread_task_runner.h"
 #include "services/network/public/mojom/fetch_api.mojom-blink.h"
@@ -66,7 +66,7 @@ bool VaryHeaderContainsAsterisk(const Response* response) {
     Vector<StringView> fields = StringView(vary_header).SplitSkippingEmpty(',');
     StringView (StringView::*strip_whitespace)() const =
         &StringView::StripWhiteSpace;
-    return std::ranges::contains(fields, "*", strip_whitespace);
+    return base::Contains(fields, "*", strip_whitespace);
   }
   return false;
 }
diff --git a/third_party/blink/renderer/modules/cookie_store/cookie_store.cc b/third_party/blink/renderer/modules/cookie_store/cookie_store.cc
index bed3476360..1622cd443b 100644
--- a/third_party/blink/renderer/modules/cookie_store/cookie_store.cc
+++ b/third_party/blink/renderer/modules/cookie_store/cookie_store.cc
@@ -4,12 +4,12 @@
 
 #include "third_party/blink/renderer/modules/cookie_store/cookie_store.h"
 
-#include <algorithm>
 #include <cstddef>
 #include <cstdint>
 #include <optional>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "net/base/features.h"
 #include "net/cookies/cookie_inclusion_status.h"
 #include "net/cookies/cookie_util.h"
@@ -199,8 +199,7 @@ network::mojom::blink::RestrictedCanonicalCookieParamsPtr ToCookieParams(
   // insecure origins. file:// are excluded too for consistency with
   // document.cookie.
   if (!network::IsUrlPotentiallyTrustworthy(GURL(cookie_url)) ||
-      std::ranges::contains(url::GetLocalSchemes(),
-                            cookie_url.Protocol().Ascii())) {
+      base::Contains(url::GetLocalSchemes(), cookie_url.Protocol().Ascii())) {
     exception_state.ThrowTypeError(
         "Cannot modify a secure cookie on insecure origin");
     return nullptr;
diff --git a/third_party/blink/renderer/modules/filesystem/file_system_dispatcher.cc b/third_party/blink/renderer/modules/filesystem/file_system_dispatcher.cc
index 2afbd35ee7..945526432b 100644
--- a/third_party/blink/renderer/modules/filesystem/file_system_dispatcher.cc
+++ b/third_party/blink/renderer/modules/filesystem/file_system_dispatcher.cc
@@ -6,6 +6,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/task/sequenced_task_runner.h"
 #include "build/build_config.h"
 #include "components/services/filesystem/public/mojom/types.mojom-blink.h"
diff --git a/third_party/blink/renderer/modules/font_access/font_access.cc b/third_party/blink/renderer/modules/font_access/font_access.cc
index f066f2052e..4316fb4a1e 100644
--- a/third_party/blink/renderer/modules/font_access/font_access.cc
+++ b/third_party/blink/renderer/modules/font_access/font_access.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/numerics/safe_conversions.h"
 #include "services/network/public/mojom/permissions_policy/permissions_policy_feature.mojom-blink.h"
@@ -157,7 +158,7 @@ void FontAccess::DidGetEnumerationResponse(
     // If the optional postscript name filter is set in QueryOptions,
     // only allow items that match.
     if (hasPostscriptNameFilter &&
-        !selection_utf8.contains(element.postscript_name().c_str())) {
+        !base::Contains(selection_utf8, element.postscript_name().c_str())) {
       continue;
     }
 
diff --git a/third_party/blink/renderer/modules/imagecapture/image_capture.cc b/third_party/blink/renderer/modules/imagecapture/image_capture.cc
index a03d9eb4aa..dc20f675d8 100644
--- a/third_party/blink/renderer/modules/imagecapture/image_capture.cc
+++ b/third_party/blink/renderer/modules/imagecapture/image_capture.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/time/time.h"
 #include "base/trace_event/trace_event.h"
@@ -684,7 +685,7 @@ bool CheckExactValueConstraint(const MediaSettingsRange* effective_capability,
 // capabilities such as whiteBalanceMode, exposureMode and focusMode.
 bool CheckExactValueConstraint(const Vector<String>& effective_capability,
                                const String& exact_constraint) {
-  return std::ranges::contains(effective_capability, exact_constraint);
+  return base::Contains(effective_capability, exact_constraint);
 }
 
 // For exact `sequence<DOMString>` constraints and `sequence<DOMString>`
@@ -692,7 +693,7 @@ bool CheckExactValueConstraint(const Vector<String>& effective_capability,
 bool CheckExactValueConstraint(const Vector<String>& effective_capability,
                                const Vector<String>& exact_constraints) {
   for (const auto& exact_constraint : exact_constraints) {
-    if (std::ranges::contains(effective_capability, exact_constraint)) {
+    if (base::Contains(effective_capability, exact_constraint)) {
       return true;
     }
   }
@@ -909,7 +910,7 @@ bool CheckValueConstraint(
     case ContentType::kBoolean:
       if (IsBareValueToBeTreatedAsExact(constraint_set_type)) {
         const bool exact_constraint = constraint->GetAsBoolean();
-        return std::ranges::contains(effective_capability, exact_constraint);
+        return base::Contains(effective_capability, exact_constraint);
       }
       return true;
     case ContentType::kConstrainBooleanParameters: {
@@ -919,7 +920,7 @@ bool CheckValueConstraint(
           constraint->GetAsConstrainBooleanParameters();
       if (dictionary_constraint->hasExact()) {
         const bool exact_constraint = dictionary_constraint->exact();
-        return std::ranges::contains(effective_capability, exact_constraint);
+        return base::Contains(effective_capability, exact_constraint);
       }
       return true;
     }
@@ -1037,15 +1038,15 @@ Vector<String> ApplyExactValueConstraint(
   // Update the effective capability.
   Vector<String> new_effective_capability;
   for (const auto& exact_constraint : exact_constraints) {
-    if (std::ranges::contains(effective_capability, exact_constraint)) {
+    if (base::Contains(effective_capability, exact_constraint)) {
       new_effective_capability.push_back(exact_constraint);
     }
   }
   DCHECK(!new_effective_capability.empty());
   // Clamp and update the setting.
   if (!*has_setting_ptr ||
-      !std::ranges::contains(exact_constraints, static_cast<const String&>(
-                                                    ToString(*setting_ptr)))) {
+      !base::Contains(exact_constraints,
+                      static_cast<const String&>(ToString(*setting_ptr)))) {
     *has_setting_ptr = true;
     *setting_ptr = ParseMeteringMode(new_effective_capability[0]);
   }
@@ -1070,7 +1071,7 @@ Vector<bool> ApplyIdealValueConstraint(bool* has_setting_ptr,
                                        bool ideal_constraint) {
   // Clamp and update the setting.
   *has_setting_ptr = true;
-  *setting_ptr = std::ranges::contains(effective_capability, ideal_constraint)
+  *setting_ptr = base::Contains(effective_capability, ideal_constraint)
                      ? ideal_constraint
                      : effective_capability[0];
   // Keep the effective capability intact.
@@ -1105,9 +1106,8 @@ Vector<String> ApplyIdealValueConstraint(
   // Validate and update the setting.
   *has_setting_ptr = true;
   *setting_ptr = ParseMeteringMode(
-      std::ranges::contains(effective_capability, ideal_constraint)
-          ? ideal_constraint
-          : current_setting);
+      base::Contains(effective_capability, ideal_constraint) ? ideal_constraint
+                                                             : current_setting);
   // Keep the effective capability intact.
   return effective_capability;
 }
@@ -1122,11 +1122,11 @@ Vector<String> ApplyIdealValueConstraint(
     const String& current_setting) {
   // Clamp and update the setting.
   if (!*has_setting_ptr ||
-      !std::ranges::contains(ideal_constraints, static_cast<const String&>(
-                                                    ToString(*setting_ptr)))) {
+      !base::Contains(ideal_constraints,
+                      static_cast<const String&>(ToString(*setting_ptr)))) {
     String setting_name = current_setting;
     for (const auto& ideal_constraint : ideal_constraints) {
-      if (std::ranges::contains(effective_capability, ideal_constraint)) {
+      if (base::Contains(effective_capability, ideal_constraint)) {
         setting_name = ideal_constraint;
         break;
       }
@@ -1455,7 +1455,7 @@ void MaybeSetBackgroundBlurSetting(bool value,
                                    const Vector<bool>& capability,
                                    bool& has_setting,
                                    BackgroundBlurMode& setting) {
-  if (!std::ranges::contains(capability, value)) {
+  if (!base::Contains(capability, value)) {
     return;
   }
 
@@ -1466,7 +1466,7 @@ void MaybeSetBackgroundBlurSetting(bool value,
 void MaybeSetBoolSetting(bool value,
                          const Vector<bool>& capability,
                          std::optional<bool>& setting) {
-  if (!std::ranges::contains(capability, value)) {
+  if (!base::Contains(capability, value)) {
     return;
   }
 
@@ -1477,7 +1477,7 @@ void MaybeSetBoolSetting(bool value,
                          const Vector<bool>& capability,
                          bool& has_setting,
                          bool& setting) {
-  if (!std::ranges::contains(capability, value)) {
+  if (!base::Contains(capability, value)) {
     return;
   }
 
@@ -1489,7 +1489,7 @@ void MaybeSetEyeGazeCorrectionSetting(
     bool value,
     const Vector<bool>& capability,
     std::optional<EyeGazeCorrectionMode>& setting) {
-  if (!std::ranges::contains(capability, value)) {
+  if (!base::Contains(capability, value)) {
     return;
   }
 
@@ -1500,7 +1500,7 @@ void MaybeSetFaceFramingSetting(bool value,
                                 const Vector<bool>& capability,
                                 bool& has_setting,
                                 MeteringMode& setting) {
-  if (!std::ranges::contains(capability, value)) {
+  if (!base::Contains(capability, value)) {
     return;
   }
 
@@ -2832,8 +2832,7 @@ void ImageCapture::UpdateMediaTrackSettingsAndCapabilities(
     Vector<bool> supported_background_blur_modes;
     for (auto mode : photo_state->supported_background_blur_modes) {
       bool boolean_mode = ToBooleanMode(mode);
-      if (!std::ranges::contains(supported_background_blur_modes,
-                                 boolean_mode)) {
+      if (!base::Contains(supported_background_blur_modes, boolean_mode)) {
         supported_background_blur_modes.push_back(boolean_mode);
       }
     }
@@ -2854,8 +2853,7 @@ void ImageCapture::UpdateMediaTrackSettingsAndCapabilities(
     Vector<bool> supported_eye_gaze_correction_modes;
     for (const auto& mode : photo_state->supported_eye_gaze_correction_modes) {
       bool boolean_mode = ToBooleanMode(mode);
-      if (!std::ranges::contains(supported_eye_gaze_correction_modes,
-                                 boolean_mode)) {
+      if (!base::Contains(supported_eye_gaze_correction_modes, boolean_mode)) {
         supported_eye_gaze_correction_modes.push_back(boolean_mode);
       }
     }
diff --git a/third_party/blink/renderer/modules/media/audio/audio_renderer_mixer_manager.cc b/third_party/blink/renderer/modules/media/audio/audio_renderer_mixer_manager.cc
index 633202307d..d70f5e33e8 100644
--- a/third_party/blink/renderer/modules/media/audio/audio_renderer_mixer_manager.cc
+++ b/third_party/blink/renderer/modules/media/audio/audio_renderer_mixer_manager.cc
@@ -15,6 +15,7 @@
 #include "base/memory/ptr_util.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "build/build_config.h"
 #include "media/audio/audio_device_description.h"
 #include "media/base/audio_renderer_sink.h"
@@ -190,7 +191,7 @@ AudioRendererMixer* AudioRendererMixerManager::GetMixer(
   auto* mixer_ref = mixer.get();
   mixers_[key] = {std::move(mixer), 1};
   DVLOG(1) << __func__ << " mixer: " << mixer
-           << " latency: " << std::to_underlying(latency)
+           << " latency: " << base::to_underlying(latency)
            << "\n input: " << input_params.AsHumanReadableString()
            << "\noutput: " << mixer_output_params.AsHumanReadableString();
   return mixer_ref;
diff --git a/third_party/blink/renderer/modules/mediarecorder/media_recorder_encoder_wrapper.cc b/third_party/blink/renderer/modules/mediarecorder/media_recorder_encoder_wrapper.cc
index 8f74a80840..d0231a311a 100644
--- a/third_party/blink/renderer/modules/mediarecorder/media_recorder_encoder_wrapper.cc
+++ b/third_party/blink/renderer/modules/mediarecorder/media_recorder_encoder_wrapper.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/modules/mediarecorder/media_recorder_encoder_wrapper.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/numerics/safe_conversions.h"
 #include "media/base/decoder_buffer.h"
 #include "media/base/video_encoder_metrics_provider.h"
@@ -61,7 +60,7 @@ MediaRecorderEncoderWrapper::MediaRecorderEncoderWrapper(
       media::VideoCodec::kHEVC,
 #endif
   };
-  CHECK(std::ranges::contains(kSupportedCodecs, codec_));
+  CHECK(base::Contains(kSupportedCodecs, codec_));
   options_.latency_mode = media::VideoEncoder::LatencyMode::Quality;
   options_.bitrate = media::Bitrate::VariableBitrate(
       bits_per_second, base::ClampMul(bits_per_second, 2u).RawValue());
diff --git a/third_party/blink/renderer/modules/mediastream/media_constraints.cc b/third_party/blink/renderer/modules/mediastream/media_constraints.cc
index 98e217a9b2..6e4c556c9d 100644
--- a/third_party/blink/renderer/modules/mediastream/media_constraints.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_constraints.cc
@@ -32,8 +32,7 @@
 
 #include <math.h>
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/memory/scoped_refptr.h"
 #include "third_party/blink/renderer/platform/wtf/text/string_builder.h"
 #include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"
@@ -571,7 +570,7 @@ bool MediaTrackConstraintSetPlatform::HasMandatoryOutsideSet(
     String& found_name) const {
   for (auto* const constraint : AllConstraints()) {
     if (constraint->HasMandatory()) {
-      if (!std::ranges::contains(good_names, constraint->GetName())) {
+      if (!base::Contains(good_names, constraint->GetName())) {
         found_name = constraint->GetName();
         return true;
       }
diff --git a/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_audio.cc b/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_audio.cc
index da15d83d7d..2754785af7 100644
--- a/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_audio.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_audio.cc
@@ -10,6 +10,7 @@
 #include <tuple>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "build/build_config.h"
 #include "media/audio/audio_features.h"
@@ -1005,14 +1006,14 @@ class ProcessingBasedContainer {
     // If the device parameters indicate that system echo cancellation is
     // available, add support for it to `echo_cancellation_modes`.
     if (EchoCanceller::IsPlatformAecAvailable(device_parameters.effects())) {
-      if (!std::ranges::contains(echo_cancellation_modes,
-                                 EchoCancellationMode::kBrowserDecides)) {
+      if (!base::Contains(echo_cancellation_modes,
+                          EchoCancellationMode::kBrowserDecides)) {
         echo_cancellation_modes.push_back(
             EchoCancellationMode::kBrowserDecides);
       }
       if (ShouldSupportExtendedEchoCancellationModes(api) &&
-          !std::ranges::contains(echo_cancellation_modes,
-                                 EchoCancellationMode::kAll)) {
+          !base::Contains(echo_cancellation_modes,
+                          EchoCancellationMode::kAll)) {
         echo_cancellation_modes.push_back(EchoCancellationMode::kAll);
       }
     }
diff --git a/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_sets.h b/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_sets.h
index 5af6fdd3ea..99b68249ea 100644
--- a/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_sets.h
+++ b/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_sets.h
@@ -12,6 +12,7 @@
 #include <utility>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/gtest_prod_util.h"
 #include "third_party/blink/renderer/modules/mediastream/media_constraints.h"
 #include "third_party/blink/renderer/modules/modules_export.h"
@@ -272,7 +273,7 @@ class DiscreteSet {
   ~DiscreteSet() = default;
 
   bool Contains(const T& value) const {
-    return is_universal_ || std::ranges::contains(elements_, value);
+    return is_universal_ || base::Contains(elements_, value);
   }
 
   bool IsEmpty() const { return !is_universal_ && elements_.empty(); }
@@ -290,7 +291,7 @@ class DiscreteSet {
     // Both sets have explicit elements.
     Vector<T> intersection;
     for (const auto& entry : elements_) {
-      if (std::ranges::contains(other.elements_, entry))
+      if (base::Contains(other.elements_, entry))
         intersection.push_back(entry);
     }
     return DiscreteSet(std::move(intersection));
diff --git a/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_video_device.cc b/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_video_device.cc
index 4c183eb67f..3ff314656a 100644
--- a/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_video_device.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_stream_constraints_util_video_device.cc
@@ -9,6 +9,7 @@
 #include <limits>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/strings/stringprintf.h"
 #include "media/base/limits.h"
 #include "media/base/video_types.h"
@@ -405,12 +406,12 @@ class CandidateFormat {
     }
 
     if (basic_constraint_set.resize_mode.HasIdeal()) {
-      if (!std::ranges::contains(basic_constraint_set.resize_mode.Ideal(),
-                                 WebMediaStreamTrack::kResizeModeNone)) {
+      if (!base::Contains(basic_constraint_set.resize_mode.Ideal(),
+                          WebMediaStreamTrack::kResizeModeNone)) {
         track_fitness_without_rescale += 1.0;
       }
-      if (!std::ranges::contains(basic_constraint_set.resize_mode.Ideal(),
-                                 WebMediaStreamTrack::kResizeModeRescale)) {
+      if (!base::Contains(basic_constraint_set.resize_mode.Ideal(),
+                          WebMediaStreamTrack::kResizeModeRescale)) {
         track_fitness_with_rescale += 1.0;
       }
     }
diff --git a/third_party/blink/renderer/modules/mediastream/media_stream_video_source.cc b/third_party/blink/renderer/modules/mediastream/media_stream_video_source.cc
index b86d99183e..6193f9004d 100644
--- a/third_party/blink/renderer/modules/mediastream/media_stream_video_source.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_stream_video_source.cc
@@ -10,6 +10,7 @@
 #include <numeric>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback.h"
 #include "base/functional/callback_helpers.h"
@@ -63,7 +64,7 @@ void MediaStreamVideoSource::AddTrack(
     MediaStreamVideoSourceCallbacks media_stream_callbacks,
     ConstraintsOnceCallback callback) {
   DCHECK(GetTaskRunner()->BelongsToCurrentThread());
-  DCHECK(!std::ranges::contains(tracks_, track));
+  DCHECK(!base::Contains(tracks_, track));
   tracks_.push_back(track);
   secure_tracker_.Add(track, true);
 
diff --git a/third_party/blink/renderer/modules/mediastream/media_stream_video_track.cc b/third_party/blink/renderer/modules/mediastream/media_stream_video_track.cc
index 39107eee73..a19ced3f28 100644
--- a/third_party/blink/renderer/modules/mediastream/media_stream_video_track.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_stream_video_track.cc
@@ -8,6 +8,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/location.h"
 #include "base/metrics/histogram_functions.h"
@@ -960,7 +961,7 @@ MediaStreamVideoTrack::CreateFromComponent(
 
 static void AddSinkInternal(Vector<WebMediaStreamSink*>* sinks,
                             WebMediaStreamSink* sink) {
-  DCHECK(!std::ranges::contains(*sinks, sink));
+  DCHECK(!base::Contains(*sinks, sink));
   sinks->push_back(sink);
 }
 
diff --git a/third_party/blink/renderer/modules/mediastream/user_media_processor.cc b/third_party/blink/renderer/modules/mediastream/user_media_processor.cc
index dabfc02766..47f884fe0b 100644
--- a/third_party/blink/renderer/modules/mediastream/user_media_processor.cc
+++ b/third_party/blink/renderer/modules/mediastream/user_media_processor.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
 #include "base/logging.h"
@@ -701,7 +702,7 @@ void UserMediaProcessor::RequestInfo::OnAudioSourceStarted(
     const String& result_name) {
   // Check if we're waiting to be notified of this source.  If not, then we'll
   // ignore the notification.
-  if (std::ranges::contains(sources_waiting_for_callback_, source)) {
+  if (base::Contains(sources_waiting_for_callback_, source)) {
     OnTrackStarted(source, result, result_name);
   }
 }
diff --git a/third_party/blink/renderer/modules/ml/ml_context.cc b/third_party/blink/renderer/modules/ml/ml_context.cc
index 48e91550f6..80eccf4f38 100644
--- a/third_party/blink/renderer/modules/ml/ml_context.cc
+++ b/third_party/blink/renderer/modules/ml/ml_context.cc
@@ -4,10 +4,9 @@
 
 #include "third_party/blink/renderer/modules/ml/ml_context.h"
 
-#include <utility>
-
 #include "base/feature_list.h"
 #include "base/numerics/checked_math.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "base/types/expected.h"
 #include "base/types/expected_macros.h"
 #include "base/types/pass_key.h"
@@ -1191,7 +1190,7 @@ ScriptPromise<MLTensor> MLContext::createTensor(
   //
   // This assertion protects against the usage flags changing without updating
   // this mapping.
-  static_assert(std::to_underlying(webnn::MLTensorUsageFlags::kMaxValue) == 3);
+  static_assert(base::to_underlying(webnn::MLTensorUsageFlags::kMaxValue) == 3);
   webnn::MLTensorUsage usage;
   if (descriptor->readable()) {
     usage.Put(webnn::MLTensorUsageFlags::kRead);
@@ -1278,7 +1277,7 @@ ScriptPromise<MLTensor> MLContext::createExportableTensor(
   //
   // This assertion protects against the usage flags changing without updating
   // this mapping.
-  static_assert(std::to_underlying(webnn::MLTensorUsageFlags::kMaxValue) == 3);
+  static_assert(base::to_underlying(webnn::MLTensorUsageFlags::kMaxValue) == 3);
   webnn::MLTensorUsage usage;
   usage.Put(webnn::MLTensorUsageFlags::kWebGpuInterop);
   if (descriptor->readable()) {
diff --git a/third_party/blink/renderer/modules/ml/webnn/ml_graph_builder.cc b/third_party/blink/renderer/modules/ml/webnn/ml_graph_builder.cc
index b47015f0e7..e06a48994c 100644
--- a/third_party/blink/renderer/modules/ml/webnn/ml_graph_builder.cc
+++ b/third_party/blink/renderer/modules/ml/webnn/ml_graph_builder.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/containers/enum_set.h"
 #include "base/containers/span.h"
 #include "base/metrics/histogram_macros.h"
@@ -1563,8 +1564,8 @@ void FoldReshapableConstants(blink_mojom::GraphInfo& graph_info) {
               graph_info.operations,
               [&constant_operand_id](
                   const blink_mojom::OperationPtr& operation) {
-                return std::ranges::contains(GetInputs(*operation),
-                                             constant_operand_id);
+                return base::Contains(GetInputs(*operation),
+                                      constant_operand_id);
               }) > 1) {
         break;
       }
diff --git a/third_party/blink/renderer/modules/peerconnection/mock_peer_connection_dependency_factory.cc b/third_party/blink/renderer/modules/peerconnection/mock_peer_connection_dependency_factory.cc
index c3cd6cb079..2179397969 100644
--- a/third_party/blink/renderer/modules/peerconnection/mock_peer_connection_dependency_factory.cc
+++ b/third_party/blink/renderer/modules/peerconnection/mock_peer_connection_dependency_factory.cc
@@ -6,6 +6,7 @@
 
 #include <stddef.h>
 
+#include "base/containers/contains.h"
 #include "base/task/single_thread_task_runner.h"
 #include "third_party/blink/renderer/modules/peerconnection/mock_peer_connection_impl.h"
 #include "third_party/blink/renderer/modules/peerconnection/mock_rtc_peer_connection_handler_platform.h"
diff --git a/third_party/blink/renderer/modules/peerconnection/mock_peer_connection_impl.cc b/third_party/blink/renderer/modules/peerconnection/mock_peer_connection_impl.cc
index 99837475f7..bc5f504276 100644
--- a/third_party/blink/renderer/modules/peerconnection/mock_peer_connection_impl.cc
+++ b/third_party/blink/renderer/modules/peerconnection/mock_peer_connection_impl.cc
@@ -11,6 +11,7 @@
 #include <vector>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/memory/raw_ptr.h"
 #include "base/notimplemented.h"
 #include "third_party/blink/renderer/modules/peerconnection/mock_data_channel_impl.h"
@@ -377,7 +378,7 @@ MockPeerConnectionImpl::AddTrack(
       return webrtc::RTCError(webrtc::RTCErrorType::INVALID_PARAMETER);
   }
   for (const auto& stream_id : stream_ids) {
-    if (!std::ranges::contains(local_stream_ids_, stream_id)) {
+    if (!base::Contains(local_stream_ids_, stream_id)) {
       stream_label_ = stream_id;
       local_stream_ids_.push_back(stream_id);
     }
@@ -403,7 +404,7 @@ webrtc::RTCError MockPeerConnectionImpl::RemoveTrackOrError(
     webrtc::scoped_refptr<webrtc::RtpSenderInterface> s) {
   webrtc::scoped_refptr<FakeRtpSender> sender(
       static_cast<FakeRtpSender*>(s.get()));
-  if (!std::ranges::contains(senders_, sender)) {
+  if (!base::Contains(senders_, sender)) {
     return webrtc::RTCError(webrtc::RTCErrorType::INVALID_PARAMETER,
                             "Mock: sender not found in senders");
   }
diff --git a/third_party/blink/renderer/modules/peerconnection/peer_connection_tracker.cc b/third_party/blink/renderer/modules/peerconnection/peer_connection_tracker.cc
index c9191f74f9..f510f9a92b 100644
--- a/third_party/blink/renderer/modules/peerconnection/peer_connection_tracker.cc
+++ b/third_party/blink/renderer/modules/peerconnection/peer_connection_tracker.cc
@@ -14,6 +14,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/types/pass_key.h"
 #include "base/values.h"
diff --git a/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection_handler.cc b/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection_handler.cc
index 388c257295..ad7dceafe0 100644
--- a/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection_handler.cc
+++ b/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection_handler.cc
@@ -6,7 +6,6 @@
 
 #include <string.h>
 
-#include <algorithm>
 #include <functional>
 #include <memory>
 #include <set>
@@ -15,6 +14,7 @@
 #include <vector>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/location.h"
 #include "base/logging.h"
 #include "base/memory/scoped_refptr.h"
@@ -2010,7 +2010,7 @@ void RTCPeerConnectionHandler::OnModifyTransceivers(
   // removed transceivers are reflected as "stopped" in JavaScript.
   Vector<uintptr_t> removed_transceivers;
   for (auto transceiver_id : previous_transceiver_ids_) {
-    if (!std::ranges::contains(ids, transceiver_id)) {
+    if (!base::Contains(ids, transceiver_id)) {
       removed_transceivers.emplace_back(transceiver_id);
       rtp_transceivers_.erase(FindTransceiver(transceiver_id));
     }
diff --git a/third_party/blink/renderer/modules/plugins/dom_mime_type_array.cc b/third_party/blink/renderer/modules/plugins/dom_mime_type_array.cc
index 662db05099..3aa911201f 100644
--- a/third_party/blink/renderer/modules/plugins/dom_mime_type_array.cc
+++ b/third_party/blink/renderer/modules/plugins/dom_mime_type_array.cc
@@ -20,8 +20,7 @@
 
 #include "third_party/blink/renderer/modules/plugins/dom_mime_type_array.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
@@ -76,8 +75,7 @@ void DOMMimeTypeArray::NamedPropertyEnumerator(Vector<String>& property_names,
 
 bool DOMMimeTypeArray::NamedPropertyQuery(const AtomicString& property_name,
                                           ExceptionState&) const {
-  return std::ranges::contains(dom_mime_types_, property_name,
-                               &DOMMimeType::type);
+  return base::Contains(dom_mime_types_, property_name, &DOMMimeType::type);
 }
 
 }  // namespace blink
diff --git a/third_party/blink/renderer/modules/screen_details/screen_details.cc b/third_party/blink/renderer/modules/screen_details/screen_details.cc
index cce3fa3395..b91543477b 100644
--- a/third_party/blink/renderer/modules/screen_details/screen_details.cc
+++ b/third_party/blink/renderer/modules/screen_details/screen_details.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/modules/screen_details/screen_details.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/core/dom/events/event.h"
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
@@ -79,8 +78,8 @@ void ScreenDetails::UpdateScreenInfosImpl(LocalDOMWindow* window,
   // Check if any screens have been removed and remove them from `screens_`.
   for (wtf_size_t i = 0; i < screens_.size();
        /*conditionally incremented*/) {
-    if (std::ranges::contains(new_infos.screen_infos, screens_[i]->DisplayId(),
-                              &display::ScreenInfo::display_id)) {
+    if (base::Contains(new_infos.screen_infos, screens_[i]->DisplayId(),
+                       &display::ScreenInfo::display_id)) {
       ++i;
     } else {
       screens_.EraseAt(i);
@@ -91,8 +90,8 @@ void ScreenDetails::UpdateScreenInfosImpl(LocalDOMWindow* window,
 
   // Check if any screens have been added, and append them to `screens_`.
   for (const auto& info : new_infos.screen_infos) {
-    if (!std::ranges::contains(screens_, info.display_id,
-                               &ScreenDetailed::DisplayId)) {
+    if (!base::Contains(screens_, info.display_id,
+                        &ScreenDetailed::DisplayId)) {
       screens_.push_back(
           MakeGarbageCollected<ScreenDetailed>(window, info.display_id));
       added_or_removed = true;
diff --git a/third_party/blink/renderer/modules/service_worker/service_worker_event_queue.cc b/third_party/blink/renderer/modules/service_worker/service_worker_event_queue.cc
index 4fc9f8e581..f486f903de 100644
--- a/third_party/blink/renderer/modules/service_worker/service_worker_event_queue.cc
+++ b/third_party/blink/renderer/modules/service_worker/service_worker_event_queue.cc
@@ -4,6 +4,7 @@
 
 #include "third_party/blink/renderer/modules/service_worker/service_worker_event_queue.h"
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/task/sequenced_task_runner.h"
 #include "base/time/default_tick_clock.h"
diff --git a/third_party/blink/renderer/modules/service_worker/thread_safe_script_container.cc b/third_party/blink/renderer/modules/service_worker/thread_safe_script_container.cc
index 8c859eccf8..81cda72590 100644
--- a/third_party/blink/renderer/modules/service_worker/thread_safe_script_container.cc
+++ b/third_party/blink/renderer/modules/service_worker/thread_safe_script_container.cc
@@ -4,6 +4,7 @@
 
 #include "third_party/blink/renderer/modules/service_worker/thread_safe_script_container.h"
 
+#include "base/containers/contains.h"
 #include "base/memory/ptr_util.h"
 
 namespace blink {
diff --git a/third_party/blink/renderer/modules/webcodecs/audio_encoder.cc b/third_party/blink/renderer/modules/webcodecs/audio_encoder.cc
index c863cd794d..5c467e4fa4 100644
--- a/third_party/blink/renderer/modules/webcodecs/audio_encoder.cc
+++ b/third_party/blink/renderer/modules/webcodecs/audio_encoder.cc
@@ -4,10 +4,10 @@
 
 #include "third_party/blink/renderer/modules/webcodecs/audio_encoder.h"
 
-#include <algorithm>
 #include <cinttypes>
 #include <limits>
 
+#include "base/containers/contains.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/strings/to_string.h"
 #include "base/trace_event/common/trace_event_common.h"
@@ -57,7 +57,7 @@ bool VerifyParameterValues(const T& value,
                            String error_message_base_base,
                            Vector<T> supported_values,
                            String* js_error_message) {
-  if (std::ranges::contains(supported_values, value)) {
+  if (base::Contains(supported_values, value)) {
     return true;
   }
 
diff --git a/third_party/blink/renderer/modules/webcodecs/video_encoder.cc b/third_party/blink/renderer/modules/webcodecs/video_encoder.cc
index 0d745e1706..b845f94a76 100644
--- a/third_party/blink/renderer/modules/webcodecs/video_encoder.cc
+++ b/third_party/blink/renderer/modules/webcodecs/video_encoder.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/format_macros.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
@@ -243,8 +244,8 @@ media::EncoderStatus IsAcceleratedConfigurationSupported(
     }
 
     if (options.scalability_mode.has_value() &&
-        !std::ranges::contains(supported_profile.scalability_modes,
-                               options.scalability_mode.value())) {
+        !base::Contains(supported_profile.scalability_modes,
+                        options.scalability_mode.value())) {
       continue;
     }
 
diff --git a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
index e3f40494b3..4bd5484ffc 100644
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -31,6 +31,7 @@
 #include "base/bit_cast.h"
 #include "base/byte_size.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/metrics/histogram_functions.h"
diff --git a/third_party/blink/renderer/modules/webgpu/gpu.cc b/third_party/blink/renderer/modules/webgpu/gpu.cc
index 4aae58a3c6..0f5332277c 100644
--- a/third_party/blink/renderer/modules/webgpu/gpu.cc
+++ b/third_party/blink/renderer/modules/webgpu/gpu.cc
@@ -206,9 +206,8 @@ void GPU::OnRequestAdapterCallback(
 
   // wgpu::RequestAdapterStatus is part of the stable API, so is safe to log to histograms.
   // The macro + `to_underlying` converts the enum to an int to calculate the max range.
-  UMA_HISTOGRAM_ENUMERATION(
-      "GPU.RequestAdapterStatus.WebGPU", status,
-      std::to_underlying(wgpu::RequestAdapterStatus::Error) + 1);
+  UMA_HISTOGRAM_ENUMERATION("GPU.RequestAdapterStatus.WebGPU", status,
+                            base::to_underlying(wgpu::RequestAdapterStatus::Error) + 1);
 
   switch (status) {
     case wgpu::RequestAdapterStatus::Success:
diff --git a/third_party/blink/renderer/modules/webrtc/webrtc_audio_device_impl.cc b/third_party/blink/renderer/modules/webrtc/webrtc_audio_device_impl.cc
index dfb5d18956..c82a4e0316 100644
--- a/third_party/blink/renderer/modules/webrtc/webrtc_audio_device_impl.cc
+++ b/third_party/blink/renderer/modules/webrtc/webrtc_audio_device_impl.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/modules/webrtc/webrtc_audio_device_impl.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/strings/stringprintf.h"
@@ -386,7 +385,7 @@ void WebRtcAudioDeviceImpl::AddAudioCapturer(
   DCHECK(!capturer->device().id.empty());
 
   base::AutoLock auto_lock(lock_);
-  DCHECK(!std::ranges::contains(capturers_, capturer));
+  DCHECK(!base::Contains(capturers_, capturer));
   capturers_.push_back(capturer);
   capturer->SetOutputDeviceForAec(output_device_id_for_aec_.Utf8());
 }
@@ -406,7 +405,7 @@ void WebRtcAudioDeviceImpl::AddPlayoutSink(
   DVLOG(1) << "WebRtcAudioDeviceImpl::AddPlayoutSink()";
   DCHECK(sink);
   base::AutoLock auto_lock(lock_);
-  DCHECK(!std::ranges::contains(playout_sinks_, sink));
+  DCHECK(!base::Contains(playout_sinks_, sink));
   playout_sinks_.push_back(sink);
 }
 
diff --git a/third_party/blink/renderer/modules/webrtc/webrtc_audio_renderer.cc b/third_party/blink/renderer/modules/webrtc/webrtc_audio_renderer.cc
index 632689b40a..e8818d6f10 100644
--- a/third_party/blink/renderer/modules/webrtc/webrtc_audio_renderer.cc
+++ b/third_party/blink/renderer/modules/webrtc/webrtc_audio_renderer.cc
@@ -8,6 +8,7 @@
 #include <utility>
 #include <vector>
 
+#include "base/containers/contains.h"
 #include "base/location.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_macros.h"
@@ -713,7 +714,7 @@ bool WebRtcAudioRenderer::AddPlayingState(webrtc::AudioSourceInterface* source,
   DCHECK(state->playing());
   // Look up or add the |source| to the map.
   PlayingStates& array = source_playing_states_[source];
-  if (std::ranges::contains(array, state))
+  if (base::Contains(array, state))
     return false;
 
   array.push_back(state);
diff --git a/third_party/blink/renderer/modules/xr/xr_plane_manager.cc b/third_party/blink/renderer/modules/xr/xr_plane_manager.cc
index 2556a41a02..5b32724f89 100644
--- a/third_party/blink/renderer/modules/xr/xr_plane_manager.cc
+++ b/third_party/blink/renderer/modules/xr/xr_plane_manager.cc
@@ -4,6 +4,7 @@
 
 #include "third_party/blink/renderer/modules/xr/xr_plane_manager.h"
 
+#include "base/containers/contains.h"
 #include "base/trace_event/trace_event.h"
 #include "third_party/blink/renderer/modules/xr/xr_plane.h"
 #include "third_party/blink/renderer/modules/xr/xr_plane_set.h"
diff --git a/third_party/blink/renderer/modules/xr/xr_render_state.cc b/third_party/blink/renderer/modules/xr/xr_render_state.cc
index 4317c3616f..460010d731 100644
--- a/third_party/blink/renderer/modules/xr/xr_render_state.cc
+++ b/third_party/blink/renderer/modules/xr/xr_render_state.cc
@@ -10,6 +10,7 @@
 #include <cmath>
 #include <ranges>
 
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_xr_render_state_init.h"
 #include "third_party/blink/renderer/modules/xr/xr_composition_layer.h"
 #include "third_party/blink/renderer/modules/xr/xr_frame_provider.h"
@@ -183,7 +184,7 @@ void XRRenderState::UpdateLayersState(FrozenArray<XRLayer>* layers) {
                                        const FrozenArray<XRLayer>& other_layers,
                                        bool needs_redraw) {
     std::ranges::for_each(source_layers, [&](auto& source_layer) {
-      if (!std::ranges::contains(other_layers, source_layer)) {
+      if (!base::Contains(other_layers, source_layer)) {
         source_layer->SetNeedsRedraw(needs_redraw);
       }
     });
@@ -237,7 +238,7 @@ bool XRRenderState::HasLayer(XRLayer* layer) const {
     return true;
   }
   if (layers_) {
-    return std::ranges::contains(*layers_, layer);
+    return base::Contains(*layers_, layer);
   }
   return false;
 }
diff --git a/third_party/blink/renderer/modules/xr/xr_session.cc b/third_party/blink/renderer/modules/xr/xr_session.cc
index 8f4add9f5f..c9f540f486 100644
--- a/third_party/blink/renderer/modules/xr/xr_session.cc
+++ b/third_party/blink/renderer/modules/xr/xr_session.cc
@@ -10,6 +10,7 @@
 #include <utility>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/dcheck_is_on.h"
 #include "base/metrics/histogram_macros.h"
 #include "base/trace_event/trace_event.h"
diff --git a/third_party/blink/renderer/modules/xr/xr_system.cc b/third_party/blink/renderer/modules/xr/xr_system.cc
index 4ad2f0739d..c99a90db75 100644
--- a/third_party/blink/renderer/modules/xr/xr_system.cc
+++ b/third_party/blink/renderer/modules/xr/xr_system.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/trace_event/trace_id_helper.h"
 #include "base/trace_event/typed_macros.h"
@@ -1494,8 +1495,8 @@ void XRSystem::OnRequestSessionReturned(
   Element* fullscreen_element = nullptr;
   const auto& enabled_features =
       result->get_success()->session->enabled_features;
-  if (std::ranges::contains(enabled_features,
-                            device::mojom::XRSessionFeature::DOM_OVERLAY)) {
+  if (base::Contains(enabled_features,
+                     device::mojom::XRSessionFeature::DOM_OVERLAY)) {
     fullscreen_element = query->DOMOverlayElement();
   }
 
@@ -1519,7 +1520,7 @@ void XRSystem::OnRequestSessionReturned(
     return;
   }
 
-  const bool session_has_camera_access = std::ranges::contains(
+  const bool session_has_camera_access = base::Contains(
       enabled_features, device::mojom::XRSessionFeature::CAMERA_ACCESS);
 
   // At this point, we know that we have an element that we need to make
diff --git a/third_party/blink/renderer/modules/xr/xr_view.cc b/third_party/blink/renderer/modules/xr/xr_view.cc
index 33f50e9726..dd5d6b0682 100644
--- a/third_party/blink/renderer/modules/xr/xr_view.cc
+++ b/third_party/blink/renderer/modules/xr/xr_view.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/trace_event/trace_event.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_xr_eye.h"
 #include "third_party/blink/renderer/modules/xr/xr_camera.h"
diff --git a/third_party/blink/renderer/platform/disk_data_allocator.cc b/third_party/blink/renderer/platform/disk_data_allocator.cc
index 7259cb65c2..a9ab1028fc 100644
--- a/third_party/blink/renderer/platform/disk_data_allocator.cc
+++ b/third_party/blink/renderer/platform/disk_data_allocator.cc
@@ -8,6 +8,7 @@
 #include <utility>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/synchronization/lock.h"
 #include "base/threading/thread_restrictions.h"
@@ -76,7 +77,7 @@ DiskDataMetadata DiskDataAllocator::FindFreeChunk(size_t size) {
 
 void DiskDataAllocator::ReleaseChunk(const DiskDataMetadata& metadata) {
   DiskDataMetadata chunk = metadata;
-  DCHECK(!free_chunks_.contains(chunk.start_offset()));
+  DCHECK(!base::Contains(free_chunks_, chunk.start_offset()));
 
   auto lower_bound = free_chunks_.lower_bound(chunk.start_offset());
   DCHECK(free_chunks_.upper_bound(chunk.start_offset()) ==
diff --git a/third_party/blink/renderer/platform/exported/file_path_conversion.cc b/third_party/blink/renderer/platform/exported/file_path_conversion.cc
index e0065d7502..867411bce1 100644
--- a/third_party/blink/renderer/platform/exported/file_path_conversion.cc
+++ b/third_party/blink/renderer/platform/exported/file_path_conversion.cc
@@ -7,6 +7,7 @@
 #include <string_view>
 
 #include "base/files/file_path.h"
+#include "base/strings/string_view_util.h"
 #include "build/build_config.h"
 #include "third_party/blink/public/platform/web_string.h"
 #include "third_party/blink/renderer/platform/wtf/text/string_utf8_adaptor.h"
@@ -19,7 +20,7 @@ base::FilePath StringViewToFilePath(const StringView& str) {
     return base::FilePath();
   }
   if (!str.Is8Bit()) {
-    return base::FilePath::FromUTF16Unsafe(std::u16string_view(str.Span16()));
+    return base::FilePath::FromUTF16Unsafe(base::as_string_view(str.Span16()));
   }
 #if BUILDFLAG(IS_POSIX) || BUILDFLAG(IS_FUCHSIA)
   StringUtf8Adaptor utf8(str);
diff --git a/third_party/blink/renderer/platform/fonts/font_description.cc b/third_party/blink/renderer/platform/fonts/font_description.cc
index 3b6a3a0c6c..49acecf861 100644
--- a/third_party/blink/renderer/platform/fonts/font_description.cc
+++ b/third_party/blink/renderer/platform/fonts/font_description.cc
@@ -32,6 +32,7 @@
 #include <set>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/memory/values_equivalent.h"
 #include "base/notreached.h"
 #include "base/strings/to_string.h"
@@ -559,7 +560,7 @@ ResolvedFontFeatures FontDescription::ResolveFontFeatures() const {
     const wtf_size_t resolved_size = merged_features.size();
     merged_features.reserve(resolved_size + resolved_font_features_.size());
     for (const auto& descriptor_feature : resolved_font_features_) {
-      if (!std::ranges::contains(
+      if (!base::Contains(
               base::span{merged_features}.first(resolved_size),
               descriptor_feature.tag, &FontFeatureValue::tag)) {
         merged_features.emplace_back(descriptor_feature);
diff --git a/third_party/blink/renderer/platform/fonts/opentype/open_type_types.h b/third_party/blink/renderer/platform/fonts/opentype/open_type_types.h
index f75194fc7e..4e38ff8a6e 100644
--- a/third_party/blink/renderer/platform/fonts/opentype/open_type_types.h
+++ b/third_party/blink/renderer/platform/fonts/opentype/open_type_types.h
@@ -25,8 +25,6 @@
 #ifndef THIRD_PARTY_BLINK_RENDERER_PLATFORM_FONTS_OPENTYPE_OPEN_TYPE_TYPES_H_
 #define THIRD_PARTY_BLINK_RENDERER_PLATFORM_FONTS_OPENTYPE_OPEN_TYPE_TYPES_H_
 
-#include <bit>
-
 #include "base/compiler_specific.h"
 #include "base/numerics/byte_conversions.h"
 #include "third_party/blink/renderer/platform/wtf/vector.h"
@@ -36,29 +34,29 @@ namespace open_type {
 
 struct Int16 {
   DISALLOW_NEW();
-  Int16(int16_t u) : v(std::byteswap(u)) {}
-  operator int16_t() const { return std::byteswap(v); }
+  Int16(int16_t u) : v(base::ByteSwap(u)) {}
+  operator int16_t() const { return base::ByteSwap(v); }
   int16_t v;  // in BigEndian
 };
 
 struct UInt16 {
   DISALLOW_NEW();
-  UInt16(uint16_t u) : v(std::byteswap(u)) {}
-  operator uint16_t() const { return std::byteswap(v); }
+  UInt16(uint16_t u) : v(base::ByteSwap(u)) {}
+  operator uint16_t() const { return base::ByteSwap(v); }
   uint16_t v;  // in BigEndian
 };
 
 struct Int32 {
   DISALLOW_NEW();
-  Int32(int32_t u) : v(std::byteswap(u)) {}
-  operator int32_t() const { return std::byteswap(v); }
+  Int32(int32_t u) : v(base::ByteSwap(u)) {}
+  operator int32_t() const { return base::ByteSwap(v); }
   int32_t v;  // in BigEndian
 };
 
 struct UInt32 {
   DISALLOW_NEW();
-  UInt32(uint32_t u) : v(std::byteswap(u)) {}
-  operator uint32_t() const { return std::byteswap(v); }
+  UInt32(uint32_t u) : v(base::ByteSwap(u)) {}
+  operator uint32_t() const { return base::ByteSwap(v); }
   uint32_t v;  // in BigEndian
 };
 
diff --git a/third_party/blink/renderer/platform/fonts/script_run_iterator.cc b/third_party/blink/renderer/platform/fonts/script_run_iterator.cc
index 511d5f9ca4..a3c0405c2a 100644
--- a/third_party/blink/renderer/platform/fonts/script_run_iterator.cc
+++ b/third_party/blink/renderer/platform/fonts/script_run_iterator.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/notreached.h"
 #include "third_party/blink/renderer/platform/runtime_enabled_features.h"
@@ -376,7 +377,7 @@ bool ScriptRunIterator::MergeSets() {
 
   // Neither is common or inherited. If current is a singleton,
   // just see if it exists in the next set. This is the common case.
-  bool have_priority = std::ranges::contains(*next_set_, priority_script);
+  bool have_priority = base::Contains(*next_set_, priority_script);
   if (current_set_it == current_end) {
     return have_priority;
   }
diff --git a/third_party/blink/renderer/platform/fonts/simple_font_data.cc b/third_party/blink/renderer/platform/fonts/simple_font_data.cc
index 2fa1abf402..39f8e79a0f 100644
--- a/third_party/blink/renderer/platform/fonts/simple_font_data.cc
+++ b/third_party/blink/renderer/platform/fonts/simple_font_data.cc
@@ -32,12 +32,12 @@
 #include <unicode/utf16.h>
 
 #include <algorithm>
-#include <bit>
 #include <memory>
 #include <utility>
 
 #include "base/compiler_specific.h"
 #include "base/memory/ptr_util.h"
+#include "base/numerics/byte_conversions.h"
 #include "build/build_config.h"
 #include "skia/ext/font_utils.h"
 #include "third_party/blink/renderer/platform/font_family_names.h"
@@ -367,7 +367,8 @@ static std::pair<int16_t, int16_t> TypoAscenderAndDescender(
                                        sizeof(buffer), buffer);
   if (size == sizeof(buffer)) {
     // The buffer values are in big endian.
-    return std::make_pair(std::byteswap(buffer[0]), -std::byteswap(buffer[1]));
+    return std::make_pair(base::ByteSwap(buffer[0]),
+                          -base::ByteSwap(buffer[1]));
   }
   return std::make_pair(0, 0);
 }
diff --git a/third_party/blink/renderer/platform/graphics/paint_worklet_paint_dispatcher.cc b/third_party/blink/renderer/platform/graphics/paint_worklet_paint_dispatcher.cc
index adc6575157..a92519a77b 100644
--- a/third_party/blink/renderer/platform/graphics/paint_worklet_paint_dispatcher.cc
+++ b/third_party/blink/renderer/platform/graphics/paint_worklet_paint_dispatcher.cc
@@ -7,6 +7,7 @@
 #include <utility>
 
 #include "base/barrier_closure.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/synchronization/waitable_event.h"
 #include "base/task/single_thread_task_runner.h"
diff --git a/third_party/blink/renderer/platform/loader/cors/cors.cc b/third_party/blink/renderer/platform/loader/cors/cors.cc
index 53fadafe5e..1bb8403609 100644
--- a/third_party/blink/renderer/platform/loader/cors/cors.cc
+++ b/third_party/blink/renderer/platform/loader/cors/cors.cc
@@ -6,6 +6,7 @@
 
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/functional/function_ref.h"
 #include "net/http/http_util.h"
 #include "services/network/public/cpp/cors/cors.h"
@@ -196,7 +197,7 @@ HTTPHeaderSet ExtractCorsExposedHeaderNamesList(
   parser.Parse(header_set);
 
   if (credentials_mode != network::mojom::CredentialsMode::kInclude &&
-      header_set.contains("*")) {
+      base::Contains(header_set, "*")) {
     header_set.clear();
     for (const auto& header : response.HttpHeaderFields())
       header_set.insert(header.key.Ascii());
@@ -218,7 +219,7 @@ bool IsCorsSafelistedResponseHeader(const String& name) {
                                       "last-modified",
                                       "pragma",
                                   }));
-  return allowed_cross_origin_response_headers.contains(name.Ascii());
+  return base::Contains(allowed_cross_origin_response_headers, name.Ascii());
 }
 
 // In the spec, https://fetch.spec.whatwg.org/#ref-for-concept-request-mode,
diff --git a/third_party/blink/renderer/platform/loader/fetch/resource_fetcher.cc b/third_party/blink/renderer/platform/loader/fetch/resource_fetcher.cc
index f7082144a3..90144907e6 100644
--- a/third_party/blink/renderer/platform/loader/fetch/resource_fetcher.cc
+++ b/third_party/blink/renderer/platform/loader/fetch/resource_fetcher.cc
@@ -35,6 +35,7 @@
 #include <utility>
 
 #include "base/auto_reset.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
@@ -3365,8 +3366,7 @@ bool ResourceFetcher::IsPotentiallyUnusedPreload(
       break;
   }
 
-  return std::ranges::contains(context_->GetPotentiallyUnusedPreloads(),
-                               params.Url());
+  return base::Contains(context_->GetPotentiallyUnusedPreloads(), params.Url());
 }
 
 void ResourceFetcher::Trace(Visitor* visitor) const {
diff --git a/third_party/blink/renderer/platform/loader/fetch/resource_load_scheduler.cc b/third_party/blink/renderer/platform/loader/fetch/resource_load_scheduler.cc
index 33a5e46bc0..ed5a6c1e0c 100644
--- a/third_party/blink/renderer/platform/loader/fetch/resource_load_scheduler.cc
+++ b/third_party/blink/renderer/platform/loader/fetch/resource_load_scheduler.cc
@@ -8,6 +8,7 @@
 #include <memory>
 #include <string>
 
+#include "base/containers/contains.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/metrics/histogram.h"
 #include "base/numerics/safe_conversions.h"
diff --git a/third_party/blink/renderer/platform/loader/fetch/resource_response.cc b/third_party/blink/renderer/platform/loader/fetch/resource_response.cc
index 9ceba4edeb..0a95a2df86 100644
--- a/third_party/blink/renderer/platform/loader/fetch/resource_response.cc
+++ b/third_party/blink/renderer/platform/loader/fetch/resource_response.cc
@@ -29,6 +29,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/containers/fixed_flat_set.h"
 #include "base/memory/scoped_refptr.h"
 #include "net/http/structured_headers.h"
diff --git a/third_party/blink/renderer/platform/loader/fetch/resource_timing_utils.cc b/third_party/blink/renderer/platform/loader/fetch/resource_timing_utils.cc
index eaf288fba3..3c3dde9830 100644
--- a/third_party/blink/renderer/platform/loader/fetch/resource_timing_utils.cc
+++ b/third_party/blink/renderer/platform/loader/fetch/resource_timing_utils.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/platform/loader/fetch/resource_timing_utils.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "base/time/time.h"
 #include "resource_response.h"
@@ -60,7 +59,7 @@ mojom::blink::ResourceTimingInfoPtr CreateResourceTimingInfo(
         response->HttpHeaderField(http_names::kServerTiming));
     info->cache_state = response->CacheState();
     // Use SecurityOrigin::Create to handle cases like blob:https://.
-    info->is_secure_transport = std::ranges::contains(
+    info->is_secure_transport = base::Contains(
         url::GetSecureSchemes(),
         SecurityOrigin::Create(response->ResponseUrl())->Protocol().Ascii());
     info->timing = response->GetResourceLoadTiming()
diff --git a/third_party/blink/renderer/platform/media/cache_util.cc b/third_party/blink/renderer/platform/media/cache_util.cc
index b9859633b5..fc2967d1a7 100644
--- a/third_party/blink/renderer/platform/media/cache_util.cc
+++ b/third_party/blink/renderer/platform/media/cache_util.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <string_view>
 
+#include "base/containers/contains.h"
 #include "base/strings/string_number_conversions.h"
 #include "base/strings/string_util.h"
 #include "base/time/time.h"
@@ -49,15 +50,15 @@ uint32_t GetReasonsForUncacheability(const WebURLResponse& response) {
   std::string cache_control_header =
       base::ToLowerASCII(response.HttpHeaderField("cache-control").Utf8());
 
-  if (cache_control_header.contains("no-cache")) {
+  if (base::Contains(cache_control_header, "no-cache")) {
     reasons |= kNoCache;
   }
 
-  if (cache_control_header.contains("no-store")) {
+  if (base::Contains(cache_control_header, "no-store")) {
     reasons |= kNoStore;
   }
 
-  if (cache_control_header.contains("must-revalidate")) {
+  if (base::Contains(cache_control_header, "must-revalidate")) {
     reasons |= kHasMustRevalidate;
   }
 
@@ -92,8 +93,8 @@ base::TimeDelta GetCacheValidUntil(const WebURLResponse& response) {
   std::string cache_control_header =
       base::ToLowerASCII(response.HttpHeaderField("cache-control").Utf8());
 
-  if (cache_control_header.contains("no-cache") ||
-      cache_control_header.contains("must-revalidate")) {
+  if (base::Contains(cache_control_header, "no-cache") ||
+      base::Contains(cache_control_header, "must-revalidate")) {
     return base::TimeDelta();
   }
 
diff --git a/third_party/blink/renderer/platform/media/cdm_session_adapter.cc b/third_party/blink/renderer/platform/media/cdm_session_adapter.cc
index 2071827a30..490c917cc1 100644
--- a/third_party/blink/renderer/platform/media/cdm_session_adapter.cc
+++ b/third_party/blink/renderer/platform/media/cdm_session_adapter.cc
@@ -7,6 +7,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram.h"
 #include "base/metrics/histogram_functions.h"
@@ -81,7 +82,7 @@ bool CdmSessionAdapter::RegisterSession(
     const std::string& session_id,
     base::WeakPtr<WebContentDecryptionModuleSessionImpl> session) {
   // If this session ID is already registered, don't register it again.
-  if (sessions_.contains(session_id))
+  if (base::Contains(sessions_, session_id))
     return false;
 
   sessions_[session_id] = session;
@@ -89,7 +90,7 @@ bool CdmSessionAdapter::RegisterSession(
 }
 
 void CdmSessionAdapter::UnregisterSession(const std::string& session_id) {
-  DCHECK(sessions_.contains(session_id));
+  DCHECK(base::Contains(sessions_, session_id));
   sessions_.erase(session_id);
 }
 
diff --git a/third_party/blink/renderer/platform/media/multi_buffer.cc b/third_party/blink/renderer/platform/media/multi_buffer.cc
index a44fe6b962..d1218e74d5 100644
--- a/third_party/blink/renderer/platform/media/multi_buffer.cc
+++ b/third_party/blink/renderer/platform/media/multi_buffer.cc
@@ -6,11 +6,13 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/location.h"
 #include "base/logging.h"
 #include "base/memory/raw_ptr.h"
 #include "base/task/single_thread_task_runner.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "media/base/media_switches.h"
 #include "third_party/blink/renderer/platform/scheduler/public/post_cross_thread_task.h"
 #include "third_party/blink/renderer/platform/wtf/cross_thread_functional.h"
@@ -121,7 +123,7 @@ void MultiBuffer::GlobalLRU::SchedulePrune() {
         *task_runner_, FROM_HERE,
         CrossThreadBindOnce(&MultiBuffer::GlobalLRU::PruneTask,
                             blink::RetainedRef(this)),
-        base::Seconds(std::to_underlying(kBlockPruneInterval)));
+        base::Seconds(base::to_underlying(kBlockPruneInterval)));
     background_pruning_pending_ = true;
   }
 }
diff --git a/third_party/blink/renderer/platform/media/new_session_cdm_result_promise.cc b/third_party/blink/renderer/platform/media/new_session_cdm_result_promise.cc
index 753bb03dd9..bcf6a5909f 100644
--- a/third_party/blink/renderer/platform/media/new_session_cdm_result_promise.cc
+++ b/third_party/blink/renderer/platform/media/new_session_cdm_result_promise.cc
@@ -4,9 +4,9 @@
 
 #include "third_party/blink/renderer/platform/media/new_session_cdm_result_promise.h"
 
-#include <algorithm>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/metrics/histogram_functions.h"
 #include "third_party/blink/public/platform/web_string.h"
@@ -75,7 +75,7 @@ void NewSessionCdmResultPromise::resolve(const std::string& session_id) {
   SessionInitStatus status = SessionInitStatus::UNKNOWN_STATUS;
   std::move(new_session_created_cb_).Run(session_id, &status);
 
-  if (!std::ranges::contains(expected_statuses_, status)) {
+  if (!base::Contains(expected_statuses_, status)) {
     reject(Exception::INVALID_STATE_ERROR, 0,
            "Cannot finish session initialization");
     return;
diff --git a/third_party/blink/renderer/platform/media/resource_multi_buffer_data_provider.cc b/third_party/blink/renderer/platform/media/resource_multi_buffer_data_provider.cc
index 61b798d0b3..0ed0cc8e68 100644
--- a/third_party/blink/renderer/platform/media/resource_multi_buffer_data_provider.cc
+++ b/third_party/blink/renderer/platform/media/resource_multi_buffer_data_provider.cc
@@ -9,6 +9,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/location.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -283,7 +284,7 @@ void ResourceMultiBufferDataProvider::DidReceiveResponse(
     // Check to see whether the server supports byte ranges.
     std::string accept_ranges =
         response.HttpHeaderField("Accept-Ranges").Utf8();
-    if (accept_ranges.contains("bytes")) {
+    if (base::Contains(accept_ranges, "bytes")) {
       destination_url_data->set_range_supported();
     }
 
diff --git a/third_party/blink/renderer/platform/media/video_decode_stats_reporter.cc b/third_party/blink/renderer/platform/media/video_decode_stats_reporter.cc
index 103992691c..29aaf89b4e 100644
--- a/third_party/blink/renderer/platform/media/video_decode_stats_reporter.cc
+++ b/third_party/blink/renderer/platform/media/video_decode_stats_reporter.cc
@@ -9,6 +9,7 @@
 
 #include "base/logging.h"
 #include "base/task/single_thread_task_runner.h"
+#include "base/types/cxx23_to_underlying.h"
 #include "media/capabilities/bucket_utility.h"
 #include "media/mojo/mojom/media_types.mojom-blink.h"
 #include "media/mojo/mojom/video_decode_stats_recorder.mojom-blink.h"
@@ -26,9 +27,9 @@ VideoDecodeStatsReporter::VideoDecodeStatsReporter(
     scoped_refptr<base::SingleThreadTaskRunner> task_runner,
     const base::TickClock* tick_clock)
     : kRecordingInterval(
-          base::Milliseconds(std::to_underlying(kRecordingIntervalMs))),
+          base::Milliseconds(base::to_underlying(kRecordingIntervalMs))),
       kTinyFpsWindowDuration(
-          base::Milliseconds(std::to_underlying(kTinyFpsWindowMs))),
+          base::Milliseconds(base::to_underlying(kTinyFpsWindowMs))),
       recorder_remote_(std::move(recorder_remote)),
       get_pipeline_stats_cb_(std::move(get_pipeline_stats_cb)),
       codec_profile_(codec_profile),
diff --git a/third_party/blink/renderer/platform/mediastream/media_stream_audio_deliverer.h b/third_party/blink/renderer/platform/mediastream/media_stream_audio_deliverer.h
index c6b7fd368d..0d9f42c562 100644
--- a/third_party/blink/renderer/platform/mediastream/media_stream_audio_deliverer.h
+++ b/third_party/blink/renderer/platform/mediastream/media_stream_audio_deliverer.h
@@ -9,6 +9,7 @@
 
 #include <algorithm>
 
+#include "base/containers/contains.h"
 #include "base/synchronization/lock.h"
 #include "base/threading/thread_checker.h"
 #include "base/trace_event/trace_event.h"
@@ -57,8 +58,8 @@ class MediaStreamAudioDeliverer {
     DCHECK_CALLED_ON_VALID_THREAD(thread_checker_);
     DCHECK(consumer);
     base::AutoLock auto_lock(consumers_lock_);
-    DCHECK(!std::ranges::contains(consumers_, consumer));
-    DCHECK(!std::ranges::contains(pending_consumers_, consumer));
+    DCHECK(!base::Contains(consumers_, consumer));
+    DCHECK(!base::Contains(pending_consumers_, consumer));
     pending_consumers_.push_back(consumer);
     SendLogMessage(
         String::Format("%s => (number of consumer: active=%u, pending=%u)",
diff --git a/third_party/blink/renderer/platform/mhtml/mhtml_archive.cc b/third_party/blink/renderer/platform/mhtml/mhtml_archive.cc
index 9e3d9fc109..9a1ba6268b 100644
--- a/third_party/blink/renderer/platform/mhtml/mhtml_archive.cc
+++ b/third_party/blink/renderer/platform/mhtml/mhtml_archive.cc
@@ -32,8 +32,7 @@
 
 #include <stddef.h>
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/i18n/time_formatting.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/strings/stringprintf.h"
@@ -270,7 +269,7 @@ bool MHTMLArchive::CanLoadArchive(const KURL& url) {
   // MHTML pages can only be loaded from local URLs, http/https URLs, and
   // content URLs(Android specific).  The latter is now allowed due to full
   // sandboxing enforcement on MHTML pages.
-  if (std::ranges::contains(url::GetLocalSchemes(), url.Protocol().Ascii()))
+  if (base::Contains(url::GetLocalSchemes(), url.Protocol().Ascii()))
     return true;
   if (url.ProtocolIsInHttpFamily()) {
     return true;
diff --git a/third_party/blink/renderer/platform/mhtml/mhtml_parser.cc b/third_party/blink/renderer/platform/mhtml/mhtml_parser.cc
index b98c8e9237..8909f6024d 100644
--- a/third_party/blink/renderer/platform/mhtml/mhtml_parser.cc
+++ b/third_party/blink/renderer/platform/mhtml/mhtml_parser.cc
@@ -34,6 +34,7 @@
 
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "third_party/blink/renderer/platform/heap/garbage_collected.h"
 #include "third_party/blink/renderer/platform/mhtml/archive_resource.h"
diff --git a/third_party/blink/renderer/platform/network/network_state_notifier.cc b/third_party/blink/renderer/platform/network/network_state_notifier.cc
index ed69c14c03..21acf29882 100644
--- a/third_party/blink/renderer/platform/network/network_state_notifier.cc
+++ b/third_party/blink/renderer/platform/network/network_state_notifier.cc
@@ -28,6 +28,7 @@
 #include <array>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/synchronization/lock.h"
 #include "base/task/single_thread_task_runner.h"
 #include "net/nqe/effective_connection_type.h"
diff --git a/third_party/blink/renderer/platform/peerconnection/rtc_stats.cc b/third_party/blink/renderer/platform/peerconnection/rtc_stats.cc
index 5cb09d6fa3..04a3b9a94b 100644
--- a/third_party/blink/renderer/platform/peerconnection/rtc_stats.cc
+++ b/third_party/blink/renderer/platform/peerconnection/rtc_stats.cc
@@ -10,6 +10,7 @@
 #include <string>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/time/time.h"
diff --git a/third_party/blink/renderer/platform/peerconnection/rtc_video_decoder_adapter.cc b/third_party/blink/renderer/platform/peerconnection/rtc_video_decoder_adapter.cc
index b9e357a441..a76b894459 100644
--- a/third_party/blink/renderer/platform/peerconnection/rtc_video_decoder_adapter.cc
+++ b/third_party/blink/renderer/platform/peerconnection/rtc_video_decoder_adapter.cc
@@ -10,6 +10,7 @@
 #include <utility>
 #include <variant>
 
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -497,7 +498,7 @@ void RTCVideoDecoderAdapter::Impl::OnOutput(
     start_time_.reset();
   }
 
-  if (!std::ranges::contains(decode_timestamps_, timestamp)) {
+  if (!base::Contains(decode_timestamps_, timestamp)) {
     DVLOG(2) << "Discarding frame with timestamp " << timestamp;
     return;
   }
diff --git a/third_party/blink/renderer/platform/peerconnection/rtc_video_encoder.cc b/third_party/blink/renderer/platform/peerconnection/rtc_video_encoder.cc
index 20ecc910fa..6dbbb8c05a 100644
--- a/third_party/blink/renderer/platform/peerconnection/rtc_video_encoder.cc
+++ b/third_party/blink/renderer/platform/peerconnection/rtc_video_encoder.cc
@@ -4,7 +4,6 @@
 
 #include "third_party/blink/renderer/platform/peerconnection/rtc_video_encoder.h"
 
-#include <algorithm>
 #include <array>
 #include <memory>
 #include <numeric>
@@ -12,6 +11,7 @@
 
 #include "base/command_line.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/feature_list.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
@@ -1977,7 +1977,7 @@ bool RTCVideoEncoder::Impl::NeedConvertToMemoryFrame(
       STORAGE_OWNED_MEMORY,
       STORAGE_SHMEM,
   };
-  if (!std::ranges::contains(kStorageTypeSupportedByMojo, storage_type)) {
+  if (!base::Contains(kStorageTypeSupportedByMojo, storage_type)) {
     // We need to convert to I420 memory frame if mojo doesn't support it.
     return true;
   }
@@ -2244,8 +2244,8 @@ void RTCVideoEncoder::Impl::EncodeOneFrame(FrameChunk frame_chunk) {
   frame->set_timestamp(timestamp);
 
   if (!failed_timestamp_match_) {
-    DCHECK(!std::ranges::contains(submitted_frames_, timestamp,
-                                  &FrameInfo::media_timestamp_));
+    DCHECK(!base::Contains(submitted_frames_, timestamp,
+                           &FrameInfo::media_timestamp_));
     submitted_frames_.emplace_back(timestamp, frame_chunk.timestamp,
                                    frame_chunk.render_time_ms,
                                    GetActiveSpatialLayers());
@@ -2376,8 +2376,8 @@ void RTCVideoEncoder::Impl::DoNativeEncodeWithNativeInput(
   frame->set_timestamp(base::Microseconds(frame_chunk.timestamp_us));
 
   if (!failed_timestamp_match_) {
-    DCHECK(!std::ranges::contains(submitted_frames_, frame->timestamp(),
-                                  &FrameInfo::media_timestamp_));
+    DCHECK(!base::Contains(submitted_frames_, frame->timestamp(),
+                           &FrameInfo::media_timestamp_));
     submitted_frames_.emplace_back(frame->timestamp(), frame_chunk.timestamp,
                                    frame_chunk.render_time_ms,
                                    GetActiveSpatialLayers());
diff --git a/third_party/blink/renderer/platform/peerconnection/rtc_video_encoder_factory.cc b/third_party/blink/renderer/platform/peerconnection/rtc_video_encoder_factory.cc
index 0fb5cc0638..bbbd2e7fa9 100644
--- a/third_party/blink/renderer/platform/peerconnection/rtc_video_encoder_factory.cc
+++ b/third_party/blink/renderer/platform/peerconnection/rtc_video_encoder_factory.cc
@@ -4,9 +4,9 @@
 
 #include "third_party/blink/renderer/platform/peerconnection/rtc_video_encoder_factory.h"
 
-#include <algorithm>
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/logging.h"
 #include "base/strings/string_util.h"
 #include "build/build_config.h"
@@ -271,7 +271,7 @@ SupportedFormats GetSupportedFormatsInternal(
       continue;
     }
 
-    if (std::ranges::contains(disabled_profiles, profile.profile)) {
+    if (base::Contains(disabled_profiles, profile.profile)) {
       continue;
     }
 
@@ -496,9 +496,8 @@ RTCVideoEncoderFactory::QueryCodecSupport(
               : std::nullopt;
       if (!scalability_mode ||
           (mode.has_value() &&
-           std::ranges::contains(
-               supported_formats.sdp_formats[i].scalability_modes,
-               mode.value()))) {
+           base::Contains(supported_formats.sdp_formats[i].scalability_modes,
+                          mode.value()))) {
         return {/*is_supported=*/true, /*is_power_efficient=*/true};
       }
       break;
diff --git a/third_party/blink/renderer/platform/peerconnection/video_encoder_state_observer_impl.cc b/third_party/blink/renderer/platform/peerconnection/video_encoder_state_observer_impl.cc
index 9e33a6f6a8..dca230c0cc 100644
--- a/third_party/blink/renderer/platform/peerconnection/video_encoder_state_observer_impl.cc
+++ b/third_party/blink/renderer/platform/peerconnection/video_encoder_state_observer_impl.cc
@@ -8,6 +8,7 @@
 
 #include "base/atomic_ref_count.h"
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/sequence_checker.h"
 #include "third_party/webrtc/api/video/encoded_image.h"
 
@@ -141,7 +142,7 @@ void VideoEncoderStateObserverImpl::OnEncoderCreated(
     int encoder_id,
     const webrtc::VideoCodec& config) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(encoder_sequence_);
-  DCHECK(!encoder_state_by_id_.contains(encoder_id));
+  DCHECK(!base::Contains(encoder_state_by_id_, encoder_id));
 
   // Initially, assume all layers active.
   // TODO(hiroh): Set the number of layers to the currently configured layers?
diff --git a/third_party/blink/renderer/platform/peerconnection/webrtc_audio_sink.cc b/third_party/blink/renderer/platform/peerconnection/webrtc_audio_sink.cc
index 7274814e09..0b003298fe 100644
--- a/third_party/blink/renderer/platform/peerconnection/webrtc_audio_sink.cc
+++ b/third_party/blink/renderer/platform/peerconnection/webrtc_audio_sink.cc
@@ -8,6 +8,7 @@
 #include <limits>
 
 #include "base/check_op.h"
+#include "base/containers/contains.h"
 #include "base/functional/callback_helpers.h"
 #include "base/location.h"
 #include "base/memory/scoped_refptr.h"
@@ -249,7 +250,7 @@ void WebRtcAudioSink::Adapter::AddSink(webrtc::AudioTrackSinkInterface* sink) {
   SendLogMessage(
       base::StringPrintf("Adapter::AddSink({label=%s})", label_.c_str()));
   base::AutoLock auto_lock(lock_);
-  DCHECK(!std::ranges::contains(sinks_, sink));
+  DCHECK(!base::Contains(sinks_, sink));
   sinks_.push_back(sink);
 }
 
diff --git a/third_party/blink/renderer/platform/scheduler/worker/worker_thread_scheduler.cc b/third_party/blink/renderer/platform/scheduler/worker/worker_thread_scheduler.cc
index ef9b3a1aa7..3598edfd24 100644
--- a/third_party/blink/renderer/platform/scheduler/worker/worker_thread_scheduler.cc
+++ b/third_party/blink/renderer/platform/scheduler/worker/worker_thread_scheduler.cc
@@ -6,6 +6,7 @@
 
 #include <memory>
 
+#include "base/containers/contains.h"
 #include "base/functional/bind.h"
 #include "base/metrics/field_trial_params.h"
 #include "base/metrics/histogram_macros.h"
diff --git a/third_party/blink/renderer/platform/testing/url_loader_mock_factory_impl.cc b/third_party/blink/renderer/platform/testing/url_loader_mock_factory_impl.cc
index ce1ebce3d0..347e501d79 100644
--- a/third_party/blink/renderer/platform/testing/url_loader_mock_factory_impl.cc
+++ b/third_party/blink/renderer/platform/testing/url_loader_mock_factory_impl.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "base/files/file_util.h"
 #include "base/memory/ptr_util.h"
 #include "base/run_loop.h"
diff --git a/third_party/blink/renderer/platform/weborigin/kurl.cc b/third_party/blink/renderer/platform/weborigin/kurl.cc
index 1975c0804b..6773d4629c 100644
--- a/third_party/blink/renderer/platform/weborigin/kurl.cc
+++ b/third_party/blink/renderer/platform/weborigin/kurl.cc
@@ -883,7 +883,7 @@ bool ProtocolIs(const StringView& url, const char* protocol) {
   if (url.Is8Bit()) {
     return url::FindAndCompareScheme(AsUrlChar8Subtle(url), protocol, nullptr);
   }
-  return url::FindAndCompareScheme(std::u16string_view(url.Span16()), protocol,
+  return url::FindAndCompareScheme(std::u16string_view(base::as_string_view(url.Span16())), protocol,
                                    nullptr);
 }
 
@@ -917,7 +917,7 @@ void KURL::Init(const KURL& base,
     is_valid_ = url::ResolveRelative(
         base_utf8.AsStringView(), base.parsed_,
         relative.IsNull() ? std::u16string_view()
-                          : std::u16string_view(relative.Span16()),
+                          : std::u16string_view(base::as_string_view(relative.Span16())),
         charset_converter, &output, &parsed_);
   }
 
diff --git a/third_party/blink/renderer/platform/weborigin/security_origin.cc b/third_party/blink/renderer/platform/weborigin/security_origin.cc
index 502177d1d8..2167d41db5 100644
--- a/third_party/blink/renderer/platform/weborigin/security_origin.cc
+++ b/third_party/blink/renderer/platform/weborigin/security_origin.cc
@@ -30,11 +30,11 @@
 
 #include <stdint.h>
 
-#include <algorithm>
 #include <memory>
 #include <string>
 #include <utility>
 
+#include "base/containers/contains.h"
 #include "net/base/url_util.h"
 #include "services/network/public/cpp/is_potentially_trustworthy.h"
 #include "third_party/blink/public/common/features.h"
@@ -118,8 +118,8 @@ static bool ShouldTreatAsOpaqueOrigin(const KURL& url) {
             relevant_url.ProtocolIs("ftp")) &&
            relevant_url.Host().empty()));
 
-  if (std::ranges::contains(url::GetNoAccessSchemes(),
-                            relevant_url.Protocol().Ascii()))
+  if (base::Contains(url::GetNoAccessSchemes(),
+                     relevant_url.Protocol().Ascii()))
     return true;
 
   // Nonstandard schemes and unregistered schemes are placed in opaque origins.
@@ -127,8 +127,7 @@ static bool ShouldTreatAsOpaqueOrigin(const KURL& url) {
     // A temporary exception is made for non-standard local schemes.
     // TODO: Migrate "content:" and "externalfile:" to be standard schemes, and
     // remove the local scheme exception.
-    if (std::ranges::contains(url::GetLocalSchemes(),
-                              relevant_url.Protocol().Ascii()))
+    if (base::Contains(url::GetLocalSchemes(), relevant_url.Protocol().Ascii()))
       return false;
 
     // Otherwise, treat non-standard origins as opaque, unless the Android
@@ -443,7 +442,7 @@ bool SecurityOrigin::CanDisplay(const KURL& url) const {
            SecurityPolicy::IsOriginAccessToURLAllowed(this, url);
   }
 
-  if (std::ranges::contains(url::GetLocalSchemes(), protocol.Ascii())) {
+  if (base::Contains(url::GetLocalSchemes(), protocol.Ascii())) {
     return CanLoadLocalResources() ||
            SecurityPolicy::IsOriginAccessToURLAllowed(this, url);
   }
@@ -489,7 +488,7 @@ void SecurityOrigin::BlockLocalAccessFromLocalOrigin() {
 }
 
 bool SecurityOrigin::IsLocal() const {
-  return std::ranges::contains(url::GetLocalSchemes(), protocol_.Ascii());
+  return base::Contains(url::GetLocalSchemes(), protocol_.Ascii());
 }
 
 bool SecurityOrigin::IsLocalhost() const {
diff --git a/third_party/blink/renderer/platform/webrtc/convert_to_webrtc_video_frame_buffer.cc b/third_party/blink/renderer/platform/webrtc/convert_to_webrtc_video_frame_buffer.cc
index 5f984f26b5..bfb77a6e60 100644
--- a/third_party/blink/renderer/platform/webrtc/convert_to_webrtc_video_frame_buffer.cc
+++ b/third_party/blink/renderer/platform/webrtc/convert_to_webrtc_video_frame_buffer.cc
@@ -4,8 +4,7 @@
 
 #include "third_party/blink/renderer/platform/webrtc/convert_to_webrtc_video_frame_buffer.h"
 
-#include <algorithm>
-
+#include "base/containers/contains.h"
 #include "base/containers/span.h"
 #include "base/logging.h"
 #include "base/numerics/safe_conversions.h"
@@ -266,7 +265,7 @@ bool CanConvertToWebRtcVideoFrameBuffer(const media::VideoFrame* frame) {
   // Currently accept I420, I420A, NV12 formats in a mapped frame,
   // or a SharedImage-backed frame.
   return (frame->HasDirectCpuAccess() &&
-          std::ranges::contains(
+          base::Contains(
               GetPixelFormatsMappableToWebRtcVideoFrameBuffer(),
               frame->format())) ||
          frame->HasSharedImage();
diff --git a/third_party/blink/renderer/platform/webrtc/webrtc_video_frame_adapter.cc b/third_party/blink/renderer/platform/webrtc/webrtc_video_frame_adapter.cc
index a9689c7379..74d0dd4749 100644
--- a/third_party/blink/renderer/platform/webrtc/webrtc_video_frame_adapter.cc
+++ b/third_party/blink/renderer/platform/webrtc/webrtc_video_frame_adapter.cc
@@ -563,8 +563,7 @@ webrtc::scoped_refptr<webrtc::VideoFrameBuffer>
 WebRtcVideoFrameAdapter::ScaledBuffer::GetMappedFrameBuffer(
     std::span<webrtc::VideoFrameBuffer::Type> types) {
   auto frame_buffer = parent_->GetOrCreateFrameBufferForSize(size_);
-  return std::ranges::contains(types, frame_buffer->type()) ? frame_buffer
-                                                            : nullptr;
+  return base::Contains(types, frame_buffer->type()) ? frame_buffer : nullptr;
 }
 
 webrtc::scoped_refptr<webrtc::VideoFrameBuffer>
@@ -621,8 +620,7 @@ webrtc::scoped_refptr<webrtc::VideoFrameBuffer>
 WebRtcVideoFrameAdapter::GetMappedFrameBuffer(
     std::span<webrtc::VideoFrameBuffer::Type> types) {
   auto frame_buffer = GetOrCreateFrameBufferForSize(full_size_);
-  return std::ranges::contains(types, frame_buffer->type()) ? frame_buffer
-                                                            : nullptr;
+  return base::Contains(types, frame_buffer->type()) ? frame_buffer : nullptr;
 }
 
 // Soft-applies cropping and scaling. The result is a ScaledBuffer.
diff --git a/third_party/blink/renderer/platform/wtf/text/text_codec_utf16.cc b/third_party/blink/renderer/platform/wtf/text/text_codec_utf16.cc
index 5021b7bbc6..364928b2a2 100644
--- a/third_party/blink/renderer/platform/wtf/text/text_codec_utf16.cc
+++ b/third_party/blink/renderer/platform/wtf/text/text_codec_utf16.cc
@@ -27,7 +27,6 @@
 
 #include <unicode/utf16.h>
 
-#include <bit>
 #include <memory>
 
 #include "base/numerics/byte_conversions.h"
@@ -133,7 +132,7 @@ String TextCodecUtf16::Decode(base::span<const uint8_t> bytes,
     UChar c = lead_byte_ | (in_span.take_first_elem() << 8);
 
     if (!little_endian_) {
-      c = std::byteswap(c);
+      c = base::ByteSwap(c);
     }
 
     have_lead_byte_ = false;
@@ -144,7 +143,7 @@ String TextCodecUtf16::Decode(base::span<const uint8_t> bytes,
   for (wtf_size_t i = 0; i < num_chars_in; ++i) {
     UChar c = base::U16FromLittleEndian(in_span.take_first<2ul>());
     if (!little_endian_) {
-      c = std::byteswap(c);
+      c = base::ByteSwap(c);
     }
     decode(c);
   }
diff --git a/third_party/blink/renderer/core/css/parser/css_variable_parser.cc b/third_party/blink/renderer/core/css/parser/css_variable_parser.cc
index f44b82fa73..630b56bea8 100644
--- a/third_party/blink/renderer/core/css/parser/css_variable_parser.cc
+++ b/third_party/blink/renderer/core/css/parser/css_variable_parser.cc
@@ -8,6 +8,7 @@
 #include <optional>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/core/css/css_attr_type.h"
 #include "third_party/blink/renderer/core/css/css_syntax_component.h"
 #include "third_party/blink/renderer/core/css/css_syntax_definition.h"
diff --git a/third_party/blink/renderer/core/html/menu_safe_triangle.cc b/third_party/blink/renderer/core/html/menu_safe_triangle.cc
index 61eff1339d..d649de2b7b 100644
--- a/third_party/blink/renderer/core/html/menu_safe_triangle.cc
+++ b/third_party/blink/renderer/core/html/menu_safe_triangle.cc
@@ -7,6 +7,7 @@
 #include <algorithm>
 #include <array>
 
+#include "base/containers/contains.h"
 #include "third_party/blink/renderer/core/dom/document.h"
 #include "third_party/blink/renderer/core/dom/flat_tree_traversal.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
@@ -33,7 +34,7 @@ constexpr base::TimeDelta kSafeTriangleDuration = base::Milliseconds(1000);
 
 bool IsInclusiveFlatTreeAncestorOf(Node* possible_ancestor,
                                    Node* possible_descendant) {
-  return std::ranges::contains(
+  return base::Contains(
       FlatTreeTraversal::InclusiveAncestorsOf(*possible_descendant),
       *possible_ancestor);
 }
diff --git a/third_party/blink/renderer/modules/nfc/ndef_record.cc b/third_party/blink/renderer/modules/nfc/ndef_record.cc
index 6b84f070c9..128c2bd8af 100644
--- a/third_party/blink/renderer/modules/nfc/ndef_record.cc
+++ b/third_party/blink/renderer/modules/nfc/ndef_record.cc
@@ -7,6 +7,7 @@
 #include <string_view>
 
 #include "base/compiler_specific.h"
+#include "base/containers/contains.h"
 #include "base/notreached.h"
 #include "services/device/public/mojom/nfc.mojom-blink.h"
 #include "third_party/blink/renderer/bindings/core/v8/idl_types.h"
@@ -120,7 +121,7 @@ bool IsValidExternalType(const String& input) {
   for (wtf_size_t i = 0; i < type.length(); i++) {
     // SAFETY: index checked against length in loop body.
     if (!IsAsciiAlphanumeric(UNSAFE_BUFFERS(type[i])) &&
-        !kOtherCharsForCustomType.contains(UNSAFE_BUFFERS(type[i]))) {
+        !base::Contains(kOtherCharsForCustomType, UNSAFE_BUFFERS(type[i]))) {
       return false;
     }
   }
