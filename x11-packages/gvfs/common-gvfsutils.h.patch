--- a/common/gvfsutils.h
+++ b/common/gvfsutils.h
@@ -38,7 +38,7 @@
 {
   if (str != NULL)
     {
-      explicit_bzero (str, strlen (str));
+      memset (str, 0, strlen (str));
       g_free (str);
     }
 }
