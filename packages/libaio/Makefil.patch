--- a/Makefile
+++ b/Makefile
@@ -4,7 +4,7 @@
 TAG = $(NAME)-$(VERSION)
 RPMBUILD=$(shell `which rpmbuild >&/dev/null` && echo "rpmbuild" || echo "rpm")
 
-prefix=/usr
+prefix?=/usr
 includedir=$(prefix)/include
 libdir=$(prefix)/lib
