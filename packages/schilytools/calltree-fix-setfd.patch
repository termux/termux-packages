Appears to be a typo; best-guess change made here

--- a/calltree/calltree.c
+++ b/calltree/calltree.c
@@ -349,7 +349,7 @@ mkcpppipe(f, fname)
 
 	if ((i = vfork()) == 0) {	/* child */
 #ifdef	F_SETFD
-		fcntl(fileno(pfd[0]), F_SETFD, FD_CLOEXEC);
+		fcntl(fileno(fpp[0]), F_SETFD, FD_CLOEXEC);
 #endif
 		fexecve(Argv[0], f, fpp[1], stderr,
 			Argv, Env);
