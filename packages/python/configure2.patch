--- Python-3.7.0/configure	2018-06-26 22:07:35.000000000 -0500
+++ ./configure	2018-07-23 14:16:48.429922172 -0500
@@ -5647,6 +5647,7 @@
 $as_echo_n "checking for the Android API level... " >&6; }
 cat >> conftest.c <<EOF
 #ifdef __ANDROID__
+__ANDROID_API__ = __ANDROID__
 android_api = __ANDROID_API__
 arm_arch = __ARM_ARCH
 #else
@@ -5660,8 +5661,12 @@
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ANDROID_API_LEVEL" >&5
 $as_echo "$ANDROID_API_LEVEL" >&6; }
   if test -z "$ANDROID_API_LEVEL"; then
-    echo 'Fatal: you must define __ANDROID_API__'
-    exit 1
+    echo 'Attempting to set ANDROID_API_LEVEL'
+    ANDROID_API_LEVEL=1000
+    if test -z "$ANDROID_API_LEVEL"; then
+      echo 'Fatal: you must define __ANDROID_API__'
+      exit 1
+    fi
   fi
 
 cat >>confdefs.h <<_ACEOF
