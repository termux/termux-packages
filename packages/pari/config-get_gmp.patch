--- a/config/get_gmp
+++ b/config/get_gmp
@@ -41,7 +41,7 @@
 fi
 
 if test -r $exe; then
-  gmp_version=`env LD_LIBRARY_PATH="$LD_LIBRARY_PATH$dir_sep$gmp" $RUNTEST $exe`;
+  gmp_version=6.2.1;
 fi
 case "$gmp_version" in
   unsupported) gmp=
