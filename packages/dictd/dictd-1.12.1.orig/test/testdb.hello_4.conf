global {
   limit_queries     0
   limit_time        0
   limit_matches     0
   limit_definitions 0
}

database testdb {
   `index' "CURR_DIR/db.expect.hello_4.`index'"
   data  "CURR_DIR/db.expect.hello_4.dict"
}
