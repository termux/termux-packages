--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -49,8 +49,8 @@
 
 # Let's find our dependencies
 include(EthDependencies)
+include(fmtlib)
 if (NOT USE_SYSTEM_LIBRARIES)
-  include(fmtlib)
   include(nlohmann-json)
   include(range-v3)
 endif()
