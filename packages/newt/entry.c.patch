--- /home/david/termux/_tmp/newt-0.52.19/entry.c	2016-05-05 20:29:01.428682245 +0200
+++ ./entry.c	2016-05-05 20:28:55.890239182 +0200
@@ -339,3 +339,3 @@ static int previous_char(const char *buf
     while (off < pos) {
-       len = mblen(buf+off, MB_CUR_MAX);
+       len = mbtowc(0, buf+off, MB_CUR_MAX);
        if (len <= 0)
@@ -349,3 +349,3 @@ static int next_char(const char *buf, in
 {
-    int len = mblen(buf + pos, MB_CUR_MAX);
+    int len = mbtowc(0, buf + pos, MB_CUR_MAX);
     if (len <= 0)
