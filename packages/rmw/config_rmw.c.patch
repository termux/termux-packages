diff --git a/src/config_rmw.c b/src/config_rmw.c
index 95d2527..d22cf8c 100644
--- a/src/config_rmw.c
+++ b/src/config_rmw.c
@@ -49,6 +49,9 @@ print_config(FILE *restrict stream)
 # Add WASTE lines below only for special cases. Each waste folder goes\n\
 # on its own line, and a folder can use the $UID variable.\n"), stream);
   fputs(_("\n\
+# Improved Termux compatibility\n\
+WASTE = /storage/emulated/0/.Trash-$UID\n"), stream);
+  fputs(_("\n\
 # Keep rmw's files separate from the desktop trash:\n"), stream);
   fputs("\
 # WASTE = $HOME/.local/share/Waste\n", stream);
