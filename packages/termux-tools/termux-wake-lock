#!/bin/sh

if [ $# != 0 ]; then
	echo 'usage: termux-wake-lock'
	echo 'Acquire the Termux wake lock to prevent the CPU from sleeping.'
	exit 1
fi

am startservice \
	--user 0 \
	-a bin.mt.plus.service_wake_lock \
	bin.mt.plus/bin.mt.plus.app.TermuxService \
	> /dev/null
