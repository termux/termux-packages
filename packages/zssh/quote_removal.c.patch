--- a/quote_removal.c
+++ b/quote_removal.c
@@ -23,10 +23,8 @@
    return (1);
 }
 
-int		pc_remove_double_chr(str,i,chr)
-char		*str;
-int		*i;
-char		chr;
+int
+pc_remove_double_chr(char *str, int *i, char chr)
 {
    str_shift(str,*i,1);
    while (str[*i] && str[*i] != chr)
