# Disable backtrace

--- a/src/htsbacktrace.c
+++ b/src/htsbacktrace.c
@@ -47,7 +47,7 @@
 #ifdef HAVE_UNISTD_H
 #include <unistd.h>
 #endif
-#if (defined(__linux) && defined(HAVE_EXECINFO_H))
+#if (defined(__linux) && defined(HAVE_EXECINFO_H) && !defined(__ANDROID__))
 #include <dlfcn.h>
 #include <errno.h>
 #include <execinfo.h>
