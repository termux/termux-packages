--- a/autosetup/sqlite-config.tcl
+++ b/autosetup/sqlite-config.tcl
@@ -1619,7 +1619,7 @@ proc sqlite-handle-dll-basename {} {
     set dn [join [opt-val dll-basename] ""]
     if {$dn in {none default}} { set dn libsqlite3 }
   } else {
-    set dn libsqlite3
+    set dn libsqlcipher
   }
   if {$dn in {auto ""}} {
     switch -glob -- [get-define host] {
--- a/main.mk
+++ b/main.mk
@@ -1636,7 +1636,8 @@ install: install-lib
 # Install C header files
 #
 install-headers: sqlite3.h $(install-dir.include)
-	$(INSTALL.noexec) sqlite3.h "$(TOP)/src/sqlite3ext.h" "$(install-dir.include)"
+	$(INSTALL.noexec) sqlite3.h                 "$(install-dir.include)/sqlcipher.h"
+	$(INSTALL.noexec) "$(TOP)/src/sqlite3ext.h" "$(install-dir.include)/sqlcipherext.h"
 install: install-headers
 
 #
@@ -2210,7 +2211,7 @@ sqlite3d$(T.exe):	shell.c $(LIBOBJS0)
 		$(LDFLAGS.libsqlite3) $(LDFLAGS.readline)
 
 install-shell-0: sqlite3$(T.exe) $(install-dir.bin)
-	$(INSTALL) sqlite3$(T.exe) "$(install-dir.bin)"
+	$(INSTALL) sqlite3$(T.exe) "$(install-dir.bin)/sqlcipher"
 install-shell-1:
 install: install-shell-$(HAVE_WASI_SDK)
 
@@ -2251,14 +2252,15 @@ install-rsync: sqlite3_rsync$(T.exe) $(install-dir.bin)
 #install: install-rsync
 
 install-man1: $(install-dir.man1)
-	$(INSTALL.noexec) "$(TOP)/sqlite3.1" "$(install-dir.man1)"
+	$(INSTALL.noexec) "$(TOP)/sqlite3.1" "$(install-dir.man1)/sqlcipher.1"
 install: install-man1
 
 #
 # sqlite3.pc is typically generated by the configure script but could
 # conceivably be generated by hand.
 install-pc: sqlite3.pc $(install-dir.pkgconfig)
-	$(INSTALL.noexec) sqlite3.pc "$(install-dir.pkgconfig)"
+	sed -i 's/sqlite3/sqlcipher/g' sqlite3.pc
+	$(INSTALL.noexec) sqlite3.pc "$(install-dir.pkgconfig)/sqlcipher.pc"
 
 srcck1$(B.exe):	$(TOP)/tool/srcck1.c
 	$(B.cc) -o srcck1$(B.exe) $(TOP)/tool/srcck1.c
