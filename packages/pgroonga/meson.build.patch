--- a/meson.build
+++ b/meson.build
@@ -25,7 +25,7 @@
 if pg_config_path == ''
   pg_config_path = 'pg_config'
 endif
-pg_config = find_program(pg_config_path)
+pg_config = find_program(pg_config_path, '@TERMUX_PREFIX@/bin/pg_config')
 pg_includedir_server = run_command(
   pg_config, '--includedir-server', check: true
 ).stdout().strip()
