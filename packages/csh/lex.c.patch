--- a/lex.c
+++ b/lex.c
@@ -34,9 +34,9 @@
 #include <sys/ioctl.h>
 #include <termios.h>
 #include <errno.h>
-#include <stdlib.h>
-#include <string.h>
-#include <unistd.h>
+#include <bsd/stdlib.h>
+#include <bsd/string.h>
+#include <bsd/unistd.h>
 #include <stdarg.h>
 
 #include "csh.h"
@@ -53,7 +53,7 @@ static int	 getC1(int);
 static void	 getdol(void);
 static void	 getexcl(int);
 static struct Hist
-		*findev(Char *, bool);
+		*findev(Char *, int);
 static void	 setexclp(Char *);
 static int	 bgetc(void);
 static void	 bfree(void);
@@ -63,9 +63,9 @@ static int	 matchs(Char *, Char *);
 static int	 getsel(int *, int *, int);
 static struct wordent
 		*getsub(struct wordent *);
-static Char	*subword(Char *, int, bool *);
+static Char	*subword(Char *, int, int *);
 static struct wordent
-		*dosub(int, struct wordent *, bool);
+		*dosub(int, struct wordent *, int);
 
 /*
  * Peekc is a peek character for getC, peekread for readc.
@@ -116,7 +116,7 @@ static Char labuf[BUFSIZ];
  * when called by the alias routine to determine whether to keep the
  * argument list.
  */
-static bool hadhist = 0;
+static int hadhist = 0;
 
 /*
  * Avoid alias expansion recursion via \!#
@@ -223,7 +223,7 @@ word(void)
     Char c, c1;
     Char *wp;
     Char    wbuf[BUFSIZ];
-    bool dolflg;
+    int dolflg;
     int i;
 
     wp = wbuf;
@@ -402,7 +402,7 @@ getdol(void)
     Char    name[4 * MAXVARLEN + 1];
     int c;
     int     sc;
-    bool    special = 0, toolong;
+    int    special = 0, toolong;
 
     np = name, *np++ = '$';
     c = sc = getC(DOEXCL);
@@ -710,7 +710,7 @@ getsub(struct wordent *en)
     int     delim;
     int c;
     int     sc;
-    bool global;
+    int global;
     Char    orhsb[sizeof(rhsb) / sizeof(Char)];
 
     do {
@@ -833,10 +833,10 @@ getsub(struct wordent *en)
 }
 
 static struct wordent *
-dosub(int sc, struct wordent *en, bool global)
+dosub(int sc, struct wordent *en, int global)
 {
     struct wordent lexi;
-    bool    didsub = 0, didone = 0;
+    int    didsub = 0, didone = 0;
     struct wordent *hp = &lexi;
     struct wordent *wdp;
     int i = exclc;
@@ -884,7 +884,7 @@ dosub(int sc, struct wordent *en, bool global)
 }
 
 static Char *
-subword(Char *cp, int type, bool *adid)
+subword(Char *cp, int type, int *adid)
 {
     Char    wbuf[BUFSIZ];
     Char *wp, *mp, *np;
@@ -1016,7 +1016,7 @@ getsel(int *al, int *ar, int dol)
 {
     int c = getC(0);
     int i;
-    bool    first = *al < 0;
+    int    first = *al < 0;
 
     switch (c) {
 
@@ -1102,7 +1102,7 @@ gethent(int sc)
     Char *np;
     int c;
     int     event;
-    bool    back = 0;
+    int    back = 0;
 
     c = sc == HISTSUB ? HIST : getC(0);
     if (c == HIST) {
@@ -1213,7 +1213,7 @@ gethent(int sc)
 }
 
 static struct Hist *
-findev(Char *cp, bool anyarg)
+findev(Char *cp, int anyarg)
 {
     struct Hist *hp;
 
@@ -1277,7 +1277,7 @@ unreadc(int c)
 }
 
 int
-readc(bool wanteof)
+readc(int wanteof)
 {
     int c;
     static int sincereal;
@@ -1394,10 +1394,10 @@ reread:
 static int
 bgetc(void)
 {
-    int buf, off, c;
+    int buf, off, c, roomleft;
 
 #ifdef FILEC
-    int numleft = 0, roomleft;
+    int numleft = 0;
     Char    ttyline[BUFSIZ];
 #endif
     char    tbuf[BUFSIZ + 1];
@@ -1474,7 +1474,6 @@ again:
 		}
 #ifdef FILEC
 	    }
-#endif
 	    if (c >= 0)
 		break;
 	    if (errno == EWOULDBLOCK) {
@@ -1485,6 +1484,7 @@ again:
 	    else if (errno != EINTR)
 		break;
 	}
+#endif
 	if (c <= 0)
 	    return (-1);
 	feobp += c;
