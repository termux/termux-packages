#!/bin/bash

echo "Installing dependencies..."
if ! command -v node &> /dev/null
then
  echo "Node.js not found. Installing Node.js..."
  apt-get update && apt-get install -y nodejs npm
fi

echo "Installation complete. You can now use anton-remini."