#!/bin/bash

if dpkg-query -W -f='${Status}' anton-remini 2>/dev/null | grep -q "install ok installed"; then
    echo "anton-remini is already installed. Proceeding with upgrade."
else
    echo "Preparing to install anton-remini."
fi