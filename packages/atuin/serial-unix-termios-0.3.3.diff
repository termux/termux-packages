--- a/Cargo.toml
+++ b/Cargo.toml
@@ -14,5 +14,5 @@ categories = ["hardware-support", "os", "os::unix-apis"]
 [dependencies]
 serial-core = { version = "0.4", path = "../serial-core" }
 libc = "0.2.1"
-termios = "0.2.2"
+termios = "0.3.3"
 ioctl-rs = "0.1.5"
