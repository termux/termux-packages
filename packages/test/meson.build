project('test', 'c', 'cpp',
  version : '0',
  # meson_version : '>= 0.60',
  # default_options: ['c_std=c11', 'cpp_std=c++17']
  )

# subdir('src')

executable(
  'test0', files('test.c'),
  install: true,
  # install_dir: get_option('destdir') / 'bin'
)
