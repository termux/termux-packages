--- a/SConstruct
+++ b/SConstruct
@@ -529,7 +529,7 @@
     PREFIX=GetOption('prefix'),
     ENV = dict([ (key, os.environ[key])
                  for key in os.environ
-                 if key in ['PATH', 'TERM', 'HOME', 'PKG_CONFIG_PATH']
+                 if key in ['PATH', 'TERM', 'HOME', 'PKG_CONFIG_PATH', 'PKG_CONFIG_LIBDIR', 'PKG_CONFIG_SYSROOT_DIR']
               ])
 )
 
