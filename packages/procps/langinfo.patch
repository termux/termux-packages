--- a/src/ps/output.c
+++ b/src/ps/output.c
@@ -235,7 +235,7 @@ static int escape_str (char *dst, const char *src, int bufsize, int *maxcells) {
 
   if(utf_init==0){
      /* first call -- check if UTF stuff is usable */
-     char *enc = nl_langinfo(CODESET);
+     char *enc = "UTF-8";
      utf_init = enc && strcasecmp(enc, "UTF-8")==0 ? 1 : -1;
   }
   if (utf_init==1 && MB_CUR_MAX>1) {
--- a/library/escape.c
+++ b/library/escape.c
@@ -119,7 +119,7 @@ int escape_str (char *dst, const char *src, int bufsize) {
    int n;
 
    if (utf_sw == 0) {
-      char *enc = nl_langinfo(CODESET);
+      char *enc = "UTF-8";
       utf_sw = enc && strcasecmp(enc, "UTF-8") == 0 ? 1 : -1;
    }
    SECURE_ESCAPE_ARGS(dst, bufsize);
